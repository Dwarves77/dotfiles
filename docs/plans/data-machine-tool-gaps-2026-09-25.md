# Data-machine tool gaps - 2026-09-25 (R14 register)

Planning lane, read-only on code and DB. Answers R14 (operator, 2026-09-25, verbatim): "we are NOT
updating the data on the site, we are building the tools that manage that data first, that must be
complete before we do anything else." This register is TOOL GAPS ONLY - the machine that Collects,
Analyses and Produces the site's data - organised in loop order, each with acceptance, dependencies, and
whether closing it needs a live data write (held under R14).

Sources read (rule 11, no re-derivation): `docs/PROGRAM-BOARD.md` resume pointer + master thread table;
`docs/plans/complete-system-build-plan-2026-09-04.md` section 6 (M0-M9, 6.2, 6.3); `docs/plans/build-plan-2026-09-25.md`
section section 1-2, 4; `docs/audits/supabase-integrity-and-wiring-audit-2026-09-25.md` (17 findings); `fsi-app/.discipline/governance/loop-manifest.mjs`
+ `loop-hops.d/*.json` (11 hops) + `F50-loop-wiring.mjs`; `docs/specs/08-flywheel-design.md` section 0, section 6; ADR-023,
ADR-024. Every row below carries a rule-14 status token.

**On the dispatch brief's "decision propagation is designed only":** the coordinator flagged that this
contradicts a standing vault correction and asked for live verification or a `[HYPOTHESIS]` label. Ran a
live, read-only count against project `kwrsbpiseruzbfwjpvsp` (method named, per rule 15): `select count(*)
from entities` = 2,880; `derived_values` = 22; `propagation_events` = 2,782; `derivation_edges` = 24;
`statutory_computations` = 0; `estimated_values` = 0. `[CONFIRMED - execute_sql, live counts above]`: the
`entities` spine and the outbox/DAG schema (migrations 282-287) are real and hold live rows, not zero
tables. But the 22 `derived_values` rows trace to `scripts/propagation/seed-derived-values.mjs` (a
manually-dispatched seed script per spec 08 section 6's own description), and F50's own hops 07/08
(`downstream-chain-to-propagation-drain`, `data-producers-to-propagation-drain`) both read
`enforceFired:false` - no artifact anywhere carries a `workflow_run`-triggered propagation-drain run. So
the precise, evidenced state is: **schema shipped and populated by hand-dispatch; the autonomous
loop-driven firing spec 08 section 0 describes ("a fact changes -> every derived value... recomputed")
has not yet fired once from the loop.** Two genuinely designed-only pieces remain: the **per-kind
attribute tables** (corridor/obligation/signpost, section 1.2) and the **research assessment model**
(build-plan-2026-09-25 decision 4, gated behind the four-question structure). The register below states
this precisely rather than as a flat "SHIPPED" or a flat "designed only."

---

## Collect

| Tool | State | Evidence | "Complete" means | Dependencies | Live-write? |
|---|---|---|---|---|---|
| Source-sweep → fetch-drain hop | **built+wired, MANIFEST STALE** `[CONFIRMED - direct file read]` | `.github/workflows/fetch-drain.yml` lines 58-59 carry `workflow_run: workflows: ["Source sweep"]` live on master; `loop-hops.d/01-sweep-to-fetch-drain.json` still says `enforceEdge:false, consumerPending:true` - the manifest itself has drifted stale against a landed lane (M1 shipped after the hop file was last edited) | Update `01-sweep-to-fetch-drain.json` to `enforceEdge:true, consumerPending:false`; F50 re-run shows 10 hops not-enforced-for-firing, not 11; downstream trigger: F50's own count is the thing every later lane reads to know what's real, so a stale manifest silently understates readiness for every consumer of F50's output | none - mechanical edit | No - editing a JSON manifest file, not data |
| Fetch-drain firing proof | built-not-fired `[CONFIRMED - F50 hop 01, enforceFired:false]` | same hop file | An artifact in `scripts/harness-runs/fetch-drain/` carrying `trigger:"workflow_run"` (not `workflow_dispatch`); downstream trigger: proves the sweep→drain chain feeds `population-turn` without a human clicking twice | needs the source-sweep dispatch itself to fire (held under R14 - see below) | **Yes** - requires a real sweep dispatch |
| Sitemap/feed/research walker completeness (M8) | **partial** `[HYPOTHESIS - cited from build-plan section 6.1 M8, not re-run this lane]` | complete-system-build-plan section 6.1 row M8: "sitemap backfill never completed; feed walker failed once; research walker never ran" | Walked-hosts count reads 100% in `population-report.mjs`; feed-walker failure diagnosed/fixed or feed retired with reason; research walker has one recorded run; execution-wired per rule 15 into the M1 sweep-head cadence | M1 (sweep head) | **Yes** for the actual walk; the diagnosis of the one feed failure is not |
| `id-redirect-target-audit.mjs` (named in build-plan-2026-09-25 section 3, reuse-first item 4) | **PRESENT on current tree - the audit's RW-6 miss was a timing gap, not a missing script** `[CONFIRMED - git log --all -- '*id-redirect*': PR #798 "Lane REG-REDIRECT" (2026-09-25, author Dwarves77/coordinator merge, co-authored Claude Opus 5.5) created scripts/verify/id-redirect-target-audit.mjs, "live read-only data-audit, 0 violations over 2,766 items x 4 surfaces"; file confirmed present on this worktree]` | RW-6's own open question 1, resolved here per coordinator's ask for "one line of what you found" | Already exists and already ran clean (0 violations). The 2026-09-25 Supabase-audit lane's worktree branched before #798 merged, so its `git grep` predated the file - a branch-point timing artifact, not spec drift. No build needed; leaving the coordinator to close RW-6/open question 1 with this citation | none | No - already read-only, already run |
| `state_cost_facts` producer (sub-national Operations cost facts) | **RULED 2026-09-25: build the producer** `[CONFIRMED - operator ruling this session]` | readers = `src/app/api/ask/route.ts:245` and `src/lib/supabase-server.ts:3507`; last (and only) producer = none, ever - migration 152 (2026-07) created the table's read dependency with no producer script alongside it; live rows = 0 today; the table's own migration comment says a 0-row state renders PENDING by design, so the surface fails safe | Collect stage: sources through registry lookup, capture event/cost, grounding cost, harness record cost. Proven on fixtures first, no live rows until R14 lifts | Operator/coordinator ruling made; this lane does not execute, only records the decision | **Yes** - produces cost-fact rows; sequenced as step 4 in build order, held under R14 |

## Analyse

| Tool | State | Evidence | "Complete" means | Dependencies | Live-write? |
|---|---|---|---|---|---|
| Ledger-consume apply (M2) | built-not-fired `[CONFIRMED - F50 hop 02, enforceEdge:true, enforceFired:false]` | `loop-hops.d/02-sweep-to-ledger-consume.json`; `.github/workflows/ledger-consume.yml` | An artifact with `config.mode:"apply"`, `apply_disarmed:false`, `promoted>0`, `trigger:"workflow_run"`; downstream trigger: promoted `portal_link_candidates` feed `population-turn`/`corpus-turn` (hops 03/04) automatically | Hop 01 (sweep must run first) | **Yes** - promoting candidates is a data write; held under R14 |
| Quarantine disposition tool (`regen-quarantined.mjs`) | **built, not driving the invariant it owns** `[CONFIRMED - script exists at fsi-app/scripts/regen-quarantined.mjs; live dwell measured via execute_sql]` | audit finding RW-3: 78 live-quarantined items, ENQUEUE clean, DWELL violated on 66/78 (85%) past the 14-day bound with no disposition | `quarantine-disposition-audit.mjs` runs inside `run-data-audit-lane.mjs` in CI (execution-wired, rule 15) and goes red on any undispositioned past-bound item; `regen-quarantined.mjs` is dispatched on a cadence (or after M6's Gate A rescan) so the red count actually falls; downstream trigger: a disposed item re-enters mint or is archived-with-reason, not left in limbo | M6 (Gate A rescan invoker) | **Yes, explicitly held under R14** (coordinator ruling): dispatching `regen-quarantined.mjs` against the 66 live rows IS updating the site's data. Its tool-completion proof runs against fixtures, a rolled-back transaction, or a branch DB only until R14 lifts; the CI-wiring of the audit script itself (registering it, not running it against live rows) is not a data write |
| Gate A rescan invoker (M6) | built-not-fired `[CONFIRMED - F50 hops 10/11, enforceEdge:true, enforceFired:false]` | `.github/workflows/gate-a-rescan.yml`; `loop-hops.d/10-*.json`, `11-*.json` | An artifact from `workflow_run`; `distinct(gate_a_version)=1` read back; downstream trigger: closes the RW-3 loop above by dispositioning post-rescan quarantines | Hops 09 (brief-export) and population-turn already firing | **Yes** - rescanning writes `gate_a_version`/flags |
| Corpus-turn / downstream-chain / propagation-drain chain (M3) | built-not-fired `[CONFIRMED - F50 hops 03-08, enforceEdge:true, enforceFired:false]` | `loop-hops.d/03..08`; the six named workflow files all exist live at repo root | An artifact per hop carrying `trigger:"workflow_run"`; downstream trigger: newly minted items get discovery + forward-events + analysis inline, not as a separate later pass (rule 17) | Hop 02 firing | **Yes** - mint + propagation write rows |
| Dead-column audit | **ABSENT, decision-ready** `[CONFIRMED - DEAD-1, coverage check 4 not run at full-schema scope since migration 185, 2026-07]` | audit finding DEAD-1 | New `scripts/verify/dead-column-audit.mjs`: enumerate `information_schema.columns` for `public`, grep `<table>.<column>`/bare `<column>` across `src/`+`scripts/` excluding the creating migration, flag zero-hit columns; registered in `run-data-audit-lane.mjs`; execution-wired per rule 15 | F14 (model to extend) | Building/running the audit: no (SELECT + grep only). Acting on its findings (dropping columns): yes, via migration |
| Duplicate/parallel-table structural checker | **ABSENT, decision-ready** `[CONFIRMED - DUP-1, only manual comment-reading done this pass over ~100 tables]` | audit finding DUP-1 | An `F45`-shaped structural comparison over `information_schema` (column-set overlap, FK-target overlap) flags candidate pairs without a human re-reading every table comment; registered as a fitness function or data-audit member | F45 (model), DEAD-1 (same information_schema scan can share a pass) | No - read-only tool build |
| UI-side orphan checker (unwired UI parts, coverage check 5) | **ABSENT** `[CONFIRMED - audit's own line: "does not exist yet and was not built in this discovery pass"]` | audit coverage check 5 closing note | A route/component AST scan cross-referenced against schema-producer coverage (the "UI-side mirror of F14"); flags a rendered field with zero producer anywhere in the schema; registered next to F14 | F14 (producer-orphan model) | No - static analysis, no DB write |
| Harness-family schedule-vs-dispatch-history walker | **ABSENT beyond a 1-family sample** `[CONFIRMED - audit PROD-2: only top-level workflow `cron:` grep + one sampled family (`connection_theme_runs`) done; not walked at full harness-family grain]` | audit finding PROD-2 | Walks every `scripts/harness-runs/*/family.json` against its declared schedule/`governing_files` and last-run artifact timestamp - the F50 "every hop fires" pattern extended past the 11 named loop hops to every harness family; execution-wired | F50 (pattern to extend) | No - read-only |
| Reuse-script execution environment (node_modules gap) | **NOT an open question - answered by contract** `[CONFIRMED - docs/dispatches/lane-common-contract.md "Where you work": "fsi-app/node_modules is a symlink to a shared install; do not npm install anything"]` | audit Environment note + Reused-mechanisms table; lane-common-contract.md line ~40 | The container-based lane contract already answers this: every lane worktree gets the shared install via symlink/junction, never `npm install`. The 2026-09-25 audit's own worktree (`.claude/worktrees/supabase-audit-0925`) simply predates or missed that junction on this Windows checkout - not a standing policy gap, a one-off setup miss. This lane's own worktree carries the junction per the dispatch instructions | none - contract already exists, apply it | No |

## Produce

| Tool | State | Evidence | "Complete" means | Dependencies | Live-write? |
|---|---|---|---|---|---|
| Market-series producer edges (M5) | **built+wired, RESOLVED KEEP** `[CONFIRMED - build-plan-2026-09-25 section 2 integration-table row, coordinator's own re-verification]` | Three producers under `scripts/producers/market/*` feed the Market Intel ribbon, `MarketSeriesBoard`, `published_price_statistics`; `edges_authored>0` assertion in the producer harness artifact | Already met for the three implemented producers; `eex-eua` has a registry entry but no producer file - that gap is workstream 11's input, not M5's | none further for M5 itself | N/A - already producing |
| Brief-export / brief-apply chain (M4) | built-not-fired `[CONFIRMED - F50 hop 09, enforceEdge:true, enforceFired:false]` | `.github/workflows/brief-export.yml`, `brief-apply.yml`; `loop-hops.d/09-*.json` | An export artifact from `workflow_run`; a dry-run artifact from a batch-file merge; downstream trigger: apply on dispatch only (never on merge, per handoff rule 4) | Hop 03/04 (population-turn/corpus-turn firing) | **Yes** for brief-apply's apply mode; the dry/export half is not (writes a batch skeleton file, not the DB) |
| Structured-action extraction (build-plan-2026-09-25 workstream 6, merged with M4) | **ABSENT** `[CONFIRMED - build-plan section 1 item 6: "items with 'do now' prose but empty recommended_actions"]` | build-plan-2026-09-25 section 1.6, section 2 (M4 row: "MERGE INTO workstream 6") | The `record-briefs` write site extracts structured `recommended_actions` from prose at generation time, not left prose-only; class fix (remediation-discipline: class over instance), not a per-item patch; coordinator sign-off required on any change to the single write site | M4's brief runtime | **Yes** - writes `recommended_actions`; sequenced lane 7 in build-plan-2026-09-25 section 4, needs coordinator sign-off before the write-site change even as a tool build |
| Statutory writer input path (M7) | **partial** `[HYPOTHESIS - cited from build-plan section 6.1 M7, not re-verified this lane]` | complete-system-build-plan section 6.1 row M7: fixture path → admin-uploaded schema-validated rows-file (spec-09 upload flow reused) | `estimated_values` gets its first real writer for W4's one named case, or W4 is amended to retire the table (no third state); upload route reused, one home | spec-09 upload flow; M7's Masthead/ListRow parts (sequenced after #800 look pass per build-plan-2026-09-25 section 2) | **Yes** for the actual upload/write; the upload-route wiring itself is a code change |
| ETS-proxy carbon-cost producer (workstream 11) | **NOT BUILT - sequenced, gated on M5 conflict resolution** `[CONFIRMED - build-plan-2026-09-25 section 1.11, section 4 lane 9]` | Decisions 1/2 (2026-09-24/25): no freight-rate tracking; carbon cost per container/tonne from carrier-published ETS surcharges, per carrier/period/source, never blended without a range, client-override labelled | A producer under `scripts/producers/market/*` emits the carbon-cost series with source/period/carrier provenance on every row, feeding the Market Intel differentiator (decision 1) | Coordinator must resolve the M5-conflict note (build-plan section 2, M5 row) before this lane touches `scripts/producers/market/*.mjs` | **Yes** - new producer writing new rows; the coordinator resolution and the code itself are not |
| Loop-manifest hop-file self-healing on lane landing | **ABSENT - process gap, not a script** `[CONFIRMED - same evidence as the fetch-drain stale-manifest finding above]` | this register's Collect section, row 1 | A lane that lands a `workflow_run` edge or a fired artifact updates its own `loop-hops.d/*.json` in the SAME commit (like the session-log/INDEX self-annealing protocol); F50 stops being the only thing that would ever catch drift, because right now nothing catches a hop file that was correct when written and is now stale | none - a lane-checklist addition | No |

## Connect / flywheel

| Tool | State | Evidence | "Complete" means | Dependencies | Live-write? |
|---|---|---|---|---|---|
| Loop A - discovery compounding (themes, pivots, cross-surface convergences) | **built** `[HYPOTHESIS - cited from INDEX/flywheel-build-plan-2026-08-10 and ADR-025, not re-verified this lane per rule 11]` | `docs/plans/flywheel-build-plan-2026-08-10.md` (U1-U9); ADR-025 (deterministic derivations auto-adopt) | Not this register's gap to re-litigate; flag only if a future audit finds it stopped firing | - | - |
| Loop B - decision propagation engine (outbox + invalidation DAG + governed drain) | **schema+runtime built and populated by hand-dispatch; autonomous loop-firing NOT YET PROVEN** `[CONFIRMED - live execute_sql against kwrsbpiseruzbfwjpvsp: entities=2880, derived_values=22, propagation_events=2782, derivation_edges=24, statutory_computations=0, estimated_values=0; cross-checked against F50 hops 07/08 both enforceFired:false]` | spec 08 section 6 table; this register's verification note above; live counts this session | The 22 `derived_values` rows came from `seed-derived-values.mjs` (manual dispatch), not from a `workflow_run`-triggered drain. "Complete" per spec 08 section 0's own claim ("a fact changes... every derived value... recomputed") means: hop 07/08 fire from real upstream events, an artifact in the `propagation` family carries `trigger:"workflow_run"`, and a live fixture-driven falsification-test run (spec 08 section 7 assertion 2: revise one factor, assert the transitive closure updates) passes without a human dispatching each step | Hops 07/08 firing (see Analyse/M3 chain row) | Proving it against real data: **Yes**, held under R14. Proving it on a fixture/branch DB: No, and preferred until R14 lifts |
| Per-kind attribute tables (corridor/obligation/signpost) | **DESIGNED ONLY** `[CONFIRMED - spec 08 section 1.2/section 6: "entities accepts kind='corridor'/'obligation'/'signpost' today with no attribute table yet"]` | spec 08 section 6 row | Attribute tables built per section 1.2's design, migrated additively beside `entities`, backfilled the same way `instrument`/`organisation` kinds were (`backfill-entities.mjs`) | entities spine (already shipped) | **Yes** - new tables + backfill rows |
| `derivation_edges` RLS gap | **CLOSED** `[CONFIRMED - migration 330, merged in PR #805 (7d4b3f32), live adversarial attack 8/8 denied]` | commits `e4d6923c`/`7d4b3f32` "Fix SEC-1: enable RLS on derivation_edges, revoke anon/authenticated CRUD"; live counts this session show `derivation_edges`=24 rows still present and readable to the service role, consistent with a working RLS policy rather than data loss | Met. RLS enabled, anon/authenticated CRUD revoked, adversarial proof run and passed 8/8 (attack, not presence, per rule 15) | none | No further action - closed, not carried into the build order |

## Harness record

| Tool | State | Evidence | "Complete" means | Dependencies | Live-write? |
|---|---|---|---|---|---|
| F50 loop-wiring gate | **built+wired, PASS, reports 11 hops not-yet-enforced-for-firing** `[CONFIRMED - direct run pattern read from F50-loop-wiring.mjs + loop-hops.d, matches build-plan's own count]` | `fsi-app/.discipline/fitness/functions/F50-loop-wiring.mjs`; 11 hop files, all `enforceFired:false`; one (hop 01) also stale on `enforceEdge` per this register's own finding | `enforceFired:true` on all 11 once the proof run (complete-system-build-plan section 6.2) fires each hop for real from its upstream, not from a person; hop 01's `enforceEdge`/`consumerPending` corrected first | Every M-lane hop above firing at least once | **Yes**, transitively - F50 can only go green by actually running the machine once |
| Dispatch ledger (M9) | **partial - hand-appended today, spec wants a workflow-appended writer** `[HYPOTHESIS - cited from complete-system-build-plan section 6.1 M9, not re-verified this lane]` | M9 row: "the dispatch ledger appended by a workflow step, never by hand" | `docs/ops/dispatch-ledger.jsonl` gains a row from a workflow step on every dispatch, not from a coordinator editing the file; execution-wired per rule 15 | M9's other components (loop-manifest, F50) already exist | No - the writer is a code/workflow change; rows it appends are operational metadata, not customer data |
| Maintenance-family committed artifacts (M9) | **partial** `[HYPOTHESIS - cited from M9 row, not re-verified]` | M9 row: "maintenance.yml commits its artifacts like the other families" | Every `maintenance.yml` dispatch step writes a harness-run artifact under its own family, same convention as `fetch-drain`/`brief-export`/etc. | none | No - artifact-writing convention, not data |
| Community promotion mechanism A retirement (M9) | **KEEP-not-done** `[HYPOTHESIS - cited from M9 row: "mechanism A (community_promotion_transitions, 0 rows, no importer) retired by migration and its module deleted"]` | M9 row | Migration drops the dead table; module deleted; mechanism B (kept) is the sole path | none | **Yes** - a DROP TABLE migration, but on a 0-row dead table, not live customer data |
| Proof run itself (complete-system-build-plan section 6.2) | **NOT STARTED - the single event that would turn most "built-not-fired" rows above green** `[CONFIRMED - section 6.2 text: "Nothing in 6.3 starts before this passes"]` | complete-system-build-plan section 6.2 | One dispatch at the head (`source-sweep.yml`, scope `changed`, one `loop_run_id`); an artifact at every hop carrying that run id and `trigger:"workflow_run"`; F50 green; population report shows the run's items minted/enriched/exported/owed-a-brief | Every M1-M6, M9 hop's edge landed (9 of 11 are; 2 pending per this register) | **Yes, explicitly** - this is THE held item under R14: the proof run's whole point is to move real data through the loop once. R14 holds it until every tool above it in this table is closed |

---

## Proposed build order (dependency-driven)

SEC-1 (`derivation_edges` RLS) is CLOSED (migration 330, PR #805) and is not carried below.

1. **Fix the stale loop-manifest hop (hop 01)** - zero-cost, unblocks an honest F50 count for every later decision.
2. **Dead-column audit + duplicate-table structural checker** - both are read-only, both share one `information_schema` scan, both close audit-register coverage gaps 2 and 4 outright with no data risk.
3. **UI-side orphan checker + harness-family schedule walker** - read-only, extends F14/F50 patterns already proven; closes coverage gaps 5 and 6.
4. **`state_cost_facts` producer build** - collects sub-national cost facts (sources through registry, capture event/cost, grounding cost, harness record cost); proven on fixtures first, held under R14 for live dispatch. Unblocks one Collect-stage row. (`id-redirect-target-audit.mjs` is resolved this pass - see Collect table - and drops out of the order.)
5. **Quarantine disposition dispatch (`regen-quarantined.mjs` against the 66 dwell-violating rows)** - the one Analyse-stage action item that is a live data write held by R14; its tool-completion is proved on fixtures/rollback/a branch DB only until R14 lifts, then it rides the proof run below rather than running standalone.
6. **The proof run (complete-system-build-plan section 6.2)** - gates on 1-3 above; this is the one row in the whole register that turns "built-not-fired" into "fired" (including Loop B's own autonomous-firing proof), and R14 holds it explicitly until the tool gaps above are closed.
7. **Structured-action extraction, ETS-proxy producer, statutory writer, attribute tables** - all NOT BUILT, all sequenced after coordinator decisions this register cannot make (write-site sign-off, spec-09 upload reuse). ETS-proxy's M5 conflict is RESOLVED (KEEP, #804) and no longer blocks it.

## First 2 lanes to dispatch

1. **Lane TOOL-GAP-1 (Haiku, mechanical):** fix `loop-hops.d/01-sweep-to-fetch-drain.json` (enforceEdge:true, consumerPending:false), re-run F50, confirm the new count.
2. **Lane TOOL-GAP-2 (Sonnet, judgment):** build `dead-column-audit.mjs` (information_schema column enumeration + read/write grep, DEAD-1's spec is decision-ready) and the duplicate/parallel-table structural checker (DUP-1, F45-shaped), sharing one `information_schema` scan; register both in `run-data-audit-lane.mjs`. Both are read-only, need no coordinator decision, and close two full audit-register coverage gaps outright.

(SEC-1 dropped from both build order and first-2 lanes - closed per coordinator ruling, migration 330/#805.)

## Open questions for the coordinator or operator (not decided here)

1. RW-6: resolved this pass - `id-redirect-target-audit.mjs` exists (PR #798, 2026-09-25); the 2026-09-25 Supabase-audit lane's worktree branched before that PR merged, so its search missed a file that was already there. No further question; left for the coordinator to close RW-6 with this citation.
2. RW-2/UI-3: **RULED 2026-09-25: build the producer (collect stage)** - sources through registry lookup, capture event/cost, grounding cost, harness record cost. Proven on fixtures first; no live rows until R14 lifts. Build order step 4 replaces the prior "operator ruling" step.
3. Does R14 ("tools before data") hold the `regen-quarantined.mjs` dispatch as a data write? Coordinator ruling received: **yes, explicitly** - stated as such in the Analyse table row and the build order above; no longer open.
4. node_modules for lane worktrees: **not an open question** - `docs/dispatches/lane-common-contract.md` already specifies the shared-install junction/symlink; the 2026-09-25 audit worktree's gap was a one-off setup miss, not a policy question. Closed.
5. SEC-1 remediation-lane priority: **closed** - migration 330 landed in #805 before this question needed an answer.
6. The M5/workstream-11 conflict: **resolved** - KEEP (#804, build-plan-2026-09-25 section 2); no longer blocks the ETS-proxy producer lane.

---

*Landing note: worktree `.claude/worktrees/data-machine-gaps`, branch `coord/data-machine-tool-gaps`, off
`origin/master` (7d4b3f32). `node_modules` linked via junction per dispatch instructions - this closes
open question 4 above for THIS lane's own reads (all reuse-first tool inspection here was direct file/JSON
reads, no DB writes, no CLI execution needed).*
