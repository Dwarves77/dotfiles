> **Superseded as a tracker on 2026-09-04** by `docs/plans/complete-system-build-plan-2026-09-04.md` (definition of done §0; the board is the only tracker). Kept as history.

# Wave 3 lanes — write sets, pairwise disjoint (2026-09-03)

Operator rulings 2026-09-03: **R-H yes** (Community: build all the differentiators) and **R-G yes**
(spec-09 domain tables, sequenced after corridor identity, which is now live: `entities.kind='corridor'`,
seeded 2026-09-03). Build before populate stays in force: no population dispatch until this wave lands.

Base: `origin/master` at launch (ec6b1b85 or later). Worktrees `/root/work/lanes/<lane>`, branches
`lane/<lane>-2026-09-03`. Lane contract: `docs/plans/wave2-lanes-2026-09-02.md` §"Lane contract
additions" applies verbatim (F34, route files export only handlers, `next build` proof for page-graph
changes, migration numbers assigned here).

## Three lanes

### COMMUNITY-A — the guard and the data layer (spec 05 §1–§4, required components 1, 3, 4, 5, 6, 7, 12)
Write set: `fsi-app/supabase/migrations/293_community_identity_and_guard.sql`,
`294_community_aggregate_instruments.sql`, `295_community_promotion_machine.sql` (use only what is
needed; each self-checking, RLS, header in the 288–290 style); `fsi-app/src/lib/community/**` (new: the
antitrust write-time guard with refusal explanation and the aggregate-only route; k-anonymity and 25%
dominance and three-month lag as pure functions; verified-pseudonymous identity projection; corroboration
counter (distinct organisations); promotion state machine with logged transitions; evidence time-decay);
`fsi-app/src/app/api/community/**` (route handlers only; pure logic in `src/lib/community`);
`fsi-app/scripts/community/**` (new: the house-seeded recurring benchmark, dry by default, calendar-driven,
scoped to sector_profile; seeds only aggregate instruments, never posts pretending to be members); tests.
Read first, in full: `docs/specs/05-community.md`, `docs/specs/00-foundation-the-spine.md` §3.6
(`origin_class`), migrations 028–030 and every later `community_*` migration, the existing
`src/app/api/community/**`, `src/components/community/types.ts` (READ-ONLY; COMMUNITY-B owns it), and
`docs/decisions/` for anything naming community, antitrust, k-anonymity, promotion.
Interface contract with COMMUNITY-B (both lanes build to this; the coordinator reconciles at integration):
`src/lib/community/index.mjs` exports `evaluateAntitrustGuard(post) → { allowed, reason, aggregateRoute }`,
`projectAuthorIdentity(profile) → { orgType, role, sector, region, verified }`,
`corroborationCount(thread) → { organisations, posts }`, `promotionState(thread) → { state, transitions[] }`,
`evidenceAge(evidence, now) → { ageDays, weight, chip }`, and the API routes
`POST /api/community/posts` (guard-enforced), `GET /api/community/threads/[id]/corroboration`,
`GET /api/community/entities/[entityId]/threads` (entity-bound discovery),
`GET /api/community/benchmarks/current`. Acceptance criteria spec 05 §6 items 1–3, 5–9 are yours to prove
with tests (no DM path exists; every thread binds to ≥1 spine entity; refusal at write time).

### COMMUNITY-B — the surface (required components 2, 9, 10, 11 and the cross-surface differentiators)
Write set: `fsi-app/src/components/community/**` (including `types.ts`); `fsi-app/src/app/community/**`;
the cross-surface "peers are discussing this" strip on DETAIL pages only:
`fsi-app/src/app/regulations/[slug]/**`, `fsi-app/src/app/market/[slug]/**`, `fsi-app/src/app/research/[slug]/**`
(insertion of one component each; if a detail route has a different folder name, use it, and name it);
`fsi-app/src/components/shared/PeersDiscussing*.tsx` (new); rendering-guard fixtures under
`fsi-app/.discipline/rendering/fixtures-community/**` (new dir; report the one registration line for
`fixtures.mjs`); tests. Build: entity-bound posting UI (bind to corridor / jurisdiction / instrument /
technology / organisation from the spine; read `src/lib/entities/**`), peer-org context on posts and the
author identity rendering (org type + role + sector + region, never name or company), cross-group topic
discovery, peer-org directory (aggregate, pseudonymous), region and sector group structure seeded from
`sector_profile` (the seeding itself is a script in COMMUNITY-A's `scripts/community/**`; you render what
exists), editorial pickup surface (an editor surfaces a public thread inside platform intelligence with
the gate-4 provenance treatment; the `origin_class` label stays visible in that context), topic follow and
digest. **No direct messaging anywhere** (spec 05 §2, acceptance 5): the finish plan's "trusted-peer DM"
is superseded by the spec; do not build it, say so on the surface where a reader would look for it.
Consume COMMUNITY-A's API contract above; where A's route is not present in your worktree, build against
the contract with a typed client in `src/components/community/api-client.ts` and fixtures.

### SPEC-09 — the eight domain tables (spec 09 §1.1–§1.8), corridor-keyed
Write set: `fsi-app/supabase/migrations/296_spec09_market_tables.sql` (§1.1 OEM roadmap, §1.2 surcharge
audit + FuelEU pooling, §1.3 indexation, §1.7 rerouting multipliers), `297_spec09_operations_tables.sql`
(§1.4 DQI and primary-data share, §1.5 auxiliary energy, §1.6 grid queue), `298_spec09_regulations_tables.sql`
(§1.8 EUDR geo-traceability and book-and-claim custody); `fsi-app/src/lib/spec09/**` (new: pure
calculators, statutory/estimate isolation per spec 08 §3 and migration 286's purity rule; every derived
value labelled; `M` where the source discloses nothing); `fsi-app/scripts/spec09/**` (new: producers dry
by default; $0 sources only, named per table; where no free source exists the table ships empty with the
gap named); `fsi-app/src/components/market/SurchargeAudit*.tsx`, `OemRoadmap*.tsx`, `Rerouting*.tsx`;
`fsi-app/src/components/operations/Dqi*.tsx`, `AuxiliaryEnergy*.tsx`, `GridQueue*.tsx`;
`fsi-app/src/components/regulations/EudrCustody*.tsx`; INDEX pages only: `fsi-app/src/app/market/page.tsx`,
`fsi-app/src/app/operations/page.tsx`, `fsi-app/src/app/regulations/page.tsx` (one block insertion each;
the detail pages are COMMUNITY-B's); tests. Corridor key: `entities` rows with `kind='corridor'`
(`cl:corridor:<hash>`, canonical_name `ORIGIN-DEST:mode`; `src/lib/contracts/corridor-id.mjs` and
`src/lib/entities/**` read-only). Spec 09 §5's open operator decisions are taken with the spec's own
conservative defaults and labelled on the surface as such: pool-position inference held internally
(publish statutory variance only); indexation as mechanics and arithmetic, never drafted clause text;
OEM density `M` where only cell-level is disclosed; confidence floors unset (surface says "floor not yet
set"). Sequence inside the lane per spec 09 §4: surcharge audit first.

## Coordinator-only
Everything listed in `wave2-lanes-2026-09-02.md` "Coordinator-only", plus `.github/workflows/**` (no lane
adds a workflow this wave; report the dispatch step you need and the coordinator adds it to
`maintenance.yml` or `producers.yml`).

## Pairwise check
| Path | Lane |
|---|---|
| migrations 293–295, `src/lib/community/**`, `src/app/api/community/**`, `scripts/community/**` | COMMUNITY-A |
| `src/components/community/**`, `src/app/community/**`, `src/app/{regulations,market,research}/[slug]/**`, `src/components/shared/PeersDiscussing*`, `.discipline/rendering/fixtures-community/**` | COMMUNITY-B |
| migrations 296–298, `src/lib/spec09/**`, `scripts/spec09/**`, the named `src/components/{market,operations,regulations}/*` files, `src/app/{market,operations,regulations}/page.tsx` | SPEC-09 |

Detail pages vs index pages is the seam between COMMUNITY-B and SPEC-09; `src/components/regulations/`
is shared by name only (COMMUNITY-B writes nothing there; SPEC-09 writes only `EudrCustody*`).
