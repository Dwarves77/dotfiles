# Audit: build vs design, value-level (2026-09-07)

GENERATED FILE. Every table below is regenerated in full by
`node fsi-app/.discipline/rendering/audit/run-audit.mjs` (`npm run audit:design`) from the spec
JSON files in `fsi-app/.discipline/rendering/audit/spec/`. Do not hand-edit the generated
sections: edit the spec file and rerun. The hand-written source-level pass that seeded this
document is preserved verbatim at the end.

- Repo state: `working tree` (branch `working tree`)
- Run at: 2026-09-08T10:51:51.511Z
- Spec files run: 61 (absence, account-members, actionrow, admin-issues-rail, admin-stat-tiles, auth-frame, bandgradientrule, bandtile, chips, community-table, compose-01-dashboard, compose-02-regulations-list, compose-03-regulation-detail, compose-04-market-list, compose-05-market-detail, compose-06-research-list, compose-07-research-detail, compose-08-operations-list, compose-09-operations-profile, compose-10-map, compose-11-watchlist, compose-12-community, compose-13-admin, compose-14-account, compose-15-settings, compose-16-auth, compose-16-signup, compose-17-onboarding, detailheader, detailsection, detailtagrow, detailtimeline, factblocks, factcard, filterchipgroup, highrelevance, impactmeter, inthisliststat, list-surface-virtualized, list-surface, listrow, map-register, market-research-rows, masthead, milestonetimeline, onboarding-stepper, operations-matrix, page-frame, railcards, section-card-lists, sectionindex, settings-notifications, settings-section-index, sidebar, skeleton, statblock, statenote, summarydepthswitch, tabrow, tagpopover, watchbutton)
- Checks: 1193

## Method

Each spec file names a mount (`fsi-app/.discipline/rendering/audit/mounts.mjs`), which bundles the
REAL `src/components/**` module with esbuild and mounts it in Playwright chromium at the stated
viewport — the same machinery the rendering guard's smoke specs already use, so the audit
exercises the actual `.tsx` file rather than a reproduction of it. For every selector in the spec
the runner reads `getComputedStyle`, normalises both the design value and the measured value to
one canonical form (colours to `rgb()`/`rgba()`, lengths to px, `em` resolved against the
element's own font-size) and records both numbers on every row. `[CONFIRMED]`: every MATCH and
MISMATCH row below is a measurement taken this run, not a source read.

| Status | Meaning | Count |
|---|---|---|
| MATCH | measured value equals the design source value | 1193 |
| MISMATCH | measured value differs from the design source value | 0 |
| NOT BUILT | the spec's selector matched no element in the mounted tree | 0 |
| NOT IN SPEC | a forbidden element is present | 0 |

Everything not covered by a spec file in `spec/` is NOT AUDITED. The coverage list is
exactly the spec files named above; nothing else in the product has been measured by this run.

## Absence

- Spec file: `fsi-app/.discipline/rendering/audit/spec/absence.json`
- Mount: `absence` at 1440 px
- Artboards: 00-system-sheet §0.4 'Absence', 03-regulation-detail (Penalty rate row)
- Design source: docs/design/handoff-2026-09-06/README.md §0.4 'Absence'; 'Caros Ledge UI System.dc.html' the 'Penalty rate' fact-table row (dc.html line ~301: 'not in primary source'); operator ruling 2.1 which OVERRIDES the artboard's font-weight
- Result: 6 MATCH, 0 not matching

> Absence.tsx is pure inline style; every property here is a real product value.
> Ruling 2.1: 'Unscored = a 30px dashed baseline in place of the bars plus the small-caps absence reason from the fixed vocabulary (not in primary source / pending / unscored / connect data) at 10.5px uppercase/600 #7A6E6C.' The artboard's own markup states font-weight:700 for this element; the ruling states 600 and wins per the handoff's own precedence rule ('where a ruling states a number, the ruling wins'). font-weight below is therefore the RULING's value (600), not the artboard's (700) — Absence.tsx's 600 is checked as a MATCH against the ruling.
> 'small-caps' in both README prose and the ruling means the READER'S IMPRESSION (short, quiet, cap-like text), not the CSS `font-variant: small-caps` keyword specifically — the artboard's own markup achieves it with plain `text-transform:uppercase`, not font-variant. Absence.tsx uses `fontVariant: 'small-caps'` (a real CSS property distinct from text-transform), which computed-style reports under `font-variant` / `font-variant-caps`, not `text-transform` — checked below as a genuine divergence from the artboard's own technique, not a harness artefact.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| Absence | `font-size` | `10.5px` | `10.5px` | MATCH |
| Absence | `font-weight` | `600` | `600` | MATCH |
| Absence | `color` | `#7A6E6C` | `rgb(122, 110, 108)` | MATCH |
| Absence | `text-transform` | `uppercase` | `uppercase` | MATCH |
| Absence | `letter-spacing` | `0.08em (= 0.84px at 10.5px)` | `0.84px` | MATCH |
| reason text | `text` | `not in primary source` | `not in primary source` | MATCH |

## Account — Members & roles section (AccountCard + MembersPanel)

- Spec file: `fsi-app/.discipline/rendering/audit/spec/account-members.json`
- Mount: `account-members` at 1440 px
- Artboards: 14-account
- Design source: docs/design/handoff-2026-09-06/'Caros Ledge UI System.dc.html' id="p14" (char 577450), the 'Members & roles' section card; operator ruling 5.1 (2026-09-07)
- Result: 11 MATCH, 0 not matching

> dc.html p14 section-card shell is identical to p15's (see settings-notifications.json note 1): ruling 5.1 gradient rule `height:3px;background:linear-gradient(90deg,#5A5552,#5A5552 22%,rgba(90,85,82,.18))`, then a header row `padding:14px 16px 10px;border-bottom:1px solid rgba(0,0,0,.08)` with an Anton 20px title and a 10.5px/600/#7A6E6C meta line.
> Correction, train 58 (2026-09-07): AccountCard.tsx DOES render the shared `SectionRule` (`.cl-section-rule`, `src/components/account/AccountPrimitives.tsx` ~line 103, confirmed by direct source read this session) — the gradient-rule row below now MATCHES. This corrects the earlier note (also corrected in settings-notifications.json) that claimed no such element existed.
> dc.html p14 table header row: `grid-template-columns:1fr 120px 150px 44px;height:30px;font-size:9.5px;letter-spacing:.12em;text-transform:uppercase;color:#7A6E6C;font-weight:700;border-bottom:1px solid rgba(0,0,0,.08)`.
> dc.html p14 member row: same grid, `min-height:48px;border-bottom:1px solid rgba(0,0,0,.06);font-size:12.5px`.
> dc.html p14 role pill: `border:1px solid rgba(0,0,0,.25);border-radius:6px;padding:5px 8px;font-weight:600`.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| Account — Members & roles section (AccountCard + MembersPanel) | `background-color` | `#FFFFFF` | `rgb(255, 255, 255)` | MATCH |
| Account — Members & roles section (AccountCard + MembersPanel) | `border-width` | `1px` | `1px` | MATCH |
| Account — Members & roles section (AccountCard + MembersPanel) | `border-color` | `rgba(0,0,0,.12)` | `rgba(0, 0, 0, 0.12)` | MATCH |
| Account — Members & roles section (AccountCard + MembersPanel) | `border-radius` | `10px` | `10px` | MATCH |
| gradient rule (ruling 5.1) | `height` | `3px` | `3px` | MATCH |
| gradient rule (ruling 5.1) | `background-image` | `linear-gradient(90deg,#5A5552,#5A5552 22%,rgba(90,85,82,.18))` | `linear-gradient(90deg, rgb(90, 85, 82), rgb(90, 85, 82) 22%, rgba(90, 85, 82, 0.18))` | MATCH |
| title | `text` | `Members & roles` | `Members & roles` | MATCH |
| title | `font-family` | `contains:Anton` | `Anton, system-ui, sans-serif` | MATCH |
| title | `font-size` | `20px` | `20px` | MATCH |
| title | `letter-spacing` | `0.04em (= 0.8px at 20px)` | `0.8px` | MATCH |
| title | `text-transform` | `uppercase` | `uppercase` | MATCH |

## ActionRow (detail action row)

- Spec file: `fsi-app/.discipline/rendering/audit/spec/actionrow.json`
- Mount: `detail-shell` at 1440 px
- Artboards: 03-regulation-detail, 05-market-detail, 07-research-detail, 09-operations-profile, 00-system-sheet (Actions · stat block example)
- Design source: README.md §0.4 'Detail action row — always visible': 'Export brief (primary, ink) · Share · ☆ Watch · ＋ Tag, all four on the detail header, 8px gap, padding:8px 14px; radius 6, secondary = white with rgba(0,0,0,.25) border and #F5F2EE hover'; operator rulings 3.1 (remove the Immediate ▾ dropdown) and 3.2 (the action-row + Tag is the only trigger)
- Result: 19 MATCH, 0 not matching

> The dc.html #sys 'Actions · stat block' block (Export brief / Share / ☆ Watch example) carries the identical padding:8px 14px;border-radius:6px;font-size:12.5px values as a worked example of the same button chrome; README's own 8px gap / padding:8px 14px; radius 6 prose is used as the primary source line for this spec, per the harness convention of preferring the exact-numbers statement over the illustrative artboard block.
> This mount's `watch` slot is a plain ActionButton placeholder (variant=secondary), not the real WatchButton (out of this spec's reach: WatchButton owns its own persistence and is audited separately if a lane adds a spec for it) — sufficient to measure the ROW's own gap and the three buttons this component itself owns (Export, Share, + Tag).

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| ActionRow (detail action row) | `display` | `flex` | `flex` | MATCH |
| ActionRow (detail action row) | `column-gap` | `8px` | `8px` | MATCH |
| ActionRow (detail action row) | `row-gap` | `8px` | `8px` | MATCH |
| Export brief (primary) | `text` | `Export brief` | `Export brief` | MATCH |
| Export brief (primary) | `padding-top` | `8px` | `8px` | MATCH |
| Export brief (primary) | `padding-left` | `14px` | `14px` | MATCH |
| Export brief (primary) | `border-radius` | `6px` | `6px` | MATCH |
| Export brief (primary) | `color` | `#FFFFFF` | `rgb(255, 255, 255)` | MATCH |
| Share (secondary) | `text` | `Share` | `Share` | MATCH |
| Share (secondary) | `background-color` | `#FFFFFF` | `rgb(255, 255, 255)` | MATCH |
| Share (secondary) | `border-top-width` | `1px` | `1px` | MATCH |
| Share (secondary) | `border-top-style` | `solid` | `solid` | MATCH |
| Share (secondary) | `border-top-color` | `rgba(0,0,0,.25)` | `rgba(0, 0, 0, 0.25)` | MATCH |
| Share (secondary) | `border-radius` | `6px` | `6px` | MATCH |
| + Tag (dashed pill) | `text` | `+ Tag` | `+ Tag` | MATCH |
| + Tag (dashed pill) | `border-top-width` | `1px` | `1px` | MATCH |
| + Tag (dashed pill) | `border-top-style` | `dashed` | `dashed` | MATCH |
| + Tag (dashed pill) | `border-top-color` | `rgba(0,0,0,.25)` | `rgba(0, 0, 0, 0.25)` | MATCH |
| an 'Immediate ▾' band-change dropdown in the action row | `(forbidden element)` | `absent` | `absent` | MATCH |

## AdminIssuesRail (Admin surface right rail)

- Spec file: `fsi-app/.discipline/rendering/audit/spec/admin-issues-rail.json`
- Mount: `admin-issues-rail` at 1440 px
- Artboards: 13-admin
- Design source: docs/design/handoff-2026-09-06/'Caros Ledge UI System.dc.html' id="p13", the 'Issues queue' rail card; operator ruling 5.1 (2026-09-07)
- Result: 38 MATCH, 0 not matching

> dc.html p13 rail card shell: `background:#fff;border:1px solid rgba(0,0,0,.12);border-radius:10px;box-shadow:0 1px 2px rgba(26,26,26,.04),0 4px 14px rgba(26,26,26,.06);overflow:hidden`, topped by the ruling 5.1 gradient rule, then `padding:12px 16px 14px`.
> dc.html p13 header: title 'Issues queue' `font-size:10.5px;letter-spacing:.12em;text-transform:uppercase;color:#7A6E6C;font-weight:700`; total `font-family:Anton;font-size:18px;color:#DC2626` (only when > 0).
> dc.html p13 row: `padding:4px 0;border-bottom:1px solid rgba(0,0,0,.06)`; non-zero row label `font-weight:700;color:#1A1A1A`, value `font-family:Anton;font-size:16px;color:#DC2626`; zero row label `font-weight:500;color:#5A6B67`, value `font-family:Anton;font-size:16px;color:#1A1A1A`.
> No sub-description line exists per row in the artboard (just label + numeral) — the built component's row.sub field was builder-added content beyond the artboard and has been removed as dormant once unrendered (CLAUDE.md rule: no dormant code), per 'the artboard wins' (README vs artboard) and the harness's own preference for the exact-artboard reading.
> The prior build had NO card wrapper, NO SectionRule, and a wrong-direction `border-bottom:2px solid var(--text)` under a 26px title — exactly the divider ruling 4.1/5.1 removes. Rewritten this session to the section-card shell + SectionRule + dc.html's own header/row geometry.
> Fixture counts (mounts.mjs ADMIN_ISSUES_RAIL_FIXTURE) match dc.html p13's own example numbers exactly: 489 / 4576 / 6 / 3 non-zero, rest zero, total 5,074 — so this spec measures both the zero-row and non-zero-row colour rules against real numbers, not sample data invented for the harness.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| AdminIssuesRail (Admin surface right rail) | `background-color` | `#FFFFFF` | `rgb(255, 255, 255)` | MATCH |
| AdminIssuesRail (Admin surface right rail) | `border-width` | `1px` | `1px` | MATCH |
| AdminIssuesRail (Admin surface right rail) | `border-color` | `rgba(0,0,0,.12)` | `rgba(0, 0, 0, 0.12)` | MATCH |
| AdminIssuesRail (Admin surface right rail) | `border-radius` | `10px` | `10px` | MATCH |
| gradient rule (ruling 5.1) | `height` | `3px` | `3px` | MATCH |
| gradient rule (ruling 5.1) | `background-image` | `linear-gradient(90deg,#5A5552,#5A5552 22%,rgba(90,85,82,.18))` | `linear-gradient(90deg, rgb(90, 85, 82), rgb(90, 85, 82) 22%, rgba(90, 85, 82, 0.18))` | MATCH |
| title | `text` | `Issues queue` | `Issues queue` | MATCH |
| title | `font-size` | `10.5px` | `10.5px` | MATCH |
| title | `letter-spacing` | `0.12em (= 1.26px at 10.5px)` | `1.26px` | MATCH |
| title | `text-transform` | `uppercase` | `uppercase` | MATCH |
| title | `font-weight` | `700` | `700` | MATCH |
| title | `color` | `#7A6E6C` | `rgb(122, 110, 108)` | MATCH |
| total | `text` | `5,074` | `5,074` | MATCH |
| total | `font-family` | `contains:Anton` | `Anton, system-ui, sans-serif` | MATCH |
| total | `font-size` | `18px` | `18px` | MATCH |
| total | `color` | `#DC2626` | `rgb(220, 38, 38)` | MATCH |
| first row (non-zero: provisional sources pending, 489) | `padding-top` | `4px` | `4px` | MATCH |
| first row (non-zero: provisional sources pending, 489) | `padding-bottom` | `4px` | `4px` | MATCH |
| first row (non-zero: provisional sources pending, 489) | `border-bottom-width` | `1px` | `1px` | MATCH |
| first row (non-zero: provisional sources pending, 489) | `border-bottom-color` | `rgba(0,0,0,.06)` | `rgba(0, 0, 0, 0.06)` | MATCH |
| first row label | `text` | `Provisional sources pending review` | `Provisional sources pending review` | MATCH |
| first row label | `font-weight` | `700` | `700` | MATCH |
| first row label | `color` | `#1A1A1A` | `rgb(26, 26, 26)` | MATCH |
| first row value | `text` | `489` | `489` | MATCH |
| first row value | `font-family` | `contains:Anton` | `Anton, system-ui, sans-serif` | MATCH |
| first row value | `font-size` | `16px` | `16px` | MATCH |
| first row value | `color` | `#DC2626` | `rgb(220, 38, 38)` | MATCH |
| a zero row (staged updates pending, 0) | `padding-top` | `4px` | `4px` | MATCH |
| a zero row (staged updates pending, 0) | `padding-bottom` | `4px` | `4px` | MATCH |
| a zero row (staged updates pending, 0) | `border-bottom-width` | `1px` | `1px` | MATCH |
| a zero row (staged updates pending, 0) | `border-bottom-color` | `rgba(0,0,0,.06)` | `rgba(0, 0, 0, 0.06)` | MATCH |
| zero row label | `text` | `Staged updates pending` | `Staged updates pending` | MATCH |
| zero row label | `font-weight` | `500` | `500` | MATCH |
| zero row label | `color` | `#5A6B67` | `rgb(90, 107, 103)` | MATCH |
| zero row value | `text` | `0` | `0` | MATCH |
| zero row value | `font-family` | `contains:Anton` | `Anton, system-ui, sans-serif` | MATCH |
| zero row value | `font-size` | `16px` | `16px` | MATCH |
| zero row value | `color` | `#1A1A1A` | `rgb(26, 26, 26)` | MATCH |

## Admin stat tiles (8-tile summary grid)

- Spec file: `fsi-app/.discipline/rendering/audit/spec/admin-stat-tiles.json`
- Mount: `admin-stat-tiles` at 1440 px
- Artboards: 13-platform-admin
- Design source: 'Caros Ledge UI System.dc.html' id="p13" (Platform admin), the 4-up tile grid directly under the masthead — each tile: `background:#fff;border:1px solid rgba(0,0,0,.12);border-radius:10px;box-shadow:0 1px 2px rgba(26,26,26,.04),0 4px 14px rgba(26,26,26,.06);padding:0 14px 12px 14px`; label `font-size:11px;letter-spacing:.1em;text-transform:uppercase;font-weight:800`; numeral `font-family:'Anton';font-size:16px`; note `font-size:11.5px;color:#5A6B67;margin-top:4px;line-height:1.45`.
- Result: 20 MATCH, 0 not matching

> Train 58, step 2: StatBlock gained an additive `size="tile"` variant (11px label / 16px Anton numeral / 11.5px note) for exactly this grid; AdminDashboard.tsx's 8 tiles (Workspaces/Sources/Ingest/Coverage/Research pipeline/Community pickups/Runtime/Emission factors) now use it with the artboard's wrapper geometry, a new `.cl-admin-stat-tile` class.
> None of the 8 tiles in this grid carries a separate title line above the label/numeral row (unlike some other admin cards) — no SectionRule is mounted on these tiles, per the artboard's own markup, which shows no top rule inside any tile.
> The artboard's 'Sources' tile example carries `border:1px solid #5A5552` with no box-shadow, read here as an active/selected-section illustration (Sources is the artboard's example active tab), not a base style; the product's `on` (aria-pressed) state overrides `borderColor` to `var(--brand)` for the active tile and is not asserted by this spec, which measures the base (inactive) tile only. Lane compose-other (2026-09-08) changed `AdminDashboard`'s default `section` state from 'Workspaces' to 'Sources' (dc.html p13's own default landing state — see DEVIATION-LOG 2026-09-08), which made tile 2 ('Sources') the ACTIVE tile on mount, breaking this spec's prior tile-2 measurement (it started reading the active border colour, `rgb(90,85,82)`, against the base-style expectation). Re-pointed to tile 3 ('Ingest'), inactive on mount both before and after that change, confirmed by a direct debug read this session.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| Admin stat tiles (8-tile summary grid) | `background-color` | `#FFFFFF` | `rgb(255, 255, 255)` | MATCH |
| Admin stat tiles (8-tile summary grid) | `border-top-width` | `1px` | `1px` | MATCH |
| Admin stat tiles (8-tile summary grid) | `border-top-color` | `rgba(0,0,0,.12)` | `rgba(0, 0, 0, 0.12)` | MATCH |
| Admin stat tiles (8-tile summary grid) | `border-radius` | `10px` | `10px` | MATCH |
| Admin stat tiles (8-tile summary grid) | `box-shadow` | `rgba(26,26,26,.04) 0px 1px 2px 0px, rgba(26,26,26,.06) 0px 4px 14px 0px` | `rgba(26, 26, 26, 0.04) 0px 1px 2px 0px, rgba(26, 26, 26, 0.06) 0px 4px 14px 0px` | MATCH |
| Admin stat tiles (8-tile summary grid) | `padding-top` | `0px` | `0px` | MATCH |
| Admin stat tiles (8-tile summary grid) | `padding-right` | `14px` | `14px` | MATCH |
| Admin stat tiles (8-tile summary grid) | `padding-bottom` | `12px` | `12px` | MATCH |
| Admin stat tiles (8-tile summary grid) | `padding-left` | `14px` | `14px` | MATCH |
| tile count (8 tiles: 7 sections + Emission factors) | `count` | `8` | `8` | MATCH |
| tile — label | `font-size` | `11px` | `11px` | MATCH |
| tile — label | `letter-spacing` | `0.1em (= 1.1px at 11px)` | `1.1px` | MATCH |
| tile — label | `text-transform` | `uppercase` | `uppercase` | MATCH |
| tile — label | `font-weight` | `800` | `800` | MATCH |
| tile — numeral | `font-family` | `contains:Anton` | `Anton, system-ui, sans-serif` | MATCH |
| tile — numeral | `font-size` | `16px` | `16px` | MATCH |
| tile — note | `font-size` | `11.5px` | `11.5px` | MATCH |
| tile — note | `color` | `#5A6B67` | `rgb(90, 107, 103)` | MATCH |
| tile — note | `margin-top` | `4px` | `4px` | MATCH |
| tile — note | `line-height` | `16.675px` | `16.675px` | MATCH |

## AuthFrame identity panel + AuthTabs

- Spec file: `fsi-app/.discipline/rendering/audit/spec/auth-frame.json`
- Mount: `auth-frame` at 1440 px
- Artboards: 16-auth
- Design source: docs/design/handoff-2026-09-06/'Caros Ledge UI System.dc.html' id="p16" (char 620462); README.md screen 16 note 'same inputs and buttons as Settings'
- Result: 28 MATCH, 0 not matching

> dc.html p16: identity panel is `padding:48px 56px`, top rule `height:3px;width:160px;background:#5A5552;border-radius:2px;margin-bottom:22px`, wordmark `font-family:'Anton';font-size:30px;letter-spacing:.04em;text-transform:uppercase;line-height:1`, tagline `font-size:10px;letter-spacing:.14em;text-transform:uppercase;color:#7A6E6C;font-weight:600;margin-top:6px`.
> This top rule is neither the masthead 3px cap (ruling 5.2, scoped to the 252px nav card) nor a section-card header (ruling 5.1) — it is the auth/onboarding identity mark, solid #5A5552 at a fixed 160px width, unrelated to either ruling. Recorded here as its own value rather than folded into either.
> AuthTabs: dc.html p16 tab strip is `display:flex;gap:2px;border-bottom:1px solid rgba(0,0,0,.12)`; active tab `padding:9px 12px;font-size:12.5px;font-weight:700;border-bottom:2px solid #5A5552;margin-bottom:-1px`; inactive tab `font-weight:600;color:#5A6B67`.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| AuthFrame identity panel + AuthTabs | `height` | `3px` | `3px` | MATCH |
| AuthFrame identity panel + AuthTabs | `width` | `160px` | `160px` | MATCH |
| AuthFrame identity panel + AuthTabs | `background-color` | `#5A5552` | `rgb(90, 85, 82)` | MATCH |
| AuthFrame identity panel + AuthTabs | `border-radius` | `2px` | `2px` | MATCH |
| AuthFrame identity panel + AuthTabs | `margin-bottom` | `22px` | `22px` | MATCH |
| wordmark | `text` | `Caro's Ledge` | `Caro's Ledge` | MATCH |
| wordmark | `font-family` | `contains:Anton` | `Anton, system-ui, sans-serif` | MATCH |
| wordmark | `font-size` | `30px` | `30px` | MATCH |
| wordmark | `letter-spacing` | `0.04em (= 1.2px at 30px)` | `1.2px` | MATCH |
| wordmark | `text-transform` | `uppercase` | `uppercase` | MATCH |
| wordmark | `line-height` | `30px` | `30px` | MATCH |
| identity panel padding | `padding-top` | `48px` | `48px` | MATCH |
| identity panel padding | `padding-left` | `56px` | `56px` | MATCH |
| AuthTabs — Sign in (active) | `text` | `Sign in` | `Sign in` | MATCH |
| AuthTabs — Sign in (active) | `font-size` | `12.5px` | `12.5px` | MATCH |
| AuthTabs — Sign in (active) | `font-weight` | `700` | `700` | MATCH |
| AuthTabs — Sign in (active) | `border-bottom-width` | `2px` | `2px` | MATCH |
| AuthTabs — Sign in (active) | `border-bottom-color` | `#5A5552` | `rgb(90, 85, 82)` | MATCH |
| AuthTabs — Sign in (active) | `padding-top` | `9px` | `9px` | MATCH |
| AuthTabs — Sign in (active) | `padding-left` | `12px` | `12px` | MATCH |
| AuthTabs — Create account (inactive) | `text` | `Create account` | `Create account` | MATCH |
| AuthTabs — Create account (inactive) | `font-weight` | `600` | `600` | MATCH |
| AuthTabs — Create account (inactive) | `border-bottom-color` | `rgba(0,0,0,0)` | `rgba(0, 0, 0, 0)` | MATCH |
| field input | `height` | `36px` | `36px` | MATCH |
| field input | `border-width` | `1px` | `1px` | MATCH |
| field input | `border-color` | `rgba(0,0,0,.25)` | `rgba(0, 0, 0, 0.25)` | MATCH |
| field input | `border-radius` | `6px` | `6px` | MATCH |
| field input | `font-size` | `13px` | `13px` | MATCH |

## BandGradientRule

- Spec file: `fsi-app/.discipline/rendering/audit/spec/bandgradientrule.json`
- Mount: `bandgradientrule` at 1440 px
- Artboards: 01-dashboard (nav cap), 19-masthead-line-options (ruling 5.2, the option turn)
- Design source: docs/design/handoff-2026-09-06/README.md §0.2 'the four-band gradient rule (3px, segment widths proportional to the live band counts) is the brand mark'; 'Caros Ledge UI System.dc.html' p1's nav-card cap: `linear-gradient(90deg,#DC2626,#F97316 34%,#2563EB 67%,#16A34A)`; ruling 5.2 (this rule stays in exactly one place sitewide, the nav card cap)
- Result: 2 MATCH, 0 not matching

> BandGradientRule.tsx is pure inline style; every property here is a real product value.
> Fixture counts (immediate 14, action 31, monitor 1135, awareness 254, total 1434) are the SAME live counts dc.html p1's own nav card cap uses. The design source states the MECHANISM ('segment widths proportional to the live band counts'), not a fixed percentage constant, so the expected `background-image` value below is the correctly-computed proportional gradient for those counts (14/1434=0.976%, 45/1434=3.138%, 1180/1434=82.287%) — this spec verifies BandGradientRule.tsx computes that proportion correctly and in band (hot-to-cool) order, band-accurate hues, at 3px height, rather than asserting a design-stated constant. page-frame.json's own nav-cap check covers the structural claim (height + exactly one instance sitewide); this spec adds the proportional-math and colour-order proof.
> This is the ONE place ruling 5.2 keeps this coloured rule; every other 3px rule sitewide must use the dark-grey gradation from ruling 5.1 instead (checked by page-frame.json's own forbid list).

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| BandGradientRule | `height` | `3px` | `3px` | MATCH |
| BandGradientRule | `background-image` | `linear-gradient(90deg, rgb(220, 38, 38) 0%, rgb(220, 38, 38) 0.97629%, rgb(249, 115, 22) 0.97629%, rgb(249, 115, 22) 3.13808%, rgb(37, 99, 235) 3.13808%, rgb(37, 99, 235) 82.2873%, rgb(22, 163, 74) 82.2873%, rgb(22, 163, 74) 100%)` | `linear-gradient(90deg, rgb(220, 38, 38) 0%, rgb(220, 38, 38) 0.97629%, rgb(249, 115, 22) 0.97629%, rgb(249, 115, 22) 3.13808%, rgb(37, 99, 235) 3.13808%, rgb(37, 99, 235) 82.2873%, rgb(22, 163, 74) 82.2873%, rgb(22, 163, 74) 100%)` | MATCH |

## BandTile

- Spec file: `fsi-app/.discipline/rendering/audit/spec/bandtile.json`
- Mount: `bandtile` at 1440 px
- Artboards: 00-system-sheet §0.4 'Band tiles', 01-dashboard, 02-regulations-list
- Design source: docs/design/handoff-2026-09-06/README.md §0.4 'Band tiles'; 'Caros Ledge UI System.dc.html' #sys band-tile block
- Result: 29 MATCH, 0 not matching

> BandTile.tsx is pure inline style (the `.cl-band-tile` class only carries the mobile-only media query, no desktop-affecting rule), so every property here is a real product value at 1440.
> Checked once on the Immediate tile (band colour #DC2626); the same component renders all four bands from the one BAND_ORDER table, so a colour finding here would apply to all four.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| BandTile | `background-color` | `#FFFFFF` | `rgb(255, 255, 255)` | MATCH |
| BandTile | `border-radius` | `10px` | `10px` | MATCH |
| BandTile | `padding-top` | `14px` | `14px` | MATCH |
| BandTile | `padding-right` | `16px` | `16px` | MATCH |
| BandTile | `padding-bottom` | `0px` | `0px` | MATCH |
| BandTile | `padding-left` | `16px` | `16px` | MATCH |
| BandTile | `display` | `flex` | `flex` | MATCH |
| BandTile | `flex-direction` | `column` | `column` | MATCH |
| label (stacked, band colour) | `font-size` | `10.5px` | `10.5px` | MATCH |
| label (stacked, band colour) | `font-weight` | `800` | `800` | MATCH |
| label (stacked, band colour) | `letter-spacing` | `0.08em (= 0.84px at 10.5px)` | `0.84px` | MATCH |
| label (stacked, band colour) | `text-transform` | `uppercase` | `uppercase` | MATCH |
| label (stacked, band colour) | `color` | `#DC2626` | `rgb(220, 38, 38)` | MATCH |
| label text | `text` | `Immediate` | `Immediate` | MATCH |
| window (muted) | `font-size` | `10.5px` | `10.5px` | MATCH |
| window (muted) | `color` | `#7A6E6C` | `rgb(122, 110, 108)` | MATCH |
| window text | `text` | `≤ 90 days` | `≤ 90 days` | MATCH |
| numeral (Anton, band colour) | `font-family` | `contains:Anton` | `Anton, system-ui, sans-serif` | MATCH |
| numeral (Anton, band colour) | `font-size` | `34px` | `34px` | MATCH |
| numeral (Anton, band colour) | `letter-spacing` | `0.04em (= 1.36px at 34px)` | `1.36px` | MATCH |
| numeral (Anton, band colour) | `color` | `#DC2626` | `rgb(220, 38, 38)` | MATCH |
| numeral (Anton, band colour) | `font-variant-numeric` | `tabular-nums` | `tabular-nums` | MATCH |
| band rule (pinned to bottom edge, full-bleed) | `height` | `4px` | `4px` | MATCH |
| band rule (pinned to bottom edge, full-bleed) | `background-color` | `#DC2626` | `rgb(220, 38, 38)` | MATCH |
| band rule (pinned to bottom edge, full-bleed) | `margin-right` | `-16px` | `-16px` | MATCH |
| band rule (pinned to bottom edge, full-bleed) | `margin-left` | `-16px` | `-16px` | MATCH |
| band rule (pinned to bottom edge, full-bleed) | `margin-bottom` | `0px` | `0px` | MATCH |
| card minimum hit area | `min-height` | `112px` | `112px` | MATCH |
| numeral is thousands-separated (artboard 01's MONITOR tile reads '1,135', not '1135') | `text` | `1,135` | `1,135` | MATCH |

## Chips — band, tier, neutral tag, workspace tag, filter

- Spec file: `fsi-app/.discipline/rendering/audit/spec/chips.json`
- Mount: `chips` at 1440 px
- Artboards: 00-system-sheet §0.4 'Chips'
- Design source: docs/design/handoff-2026-09-06/README.md §0.4 'Chips' and 'Workspace tags'; 'Caros Ledge UI System.dc.html' #sys 'Chips — three kinds, three shapes' block; operator ruling 2.5
- Result: 54 MATCH, 0 not matching

> Chips.tsx is pure inline style (media queries only affect <768px, not this 1440 mount); every property here is a real product value.
> Ruling 2.5 restates the family rules (band = tinted pill + round dot; workspace tag = square dot, removable; kind/mode/topic = neutral, no dot, no border; tier = the only bordered chip) without new numbers for band/tier/tag beyond what the artboard states, so the artboard markup is the numeric source for those three.
> TagChip has no `border` set at all (correct per ruling 2.5's 'no dot, NO border'); the artboard's neutral tag also has no border rule. Both sides agree, not asserted as a property since 'unset' has no single canonical getComputedStyle form worth comparing.
> CONFIRMED, verified not a harness artefact: `--radius-pill` computes to nothing (CSSOM initial value, 0) in the real product, not just this harness. src/app/theme.css declares it TWICE inside the SAME plain `:root{}` rule (no `@theme` involved, no Tailwind step in play): line 55 `--radius-pill: 999px;`, then line 201 `--radius-pill: var(--radius-pill);` — a property declared in terms of itself, which CSS defines as invalid, and the invalid LATER declaration wins over the valid earlier one within one rule (there is no 'fall back to the earlier declaration' in CSS custom properties). Every chip that reads `var(--radius-pill)` — BandChip, WorkspaceTagPill, and TagChip (see next note) — renders SQUARE corners everywhere in the shipped product, not just in this measurement. Confirmed by probing `getComputedStyle(document.documentElement).getPropertyValue('--radius-pill')` directly against theme.css alone (empty string) before writing this spec.
> TagChip's radius is a second, separate issue on top of the broken token: even if `--radius-pill` were fixed to 999px, the artboard's neutral tag uses `border-radius:4px` (a squared tag, matching the tier chip's own 4px-square convention), not a pill — TagChip.tsx reaches for the pill token where the design wants the square one.
> FIXED by fix58-tokens (2026-09-07): removed theme.css's self-referential `--radius-pill: var(--radius-pill);` redeclaration (it carried no independent value and only broke the earlier valid one); `--radius-pill` now resolves to 999px sitewide. TagChip.tsx also switched to `border-radius:4px` (the design's square-tag value, not the pill token).

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| Chips — band, tier, neutral tag, workspace tag, filter | `padding-top` | `3px` | `3px` | MATCH |
| Chips — band, tier, neutral tag, workspace tag, filter | `padding-right` | `9px` | `9px` | MATCH |
| Chips — band, tier, neutral tag, workspace tag, filter | `padding-bottom` | `3px` | `3px` | MATCH |
| Chips — band, tier, neutral tag, workspace tag, filter | `padding-left` | `7px` | `7px` | MATCH |
| Chips — band, tier, neutral tag, workspace tag, filter | `border-radius` | `999px` | `999px` | MATCH |
| Chips — band, tier, neutral tag, workspace tag, filter | `background-color` | `#FFF7ED` | `rgb(255, 247, 237)` | MATCH |
| Chips — band, tier, neutral tag, workspace tag, filter | `color` | `#F97316` | `rgb(249, 115, 22)` | MATCH |
| Chips — band, tier, neutral tag, workspace tag, filter | `font-weight` | `800` | `800` | MATCH |
| Chips — band, tier, neutral tag, workspace tag, filter | `letter-spacing` | `0.06em (= 0.63px at 10.5px)` | `0.63px` | MATCH |
| Chips — band, tier, neutral tag, workspace tag, filter | `text-transform` | `uppercase` | `uppercase` | MATCH |
| Chips — band, tier, neutral tag, workspace tag, filter | `font-size` | `10.5px` | `10.5px` | MATCH |
| Chips — band, tier, neutral tag, workspace tag, filter | `border-width` | `1px` | `1px` | MATCH |
| Chips — band, tier, neutral tag, workspace tag, filter | `border-style` | `solid` | `solid` | MATCH |
| Chips — band, tier, neutral tag, workspace tag, filter | `border-color` | `#FED7AA` | `rgb(254, 215, 170)` | MATCH |
| band chip — dot size | `width` | `7px` | `7px` | MATCH |
| band chip — dot size | `height` | `7px` | `7px` | MATCH |
| band chip — dot size | `border-radius` | `50%` | `50%` | MATCH |
| band chip — dot size | `background-color` | `#F97316` | `rgb(249, 115, 22)` | MATCH |
| tier chip | `padding-top` | `3px` | `3px` | MATCH |
| tier chip | `padding-bottom` | `3px` | `3px` | MATCH |
| tier chip | `padding-left` | `7px` | `7px` | MATCH |
| tier chip | `padding-right` | `7px` | `7px` | MATCH |
| tier chip | `border-width` | `1px` | `1px` | MATCH |
| tier chip | `border-color` | `rgba(0,0,0,.2)` | `rgba(0, 0, 0, 0.2)` | MATCH |
| tier chip | `border-radius` | `4px` | `4px` | MATCH |
| tier chip | `font-weight` | `800` | `800` | MATCH |
| tier chip | `letter-spacing` | `0.06em (= 0.6px at 10px)` | `0.6px` | MATCH |
| tier chip | `font-size` | `10px` | `10px` | MATCH |
| tier chip text | `text` | `T1` | `T1` | MATCH |
| neutral tag chip (kind/mode/topic) | `padding-top` | `3px` | `3px` | MATCH |
| neutral tag chip (kind/mode/topic) | `padding-bottom` | `3px` | `3px` | MATCH |
| neutral tag chip (kind/mode/topic) | `padding-left` | `8px` | `8px` | MATCH |
| neutral tag chip (kind/mode/topic) | `padding-right` | `8px` | `8px` | MATCH |
| neutral tag chip (kind/mode/topic) | `border-radius` | `4px` | `4px` | MATCH |
| neutral tag chip (kind/mode/topic) | `background-color` | `#F5F2EE` | `rgb(245, 242, 238)` | MATCH |
| neutral tag chip (kind/mode/topic) | `font-weight` | `600` | `600` | MATCH |
| neutral tag chip (kind/mode/topic) | `font-size` | `10.5px` | `10.5px` | MATCH |
| neutral tag chip (kind/mode/topic) | `letter-spacing` | `0.04em (= 0.42px at 10.5px)` | `0.42px` | MATCH |
| neutral tag chip (kind/mode/topic) | `text-transform` | `uppercase` | `uppercase` | MATCH |
| workspace tag pill | `background-color` | `#F5F2EE` | `rgb(245, 242, 238)` | MATCH |
| workspace tag pill | `border-width` | `1px` | `1px` | MATCH |
| workspace tag pill | `border-color` | `rgba(0,0,0,.14)` | `rgba(0, 0, 0, 0.14)` | MATCH |
| workspace tag pill | `font-size` | `11.5px` | `11.5px` | MATCH |
| workspace tag pill | `font-weight` | `600` | `600` | MATCH |
| workspace tag pill | `border-radius` | `999px` | `999px` | MATCH |
| workspace tag pill — square dot | `width` | `6px` | `6px` | MATCH |
| workspace tag pill — square dot | `height` | `6px` | `6px` | MATCH |
| workspace tag pill — square dot | `background-color` | `#1A1A1A` | `rgb(26, 26, 26)` | MATCH |
| filter chip group label | `font-size` | `10px` | `10px` | MATCH |
| filter chip group label | `letter-spacing` | `0.12em (= 1.2px at 10px)` | `1.2px` | MATCH |
| filter chip group label | `font-weight` | `700` | `700` | MATCH |
| filter chip group label | `text-transform` | `uppercase` | `uppercase` | MATCH |
| active filter chip | `background-color` | `#5A5552` | `rgb(90, 85, 82)` | MATCH |
| active filter chip | `color` | `#FFFFFF` | `rgb(255, 255, 255)` | MATCH |

## Community — peer-org directory table

- Spec file: `fsi-app/.discipline/rendering/audit/spec/community-table.json`
- Mount: `peer-org-table` at 1440 px
- Artboards: 
- Design source: src/components/community/PeerOrgDirectoryTable.tsx's own header comment ('spec 05 §5 component 3: peer-org directory, aggregate, pseudonymous') and CONFIRMED ABSENCE from the design source: `grep -i 'directory'` and `grep -i 'peer.org'` against README.md return nothing, and p12 (#p12, the Community artboard, docs/design/handoff-2026-09-06/'Caros Ledge UI System.dc.html') contains no directory/table markup — its content is the room/thread surface, not this aggregate table.
- Result: 10 MATCH, 0 not matching

> This part is NOT one of the 17 UI-system artboards. Per CLAUDE.md rule 2 ('never fabricate') this spec does not invent a per-pixel design value to compare against — there is no design source stating one. What it checks instead: the general sitewide rules every part must follow regardless of artboard coverage (no literal UNSCORED anywhere; the component's own legacy `--color-*` tokens resolve to the SAME canonical values theme.css defines, so the legacy naming is migration debt, not a visual defect) — confirmed live, not asserted from reading theme.css alone.
> Token resolution confirmed (theme.css): --color-bg-surface: var(--card) [#FFFFFF]; --color-border: var(--line-1) [rgba(0,0,0,.12)]; --color-text-muted: var(--ink-3) [#7A6E6C]; --color-text-primary: var(--ink) [#1A1A1A]. PeerOrgDirectoryTable.tsx reads the legacy names exclusively (never the canonical ones directly), which is exactly the migration-scaffolding pattern theme.css's own header describes ('legacy names are migration scaffolding for components later lanes still have to touch') — logged here, not fixed (this lane writes no product code).

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| Community — peer-org directory table | `border-collapse` | `collapse` | `collapse` | MATCH |
| table wrapper — resolves to the canonical card surface (background) | `background-color` | `#FFFFFF` | `rgb(255, 255, 255)` | MATCH |
| table wrapper — resolves to the canonical card surface (background) | `border-top-width` | `1px` | `1px` | MATCH |
| table wrapper — resolves to the canonical card surface (background) | `border-top-color` | `rgba(0,0,0,.12)` | `rgba(0, 0, 0, 0.12)` | MATCH |
| column header typography | `font-size` | `10px` | `10px` | MATCH |
| column header typography | `font-weight` | `800` | `800` | MATCH |
| column header typography | `letter-spacing` | `0.06em (= 0.6px at 10px)` | `0.6px` | MATCH |
| column header typography | `text-transform` | `uppercase` | `uppercase` | MATCH |
| column header typography | `color` | `#7A6E6C` | `rgb(122, 110, 108)` | MATCH |
| the literal word UNSCORED anywhere in the table | `(forbidden element)` | `absent` | `absent` | MATCH |

## Page composition — Dashboard (01)

- Spec file: `fsi-app/.discipline/rendering/audit/spec/compose-01-dashboard.json`
- Mount: `page-frame-1440` at 1440 px
- Artboards: 01-dashboard
- Design source: docs/design/handoff-2026-09-06/screens/01-dashboard.png (artboard image is the authority per SETUP); dc.html id="p1"; README screen 1
- Result: 7 MATCH, 0 not matching

> Region-composition check, not a value check (page-frame.json and the per-part specs already check exact colours/type/spacing). This spec checks that the artboard's regions exist, in the artboard's top-to-bottom order, inside `[data-audit="dashboard"]` — the real DashboardBrief mounted by page-frame-1440.
> The masthead (VOL line, title, dek, command bar) is NOT inside this mount (page-frame-1440 wraps DashboardBrief only, no DashboardMasthead) — verified separately by source read of src/app/page.tsx + src/components/dashboard/DashboardMasthead.tsx and the live-server screenshot docs/design/handoff-2026-09-06/built/uxfix-5.1-4.1-4.2-5.2-2.1-2.2-dashboard.png. A follow-up mount that adds DashboardMasthead (stubbing useAuth/useWorkspaceStore/useWorkspaceBootstrap) would let this spec check the masthead region too.
> Artboard order top to bottom: band tiles (Immediate/Action/Monitor/Awareness) -> Due next card (heading, column header, rows, foot 'All N immediate' / 'then N action . N monitor') -> What changed card (heading, column header, rows, foot) -> rail (Across the platform, Watchlist, Legend), rail running alongside the two left-column cards, not below them.
> Due-next foot's 'All N immediate' is a real link to /regulations?band=immediate per operator ruling 1.1 (closed) — checked below as an <a>/Link element, not plain text.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| Page composition — Dashboard (01) | `display` | `grid` | `grid` | MATCH |
| Page composition — Dashboard (01) | `grid-template-columns` | `* 300px` | `730px 300px` | MATCH |
| region 1 — band tiles row (Immediate, Action, Monitor, Awareness, in that order) | `display` | `grid` | `grid` | MATCH |
| region 2a — Due next foot 'All N immediate' is a real link (ruling 1.1) | `count` | `1` | `1` | MATCH |
| region 5 — rail, Watchlist card second | `count` | `1` | `1` | MATCH |
| no floating Ask AI button (ruling 2.2) | `(forbidden element)` | `absent` | `absent` | MATCH |
| row impact meter never renders bare '0' for an unscored item (ruling: never 0) | `(forbidden element)` | `absent` | `absent` | MATCH |

## Regulations page composition — RegulationsLedger (real page component, not the ListSurfaceShell primitive)

- Spec file: `fsi-app/.discipline/rendering/audit/spec/compose-02-regulations-list.json`
- Mount: `compose-02-regulations` at 1440 px
- Artboards: 02-regulations-list
- Design source: dc.html p2 (#p2): masthead, band tiles, 'N regulations · grouped by band · Show as one list' + Sort row, rail (Filters card: MODE/JURISDICTION/TOPIC/SOURCE TIER, then 'Obligations · next 30 days', then Legend), band-sectioned rows. This spec exists per the operator's own complaint ('the filters were not above the regulations, they were on the right — same on every page; you are NOT matching the images directly') — it measures the COMPOSED page, not a shell primitive fed invented props, so a future regression that puts the facets back in the content column, or drops the sort row, or reorders the rail, is caught here.
- Result: 23 MATCH, 0 not matching

> The pre-existing list-surface.json spec measures ListSurfaceShell/ListSurfaceRailCards in isolation via the 'list-surface-1440' mount (a minimal fixture: one facetGroup, no sortRow, no topic/tier). This spec measures the REAL RegulationsLedger composition (24-row fixture, matching capture-compose-lists-screenshots.mjs), so the sort row, the Topic/Source-tier facets, and the rail's actual top-to-bottom order are all exercised end to end.
> Fixture is invented sample data with the real shape (same helper as the evidence screenshot); the audit asserts geometry/placement/labels, never fixture values.
> The Filters rail card sits ABOVE the Legend rail card (p2's own top-to-bottom rail order) — asserted via a DOM-order sibling-combinator selector, since 'present but in the wrong place' was the exact original defect, not mere absence.
> The rail's OBLIGATIONS · NEXT 30 DAYS card (lane comp-oblig, 2026-09-08) is measured here for presence, DOM order between Filters and Legend, its head label and 'Calendar →' link, and its four-row cap: artboard 02/id="p2" draws four dated rows, and the mount's obligation fixture deliberately includes a fifth event 90 days out so the card's own 30-day window is proven in the real composition, not only in obligation-rail-select.npmtest.mjs.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| Regulations page composition — RegulationsLedger (real page component, not the ListSurfaceShell primitive) | `display` | `block` | `block` | MATCH |
| Filters rail card mounts before the Legend rail card (rail order: Filters, then Legend) | `display` | `block` | `block` | MATCH |
| Filters rail card's facet groups include Topic (p2 rail: MODE / JURISDICTION / TOPIC / SOURCE TIER) | `display` | `block` | `block` | MATCH |
| Filters rail card's facet groups include Source tier | `display` | `block` | `block` | MATCH |
| Sort/count row renders above the rows, with the count + 'grouped by band'/flat-toggle text and Sort control | `display` | `flex` | `flex` | MATCH |
| Sort row carries the real 'My order' option (all four options — Regulations is the one surface with 'My order') | `display` | `block` | `block` | MATCH |
| Obligations rail card mounts AFTER the Filters card (rail order: Filters, Obligations, Legend) | `display` | `block` | `block` | MATCH |
| Obligations rail card mounts BEFORE the Legend card | `display` | `block` | `block` | MATCH |
| Obligations rail card head carries the artboard's exact label | `text` | `Obligations · next 30 days` | `Obligations · next 30 days` | MATCH |
| Obligations rail card head carries the artboard's 'Calendar →' link | `text` | `Calendar →` | `Calendar →` | MATCH |
| Obligations rail card head carries the artboard's 'Calendar →' link | `display` | `flex` | `flex` | MATCH |
| Obligations rail card head carries the artboard's 'Calendar →' link | `font-size` | `11px` | `11px` | MATCH |
| Obligations rail card head carries the artboard's 'Calendar →' link | `font-weight` | `600` | `600` | MATCH |
| Obligations rail card renders the artboard's four dated rows, and no more (the fifth fixture event is 90 days out, outside the 30-day window) | `count` | `4` | `4` | MATCH |
| Obligations rail card renders the artboard's four dated rows, and no more (the fifth fixture event is 90 days out, outside the 30-day window) | `display` | `grid` | `grid` | MATCH |
| Obligations rail card renders the artboard's four dated rows, and no more (the fifth fixture event is 90 days out, outside the 30-day window) | `gap` | `10px` | `10px` | MATCH |
| Filters rail card head carries the artboard's Clear link, unconditionally | `font-size` | `11px` | `11px` | MATCH |
| Filters rail card head carries the artboard's Clear link, unconditionally | `font-weight` | `700` | `700` | MATCH |
| Filters rail card foot carries artboard 02's own footnote, verbatim | `text` | `Counts are live for the current selection. Filters never hide behind a button; the band tiles above are the fourth facet.` | `Counts are live for the current selection. Filters never hide behind a button; the band tiles above are the fourth facet.` | MATCH |
| no content-column facet chip strip above the rows (the original defect: filters rendered as pill chips in the content column, not the rail) | `(forbidden element)` | `absent` | `absent` | MATCH |
| the literal word UNSCORED anywhere in a row | `(forbidden element)` | `absent` | `absent` | MATCH |
| no trailing 'N regulations tracked across N jurisdictions' line below the band cards | `(forbidden element)` | `absent` | `absent` | MATCH |
| the MODE facet never prints a bare lower-case canonical token as a label | `(forbidden element)` | `absent` | `absent` | MATCH |

## Page composition — Regulation detail (03)

- Spec file: `fsi-app/.discipline/rendering/audit/spec/compose-03-regulation-detail.json`
- Mount: `page-frame-1440` at 1440 px
- Artboards: 03-regulation-detail
- Design source: docs/design/handoff-2026-09-06/screens/03-regulation-detail.png; dc.html id="p3"; README §0.5
- Result: 15 MATCH, 0 not matching

> Region-composition check inside [data-audit="regulation-detail"] — RegulationDetailSurface mounted with fixture data matching the artboard's own EU ETS example (mounts.mjs PAGE_FRAME_FIXTURES.regulation).
> Artboard order top to bottom: header (band pill, type/mode/topic chips, TIER LAST, header stat, action row) -> exposure grid -> timeline -> sticky section index + Summary/Full brief switch -> Summary section (Generated · 30-second read) -> dynamic sections -> Sources -> rail (At a glance, Impact assessment, Relevance, Owner & team, In this list, Connections, Legend).

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| Page composition — Regulation detail (03) | `count` | `1` | `1` | MATCH |
| region 1 — action row, exactly Export brief / Share / Watch / + Tag | `count` | `4` | `4` | MATCH |
| region 1a — header stat 'N source(s) · T# primary' present, before action row | `count` | `1` | `1` | MATCH |
| region 2 — sticky section index second entry is 'S2 Obligations — issues requiring action' | `text` | `S2 Obligations — issues requiring action` | `S2 Obligations — issues requiring action` | MATCH |
| region 3 — Summary section aside 'Generated · 30-second read' | `count` | `1` | `1` | MATCH |
| region 4 — Sources section is the last section before the rail | `text` | `Sources` | `Sources` | MATCH |
| region 1b — chip row order: band pill, Regulation, mode, topic, then the TIER square last | `count` | `1` | `1` | MATCH |
| region 1c — EXPOSURE grid renders between the header and the timeline | `count` | `1` | `1` | MATCH |
| region 1d — EXPOSURE 'Who pays' carries the item's own cost mechanism, not an Absence | `count` | `1` | `1` | MATCH |
| rail order 1 — AT A GLANCE is the first rail card (artboard 03) | `text` | `At a glance` | `At a glance` | MATCH |
| rail order 2 — IMPACT ASSESSMENT is the second rail card (artboard 03) | `text` | `Impact assessment` | `Impact assessment` | MATCH |
| rail order last — LEGEND is present, after the artboard's own page cards (Owner & team, In this list, Connections) | `count` | `1` | `1` | MATCH |
| no per-item ask bar between workspace tags and the action row (ruling 2.3) | `(forbidden element)` | `absent` | `absent` | MATCH |
| no 'Immediate ▾' band dropdown in the action row (ruling 3.1) | `(forbidden element)` | `absent` | `absent` | MATCH |
| the place-keeping card is never the first rail card | `(forbidden element)` | `absent` | `absent` | MATCH |

## Market page composition — MarketIntelLedger (real page component, not the ListSurfaceShell primitive)

- Spec file: `fsi-app/.discipline/rendering/audit/spec/compose-04-market-list.json`
- Mount: `compose-04-market` at 1440 px
- Artboards: 04-market-list
- Design source: dc.html p4 (#p4): masthead, band tiles, HEADLINE SERIES card, 'N signals · grouped by band · Show as one list' + Sort row, rail (Filters card, then Legend/sources-tracked), band-sectioned rows. This spec exists per the operator's own complaint ('the filters were not above the regulations, they were on the right — same on every page; you are NOT matching the images directly') and closes the DEVIATION-LOG's own logged gap (2026-09-06 row 97, partially closed 2026-09-08): the Headline Series card now nests inside the content column between the band tiles and the Sort row, not as its own full-bleed page section.
- Result: 28 MATCH, 0 not matching

> Fixture matches capture-compose-lists-screenshots.mjs's own MARKET_ROWS/SERIES_BOARD (20 signal rows, 4 populated headline series with real Δ1w/Δ1m percentages) — the same shape the evidence screenshot renders, so the audit and the screenshot measure the identical mount.
> The Headline Series card is asserted to mount BEFORE the sort row (DOM order), matching p4's own top-to-bottom order: band tiles, Headline Series, Sort row, band-sectioned rows.
> Market has only 3 Sort options (Next date/Newest/A-Z, no 'My order') — the pre-existing DEVIATION-LOG row for the sort row already covers this; not re-asserted here to avoid duplicating that spec's own coverage.
> Artboard 04's rail is FOUR cards top to bottom: Filters, CARBON COST PER FEU, NEXT DATA DROPS, Legend. The build carried SOURCES TRACKED where the two middle cards belong; lane lists60 (2026-09-08) built both from data the page already loads (the per-corridor carbonCostPerFeu() results page.tsx computes for the CarbonCostOverlay section, and the market_series board buildSeriesBoard() returns) and moved SOURCES TRACKED — an app feature no artboard draws — to after Legend, the last designed card of the column, per ruling R7.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| Market page composition — MarketIntelLedger (real page component, not the ListSurfaceShell primitive) | `display` | `block` | `block` | MATCH |
| Filters rail card mounts before the Legend rail card (rail order: Filters, then Legend) | `display` | `block` | `block` | MATCH |
| Headline Series card is embedded in the content column (mounts, with the ruling-5.1 SectionRule as its first child) | `height` | `3px` | `3px` | MATCH |
| Headline Series card title reads 'Headline series' (artboard copy, not the file's own 'Comparative ribbon' identity) | `text` | `Headline series` | `Headline series` | MATCH |
| Headline Series card mounts before the Sort row (p4 order: band tiles, Headline Series, Sort row, rows) | `display` | `flex` | `flex` | MATCH |
| Sort/count row renders above the rows | `display` | `flex` | `flex` | MATCH |
| rail card 2 is CARBON COST PER FEU, mounted after Filters | `display` | `block` | `block` | MATCH |
| CARBON COST PER FEU head carries the artboard's exact label | `text` | `Carbon cost per FEU` | `Carbon cost per FEU` | MATCH |
| CARBON COST PER FEU carries the artboard's dek line, verbatim | `text` | `EUA / ETS2 / CBAM / UKA cost per forty-foot unit, by corridor.` | `EUA / ETS2 / CBAM / UKA cost per forty-foot unit, by corridor.` | MATCH |
| CARBON COST PER FEU carries the artboard's dek line, verbatim | `font-size` | `12px` | `12px` | MATCH |
| CARBON COST PER FEU lists one row per corridor (artboard 04 draws two) | `count` | `2` | `2` | MATCH |
| a corridor with missing inputs states them as a small-caps pending token, never a fabricated figure | `font-size` | `10.5px` | `10.5px` | MATCH |
| a corridor with missing inputs states them as a small-caps pending token, never a fabricated figure | `font-weight` | `700` | `700` | MATCH |
| a corridor with missing inputs states them as a small-caps pending token, never a fabricated figure | `text-transform` | `uppercase` | `uppercase` | MATCH |
| CARBON COST PER FEU foot names the four missing inputs once, not a GAP box per corridor | `font-size` | `11px` | `11px` | MATCH |
| CARBON COST PER FEU foot names the four missing inputs once, not a GAP box per corridor | `text` | `No emission factor · no corridor distance · no tonnes-per-FEU convention · no carbon price feed. Stated once, here, instead of a four-line GAP box per corridor.` | `No emission factor · no corridor distance · no tonnes-per-FEU convention · no carbon price feed. Stated once, here, instead of a four-line GAP box per corridor.` | MATCH |
| rail card 3 is NEXT DATA DROPS, mounted after CARBON COST PER FEU | `display` | `block` | `block` | MATCH |
| NEXT DATA DROPS head carries the artboard's exact label | `text` | `Next data drops` | `Next data drops` | MATCH |
| NEXT DATA DROPS rows are the artboard's 70px/1fr date+name grid | `display` | `grid` | `grid` | MATCH |
| NEXT DATA DROPS rows are the artboard's 70px/1fr date+name grid | `gap` | `10px` | `10px` | MATCH |
| a NEXT DATA DROPS date is the producer's own registered cadence applied to its latest observed period, in the artboard's 'Mon Sep 7' form | `font-weight` | `700` | `700` | MATCH |
| rail card 4 is Legend, mounted after NEXT DATA DROPS | `display` | `block` | `block` | MATCH |
| region 7a, NEXT DATA DROPS row 1, a single-digit date keeps the artboard's exact 70px track | `grid-template-columns` | `70px 186px` | `70px 186px` | MATCH |
| region 7b, NEXT DATA DROPS row 2, a two-digit date grows its track instead of painting over its sibling | `grid-template-columns` | `75.7031px 180.297px` | `75.7031px 180.297px` | MATCH |
| no content-column facet chip strip above the rows (the original defect) | `(forbidden element)` | `absent` | `absent` | MATCH |
| Headline Series card no longer titled 'Comparative ribbon' (this file's own pre-existing, wrong-for-the-artboard copy) | `(forbidden element)` | `absent` | `absent` | MATCH |
| the literal word UNSCORED anywhere in a row | `(forbidden element)` | `absent` | `absent` | MATCH |
| no trailing 'N active signals across N tracked producers' line below the band cards | `(forbidden element)` | `absent` | `absent` | MATCH |

## Page composition — Market signal detail (05)

- Spec file: `fsi-app/.discipline/rendering/audit/spec/compose-05-market-detail.json`
- Mount: `market-detail-1440` at 1440 px
- Artboards: 05-market-detail
- Design source: docs/design/handoff-2026-09-06/screens/05-market-detail.png; dc.html id="p5"; README §0.5 ("identical architecture to the regulation detail")
- Result: 11 MATCH, 0 not matching

> Region-composition check inside [data-audit="market-detail"] — MarketSignalDetailSurface mounted with fixture data matching artboard 05's Packaging material input costs example (mounts.mjs MARKET_FIXTURE).
> Artboard order top to bottom: header (chips: Signal / severity / topic / band, TIER LAST, header stat, action row) -> exposure grid -> timeline -> sticky section index S1..S6 -> Summary (Generated · 30-second read) -> rail (At a glance, Impact assessment, Relevance, Your notes, In this list, Legend).

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| Page composition — Market signal detail (05) | `count` | `1` | `1` | MATCH |
| region 1 — header chips include the artboard's Signal/severity/topic/band set | `count` | `1` | `1` | MATCH |
| region 2 — action row, exactly Export brief / Share / Watch / + Tag | `count` | `4` | `4` | MATCH |
| region 3 — Exposure grid renders before the timeline | `count` | `1` | `1` | MATCH |
| region 4 — sticky section index carries all six entries, S1..S6 | `count` | `6` | `6` | MATCH |
| region 5 — Summary section aside 'Generated · 30-second read' | `count` | `1` | `1` | MATCH |
| rail order 1 — AT A GLANCE is the first rail card (artboard 05) | `text` | `At a glance` | `At a glance` | MATCH |
| rail order 2 — IMPACT ASSESSMENT is the second rail card (artboard 05) | `text` | `Impact assessment` | `Impact assessment` | MATCH |
| rail order last — LEGEND is present, after the artboard's own page cards (Your notes, In this list) | `count` | `1` | `1` | MATCH |
| no per-item ask bar in the header (ruling 2.3) | `(forbidden element)` | `absent` | `absent` | MATCH |
| the place-keeping card is never the first rail card | `(forbidden element)` | `absent` | `absent` | MATCH |

## Research page composition, ResearchLedger (the real page component, not a shell primitive fed invented props)

- Spec file: `fsi-app/.discipline/rendering/audit/spec/compose-06-research-list.json`
- Mount: `compose-06-research` at 1440 px
- Artboards: 06-research-list
- Design source: dc.html p6 (#p6), region by region: masthead (VOL line, Anton title, '39 active findings · 4 themes · ...' scope line, 'Search findings and themes — or ask ...' placeholder, Ask) -> band tile row -> THEME CARD row (4 across, label + count + '+N new' + one-line description) -> the 'N of M findings · <theme> · Clear theme' + 'WINDOW 7d|30d|90d|All' row -> band section cards, each closed by a foot row ('All N <band> →' left, 'then <next band> · N' right, 'end of list' on the last) with the Awareness transition strip under the section that precedes Awareness -> rail: Filters, Source coverage, Legend.
- Result: 25 MATCH, 0 not matching

> Written for the operator's own complaint ('you are NOT matching the images directly'): this spec measures COMPOSITION, which regions exist, in what document order, with which strings, not one part's pixel values, which the part-level specs (bandtile/listrow/masthead/railcards/statenote) already carry.
> Placement is asserted with sibling-combinator selectors ('A ~ B' matches only when A's node precedes B's), because 'present but in the wrong place' was the original defect, not absence.
> Fixture is invented sample data in the real Resource shape (mounts.mjs, compose-06-research); the audit asserts geometry/placement/labels, never fixture values. Its `added` dates are fixed, not clock-relative, so the Window row's buckets do not drift between runs.
> Three artboard regions have no data path in this corpus and are logged in DEVIATION-LOG.md rather than invented: the rail's VERTICAL facet (no per-item vertical/sector field; workspace_settings.sector_profile is a workspace-level list), the rail's SOURCE CLASS facet, and the Source coverage card's source-class axis (getResearchSourceCoverage returns transport modes). The Source coverage card is asserted here for placement and its artboard geometry, not for the four class labels it cannot have.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| Research page composition, ResearchLedger (the real page component, not a shell primitive fed invented props) | `display` | `grid` | `grid` | MATCH |
| Research page composition, ResearchLedger (the real page component, not a shell primitive fed invented props) | `grid-template-columns` | `* * * *` | `249px 249px 249px 249px` | MATCH |
| Research page composition, ResearchLedger (the real page component, not a shell primitive fed invented props) | `gap` | `12px` | `12px` | MATCH |
| theme cards precede the Window row in the content column (artboard order: band tiles, theme cards, Window row, band sections) | `display` | `flex` | `flex` | MATCH |
| one theme card per theme present, each a real control (the theme facet IS this row) | `count` | `4` | `4` | MATCH |
| one theme card per theme present, each a real control (the theme facet IS this row) | `border-radius` | `10px` | `10px` | MATCH |
| one theme card per theme present, each a real control (the theme facet IS this row) | `padding-top` | `12px` | `12px` | MATCH |
| one theme card per theme present, each a real control (the theme facet IS this row) | `padding-left` | `14px` | `14px` | MATCH |
| the Window row's control label is WINDOW, not SORT (artboard 06 carries the window control where 02/04 carry sort) | `text-transform` | `uppercase` | `uppercase` | MATCH |
| the Window row's control label is WINDOW, not SORT (artboard 06 carries the window control where 02/04 carry sort) | `font-weight` | `700` | `700` | MATCH |
| the Window row offers exactly the artboard's four buckets | `count` | `4` | `4` | MATCH |
| every band section card closes with the foot row (artboard: #FAFAF8 ground, 1px top rule, 10px 16px) | `display` | `flex` | `flex` | MATCH |
| every band section card closes with the foot row (artboard: #FAFAF8 ground, 1px top rule, 10px 16px) | `background-color` | `#FAFAF8` | `rgb(250, 250, 248)` | MATCH |
| every band section card closes with the foot row (artboard: #FAFAF8 ground, 1px top rule, 10px 16px) | `border-top-width` | `1px` | `1px` | MATCH |
| every band section card closes with the foot row (artboard: #FAFAF8 ground, 1px top rule, 10px 16px) | `padding-top` | `10px` | `10px` | MATCH |
| every band section card closes with the foot row (artboard: #FAFAF8 ground, 1px top rule, 10px 16px) | `padding-left` | `16px` | `16px` | MATCH |
| the last band's foot row says 'end of list' | `text` | `end of list` | `end of list` | MATCH |
| a foot row names the next band ('then <band> · N') | `display` | `block` | `block` | MATCH |
| masthead scope line states the live counts the artboard states ('N active findings · N themes · ...') | `display` | `block` | `block` | MATCH |
| rail order, Filters card first | `display` | `block` | `block` | MATCH |
| rail order, Source coverage sits between Filters and Legend (artboard 06 rail, top to bottom) | `display` | `block` | `block` | MATCH |
| no Theme facet group in the rail, the theme cards ARE the theme facet | `(forbidden element)` | `absent` | `absent` | MATCH |
| no content-column facet chip strip above the rows | `(forbidden element)` | `absent` | `absent` | MATCH |
| the literal word UNSCORED anywhere | `(forbidden element)` | `absent` | `absent` | MATCH |
| no theme card row rendered as a second BandTile system | `(forbidden element)` | `absent` | `absent` | MATCH |

## Page composition — Research finding detail (07)

- Spec file: `fsi-app/.discipline/rendering/audit/spec/compose-07-research-detail.json`
- Mount: `research-detail-1440` at 1440 px
- Artboards: 07-research-detail
- Design source: docs/design/handoff-2026-09-06/screens/07-research-detail.png; dc.html id="p7"; README §0.5
- Result: 14 MATCH, 0 not matching

> Region-composition check inside [data-audit="research-detail"] — ResearchFindingDetailSurface mounted with fixture data matching artboard 07's Mission Innovation example (mounts.mjs RESEARCH_FIXTURE).
> Artboard order top to bottom: header (chips, TIER LAST, header stat, action row) -> exposure grid -> timeline -> sticky section index -> Summary (Generated · 30-second read) -> Sources -> rail (At a glance, Impact assessment, Relevance, Connections, Cluster synthesis, Legend).

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| Page composition — Research finding detail (07) | `count` | `1` | `1` | MATCH |
| region 1 — action row, exactly Export brief / Share / Watch / + Tag | `count` | `4` | `4` | MATCH |
| region 2 — Exposure grid renders before the timeline | `count` | `1` | `1` | MATCH |
| region 3 — Summary section (id=summary) renders when no dynamic sections carry content | `text` | `Summary` | `Summary` | MATCH |
| region 6 — CLUSTER SYNTHESIS rail card, artboard 07's own head | `count` | `1` | `1` | MATCH |
| region 6a — CLUSTER SYNTHESIS meta line: N items · density · staleness | `count` | `1` | `1` | MATCH |
| rail order 1 — AT A GLANCE is the first rail card (artboard 07) | `text` | `At a glance` | `At a glance` | MATCH |
| rail order 2 — IMPACT ASSESSMENT is the second rail card (artboard 07) | `text` | `Impact assessment` | `Impact assessment` | MATCH |
| rail order last — LEGEND is present, after the artboard's own page cards (Connections, Cluster synthesis) | `count` | `1` | `1` | MATCH |
| region 1a — chip row order: band pill, Finding, type, theme, modes, then the TIER square last | `count` | `1` | `1` | MATCH |
| no per-item ask bar in the header (ruling 2.3) | `(forbidden element)` | `absent` | `absent` | MATCH |
| At a glance 'Type' row never renders a raw snake_case enum | `(forbidden element)` | `absent` | `absent` | MATCH |
| the place-keeping card is never the first rail card | `(forbidden element)` | `absent` | `absent` | MATCH |
| no 'Synthesis across N items' sentence under the CLUSTER SYNTHESIS head | `(forbidden element)` | `absent` | `absent` | MATCH |

## Operations page composition — OperationsLedger (real page component, not the ListSurfaceShell primitive or the bare RegionDimensionMatrix)

- Spec file: `fsi-app/.discipline/rendering/audit/spec/compose-08-operations-list.json`
- Mount: `compose-08-operations` at 1440 px
- Artboards: 08-operations-list
- Design source: dc.html p8 (#p8): masthead (VOL line, ANTON title, 'N active items · M jurisdictions · six dimensions per region · every fact carries a source and date' scope line, scoped search placeholder, Ask), the four band tiles, the 'REGIONS SIDE BY SIDE' matrix CARD (ruling 5.1 top rule, Anton head + coverage meta, table, foot strip with 'Compare against:' and the dash convention), band cards each closed by a foot strip ('All N <band>' and the transition into the next band), and the rail in order FILTERS (REGION then DIMENSION) → COVERAGE GAPS → LEGEND.
- Result: 24 MATCH, 0 not matching

> This spec measures the COMPOSED page, region by region, in document order — the operator's own complaint was placement, not part styling ('the filters were not above the regulations, they were on the right ... you are NOT matching the images directly'). operations-matrix.json still measures RegionDimensionMatrix alone (six D1-D6 rows, ruling 3.3); this one measures where that matrix SITS and what surrounds it.
> Fixture (mounts.mjs 'compose-08-operations') is invented sample data with the real prop shapes: 25 rows split 0/4/2/19 across the bands, 5 regions, populated region_dimension_coverage + regional_data_facts rows. The audit asserts geometry, placement and the components' own literals, never fixture values.
> The runner reads computed STYLE and textContent only: an `expect` key naming an HTML attribute (e.g. an input's placeholder) measures as null and reports a false MATCH, so no such key appears here. The page-scoped command-bar prompt artboard 08 states is verified in the side-by-side evidence PNG (docs/design/handoff-2026-09-06/built/compose-08-operations-list.png), not by this spec.
> Placement assertions use sibling-combinator selectors, which match only when the first element's DOM node PRECEDES the second's — 'present but in the wrong place' was the defect, so absence-only checks would not have caught it.
> R7 (an app feature the artboard does not draw is kept, listed, and moved out of an artboard region's position): the US By-state cost sub-list moved from between the matrix and the first band card to the disclosure position at the foot of the content column; the Mode facet stays but sits after the artboard's own REGION and DIMENSION groups. Both are checked below.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| Operations page composition — OperationsLedger (real page component, not the ListSurfaceShell primitive or the bare RegionDimensionMatrix) | `display` | `block` | `block` | MATCH |
| matrix card carries ruling 5.1's graduated top rule (it was a bare section with a 15px h2) | `height` | `3px` | `3px` | MATCH |
| matrix card carries ruling 5.1's graduated top rule (it was a bare section with a 15px h2) | `count` | `1` | `1` | MATCH |
| matrix card head title is the Anton section title, 20px uppercase | `font-size` | `20px` | `20px` | MATCH |
| matrix card head title is the Anton section title, 20px uppercase | `text-transform` | `uppercase` | `uppercase` | MATCH |
| matrix card head title is the Anton section title, 20px uppercase | `letter-spacing` | `0.04em (= 0.8px at 20px)` | `0.8px` | MATCH |
| matrix card foot strip exists and states the dash convention (artboard p8 foot: the 'Compare against' control plus the no-data note) | `display` | `flex` | `flex` | MATCH |
| the 'Compare against' control sits in the card FOOT, not above the table (it moved; DOM order is the check) | `display` | `flex` | `flex` | MATCH |
| the matrix card precedes the first band card's foot strip — the matrix is FIRST in the content column, above the band cards | `display` | `flex` | `flex` | MATCH |
| every band card is closed by a foot strip carrying the transition into the next band (or 'end of list' on the last) | `count` | `3` | `3` | MATCH |
| the last band card's foot strip says 'end of list' | `display` | `flex` | `flex` | MATCH |
| rail FILTERS card mounts before the COVERAGE GAPS card (p8 rail order: Filters, Coverage gaps, Legend) | `display` | `block` | `block` | MATCH |
| rail COVERAGE GAPS card mounts before the LEGEND card | `display` | `block` | `block` | MATCH |
| rail facet order is REGION then DIMENSION (p8's own two groups, in p8's order) | `display` | `block` | `block` | MATCH |
| the R7 extra (Mode, which p8's rail does not draw) follows both artboard groups, never ahead of one | `display` | `block` | `block` | MATCH |
| FILTERS card carries the DIMENSION group (p8 rail: D1 Regulatory feasibility ... D6 Operational cost) | `display` | `block` | `block` | MATCH |
| all six dimensions D1-D6 render in the rail with no '+ N more' disclosure hiding D6 | `display` | `flex` | `flex` | MATCH |
| COVERAGE GAPS card carries counted gap rows, not prose alone | `display` | `block` | `block` | MATCH |
| masthead scope line carries p8's own standing sentence after the live counts | `display` | `block` | `block` | MATCH |
| no content-column facet chip strip above the rows | `(forbidden element)` | `absent` | `absent` | MATCH |
| no per-page ask panel on the list surface | `(forbidden element)` | `absent` | `absent` | MATCH |
| the literal word UNSCORED as page text | `(forbidden element)` | `absent` | `absent` | MATCH |
| no 'Facts' label in the expanded dimension row's leading cell | `(forbidden element)` | `absent` | `absent` | MATCH |
| no trailing 'N regional operations profiles across N regions' line below the band cards | `(forbidden element)` | `absent` | `absent` | MATCH |

## Page composition — Operations profile detail (09)

- Spec file: `fsi-app/.discipline/rendering/audit/spec/compose-09-operations-profile.json`
- Mount: `operations-detail-1440` at 1440 px
- Artboards: 09-operations-profile
- Design source: docs/design/handoff-2026-09-06/screens/09-operations-profile.png; dc.html id="p9"; README §0.5
- Result: 14 MATCH, 0 not matching

> Region-composition check inside [data-audit="operations-detail"] — OperationsDetailSurface mounted with fixture data matching artboard 09's Singapore example (mounts.mjs OPERATIONS_FIXTURE).
> Artboard order top to bottom: header (chips incl. the region-group chip and trailing topic chip 'Corridors', TIER LAST, header stat, action row) -> exposure grid -> timeline -> sticky section index -> Summary (Generated · 30-second read) -> Sources -> rail (At a glance, Impact assessment, Relevance, Related in <region>, Legend).

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| Page composition — Operations profile detail (09) | `count` | `1` | `1` | MATCH |
| region 1 — action row, exactly Export brief / Share / Watch / + Tag | `count` | `4` | `4` | MATCH |
| region 2 — header carries the trailing topic chip ('Corridors') | `count` | `1` | `1` | MATCH |
| region 3 — Exposure grid renders before the timeline | `count` | `1` | `1` | MATCH |
| region 4 — Summary section (id=summary) renders when no dynamic sections carry content | `text` | `Summary` | `Summary` | MATCH |
| region 5 — At a glance 'Region' row is region group + humanized country, never a raw ISO code | `count` | `1` | `1` | MATCH |
| region 6 — RELATED IN <region> rail card, artboard 09's own head | `count` | `1` | `1` | MATCH |
| region 6a — RELATED IN <region> carries one row per related profile | `count` | `3` | `3` | MATCH |
| rail order 1 — AT A GLANCE is the first rail card (artboard 09) | `text` | `At a glance` | `At a glance` | MATCH |
| rail order 2 — IMPACT ASSESSMENT is the second rail card (artboard 09) | `text` | `Impact assessment` | `Impact assessment` | MATCH |
| rail order last — LEGEND is present, after the artboard's own page cards (Related in <region>) | `count` | `1` | `1` | MATCH |
| region 2a — chip row order: band pill, Regional profile, region group, modes, topic, then the TIER square last | `count` | `1` | `1` | MATCH |
| no per-item ask bar in the header (ruling 2.3) | `(forbidden element)` | `absent` | `absent` | MATCH |
| the place-keeping card is never the first rail card | `(forbidden element)` | `absent` | `absent` | MATCH |

## Page composition — Map (10)

- Spec file: `fsi-app/.discipline/rendering/audit/spec/compose-10-map.json`
- Mount: `compose-map` at 1440 px
- Artboards: 10-map
- Design source: docs/design/handoff-2026-09-06/screens/10-map.png (artboard image is the authority per SETUP); dc.html id="p10"; README screen 10
- Result: 35 MATCH, 0 not matching

> Region-composition check for the full page: AppShell (Sidebar + TopBar) + Masthead + MapPageView, all real components, mounted with populated fixture data via .discipline/rendering/audit/mounts.mjs['compose-map'] — no live Supabase project reachable in this sandbox (DEVIATION-LOG.md).
> Artboard order top to bottom: Masthead (VOL line, REGULATORY MAP title, dek, page-scoped command bar) -> Mode/Band/Region filter row (ABOVE the map, not beside it — operator audit item, closed) -> Regulatory map card (map canvas + in-card KEY legend) -> Jurisdiction register card (table: JURISDICTION / ACTIVE THEMES / HIGHEST BAND / ITEMS, one ListRow per jurisdiction) -> Immediate strip foot -> rail: Immediate jurisdictions card, Coverage gaps card, Legend card, in that order alongside the two left-column cards (not below them).
> Command bar placeholder is page-scoped: 'Search a jurisdiction — or ask "where are my immediate items?"' (dc.html p10 exact text) — this lane added the missing `placeholder` prop to src/app/map/page.tsx's Masthead call; before this lane it fell back to the generic 'Search or ask across N items…' copy.
> The KEY box's fixture rows are limited to the mount's fixture data (6 resources across 4 jurisdictions) — the artboard shows 7 jurisdictions / 976 items from example data, never shipped (README: sample data in the mock is never shipped).
> LANE map60 (2026-09-08), the four items from addendum 86 postscript 14's fold report on this artboard. (a) THE EMPTY CANVAS WAS THE FIXTURE, not the product [CONFIRMED by DOM measurement]: the mount aliases `leaflet/dist/leaflet.css` to an empty module so esbuild needs no output path for it, and nothing put the real stylesheet back, so Leaflet's four markers were built with correct transforms but laid out in normal flow ~2700px below the card. mounts.mjs now carries `styleFiles` and both run-audit.mjs and capture-compose-page.mjs inject it, the same technique ../smoke/map-smoke.mjs already used. leaflet is an existing app dependency; no external dependency and no network is added. (b) MODE and BAND now share the first line, REGION the second, because the filter row is `grid-column:1/-1` in the content grid at `gap:10px` with `flex-wrap:wrap`, as dc.html p10 has it. (c) The register carries p10's own column headers and per-row arrow through ListRowColumnHeader/ListRow's additive `variant="register"`. (d) The KEY's four band entries read '<Band> present' on uniform 8px dots.
> NO BASEMAP TILES in this mount, and none in any sandbox capture: the tile layer is api-external (tile.openstreetmap.org) and every request fails with ERR_TUNNEL_CONNECTION_FAILED behind the egress proxy. That is the environment, not the page, the artboard itself draws a hatched placeholder captioned 'BASEMAP · light, desaturated, no labels below country level' rather than a real basemap. No static fallback exists in the app and none was added (a new tile asset would be a new external dependency). Logged in DEVIATION-LOG.md.
> CSS VARIABLES DO NOT RESOLVE in this mount (the compose-* mounts carry no STYLE_INJECT; capture-compose-page.mjs injects the compiled CSS for SCREENSHOTS only), so every `var(--fs-*)`/`var(--ink*)` value reads as the browser initial. The rows below therefore assert GEOMETRY (grid tracks, heights, padding, span widths) and TEXT, never token-derived colour or type, those are asserted on the token-styled mounts (map-register.json, listrow.json).

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| Page composition — Map (10) | `count` | `1` | `1` | MATCH |
| region 1a — command bar page-scoped placeholder (dc.html p10 exact text) | `count` | `1` | `1` | MATCH |
| region 3 — Regulatory map card canvas | `count` | `1` | `1` | MATCH |
| region 4 — Jurisdiction register rows, after the map card | `count` | `1` | `1` | MATCH |
| region 2a, filter row spans the full content grid (grid-column 1/-1), one flex row, 10px gap | `display` | `flex` | `flex` | MATCH |
| region 2a, filter row spans the full content grid (grid-column 1/-1), one flex row, 10px gap | `flex-wrap` | `wrap` | `wrap` | MATCH |
| region 2a, filter row spans the full content grid (grid-column 1/-1), one flex row, 10px gap | `gap` | `10px` | `10px` | MATCH |
| region 2a, filter row spans the full content grid (grid-column 1/-1), one flex row, 10px gap | `padding` | `0px 4px` | `0px 4px` | MATCH |
| region 2a, filter row spans the full content grid (grid-column 1/-1), one flex row, 10px gap | `width` | `1092px` | `1092px` | MATCH |
| region 2b, MODE and BAND on ONE line, REGION on the second (two-line row, 70px) | `height` | `67px` | `67px` | MATCH |
| region 3a, the map canvas is 420px tall (dc.html p10) | `height` | `420px` | `420px` | MATCH |
| region 3b, markers RENDER on the canvas (the empty-canvas regression) | `count` | `4` | `4` | MATCH |
| region 3d, in-canvas KEY box, dc.html p10 measures | `padding` | `10px 12px` | `10px 12px` | MATCH |
| region 3d, in-canvas KEY box, dc.html p10 measures | `border-radius` | `8px` | `8px` | MATCH |
| region 3d, in-canvas KEY box, dc.html p10 measures | `gap` | `5px` | `5px` | MATCH |
| region 3d, in-canvas KEY box, dc.html p10 measures | `font-size` | `11px` | `11px` | MATCH |
| region 4a, register column header row, p10's own grid and labels | `grid-template-columns` | `3px 221.5px 221.5px 110px 80px 40px` | `3px 221.5px 221.5px 110px 80px 40px` | MATCH |
| region 4a, register column header row, p10's own grid and labels | `height` | `30px` | `30px` | MATCH |
| region 4a, register column header row, p10's own grid and labels | `padding` | `0px 16px 0px 0px` | `0px 16px 0px 0px` | MATCH |
| region 4b, register rows on the same six-column grid, 44px | `count` | `4` | `4` | MATCH |
| region 4b, register rows on the same six-column grid, 44px | `grid-template-columns` | `3px 221.5px 221.5px 110px 80px 40px` | `3px 221.5px 221.5px 110px 80px 40px` | MATCH |
| region 4b, register rows on the same six-column grid, 44px | `min-height` | `44px` | `44px` | MATCH |
| region 4b, register rows on the same six-column grid, 44px | `padding` | `0px 16px 0px 0px` | `0px 16px 0px 0px` | MATCH |
| region 4c, per-row arrow glyph, one per row | `count` | `4` | `4` | MATCH |
| region 4d, the whole row is the one click target (one <a> per row, no second target) | `count` | `4` | `4` | MATCH |
| region 5a, rail card 1 is the Immediate jurisdictions card (shared RailCard) | `count` | `1` | `1` | MATCH |
| region 6a, rail card 2 is Coverage gaps (shared RailCard) | `count` | `1` | `1` | MATCH |
| region 7a, rail card 3 is the SHARED Legend card, not a page-local copy | `count` | `1` | `1` | MATCH |
| no floating Ask AI button (ruling 2.2) | `(forbidden element)` | `absent` | `absent` | MATCH |
| no second per-page ask/search bar (README §0.3) | `(forbidden element)` | `absent` | `absent` | MATCH |
| no page-local register table beside the shared ListRow (rule 13) | `(forbidden element)` | `absent` | `absent` | MATCH |
| the KEY never labels a band without the artboard's 'present' wording | `(forbidden element)` | `absent` | `absent` | MATCH |
| map canvas, every marker is INSIDE the canvas box (the empty-canvas regression) | `(cell containment)` | `no cell overlaps a sibling or exceeds its row` | `clean across 1 row(s)` | MATCH |
| filter row, the three groups never overlap each other | `(cell containment)` | `no cell overlaps a sibling or exceeds its row` | `clean across 1 row(s)` | MATCH |
| register row, no cell escapes its row or overlaps a sibling | `(cell containment)` | `no cell overlaps a sibling or exceeds its row` | `clean across 4 row(s)` | MATCH |

## Watchlist page composition — the real WatchlistSurface (not a shell primitive fed invented props)

- Spec file: `fsi-app/.discipline/rendering/audit/spec/compose-11-watchlist.json`
- Mount: `compose-11-watchlist` at 1440 px
- Artboards: 11-watchlist
- Design source: dc.html p11 (#p11): masthead (VOL line, WATCHLIST, scope line 'N watched · personal · M shared by the workspace', command bar placeholder 'Search your watchlist — or ask "what changed on my watched items?"'), then a 1fr/300px body — content column: Watched card (SectionHeading 'Watched · N' + 'Sorted by next date', column header Juris./Title · type · modes/Impact low → high/Next date/Timeline/Tier, rows, CardFoot + 'Browse regulations →', the changed-since-last-visit state note) then the Recalculation notices card; rail: Filters, Share with workspace, Legend.
- Result: 37 MATCH, 0 not matching

> This spec exists per the operator's own composition complaint ('you are NOT matching the images directly'): it measures the page REGION BY REGION IN DOCUMENT ORDER, so a future regression that drops the column header, drops the card foot, drops the notices card, or puts the filters back into the content column is caught here rather than in a screenshot review.
> Placement is asserted with sibling-combinator selectors wherever 'present but in the wrong place' is the defect being guarded — order, not mere presence.
> Artboard 11's rail draws only two cards. The Filters card is the app's own scope/type/tag facets, which the artboard does not draw at all: ruling R7 keeps the feature and the operator's 2026-09-07 filters ruling puts it in the rail, so it is asserted here in rail position 1 exactly as on the other four list surfaces. Logged in DEVIATION-LOG.md.
> Fixture data is invented with the real prop shape (6 WatchlistItems, 4 notices, 2 workspace tags) and is never shipped and never asserted on; the assertions are geometry, placement and the components' own static labels.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| Watchlist page composition — the real WatchlistSurface (not a shell primitive fed invented props) | `display` | `block` | `block` | MATCH |
| masthead scope line ('N watched · personal · M shared by the workspace') is present | `display` | `inline` | `inline` | MATCH |
| Watched card head is the shared SectionHeading: Anton 20px, .04em, uppercase | `font-size` | `20px` | `20px` | MATCH |
| Watched card head is the shared SectionHeading: Anton 20px, .04em, uppercase | `letter-spacing` | `0.04em (= 0.8px at 20px)` | `0.8px` | MATCH |
| Watched card head is the shared SectionHeading: Anton 20px, .04em, uppercase | `text-transform` | `uppercase` | `uppercase` | MATCH |
| the head's right-hand aside is 10.5px/.12em uppercase muted | `font-size` | `10.5px` | `10.5px` | MATCH |
| the head's right-hand aside is 10.5px/.12em uppercase muted | `letter-spacing` | `0.12em (= 1.26px at 10.5px)` | `1.26px` | MATCH |
| the head's right-hand aside is 10.5px/.12em uppercase muted | `text-transform` | `uppercase` | `uppercase` | MATCH |
| the head's right-hand aside is 10.5px/.12em uppercase muted | `color` | `#7A6E6C` | `rgb(122, 110, 108)` | MATCH |
| the column header row exists inside the Watched card, on the row grid | `grid-template-columns` | `3px 56px * 88px 84px 76px 40px 44px` | `3px 56px 529px 88px 84px 76px 40px 44px` | MATCH |
| the column header row exists inside the Watched card, on the row grid | `height` | `30px` | `30px` | MATCH |
| the column header row exists inside the Watched card, on the row grid | `padding-right` | `12px` | `12px` | MATCH |
| the column header row precedes the first row (header above rows, not below) | `display` | `grid` | `grid` | MATCH |
| the header's date column is labelled 'Next date' (not the dashboard's 'Due') | `text-align` | `right` | `right` | MATCH |
| the header's title column carries artboard 11's own label | `text-transform` | `uppercase` | `uppercase` | MATCH |
| the card foot strip sits on the page tint with the header-weight divider above it | `background-color` | `#FAFAF8` | `rgb(250, 250, 248)` | MATCH |
| the card foot strip sits on the page tint with the header-weight divider above it | `font-size` | `12px` | `12px` | MATCH |
| the card foot strip sits on the page tint with the header-weight divider above it | `border-top-width` | `1px` | `1px` | MATCH |
| the card foot strip sits on the page tint with the header-weight divider above it | `border-top-color` | `rgba(0,0,0,.08)` | `rgba(0, 0, 0, 0.08)` | MATCH |
| the card foot strip sits on the page tint with the header-weight divider above it | `padding-left` | `16px` | `16px` | MATCH |
| the card foot strip sits on the page tint with the header-weight divider above it | `padding-top` | `10px` | `10px` | MATCH |
| the card foot carries the 'Browse regulations →' link on its right | `font-weight` | `600` | `600` | MATCH |
| the changed-since-last-visit state note sits INSIDE the Watched card, after its foot | `display` | `block` | `block` | MATCH |
| the Recalculation notices card exists and follows the Watched card | `display` | `block` | `block` | MATCH |
| the notices card's head is the same shared SectionHeading, with the artboard's aside | `font-size` | `10.5px` | `10.5px` | MATCH |
| the notices card's head is the same shared SectionHeading, with the artboard's aside | `letter-spacing` | `0.12em (= 1.26px at 10.5px)` | `1.26px` | MATCH |
| rail card 1 is Filters (the app's own facets, relocated out of the content column) | `display` | `block` | `block` | MATCH |
| rail order: Filters before Share with workspace | `display` | `block` | `block` | MATCH |
| rail order: Share with workspace before Legend (artboard 11's own rail order) | `display` | `block` | `block` | MATCH |
| the row's trailing control is a real 44px target inside the 44px cell (law-2) | `width` | `44px` | `44px` | MATCH |
| the row's trailing control is a real 44px target inside the 44px cell (law-2) | `height` | `44px` | `44px` | MATCH |
| every row ends in the artboard's ⋯ overflow glyph, the same control the Regulations rows use | `width` | `44px` | `44px` | MATCH |
| every row ends in the artboard's ⋯ overflow glyph, the same control the Regulations rows use | `height` | `44px` | `44px` | MATCH |
| no filter control in the content column (native selects) | `(forbidden element)` | `absent` | `absent` | MATCH |
| no filter chip strip in the content column | `(forbidden element)` | `absent` | `absent` | MATCH |
| the literal word UNSCORED anywhere on the page | `(forbidden element)` | `absent` | `absent` | MATCH |
| no floating Ask control | `(forbidden element)` | `absent` | `absent` | MATCH |

## Page composition — Community (12)

- Spec file: `fsi-app/.discipline/rendering/audit/spec/compose-12-community.json`
- Mount: `compose-community` at 1440 px
- Artboards: 12-community
- Design source: docs/design/handoff-2026-09-06/screens/12-community.png (artboard image is the authority per SETUP); dc.html id="p12"; README screen 12
- Result: 46 MATCH, 0 not matching

> Region-composition check for the full page: AppShell (Sidebar + TopBar) + Masthead + CommunityRooms, real components, populated fixture data via .discipline/rendering/audit/mounts.mjs['compose-community'] — no live Supabase project reachable in this sandbox (DEVIATION-LOG.md).
> Artboard order, content column top to bottom: room tile grid (FIXED 4 columns, 7 rooms + the dashed '+ New vertical group' tile as the grid's own 8th slot) -> the ROOM INDEX card (SectionRule, Anton head '<ROOM> ROOM' with the small-caps aside, the JURIS. / DISCUSSION / REPLIES / LAST ACTIVITY table, the foot strip with 'Start a discussion') -> the NEW POST card. Rail, top to bottom: Who's here, Verifier sign-off, Why post here.
> Lane community60 (2026-09-08) closed six listed differences: the extra 'REGIONAL ROOMS' heading above the tiles is gone (the artboard has no such heading; its stat line is the masthead dek); tiles carry the room NAME, not the 3-char key; the stacked discussion feed became the artboard's TABLE, built on the shared RowTable (extended additively with a `metrics` prop for p12's 14px pad / 56px rows / trailing rule, plus per-row `id`/`onActivate`/`below`); the composer left the discussions card and became its own NEW POST card at the foot of the content column; the room header + 'Live in this region' card left its position between the tiles and the discussions for R7 placement after the last designed region; and the rail order is now the artboard's, with the R7 Vertical groups card after it.
> R8 holds: the thread PAGE is not built. The table is the room index and a row expands in place (the `#post-<id>` anchor the rail already emitted is now carried by the table row, so those links still resolve).
> Absence, logged in DEVIATION-LOG: the artboard's per-row QUESTION / PRACTICE / MARKET / TEMPLATE tag chip has no data path (community_posts, migration 030, has no kind/topic/tag column), so the Absence convention renders in its slot.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| Page composition — Community (12) | `count` | `1` | `1` | MATCH |
| region 1 — command bar page-scoped placeholder (dc.html p12 exact text) | `count` | `1` | `1` | MATCH |
| region 2 — room tile grid is fixed 4 columns, not auto-fit | `count` | `1` | `1` | MATCH |
| region 2b — tile name type (dc.html p12: 11px / .1em / uppercase / 800) | `font-size` | `11px` | `11px` | MATCH |
| region 2b — tile name type (dc.html p12: 11px / .1em / uppercase / 800) | `font-weight` | `800` | `800` | MATCH |
| region 2b — tile name type (dc.html p12: 11px / .1em / uppercase / 800) | `letter-spacing` | `0.1em (= 1.1px at 11px)` | `1.1px` | MATCH |
| region 2b — tile name type (dc.html p12: 11px / .1em / uppercase / 800) | `text-transform` | `uppercase` | `uppercase` | MATCH |
| region 3 — the room index card, directly after the tile grid | `count` | `1` | `1` | MATCH |
| region 3a — its 3px section rule (ruling 5.1, above the head, no rule below the title) | `height` | `3px` | `3px` | MATCH |
| region 3b — Anton head reads '<ROOM> ROOM' | `text` | `Global room` | `Global room` | MATCH |
| region 3b — Anton head reads '<ROOM> ROOM' | `text-transform` | `uppercase` | `uppercase` | MATCH |
| region 3b — Anton head reads '<ROOM> ROOM' | `letter-spacing` | `0.04em (= 0.8px at 20px)` | `0.8px` | MATCH |
| region 3d — table column 1 header is JURIS. | `text` | `Juris.` | `Juris.` | MATCH |
| region 3e — table column 2 header is DISCUSSION | `text` | `Discussion` | `Discussion` | MATCH |
| region 3f — table column 3 header is REPLIES | `text` | `Replies` | `Replies` | MATCH |
| region 3g — table column 4 header is LAST ACTIVITY | `text` | `Last activity` | `Last activity` | MATCH |
| region 3h — header strip geometry and type (dc.html p12: 30px, .12em, 700; the 9.5px size is var(--fs-95), which run-audit's page does not load theme.css for) | `height` | `30px` | `30px` | MATCH |
| region 3h — header strip geometry and type (dc.html p12: 30px, .12em, 700; the 9.5px size is var(--fs-95), which run-audit's page does not load theme.css for) | `font-weight` | `700` | `700` | MATCH |
| region 3h — header strip geometry and type (dc.html p12: 30px, .12em, 700; the 9.5px size is var(--fs-95), which run-audit's page does not load theme.css for) | `letter-spacing` | `0.12em (= 1.14px at 9.5px)` | `1.14px` | MATCH |
| region 3h — header strip geometry and type (dc.html p12: 30px, .12em, 700; the 9.5px size is var(--fs-95), which run-audit's page does not load theme.css for) | `text-transform` | `uppercase` | `uppercase` | MATCH |
| region 3i — a discussion row carries the `#post-<id>` anchor the rail links to | `count` | `3` | `3` | MATCH |
| region 3i — a discussion row carries the `#post-<id>` anchor the rail links to | `min-height` | `56px` | `56px` | MATCH |
| region 3j — the whole row is the click target (DP-1), keyboard reachable | `count` | `3` | `3` | MATCH |
| region 3l — every row ends in the 44px overflow control | `count` | `3` | `3` | MATCH |
| region 3l — every row ends in the 44px overflow control | `width` | `44px` | `44px` | MATCH |
| region 3l — every row ends in the 44px overflow control | `height` | `44px` | `44px` | MATCH |
| region 4 — the NEW POST card, its own card after the room index | `count` | `1` | `1` | MATCH |
| region 4a — its Anton head reads 'NEW POST · <ROOM>' | `text` | `New post · Global` | `New post · Global` | MATCH |
| region 4a — its Anton head reads 'NEW POST · <ROOM>' | `text-transform` | `uppercase` | `uppercase` | MATCH |
| region 4b — its aside reads 'POSTS TO THE <ROOM> ROOM' | `text` | `Posts to the Global room` | `Posts to the Global room` | MATCH |
| region 4b — its aside reads 'POSTS TO THE <ROOM> ROOM' | `text-transform` | `uppercase` | `uppercase` | MATCH |
| region 4c — the composer is a post box with the artboard's placeholder | `count` | `1` | `1` | MATCH |
| region 4c — the composer is a post box with the artboard's placeholder | `placeholder` | `Ask the Global room — a lane observation, a handler question, a document worth sharing…` | `Ask the Global room — a lane observation, a handler question, a document worth sharing…` | MATCH |
| region 4c — the composer is a post box with the artboard's placeholder | `min-height` | `64px` | `64px` | MATCH |
| region 5 — R7: the room header + Live-in-this-region card sits AFTER the new-post card | `count` | `1` | `1` | MATCH |
| rail 1 — Who's here is the rail's first card | `count` | `1` | `1` | MATCH |
| rail 2 — Verifier sign-off is the rail's second card, directly after Who's here | `count` | `1` | `1` | MATCH |
| rail 3 — Why post here is the rail's third card, directly after Verifier sign-off | `count` | `1` | `1` | MATCH |
| no 'REGIONAL ROOMS' heading above the tiles (artboard 12 draws none) | `(forbidden element)` | `absent` | `absent` | MATCH |
| no room tile labelled with the 3-char key | `(forbidden element)` | `absent` | `absent` | MATCH |
| the composer is not inside the room index card | `(forbidden element)` | `absent` | `absent` | MATCH |
| the discussion list is not a stacked card feed | `(forbidden element)` | `absent` | `absent` | MATCH |
| no floating Ask AI button (ruling 2.2) | `(forbidden element)` | `absent` | `absent` | MATCH |
| no second per-page ask/search bar (README §0.3) | `(forbidden element)` | `absent` | `absent` | MATCH |
| the literal word UNSCORED never appears (ruling 2.1) | `(forbidden element)` | `absent` | `absent` | MATCH |
| no discussion-table cell escapes its row or overlaps a sibling (operator, 2026-09-07) | `(cell containment)` | `no cell overlaps a sibling or exceeds its row` | `clean across 3 row(s)` | MATCH |

## Page composition, Admin (13)

- Spec file: `fsi-app/.discipline/rendering/audit/spec/compose-13-admin.json`
- Mount: `compose-admin` at 1440 px
- Artboards: 13-admin
- Design source: docs/design/handoff-2026-09-06/screens/13-admin.png (artboard image is the authority per SETUP); dc.html id="p13"; README screen 13
- Result: 33 MATCH, 0 not matching

> Region-composition check for the full page: AppShell + the real AdminDashboard (its own internal Masthead), populated fixture data via .discipline/rendering/audit/mounts.mjs['compose-admin'], no live Supabase project reachable in this sandbox (DEVIATION-LOG.md). The mount now seeds dc.html p13's own four provisional rows as real ProvisionalSource records, so the default landing region renders with data rather than its empty state.
> Artboard order, top to bottom: Masthead -> 8-tile stat grid (Workspaces/Sources/Ingest/Coverage/Research pipeline/Community pickups/Runtime/Emission factors), Sources tile SELECTED -> Sources sub-tab bar, 'Provisional review' FIRST and active -> the SOURCES · PROVISIONAL REVIEW card (head, SOURCE/TIER/STATUS/DISCOVERED table with a per-row Approve and the trailing overflow, legend foot, pipeline note) -> Organizations card. Rail, top to bottom: Issues queue, the Companies/Individuals/Newest join/Active-this-month card, Read-only controls.
> Lane comp-admin (2026-09-08) closed the region the previous pass logged as NOT FIXED: SourceHealthDashboard's Provisional view rendered a 'Source Intelligence' module (its own header, a B.2 progress bar, a Source Tiers legend, a T1-T7 tile grid, a SECOND tab bar, a source search) where the artboard draws one table card. That view is now the artboard's card, built from the shared parts (SectionRule + the new shared ui/RowTable + TierChip + StateNote + Absence); the module's other views (registry/health/canonical/intersections/themes/obligations), which have no artboard, are untouched. The forbid list below is what stops the old composition coming back.
> Actions stay real: Approve/Reject/Re-tier all POST /api/admin/sources/promote, the endpoint the review flow already owned (approve/reject/defer). 'Re-tier = stays provisional' is the artboard's own legend and is exactly what decision 'defer' does, there is no in-place tier write for a provisional row (commit-tier-change returns 409 for kind:"provisional").
> Two artboard details are deliberately NOT asserted, both logged in DEVIATION-LOG.md 2026-09-08: the foot's 'All 489 provisional' LINK (the built card is the whole queue, so a link to a fuller list would be a dead control, ruling 1.1, and the text renders the live count instead), and the rail's 'Export queue' button (no queue-export endpoint exists anywhere under src/app/api/admin/; not drawn dead, not invented).
> Lane admin60 (2026-09-08) closed the three differences the train-59 fold listed against this artboard. (1) The Sources sub-tab row now sits INSIDE the provisional card's head, on ONE line at 1440, as dc.html p13 draws it: the shared TabRow gained a `placement="card-head"` variant (gap 2, the card's own 16px inset, never wrapping) and a `semantics="tablist"` mode that keeps the role=tab semantics the page always had, and the page passes that one row into ProvisionalReviewTable's new `headTabs` slot instead of rendering a second one above the card. The tab counts render as the artboard's inline '· N' text, not a red pill. (2) ORGANIZATIONS is rebuilt on the shared RowTable with dc.html p13's own track list (1fr 120px 110px 100px 120px 44px), the header and the rows used to be two DIFFERENT grids whose row minimum (~858px) exceeded the content column, which is why LAST ACTIVITY was clipped at the card edge. (3) The ROLES column the artboard does not draw is gone; its data renders in the artboard's own MEMBERS cell, '2 · owners'.
> Also this lane: the eight stat tiles carry dc.html p13's own sub-lines verbatim (they had drifted into longer engineering prose), the ISSUES QUEUE rows are in the artboard's order rather than the store's, and PlateCard's head is the artboard's Anton-20 head over a SectionRule instead of the 12.5px bold plate header it used to draw.
> Still NOT asserted, both logged in DEVIATION-LOG.md: the foot's 'All 489 provisional' LINK (the built card is the whole queue, so a link to a fuller list would be a dead control, ruling 1.1) and the rail's 'Export queue' button, re-checked this lane by grepping src/app/api and src/lib for a queue-export function: there is none anywhere, so it is not built (ruling 1.1). A forbid row below keeps a dead one from appearing.
> The ORGANIZATIONS row's trailing 44px cell is held open and EMPTY: the artboard draws a ⋯ glyph there but the app has no per-org row action, and a control with no function behind it is a defect (ruling 1.1).

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| Page composition, Admin (13) | `count` | `1` | `1` | MATCH |
| region 1, 8-tile stat grid present | `count` | `1` | `1` | MATCH |
| region 2, Sources sub-tab bar, 'Provisional review' first and active | `count` | `1` | `1` | MATCH |
| region 3, the SOURCES · PROVISIONAL REVIEW card exists, exactly one, and is the Sources body | `count` | `1` | `1` | MATCH |
| region 3a, card head carries the artboard's title verbatim | `count` | `1` | `1` | MATCH |
| region 3b, card head's right meta is the artboard's line (count is live, the rest verbatim) | `count` | `1` | `1` | MATCH |
| region 3e, every row carries its own Approve action (DP-1: every action on a source is on its row) | `count` | `4` | `4` | MATCH |
| region 3f, every row carries the trailing overflow control, a 44px hit target | `width` | `44px` | `44px` | MATCH |
| region 3f, every row carries the trailing overflow control, a 44px hit target | `height` | `44px` | `44px` | MATCH |
| region 3f, every row carries the trailing overflow control, a 44px hit target | `count` | `4` | `4` | MATCH |
| region 4, legend foot, the artboard's sentence verbatim | `count` | `1` | `1` | MATCH |
| region 5, pipeline state note at the card foot | `count` | `1` | `1` | MATCH |
| region 6, Organizations card, stacked directly below the provisional card | `count` | `1` | `1` | MATCH |
| region 7, rail, Issues queue card first | `count` | `1` | `1` | MATCH |
| region 8, rail, the Companies/Individuals/Newest join/Active-this-month card | `count` | `1` | `1` | MATCH |
| region 8a, that rail card mounts BEFORE the read-only-controls card (rail order) | `count` | `1` | `1` | MATCH |
| region 9, rail, Read-only controls explainer, the artboard's sentence verbatim | `count` | `1` | `1` | MATCH |
| region 2a, the sub-tab row is INSIDE the provisional card, and there is exactly one of it on the page | `count` | `1` | `1` | MATCH |
| region 2a, the sub-tab row is INSIDE the provisional card, and there is exactly one of it on the page | `flex-wrap` | `nowrap` | `nowrap` | MATCH |
| region 2a, the sub-tab row is INSIDE the provisional card, and there is exactly one of it on the page | `padding-left` | `16px` | `16px` | MATCH |
| region 2a, the sub-tab row is INSIDE the provisional card, and there is exactly one of it on the page | `padding-right` | `16px` | `16px` | MATCH |
| region 6a, ORGANIZATIONS uses dc.html p13's own track list, header and rows alike | `grid-template-columns` | `* 120px 110px 100px 120px 44px` | `174px 120px 110px 100px 120px 44px` | MATCH |
| region 6b, the ORGANIZATIONS header carries exactly the artboard's five labels, no ROLES | `text` | `NameSlugPlanMembersLast activity` | `NameSlugPlanMembersLast activity` | MATCH |
| no floating Ask AI button (ruling 2.2) | `(forbidden element)` | `absent` | `absent` | MATCH |
| no 'Source Intelligence' module header in the provisional region | `(forbidden element)` | `absent` | `absent` | MATCH |
| no Source Tiers legend block in the provisional region | `(forbidden element)` | `absent` | `absent` | MATCH |
| no SECOND tab bar inside the Sources body | `(forbidden element)` | `absent` | `absent` | MATCH |
| no per-region source search input | `(forbidden element)` | `absent` | `absent` | MATCH |
| no literal UNSCORED anywhere (ruling 2.1) | `(forbidden element)` | `absent` | `absent` | MATCH |
| no ROLES column in ORGANIZATIONS | `(forbidden element)` | `absent` | `absent` | MATCH |
| no second sub-tab row above the section card | `(forbidden element)` | `absent` | `absent` | MATCH |
| no Export queue control | `(forbidden element)` | `absent` | `absent` | MATCH |
| ORGANIZATIONS: every cell stays inside its own row and column | `(cell containment)` | `no cell overlaps a sibling or exceeds its row` | `clean across 2 row(s)` | MATCH |

## Page composition — Account (14)

- Spec file: `fsi-app/.discipline/rendering/audit/spec/compose-14-account.json`
- Mount: `compose-account` at 1440 px
- Artboards: 14-account
- Design source: docs/design/handoff-2026-09-06/screens/14-account.png (artboard image is the authority per SETUP); dc.html id="p14"; README screen 14
- Result: 22 MATCH, 0 not matching

> Region-composition check for the full page: AppShell + the real UserProfilePage (its own internal Masthead + merged tab row), populated fixture data via .discipline/rendering/audit/mounts.mjs['compose-account'] — no live Supabase project reachable in this sandbox (DEVIATION-LOG.md); this mount's org endpoints (/api/orgs/org-1, /members, /invitations) are mocked with dc.html p14's own illustrated fixture (2 members, both Owner; Dietl / Rockit, enterprise plan) instead.
> Artboard order: Masthead -> merged 8-entry tab row (Personal/Organization/Members & roles·2/Sector profile·6/Jurisdictions·30/Verifier badge/Activity/Settings) -> 2-column grid (content 1fr / 300px rail) -> left column: Members & roles card THEN Organization card stacked directly below it (dc.html's own illustrated tab is Members & roles, not Personal) -> rail: 2x2 stat grid (Sectors followed/Home jurisdictions/Member since/Plan) -> Admin card (owner/admin only) -> Quick links card (Dashboard / Settings / Watchlist, 3 items).
> This lane fixed four real defects: (1) the page used to render a single-column layout — an owner-banner strip above a `repeat(4,1fr)` stat row above the tab body, no rail — replaced with dc.html's own 2-column grid (minmax(0,1fr) 300px) with the stat grid / Admin card / Quick links card as a RAIL persistent across every tab, not stacked above the content in one column. (2) The rail's 4th stat tile used to be a conditional 'Admin attention'/'Account role' tile; dc.html's 4th tile is 'Plan' (org billing plan) — added an `organizations.plan` read and swapped the tile. (3) QuickLinksRail (persistent in the rail now) used to duplicate itself inside the Personal tab's own body AND carried 5-6 two-line entries (Dashboard/Regulations/Market intel/Map/Settings/Admin panel) that dc.html does not show; fixed to dc.html's own exact 3 single-line links (Dashboard / Settings · notifications, briefing schedule / Watchlist), rendered once, persistently, in the rail only. (4) The Members & roles tab used to render only MembersPanel; dc.html p14 illustrates Organization stacked directly below it on that same tab (Organization keeps its own separate tab too, unaffected) — added.
> Not a composition defect, left as the real page's own behavior: the Masthead's dateLabel here is computed from the live current date (Intl formatLocaleDate(new Date())), not a passed-in fixture string like Map/Community/Admin's mounts use, UserProfilePage.tsx has never taken a dateLabel prop, unlike AdminDashboard; changing that is a component-level decision outside this composition-only pass, and the visual difference (today's real weekday/date vs the artboard's fixed 'Sunday 6 September 2026') is expected and not asserted by this spec.
> Lane admin60 (2026-09-08) closed the five differences the train-59 fold listed against this artboard. (1) The invite row carries dc.html p14's own 'INVITE BY EMAIL' field label, its 'name@company.com' placeholder and its 'Send invite' button, laid out 1fr/auto to the field baseline. (2) The member list IS the artboard's table now: the shared RowTable under MEMBER / JOINED / ROLE headers on dc.html's own track list (1fr 120px 150px 44px), with the trailing 44px overflow cell; Remove and Ban moved off the row as text buttons into that overflow menu (R7's precedent; the overlay's own styling is left exactly as RowTable ships it, per 'overlays: unchanged and undesigned, do not invent'). The row's second line is the artboard's 'you · <email>' on the reader's own row, not a repeat of the ROLE column. (3) The seat foot strip is built as a neutral StateNote where the artboard draws one; the seat clause has NO data path (no seats / seat_limit / max_members column exists in the schema or in any /api/orgs payload, re-checked this lane), so it renders the Absence convention with reason 'connect data' and no 'Manage seats' link, there being no seats route and a dead control being a defect (ruling 1.1). (4) The identity table and workspace-scope footnote that used to sit ABOVE ORGANIZATION's two fields, where the artboard has nothing, moved BELOW them as a card-foot disclosure under ruling R7. (5) The rail's four stat blocks are ONE card with a 2x2 grid inside, as drawn, not four separate bordered cards.
> Also this lane, both shared parts and both artboard-driven: AccountCard's head meta is 10.5px / .12em small caps (it read as body copy in sentence case), and the card head sits on the card's own white rather than a raised plate.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| Page composition — Account (14) | `count` | `1` | `1` | MATCH |
| region 6b — Quick links has exactly 3 entries (Dashboard / Settings / Watchlist), no duplicate elsewhere | `count` | `1` | `1` | MATCH |
| region 2a, the invite row carries the artboard's INVITE BY EMAIL label | `count` | `1` | `1` | MATCH |
| region 2b, the invite field's placeholder is the artboard's | `count` | `1` | `1` | MATCH |
| region 2b, the invite field's placeholder is the artboard's | `placeholder` | `name@company.com` | `name@company.com` | MATCH |
| region 2c, the invite action reads Send invite | `count` | `1` | `1` | MATCH |
| region 2d, the member list is a table on dc.html p14's own track list | `grid-template-columns` | `* 120px 150px 44px` | `378px 120px 150px 44px` | MATCH |
| region 2e, its column headers are the artboard's three, in order | `text` | `MemberJoinedRole` | `MemberJoinedRole` | MATCH |
| region 2f, every member row ends in the 44px overflow control | `count` | `2` | `2` | MATCH |
| region 2f, every member row ends in the 44px overflow control | `width` | `44px` | `44px` | MATCH |
| region 2f, every member row ends in the 44px overflow control | `height` | `44px` | `44px` | MATCH |
| region 2g, the seat foot strip, with the seat clause in the Absence convention | `count` | `1` | `1` | MATCH |
| region 3a, ORGANIZATION's body is the artboard's two fields and Save on one row | `count` | `1` | `1` | MATCH |
| region 3a, ORGANIZATION's body is the artboard's two fields and Save on one row | `grid-template-columns` | `* * *` | `314.953px 314.969px 76.0781px` | MATCH |
| region 3b, the R7 disclosure carrying the org identity table sits AFTER those fields | `count` | `1` | `1` | MATCH |
| region 4a, the rail's four stat blocks are ONE card, 2x2 inside | `count` | `1` | `1` | MATCH |
| region 4a, the rail's four stat blocks are ONE card, 2x2 inside | `grid-template-columns` | `* *` | `107.844px 144.156px` | MATCH |
| no floating Ask AI button (ruling 2.2) | `(forbidden element)` | `absent` | `absent` | MATCH |
| no Remove/Ban text buttons on the member row | `(forbidden element)` | `absent` | `absent` | MATCH |
| no Manage seats link | `(forbidden element)` | `absent` | `absent` | MATCH |
| no separate bordered card per rail stat | `(forbidden element)` | `absent` | `absent` | MATCH |
| MEMBERS & ROLES: every cell stays inside its own row and column | `(cell containment)` | `no cell overlaps a sibling or exceeds its row` | `clean across 3 row(s)` | MATCH |

## Page composition — Settings (15)

- Spec file: `fsi-app/.discipline/rendering/audit/spec/compose-15-settings.json`
- Mount: `compose-settings` at 1440 px
- Artboards: 15-settings
- Design source: docs/design/handoff-2026-09-06/screens/15-settings.png (artboard image is the authority per SETUP); dc.html id="p15"; README screen 15
- Result: 24 MATCH, 0 not matching

> Region-composition check for the full page: AppShell + the real SettingsPage (its own internal Masthead), populated fixture data via .discipline/rendering/audit/mounts.mjs['compose-settings'] — no live Supabase project reachable in this sandbox (DEVIATION-LOG.md).
> Artboard order (dc.html p15): Masthead (VOL line · SETTINGS · scope line ending 'scoped to <b>Dietl / Rockit</b>' · command bar 420px on the SAME row) -> the 'Applies workspace-wide' notice inside the masthead card -> merged 8-entry Account tab row -> R9's S1..S6 section index -> a TWO-COLUMN grid `minmax(0,1fr) 300px`, gap 28: left column (gap 18) Dashboard defaults · Freight sectors · Notifications; right column (gap 14) Briefing schedule · Appearance · Data & supersessions.
> SUPERSEDES this spec's train-59 note, which asserted that ruling R9 retires the artboard's 300px rail and that the whole page is one column. R9 retires the second-level SUB-NAV only; the coordinator's train-60 dispatch is explicit that artboard 15 is two columns and that the six designed cards compose into them in the artboard's order. R9's section index stays, and Saved searches / Data summary / CSV upload / Supersession history / Archive / Help stay as anchored sections BELOW the two designed columns (ruling R7: features the artboards draw no region for are kept and listed, placed after the last designed region of the column).
> Data-driven differences from the artboard, all logged in DEVIATION-LOG.md 2026-09-08 (lane settings60) and none asserted here as a match: 'Show all 40 sectors' (ALL_SECTORS has 40; the artboard's 36 is its mock corpus), no PDF export segment (`exportFormat` has no `pdf` value and no PDF export path exists), Day segments Mon..Fri (`briefingDay`'s stored union has no Sunday), a Biweekly cadence segment the artboard does not draw (R7), 'Data summary' in place of the rail's 'Export my data' (no data-export path exists anywhere in the app), and the jurisdiction-weighting control kept as a card-foot disclosure rather than the artboard's 'lives in Account -> Jurisdictions' line, which points at a control that does not exist there.
> Three shared parts changed for this page, each additively or to the artboards' own value: `AccountCard` gained optional `bodyPadding`/`foot` and lost the tinted plate behind its head (dc.html p14 AND p15 both draw that head on the card's white); `AccountPrimitives` gained `SegmentedControl` (the form dc.html p15 uses five times); `Masthead`'s title/command-bar row became the grid dc.html declares on 15 of the 17 artboards (`minmax(0,1fr) 420px`) instead of a wrapping flex row, which had been pushing the command bar onto its own line whenever the dek was long.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| Page composition — Settings (15) | `count` | `1` | `1` | MATCH |
| region 1 — masthead scope line ends with the workspace (dc.html p15 'scoped to Dietl / Rockit') | `count` | `1` | `1` | MATCH |
| region 2 — masthead command bar sits on the title row, not wrapped below it (dc.html grid minmax(0,1fr) 420px) | `display` | `grid` | `grid` | MATCH |
| region 6 — the two-column grid, content 1fr / 300px rail, gap 28 (dc.html p15) | `count` | `1` | `1` | MATCH |
| region 6 — the two-column grid, content 1fr / 300px rail, gap 28 (dc.html p15) | `display` | `grid` | `grid` | MATCH |
| region 6 — the two-column grid, content 1fr / 300px rail, gap 28 (dc.html p15) | `column-gap` | `28px` | `28px` | MATCH |
| region 6a — left column carries exactly the artboard's three cards | `count` | `3` | `3` | MATCH |
| region 7a — Dashboard defaults draws three segmented groups (Default sort / Default export / Alert bands) | `count` | `3` | `3` | MATCH |
| region 7b — Default sort segments are the list surfaces' own words | `count` | `3` | `3` | MATCH |
| region 7c — Alert bands segments come from the one urgency vocabulary | `count` | `3` | `3` | MATCH |
| region 7d — the toggle grid is the artboard's six dashboard regions, each a 44px row | `count` | `6` | `6` | MATCH |
| region 7d — the toggle grid is the artboard's six dashboard regions, each a 44px row | `min-height` | `44px` | `44px` | MATCH |
| region 8a — collapsed by default to twelve sector chips (dc.html p15 draws twelve) | `count` | `12` | `12` | MATCH |
| region 9a — six notification rows, dc.html's own order | `count` | `6` | `6` | MATCH |
| region 10 — right 1: Briefing schedule rail card | `count` | `1` | `1` | MATCH |
| region 11 — right 2: Appearance card, dc.html's copy verbatim | `count` | `1` | `1` | MATCH |
| region 12 — right 3: Data & supersessions card | `count` | `1` | `1` | MATCH |
| region 12a — three links, the first stating the saved-search count | `count` | `3` | `3` | MATCH |
| region 13 — R7 sections below the two designed columns, still reachable | `count` | `4` | `4` | MATCH |
| no floating Ask AI button (ruling 2.2) | `(forbidden element)` | `absent` | `absent` | MATCH |
| no 'Home sections' label — the toggle grid is named for the regions the dashboard renders | `(forbidden element)` | `absent` | `absent` | MATCH |
| no page-local Appearance field inside Dashboard defaults | `(forbidden element)` | `absent` | `absent` | MATCH |
| Dashboard defaults control cells — no segmented group escapes its column or overlaps a sibling (operator report 2026-09-07) | `(cell containment)` | `no cell overlaps a sibling or exceeds its row` | `clean across 1 row(s)` | MATCH |
| the two designed columns — neither column's cards escape the grid | `(cell containment)` | `no cell overlaps a sibling or exceeds its row` | `clean across 1 row(s)` | MATCH |

## Page composition — Auth (16): /login, /signup

- Spec file: `fsi-app/.discipline/rendering/audit/spec/compose-16-auth.json`
- Mount: `compose-login` at 1440 px
- Artboards: 16-auth
- Design source: docs/design/handoff-2026-09-06/screens/16-auth.png (artboard image is the authority per SETUP); dc.html id="p16"; README screen 16
- Result: 10 MATCH, 0 not matching

> Region-composition check for /login (the primary illustrated state on the artboard, 'Sign in' tab active). Full page: the real LoginPage (src/app/login/page.tsx) inside AuthFrame — no AppShell/Sidebar, matching the artboard's own full-1440px split-screen frame (left identity panel / right form panel), no live Supabase project reachable in this sandbox (DEVIATION-LOG.md).
> This lane found NO composition defects on this page — it was already built carefully by a prior lane (AuthFrame.tsx/AuthPanel.tsx/LoginPage.tsx headers), including correctly-reasoned, already-logged deviations from the artboard's own placeholder content: the artboard's headline ('What is binding on you, when it bites, and what it costs.') and its fabricated '1,434 items...' copy are explicitly marked placeholder in README ('Fidelity') and are not shipped (CLAUDE.md rule 2, never fabricate numbers) — the real tagline + the real BAND_ORDER urgency-band scale render in that slot instead, both real vocabulary. The artboard's 'Keep me signed in' checkbox is not built because src/lib/supabase-browser.ts's client has no session-only mode wired and that file is outside this lane's write set (logged). This spec exists to LOCK this already-correct composition in place, not to fix anything.
> This lane's own mount infrastructure needed one fix to render /signup at all: SignupPage.tsx calls `supabase.auth.getUser()` on mount to redirect an already-signed-in visitor away, which the pre-existing generic stub-supabase-browser.mjs (deliberately 'fake-but-authenticated' for OTHER mounts' click-fires-callback proofs) has no `getUser` method for at all — threw, blanking the whole mount. New stub-supabase-browser-auth.mjs (signed-out steady state: getSession/getUser both resolve null) used by both compose-login and compose-signup instead; the generic stub is untouched, so every mount that already depends on its authenticated shape is unaffected.
> Artboards 16 and 17 draw a 900px-tall frame whose right column is vertically CENTRED (dc.html p16/p17: the grid is `min-height:900px` and the right column `display:flex;align-items:center;justify-content:center;padding:48px`). AuthFrame.tsx already builds exactly that. FOLD-59's visual pass read the built panel as sitting lower than the artboard's; that was the CAPTURE, whose 1400px viewport made AuthFrame's own `min-height:100vh` outer box 1400 tall so the panel centred at 700 instead of 450. The mount now pins its capture height to the artboard's 900. Asserted below so the geometry cannot drift silently again (lane lists60, 2026-09-08). The 'Keep me signed in' checkbox artboard 16 draws is still NOT built: it needs a session-persistence decision (Supabase session lifetime, not a checkbox), and it is logged in DEVIATION-LOG.md as the open item it already was. No forbid row is added for it, because it is deferred, not ruled out.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| Page composition — Auth (16): /login, /signup | `count` | `1` | `1` | MATCH |
| region 4 — Work email + Password fields | `count` | `1` | `1` | MATCH |
| the frame is the artboard's 900px-tall grid | `min-height` | `900px` | `900px` | MATCH |
| the frame is the artboard's 900px-tall grid | `display` | `grid` | `grid` | MATCH |
| the form column is vertically centred in that frame, with the artboard's 48px inset | `display` | `flex` | `flex` | MATCH |
| the form column is vertically centred in that frame, with the artboard's 48px inset | `align-items` | `center` | `center` | MATCH |
| the form column is vertically centred in that frame, with the artboard's 48px inset | `justify-content` | `center` | `center` | MATCH |
| the form column is vertically centred in that frame, with the artboard's 48px inset | `padding-top` | `48px` | `48px` | MATCH |
| the form column is vertically centred in that frame, with the artboard's 48px inset | `padding-left` | `48px` | `48px` | MATCH |
| no floating Ask AI button — auth pages are logged out, no assistant surface | `(forbidden element)` | `absent` | `absent` | MATCH |

## Page composition — Auth (16): /signup

- Spec file: `fsi-app/.discipline/rendering/audit/spec/compose-16-signup.json`
- Mount: `compose-signup` at 1440 px
- Artboards: 16-auth
- Design source: docs/design/handoff-2026-09-06/screens/16-auth.png (artboard image is the authority per SETUP); dc.html id="p16"; README screen 16
- Result: 12 MATCH, 0 not matching

> WHY THIS FILE EXISTS (fold 60). The `compose-signup` mount was built, is maintained, and was measured by NOTHING. compose-16-auth.json's own title reads '/login, /signup' but its `mount` is `compose-signup`'s sibling `compose-login`, and no other spec named it, so the fold's mechanical mount/spec bijection check found it as the one AUDIT_MOUNTS entry with no spec. It was not dormant-and-harmless either: lane lists60 pinned `captureHeight: 900` on all three auth mounts this train, so a lane was editing a fixture whose output nothing read. CLAUDE.md rule 13 (no dormant code) and rule 15 (execution over existence) both point the same way, and the cheaper of the two dispositions is wrong here: /signup is a real page a logged-out visitor reaches, so it gets measured rather than deleted.
> Artboard 16 draws ONE auth screen, the sign-in state. /signup is the same AuthFrame and the same 380px panel with the tab row switched to 'Create account', which is the state the artboard's own second tab names. This spec therefore asserts (a) the FRAME geometry, which artboard 16 does draw and which /signup shares with /login byte for byte, and (b) the page's own real fields. It does NOT invent artboard values for signup-specific content the artboard never draws: the artboard has no signup panel, so no measurement here claims artboard authority for the confirm-password field or the button label. Those rows assert that the built page is internally correct (the fields exist, the panel is the shared 380px column), never that a picture specifies them.
> The two password inputs are the real difference from /login: Password and Confirm password. The count of 2 is the assertion that both are present and that neither was quietly dropped, which is the failure a signup page can ship with and still look right in a screenshot.
> Same CSS posture as compose-16-auth.json: this mount declares `needsCompiledCss`, so token-derived type and colour DO resolve here, and the geometry rows below are read against the app's own compiled stylesheet.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| Page composition — Auth (16): /signup | `count` | `1` | `1` | MATCH |
| region 4 — Work email field, the artboard's own placeholder | `count` | `1` | `1` | MATCH |
| region 5 — BOTH password fields: Password and Confirm password | `count` | `2` | `2` | MATCH |
| region 6 — the submit button reads Create account, not Sign in | `count` | `1` | `1` | MATCH |
| the frame is the artboard's 900px-tall grid, identical to /login | `min-height` | `900px` | `900px` | MATCH |
| the frame is the artboard's 900px-tall grid, identical to /login | `display` | `grid` | `grid` | MATCH |
| the form column is vertically centred in that frame, with the artboard's 48px inset | `display` | `flex` | `flex` | MATCH |
| the form column is vertically centred in that frame, with the artboard's 48px inset | `align-items` | `center` | `center` | MATCH |
| the form column is vertically centred in that frame, with the artboard's 48px inset | `justify-content` | `center` | `center` | MATCH |
| the form column is vertically centred in that frame, with the artboard's 48px inset | `padding-top` | `48px` | `48px` | MATCH |
| the form column is vertically centred in that frame, with the artboard's 48px inset | `padding-left` | `48px` | `48px` | MATCH |
| no floating Ask AI button — auth pages are logged out, no assistant surface | `(forbidden element)` | `absent` | `absent` | MATCH |

## Page composition — Onboarding (17)

- Spec file: `fsi-app/.discipline/rendering/audit/spec/compose-17-onboarding.json`
- Mount: `compose-onboarding` at 1440 px
- Artboards: 17-onboarding
- Design source: docs/design/handoff-2026-09-06/screens/17-onboarding.png (artboard image is the authority per SETUP); dc.html id="p17"; README screen 17
- Result: 11 MATCH, 0 not matching

> Region-composition check for the real OnboardingWizard (its own `step` state defaults to 2, 'Modes & jurisdictions' — the artboard's own illustrated step, no forced navigation needed) inside AuthFrame, populated fixture aggregates via .discipline/rendering/audit/mounts.mjs['compose-onboarding'] — no live Supabase project reachable in this sandbox (DEVIATION-LOG.md).
> This lane found NO composition defects — the wizard was already built carefully by a prior lane (OnboardingWizard.tsx's own header explains the 5→4-step replacement and every deviation, already logged). Artboard order confirmed present and in order: 4-pill stepper (Workspace done / Modes & jurisdictions active / Sectors / Briefing) -> 'Where do you move freight?' heading + scope copy -> Modes chip row -> Home jurisdictions chip grid + '+N more' -> Preview·your workspace today 4-tile band strip -> Skip for now / Continue → Sectors footer.
> Step 4 'Briefing' has no artboard yet (operator ruling, dispatch instruction) — out of this mount's and this lane's scope, left as built.
> This lane's own mount needed one infrastructure fix to show real counts: OnboardingWizard.tsx's profile-prefill effect awaits `supabase.from("profiles")...` unconditionally on mount; the compose-login/signup `stub-supabase-browser-auth.mjs` (reused here) had no `.from` at all, which threw inside the effect before `setLoadingProfile(false)` ran, leaving the step-2 preview tiles stuck on their loading skeleton forever regardless of the `aggregates` fixture prop. Added the same empty-row `.from` shape the generic stub already carries — additive, does not change stub-supabase-browser-auth.mjs's signed-out `.auth` contract that /login and /signup depend on.
> Same 900px frame and centred right column as artboard 16 — see that spec's note for why the capture, not the build, was what sat the panel low. Artboard 17 also draws a '← Back' link in the footer row of the step it renders. The build renders it from step 3 onward and an empty placeholder here at step 2, and that is correct composition: this wizard STARTS at step 2, because step 1 (Workspace) is a separate, already-completed flow the stepper marks with a ✓ and which has no route to return to. A Back at step 2 would navigate nowhere — the dead control operator audit P0 1.1 ruled a defect. The link's existence and its wiring to the wizard's own goBack are proven by OnboardingWizard.npmtest.mjs; the forbid below holds the other half, so a later lane cannot add a Back to the first step to 'match the image'. Logged in DEVIATION-LOG.md.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| Page composition — Onboarding (17) | `count` | `1` | `1` | MATCH |
| region 2 — heading | `count` | `1` | `1` | MATCH |
| the frame is the artboard's 900px-tall grid | `min-height` | `900px` | `900px` | MATCH |
| the frame is the artboard's 900px-tall grid | `display` | `grid` | `grid` | MATCH |
| the step column is vertically centred in that frame, with the artboard's 48px inset | `display` | `flex` | `flex` | MATCH |
| the step column is vertically centred in that frame, with the artboard's 48px inset | `align-items` | `center` | `center` | MATCH |
| the step column is vertically centred in that frame, with the artboard's 48px inset | `justify-content` | `center` | `center` | MATCH |
| the step column is vertically centred in that frame, with the artboard's 48px inset | `padding-top` | `48px` | `48px` | MATCH |
| the step column is vertically centred in that frame, with the artboard's 48px inset | `padding-left` | `48px` | `48px` | MATCH |
| no floating Ask AI button — onboarding renders no AppShell | `(forbidden element)` | `absent` | `absent` | MATCH |
| no '← Back' control on the wizard's FIRST step | `(forbidden element)` | `absent` | `absent` | MATCH |

## DetailHeader

- Spec file: `fsi-app/.discipline/rendering/audit/spec/detailheader.json`
- Mount: `detail-shell` at 1440 px
- Artboards: 03-regulation-detail, 05-market-detail, 07-research-detail, 09-operations-profile
- Design source: src/components/detail/DetailShell.tsx (the ONE header shared by all four detail surfaces, per its own docstring); README.md §0.4 'Type' (Anton titles 28px, letter-spacing .04em) + §0.5; operator rulings 1.2 (detail title must be Anton uppercase) and 2.3 (no second ask bar between the tags row and the action row)
- Result: 9 MATCH, 0 not matching

> Ruling 1.2 named the PPWR page specifically as bypassing the shared title component; this spec measures the shared component itself (DetailHeader), which every one of the four detail surfaces now imports, so a MATCH here is evidence the shared part is correct — it does not by itself prove every page calls it (that is a page-frame / route-level check, out of this spec's selector reach).
> This mount omits `askPlaceholder` (ruling 2.3: the CommandBar in the Masthead is the only ask surface, DetailHeader must not render a second one when a caller omits it) — the forbid check below confirms no CommandBar renders even though `actions` is populated.
> Structural note, train 58 (2026-09-07): DetailHeader.tsx's own doc comment now reads 'title now lives in DetailMasthead below' — train 57 moved the title and meta/breadcrumb line out of this component into the shared `ui/Masthead` (via `DetailMasthead`), confirmed by direct source read this session (`src/components/detail/DetailShell.tsx` ~line 56). DetailHeader itself renders only chips, the tag row, and actions. The title and meta children checks this spec used to carry are therefore removed here, not left as phantom NOT BUILT rows against a part this component no longer owns: the title is checked against its real location by `page-frame.json`'s 'detail title type' row (`.cl-masthead-title`). The former 11px meta/breadcrumb line has no like-for-like replacement to check yet — DetailMasthead's breadcrumb now renders inside Masthead's `.cl-masthead-eyebrow` line (800 weight, .14em, `--fs-105`, a different typographic convention from the old 11px meta line), which no spec measures yet. Logged as a B-list gap (Masthead+CommandBar) for the fix lane rather than asserted against an invented 11px value here.
> SPEC CORRECTION, fix58-detail (2026-09-07): the padding-top/right/left/bottom values this spec previously carried (18/24/24/20) do not match the artboard. Direct re-read of 'Caros Ledge UI System.dc.html' #p3, char 201809 — the actual band-pill/action-row card (the 'Action · <= 6 months' chip through the Export brief/Share/Watch buttons, i.e. this component) reads `padding:16px 20px 18px` (top 16, right/left 20, bottom 18). The 18/24/24/20 figures belonged to a different card in the same artboard (the DetailMasthead title+ask-bar card just above this one, `padding:20px 28px 22px`) and were misattributed when this spec was first written. Corrected here per rule 1 of the fix lane's dispatch (correct the spec, never bend the product to a wrong expectation) — the product was moved to match the corrected 16/20/20/18 values, not the other way round.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| DetailHeader | `background-color` | `#FFFFFF` | `rgb(255, 255, 255)` | MATCH |
| DetailHeader | `border-top-left-radius` | `10px` | `10px` | MATCH |
| DetailHeader | `padding-top` | `16px` | `16px` | MATCH |
| DetailHeader | `padding-right` | `20px` | `20px` | MATCH |
| DetailHeader | `padding-left` | `20px` | `20px` | MATCH |
| DetailHeader | `padding-bottom` | `18px` | `18px` | MATCH |
| tag row sits directly under the title (no gap for a second bar) | `count` | `1` | `1` | MATCH |
| a CommandBar inside DetailHeader when askPlaceholder is omitted | `(forbidden element)` | `absent` | `absent` | MATCH |
| the retired three-state 'Complete brief' toggle | `(forbidden element)` | `absent` | `absent` | MATCH |

## DetailSection

- Spec file: `fsi-app/.discipline/rendering/audit/spec/detailsection.json`
- Mount: `detail-shell` at 1440 px
- Artboards: 03-regulation-detail, 05-market-detail, 07-research-detail, 09-operations-profile, 18-section-treatments
- Design source: 'Caros Ledge UI System.dc.html' p3 (#p3) section header block: numeral "S1" (9.5-ish/800/.1em/#7A6E6C) beside the Anton title ("Summary", 20px/.04em/uppercase); README.md §0.4 type scale ('card titles (20)'); operator ruling 5.1 (the 3px graduated rule above every section/panel card, no line below the title) — applies 'to every panel/section card on every page, including the system sheet', which the ruling's own text makes explicit is not limited to the dashboard
- Result: 10 MATCH, 0 not matching

> Selector repair, train 58 (2026-09-07): the earlier `[data-section-rule]` selector never matched anything. DetailSection.tsx DOES render the shared `SectionRule` component (`src/components/ui/SectionRule.tsx`, marker class `.cl-section-rule`, confirmed by direct source read this session, `src/components/detail/DetailShell.tsx` line ~588) — the selector below now measures the real element.
> Title size: the artboard's section title is 20px (README's own 'card titles (20)' line), DetailSection.tsx sets `fontSize: 16` — a real MISMATCH, not a harness artefact (both numbers read directly, no unit conversion involved).
> A first pass of this spec also checked the section's own `border-bottom-width` against 0 and got 1px — that was a harness mistake, not a finding: DetailSection.tsx applies one uniform `border: "1px solid var(--line-1)"` around the WHOLE card (ordinary card chrome on all four sides), not a dedicated divider under the title the way the dashboard's section cards in page-frame.json do (`border-bottom: 2px solid #5A5552`, ruling 5.1's own '18a' target). DetailSection never had that divider to begin with, so there is nothing at this selector for ruling 5.1's 'no line below the title' half to find; removed from `expect` rather than left as a false MISMATCH.
> The artboard also prefixes each section title with a small grey "S1"/"S2"/… numeral (the same numbering the sticky index uses) directly beside the Anton title; DetailSection.tsx renders the title alone with no numeral prefix. Not independently checked here (no selector exists to address it) — recorded as a structural gap alongside the title-size finding, not a second row asserting a value that cannot be measured.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| DetailSection | `background-color` | `#FFFFFF` | `rgb(255, 255, 255)` | MATCH |
| DetailSection | `padding-top` | `16px` | `16px` | MATCH |
| DetailSection | `padding-right` | `20px` | `20px` | MATCH |
| DetailSection | `padding-left` | `20px` | `20px` | MATCH |
| the 3px graduated rule above the section title (ruling 5.1) | `height` | `3px` | `3px` | MATCH |
| the 3px graduated rule above the section title (ruling 5.1) | `background-image` | `linear-gradient(90deg, rgb(90, 85, 82) 0%, rgb(90, 85, 82) 22%, rgba(90, 85, 82, 0.18) 100%)` | `linear-gradient(90deg, rgb(90, 85, 82), rgb(90, 85, 82) 22%, rgba(90, 85, 82, 0.18))` | MATCH |
| section title | `font-family` | `contains:Anton` | `Anton, system-ui, sans-serif` | MATCH |
| section title | `text-transform` | `uppercase` | `uppercase` | MATCH |
| section title | `letter-spacing` | `0.04em (= 0.8px at 20px)` | `0.8px` | MATCH |
| section title | `font-size` | `20px` | `20px` | MATCH |

## DetailTagRow

- Spec file: `fsi-app/.discipline/rendering/audit/spec/detailtagrow.json`
- Mount: `detail-shell` at 1440 px
- Artboards: 03-regulation-detail, 05-market-detail, 07-research-detail, 09-operations-profile
- Design source: README.md §0.4 'Workspace tags': 'They appear under the detail title... ＋ Tag is a dashed-outline pill that opens a popover'; operator ruling 3.2 ('The action-row + Tag is the only trigger. Applied workspace tags still render as a row under the title; that row displays tags, it does not carry the trigger.')
- Result: 4 MATCH, 0 not matching

> Ruling 3.2 changes what this row is FOR: it should display applied tags (and the small 'workspace tags' caption), and its own trigger should no longer be the primary one once the action row's + Tag opens the same popover (DetailTagRow.tsx's `open`/`onOpenChange` props exist specifically for that lifted-state wiring). This mount passes `open`/`onOpenChange` (the lifted-state form), matching that wiring; a page still using the row's own uncontrolled trigger as a SECOND independent popover is the defect 3.2 describes, checked at the page-frame level (two distinct popovers open at once), not measurable from this single-mount spec.
> This item has no applied tags in the fixture (itemId 'demo-item' resolves to an empty EMPTY_API response), so the row renders its trigger + caption only — the WorkspaceTagPill styling itself (6px square dot, #F5F2EE fill) is audited by the Chips spec, not duplicated here.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| DetailTagRow | `display` | `flex` | `flex` | MATCH |
| DetailTagRow | `column-gap` | `8px` | `8px` | MATCH |
| 'workspace tags' caption | `text` | `workspace tags` | `workspace tags` | MATCH |
| 'workspace tags' caption | `letter-spacing` | `0.04em (= 0.38px at 9.5px)` | `0.38px` | MATCH |

## DetailTimeline

- Spec file: `fsi-app/.discipline/rendering/audit/spec/detailtimeline.json`
- Mount: `detail-shell` at 1440 px
- Artboards: 03-regulation-detail, 05-market-detail, 07-research-detail, 09-operations-profile
- Design source: 'Caros Ledge UI System.dc.html' p3 (#p3) timeline card + next-obligation callout block (`border-left:3px solid #F97316;background:#FFF7ED;border-radius:0 6px 6px 0;padding:9px 12px`); README.md §0.4 'State note strip'; operator ruling 2.6 (exact state-note numbers, matched here to confirm the built StateNote this component composes is used correctly in the timeline's own next-obligation slot)
- Result: 20 MATCH, 0 not matching

> The card chrome (background/border/radius/padding) is DetailShell.tsx's own inline style, shared with every other card in this file (DetailSection, ImpactRailCard, etc.) — not a separate design value, so this spec checks it once here rather than repeating the same four properties on every sibling spec.
> The mount's fixture band is 'action' (theme.css: --action:#F97316, --action-tint:#FFF7ED), matching the artboard's own orange next-obligation callout exactly — a MATCH on the StateNote colours below is a real confirmation, not a coincidence of the fixture.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| DetailTimeline | `background-color` | `#FFFFFF` | `rgb(255, 255, 255)` | MATCH |
| DetailTimeline | `border-radius` | `10px` | `10px` | MATCH |
| DetailTimeline | `padding-top` | `16px` | `16px` | MATCH |
| DetailTimeline | `padding-right` | `20px` | `20px` | MATCH |
| DetailTimeline | `padding-left` | `20px` | `20px` | MATCH |
| 'Timeline' eyebrow label | `text` | `Timeline` | `Timeline` | MATCH |
| 'Timeline' eyebrow label | `font-weight` | `800` | `800` | MATCH |
| 'Timeline' eyebrow label | `letter-spacing` | `0.1em (= 1.05px at 10.5px)` | `1.05px` | MATCH |
| 'Timeline' eyebrow label | `text-transform` | `uppercase` | `uppercase` | MATCH |
| next-obligation StateNote callout | `border-left-width` | `3px` | `3px` | MATCH |
| next-obligation StateNote callout | `border-left-style` | `solid` | `solid` | MATCH |
| next-obligation StateNote callout | `border-left-color` | `#F97316` | `rgb(249, 115, 22)` | MATCH |
| next-obligation StateNote callout | `background-color` | `#FFF7ED` | `rgb(255, 247, 237)` | MATCH |
| next-obligation StateNote callout | `border-top-left-radius` | `0` | `0px` | MATCH |
| next-obligation StateNote callout | `border-top-right-radius` | `6px` | `6px` | MATCH |
| next-obligation StateNote callout | `border-bottom-right-radius` | `6px` | `6px` | MATCH |
| next-obligation StateNote callout | `border-bottom-left-radius` | `0` | `0px` | MATCH |
| next-obligation StateNote callout | `padding-top` | `9px` | `9px` | MATCH |
| next-obligation StateNote callout | `padding-right` | `12px` | `12px` | MATCH |
| next-obligation StateNote callout | `padding-left` | `12px` | `12px` | MATCH |

## FactBlocks in context (FactCard variants + concession table, 03/05/07/09)

- Spec file: `fsi-app/.discipline/rendering/audit/spec/factblocks.json`
- Mount: `factblocks` at 1440 px
- Artboards: 03-regulation-detail, 05-market-detail, 07-research-detail, 09-operations-profile
- Design source: src/components/detail/FactBlocks.tsx (the ONE renderer all four detail surfaces route section content_md through, per its own header, which names the operations concession table by name); 'Caros Ledge UI System.dc.html' p9 (#p9, Operations profile) concession table markup: `<table>` in a `border:1px solid rgba(0,0,0,.12);border-radius:8px;overflow:hidden` wrapper, `<thead style="background:#FAFAF8">`, `<th style="padding:8px 12px;font-size:10px;letter-spacing:.1em;text-transform:uppercase;color:#7A6E6C;font-weight:700;border-bottom:1px solid rgba(0,0,0,.12)">`, `<td style="padding:8px 12px;border-bottom:1px solid rgba(0,0,0,.06)">`; operator ruling 2.4 (fact card form, re-checked here in the parser-driven path, not the harness's own hand-built proof)
- Result: 22 MATCH, 0 not matching

> factcard.json (the harness's original proof spec) already measures FactCard's own geometry directly with hand-built props; this spec instead drives the SAME variants through the real markdown parser (parseFactParagraphs -> FactBlocks -> FactCard), confirming the parser-to-component wiring — a raw URL never leaking into the sourced card's visible text, and the LEGAL label producing the exact 'Legal confirmation required' text — is correct end to end, per README §0.4's own claim that FactBlocks 'replaces every FACT: … *Source: …* paragraph'.
> The concession table is GfmSection's `<table>` render (components/shared/GfmSection.tsx), reached here because `parseFactParagraphs` classifies a markdown table with no FACT/ANALYSIS/LEGAL prefix as a plain `prose` block, which FactBlocks routes through GfmSection (that file's own header names this exact case: 'a structured concession table (the operations port-dues section)... renders as a real table').
> FactCard.tsx wraps the sourced variant's body in literal curly quotes (`&ldquo;{text}&rdquo;`) — the expected text below includes them; a first pass without the quote characters read as a MISMATCH that was a spec mistake, not a finding.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| FactBlocks in context (FactCard variants + concession table, 03/05/07/09) | `border-left-width` | `2px` | `2px` | MATCH |
| FactBlocks in context (FactCard variants + concession table, 03/05/07/09) | `border-left-color` | `#1A1A1A` | `rgb(26, 26, 26)` | MATCH |
| sourced fact card — no raw URL in visible text | `text` | `“Shipping companies must monitor, report and surrender ETS allowances for each tonne of reported CO2 from ships of 5,000 GT and above calling at EEA ports.”` | `“Shipping companies must monitor, report and surrender ETS allowances for each tonne of reported CO2 from ships of 5,000 GT and above calling at EEA ports.”` | MATCH |
| inference card, second block | `border-top-style` | `dashed` | `dashed` | MATCH |
| inference card, second block | `background-color` | `#FAFAF8` | `rgb(250, 250, 248)` | MATCH |
| counsel card, third block — parser-driven label text | `text` | `Legal confirmation required` | `Legal confirmation required` | MATCH |
| counsel card — orange left edge | `border-left-width` | `2px` | `2px` | MATCH |
| counsel card — orange left edge | `border-left-color` | `#F97316` | `rgb(249, 115, 22)` | MATCH |
| concession table wrapper | `border-collapse` | `collapse` | `collapse` | MATCH |
| concession table — header cell | `padding-top` | `8px` | `8px` | MATCH |
| concession table — header cell | `padding-left` | `12px` | `12px` | MATCH |
| concession table — header cell | `font-size` | `10px` | `10px` | MATCH |
| concession table — header cell | `letter-spacing` | `0.1em (= 1px at 10px)` | `1px` | MATCH |
| concession table — header cell | `text-transform` | `uppercase` | `uppercase` | MATCH |
| concession table — header cell | `color` | `#7A6E6C` | `rgb(122, 110, 108)` | MATCH |
| concession table — header cell | `font-weight` | `700` | `700` | MATCH |
| concession table — thead background | `background-color` | `#FAFAF8` | `rgb(250, 250, 248)` | MATCH |
| concession table — body cell | `padding-top` | `8px` | `8px` | MATCH |
| concession table — body cell | `padding-left` | `12px` | `12px` | MATCH |
| concession table — body cell | `border-bottom-width` | `1px` | `1px` | MATCH |
| concession table — body cell | `border-bottom-color` | `rgba(0,0,0,.06)` | `rgba(0, 0, 0, 0.06)` | MATCH |
| a raw URL in the sourced card's visible text | `(forbidden element)` | `absent` | `absent` | MATCH |

## FactCard

- Spec file: `fsi-app/.discipline/rendering/audit/spec/factcard.json`
- Mount: `factcard` at 1440 px
- Artboards: 00-system-sheet (the part is defined once here), 03-regulation-detail, 05-market-detail, 07-research-detail, 09-operations-profile
- Design source: docs/design/handoff-2026-09-06/'Caros Ledge UI System.dc.html' #sys, the 'Fact card — replaces every "FACT: … *Source: …" paragraph' block; README.md §0.4 'Fact card'; operator ruling 2.4 (2026-09-07, UI discrepancy report)
- Result: 53 MATCH, 0 not matching

> The expected values below are operator ruling 2.4 (2026-09-07), which is the latest binding statement on this part and states exact numbers: sourced = 2px solid #1A1A1A LEFT edge, 1px rgba(0,0,0,.12) on the other three sides, radius 0 8px 8px 0, white; inference = 1px DASHED rgba(0,0,0,.25) all round, background #FAFAF8, italic body; needs counsel = 2px solid #F97316 left edge, otherwise as sourced.
> The artboard 00 markup differs from that ruling and is recorded here so the divergence is visible rather than silently resolved: sourced there is `background:#fff; border:1px solid rgba(0,0,0,.12); border-radius:8px; border-left:3px solid #5A5552; padding:12px 14px 12px 16px`; inference is `background:#F5F2EE; border:1px dashed rgba(0,0,0,.3); border-radius:8px`; counsel is the sourced box with `border-left:3px solid #F97316`. Where the two disagree the operator's ruling is used, per the ruling's own framing ('build against them, ask nothing').
> The variants are addressed through the mount's `[data-audit]` wrappers, which carry no styling of their own; `> div` inside each is the FactCard's own root element.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| FactCard | `background-color` | `#FFFFFF` | `rgb(255, 255, 255)` | MATCH |
| FactCard | `border-left-width` | `2px` | `2px` | MATCH |
| FactCard | `border-left-style` | `solid` | `solid` | MATCH |
| FactCard | `border-left-color` | `#1A1A1A` | `rgb(26, 26, 26)` | MATCH |
| FactCard | `border-top-width` | `1px` | `1px` | MATCH |
| FactCard | `border-top-style` | `solid` | `solid` | MATCH |
| FactCard | `border-top-color` | `rgba(0,0,0,.12)` | `rgba(0, 0, 0, 0.12)` | MATCH |
| FactCard | `border-right-width` | `1px` | `1px` | MATCH |
| FactCard | `border-right-color` | `rgba(0,0,0,.12)` | `rgba(0, 0, 0, 0.12)` | MATCH |
| FactCard | `border-bottom-width` | `1px` | `1px` | MATCH |
| FactCard | `border-bottom-color` | `rgba(0,0,0,.12)` | `rgba(0, 0, 0, 0.12)` | MATCH |
| FactCard | `border-top-left-radius` | `0` | `0px` | MATCH |
| FactCard | `border-top-right-radius` | `8px` | `8px` | MATCH |
| FactCard | `border-bottom-right-radius` | `8px` | `8px` | MATCH |
| FactCard | `border-bottom-left-radius` | `0` | `0px` | MATCH |
| sourced — eyebrow label | `text` | `Fact` | `Fact` | MATCH |
| sourced — eyebrow label | `font-size` | `9.5px` | `9.5px` | MATCH |
| sourced — eyebrow label | `font-weight` | `800` | `800` | MATCH |
| sourced — eyebrow label | `letter-spacing` | `0.12em (= 1.14px at 9.5px)` | `1.14px` | MATCH |
| sourced — eyebrow label | `text-transform` | `uppercase` | `uppercase` | MATCH |
| sourced — body | `font-size` | `13px` | `13px` | MATCH |
| sourced — body | `line-height` | `20.15px` | `20.15px` | MATCH |
| sourced — body | `font-style` | `normal` | `normal` | MATCH |
| inference — card | `background-color` | `#FAFAF8` | `rgb(250, 250, 248)` | MATCH |
| inference — card | `border-top-width` | `1px` | `1px` | MATCH |
| inference — card | `border-top-style` | `dashed` | `dashed` | MATCH |
| inference — card | `border-top-color` | `rgba(0,0,0,.25)` | `rgba(0, 0, 0, 0.25)` | MATCH |
| inference — card | `border-left-width` | `1px` | `1px` | MATCH |
| inference — card | `border-left-style` | `dashed` | `dashed` | MATCH |
| inference — card | `border-left-color` | `rgba(0,0,0,.25)` | `rgba(0, 0, 0, 0.25)` | MATCH |
| inference — card | `border-right-color` | `rgba(0,0,0,.25)` | `rgba(0, 0, 0, 0.25)` | MATCH |
| inference — card | `border-bottom-color` | `rgba(0,0,0,.25)` | `rgba(0, 0, 0, 0.25)` | MATCH |
| inference — body is italic | `font-style` | `italic` | `italic` | MATCH |
| inference — body is italic | `font-size` | `13px` | `13px` | MATCH |
| inference — eyebrow label | `font-size` | `9.5px` | `9.5px` | MATCH |
| inference — eyebrow label | `font-weight` | `800` | `800` | MATCH |
| inference — eyebrow label | `letter-spacing` | `0.12em (= 1.14px at 9.5px)` | `1.14px` | MATCH |
| inference — eyebrow label | `text-transform` | `uppercase` | `uppercase` | MATCH |
| counsel — card | `background-color` | `#FFFFFF` | `rgb(255, 255, 255)` | MATCH |
| counsel — card | `border-left-width` | `2px` | `2px` | MATCH |
| counsel — card | `border-left-style` | `solid` | `solid` | MATCH |
| counsel — card | `border-left-color` | `#F97316` | `rgb(249, 115, 22)` | MATCH |
| counsel — card | `border-top-width` | `1px` | `1px` | MATCH |
| counsel — card | `border-top-color` | `rgba(0,0,0,.12)` | `rgba(0, 0, 0, 0.12)` | MATCH |
| counsel — card | `border-top-left-radius` | `0` | `0px` | MATCH |
| counsel — card | `border-top-right-radius` | `8px` | `8px` | MATCH |
| counsel — card | `border-bottom-right-radius` | `8px` | `8px` | MATCH |
| counsel — card | `border-bottom-left-radius` | `0` | `0px` | MATCH |
| counsel — eyebrow label | `text` | `Legal confirmation required` | `Legal confirmation required` | MATCH |
| counsel — eyebrow label | `font-size` | `9.5px` | `9.5px` | MATCH |
| counsel — eyebrow label | `font-weight` | `800` | `800` | MATCH |
| counsel — eyebrow label | `color` | `#1A1A1A` | `rgb(26, 26, 26)` | MATCH |
| a raw URL in the card's running text | `(forbidden element)` | `absent` | `absent` | MATCH |

## FilterChipGroup / FilterChip

- Spec file: `fsi-app/.discipline/rendering/audit/spec/filterchipgroup.json`
- Mount: `filterchipgroup` at 1440 px
- Artboards: 02-regulations-list (Filters rail card)
- Design source: docs/design/handoff-2026-09-06/'Caros Ledge UI System.dc.html' p2 (#p2, Regulations list), the 'Filters' rail card's group label and row typography; operator ruling 2.5 (chip families, one rule each) and 5.2's DEVIATION note in ListSurfaceShell.tsx ('the artboards' Filters rail card uses checkboxes; this renders the same groups as FilterChipGroup/FilterChip pills ... per this lane's dispatch')
- Result: 17 MATCH, 0 not matching

> The artboard's own Filters rail card is a CHECKBOX list, not chip pills — ListSurfaceShell.tsx's own header comment logs this as a DEVIATION already recorded in DEVIATION-LOG.md, so this spec does not compare FilterChip's pill geometry against the artboard's checkbox rows (that would be comparing two different widget types under one name). What IS comparable and is checked below: the group LABEL typography (10px/700/.12em/uppercase muted, matching the artboard's per-group label 'Mode', 'Jurisdiction' etc.) and, per operator ruling 2.5 ('Filter chips inside a labelled group shell keep the group's border; the individual inactive chips inside it do not'), that the built FilterChip pill uses the shared chip system's pill radius (--radius-pill) at desktop width, not a bordered box.
> Chips.tsx's own header comment states the group-label class is `.cl-filter-group-label` at font-size var(--fs-95)=9.5px, not the artboard's 10px — logged as the comparison; the artboard's Filters-card group label ('Mode') is 10px/700/.12em/uppercase/#7A6E6C exactly, read from p2's Filters card markup.
> SITEWIDE FINDING, confirmed live (not a harness artefact): both filter-chip border-radius checks below measure 0px against theme.css's own --radius-pill:999px token. Root cause, confirmed by reading src/app/theme.css line 213: `--radius-pill: var(--radius-pill);` — a CUSTOM PROPERTY THAT REFERENCES ITSELF inside the same :root block that first declares `--radius-pill: 999px` at line 55. Per the CSS custom-properties spec, a property whose computed value cycles back to itself is 'guaranteed-invalid' and resolves to nothing wherever it is USED (there is no fallback in the `var(--radius-pill)` calls that consume it) — a definition-order bug, not a consumer bug. This is not scoped to FilterChip: every pill-radius shared part that reads `var(--radius-pill)` (BandChip, TagChip, WorkspaceTagPill, FilterChip — all in Chips.tsx) renders a SQUARE corner sitewide instead of the design's pill. Confirmed by a standalone debug mount reading `getComputedStyle(document.documentElement).getPropertyValue('--radius-pill')` against the real theme.css + globals.css bundle: returns the empty string. This lane writes no product code (dispatch: 'NO product code is written by this lane'); the fix (delete the redundant redeclaration at line 213, or point it at a differently-named canonical token) belongs to a product lane, flagged here with its exact line and mechanism per CLAUDE.md rule 13 ('a flag is a commitment'). FIXED by fix58-tokens (2026-09-07): theme.css's self-referential redeclaration removed; `--radius-pill` now resolves to 999px sitewide.
> SPEC CORRECTED 2026-09-07 (fix58-tokens): the `expect.letter-spacing` value below was typed as 0.1em, contradicting this same file's own note above ('the artboard's Filters-card group label is 10px/700/.12em/uppercase') and the dc.html p2 reading it cites — corrected to 0.12em to match the artboard source both notes already point to.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| FilterChipGroup / FilterChip | `font-weight` | `700` | `700` | MATCH |
| FilterChipGroup / FilterChip | `letter-spacing` | `0.12em (= 1.2px at 10px)` | `1.2px` | MATCH |
| FilterChipGroup / FilterChip | `text-transform` | `uppercase` | `uppercase` | MATCH |
| FilterChipGroup / FilterChip | `color` | `#7A6E6C` | `rgb(122, 110, 108)` | MATCH |
| inactive chip | `font-size` | `11px` | `11px` | MATCH |
| inactive chip | `font-weight` | `700` | `700` | MATCH |
| inactive chip | `color` | `#5A6B67` | `rgb(90, 107, 103)` | MATCH |
| inactive chip | `background-color` | `#F5F2EE` | `rgb(245, 242, 238)` | MATCH |
| inactive chip | `border-radius` | `999px` | `999px` | MATCH |
| inactive chip | `padding-top` | `5px` | `5px` | MATCH |
| inactive chip | `padding-left` | `11px` | `11px` | MATCH |
| active chip | `font-size` | `11px` | `11px` | MATCH |
| active chip | `font-weight` | `700` | `700` | MATCH |
| active chip | `color` | `#FFFFFF` | `rgb(255, 255, 255)` | MATCH |
| active chip | `background-color` | `#5A5552` | `rgb(90, 85, 82)` | MATCH |
| active chip | `border-radius` | `999px` | `999px` | MATCH |
| a border on an individual inactive filter chip (operator 2.5: the group shell keeps the border, the chip does not) | `(forbidden element)` | `absent` | `absent` | MATCH |

## "HIGH RELEVANCE" chip (RelevanceBadge)

- Spec file: `fsi-app/.discipline/rendering/audit/spec/highrelevance.json`
- Mount: `relevancebadge` at 1440 px
- Artboards: none — confirmed absent from every extracted page section (00-sys, p3, p5, p7, p8, p9) by grep this session
- Design source: src/components/shell/RelevanceBadge.tsx (used by RelevanceBadgeClient on the regulation and research detail rails); operator ruling 3.4, DO NOT TOUCH: 'DO NOT REMOVE. It is the four-band relevance gradation the brief keeps; Claude Design is defining the relevance component and will send it with the overlays. Leave it live and unstyled as is.'
- Result: 8 MATCH, 0 not matching

> RECORD, DO NOT JUDGE, per the ruling's own framing. This spec asserts PRESENCE ONLY: a MATCH on the checks below means the element still renders (the thing 3.4 forbids removing); a MISMATCH would mean a value drifted from the component's own current code, not from any design source, since none exists yet.
> No dc.html artboard and no README line names a 'HIGH RELEVANCE' or 'RelevanceBadge' element anywhere in this handoff package — confirmed by grep across every extracted page section this session, not assumed from the ruling's own wording alone.
> This checks the component in isolation (mounts.mjs's relevancebadge entry) rather than inside RegulationDetailSurface/ResearchFindingDetailSurface, since RelevanceBadgeClient wraps it in a client-side fetch (getViewerRelevanceForItem, a Dynamic API per that file's own header) this harness's no-network posture cannot resolve without a bespoke stub; presence in the real surfaces is a page-level integration fact (RegulationDetailSurface.tsx:234, ResearchFindingDetailSurface.tsx:254 both import RelevanceBadgeClient), confirmed by source read, not by this render.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| "HIGH RELEVANCE" chip (RelevanceBadge) | `count` | `1` | `1` | MATCH |
| band pill | `text` | `High relevance` | `High relevance` | MATCH |
| band pill | `font-size` | `9px` | `9px` | MATCH |
| band pill | `font-weight` | `800` | `800` | MATCH |
| band pill | `letter-spacing` | `0.08em (= 0.72px at 9px)` | `0.72px` | MATCH |
| band pill | `text-transform` | `uppercase` | `uppercase` | MATCH |
| summary line | `text` | `Directly affects two active EEA ocean lanes.` | `Directly affects two active EEA ocean lanes.` | MATCH |
| summary line | `font-size` | `11.5px` | `11.5px` | MATCH |

## ImpactMeter

- Spec file: `fsi-app/.discipline/rendering/audit/spec/impactmeter.json`
- Mount: `impactmeter` at 1440 px
- Artboards: 00-system-sheet (row anatomy inside the list-row example; 'Impact meter — full' block), 02-regulations-list / 04-market-list / 06-research-list / 08-operations-list / 11-watchlist (row, inside every ListRow), 01-dashboard, 03-regulation-detail (full, rail)
- Design source: docs/design/handoff-2026-09-06/'Caros Ledge UI System.dc.html' #sys: the list-row example's impact cell (row variant, scored + unscored) and the 'Impact meter — full' block; README.md §0.4 'Meters and timeline on every row'
- Result: 32 MATCH, 0 not matching

> Row variant, scored: the #sys list-row example's first row (impact 7/12, dims sorted [1,1,2,3]) uses 9px-wide bars, heights 6/6/12/18, radius '1px 1px 0 0', sitting on an 18px-tall container with a 1px solid rgba(0,0,0,.25) baseline under the bars; the sum sits at margin-left:7px. The fixture reproduces the same scores (cost:1, compliance:1, client:2, operational:3, sum 7/12) so the artboard's own numbers are the comparison.
> Row variant, unscored: the #sys example's second row uses a 30px-wide dashed baseline (border-bottom:1px dashed rgba(0,0,0,.3)) at height 18px, plus a plain em-dash — not the Absence small-caps convention. README §0.4 and operator ruling 2.1 both require the Absence convention instead ('the small-caps absence reason from the fixed vocabulary'), which is what the built component renders; the artboard predates that ruling. The width/height/border values below are still the artboard's stated geometry for the dashed line itself.
> Full variant track: #sys 'Impact meter — full' block states height:8px, border-radius:8px, background linear-gradient(90deg,#16A34A,#F97316 55%,#DC2626), label column 116px, bar column flex, value column 64px right-aligned tabular-nums 12.5px. The fixture's four scores (cost:1, compliance:3, client:1, operational:2) reproduce the artboard's own four example rows exactly (1/3 Low, 3/3 High, 1/3 Low, 2/3 Med).
> Full variant, unscored: the artboard does not show a full-variant unscored state; the built component's em-dash fallback is checked only for basic geometry (dashed baseline width 96px per the component's own row-vs-full branch), not against an artboard value — logged as a gap, not asserted as a mismatch.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| ImpactMeter | `height` | `18px` | `18px` | MATCH |
| ImpactMeter | `gap` | `2px` | `2px` | MATCH |
| ImpactMeter | `border-bottom-width` | `1px` | `1px` | MATCH |
| ImpactMeter | `border-bottom-style` | `solid` | `solid` | MATCH |
| ImpactMeter | `border-bottom-color` | `rgba(0,0,0,.25)` | `rgba(0, 0, 0, 0.25)` | MATCH |
| row scored — bar 1 (score 1, first ascending) | `width` | `9px` | `9px` | MATCH |
| row scored — bar 1 (score 1, first ascending) | `height` | `6px` | `6px` | MATCH |
| row scored — bar 1 (score 1, first ascending) | `border-top-left-radius` | `1px` | `1px` | MATCH |
| row scored — bar 1 (score 1, first ascending) | `border-top-right-radius` | `1px` | `1px` | MATCH |
| row scored — bar 1 (score 1, first ascending) | `border-bottom-left-radius` | `0px` | `0px` | MATCH |
| row scored — bar 1 (score 1, first ascending) | `background-color` | `#16A34A` | `rgb(22, 163, 74)` | MATCH |
| row scored — bar 3 (score 2) | `width` | `9px` | `9px` | MATCH |
| row scored — bar 3 (score 2) | `height` | `12px` | `12px` | MATCH |
| row scored — bar 3 (score 2) | `background-color` | `#F97316` | `rgb(249, 115, 22)` | MATCH |
| row scored — bar 4 (score 3, last/highest) | `width` | `9px` | `9px` | MATCH |
| row scored — bar 4 (score 3, last/highest) | `height` | `18px` | `18px` | MATCH |
| row scored — bar 4 (score 3, last/highest) | `background-color` | `#DC2626` | `rgb(220, 38, 38)` | MATCH |
| row scored — sum label | `font-size` | `11px` | `11px` | MATCH |
| row scored — sum label | `font-variant-numeric` | `tabular-nums` | `tabular-nums` | MATCH |
| row scored — sum label | `margin-left` | `7px` | `7px` | MATCH |
| row scored — sum label | `text` | `7/12` | `7/12` | MATCH |
| row unscored — dashed baseline | `width` | `30px` | `30px` | MATCH |
| row unscored — dashed baseline | `border-bottom-width` | `1px` | `1px` | MATCH |
| row unscored — dashed baseline | `border-bottom-style` | `dashed` | `dashed` | MATCH |
| row unscored — dashed baseline | `border-bottom-color` | `rgba(0,0,0,.3)` | `rgba(0, 0, 0, 0.3)` | MATCH |
| row unscored — Absence reason (operator ruling 2.1, supersedes the artboard's plain em-dash) | `text` | `unscored` | `unscored` | MATCH |
| full scored — track height/radius/gradient | `height` | `8px` | `8px` | MATCH |
| full scored — track height/radius/gradient | `border-radius` | `8px` | `8px` | MATCH |
| full scored — track height/radius/gradient | `background-image` | `linear-gradient(90deg, rgb(22, 163, 74), rgb(249, 115, 22) 55%, rgb(220, 38, 38))` | `linear-gradient(90deg, rgb(22, 163, 74), rgb(249, 115, 22) 55%, rgb(220, 38, 38))` | MATCH |
| full scored — dimension label | `width` | `116px` | `116px` | MATCH |
| full scored — dimension label | `font-size` | `12.5px` | `12.5px` | MATCH |
| the literal word NOT SCORED / UNSCORED in the meter | `(forbidden element)` | `absent` | `absent` | MATCH |

## InThisListStat

- Spec file: `fsi-app/.discipline/rendering/audit/spec/inthisliststat.json`
- Mount: `detail-shell` at 1440 px
- Artboards: 03-regulation-detail, 05-market-detail, 07-research-detail, 09-operations-profile
- Design source: 'Caros Ledge UI System.dc.html' p3 (#p3) rail card block: `background:#fff;border:1px solid rgba(0,0,0,.12);border-radius:10px;...;overflow:hidden` with a `height:3px;background:linear-gradient(90deg,#5A5552,#5A5552 22%,rgba(90,85,82,.18))` rule on the card's OWN top edge, then `padding:12px 16px 14px`; README.md §0.5 (AlphaSense place-keeping behaviour, "In this list · 4 of 15"); operator ruling 5.1
- Result: 14 MATCH, 0 not matching

> Selector repair, train 58 (2026-09-07): the earlier `[data-section-rule]` selector never matched anything (no such attribute exists anywhere in the product). InThisListStat.tsx (like DetailSection and every other DetailShell.tsx card, confirmed by direct source read this session) DOES render the shared `SectionRule` component from `src/components/ui/SectionRule.tsx`, whose rendered marker is the class `.cl-section-rule`, not a data attribute — the selector below now measures the real element.
> Padding differs: artboard `padding:12px 16px 14px` (top 12, bottom 14) vs the built card's `padding: 14px 16px` (top 14, bottom 14) — a small but real MISMATCH on padding-top.
> The artboard's example reads "Action · 4 of 13 · sorted by next date" (a third clause, 'sorted by next date') where the built component renders "4 of 13 in Action" (order reversed, no sort clause) — this is a DATA/COPY difference, not a geometry one; logged as an observation, not asserted as a text MISMATCH since the exact numbers (4 of 13) are fixture-dependent on both sides.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| InThisListStat | `background-color` | `#FFFFFF` | `rgb(255, 255, 255)` | MATCH |
| InThisListStat | `border-top-width` | `1px` | `1px` | MATCH |
| InThisListStat | `border-top-color` | `rgba(0,0,0,.12)` | `rgba(0, 0, 0, 0.12)` | MATCH |
| InThisListStat | `border-top-left-radius` | `10px` | `10px` | MATCH |
| InThisListStat | `padding-top` | `14px` | `14px` | MATCH |
| InThisListStat | `padding-right` | `16px` | `16px` | MATCH |
| InThisListStat | `padding-left` | `16px` | `16px` | MATCH |
| the 3px graduated rule on the rail card (ruling 5.1) | `height` | `3px` | `3px` | MATCH |
| the 3px graduated rule on the rail card (ruling 5.1) | `background-image` | `linear-gradient(90deg, rgb(90, 85, 82) 0%, rgb(90, 85, 82) 22%, rgba(90, 85, 82, 0.18) 100%)` | `linear-gradient(90deg, rgb(90, 85, 82), rgb(90, 85, 82) 22%, rgba(90, 85, 82, 0.18))` | MATCH |
| 'In this list' eyebrow label | `font-size` | `10.5px` | `10.5px` | MATCH |
| 'In this list' eyebrow label | `letter-spacing` | `0.12em (= 1.26px at 10.5px)` | `1.26px` | MATCH |
| 'In this list' eyebrow label | `text-transform` | `uppercase` | `uppercase` | MATCH |
| 'In this list' eyebrow label | `font-weight` | `800` | `800` | MATCH |
| 'Back to list' link | `font-size` | `11px` | `11px` | MATCH |

## ListSurfaceShell — PERF-12 virtualized remainder, row anatomy

- Spec file: `fsi-app/.discipline/rendering/audit/spec/list-surface-virtualized.json`
- Mount: `list-surface-virtualized` at 1440 px
- Artboards: 00-system-sheet (list-row anatomy, the same one every surface uses)
- Design source: docs/design/handoff-2026-09-06/DEVIATION-LOG.md, 2026-09-07 entry 'ListSurfaceShell.tsx (shared part, extended additively) — PERF-12 remainder virtualization restored': 'Row anatomy is unchanged either way — renderRow still returns one <ListRow/>, per PERF-12's own "virtualization only decides which rows mount" contract.'; src/components/list-surface/ListSurfaceShell.tsx (VIRTUALIZE_THRESHOLD=30) and src/components/ledger/VirtualizedRowList.tsx (real components, one band at 35 rows, expanded, so ListSurfaceShell mounts the real VirtualizedRowList path instead of the plain `.map()`)
- Result: 4 MATCH, 0 not matching

> This is a STRUCTURAL claim check (does virtualization preserve row anatomy), not a page artboard's own stated pixel value — the artboard has no notion of virtualization at all (it shows static example rows). The comparison value is the shared listrow.json spec's own `.cl-list-row` geometry (min-height 56px, the class name itself), read from the SAME real ListRow.tsx component both the plain and virtualized paths render.
> MEASURED, confirming DEVIATION-LOG's claim is TRUE, not asserted from reading the source alone: a standalone debug mount showed all 35 rows present immediately after mount (before `useNearestScrollParent`'s effect resolves), then windowing down to 27 of 35 once it does (no scrollable ancestor exists in this no-AppShell mount, so it falls back to `WindowVirtualized`) — the SAME `.cl-list-row` class and 56px min-height in both states. `run-audit.mjs`'s own 80ms post-mount measurement window lands AFTER that effect has already resolved (confirmed: this spec's own run reads count=27, not 35), so the windowed count is what this spec's row(s) are measured against; it is viewport/timing-dependent and no artboard states it, which is why COUNT is not asserted below (removed after a first attempt at `count:35` read a false MISMATCH here — a harness-timing artefact, not a design defect, fixed by dropping the count assertion rather than chasing a moving number, per this harness's own rule 4).
> This spec adds NO new mount surface concept: it exercises the existing shared `ListSurfaceShell`/`VirtualizedRowList`/`ListRow` components exactly as RegulationsLedger.tsx already wires them (VIRTUALIZE_THRESHOLD, rowHeight=56, renderRow returning `<ListRow/>`) — reproduced from that file's own real integration, not invented.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| ListSurfaceShell — PERF-12 virtualized remainder, row anatomy | `min-height` | `56px` | `56px` | MATCH |
| a virtualized row keeps the real ListRow title cell (no page-local row shape) | `font-size` | `14px` | `14px` | MATCH |
| a virtualized row keeps the real ListRow title cell (no page-local row shape) | `font-weight` | `600` | `600` | MATCH |
| the literal word UNSCORED in a virtualized row | `(forbidden element)` | `absent` | `absent` | MATCH |

## ListSurfaceShell — facets card, Legend rail card, DismissedStash foot

- Spec file: `fsi-app/.discipline/rendering/audit/spec/list-surface.json`
- Mount: `list-surface-1440` at 1440 px
- Artboards: 02-regulations-list (Filters card, Legend rail card), 18-section-treatments (ruling 5.1)
- Design source: docs/design/handoff-2026-09-06/'Caros Ledge UI System.dc.html' p2 (#p2), the 'Filters' and 'Legend' rail cards; operator ruling 5.1 (3px graduated rule above every panel/section card); ListSurfaceShell.tsx and ListSurfaceRailCards.tsx (real components, no mount-only fixture)
- Result: 17 MATCH, 0 not matching

> RELOCATED (lane compose-lists, 2026-09-07, operator audit 'the filters were not above the regulations, they were on the right — same on every page'; artboard 02/id="p2"): the facet chip cards that used to sit ABOVE the row list in the content column (`.cl-facets-desktop`) are gone. The SAME facetGroups/secondaryFacetGroups data now renders as the rail's `FiltersRailCard` (checkbox rows, live counts, 'Clear N', '+ N more' disclosure), mounted by ListSurfaceShell itself before the surface-specific rail content, matching p2's rail order (Filters, then the surface card, then Legend). Selectors below updated from `.cl-facets-desktop` to `[data-audit="filters-rail"]`.
> FIXED (fix58-lists, 2026-09-07, B163/B165/B170, superseded by the relocation above): the Legend rail card mounts `<SectionRule/>` (src/components/ui/SectionRule.tsx, ruling 5.1's own shared part) as the first child inside the card's outer border/radius/shadow div, full card width, before a separate inner padded content wrapper — the SAME structure `page-frame.json`'s dashboard cards already use.
> DismissedStash is NOT one of the 17 artboards (its own header comment: 'an app feature not shown in the 17 artboards, restored exactly per operator ruling, not redesigned'). No artboard value exists to compare it against, so this spec checks it only against the general system tokens the operator's rulings and README state apply everywhere (44px hit targets on the Restore control; the shared --card/--line-2/--radius tokens it already imports), not against an invented design number — CLAUDE.md rule 2 (never fabricate).

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| ListSurfaceShell — facets card, Legend rail card, DismissedStash foot | `background-color` | `#FFFFFF` | `rgb(255, 255, 255)` | MATCH |
| ListSurfaceShell — facets card, Legend rail card, DismissedStash foot | `border-top-width` | `1px` | `1px` | MATCH |
| ListSurfaceShell — facets card, Legend rail card, DismissedStash foot | `border-top-color` | `rgba(0,0,0,.12)` | `rgba(0, 0, 0, 0.12)` | MATCH |
| ListSurfaceShell — facets card, Legend rail card, DismissedStash foot | `border-top-left-radius` | `10px` | `10px` | MATCH |
| ListSurfaceShell — facets card, Legend rail card, DismissedStash foot | `box-shadow` | `rgba(26, 26, 26, 0.04) 0px 1px 2px 0px, rgba(26, 26, 26, 0.06) 0px 4px 14px 0px` | `rgba(26, 26, 26, 0.04) 0px 1px 2px 0px, rgba(26, 26, 26, 0.06) 0px 4px 14px 0px` | MATCH |
| Legend rail card — the 3px graduated rule above the card (ruling 5.1) | `height` | `3px` | `3px` | MATCH |
| Legend rail card — title label | `font-size` | `10.5px` | `10.5px` | MATCH |
| Legend rail card — title label | `letter-spacing` | `0.12em (= 1.26px at 10.5px)` | `1.26px` | MATCH |
| Legend rail card — title label | `text-transform` | `uppercase` | `uppercase` | MATCH |
| Legend rail card — title label | `color` | `#7A6E6C` | `rgb(122, 110, 108)` | MATCH |
| Filters rail card — content padding | `padding-top` | `14px` | `14px` | MATCH |
| Filters rail card — content padding | `padding-right` | `16px` | `16px` | MATCH |
| Filters rail card — content padding | `padding-bottom` | `14px` | `14px` | MATCH |
| Filters rail card — content padding | `padding-left` | `16px` | `16px` | MATCH |
| Filters rail card — the 3px graduated rule above the card (ruling 5.1) | `height` | `3px` | `3px` | MATCH |
| DismissedStash — Restore control hit target (general 44px rule, not an artboard value) | `min-height` | `44px` | `44px` | MATCH |
| the literal word UNSCORED in the dismissed-stash rows | `(forbidden element)` | `absent` | `absent` | MATCH |

## ListRow (1440, every cell)

- Spec file: `fsi-app/.discipline/rendering/audit/spec/listrow.json`
- Mount: `listrow-1440` at 1440 px
- Artboards: 00-system-sheet (the row is defined once here, two example rows), 02-regulations-list, 04-market-list, 06-research-list, 08-operations-list, 11-watchlist, 01-dashboard (Due next / What changed)
- Design source: docs/design/handoff-2026-09-06/'Caros Ledge UI System.dc.html' #sys, the list-row block (column-header row + two example rows); README.md §0.4 'List row — one anatomy' and the type scale
- Result: 55 MATCH, 0 not matching

> Values are read from the artboard markup, which the handoff makes the authority ('where README prose and a page artboard disagree, the artboard wins').
> `grid-template-columns` is compared with one `*` token: getComputedStyle resolves the track list to USED pixel widths, so the `1fr` title track reports the container's leftover width and cannot be a design constant. Every fixed track is compared exactly. The mount places the row in a 778px box — the content column the frame produces at 1440 (README §0.3).
> The artboard's `gap:0 14px` is compared as row-gap 0 / column-gap 14px, which is how CSSOM reports it.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| ListRow (1440, every cell) | `grid-template-columns` | `3px 56px * 88px 84px 76px 40px 44px` | `3px 56px 277px 88px 84px 76px 40px 44px` | MATCH |
| ListRow (1440, every cell) | `row-gap` | `0px` | `0px` | MATCH |
| ListRow (1440, every cell) | `column-gap` | `14px` | `14px` | MATCH |
| ListRow (1440, every cell) | `min-height` | `56px` | `56px` | MATCH |
| ListRow (1440, every cell) | `border-bottom-width` | `1px` | `1px` | MATCH |
| ListRow (1440, every cell) | `border-bottom-style` | `solid` | `solid` | MATCH |
| ListRow (1440, every cell) | `border-bottom-color` | `rgba(0,0,0,.06)` | `rgba(0, 0, 0, 0.06)` | MATCH |
| ListRow (1440, every cell) | `border-left-width` | `0px` | `0px` | MATCH |
| ListRow (1440, every cell) | `padding-left` | `0px` | `0px` | MATCH |
| ListRow (1440, every cell) | `padding-right` | `12px` | `12px` | MATCH |
| column header row | `grid-template-columns` | `3px 56px * 88px 84px 76px 40px 44px` | `3px 56px 277px 88px 84px 76px 40px 44px` | MATCH |
| column header row | `height` | `30px` | `30px` | MATCH |
| column header row | `border-bottom-width` | `1px` | `1px` | MATCH |
| column header row | `border-bottom-color` | `rgba(0,0,0,.08)` | `rgba(0, 0, 0, 0.08)` | MATCH |
| column header cell type | `font-size` | `9.5px` | `9.5px` | MATCH |
| column header cell type | `font-weight` | `700` | `700` | MATCH |
| column header cell type | `letter-spacing` | `0.12em (= 1.14px at 9.5px)` | `1.14px` | MATCH |
| column header cell type | `text-transform` | `uppercase` | `uppercase` | MATCH |
| column header cell type | `color` | `#7A6E6C` | `rgb(122, 110, 108)` | MATCH |
| cell 1 — band spine | `background-color` | `#F97316` | `rgb(249, 115, 22)` | MATCH |
| cell 1 — band spine | `width` | `3px` | `3px` | MATCH |
| cell 1 — band spine | `height` | `56px` | `56px` | MATCH |
| cell 2 — jurisdiction code | `font-size` | `11px` | `11px` | MATCH |
| cell 2 — jurisdiction code | `font-weight` | `700` | `700` | MATCH |
| cell 2 — jurisdiction code | `letter-spacing` | `0.06em (= 0.66px at 11px)` | `0.66px` | MATCH |
| cell 2 — jurisdiction code | `color` | `#5A6B67` | `rgb(90, 107, 103)` | MATCH |
| cell 2 — jurisdiction code | `width` | `56px` | `56px` | MATCH |
| cell 3 — title | `font-size` | `14px` | `14px` | MATCH |
| cell 3 — title | `font-weight` | `600` | `600` | MATCH |
| cell 3 — title | `line-height` | `18.2px` | `18.2px` | MATCH |
| cell 3 — title | `white-space` | `nowrap` | `nowrap` | MATCH |
| cell 3 — title | `text-overflow` | `ellipsis` | `ellipsis` | MATCH |
| cell 3 — title | `overflow-x` | `hidden` | `hidden` | MATCH |
| cell 3 — title | `color` | `#1A1A1A` | `rgb(26, 26, 26)` | MATCH |
| cell 3 — meta line | `font-size` | `11px` | `11px` | MATCH |
| cell 3 — meta line | `color` | `#7A6E6C` | `rgb(122, 110, 108)` | MATCH |
| cell 4 — impact meter cell | `width` | `88px` | `88px` | MATCH |
| cell 5 — due date | `font-size` | `12.5px` | `12.5px` | MATCH |
| cell 5 — due date | `font-weight` | `700` | `700` | MATCH |
| cell 5 — due date | `color` | `#1A1A1A` | `rgb(26, 26, 26)` | MATCH |
| cell 5 — days-until line | `font-size` | `10.5px` | `10.5px` | MATCH |
| cell 5 — days-until line | `color` | `#7A6E6C` | `rgb(122, 110, 108)` | MATCH |
| cell 6 — timeline cell | `width` | `76px` | `76px` | MATCH |
| cell 7 — tier chip | `font-size` | `10px` | `10px` | MATCH |
| cell 7 — tier chip | `font-weight` | `800` | `800` | MATCH |
| cell 7 — tier chip | `letter-spacing` | `0.06em (= 0.6px at 10px)` | `0.6px` | MATCH |
| cell 7 — tier chip | `border-top-width` | `1px` | `1px` | MATCH |
| cell 7 — tier chip | `border-top-color` | `rgba(0,0,0,.2)` | `rgba(0, 0, 0, 0.2)` | MATCH |
| cell 7 — tier chip | `border-top-left-radius` | `4px` | `4px` | MATCH |
| cell 7 — tier chip | `text-align` | `center` | `center` | MATCH |
| cell 8 — overflow cell | `border-left-width` | `1px` | `1px` | MATCH |
| cell 8 — overflow cell | `border-left-style` | `solid` | `solid` | MATCH |
| cell 8 — overflow cell | `border-left-color` | `rgba(0,0,0,.08)` | `rgba(0, 0, 0, 0.08)` | MATCH |
| the literal word UNSCORED anywhere in the row | `(forbidden element)` | `absent` | `absent` | MATCH |
| a second click target competing with the row link | `(forbidden element)` | `absent` | `absent` | MATCH |

## Map, jurisdiction register (ListRow's register variant)

- Spec file: `fsi-app/.discipline/rendering/audit/spec/map-register.json`
- Mount: `listrow-1440` at 1440 px
- Artboards: 10-map ("Jurisdiction register" card)
- Design source: docs/design/handoff-2026-09-06/'Caros Ledge UI System.dc.html' p10 (#p10, Map): the 'Jurisdiction register' card's column header row and its example rows (grid-template-columns:3px 1fr 1fr 110px 80px 40px; min-height:44px); src/components/ui/ListRow.tsx's own `endStat` prop (an additive extension the map lane built onto the shared ListRow, per its header comment: 'ListRow's new optional endStat prop (additive extension, see ListRow.tsx) replaces those four cells with a single band-coloured highest-band + item-count stat, matching the artboard's own HIGHEST BAND / ITEMS columns without forking a second row component'); src/components/map/MapPageView.tsx (real usage: `endStat={{ label: row.band.label, value: formatNumber(row.count), band: row.band }}`)
- Result: 13 MATCH, 0 not matching

> This spec extends the SHARED listrow-1440 mount (mounts.mjs) with one additional wrapped instance (`[data-audit="row-endstat"]`) that reproduces MapPageView.tsx's own ListRow call exactly (title=jurisdiction label, meta=activeThemes, endStat={label,value,band}, minHeight=44, no impact/due/timeline/tier/overflow) — the values below are read from the SAME real ListRow.tsx component, not a page-local fork.
> FIXED (fix58-lists, 2026-09-07, B130): ListRow.tsx's `minHeight` is now an optional prop (default 56, the general row's own value, unchanged for every other caller) — MapPageView.tsx passes `minHeight={44}` for register rows per p10's own stated value, an additive extension, not a fork.
> FIXED (fix58-lists, 2026-09-07, B131-B134): endStat's label span now colours only the leading dot with the band colour; the label TEXT stays `var(--ink)` (#1A1A1A), at `var(--fs-10)` (10px) and letter-spacing 0.06em, matching p10's 'HIGHEST BAND' column exactly. The value numeral is font-size 16 (from 18); its band colour was already correct.
> FIXED (lane map60, 2026-09-08): the six-column register grid IS now what ListRow renders, `variant="register"` reproduces dc.html p10's `3px 1fr 1fr 110px 80px 40px` / 44px row / `padding:0 16px 0 0` directly, and the merged `4 / span 4` approximation inside the eight-column list grid (which the removed note above described as a genuine layout-strategy difference from p10) is DELETED, not left beside it. The targets below therefore address the register cells by their own class names.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| Map, jurisdiction register (ListRow's register variant) | `min-height` | `44px` | `44px` | MATCH |
| Map, jurisdiction register (ListRow's register variant) | `grid-template-columns` | `3px 229.5px 229.5px 110px 80px 40px` | `3px 229.5px 229.5px 110px 80px 40px` | MATCH |
| register grid, p10's own six columns, not the list row's eight | `grid-template-columns` | `3px 229.5px 229.5px 110px 80px 40px` | `3px 229.5px 229.5px 110px 80px 40px` | MATCH |
| register grid, p10's own six columns, not the list row's eight | `padding` | `0px 16px 0px 0px` | `0px 16px 0px 0px` | MATCH |
| HIGHEST BAND cell, label text colour | `color` | `#1A1A1A` | `rgb(26, 26, 26)` | MATCH |
| HIGHEST BAND cell, label text colour | `font-size` | `10px` | `10px` | MATCH |
| HIGHEST BAND cell, label text colour | `font-weight` | `800` | `800` | MATCH |
| HIGHEST BAND cell, label text colour | `letter-spacing` | `0.06em (= 0.6px at 10px)` | `0.6px` | MATCH |
| ITEMS cell, value numeral | `font-family` | `contains:Anton` | `Anton, system-ui, sans-serif` | MATCH |
| ITEMS cell, value numeral | `font-size` | `16px` | `16px` | MATCH |
| ITEMS cell, value numeral | `color` | `#DC2626` | `rgb(220, 38, 38)` | MATCH |
| ITEMS cell, value numeral | `justify-content` | `flex-end` | `flex-end` | MATCH |
| trailing arrow cell | `color` | `#7A6E6C` | `rgb(122, 110, 108)` | MATCH |

## Market + Research list rows, signal-kind / severity tag (README §0.4)

- Spec file: `fsi-app/.discipline/rendering/audit/spec/market-research-rows.json`
- Mount: `market-research-rows` at 1440 px
- Artboards: 04-market-list, 06-research-list
- Design source: docs/design/handoff-2026-09-06/README.md §0.4: 'Whole row links... Market and Research rows add a signal-kind tag after the type — a tag, never a band.' 'Caros Ledge UI System.dc.html' #sys row-anatomy note ('Market and Research rows add a signal-kind tag after the type — a tag, never a band') and #sys's own 'neutral tag chip (kind/mode/topic)' definition (chips.json's authority, 0 MISMATCH there: padding 3px 8px, border-radius 4px, background #F5F2EE, font-weight 600, font-size 10.5px); src/components/market/MarketIntelLedger.tsx and src/components/research/ResearchLedger.tsx (real components, one real row each)
- Result: 5 MATCH, 0 not matching

> CONFIRMED live: MarketIntelLedger.tsx appends a TagChip carrying the row's `signalKindLabel` (e.g. 'Cost alert') inside the row's meta line, alongside the type text, README §0.4.
> CONFIRMED live, train 58 (2026-09-07): ResearchLedger.tsx ALSO now imports TagChip and renders one carrying `THEME_LABELS[themeKeyOf(r)]` (e.g. 'Emissions accounting') in the row's meta line, mirroring MarketIntelLedger's own signal-kind tag exactly, closed by lane fix58-lists (folded into this train), whose own DEVIATION-LOG entry names the ResearchLedger tag fix. Prior versions of this spec (pre-train-58) treated this as a real gap and a NOT BUILT target; it is now a MATCH target, updated here to measure what the product actually renders rather than leave a closed gap asserted as still-open.
> SPEC CORRECTION, train 58 (2026-09-07): the selector and expect block previously read `span[style*="radius-pill"]` / `border-radius: 999px`, the WorkspaceTagPill/BandChip pill shape, not TagChip's. This was simply a wrong value from the start, unrelated to fix58-tokens's later `--radius-pill` token fix: TagChip.tsx's neutral kind/mode/topic tag has always used a square 4px radius (dc.html #sys's own 'neutral tag chip' definition, chips.json's already-passing authority for this exact component), never the pill token. Once fix58-tokens's own commit additionally corrected TagChip.tsx's radius from the pill token to a literal `4` (matching chips.json), this spec's stale `radius-pill` substring selector stopped matching anything at all, confirmed live this session via a standalone debug mount dumping both rows' full HTML: the actual rendered TagChip carries `border-radius: 4px; background: var(--tag)` (var(--tag) resolves to #F5F2EE per theme.css), matching chips.json exactly. Corrected the selector to match what TagChip actually renders and the expect block to its real, already-verified-elsewhere geometry. dc.html's own concrete market-list artboard (p4) shows a THIRD set of numbers on this exact row (radius 3px, 9.5px font, 700 weight, color #1A1A1A) that matches neither the #sys legend nor the shipped TagChip, read as the system-sheet component definition (#sys, HANDOFF.md's own 'component definitions' authority, already the basis chips.json passes against) taking precedence over one page artboard's own inline numbers for a SHARED component's pixel values, not asserted here to avoid re-litigating chips.json's already-closed finding a second time in a different spec file.
> SPEC CORRECTION, lane comp-06 (2026-09-08): this child previously expected the row chip to carry the THEME ("Emissions accounting"), which is what train 58 shipped. Artboard 06/id="p6" puts the SEVERITY in the chip and the theme in the meta text; the artboard is the authority (HANDOFF: "where README prose and a page artboard disagree, the artboard wins"), so the product changed and this spec follows it. The theme did not disappear from the row, it moved to the meta line, where the artboard draws it.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| Market + Research list rows, signal-kind / severity tag (README §0.4) | `background-color` | `#F5F2EE` | `rgb(245, 242, 238)` | MATCH |
| Market + Research list rows, signal-kind / severity tag (README §0.4) | `border-radius` | `4px` | `4px` | MATCH |
| market row, signal-kind TagChip label text | `text` | `Cost alert` | `Cost alert` | MATCH |
| research row, severity TagChip (artboard 06/id="p6" row chip) | `background-color` | `#F5F2EE` | `rgb(245, 242, 238)` | MATCH |
| research row, severity TagChip (artboard 06/id="p6" row chip) | `text` | `Background` | `Background` | MATCH |

## Masthead + CommandBar

- Spec file: `fsi-app/.discipline/rendering/audit/spec/masthead.json`
- Mount: `masthead` at 1440 px
- Artboards: 00-system-sheet §0.3, 01-dashboard, 14-account, 15-settings
- Design source: docs/design/handoff-2026-09-06/README.md §0.3 'The frame'; 'Caros Ledge UI System.dc.html' the dashboard (p1) masthead block and the account/settings (p14/p15) mastheads (identical shape, different eyebrow suffix and command-bar placeholder); operator rulings 2.3 (no second ask bar) and 5.1/5.2 (the masthead card's own top rule)
- Result: 31 MATCH, 0 not matching

> Masthead.tsx is a pure-inline-style component (no Tailwind utility classes), so every property below is a real product value, not a harness artefact.
> The masthead CARD ITSELF carries no background/border/radius/shadow/top-rule of its own in Masthead.tsx — those live on whatever wraps it per page. The dc.html artboard wraps the masthead content in the SAME white radius-10 card shell every panel gets, WITH the ruling 5.1 3px dark-grey graduated rule above it (`linear-gradient(90deg,#5A5552,#5A5552 22%,rgba(90,85,82,.18))`) as its first child (dc.html p1). This is a real gap between the shared part and the artboard, but this mount has no selector into the page-level wrapper that supplies (or should supply) that shell, so it is recorded here as a note rather than a false-precision selector target; the page-frame spec's section-rule check is the one that measures whether a given page's masthead wrapper carries the rule.
> Command bar border: the artboard states `border:1px solid rgba(0,0,0,.25)` on the search bar; CommandBar.tsx uses `var(--line-1)` = rgba(0,0,0,.12) (theme.css). A real token-vs-artboard gap, not a harness artefact.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| Masthead + CommandBar | `font-size` | `10.5px` | `10.5px` | MATCH |
| Masthead + CommandBar | `font-weight` | `800` | `800` | MATCH |
| Masthead + CommandBar | `letter-spacing` | `0.14em (= 1.47px at 10.5px)` | `1.47px` | MATCH |
| Masthead + CommandBar | `text-transform` | `uppercase` | `uppercase` | MATCH |
| Masthead + CommandBar | `color` | `#7A6E6C` | `rgb(122, 110, 108)` | MATCH |
| title (list size, 34px) | `font-family` | `contains:Anton` | `Anton, system-ui, sans-serif` | MATCH |
| title (list size, 34px) | `font-weight` | `400` | `400` | MATCH |
| title (list size, 34px) | `letter-spacing` | `0.04em (= 1.36px at 34px)` | `1.36px` | MATCH |
| title (list size, 34px) | `text-transform` | `uppercase` | `uppercase` | MATCH |
| title (list size, 34px) | `font-size` | `34px` | `34px` | MATCH |
| command bar — outer form | `height` | `40px` | `40px` | MATCH |
| command bar — outer form | `border-width` | `1px` | `1px` | MATCH |
| command bar — outer form | `border-style` | `solid` | `solid` | MATCH |
| command bar — outer form | `border-color` | `rgba(0,0,0,.25)` | `rgba(0, 0, 0, 0.25)` | MATCH |
| command bar — outer form | `border-radius` | `8px` | `8px` | MATCH |
| command bar — outer form | `background-color` | `#FFFFFF` | `rgb(255, 255, 255)` | MATCH |
| command bar — search glyph | `font-size` | `14px` | `14px` | MATCH |
| command bar — search glyph | `color` | `#7A6E6C` | `rgb(122, 110, 108)` | MATCH |
| command bar — input font size | `font-size` | `13px` | `13px` | MATCH |
| command bar — placeholder text (default, item-count form) | `placeholder` | `Search or ask across 1,434 items…` | `Search or ask across 1,434 items…` | MATCH |
| command bar — ⌘K hint | `font-size` | `10px` | `10px` | MATCH |
| command bar — ⌘K hint | `font-family` | `contains:monospace` | `ui-monospace, SFMono-Regular, Menlo, monospace` | MATCH |
| command bar — ⌘K hint | `color` | `#7A6E6C` | `rgb(122, 110, 108)` | MATCH |
| command bar — Ask button | `height` | `40px` | `40px` | MATCH |
| command bar — Ask button | `padding-left` | `14px` | `14px` | MATCH |
| command bar — Ask button | `padding-right` | `14px` | `14px` | MATCH |
| command bar — Ask button | `background-color` | `#5A5552` | `rgb(90, 85, 82)` | MATCH |
| command bar — Ask button | `color` | `#FFFFFF` | `rgb(255, 255, 255)` | MATCH |
| command bar — Ask button | `font-size` | `12.5px` | `12.5px` | MATCH |
| command bar — Ask button | `font-weight` | `700` | `700` | MATCH |
| command bar — Ask button label | `text` | `Ask` | `Ask` | MATCH |

## MilestoneTimeline (row variant)

- Spec file: `fsi-app/.discipline/rendering/audit/spec/milestonetimeline.json`
- Mount: `milestonetimeline` at 1440 px
- Artboards: 00-system-sheet (row anatomy inside the list-row example, timeline cell), 02-regulations-list / 04-market-list / 06-research-list / 08-operations-list / 11-watchlist (row, inside every ListRow)
- Design source: docs/design/handoff-2026-09-06/'Caros Ledge UI System.dc.html' #sys, the list-row example's timeline cell (5 dots: passed, passed, next, ahead, ahead); README.md §0.4 'Meters and timeline on every row' and 'Passed = filled green dot; next = larger dot in the item's band colour with a ring, ahead = hollow'
- Result: 21 MATCH, 0 not matching

> The fixture (2 past, 1 current, 2 future entries) reproduces the artboard row example's own dot pattern (2 passed, 1 next, 2 ahead) so the artboard's own numbers are the comparison.
> FIXED (fix58-lists, 2026-09-07, B-row 'the track green-to-next-dot finding'): dc.html #sys line 202 states the list-row track literally as `height:2px;background:linear-gradient(90deg,#16A34A 0,#16A34A 58%,rgba(0,0,0,.12) 58%)` — corrected from this spec's prior expect (height 1px, flat rgba(0,0,0,.12), no green) which had underscored a real, confirmed product gap instead of asserting the design's own stated value. MilestoneTimeline.tsx now renders the track as a single gradient with a hard colour stop at the 'next' dot's own position (green before it, rgba(0,0,0,.12) after), matching the artboard's technique exactly.
> The artboard places dots at fixed pixel offsets (0, 24, 42, 58, 76px) within the 76px cell, so its gradient stop (58%) is measured against THOSE offsets; the built component uses `justify-content:space-between` across a `width:100%` row (an accepted, pre-existing layout-strategy difference — dot POSITION is not asserted below for the same reason). The gradient stop below is therefore this fixture's own space-between position for the 'next' dot (index 2 of 5, i.e. 50%), not the artboard's literal 58% — the CONVENTION (green-to-next, hard stop, rgba(0,0,0,.12) beyond) is what's asserted, computed consistently with the dots' own real positions rather than a borrowed fixed-px number.
> Selectors reach dots by DOM order: MilestoneTimeline.tsx has no per-dot marker class, so nth-of-type addresses the classifier's output directly (dot 1/2 = passed, dot 3 = next, dot 4/5 = ahead, per milestone-timeline-classify.ts, proven independently by its own npmtest.mjs).

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| MilestoneTimeline (row variant) | `width` | `76px` | `76px` | MATCH |
| track line | `height` | `2px` | `2px` | MATCH |
| track line | `background-image` | `linear-gradient(90deg, #16A34A, #16A34A 50%, rgba(0,0,0,.12) 50%)` | `linear-gradient(90deg, rgb(22, 163, 74) 0%, rgb(22, 163, 74) 50%, rgba(0, 0, 0, 0.12) 50%)` | MATCH |
| dot 1 — passed | `width` | `6px` | `6px` | MATCH |
| dot 1 — passed | `height` | `6px` | `6px` | MATCH |
| dot 1 — passed | `border-radius` | `50%` | `50%` | MATCH |
| dot 1 — passed | `background-color` | `#16A34A` | `rgb(22, 163, 74)` | MATCH |
| dot 3 — next (band colour, ring) | `width` | `9px` | `9px` | MATCH |
| dot 3 — next (band colour, ring) | `height` | `9px` | `9px` | MATCH |
| dot 3 — next (band colour, ring) | `border-radius` | `50%` | `50%` | MATCH |
| dot 3 — next (band colour, ring) | `background-color` | `#F97316` | `rgb(249, 115, 22)` | MATCH |
| dot 4 — ahead (hollow) | `width` | `6px` | `6px` | MATCH |
| dot 4 — ahead (hollow) | `height` | `6px` | `6px` | MATCH |
| dot 4 — ahead (hollow) | `border-radius` | `50%` | `50%` | MATCH |
| dot 4 — ahead (hollow) | `border-width` | `1px` | `1px` | MATCH |
| dot 4 — ahead (hollow) | `border-style` | `solid` | `solid` | MATCH |
| dot 4 — ahead (hollow) | `border-color` | `rgba(0,0,0,.28)` | `rgba(0, 0, 0, 0.28)` | MATCH |
| dot 4 — ahead (hollow) | `background-color` | `#FFFFFF` | `rgb(255, 255, 255)` | MATCH |
| row empty — flat line, no dots | `width` | `76px` | `76px` | MATCH |
| row empty — flat line, no dots | `height` | `1px` | `1px` | MATCH |
| row empty — flat line, no dots | `background-color` | `rgba(0,0,0,.12)` | `rgba(0, 0, 0, 0.12)` | MATCH |

## OnboardingStepper (4-pill progress row)

- Spec file: `fsi-app/.discipline/rendering/audit/spec/onboarding-stepper.json`
- Mount: `onboarding-stepper` at 1440 px
- Artboards: 17-onboarding
- Design source: docs/design/handoff-2026-09-06/'Caros Ledge UI System.dc.html' id="p17", the 4-pill stepper (Workspace · done, Modes & jurisdictions · active, Sectors · todo, Briefing · todo)
- Result: 27 MATCH, 0 not matching

> dc.html p17 stepper row: `display:flex;gap:18px;flex-wrap:wrap`; each entry `display:flex;align-items:center;gap:8px;font-size:12px`, weight/colour by state (done 600/#1A1A1A, active 700/#1A1A1A, todo 600/#7A6E6C).
> Circle: `width:20px;height:20px;border-radius:50%` — done `background:#16A34A;color:#fff` with a check glyph; active `background:#5A5552;color:#fff` with the step number; todo `border:1.5px solid rgba(0,0,0,.3)` no fill.
> Mounted at step 2 ('current: 2') so this spec exercises all three states (done/active/todo) in one render, matching the artboard's own step-2 render exactly.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| OnboardingStepper (4-pill progress row) | `display` | `flex` | `flex` | MATCH |
| OnboardingStepper (4-pill progress row) | `column-gap` | `18px` | `18px` | MATCH |
| OnboardingStepper (4-pill progress row) | `row-gap` | `18px` | `18px` | MATCH |
| done step (Workspace) | `font-size` | `12px` | `12px` | MATCH |
| done step (Workspace) | `font-weight` | `600` | `600` | MATCH |
| done step (Workspace) | `color` | `#1A1A1A` | `rgb(26, 26, 26)` | MATCH |
| done step circle | `width` | `20px` | `20px` | MATCH |
| done step circle | `height` | `20px` | `20px` | MATCH |
| done step circle | `border-radius` | `50%` | `50%` | MATCH |
| done step circle | `background-color` | `#16A34A` | `rgb(22, 163, 74)` | MATCH |
| done step circle | `color` | `#FFFFFF` | `rgb(255, 255, 255)` | MATCH |
| active step (Modes & jurisdictions) | `font-size` | `12px` | `12px` | MATCH |
| active step (Modes & jurisdictions) | `font-weight` | `700` | `700` | MATCH |
| active step (Modes & jurisdictions) | `color` | `#1A1A1A` | `rgb(26, 26, 26)` | MATCH |
| active step circle | `width` | `20px` | `20px` | MATCH |
| active step circle | `height` | `20px` | `20px` | MATCH |
| active step circle | `border-radius` | `50%` | `50%` | MATCH |
| active step circle | `background-color` | `#5A5552` | `rgb(90, 85, 82)` | MATCH |
| active step circle | `color` | `#FFFFFF` | `rgb(255, 255, 255)` | MATCH |
| todo step (Sectors) | `font-size` | `12px` | `12px` | MATCH |
| todo step (Sectors) | `font-weight` | `600` | `600` | MATCH |
| todo step (Sectors) | `color` | `#7A6E6C` | `rgb(122, 110, 108)` | MATCH |
| todo step circle | `width` | `20px` | `20px` | MATCH |
| todo step circle | `height` | `20px` | `20px` | MATCH |
| todo step circle | `border-radius` | `50%` | `50%` | MATCH |
| todo step circle | `border-width` | `1px` | `1px` | MATCH |
| todo step circle | `border-color` | `rgba(0,0,0,.3)` | `rgba(0, 0, 0, 0.3)` | MATCH |

## Operations region x dimension matrix (D1-D6)

- Spec file: `fsi-app/.discipline/rendering/audit/spec/operations-matrix.json`
- Mount: `ops-matrix` at 1440 px
- Artboards: 08-operations-list ("Regions side by side" matrix table; the Filters rail card's D1-D6 breakdown)
- Design source: docs/design/handoff-2026-09-06/'Caros Ledge UI System.dc.html' p8 (#p8, Operations list): the matrix's own dek ('25 active items · 18 jurisdictions · six dimensions per region') and its <table> row set; the rail card's own D1-D6 labelled breakdown (D1 Regulatory feasibility, D2 Regional resource, D3 Labor markets, D4 Materials sourcing, D5 Infrastructure, D6 Operational cost); operator ruling 3.3 ('Operations matrix renders 5 of 6 dimensions; add D6 Regulatory Feasibility. Six dimensions, D1-D6, same order everywhere.'); src/components/operations/OperationsLedger.tsx and RegionDimensionMatrix.tsx (real components)
- Result: 3 MATCH, 0 not matching

> FIXED, fix lane fix58-detail (2026-09-07): OperationsLedger.tsx's `MATRIX_DIMENSIONS` (feeding RegionDimensionMatrix's `dimensions` prop, ~line 356) was `= DIMENSIONS` already at the time this lane read the file — the retired `SOURCED_DIMENSIONS = DIMENSIONS.filter((d) => d.key !== "regulatory")` derivation this note used to describe is gone from the product; the file's own DEFECT-FIX comment above `MATRIX_DIMENSIONS` (2026-09-07) records the same change. The MISMATCH this spec measured was the audit mount (`mounts.mjs`'s `ops-matrix` entry) still hardcoding the retired 5-item `SOURCED_DIMENSIONS` shape — a stale harness fixture, not a live product defect. The mount now reproduces the real `MATRIX_DIMENSIONS = DIMENSIONS` (all 6, unfiltered).
> The artboard's OWN 'Regions side by side' matrix table (p8, the <table> under the 'six dimensions per region' dek) shows only 5 rows in its example markup, no Regulatory feasibility row — a self-contradiction against its own 'six dimensions' dek text. Operator ruling 3.3 is the binding, later statement over the artboard's own example: 'Operations matrix renders 5 of 6 dimensions; add D6 Regulatory Feasibility... same order everywhere' — built against, per this dispatch's framing of the closed rulings.
> `buildRegionGrid` (region-grid.mjs) computes `state: 'absent'` for every regulatory_feasibility cell (0 rows in regional_data_facts, always) and RegionDimensionMatrix's own empty-cell branch renders the shared `Absence` component for any `state === 'absent'` cell — the D1 row therefore renders with the Absence convention in every cell, never a fabricated count, never blank.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| Operations region x dimension matrix (D1-D6) | `count` | `1` | `1` | MATCH |
| dimension rows (one per dimension, top-level tr only — an open dimension inserts a second Facts tr the count below must not conflate with a dimension row) | `count` | `6` | `6` | MATCH |
| dimension row 1's name (order check: D1 should be first when all 6 are present) | `text` | `Regulatory feasibility` | `Regulatory feasibility` | MATCH |

## Page frame (1440)

- Spec file: `fsi-app/.discipline/rendering/audit/spec/page-frame.json`
- Mount: `page-frame-1440` at 1440 px
- Artboards: 00-system-sheet §0.3 'The frame', 01-dashboard, 03-regulation-detail, 18-section-treatments (ruling 5.1), 19-masthead-line-options (ruling 5.2)
- Design source: docs/design/handoff-2026-09-06/README.md §0.3 'The frame'; 'Caros Ledge UI System.dc.html' page artboards (frame grid `252px 1fr`, content grid `padding:20px 40px 40px; grid-template-columns:minmax(0,1fr) 300px; gap:28px`); operator rulings 4.2 (nav card top margin), 5.1 (section treatment), 5.2 (one coloured rule per screen), 2.2/2.3/3.1 (the forbid list)
- Result: 39 MATCH, 0 not matching

> The mount is the REAL AppShell wrapping DashboardBrief and RegulationDetailSurface, so the nav card, the content column, the dashboard section cards, the detail header and the detail action row are all in ONE measured tree. Composing the two surfaces together is an audit fixture, not a claim the product renders them together.
> Nav card top margin is expected at 20px per operator ruling 4.2 ('margin: 20px 0 16px 16px so it aligns with the content column's 20px top padding. Fix it in the frame, once'). The artboard markup still shows 16px; the ruling supersedes it.
> Ruling 5.1 requires a 3px graduated rule ABOVE each section title (full card width, top edge, no radius, linear-gradient(90deg,#5A5552,#5A5552 22%,rgba(90,85,82,.18))) and NO line below the title. Selector repair, train 58 (2026-09-07): the earlier `[data-section-rule]` selector never matched anything; the dashboard's Card DOES render the shared `SectionRule` component (`src/components/ui/SectionRule.tsx`, marker class `.cl-section-rule`, confirmed by direct source read this session, `src/components/dashboard/DashboardBrief.tsx` line ~121) — the check below now measures the real element.
> Ruling 5.2's sitewide count ('one coloured rule per screen') cannot be counted reliably today: BandGradientRule renders an unmarked `div[aria-hidden="true"]`, and the four-band gradient is not distinguishable by computed style alone from the ImpactMeter ramp or the full MilestoneTimeline track, which are also green-to-red gradients at similar heights. This spec therefore checks the ONE permitted instance exactly (the nav card cap) and forbids a band-proportion rule inside a page masthead. Adding `data-band-rule` to BandGradientRule would make the sitewide count a one-line check; that is a recommendation for the fix lane, not a deviation.
> Operator report 2026-09-07 ('the side navigation bar does not reach the length of the page'), CONFIRMED against every page artboard: the nav card is `align-self:stretch` inside the frame's `grid-template-columns:252px 1fr` and spans the full page height, footer rows at its foot. The build's `aside` previously carried `maxHeight: calc(100vh - 36px)` with no `align-self` of its own and ended at its content height instead. Fixed at the root in `Sidebar.tsx` (`align-self:stretch`, `height:auto`, no `maxHeight`) and in `AppShell.tsx` (the frame row's `display:flex`/height made inline, not Tailwind-class-only, so this harness — which injects only globals.css/theme.css, no compiled Tailwind CSS — has a real box to stretch against). The two rows below assert it: `align-self` reads back `stretch`, and `height` on this fixture (frame row's inline `height:100vh` at the harness's fixed 1400px page height) resolves to the frame height (1400px) minus the card's own vertical margins (20px top + 16px bottom = 36px) minus its own 1px top+bottom border (`box-sizing:content-box`, so the CSS `height` property excludes it) = 1362px — i.e. the card's rendered (border) box is exactly 1364px, equal to the frame's box, not its own content's height; the 1362px this row asserts is that same box read back through `getComputedStyle`'s content-box `height`, confirmed against `getBoundingClientRect().height === 1364` directly in the fixture.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| Page frame (1440) | `width` | `252px` | `252px` | MATCH |
| Page frame (1440) | `margin-top` | `20px` | `20px` | MATCH |
| Page frame (1440) | `margin-right` | `0px` | `0px` | MATCH |
| Page frame (1440) | `margin-bottom` | `16px` | `16px` | MATCH |
| Page frame (1440) | `margin-left` | `16px` | `16px` | MATCH |
| Page frame (1440) | `background-color` | `#FFFFFF` | `rgb(255, 255, 255)` | MATCH |
| Page frame (1440) | `border-top-width` | `1px` | `1px` | MATCH |
| Page frame (1440) | `border-top-color` | `rgba(0,0,0,.12)` | `rgba(0, 0, 0, 0.12)` | MATCH |
| Page frame (1440) | `border-top-left-radius` | `10px` | `10px` | MATCH |
| Page frame (1440) | `border-bottom-left-radius` | `10px` | `10px` | MATCH |
| Page frame (1440) | `align-self` | `stretch` | `stretch` | MATCH |
| Page frame (1440) | `height` | `1362px` | `1362px` | MATCH |
| nav card — band-proportion gradient cap (the ONE coloured rule, ruling 5.2) | `height` | `3px` | `3px` | MATCH |
| nav card — band-proportion gradient cap (the ONE coloured rule, ruling 5.2) | `count` | `1` | `1` | MATCH |
| content column — grid and padding (dashboard) | `padding-top` | `20px` | `20px` | MATCH |
| content column — grid and padding (dashboard) | `padding-right` | `40px` | `40px` | MATCH |
| content column — grid and padding (dashboard) | `padding-bottom` | `40px` | `40px` | MATCH |
| content column — grid and padding (dashboard) | `padding-left` | `40px` | `40px` | MATCH |
| content column — grid and padding (dashboard) | `column-gap` | `28px` | `28px` | MATCH |
| content column — grid and padding (dashboard) | `align-items` | `start` | `start` | MATCH |
| content column — grid and padding (dashboard) | `grid-template-columns` | `* 300px` | `730px 300px` | MATCH |
| section card — no line BELOW the title (ruling 5.1) | `border-bottom-width` | `0px` | `0px` | MATCH |
| section card — the 3px graduated rule ABOVE the title (ruling 5.1) | `height` | `3px` | `3px` | MATCH |
| section card — the 3px graduated rule ABOVE the title (ruling 5.1) | `background-image` | `linear-gradient(90deg, rgb(90, 85, 82) 0%, rgb(90, 85, 82) 22%, rgba(90, 85, 82, 0.18) 100%)` | `linear-gradient(90deg, rgb(90, 85, 82), rgb(90, 85, 82) 22%, rgba(90, 85, 82, 0.18))` | MATCH |
| section card — the 3px graduated rule ABOVE the title (ruling 5.1) | `border-top-left-radius` | `0px` | `0px` | MATCH |
| section title type (ruling 5.1: Anton uppercase) | `font-family` | `contains:Anton` | `Anton, system-ui, sans-serif` | MATCH |
| section title type (ruling 5.1: Anton uppercase) | `text-transform` | `uppercase` | `uppercase` | MATCH |
| section title type (ruling 5.1: Anton uppercase) | `letter-spacing` | `0.04em (= 0.8px at 20px)` | `0.8px` | MATCH |
| section title type (ruling 5.1: Anton uppercase) | `font-size` | `20px` | `20px` | MATCH |
| detail title type | `font-family` | `contains:Anton` | `Anton, system-ui, sans-serif` | MATCH |
| detail title type | `text-transform` | `uppercase` | `uppercase` | MATCH |
| detail title type | `letter-spacing` | `0.04em (= 1.12px at 28px)` | `1.12px` | MATCH |
| detail title type | `font-size` | `28px` | `28px` | MATCH |
| exactly one '+ Tag' trigger in the detail header (operator 3.2, mechanism repair train 58 2026-09-07) | `count` | `1` | `1` | MATCH |
| floating Ask AI button (bottom-right, every page) | `(forbidden element)` | `absent` | `absent` | MATCH |
| a second CommandBar inside the detail header | `(forbidden element)` | `absent` | `absent` | MATCH |
| HeroPriorityDropdown in the detail action row | `(forbidden element)` | `absent` | `absent` | MATCH |
| a band-proportion (coloured) rule inside a page masthead | `(forbidden element)` | `absent` | `absent` | MATCH |
| Due next / What changed ListRow cells — no cell escapes its 88px impact column or overlaps DUE (D1, operator report 2026-09-07) | `(cell containment)` | `no cell overlaps a sibling or exceeds its row` | `clean across 5 row(s)` | MATCH |

## Rail cards (ImpactRailCard, AtAGlanceCard, RailLegend)

- Spec file: `fsi-app/.discipline/rendering/audit/spec/railcards.json`
- Mount: `detail-shell` at 1440 px
- Artboards: 03-regulation-detail, 05-market-detail, 07-research-detail, 09-operations-profile
- Design source: 'Caros Ledge UI System.dc.html' p3 (#p3) rail: 'At a glance' card (`grid-template-columns:96px 1fr;gap:7px 12px`, label `color:#7A6E6C`, value `font-weight:600`) and 'Impact assessment' card (eyebrow + trailing `0–3 per dimension` note); operator ruling 5.1 (3px rule on every card, see inthisliststat.json's same finding on the sibling rail card)
- Result: 14 MATCH, 0 not matching

> Every rail card in DetailShell.tsx (ImpactRailCard, AtAGlanceCard, RailLegend, InThisListStat) shares the identical inline card-chrome object; inthisliststat.json already records the missing ruling-5.1 rule and the 12/16/14 padding on that sibling card, so this spec does not repeat the chrome check three more times and focuses on each card's OWN content values.
> AtAGlanceCard's grid is `gridTemplateColumns: 'auto 1fr'` with `gap: '6px 14px'`; the artboard states a fixed `96px 1fr` label column and `gap:7px 12px` — both a real MISMATCH, not a harness artefact (auto-vs-fixed-px and the gap numbers are both plain CSSOM reads).
> AtAGlanceCard's value span is `fontWeight: 700`; the artboard's value text is `font-weight:600` — a real MISMATCH.
> ImpactRailCard has no trailing '0–3 per dimension' note beside its eyebrow label (the artboard's own scale caption); not asserted as a selector check since no element exists to address (an omission, not a wrong value).

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| Rail cards (ImpactRailCard, AtAGlanceCard, RailLegend) | `background-color` | `#FFFFFF` | `rgb(255, 255, 255)` | MATCH |
| At a glance — eyebrow label | `text` | `At a glance` | `At a glance` | MATCH |
| At a glance — eyebrow label | `font-weight` | `800` | `800` | MATCH |
| At a glance — eyebrow label | `text-transform` | `uppercase` | `uppercase` | MATCH |
| At a glance — grid | `grid-template-columns` | `96px *` | `96px 158px` | MATCH |
| At a glance — grid | `column-gap` | `12px` | `12px` | MATCH |
| At a glance — grid | `row-gap` | `7px` | `7px` | MATCH |
| At a glance — first value | `font-weight` | `600` | `600` | MATCH |
| Impact assessment — eyebrow label | `text` | `Impact assessment` | `Impact assessment` | MATCH |
| Impact assessment — eyebrow label | `font-weight` | `800` | `800` | MATCH |
| Impact assessment — eyebrow label | `text-transform` | `uppercase` | `uppercase` | MATCH |
| Legend — heading | `text` | `Legend` | `Legend` | MATCH |
| Legend — heading | `font-weight` | `800` | `800` | MATCH |
| Legend — heading | `text-transform` | `uppercase` | `uppercase` | MATCH |

## Section/panel card — the list surfaces' rail card (ruling 5.1)

- Spec file: `fsi-app/.discipline/rendering/audit/spec/section-card-lists.json`
- Mount: `railcard` at 1440 px
- Artboards: 02-regulations-list, 04-market-list, 06-research-list, 08-operations-list, 11-watchlist, 18-section-treatments (ruling 5.1)
- Design source: docs/design/handoff-2026-09-06/README.md §0.4 (rail labels convention); 'Caros Ledge UI System.dc.html' the Legend rail card shared by artboards 02/04/06/08/11; operator ruling 5.1 ('the graduated rule ABOVE the section title wins... Applies to every panel/section card on every page')
- Result: 15 MATCH, 0 not matching

> ListSurfaceRailCards.tsx (RailCard, used by LegendRailCard and every other rail card the five list surfaces render) is pure inline style; every property here is a real product value.
> This is the ONE panel/section card shape shared by all five list surfaces (Regulations/Market/Research/Operations/Watchlist) that carries a visible title — the OTHER card on those pages (ListSurfaceShell's per-band grouping, `BandSectionHeader`) uses its own distinct convention (a coloured 3px TOP-BORDER per band, `borderTop:'3px solid '+band.cssVar`, README §0.2's band scale, not ruling 5.1's dark-grey gradation) and is NOT this ruling's target — a band subsection is not a titled panel in the ruling's sense, it is the list's own per-band grouping, unchanged by this ruling.
> FIXED (fix58-lists, 2026-09-07, B170): RailCard.tsx now mounts `<SectionRule/>` (src/components/ui/SectionRule.tsx) as the first child inside the card's outer border/radius/shadow div, matching `DashboardBrief.tsx`'s own `Card` convention (page-frame.json) exactly — the rule sits full-width at the true top edge, so the outer div itself can carry no padding any more; content padding (14px 16px, unchanged) moved to a new inner wrapper div, checked separately below.
> The title itself ('Legend') is styled 10.5px/800/.12em uppercase — README §0.4's RAIL LABEL convention, not the Anton section-title convention ruling 5.1 also states ('Section title itself: Anton uppercase, no divider, no underline'). Whether a rail card's meta-style label counts as ruling 5.1's 'section title' (requiring Anton) is not resolved by the ruling's own wording — it names the DASHBOARD's own primary section titles ('Due Next', 'By Urgency') as its worked example, and a rail card's compact label is a documented, PRE-EXISTING distinct convention (README type scale: 'rail labels 10.5px uppercase .12em/700'), so this is logged as an open question for a future lane rather than asserted as a MISMATCH against a scale the ruling did not clearly extend to this element.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| Section/panel card — the list surfaces' rail card (ruling 5.1) | `background-color` | `#FFFFFF` | `rgb(255, 255, 255)` | MATCH |
| Section/panel card — the list surfaces' rail card (ruling 5.1) | `border-width` | `1px` | `1px` | MATCH |
| Section/panel card — the list surfaces' rail card (ruling 5.1) | `border-color` | `rgba(0,0,0,.12)` | `rgba(0, 0, 0, 0.12)` | MATCH |
| Section/panel card — the list surfaces' rail card (ruling 5.1) | `border-radius` | `10px` | `10px` | MATCH |
| Section/panel card — the list surfaces' rail card (ruling 5.1) | `border-top-width` | `1px` | `1px` | MATCH |
| ruling 5.1 rule ABOVE the title | `height` | `3px` | `3px` | MATCH |
| ruling 5.1 rule ABOVE the title | `count` | `1` | `1` | MATCH |
| content wrapper — padding (moved off the outer card div so the rule can sit at the true top edge) | `padding-top` | `14px` | `14px` | MATCH |
| content wrapper — padding (moved off the outer card div so the rule can sit at the true top edge) | `padding-right` | `16px` | `16px` | MATCH |
| content wrapper — padding (moved off the outer card div so the rule can sit at the true top edge) | `padding-bottom` | `14px` | `14px` | MATCH |
| content wrapper — padding (moved off the outer card div so the rule can sit at the true top edge) | `padding-left` | `16px` | `16px` | MATCH |
| title (rail-label convention, not asserted as a MISMATCH against Anton per the note above) | `font-size` | `10.5px` | `10.5px` | MATCH |
| title (rail-label convention, not asserted as a MISMATCH against Anton per the note above) | `font-weight` | `800` | `800` | MATCH |
| title (rail-label convention, not asserted as a MISMATCH against Anton per the note above) | `letter-spacing` | `0.12em (= 1.26px at 10.5px)` | `1.26px` | MATCH |
| title (rail-label convention, not asserted as a MISMATCH against Anton per the note above) | `text-transform` | `uppercase` | `uppercase` | MATCH |

## SectionIndex

- Spec file: `fsi-app/.discipline/rendering/audit/spec/sectionindex.json`
- Mount: `detail-shell` at 1440 px
- Artboards: 03-regulation-detail, 05-market-detail, 07-research-detail, 09-operations-profile
- Design source: 'Caros Ledge UI System.dc.html' p3 (#p3, Regulation detail), 'sticky index' comment block: outer `position:sticky;top:0;background:#FAFAF8;padding:6px 0` wrapping an inner pill-group `display:flex;align-items:center;gap:4px;background:#fff;border:1px solid rgba(0,0,0,.12);border-radius:8px;padding:4px;font-size:12.5px`, whose current-section item is a FILLED pill (`padding:6px 12px;border-radius:6px;background:#5A5552;color:#fff;font-weight:700`, text "S1 Summary", no separator dot) and whose other items are plain links (`padding:6px 12px;border-radius:6px;font-weight:600;color:#5A6B67`, e.g. "S2 Obligations"). README §0.5 says only 'sticky section index (S1 · S2 · S3 …)', which is README's own shorthand notation for the artboard's numbered links, not a literal middot-separated instruction.
- Result: 7 MATCH, 0 not matching

> REAL STRUCTURAL DIVERGENCE, not a harness artefact: the artboard's index is a bordered PILL GROUP (white background, 1px border, 8px radius, 4px padding) with the current section rendered as a filled dark pill and the others as plain 6px-radius links inside that group — SectionIndex.tsx instead renders a bare flex row of underline-free links separated by a literal '·' character, with no pill container and no current-section highlight at all (no scroll-spy / active-link logic exists in the component). Per the operator's framing, the artboard wins over README's own shorthand notation.
> The label text itself differs too: the artboard's links read "S1 Summary" / "S2 Obligations" (numeral and label separated by a space only); the built component renders "S1 · Summary" (DetailShell.tsx: `S${i + 1} · ${s.label}`).

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| SectionIndex | `position` | `sticky` | `sticky` | MATCH |
| SectionIndex | `top` | `0px` | `0px` | MATCH |
| first index link (artboard: a filled current-section pill reading "S1 Summary") | `text` | `S1 Summary` | `S1 Summary` | MATCH |
| first index link (artboard: a filled current-section pill reading "S1 Summary") | `background-color` | `#5A5552` | `rgb(90, 85, 82)` | MATCH |
| first index link (artboard: a filled current-section pill reading "S1 Summary") | `color` | `#FFFFFF` | `rgb(255, 255, 255)` | MATCH |
| first index link (artboard: a filled current-section pill reading "S1 Summary") | `border-radius` | `6px` | `6px` | MATCH |
| first index link (artboard: a filled current-section pill reading "S1 Summary") | `font-weight` | `700` | `700` | MATCH |

## Settings — Notifications section (AccountCard + NotificationPreferences)

- Spec file: `fsi-app/.discipline/rendering/audit/spec/settings-notifications.json`
- Mount: `settings-notifications` at 1440 px
- Artboards: 15-settings
- Design source: docs/design/handoff-2026-09-06/'Caros Ledge UI System.dc.html' id="p15" (char 594212), the 'Notifications' section card; operator ruling 5.1 (2026-09-07)
- Result: 27 MATCH, 0 not matching

> dc.html p15 section-card shell (used by every section card on this page, including Notifications): `background:#fff;border:1px solid rgba(0,0,0,.12);border-radius:10px;box-shadow:0 1px 2px rgba(26,26,26,.04),0 4px 14px rgba(26,26,26,.06);overflow:hidden`, topped by the ruling 5.1 gradient rule `height:3px;background:linear-gradient(90deg,#5A5552,#5A5552 22%,rgba(90,85,82,.18))`. Artboard and ruling agree here exactly, nothing to reconcile.
> dc.html p15 header row: `padding:14px 16px 10px;border-bottom:1px solid rgba(0,0,0,.08)`, title 'Notifications' `font-family:'Anton';text-transform:uppercase;letter-spacing:.04em;font-size:20px`, meta 'In-app now · email and push coming' `font-size:10.5px;color:#7A6E6C;font-weight:600` (two font-weight declarations on the same rule in the source; the later one, 600, is what a browser applies and what this spec measures against).
> dc.html p15 toggle row: `min-height:48px;border-bottom:1px solid rgba(0,0,0,.06)`, label bold, sublabel `font-size:11px;color:#7A6E6C`; pill `width:32px;height:18px;border-radius:999px`, knob `width:14px;height:14px` inset 2px.
> Correction, train 58 (2026-09-07): `AccountCard` DOES render the shared `SectionRule` (`.cl-section-rule`, `src/components/account/AccountPrimitives.tsx` ~line 103, confirmed by direct source read this session) as the section's actual first child — the note this row used to carry (claiming no such element existed) is stale; the 'gradient rule' row below now MATCHES. Its presence also means every OTHER child selector below that assumed the header row was `div:first-child` was reaching the rule div instead — all four are repaired to `div:nth-child(2)`/`div:nth-child(3)` below.
> The real `AccountCard` wrapper is the shared shell for this section AND the Account 'Members & roles' section (see account-members.json) — one spec file per section per this lane's PARTS list ('account/settings ... including ... the Notifications section'), same underlying component.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| Settings — Notifications section (AccountCard + NotificationPreferences) | `background-color` | `#FFFFFF` | `rgb(255, 255, 255)` | MATCH |
| Settings — Notifications section (AccountCard + NotificationPreferences) | `border-width` | `1px` | `1px` | MATCH |
| Settings — Notifications section (AccountCard + NotificationPreferences) | `border-color` | `rgba(0,0,0,.12)` | `rgba(0, 0, 0, 0.12)` | MATCH |
| Settings — Notifications section (AccountCard + NotificationPreferences) | `border-radius` | `10px` | `10px` | MATCH |
| gradient rule (ruling 5.1) | `height` | `3px` | `3px` | MATCH |
| gradient rule (ruling 5.1) | `background-image` | `linear-gradient(90deg,#5A5552,#5A5552 22%,rgba(90,85,82,.18))` | `linear-gradient(90deg, rgb(90, 85, 82), rgb(90, 85, 82) 22%, rgba(90, 85, 82, 0.18))` | MATCH |
| header row | `padding-top` | `14px` | `14px` | MATCH |
| header row | `padding-right` | `16px` | `16px` | MATCH |
| header row | `padding-bottom` | `10px` | `10px` | MATCH |
| header row | `border-bottom-width` | `1px` | `1px` | MATCH |
| header row | `border-bottom-color` | `rgba(0,0,0,.08)` | `rgba(0, 0, 0, 0.08)` | MATCH |
| title | `text` | `Notifications` | `Notifications` | MATCH |
| title | `font-family` | `contains:Anton` | `Anton, system-ui, sans-serif` | MATCH |
| title | `font-size` | `20px` | `20px` | MATCH |
| title | `letter-spacing` | `0.04em (= 0.8px at 20px)` | `0.8px` | MATCH |
| title | `text-transform` | `uppercase` | `uppercase` | MATCH |
| meta | `text` | `In-app now · email and push coming` | `In-app now · email and push coming` | MATCH |
| meta | `font-size` | `10.5px` | `10.5px` | MATCH |
| meta | `font-weight` | `600` | `600` | MATCH |
| meta | `color` | `#7A6E6C` | `rgb(122, 110, 108)` | MATCH |
| first toggle row | `min-height` | `48px` | `48px` | MATCH |
| first toggle row | `border-bottom-width` | `1px` | `1px` | MATCH |
| first toggle row | `border-bottom-color` | `rgba(0,0,0,.06)` | `rgba(0, 0, 0, 0.06)` | MATCH |
| first toggle pill | `width` | `32px` | `32px` | MATCH |
| first toggle pill | `height` | `18px` | `18px` | MATCH |
| first toggle pill | `border-radius` | `999px` | `999px` | MATCH |
| first toggle pill | `background-color` | `#5A5552` | `rgb(90, 85, 82)` | MATCH |

## Settings — SectionIndex (S1 · S2 · … sticky row)

- Spec file: `fsi-app/.discipline/rendering/audit/spec/settings-section-index.json`
- Mount: `settings-section-index` at 1440 px
- Artboards: 15-settings
- Design source: src/components/pages/SettingsPage.tsx SETTINGS_SECTIONS constant (six entries: General, Notifications, Saved searches, Data & supersessions, Archive, Help); src/components/detail/DetailShell.tsx SectionIndex; README.md screen 15 note 'sticky S1 . S2 . S3 SectionIndex ... reusing ... the detail architecture'
- Result: 12 MATCH, 0 not matching

> The design package's numbered artboards (p13-p17) do not draw Settings' own sticky S1..S6 row explicitly — SettingsPage.tsx's own header comment states the design intent directly ('items become anchored SECTIONS in one scrolling page, reusing the sticky S1 . S2 . S3 SectionIndex from the detail architecture'), so this spec measures the real shared SectionIndex component against ITS OWN stated geometry (src/components/detail/DetailShell.tsx), not a page-artboard pixel reading — there is no p15 SectionIndex block to grep. Flagged rather than silently substituted, per the harness's own instruction to record the divergence.
> SPEC CORRECTION, train 58 (2026-09-07): this spec previously described SectionIndex's PRE-fix58-detail shape (a bare flex row of middot-separated links, 10.5px/700, `border-bottom:1px solid var(--line-2)`, `padding:10px 0`). Fix lane fix58-detail (folded into this train; see `sectionindex.json`, already 0 MISMATCH against dc.html #p3's own pill-group markup) restructured SectionIndex into a bordered pill group with a scroll-spy filled active pill — this settings mount uses the SAME component and inherited that redesign automatically, so this spec's stale expect block (10.5px text, middot separator, 10px padding, a bottom border) stopped matching the real render. Re-measured live this session and corrected below to what SectionIndex.tsx (`src/components/detail/DetailShell.tsx`) actually renders: outer `nav.cl-section-index` (`padding:6px 0`, no border, `position:sticky;top:0`), inner pill-group links (`S{n} {label}`, no middot — DetailShell.tsx line ~558, `S{i + 1} {s.label}`, a space not a middot), `font-size:12.5px`, `min-height:44px`, active link filled (`background:var(--brand)`, `color:#fff`, `font-weight:700`) — this fixture's first link (S1) is the active/default one on mount (scroll-spy `active` state starts at index 0).

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| Settings — SectionIndex (S1 · S2 · … sticky row) | `position` | `sticky` | `sticky` | MATCH |
| Settings — SectionIndex (S1 · S2 · … sticky row) | `top` | `0px` | `0px` | MATCH |
| Settings — SectionIndex (S1 · S2 · … sticky row) | `padding-top` | `6px` | `6px` | MATCH |
| Settings — SectionIndex (S1 · S2 · … sticky row) | `padding-bottom` | `6px` | `6px` | MATCH |
| first link (S1 General, active pill on mount) | `text` | `S1 General` | `S1 General` | MATCH |
| first link (S1 General, active pill on mount) | `font-size` | `12.5px` | `12.5px` | MATCH |
| first link (S1 General, active pill on mount) | `font-weight` | `700` | `700` | MATCH |
| first link (S1 General, active pill on mount) | `min-height` | `44px` | `44px` | MATCH |
| first link (S1 General, active pill on mount) | `background-color` | `#5A5552` | `rgb(90, 85, 82)` | MATCH |
| first link (S1 General, active pill on mount) | `color` | `#FFFFFF` | `rgb(255, 255, 255)` | MATCH |
| first link (S1 General, active pill on mount) | `border-radius` | `6px` | `6px` | MATCH |
| count of entries | `count` | `6` | `6` | MATCH |

## Sidebar (nav card, inline-style-driven properties only)

- Spec file: `fsi-app/.discipline/rendering/audit/spec/sidebar.json`
- Mount: `page-frame-1440` at 1440 px
- Artboards: 00-system-sheet §0.3 'The frame', 01-dashboard
- Design source: docs/design/handoff-2026-09-06/README.md §0.3 'The frame'; 'Caros Ledge UI System.dc.html' p1's nav-card block (section labels, active row, counts, footer)
- Result: 17 MATCH, 0 not matching

> Sidebar.tsx's desktop nav card mixes inline style with Tailwind utility classes (`text-xl uppercase`, `text-[10px] font-bold tracking-[0.15em] uppercase`, etc. on the title/tagline). This harness injects `globals.css`/`theme.css` as RAW text without running Tailwind's build step (no PostCSS/Vite plugin — see mounts.mjs's STYLE_INJECT), so Tailwind's `@import "tailwindcss"` and its generated utility classes never resolve in this measured tree: a Tailwind-only property (e.g. the title's `text-xl` font-size) reads back as the browser's un-styled default, which is a HARNESS LIMITATION, not a product value — confirmed empirically (probed `aside.hidden > div:nth-child(2) h1` font-size: read 32px, the UA default h1 size, and text-transform: none, neither of which Sidebar.tsx's own inline style sets). This spec therefore asserts ONLY properties Sidebar.tsx sets via inline `style={}`, which bypass Tailwind entirely and are real regardless of this harness's raw-injection limitation. Title/tagline font-size, weight and text-transform (all Tailwind-class-driven in the desktop card) are NOT asserted here for that reason, not because they are known-correct.
> The aside's own box model (width 252, margin, background, border, radius, band-gradient cap) is already covered by spec/page-frame.json's own top-level target on this same mount/selector; this spec adds the nav card's INTERNAL typography and active-state treatment, which page-frame.json does not touch.
> dc.html p1 nav card section label ('Brief'): font-size 9.5px, letter-spacing .14em, text-transform uppercase, color #7A6E6C, font-weight 700, padding 8px 8px 4px (the FIRST section only omits the divider/border-top the later two sections carry).
> dc.html p1 nav card active row ('Dashboard'): background #F5F2EE, `box-shadow: inset 3px 0 0 #5A5552` (an inset spine, not a border), font-weight 700. FIXED by fix58-tokens (2026-09-07, B96-B98): Sidebar.tsx's active row now uses that box-shadow spine (not `border-left`) and fontWeight 700 (not 800). Section-label letter-spacing (0.14em) and font-weight (700) were also unified across the card/drawer variants — the drawer variant already carried the correct values, only the desktop card diverged.
> The mount's `next/navigation` stub (../smoke/stub-next-navigation.mjs, shared guard code, reused as-is per its own header) fixes `usePathname()` to `/community/smoke-test` — so `Sidebar.isActive()` marks COMMUNITY active (prefix match), not Dashboard, in this measured tree. The active-row targets below therefore select the Network section's Community row, not Brief's Dashboard row; this is a mount artefact of which item ends up `aria-current`, not of the STYLING those properties measure (the active-row style rule itself is identical regardless of which row it lands on).

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| Sidebar (nav card, inline-style-driven properties only) | `font-size` | `9.5px` | `9.5px` | MATCH |
| Sidebar (nav card, inline-style-driven properties only) | `letter-spacing` | `0.14em (= 1.33px at 9.5px)` | `1.33px` | MATCH |
| Sidebar (nav card, inline-style-driven properties only) | `text-transform` | `uppercase` | `uppercase` | MATCH |
| Sidebar (nav card, inline-style-driven properties only) | `color` | `#7A6E6C` | `rgb(122, 110, 108)` | MATCH |
| Sidebar (nav card, inline-style-driven properties only) | `font-weight` | `700` | `700` | MATCH |
| section label text | `text` | `Brief` | `Brief` | MATCH |
| active nav row (Community, per the stub-path note above) — background | `background-color` | `#F5F2EE` | `rgb(245, 242, 238)` | MATCH |
| active nav row — font-weight | `font-weight` | `700` | `700` | MATCH |
| active nav row — spine technique | `box-shadow` | `rgb(90, 85, 82) 3px 0px 0px 0px inset` | `rgb(90, 85, 82) 3px 0px 0px 0px inset` | MATCH |
| resting nav row link text (Dashboard, the section's first item) | `text` | `Dashboard` | `Dashboard` | MATCH |
| nav card footer — row count (operator ruling 2026-09-07, superseding R2: ONE row, the logged-in person's name, opening Account/Admin/Settings/Sign out as menu options) | `count` | `1` | `1` | MATCH |
| nav card footer — the one row is the logged-in person's name (avatar glyph + name), workspace name right-aligned muted | `text` | `audit—` | `audit—` | MATCH |
| nav card footer row — button role (aria-haspopup menu trigger, not a Link) | `text` | `audit—` | `audit—` | MATCH |
| a separate Account row/link | `(forbidden element)` | `absent` | `absent` | MATCH |
| a separate Admin row/link or OWNER badge in the footer | `(forbidden element)` | `absent` | `absent` | MATCH |
| a third footer row of any kind | `(forbidden element)` | `absent` | `absent` | MATCH |
| the old UserMenu trigger button (aria-label 'Open user menu', pre-coordinator-default component, deleted) | `(forbidden element)` | `absent` | `absent` | MATCH |

## Skeletons (loading state, README §0.6)

- Spec file: `fsi-app/.discipline/rendering/audit/spec/skeleton.json`
- Mount: `skeleton` at 1440 px
- Artboards: 00-system-sheet §0.6 'Loading'
- Design source: docs/design/handoff-2026-09-06/README.md §0.6: 'Nav and masthead render immediately; tiles and rows arrive as skeletons in their final geometry so nothing jumps. A count still loading shows a skeleton, never 0 — a zero is a fact, not a placeholder.'
- Result: 13 MATCH, 0 not matching

> The design source states a BEHAVIOUR (final-geometry skeletons, never a bare 0) rather than a pixel spec of its own — Skeleton.tsx is checked here for internal consistency against the real components it stands in for: SkeletonListRow's grid must equal ListRow's own grid (spec/listrow.json), SkeletonBandTile's box model must equal BandTile's own (spec/bandtile.json). A skeleton whose geometry drifts from its real component is exactly the 'jumps on load' defect §0.6 rules out.
> Skeleton.tsx is pure inline style; every property here is a real product value.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| Skeletons (loading state, README §0.6) | `display` | `grid` | `grid` | MATCH |
| Skeletons (loading state, README §0.6) | `grid-template-columns` | `3px 56px * 88px 84px 76px 40px 44px` | `3px 56px 273px 88px 84px 76px 40px 44px` | MATCH |
| Skeletons (loading state, README §0.6) | `row-gap` | `0px` | `0px` | MATCH |
| Skeletons (loading state, README §0.6) | `column-gap` | `14px` | `14px` | MATCH |
| Skeletons (loading state, README §0.6) | `min-height` | `56px` | `56px` | MATCH |
| band-tile skeleton — card box model | `padding-top` | `14px` | `14px` | MATCH |
| band-tile skeleton — card box model | `padding-right` | `16px` | `16px` | MATCH |
| band-tile skeleton — card box model | `padding-bottom` | `0px` | `0px` | MATCH |
| band-tile skeleton — card box model | `padding-left` | `16px` | `16px` | MATCH |
| band-tile skeleton — card box model | `border-radius` | `10px` | `10px` | MATCH |
| band-tile skeleton — card box model | `min-height` | `112px` | `112px` | MATCH |
| stat-block skeleton — structural | `display` | `flex` | `flex` | MATCH |
| stat-block skeleton — structural | `flex-direction` | `column` | `column` | MATCH |

## StatBlock

- Spec file: `fsi-app/.discipline/rendering/audit/spec/statblock.json`
- Mount: `statblock` at 1440 px
- Artboards: 14-account (stat-block grid), 01-dashboard rail ('Across the platform'), 00-system-sheet §0.4 (inline example)
- Design source: docs/design/handoff-2026-09-06/README.md §0.4 'Buttons / stat blocks'; 'Caros Ledge UI System.dc.html' p14's stat-block grid ('Companies'/'Individuals'/'Newest join'/'Active this month', the definitive `stack` usage) and p1's dashboard rail 'Across the platform' rows (the definitive `row` usage README §0.4 names explicitly)
- Result: 19 MATCH, 0 not matching

> StatBlock.tsx is pure inline style; every property here is a real product value.
> The system-sheet's own inline example (dc.html #sys, 'Actions · stat block' row, 22px numeral) is a THIRD size not represented by either the `stack` or `row` prop, and is not asserted here — p14's grid is the definitive stack-layout usage the README names ('profile and admin counters').

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| StatBlock | `display` | `block` | `block` | MATCH |
| stack — label | `font-size` | `10px` | `10px` | MATCH |
| stack — label | `letter-spacing` | `0.12em (= 1.2px at 10px)` | `1.2px` | MATCH |
| stack — label | `text-transform` | `uppercase` | `uppercase` | MATCH |
| stack — label | `color` | `#7A6E6C` | `rgb(122, 110, 108)` | MATCH |
| stack — label | `font-weight` | `700` | `700` | MATCH |
| stack — numeral | `font-family` | `contains:Anton` | `Anton, system-ui, sans-serif` | MATCH |
| stack — numeral | `font-size` | `26px` | `26px` | MATCH |
| stack — numeral | `line-height` | `26px` | `26px` | MATCH |
| stack — note | `font-size` | `10.5px` | `10.5px` | MATCH |
| stack — note | `color` | `#7A6E6C` | `rgb(122, 110, 108)` | MATCH |
| row — label (bold, ink) | `font-weight` | `700` | `700` | MATCH |
| row — label (bold, ink) | `font-size` | `12.5px` | `12.5px` | MATCH |
| row — label (bold, ink) | `color` | `#1A1A1A` | `rgb(26, 26, 26)` | MATCH |
| row — note (muted, under the label) | `display` | `block` | `block` | MATCH |
| row — note (muted, under the label) | `font-size` | `10.5px` | `10.5px` | MATCH |
| row — note (muted, under the label) | `color` | `#7A6E6C` | `rgb(122, 110, 108)` | MATCH |
| row — numeral | `font-family` | `contains:Anton` | `Anton, system-ui, sans-serif` | MATCH |
| row — numeral | `font-size` | `18px` | `18px` | MATCH |

## StateNote

- Spec file: `fsi-app/.discipline/rendering/audit/spec/statenote.json`
- Mount: `statenote` at 1440 px
- Artboards: 00-system-sheet §0.4 'State note strip', 15-settings (neutral variant, 'Applies workspace-wide')
- Design source: docs/design/handoff-2026-09-06/README.md §0.4 'State note strip'; ruling 2.6 (exact geometry, restated verbatim from the README); 'Caros Ledge UI System.dc.html' p15's neutral state note ('Applies workspace-wide')
- Result: 13 MATCH, 0 not matching

> StateNote.tsx is pure inline style; every property here is a real product value.
> Ruling 2.6: 'border-left 3px solid <band>; background <band tint>; border-radius 0 6px 6px 0; padding 9px 12px. Left side straight. Neutral variant #5A5552 on #F5F2EE.' '#5A5552' is the `--brand` token, NOT `--ink-2` (#5A6B67) — StateNote.tsx's neutral variant uses `band ? ... : 'var(--ink-2)'`, i.e. the wrong token for the neutral border-left colour (background is correct, `--tag` = #F5F2EE).

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| StateNote | `border-left-width` | `3px` | `3px` | MATCH |
| StateNote | `border-left-style` | `solid` | `solid` | MATCH |
| StateNote | `border-left-color` | `#F97316` | `rgb(249, 115, 22)` | MATCH |
| StateNote | `background-color` | `#FFF7ED` | `rgb(255, 247, 237)` | MATCH |
| StateNote | `border-radius` | `0px 6px 6px 0px` | `0px 6px 6px 0px` | MATCH |
| StateNote | `padding-top` | `9px` | `9px` | MATCH |
| StateNote | `padding-bottom` | `9px` | `9px` | MATCH |
| StateNote | `padding-left` | `12px` | `12px` | MATCH |
| StateNote | `padding-right` | `12px` | `12px` | MATCH |
| neutral variant — border-left colour | `border-left-color` | `#5A5552` | `rgb(90, 85, 82)` | MATCH |
| neutral variant — background | `background-color` | `#F5F2EE` | `rgb(245, 242, 238)` | MATCH |
| action link | `font-size` | `11px` | `11px` | MATCH |
| action link | `font-weight` | `600` | `600` | MATCH |

## SummaryDepthSwitch

- Spec file: `fsi-app/.discipline/rendering/audit/spec/summarydepthswitch.json`
- Mount: `detail-shell` at 1440 px
- Artboards: 03-regulation-detail, 05-market-detail, 07-research-detail, 09-operations-profile
- Design source: src/components/detail/DetailShell.tsx ("the ONLY summary depth control in this architecture — two states, not the old three-state summary-depth toggle README §0.5 names as removed"); README.md §0.5
- Result: 8 MATCH, 0 not matching

> No dc.html artboard shows this exact control in isolation (the p3/p5/p7/p9 artboards show the sticky index row without an explicit depth-switch example); the two rows below are read from the built component itself as a regression baseline (its own two-state contract, replacing the removed three-state toggle) rather than an artboard pixel value.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| SummaryDepthSwitch | `display` | `inline-flex` | `inline-flex` | MATCH |
| SummaryDepthSwitch | `border-top-width` | `1px` | `1px` | MATCH |
| SummaryDepthSwitch | `border-top-color` | `rgba(0,0,0,.12)` | `rgba(0, 0, 0, 0.12)` | MATCH |
| Summary option (selected by default) | `text` | `Summary` | `Summary` | MATCH |
| Summary option (selected by default) | `min-height` | `44px` | `44px` | MATCH |
| Full brief option | `text` | `Full brief` | `Full brief` | MATCH |
| Full brief option | `min-height` | `44px` | `44px` | MATCH |
| a third depth state | `(forbidden element)` | `absent` | `absent` | MATCH |

## TabRow

- Spec file: `fsi-app/.discipline/rendering/audit/spec/tabrow.json`
- Mount: `tabrow` at 1440 px
- Artboards: 14-account, 15-settings
- Design source: docs/design/handoff-2026-09-06/README.md §0.3 'Tab rows (account, settings) span the frame above the two-column grid'; 'Caros Ledge UI System.dc.html' p14/p15's tab-row block (identical on both, only the active tab differs)
- Result: 22 MATCH, 0 not matching

> TabRow.tsx is pure inline style (no Tailwind classes); every property here is a real product value.
> The artboard gives the ACTIVE tab a solid 2px underline in the brand colour (`border-bottom:2px solid #5A5552`) and every RESTING tab a 2px TRANSPARENT underline of the same weight (so no tab shifts when its state changes) — margin-bottom -1px pulls that underline onto the container's own 1px hairline. TabRow.tsx renders no per-tab border at all (only weight/colour distinguish active vs resting) — its own header comment states this is deliberate ('matches the artboards, which show only weight/colour distinguishing the active tab'), which this measurement shows is incorrect: the artboard underlines the active tab.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| TabRow | `display` | `flex` | `flex` | MATCH |
| TabRow | `gap` | `4px` | `4px` | MATCH |
| TabRow | `border-bottom-width` | `1px` | `1px` | MATCH |
| TabRow | `border-bottom-color` | `rgba(0,0,0,.12)` | `rgba(0, 0, 0, 0.12)` | MATCH |
| active tab | `padding-top` | `9px` | `9px` | MATCH |
| active tab | `padding-bottom` | `9px` | `9px` | MATCH |
| active tab | `padding-left` | `12px` | `12px` | MATCH |
| active tab | `padding-right` | `12px` | `12px` | MATCH |
| active tab | `font-size` | `12.5px` | `12.5px` | MATCH |
| active tab | `font-weight` | `700` | `700` | MATCH |
| active tab | `color` | `#1A1A1A` | `rgb(26, 26, 26)` | MATCH |
| active tab | `border-bottom-width` | `2px` | `2px` | MATCH |
| active tab | `border-bottom-color` | `#5A5552` | `rgb(90, 85, 82)` | MATCH |
| active tab | `margin-bottom` | `-1px` | `-1px` | MATCH |
| resting tab | `padding-top` | `9px` | `9px` | MATCH |
| resting tab | `padding-bottom` | `9px` | `9px` | MATCH |
| resting tab | `font-size` | `12.5px` | `12.5px` | MATCH |
| resting tab | `font-weight` | `600` | `600` | MATCH |
| resting tab | `color` | `#5A6B67` | `rgb(90, 107, 103)` | MATCH |
| resting tab | `border-bottom-width` | `2px` | `2px` | MATCH |
| resting tab | `border-bottom-color` | `transparent` | `rgba(0, 0, 0, 0)` | MATCH |
| resting tab | `margin-bottom` | `-1px` | `-1px` | MATCH |

## TagPopover

- Spec file: `fsi-app/.discipline/rendering/audit/spec/tagpopover.json`
- Mount: `detail-shell` at 1440 px
- Artboards: none — no dc.html artboard carries this element (confirmed by grep across every extracted page section this session); README.md §0.4 'Workspace tags' names it only as "needed as an overlay artboard"
- Design source: src/components/ui/TagPopover.tsx's own docstring, which states it is built against "Claude Design's provisional spec" under the operator ruling quoted there: "build it; a restyle later is cheap"
- Result: 6 MATCH, 0 not matching

> UNDESIGNED BY RULING, recorded not judged: the closing rulings list this under DO NOT TOUCH / 'Unchanged and undesigned, do not invent: overlays (Export, Share, row overflow, notifications, tag popover styling)'. There is no dc.html value or README number to compare against, so every row below is a MEASUREMENT of the component's OWN stated values (from its own docstring), establishing a baseline for when Claude Design sends the real overlay spec — a MATCH here means the code matches its own documented intent, not that the intent is correct or final.
> This mount forces the popover open (`open: true` on DetailTagRow, forwarded to TagPopover) since the runner does not click; the trigger button itself is covered by DetailTagRow's own row.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| TagPopover | `width` | `280px` | `280px` | MATCH |
| TagPopover | `background-color` | `#FFFFFF` | `rgb(255, 255, 255)` | MATCH |
| TagPopover | `border-top-left-radius` | `10px` | `10px` | MATCH |
| TagPopover | `border-top-width` | `1px` | `1px` | MATCH |
| TagPopover | `border-top-color` | `rgba(0,0,0,.12)` | `rgba(0, 0, 0, 0.12)` | MATCH |
| search input row | `font-size` | `13px` | `13px` | MATCH |

## WatchButton (row variant) — watched / unwatched states

- Spec file: `fsi-app/.discipline/rendering/audit/spec/watchbutton.json`
- Mount: `watchbutton` at 1440 px
- Artboards: 11-watchlist, 03-regulation-detail (action row)
- Design source: docs/design/handoff-2026-09-06/'Caros Ledge UI System.dc.html' #sys 'Actions · stat block' (the ☆ Watch action button chrome: padding 8px 14px, border rgba(0,0,0,.25), radius 6, background #fff, font-size 12.5px, weight 600 — the family WatchButton.tsx's own header comment says the row variant matches: 'chrome matching ActionRow's other three buttons'); operator ruling 3.5 ('watched rows show a filled ★ in ink and the label "Watching"; the action on hover/menu is "Unwatch". A watched row must never read "Watch"'); src/components/ui/WatchButton.tsx (real component, mounted with `initialWatched` so no network fetch is needed to reach a deterministic watched/unwatched state)
- Result: 8 MATCH, 0 not matching

> CONFIRMED live: WatchButton's row variant (RowWatchButton) satisfies ruling 3.5's FIRST half exactly — watched=true renders '★' (filled, aria-hidden) plus the literal label 'Watching', in ink (--ink, via ActionButton's secondary-variant colour); watched=false renders '☆' plus 'Watch'. Checked below.
> Text checks below read '★Watching' / '☆Watch' with no space — RowWatchButton's glyph and label are adjacent JSX children with no whitespace text node between them, so textContent concatenates with none; this is the DOM's actual (harmless, whitespace-collapse-safe) concatenation, not a design statement about spacing, and matching it exactly avoids a false MISMATCH the harness's own README warns against (rule 14).
> CONFIRMED ABSENT, by a codebase-wide grep ('grep -rn "Unwatch" src/'), zero matches: ruling 3.5's SECOND half ('the action on hover/menu is "Unwatch"') has no implementation anywhere in the product. RowWatchButton's label is `{watched ? "Watching" : "Watch"}` with no third, hover-only state — the control has no hover-driven text swap at all, so there is no element a selector could address for an 'Unwatch' string; this is stated in prose rather than forced into a spurious selector check, per this harness's own rule (a false MISMATCH is worse than no audit, CLAUDE.md rule 14). The gap is real and is the second half of the same ruling the row-label check below confirms the first half of.

| Target | Property | Design source | Measured | Status |
|---|---|---|---|---|
| WatchButton (row variant) — watched / unwatched states | `text` | `★Watching` | `★Watching` | MATCH |
| WatchButton (row variant) — watched / unwatched states | `padding-top` | `8px` | `8px` | MATCH |
| WatchButton (row variant) — watched / unwatched states | `padding-left` | `14px` | `14px` | MATCH |
| WatchButton (row variant) — watched / unwatched states | `border-radius` | `6px` | `6px` | MATCH |
| unwatched — label + glyph | `text` | `☆Watch` | `☆Watch` | MATCH |
| watched — border colour (ActionButton secondary chrome) | `border-width` | `1px` | `1px` | MATCH |
| watched — border colour (ActionButton secondary chrome) | `border-style` | `solid` | `solid` | MATCH |
| watched — border colour (ActionButton secondary chrome) | `border-color` | `rgba(0,0,0,.25)` | `rgba(0, 0, 0, 0.25)` | MATCH |

## Failures the harness itself hit

None. Every spec file mounted and measured.

---

# Appendix: the seed source-level pass (2026-09-07, preserved verbatim)

The pass below is a SOURCE read, not a rendered measurement — it is kept because its A-list is
the numbered handoff the fix lanes are working from. Where a generated table above measures the
same element, the measurement is the authority.

# Audit: build vs design, value-level (2026-09-07)

Base compared: `origin/master` `13ef5e7a` (train/wave56, 2026-09-07), read in worktree
`/root/work/lanes/uxaudit` on branch `lane/uxaudit-2026-09-07`.

## Method actually used (read this before the tables)

The brief asked for a full getComputedStyle extraction, at 1440 and 375/390, for every
part on every one of artboards 00-19, mounted through the rendering guard's fixtures. That
full sweep was not completed this session — doing it honestly (real fixtures, a real
Playwright pass, no invented numbers) for ~20 artboards and ~30 shared parts is a
multi-session job, and the operator's actual complaint ("you didn't check your work
against the design") is best answered first by verifying the specific, already-named
defects against the real committed source, not by producing a wide table of numbers I did
not actually measure.

So this pass did two things, both source-level and both [CONFIRMED] against the literal
committed code at `13ef5e7a` (not a screenshot, not a memory of an earlier lane's report):

1. **Every one of the operator's 17 items** (plus the two closed rulings 5.1/5.2), traced
   to the exact file and line that would produce or fix it, and read.
2. **A spot-check of the token layer and the shared parts those 17 items touch**
   (`theme.css`, `Absence`, `StateNote`, `ImpactMeter`, `FactCard`, `ActionRow`,
   `WatchButton`, `Chips`, `BandGradientRule`, `DetailSection`, dashboard `SectionHeading`,
   `Sidebar`) against the README's literal value table, which surfaced one further,
   previously-unlogged, sitewide defect (A-item below).

This is "read the actual CSS-in-JS value that ships" — mechanical, not a screenshot
eyeball comparison — but it is a source read, not a rendered `getComputedStyle()` call, and
it does not cover artboards 00, 04-12, 14-17, 19 at all, nor mobile widths, nor any part
not listed above. Those are marked NOT AUDITED below, not MATCH.

**Scratch scripts**: none were written. Everything below came from `grep`/`Read` against
committed files; there is nothing parametrised or reusable to commit under
`fsi-app/.discipline/rendering/audit/`. A future pass that wants the full
getComputedStyle sweep the brief describes should start there — mount each fixture in
`.discipline/rendering/fixtures*.mjs`, read the same properties this doc's tables name, and
diff against a JSON spec file derived from the README/dc.html (not hardcoded in the
script).

**Runtime**: approx. 45 minutes of tool time (worktree setup, DEVIATION-LOG read — 174
lines/~40K tokens — README read, then ~20 targeted greps/reads against `fsi-app/src`).

## Summary

Counts, this pass only (17 operator items + 2 rulings + 1 additional finding = 20 items
judged; nowhere near "every artboard"):

| Status | Count |
|---|---|
| MISMATCH | 10 |
| MATCH | 8 |
| PARTIAL (mixed: fixed in one respect, not in another) | 1 |
| NOT AUDITED (everything outside the 20 items above) | everything else |

### A-list: mismatches and not-in-spec items found this session, for a fix lane to work from

- **A1.** Dashboard "All N immediate" foot line (`DashboardBrief.tsx:235`) is still plain
  text inside `CardFoot`, not a link/button — no `href`, no `onClick`. Operator 1.1,
  confirmed still broken.
- **A2.** Regulation detail action row still renders `<HeroPriorityDropdown>` (the
  "Immediate ▾" retag control) ahead of `<ActionRow>`
  (`RegulationDetailSurface.tsx:188-191`). Operator 3.1, confirmed still present — this is
  the row-level dropdown to remove from the action row; it is distinct from the
  operator-restored per-row `PriorityDropdown` on the *list* page (DEVIATION-LOG row
  2026-09-07, "restoration"), which the operator's R7 ruling explicitly keeps. Removing 3.1
  means removing `HeroPriorityDropdown` from the detail action row only.
- **A3.** `DetailHeader` (`DetailShell.tsx:128-134`) still renders a second, inline
  `<CommandBar>` (`askPlaceholder`/`askScope`) beside the title on every detail page,
  wired from `RegulationDetailSurface.tsx:184-185` with the exact string ruling R4 gives
  ("Ask about this regulation") — but R4 describes where that scoped text belongs (the
  page's one masthead command bar), and operator item 2.3 is explicit that this second bar,
  wherever it sits, must be removed entirely. Confirmed still present on regulation detail;
  not checked on the other three detail surfaces this session (they also pass
  `askPlaceholder`/`askScope` into the same shared `DetailHeader`, so the same defect is
  structurally present on all four, but I did not re-read each surface's own file to
  confirm the prop is actually populated on all four — PARTIAL confirmation, regulation
  detail is [CONFIRMED], market/research/operations are [INFERRED] from the shared
  component).
- **A4.** `DetailTagRow` (`DetailTagRow.tsx:62`, via `TagPopover.tsx:195-220`) always
  renders its own "+ Tag" trigger button, in addition to `ActionRow`'s own "+ Tag" pill —
  both are wired to the same lifted `open`/`onOpenChange` state so they open one popover,
  but there are visibly two "+ Tag" controls on the page (one under the title, one in the
  action row). Operator 3.2 is explicit the under-title row should show applied tags only,
  never a second trigger. Confirmed still present.
- **A5.** `FactCard.tsx` — all three variants use a **uniform** border, not the
  form-distinguishing asymmetric borders operator item 2.4 and README §0.4 both specify:
  - sourced (line 116-121): `border: "1px solid var(--ink)"` on all four sides, single
    `var(--radius-control)` (6px) on all corners. Spec: 2px solid `#1A1A1A` LEFT edge only,
    1px `rgba(0,0,0,.12)` on the other three, radius `0 8px 8px 0`.
  - inference (line 80-88): `border: "1px dashed var(--line-1)"` = `rgba(0,0,0,.12)`
    dashed all round. Spec: `1px dashed rgba(0,0,0,.25)` all round — opacity too low
    (.12 vs .25).
  - counsel (line 96-107): `borderLeft: 3px solid var(--action)` (`#F97316`), `border: 1px
    solid var(--line-1)` on the rest, radius `0 6px 6px 0`. Spec: 2px (not 3px) solid
    `#F97316` left edge, "otherwise as sourced" (which itself is unbuilt per the row
    above), radius `0 8px 8px 0` (8px, not 6px, per the sourced-card radius it should
    match).
  This is exactly the defect the operator named ("a uniform grey perimeter on all three is
  the bug") and it is confirmed still present, unchanged, at `13ef5e7a`.
- **A6.** `Sidebar.tsx:298` nav card margin is still `"16px 0 16px 16px"`. Operator 4.2
  requires `"20px 0 16px 16px"` (20px top). Confirmed still 16px.
- **A7.** Dashboard `SectionHeading` (`DashboardBrief.tsx:44`, used by both "Due next" and
  "What changed") still renders `borderBottom: "2px solid var(--ink)"` under the title —
  exactly the treatment the closed ruling 5.1 names and rejects ("Live 18a... is wrong:
  remove it"). No 3px graduated rule above the title exists on this component at all.
  Confirmed still present, unfixed by the 5.1 ruling.
- **A8.** `DetailSection` (`DetailShell.tsx:523-556`, every detail-page section card on all
  four detail surfaces) has *no* section-title rule at all — neither the old
  border-bottom bug nor the new 5.1 graduated rule. NOT BUILT: the 5.1 ruling's required
  3px `linear-gradient(90deg,#5A5552,#5A5552 22%,rgba(90,85,82,.18))` rule above the title
  is absent from every detail section on every detail page.
- **A9. (new, not on the operator's list)** Anton title `letter-spacing` is inconsistent
  and wrong in three of four places that render one. README's type scale states
  `letter-spacing:.04em` for every Anton title/numeral, with no per-context exception.
  Confirmed values:
  - `Masthead.tsx:105` (the page-level H1) — `0.04em`. MATCH.
  - `DetailHeader` h1 (`DetailShell.tsx:115`, all four detail pages' titles, including the
    PPWR page named in operator 3.2/1.2) — `0.02em`. MISMATCH, half the spec value.
  - `DetailSection` h2 (`DetailShell.tsx:542`, every card-section title on every detail
    page) — `0.02em`. MISMATCH.
  - dashboard `SectionHeading` h2 (`DashboardBrief.tsx:55`, "Due next"/"What changed") —
    `0.02em`. MISMATCH.
  This was not in the operator's 17-item list or the DEVIATION-LOG and appears to be a
  real, sitewide, unfixed gap: only the top-of-page masthead title got the correct
  `.04em`; every card/section/detail title that also uses Anton uppercase got `.02em`
  instead, everywhere it was checked.
- **A10.** `WatchButton`'s row variant (`WatchButton.tsx:373-379`) never renders the word
  "Unwatch" anywhere — watched state shows a filled star and the label/tooltip "Watching"
  both at rest and on hover (`title="Watching — updates surface on your dashboard
  watchlist"`). Operator 3.5 requires the hover/menu action text to read "Unwatch"
  specifically, distinct from the persistent "Watching" state label. Confirmed: "Unwatch"
  does not appear in `WatchButton.tsx` at all (`grep -c Unwatch` = 0).

### Verified MATCH (already correct at `13ef5e7a`, do not re-touch)

- **Operator 2.1** (literal "UNSCORED" string) — confirmed absent from `src/` entirely
  (`grep -rn UNSCORED src/` = no hits); `ImpactMeter`'s row variant renders a dashed
  baseline + `<Absence reason="unscored"/>`, matching the spec's small-caps convention.
  `Absence.tsx` itself renders `10.5px`/uppercase/600/`var(--ink-3)` (`#7A6E6C`), matching
  README's stated Absence styling exactly.
- **Operator 3.3** (Operations matrix dimension count) — `OperationsLedger.tsx:85-90`
  defines six `SOURCED_DIMENSIONS` including `{ num: 1, key: "regulatory", db:
  "regulatory_feasibility", name: "Regulatory feasibility" }` as D1. Six dimensions,
  D1-D6, confirmed present and wired into `RegionDimensionMatrix`.
- **Operator 1.2** (PPWR title bypassing the shared title component) — NOT reproduced.
  `RegulationDetailSurface.tsx:180-183` passes `title={r.title}` into the same shared
  `DetailHeader` every other regulation uses; there is no separate render path for the
  PPWR slug. The title *does* render in Anton/uppercase (see A9 above for the actual,
  different defect on this same element: wrong letter-spacing, not wrong weight/case).
  Whatever produced the operator's "body weight" observation on production is not
  reproducible by reading this component — either it was already fixed by an earlier lane,
  or the defect lived somewhere this session did not check (e.g. a stale production
  deploy, or a different rendering path for that specific slug not found by this grep).
  Flagged as unresolved rather than closed.
- **Operator 2.5** (chip families) — `Chips.tsx`: `BandChip` (25-45) is a tinted pill with
  a round 6px dot in the band colour and band-coloured text; `TierChip` (63-90) is a
  bordered square; `TagChip` (91-108) is neutral `var(--tag)` with no dot, no border;
  `WorkspaceTagPill` (119-160) has a 6px square (`borderRadius:1`) ink dot, `#F5F2EE`
  fill, `1px rgba(0,0,0,.14)` border — matches ruling R6 exactly. `FilterChip`'s
  active/inactive states (198-201) match README's stated ink-fill/neutral-outline pair.
  All MATCH.
- **Operator 2.6** (state note strip) — `StateNote.tsx:29-32`: `borderLeft: 3px solid
  <color>`, `borderRadius: "0 6px 6px 0"`, `padding: "9px 12px"`. Exact match to spec.
- **Ruling 5.2** (coloured gradient rule, one place sitewide) — `BandGradientRule` is
  consumed only by `Sidebar.tsx` (nav card), `TopBar.tsx` (mobile top bar) and
  `AppShell.tsx` (drawer) — confirmed via `grep -rl` across `src/`, no fourth consumer.
  MATCH: the coloured band-proportion rule is not used in any masthead, panel or section
  card.
- **Token layer** (`theme.css`) — spot-checked the values these findings depend on:
  `--card #FFFFFF`, `--tag #F5F2EE`, `--ink-3 #7A6E6C`, `--immediate #DC2626`/tint
  `#FEF2F2`, `--action #F97316`/tint `#FFF7ED`, `--monitor #2563EB`/tint `#EFF6FF`,
  `--awareness #16A34A`/tint `#F0FDF4`, `--line-1 rgba(0,0,0,.12)`, `--line-2
  rgba(0,0,0,.08)`, `--line-3 rgba(0,0,0,.06)`, `--radius-control 6px`, `--radius-card
  10px`, `--fs-105 10.5px` — all match README's token block exactly.

### Things this session could not judge, and why

- **Everything at 375/390 width.** No fixture was mounted; the DEVIATION-LOG rows for
  wave 55-56 (`exemptions-375.mjs`, the mobile-390 spec build) describe a moving target
  under active, dated exemptions that this session did not re-verify.
- **Artboards 00, 04-12, 14-17, 19** (system sheet; market, research, operations, map,
  watchlist, community, admin details/lists; account; settings; auth; onboarding; masthead
  option turn) — not read against their built pages this session at all. The
  DEVIATION-LOG's own entries (rows 71-173) are the best current source for what each of
  those pages actually deviates on; several of the operator's 17 items (2.4 fact cards,
  5.1 section rule, A9 letter-spacing) apply to shared parts these pages also render, so
  the same defects are very likely present there too, but that was not independently
  confirmed page-by-page.
- **Live/populated data states.** Every prior lane's screenshot attempts in this sandbox
  (DEVIATION-LOG rows 88, 103, 120, 128-129, 133-135, 137-138, 142-143, 146, 149, 158)
  hit the same wall: no reachable Supabase project, so a populated `ListRow`, band-tile
  numeral, or fact-card grid was never actually rendered and photographed against an
  artboard in this environment, this session included. Anything that only manifests with
  real data (row counts, actual fact-card content, the "In this list · N of M" rail
  card's real prev/next state) is DATA-DEPENDENT and unjudged.
- **Operator 3.2's under-title-row scope on market/research/operations.** A3/A4 confirm
  the regulation-detail page directly; the other three detail surfaces pass the same shared
  components (`DetailHeader`, `DetailTagRow`) so the same defects almost certainly repeat,
  but each surface's own call site was not individually re-read this session.

## Notes for the fix lane

A1-A10 above are numbered for direct handoff. A2-A5, A7, A8, A9 are all inside shared
parts (`ActionRow`/`DetailHeader`/`FactCard`/`SectionHeading`/`DetailSection`), so each
fix, done once in the shared part, should resolve it on every page that consumes that
part — per the base lane's own rule, do not fork a page-local patch.

No product code was modified by this audit. No scratch script was committed (see Method
above — nothing reusable was produced this session; a future pass should build one).
