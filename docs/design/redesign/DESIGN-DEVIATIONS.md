# Design Deviations Log

Deviations are PROPOSALS for Jason to review, never decisions. One entry per deviation.

Redesign template agents record here any place where the built UI departs from the approved mock in this folder (the visual source of truth). Do not silently diverge: log it, keep the deviation minimal, and leave the decision to Jason. If in doubt, match the mock to the pixel (HANDOFF §8.1) and file the divergence below instead.

| Template | What deviated | Why | Screenshot/ref |
| --- | --- | --- | --- |
