# Coordinator handoff, 2026-09-18 17:55 EDT: fix the machine, then the data

Written for a cold session. Read this once, top to bottom, before any tool call that changes anything. It
replaces chat archaeology. This file is in the repo (`docs/ops/HANDOFF-2026-09-18.md`) because git is the only
transport: its first version sat in an untracked local folder, and the next session, which had no access to
that machine, could not read it. On the operator's machine the running ledger is `progress.md` and the project
memory is `project_brief_chain_w9_2026_09_11.md`; neither is in the repo.

## 0a. If you have NO access to the operator's machine (a cloud or remote session), read this first

You can read GitHub and the vault (`docs/` on master). You cannot see the lock, the train's log, the process
table, or any local worktree. Three consequences:

1. **A merge train is probably running on the operator's machine.** It rebases each open lane PR, force-pushes
   it with a lease, waits for the four required checks, squash-merges, about eleven minutes per PR, and stops by
   itself on any conflict or red check. Read its state from GitHub alone:
   `gh pr list --state open --json number -q '[.[].number|select(.>=704)]|sort'` (what is left) and
   `gh pr list --state merged --limit 20 --json number,mergedAt,title` (the cadence). Merges arriving every ten
   to fifteen minutes means it is alive. No merge for more than thirty minutes with lane PRs still open means it
   stopped (section 4) or died.
2. **While it may be alive, do not touch what it touches.** Do not push to, rebase, or force-push any branch that
   has an open PR numbered 704 or higher: the train's lease would fail and it would stop with a rebase left in
   progress on the operator's machine. Do not merge those PRs by hand; out-of-order merges multiply conflicts.
   Safe for you: read anything; write NEW docs or briefs on NEW branches through a PR; plan and write the next
   lane briefs (section 3); answer the operator's open decisions (section 6).
3. **You cannot finish what is local.** A stopped train leaves its rebase inside a local worktree, so only a
   session on the operator's machine can resolve it (section 4). Two finished lanes, M2 and W10-A, are
   committed locally and NOT pushed: their branches do not exist on GitHub, so you cannot see or review them;
   section 2 describes them. The coordinator scripts (section 7) are local too.

If you DO have access to the operator's machine, start at section 0.

## 0. The first five minutes: look before you start anything

A merge train was running when this session ended. Today a push queue SURVIVED a session restart as an orphan,
and starting a second one caused thirty minutes of colliding test suites. Sub-agents did NOT survive. So expect
the train alive and the two lanes gone, and verify. Run these, read-only, before anything else:

```
S="C:/Users/jason/AppData/Local/Temp/claude/C--Users-jason/fddbeade-7f79-480a-9254-e8fdb3278567/scratchpad"
cat "$S/push-queue.lock/pid" 2>/dev/null || echo "no lock held"        # a pid = a queue or train is running
grep -E "^== PR #|merged #|train stops|CONFLICT|push failed|train complete" "$S/merge-train.log" | tail -n 12
gh pr list --state open --json number -q '[.[].number|select(.>=704)]|sort|map(tostring)|join(" ")'
```
and the process table (PowerShell): `Get-CimInstance Win32_Process | ? { $_.CommandLine -match 'push-queue|merge-train|pre-push|run-test-suite' }`.

- A lock with a live pid: a queue or train is running. Leave it. Both scripts refuse or wait on that lock by
  themselves now, so you cannot double-start them, but do not write into any worktree the train will visit.
- The train log ends in `train stops` or `CONFLICT`: expected, see section 4.
- If that scratchpad folder is gone (it is session temp), the scripts are lost: see section 7 for what each did.

## 1. What the operator said, and what it binds you to

The operator's instruction for this handoff, verbatim: "Make sure your instructions follow absolutely everything
I said." So this section quotes the operator's own words from 2026-09-18 (typing as sent) and states what each
one requires. Where a quote and my paraphrase disagree, the quote wins. `CLAUDE.md` at the repo root (18
standing rules) and the `ledger` skill are binding too and are NOT restated here: read them. A remote session
does not auto-load `CLAUDE.md`; open it.

### 1A. Today's rulings

1. **The system before the data.**
   "we have been updating the sytem and the data on that system. BUt before we continue updating the data and
   eating tokens doing it, i want tyo make sure we have fully finished builing the system that collects
   evaluates and uses the data and puts it into the site, i do not think that is complete and we will just have
   to go back and fix the data again"
   "the sytem is the focus right now, NOT the data, then after we fix the system and make sure it works, we fix
   the data"
   "Remember. We are fixing the system before we run everything through it"
   Binds: no batch apply, provenance heal, bulk re-scan or brief-authoring lane until the proof run (plan 6.2)
   passes. A coordinator run before that is a bounded proof of ONE hop (a capped fetch drain, a capped
   ledger-consume apply, one producer run), never a pass over the corpus.
2. **Report the machine, not the data.**
   "your telling m,e about violations of the data, but i specificly asked about the build and the tools to make
   these items work and work togther, the flywheel the harnass ect, NOT the data, the data is a problem because
   the tool build is not wired togther and complete."
   Binds: lead every report with machine state (is it wired, has it fired on its own, is it gated). Corpus
   counts are an appendix. I got this wrong once today and was corrected.
3. **Fix all of it.** "after this audit is complete, make a build plan to fix it ALL"
   Binds: plan section 6 decides every audit row (finish unless a rule says delete). Nothing is left "built,
   dormant". No per-row sign-off is needed; the operator's word overrides any line.
4. **Sub-agents execute exactly the brief.**
   "us sub agents of sonnet and haiku to complete the work for the plan, do not allow them to deviate or solve
   probelms that your plan does not address, they must have yuou solve the problme that they fix"
   Binds: Sonnet and Haiku lanes do the work. A lane that meets a problem its brief does not address STOPS and
   reports; the coordinator solves it, amends the plan or brief, and re-dispatches. No workarounds, no widened
   scope. (A lane refuting a false premise with evidence is not deviation: see the learned rules below.)
5. **Duplicates are part of this build, and the build does not stop for them.**
   "A couple days ago we found that duplicate pages and processes were being completed and then forgotten.
   Clogging the work stream And the data. Did we complete that work making sure it couldn't happen again and all
   duplicates were removed? Is that wired. ... If not it needs to be part of this build Plan. We do not stop what
   we are doing we make sure this is a part of it"
   Binds: plan section 6.7 (the 2026-09-17 system-health audit's remaining removals). And as a working rule: a
   side question gets folded into the plan; it never halts the build.
6. **Obsidian must be current, and the process must guarantee it.**
   "i want YOU to fix this now. Obsidian NEEDS to be updated"
   "i want to make sure the proceeses built into the sytem allow for backups to obsidian going forward"
   Binds: the vault is the `docs/` folder of the operator's main checkout, kept on master by
   `.claude/hooks/vault-sync.mjs` (SessionStart, the done skill's last step, the merge train after every merge).
   Anything a future session needs must be COMMITTED to the repo: that is the only thing that reaches Obsidian
   and other sessions. The first version of this handoff broke that and the next session could not read it.
7. **Lanes run the push gate themselves.** On my recommendation to have lanes run the repo's pre-push check
   once, last, before reporting, the operator ruled: "This should be fixed"
   Binds: the locked wrapper `lane-prepush-check.sh`, started as ONE background task and never polled. A lane
   never allowlists, exempts, re-seeds upward or weakens a gate to pass; two FAILs on one step = STOP. This
   reverses the earlier "lanes never run the hook". The old rule's cause is still real (on 2026-09-13 four lanes
   sat in "waiting for the gate" loops and never reported), which is why it is one background task, not a poll.
8. **The site design went off track; parts, not pages.**
   "additionally we need to adjust how the site design has gone off track, i'm providing a redesign info and
   image to match to. do this now and the design build plan" and "add this to the build plan as well as a fix
   2.16 IMPACT METER"
   Binds: `docs/design/parts-brief-2026-09-18.md` is the operator's brief, landed verbatim, and governs every
   surface change. Its own rules: a lane owns one shared PART and every call site on all routes; the operator
   signs off PARTS, page screenshots are not the review unit; the artboard wins over the README and you tell
   the operator; a case not drawn is ASKED, never invented; gate F49 with no grandfathering; lanes in the
   brief's order (FactCard first, and it needs artboard 21 in the bundle).

### 1B. How the operator wants to be worked with

- **Show it in chat.** "i dont see the estimated time, when i ask you somthing show me in chat". Also "i want to
  see the build plan" and "show me the roport in this chat". When the operator asks for something, put it in
  the reply. Writing it to a file and naming the path is not an answer.
- **Do not promote a statement into a rule.** I saved a memory from the operator's remark about which
  processes should be running. The reply: "that is not a rule, remove it, that was a ststemnt of fact for the
  present". Record a rule only when the operator gives one.
- **Build, do not keep planning.** "Are you still just running an audit to build a plan?" Say what is being
  built, not what is being studied.
- **Tokens are budgeted.** "What's the estimated time to finish the build. We have used 27% of our tokens for
  the week". Give an estimate when asked and label it an estimate. See section 8.
- From the operator's own preferences: clear, structured output, no fluff; diagnose before fixing; no
  assumptions; no hallucinations; think in systems; eliminate technical debt and duplication.
- The operator's last two messages of the session, about this file: "Do NOT fuck this up" and the quote at the
  top of this section.

### 1C. Standing constraints (carried into this session; all still bind)

Git and pushes: never `--no-verify`; never `git stash`; stage explicit paths, never `git add -A`; one writing
agent per worktree; serial pushes, one hook run at a time, one queue process; nobody COMMITS in the operator's
main checkout (the vault-sync hook fast-forwards it; lanes commit in worktrees); never run
`wire-pretooluse-settings.mjs --apply`; never stage `.env.local` or any secret; a push is done only when the
GitHub Actions run on that commit is green; the coordinator merges its own PRs on green. Commit trailer exactly
`Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`; PR bodies end with the Claude Code line.
Database: agents are SELECT-only, no database writes; DDL is applied by the coordinator BEFORE the dependent
code merges (CLAUDE.md rule 3); a policy change on live tables is an operator decision; never a corpus-wide
`length(result_content)` scan or any expression over a large text column (the small Supabase tier has a daily
IO budget: stored length columns, aggregates, LIMIT); batch applies use `--allow-brief-overwrite`, the 400 MB IO
budget and a 30 minute cooldown between applies; the refetch-capped apply needs the operator's
`GUARD-1-accepted` token (ADR-016) and is never self-granted.
Runtimes and spend: no LLM, API or paid calls in runtimes; no crons, no schedules (CLAUDE.md rule 16:
`scrape_cadence` stays off, never propose flipping it); everything free; no Browserless spend; no exploratory
workflow dispatches (answer a state question with SQL, never by dispatching a workflow).
Truth: every finding carries `[CONFIRMED]`, `[HYPOTHESIS]` or `[REFUTED]`, and a refuted one is corrected in
place, never deleted; never report a planner estimate as a count; every number says how it was obtained; a GAP
claim is licensed ONLY by the source's own statement of absence, never by the item type; never fabricate.
Building: grep the repo for prior art before building anything (I broke this today by writing my own lane
contract beside `docs/dispatches/lane-common-contract.md`); decide from doctrine and do not ask the operator
what doctrine already answers; no em dash, en dash or section sign in new prose (pre-commit rule 022; genuine
verbatim text carries the `glyph:verbatim` marker on the same line).

Coordinator rules learned the hard way today (all in the ledger with evidence):
- **Never edit a script while it is running.** Bash reads scripts incrementally. Patch only when idle, by temp
  file plus atomic rename.
- **The coordinator assigns every shared id; lanes never allocate.** Three collisions today from "take the next
  free number": RD-72, migration 326, skill category 46. Registry: RD-72 = L41 (F48); RD-73 = W10-A (F49);
  RD-74 = M9a (F50); migrations 326, 328 = L39 (applied), 329 = M9c (applied live), 327 unused on purpose; skill
  category 46 = M9a, 47 = W10-A. NEXT FREE: RD-75, F51, migration 330, category 48.
- **A sub-agent's report is a claim.** Verify commits, run the gate, read the diff that matters. Today: M2's
  allow-marker and W10-A's "false trip" label both needed reading, not trusting.
- **Run a fix on the real failing case BEFORE writing its tests**, then run the regression test RED before the
  fix and GREEN after. A unit test that passes with the bug present proves nothing.
- **Measure before theorising.** Four wrong explanations of the vault phantom today; `cmp` and `git hash-object`
  settled it in one step.
- **Run repo-wide git measurements from the REPO ROOT, and distrust a surprising zero.** `git grep <rev> -- <path>`
  resolves the pathspec against the current directory. From inside `fsi-app/`, `-- fsi-app/src` matches nothing,
  and I reported "zero uses on master" of a marker that 56 files carry. Before acting on a zero, run the same
  search for something known to exist.
- **A lane that refutes the coordinator's premise with evidence is doing its job.** The execution rule forbids
  improvising scope, not contradicting a false fact. M2 did the requested change AND reported my premise false,
  with a named file and a commit date. Verify such a refutation at once and correct the record in place.

## 2. Where the build stands

Plan: `docs/plans/complete-system-build-plan-2026-09-04.md` sections 5 (the machine before the data), 6 (the
build plan that fixes it all: lanes M0 to M9, proof run 6.2, data once 6.3, W10 parts 6.4, operator decisions
6.6, duplicates 6.7). Audit: `docs/audits/stage-audit-2026-09-18/README.md` (every hop exists as code; the loop
has never run end to end on its own; six wiring gaps). Design: `docs/design/parts-brief-2026-09-18.md` (the
operator's brief, verbatim, with 2.16) and `docs/design/parts-inventory.md`.

**Merged today, as of 18:48 EDT when this file was published (the train kept merging afterwards; the live truth
is GitHub, read as section 0a or section 0 describes, never this line):** #713 L44 vault-sync, #714 audit + plan
+ parts brief + inventory, #716 M5; then by the merge train, one every eleven minutes or so: #723 L44b (the vault
now carries the fixed sync hook), #704 L39 (migrations 326 and 328 had been live with their code unmerged;
closed), #705 P8, #706 L41, #707 reg-a, #708 research-ops. Master `227aba72` at 18:48. Migration 329 applied live
(drops `community_promotion_transitions`).

**Open lane PRs at 18:48, in train order (all listed in the coordinator's `merge-train.txt`):** #709 reg-b (the
train was on this one), #710 market, #711 005c, #712 L36, #715 P10, #717 M9c, #718 docs follow-ups, #719 M8,
#720 M9b, #721 M9a, #722 M1. Batch-file PRs (#709 to #711, #715) may merge: they are files, not data, and nothing
applies them. The predicted safe stop is among #719 to #722 (section 4).

**Two lanes DONE, verified by the coordinator, committed, trees clean, STAGED for push** (both reported in
the last minutes of the session; their lines are already in `push-queue.txt`, bodies `pr-m2.md` and
`pr-w10a.md`; the queue could not run because the train held the lock: start `push-queue.sh` once the lock is
free, then add their PR numbers to `merge-train.txt`):
- **M2** (`wt-session-d`, `lane/m2-ledger-consume-apply-2026-09-18`, HEAD `d8021e62`, three commits). The
  chained apply arms itself on the union of committed verdict batches under `scripts/turns/ledger-verdicts/`
  (`isApplyArmed({verdictsFilesCount}) > 0`; disarmed only at zero batches), capped by `--max-promote` 50
  (ceiling 200 refused), `LEDGER_CONSUME_APPLY_ENABLED` deleted, `metrics.verdicts_owed` plus the population
  report line "ledger-consume: N candidates await a verdict". `countCandidatesAwaitingVerdict` now goes through
  the shared reader `readAllByIds(..., {idColumn:"url", client:sb, match:q=>q.eq("status","candidate")})`
  (`scripts/lib/db.mjs:275`); that also fixed a latent double count in its old loop (a URL in two verdict
  batches landing in two chunks). 194/194, tsc clean, 41 fitness functions 0 violations, F45 6,227, F28 green
  after `ledger-consume-run-010.json`. This family never writes `census_worklist` (mints go through the
  chokepoint into `intelligence_items`).
  **CORRECTION, coordinator error [REFUTED by lane M2, then reproduced by the coordinator]:** I told M2 that
  master had ZERO uses of `// fitness-allow: F39` and that its marker would be the codebase's first gate
  bypass. False: 56 files on master carry that marker; it is a sanctioned, established form. My zero came from
  running `git grep <rev> -- fsi-app/src` from INSIDE `fsi-app/`, where that pathspec means `fsi-app/fsi-app/src`
  and matches nothing. The change itself stands on the reason that was always true (F39's header names the
  shared chunked reader as the one chunking home, and it expressed the need directly). M2 refuted its
  coordinator with evidence rather than complying silently: that is the lane doing its job.
- **W10-A** (`wt-landdocs-0911`, `lane/w10-a-f49-impact-meter-2026-09-18`, HEAD `056abef4`, five commits). F49
  parts-not-pages (RD-73), impact meter row variant per brief 2.16, UX compliance block, smoke spec
  `impact-meter-partial-smoke.mjs` rewritten for the stepped-fill model, skill category renumbered 46 to 47,
  pin OK. Its own full pre-push run: all eight steps OK. Verified by the coordinator: commit `39cf424c` (the
  "glob-portability false trip") touched ONLY its own `F49-parts-not-pages.test.mjs` and the skill pin; the
  gate file was NOT touched. The trip was real mechanics, honestly labelled: glob-portability scans raw file
  text for `from "..."`, and a fixture STRING spelling out an import line matched it; the lane removed the
  decorative fixture lines. WATCH on its PR: it could not execute the smoke spec locally (no Playwright in
  that worktree), so the 375 px geometry of the meter is [HYPOTHESIS]; CI's "Rendering guard" check measures it
  for real. It left eight dated `fitness-allow: F49` entries on Anton-title sites the inventory missed (two
  auth pages, six community sub-routes); the Masthead lane must clear them (plan 6.4).

## 3. What to do next, in order

1. Let the train finish or stop. When it stops on a conflict, section 4.
2. M2 and W10-A are DONE, verified and staged (section 2): do not redo them. When the lock is free, start
   `push-queue.sh` once (it takes the lock, pushes both through the hook, opens their PRs, exits on drain), add
   the two PR numbers to `merge-train.txt`, and run `merge-train.sh`. W10-A will hit the predicted skill-file
   and pin conflict once M9a (#721) has merged: section 4.
3. Dispatch **M3** (turns chained: briefs written, `brief-m3.md`) only after M1 (#722) AND M2 are merged. Then
   **M4** (brief chain at mint, `brief-m4.md`) after M3; **M6** (evaluate invokers, `brief-m6.md`) after M4;
   **M9d** (producer harness family, `brief-m9d.md`) after M9a and M9b merge; **M7** (last mile) after M5 and
   the FactCard lane: not yet briefed. Every dispatch: `brief-m-common.md` first (it now carries the repo lane
   contract, the UX contract, coordinator-assigned ids and the locked push gate), one idle worktree with
   `node_modules` (`npm ci` a fresh one), branch cut from current master, ids assigned IN the brief.
4. Coordinator hop proofs, each bounded, after the relevant merge: one maintenance dispatch (first
   machine-written ledger row and `maintenance-run-001.json`); fetch-drain apply, limit 8; ledger-consume apply,
   cap 50 (needs a committed verdict batch covering live candidates, else it promotes nothing: the population
   report's "candidates await a verdict" line says how many); one ecb-fx producer run (edges self-heal once the
   history window exceeds seven days); `backfill-derivation-edges`; sitemap slices (`--slice 100`) until the
   never-walked bucket (2,201 of 2,572) reads 0. After each, flip that hop's `enforceFired` in
   `.discipline/governance/loop-manifest.mjs`; F50's "hops not yet enforced" must reach 0.
5. Plan 6.7 duplicates: L36 (#712, on the train), L37, L38, L35h (eur-lex second home, F46 to 0), D2 (read-only
   data duplicate census, Haiku), P7 close.
6. **Proof run 6.2**: one dispatch at the head (`source-sweep.yml`, scope changed, a `loop_run_id`); an artifact
   at every hop with that id and `trigger: workflow_run`; F50 green. Record as `docs/audits/loop-proof-run-<date>.md`.
7. Only then data, once each (6.3): migration 299 WITH the re-mint of the 146 items its self-check names
   (67 initiative, 46 market_signal, 33 research_finding; applying it alone quarantines them); the authored W9
   batches in PR order through brief-apply (IO budget 400 MB, 30 minute cooldown, overwrite flag); heals; the
   refetch-capped apply on the operator's GUARD-1 token.
8. W10 parts lanes in the brief's order (FactCard needs artboard 21 in the bundle first), each signed off by
   the operator; page screenshots are not the review unit.

## 4. When the train stops (it will, and that is by design)

`push_one` stops on any rebase conflict outside `docs/ops/session-log.md`, leaving the rebase in progress.
Predicted stops:
- **M1 #722, M8 #719, M9a #721, M9b #720** each added a different section to
  `fsi-app/scripts/harness-runs/CONVENTION.md`, `governing-files.mjs`, `fsi-app/scripts/lib/run-artifact.mjs`
  (`ALLOWED_FAMILIES`) and re-pinned `meta-harness/PENDING-RUN.md`. Resolve BY HAND in the stopped worktree:
  keep BOTH sides' additions (a blind union of hunks in a `.mjs` can produce invalid JavaScript: check the array
  and object syntax, run `node --check`), `git add`, `GIT_EDITOR=true git rebase --continue`, then from its
  `fsi-app`: `node <scratchpad>/repin.mjs <branch>` and `node <scratchpad>/reseed-f45.mjs "<note>"`, commit what
  changed, run the F28 test, and re-run `merge-train.sh` (it skips merged PRs).
- **M9a #721 and W10-A** both edit `fsi-app/.claude/skills/remediation-discipline/SKILL.md` and both re-pin
  `.discipline/governance/skill-contract-map.mjs`. The skill file: union (category 46 then 47). The pin: take
  either side, then `cd fsi-app && node <scratchpad>/repin-skills.mjs` (rewrites only stale content-hash
  literals, then runs checkDrift; prints `unchanged`, `re-pinned: ...` or `PROBLEMS`).
- A suite failure that looks like timing: a lane's bare self-run may have overlapped. Re-run once before
  sending any lane back.

## 5. Owed fixes to the coordinator tooling (do these when nothing is running)

1. **Split the lock by what it protects.** Today one lock (`push-queue.lock`) means both "one queue or train
   instance" and "one hook run", and the train holds it for its whole multi-hour life, so a lane's wrapper waits
   hours. Correct design: an instance lock per script held for life, plus `hook.lock` held only around
   `push_one` (the hook run), released while the train waits on CI. The wrapper already takes `hook.lock`; move
   the queue and the train onto it in ONE change, tested together in an isolated lock folder
   (`LANE_CHECK_SP`-style override), never half-migrated.
2. **`mergeable=UNKNOWN` in `merge-train.sh`** is GitHub's transient state for some seconds after master moves;
   the train treats it like CONFLICTING and does a needless rebase, hook run and re-push. Poll
   `gh pr view --json mergeable` until it is not UNKNOWN (up to about 60 s) before deciding.
3. **Land the tooling in the repo (plan row P6).** push-queue, merge-train, repin, reseed-f45, repin-skills,
   lane-prepush-check live in session temp and die with it. The operator's standing concern is processes built
   and then forgotten. Home: `fsi-app/scripts/coordinator/` with a runbook section and one line in the repo lane
   contract naming the wrapper and its lock.
4. **Teach `push_one` the two predictable conflicts** (skill file union plus pin recompute; harness
   PENDING-RUN already handled), proven on a throwaway clone first.
5. **Vault phantom, cause unknown.** Git on the vault checkout reported three byte-identical files as modified
   (mtimes at the minute of a fast-forward that did not touch them). Refuted: line endings, stale stat cache,
   file mode, fsmonitor or split index. The hook now classifies by content and reports the phantom count each
   run. If the count is ever non-zero again, note which files and what ran in that minute.

## 6. Operator decisions open

- Region-scoped community rooms (plan 6.6): vertical groups are GLOBAL by design; no workspace-level region
  field exists (`profiles.region` is a per-user array). A second, region-bound room type is a product call.
- Artboard 21 (`screens/21-detail-parts.png`) for `docs/design/handoff-2026-09-06/`, and the eight cases not
  drawn in the parts inventory section 4, including a conflict between brief 2.3 and the 2026-09-07
  SectionHeading ruling and the brief reversing the 2026-09-09 CommandBar ruling.
- Later, in the data phase: the GUARD-1 token for refetch-capped; retype decisions on cfcf9e4c, 9ffa15d6,
  d2da85da; the capture list in the ledger.

## 7. The coordinator scratchpad (session temp; may not survive)

`C:\Users\jason\AppData\Local\Temp\claude\C--Users-jason\fddbeade-7f79-480a-9254-e8fdb3278567\scratchpad`

**Durable copy, taken 2026-09-18 18:00:** `coordinator-tooling-2026-09-18/` beside this file (329 entries:
every script, brief, PR body, lane report, the queue and train files, the seven audit reports). It is not
inside any worktree the train visits.
**TRAP, read before running anything from the copy.** Each script hardcodes `SP=` to the session scratchpad
above, and the LIVE lock, logs and queue files are there; the running train uses them. The lock only
guarantees one runner if everyone shares ONE lock folder. So: while the original scratchpad exists, run the
scripts FROM it, never from the copy. Use the copy only if the scratchpad is gone, and then FIRST confirm no
queue or train process is alive (section 0), then repoint `SP=` in `push-queue.sh`, `merge-train.sh` and
`lane-prepush-check.sh` to ONE new folder together. Two lock folders means two runners can collide, which is
the failure this whole evening's tooling work closed.
- `push-queue.sh` + `push-queue.txt` (`worktree|pr-body|tag`) + `.done`: serial pushes. Per line: rebase onto
  master (union-resolves session-log), re-pin stale harness markers (`repin.mjs`), re-seed F45 DOWN and refuse
  upward (`reseed-f45.mjs`), check the installed hook is the trampoline, push through the hook, open the PR.
  Single instance by lock; exits on drain.
- `merge-train.sh` + `merge-train.txt` (`pr|worktree|subject`): waits for the lock, then per open PR: rebase
  and re-push if not MERGEABLE, wait for the four required checks, squash-merge, run vault-sync. Stops on any
  failure. Backup of the pre-lock version: `merge-train.sh.bak-2026-09-18`.
- `lane-prepush-check.sh <worktree>`: the locked push gate for lanes (tested: pass, fail, waits on the
  coordinator lock, waits on another lane, reclaims a stale lock, bad usage).
- `repin-skills.mjs`, `repin.mjs`, `reseed-f45.mjs`, `resolve-conflicts.mjs`, `validate-slots-live.mjs`.
- Briefs `brief-m-common.md`, `brief-m1.md` to `brief-m9d.md`, `brief-w10a.md`, `brief-stage-audit-*.md`;
  reports `report-<lane>.md`; PR bodies `pr-<lane>.md`; `main-checkout-backup-2026-09-18/`.
- Read-only SQL: `node --env-file=C:/Users/jason/dotfiles/.worktrees/wt-adr016-ft/fsi-app/.env.local` with
  `scripts/lib/pg-conn.mjs` `connectPg()`; SELECT only, aggregates or LIMIT, never a large text column.

## 8. Tokens

The operator is watching the weekly budget. Measured today: read-only audit lanes 200k to 280k each; build
lanes 250k to 840k (W10-A the most, from fix cycles on the gate). A very long session re-bills its whole
context every turn: start fresh, load this file and the plan sections, not the ledger's full history. Haiku for
mechanical lanes (M9d, D2, PR bodies, the data-pass chain); Sonnet for anything touching workflow logic. Three
lanes at a time keeps merge conflicts on shared files manageable.
