# GATE-A execution state — resume anchor (2026-07-14, post-Unit-A, pre-compaction)

Resume anchor for the acquisition-rebuild dispatch after the GATE-A ruling. **Paid work is BLOCKED on the
Anthropic usage ceiling.** Related: [gate-a-truth-basis](./gate-a-truth-basis-2026-07-14.md) ·
[acquisition-ladder-post-mortem](../audits/acquisition-ladder-post-mortem-2026-07-14.md).

## Standing state
- Master at #335. **Lock OFF** (Unit A disarmed it). **Paid grounding CEILING-BLOCKED** (`HTTP 400 You have
  reached…` = the $75 Anthropic usage cap). No further paid calls succeed this session.
- Merged this dispatch: **#330** holdings audit (930 rows), **#331** RD-33 retro + protocols, **#332**
  fetch-align-diff engine, **#333** acquisition discovery rung (one-home + split-wake + furniture gate +
  flow-golden + RD-34/RD-35 doctrines), **#334** Unit-2 re-diagnosis + GATE A, **#335** session-log.

## GATE-A ruling (operator, all 3 authorized)
1. Fetch plan (4 re-points → 4 open-web → 2 diff-engine re-collections; eu_clean_trucking→CELEX + mint Krone
   T-456/24 to timeline). 2. Run the 21 reattribution/slot as a $0-fetch unit. 3. BUILD g14 non-EN
   extraction + RECONCILE runSeekMore (no-shadow). Then GATE B.

## Unit A — RAN (the 21's 12 fresh; 9 already-run held under one-paid-pass). Actuals **$4.12**.
- ✅ **1 verified: EU Weights & Dimensions Directive** (40c05a1e; 41/5-below → 35/0-below). **Proof floor-first
  re-attribution works** (reattributeToFloor wired at canonical-pipeline.ts:1440).
- ⚠️ **REGRESSION: Brazil Lei 12.305/2010** (6a857887): **55 FACTs → 2 GAP** (ledger destroyed). Portuguese
  source → non-EN extraction re-extracted nothing AND the pipeline overwrote the good ledger. **Needs
  restoration via re-ground (parked: ceiling + extraction fix).**
- ⛔ **API-ceiling cut (3, $0):** ny-truck, US-HD-GHG, UK-RTFO — grounding never completed.
- held portal-guard (5, $0): IEA×2, EPA, C376, nashville (runner hard-divergence over-broad for the stored
  resynth path — source_url never fetched on a stored re-ground).
- held genuine sub-floor (2): IMO MEPC 338 (16→10 below, partial), UK TDP (17 below).

## TWO BLOCKING FINDINGS
1. **Anthropic ceiling reached** → the entire fetch plan + g14 re-ground + Brazil restore + IEA re-grounds
   are paid and BLOCKED. The authorized paid plan cannot run this session.
2. **Non-EN re-ground is destructive** (Brazil = proof). Two defects: (a) grounder doesn't extract
   original-language spans for non-English sources (g14/Brazil class); (b) a re-ground producing a WEAKER
   ledger OVERWRITES the prior good one (destroys any regressing item, not just non-EN).

## PENDING — $0 track (ceiling-independent), awaiting operator go
- **Class-fix (the important one):** non-EN original-language span extraction (system-prompt.ts addition) +
  a **non-destructive-replace guard** (a regressing re-ground must not wipe a better prior ledger). Prevents
  recurrence of the Brazil harm.
- **runSeekMore reconcile** (no-shadow, one home) + **portal-guard resynth fix**.
- **Re-point wrong_url source_urls** (eu_clean_trucking→CELEX derived) + **Krone T-456/24 to item_timelines**
  — durable DATA, no grounding, so a future ground uses the right URL.

## PARKED ON CEILING (paid, resume when the cap lifts)
- Fetch plan (10 items). g14 verify re-ground. Brazil restore re-ground. IEA/portal re-grounds. The 9
  already-run-this-session items (one-paid-pass — retry only if operator wants).

## OWED AT GATE B
T9 cert (last gate = a machine-gated run; 1 verified this run), actuals + cost breakdown, coverage-universe
reconciliation + instrument-gap table (from [coverage-universe-input](../../fsi-app/scripts/tmp/coverage-universe-input.txt)),
final quarantine distribution, stale_verified proposal (45 captures), reattribution-verified-half sizing (42
leaked spans on VERIFIED briefs — stays parked, this ruling did NOT authorize mutating verified briefs),
board + /done + commit.
