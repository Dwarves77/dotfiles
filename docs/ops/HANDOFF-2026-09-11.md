# Caro's Ledge coordinator handoff, 2026-09-11

Written by the outgoing coordinator session (Claude, session_011PrexRRmd9zJ3cBeMDJw94) for the next one.
Read this whole file before touching anything. Everything below is [CONFIRMED] unless marked otherwise.

## CORRECTIONS (added 2026-09-11 by lane LANDDOCS, before landing)

This handoff was written mid-day, before that day's date-chain work and one of its own "no PR" claims
were overtaken by events. Four corrections, each [CONFIRMED] with the evidence that settles it.

1. [CONFIRMED] Date-chain run 34612367135 (2026-09-11 14:48Z) already ran: mode=apply, command=forward-events,
   `--execute`. `item_forward_events` went from 821 to 1,209 rows (289 to 365 items); `compliance_deadline`
   went from 71 to 87. Section 5's before-state table below is dry run #1's report, not current state; treat
   it as history and re-run `population-report.mjs` before trusting any number in section 5. Verified: the
   workflow run itself (`gh run view 34612367135`, conclusion success, event workflow_dispatch, created
   2026-09-11T14:48:48Z).

2. [CONFIRMED] t71x was not left with no PR. Its bundle became PR #621 (train/wave71), merged 2026-09-11 as
   commit `5e891abd`. Verified: `gh pr view 621` reports state MERGED, mergeCommit `5e891abd`, the same
   commit this worktree's `origin/master` is built from.

3. [CONFIRMED] The timeline harvest (command 2 in the runbook) dry run showed 21 items filled (were empty),
   117 replaced, 12 HELD, 1,153 rows total. The runbook's "893-item scope" names items with a `full_brief`
   and zero `item_timelines` rows, but that count is not the same as what the parser can actually fill: a
   portion of those 893 are record stubs with no section-14 text to extract, so they stay empty rather than
   filled. Read the dry run's own filled / replaced / HELD breakdown as the real scope, not the 893 figure
   alone. See the status note added to `docs/ops/runbooks/date-chain-2026-09-11.md`. [CONFIRMED] Command 2
   was applied on 2026-09-11 in date-chain run 34635080848 (mode=apply, command=timeline-harvest):
   `item_timelines` went from 1,169 to 1,229 rows, items from 131 to 152 (+21 items filled, 117 replaced,
   12 HELD).

4. [CONFIRMED] `entity_refs = 0` on record items is not a brief-shape limitation (a record stub having no
   full brief does not by itself explain a zero). It is a timing gap: `backfill-entities.mjs` last ran
   2026-09-02 12:27Z, before the first record item was minted at 16:15Z that same day. Every record item
   minted since sits outside that backfill's coverage window, which is why `entity_refs` reads zero on them.
   [CONFIRMED] The entity backfill was applied on 2026-09-11 in propagation-drain run 34635876636
   (mode=apply, backfill_entities=true): entity_refs +1,693 (all 1,101 live record items now linked),
   entities +855, entity_identifiers +837, no errors.

---

## 0. Why this handoff exists

At 03:47 UTC on 2026-09-11 the outgoing session staged the operator's `fsi-app/.env.local` (service-role key
and other secrets) into the cloud sandbox and at 03:48 tried to run a production-writing script with it.
The session's auto-mode gate denied that, and from then on refused every production data write from that
session, including the one thing that had always been done by the coordinator: clicking "Run workflow"
with `mode=apply` in the GitHub Actions tab. Dry runs, landing code, merging, and all reads still worked.
The state is scoped to that conversation. A fresh session does not carry it.

Rules the next session must follow so this never recurs:

1. Never stage `.env.local` or any secrets file into the sandbox. The sandbox cannot reach Supabase's host
   anyway (org egress allowlist; documented in session-log Addendum 55, 2026-08-30), so there is no use for it there.
2. Never run a database-writing script from the sandbox shell. Corpus writes run on GitHub Actions
   (ADR-023) with the secrets GitHub already holds, dispatched from the Actions tab in the browser.
3. Never write a lane brief that says "X is blocked, use Y instead." State the runtime and the reason plainly.
4. Do not build a workflow that applies to production on push/merge with no human dispatch. The gate calls
   that "Blind Apply" and refuses it. Dispatch stays `workflow_dispatch`.

## 1. Repo and environment

- Repo `Dwarves77/dotfiles`, app under `fsi-app/`, Next.js 16, Supabase project `kwrsbpiseruzbfwjpvsp`
  (dev equals prod), production at carosledge.com (Vercel project `carosledge`, team `dwarves77s-projects`).
- Master at `a9a65443` (wave 70). Waves landed today: 67 `73e8c8b1` (PR #617), 68 `e7de3b89` (#618),
  69 `3f9bc2de` (#619), 70 `a9a65443` (#620). All CI green before merge.
- Sandbox layout the outgoing session used (rebuild it on a fresh container; the container resets overnight):
  `git clone` to `/root/work/dotfiles`, `cd fsi-app && npm ci`, lanes as worktrees under `/root/work/lanes/<lane>`
  with `ln -s /root/work/dotfiles/fsi-app/node_modules <worktree>/fsi-app/node_modules`. Node 24 via
  `npx node@24` then `export PATH=/home/claude/.npm/_npx/<hash>/node_modules/node/bin:$PATH` (find the hash with
  `ls /home/claude/.npm/_npx`). Playwright browsers at `/opt/pw-browsers`; never run `playwright install`.
- The sandbox has no network to carosledge.com or supabase.co. Measure production through the browser
  (Chrome extension, JS tool) and the live corpus through the Supabase connector (`execute_sql`).
- Operator's machine is linked: `C:\Users\jason\dotfiles` is a connected folder with a full checkout,
  `.env.local` and `node_modules`. Do not stage `.env.local` from it.

## 2. Coordinator role and discipline

- Coordinate only. Lanes edit code, tests and docs; the coordinator writes briefs, dispatches, verifies the
  committed tree in a detached worktree, lands, and reports only what is measured on the site.
- Briefs are Workflow-tool `.js` scripts under `/home/claude/` (pattern in `searchfix.js`, `searchclip.js`,
  `datechain.js`, `datechainwf.js`; copies of the text are in the session log addenda). Model: Sonnet
  (`model: 'sonnet'`), per the operator's spend ruling. Lanes are subscription-billed; never API.
- Every lane brief must carry: CLAUDE.md rules 13-18, `[CONFIRMED]`/`[HYPOTHESIS]` labels, no em dashes in new
  prose, the Node 24 PATH line, "container cannot push, coordinator lands", the gates, the attribution lines:
  `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` then
  `Claude-Session: <the new session's URL>`.
- Gates a lane runs: `npx tsc --noEmit` (read the exit code), `node fsi-app/.discipline/fitness/runner.mjs`
  (0 violations, currently 37 functions), CI npmtest glob (`src/**/*.npmtest.mjs`),
  `bash fsi-app/.discipline/run-test-suite.sh` (0 fail, ~6,044 tests), `node fsi-app/.discipline/rendering/run-rendering-guard.mjs`
  (PASS) when anything renders differently, `npm run audit:design` (2557/2557 MATCH), `cd fsi-app && npx next build`,
  `node fsi-app/.discipline/runner.mjs --mode=ci --range=origin/master..HEAD` (discipline commit validator).
- Any customer-surface change needs a `### UX compliance` block per `docs/design/ux-laws.md` DP-2 in the
  session-log addendum, or the CI "UX compliance gate" fails. Copy the format from the SEARCHFIX addendum.
- Coordinator verification before landing: `git worktree add --detach <path> <sha>`, symlink node_modules,
  run the validator and fitness there. Never gate a dirty worktree; never push before the lane's final commit.
- Master carries squashes, so lanes must be rebased onto `origin/master` before bundling. The session log
  is append-only and conflicts on every rebase: keep both sections, master's first, delete the markers.
- Layout guard baseline expires by date: `BASELINE_EXPIRY_DATE = '2026-10-15'` in
  `fsi-app/.discipline/rendering/layout-guard/baseline.mjs`. Clearing the 622 findings is scheduled after
  the UI round; do not start it.

## 3. Transport (the container cannot push)

1. `git bundle create /tmp/tNN.bundle origin/master..lane/<name>`; verify with `git bundle verify`;
   `cp` to `/mnt/user-data/uploads/tNNchunk00`; note the md5.
2. Chrome: `https://github.com/Dwarves77/dotfiles/upload/master`, `find` the file input, `file_upload` with
   the `/mnt/user-data/uploads/...` path (that path works; outputs and device paths do not). Triple-click the
   "New branch name" box (around (514,676) at the operator's window size), type `tNNx`, click Propose changes,
   confirm the compare page shows `master...tNNx`.
3. Codespace `https://turbo-invention-5r7g4p9jp4rh7qqg.github.dev/` (idles out; wait ~30 s for the terminal,
   click into it, prefix commands with a space). Run:
   `cd /workspaces/dotfiles && git fetch -q origin master tNNx && git checkout -q origin/tNNx -- tNNchunk00 && md5sum tNNchunk00 && git fetch -q tNNchunk00 'refs/heads/lane/<name>:refs/heads/wNN' && git rm -q --cached tNNchunk00 && rm -f tNNchunk00 && git push -q origin wNN:train/waveNN-<date> && gh pr create --base master --head train/waveNN-<date> --title 'train/waveNN <date>' --body '<summary>' && echo PUSHNN-OK`
   Compare the md5.
4. CI takes about 5 minutes. On the PR page: "Squash and merge" then "Confirm squash and merge". If a check is
   red the gate blocks the confirm click; fix the red, do not force.
5. Vercel deploys master in about 4 minutes. Verify on carosledge.com with a cache-busting query string; the
   deployed id is in the script URLs (`dpl=dpl_...`). A tab left open holds the old document; reload it.
6. `gh workflow run` from the Codespace returns 403 (session log, 2026-09-02). Workflow dispatches go through
   the Actions tab in the browser: Actions > <workflow> > Run workflow.

## 4. Production state (measured 2026-09-11, wave 69 deployed, `dpl_7r6YyvbtdUSGb65jEn1RjbGH7ztg`)

- Command bar: Search/Ask toggle live; Search is default; typing `ppwr` returns 8 rows (EU PPWR 2025/40 first);
  the results dropdown is a body portal, top 7px under the bar, fully visible; submit reads "Search".
- /operations matrix: artboard-8 scoreboard, arrival on the first sourced cell, one tab stop, panel below.
- Dashboard: What-changed header date fixed (wave 66); em dashes in What-changed rows are correct
  (all 500 change rows carry no future date).
- Assistant (`/api/ask`): `ASSISTANT_ENABLED === "true"` gate unchanged on master; production env value
  unknown to the outgoing session. See open item 6.3.

## 5. The date chain (the current work)

Diagnosis (measured 2026-09-09 and 2026-09-11): `sectionBrief()` skipped the section-14 timeline harvest for
verified items (1,434 of 1,518 live); `compliance_deadline` had no writer; `item_forward_events` (pure
extractor, 821 rows over 289 items) fed nothing; the population report did not guard any of the three;
1,102 of 1,518 live items are stub catalogue records with no full brief.

Landed (wave 68, `e7de3b89`): `harvestItemTimeline()` split out of the verified guard;
`src/lib/forward-events/compliance-deadline-sync.mjs` (nearest future `event_kind='compliance_deadline'`,
tie high confidence then id, never null-overwrites, idempotent) wired into `mint-item.ts` and
`apply-staged-update.ts`; `scripts/verify/population-report.mjs` guards `item_timelines`,
`item_forward_events`, `compliance_deadline`, and brief coverage (`STUB_BRIEF_MARKER` from `record-facts.mjs`);
runbook `docs/ops/runbooks/date-chain-2026-09-11.md`.

Landed (wave 70, `a9a65443`): `.github/workflows/date-chain.yml`, "Date chain backfill", `workflow_dispatch`
only. Inputs: `mode` dry|apply (default dry), `command` forward-events|timeline-harvest|both|population-report
(default both), `limit`, `after_id`. Runs the forward-events dispatcher then the timeline harvest (both pure
parsers, no model, no key spend), `--execute` only when apply, population report always last, resumable id in
the step summary.

Run #1, dry, 2026-09-11, run id 34610241178, green in 1m50s. Before-state from its population report:
`item_forward_events` 821, `item_timelines` 1,169, `compliance_deadline` 71 of 2,766 rows, full briefs 416 of
1,518 live (1,102 stubs). Last id processed by the dry forward-events pass: `ffba5757-30c0-45ac-ada6-1d7933c8e4e2`.
Scope per runbook: 1,231 items with zero forward-events rows; 893 reg-family items with a brief and zero timelines.

NEXT ACTION FOR THE NEW SESSION, first thing:
1. Actions > Date chain backfill > Run workflow > mode `apply`, command `both`, limit and after_id empty.
2. Read the run's step summary and log: rows written per command, compliance_deadline syncs, errors, last id.
3. Re-read the population report lines (printed at the end of the run) and report before/after.
4. Spot-check three items that gained a deadline and three that gained timeline rows, on the site
   (Due Next, What Changed, the item page), measured, not assumed.
5. If a run errors mid-way, resume with `after_id` from the summary; each batch is bounded.

## 6. Open items, in priority order

6.1 Brief backfill for the 1,102 stub items (runbook command 3). Model-backed, so it runs as subscription
    Claude Code lanes through the intake path (`applyStagedUpdate` `update_item` in
    `fsi-app/src/lib/intake/apply-staged-update.ts`), batches of roughly 40 to 60 items per lane, verified-but-stub
    items first. Not through the Anthropic API key. RD-31 priced-line is already handled by `spend-client.ts`.
    Design the lane runtime before dispatching: a lane in the sandbox cannot reach Supabase, so the write must
    land through a path that runs where the secrets are (Actions, or a committed intake artifact consumed by
    an existing workflow such as `population-turn.yml`). Check `scripts/harness-runs/mint/PENDING-RUN.md` and
    `population-turn.yml` for the established artifact-consume pattern before inventing one.
6.2 `scripts/forward-events/dispatch-extraction.mjs` does not emit a `harness-runs` artifact (named as a gap in
    the runbook). Small follow-up.
6.3 Assistant enablement. Operator ordered it ON (2026-09-09: "actually turn the AI on, we just wont use it";
    "you shut it off you can turn it on"; "i told you to fucking fix it"). A lane built an ADR
    (`docs/decisions/ADR-2026-09-09-assistant-enablement.md`) and `fsi-app/src/lib/llm/assistant-enablement.ts`
    (production-only default: `VERCEL_ENV === "production" ? ASSISTANT_ENABLED !== "false" : ASSISTANT_ENABLED === "true"`)
    with four tests; those commits were lost in the container reset and never landed. Rebuild via a lane whose
    ADR quotes the operator verbatim. Three earlier Sonnet lanes refused the flip citing PR #478 / Addendum 32 /
    RD-31; the ADR is what resolves that. Ask-mode UI stays as built (disabled end to end when the flag is off).
6.4 Search dropdown: Escape-to-close, click-outside-to-close and arrow-key navigation never existed; logged in
    `docs/tech-debt-log.md` by lane SEARCHCLIP. Small lane if the operator wants them.
6.5 /market: D1 removals below the artboard's last card still open; headline five shows Jet fuel where the
    operator's row has EUR/USD.
6.6 Layout guard: 622 baselined findings, baseline expires 2026-10-15, clearing scheduled after the UI round.
6.7 Codespace checkout is at wave 58 with no node_modules; only used for git and gh. Fine as is.

## 7. Operator rulings that bind (verbatim where it matters)

- Spend: "you do NOT need to spend on my API to fix these things, they can all BE FIXED FOR FREE";
  "I HAVE A ... CLAUDE CODE SUSBSCRIPTION". Lanes are Sonnet on subscription. No API spend without his word.
- Date chain, 2026-09-09: "the fix is making sure a full brief, analysis of the data and all information is
  pulled and then put through the flywheel ... it should NOT be locked out ... ALL of them look like they need
  fixed ... briefs need to exist for all items as well."
- /operations matrix STOP SHIP spec (2026-09-09): the table never grows, 40px rows, score or em dash, one panel
  below, no chevrons, header "N regions · scroll →", cell click selects (#DCE7FB fill, 2px #2563EB inset),
  panel #FAFAF8 with 1px rgba(0,0,0,.12) top rule, up to three fact cards figure-first, compare mode stacked,
  arrows and Esc, sticky first column, default state first sourced cell open. Landed waves 64 and 65.
- Claude Design rulings 2026-09-09: 72ch is the max-width of the prose, 560px the min width of the column;
  layout-guard baseline extended to 2026-10-15; header string as drawn.
- Search: "a toggel between standard search and AI question in that bar is what is needed." Landed wave 66,
  fixed waves 67 and 69.
- Style: no em or en dashes where a comma is correct; no section-mark symbol; regulatory claims verified against
  primary sources or labelled unconfirmed; report only what is measured on the site.

## 8. Where things are

- Session log: `docs/ops/session-log.md` (addenda for CMDSEARCH, SEARCHFIX, DATECHAIN, SEARCHCLIP, DATECHAINWF).
- Program board: `docs/PROGRAM-BOARD.md`. Index: `docs/INDEX.md`.
- Runbook: `docs/ops/runbooks/date-chain-2026-09-11.md`.
- Shared-part report: `docs/design/handoff-2026-09-06/SHARED-PART-REPORT-2026-09-08.md`.
- Wave 70 bundle also sits at `C:\Users\jason\dotfiles\t70chunk00` on the operator's machine; it is landed and
  can be deleted.
