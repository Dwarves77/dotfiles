## 2026-09-19, lane N3: pending directory replaces the hash-pinned PENDING-RUN.md marker (plan 6.8, Rule B)

Lane N3 (Sonnet), worktree `wt-n3`, branch `lane/n3-pending-directory-2026-09-19`, base
`dfef72cd` (carries lanes N0, T2, N2).

**Result [CONFIRMED by running the commands below].** `F28-harness-run-integrity.mjs` rules (b)/(c) no
longer read a hash pinned inside `PENDING-RUN.md`. A family that owes a run now declares it as one file
it owns, `scripts/harness-runs/<family>/pending/<YYYY-MM-DD>-<lane>.md` (a `## Change` line and a
`## Planned run` line, no hash anywhere). Two new rules replace the old CENSUS/STALENESS-COUPLING pair:
a RANGE rule (a governing file changed in the range with no new artifact added must also add a pending/
file in that same range, skipped rather than failed when no git range resolves) and a TREE-STATE rule
(always runs: no artifact at the live hash needs a pending file; an artifact AT the live hash must have
none left, the reverse audit). All 13 `PENDING-RUN.md` markers on master today (`fsi-app/scripts/
harness-runs/*/PENDING-RUN.md`, brief-common-cloud named 11, T2's fetch-drain/propagation markers on
2026-09-19 brought it to 13) are migrated to `pending/2026-09-19-n3-migrated.md` and the old marker files
deleted. `node fsi-app/.discipline/fitness/runner.mjs --function=F28` PASSes on the migrated tree.

**Findings.** [CONFIRMED] `fsi-app/scripts/verify/verification-audit-report.mjs` (and its test) is a
real reader of `PENDING-RUN.md` (`pendingMarker: fileExists(join(dir, "PENDING-RUN.md"))`) that the
brief did not name among "other readers"; it is outside this lane's write set and now reports every
family's `pendingMarker` as `no` even where a `pending/` file exists. Not gated by any CI job (a
standalone report generator, not invoked by any fitness function or workflow). Flagged for a coordinator
follow-up, not fixed here (write-set discipline). [CONFIRMED] `grep -rln "harness_version at write
time" fsi-app/scripts/harness-runs/` still returns 5 pre-existing files
(`forward-events/LAST-PROPOSER-PASS.md`, `change-detection/LAST-PROPOSER-PASS.md`,
`fetch-drain/fetch-drain-run-003.json`, `meta-harness/meta-harness-run-008.json`,
`mint/LAST-PROPOSER-PASS.md`) quoting the phrase as historical record of past re-pins; these are dated
evidence outside this lane's write set (CLAUDE.md rule 5, machine evidence is not rewritten) and are the
reason the acceptance grep does not read literally empty. The 13 `PENDING-RUN.md` marker files
themselves are gone.

**Next.** Coordinator dispatch to update `verification-audit-report.mjs`'s `collectHarnessMarkers` to
read `pending/` files instead of a single `PENDING-RUN.md`. `repin.mjs` was confirmed absent from this
repo (lives only on the operator's machine per the brief); no re-pin helper found under
`fsi-app/scripts/coordinator/`.

### UX compliance (N3)

Not a UI change; no customer surface touched by this branch.
