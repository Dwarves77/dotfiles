## 2026-09-21, lane M6b: gate-a-rescan hop built (items 2 through 8); item 6 (loop-hops.d/) still awaits lane R7m

Replacement agent, continuing commit `007e2f86` (the first M6b agent's item 1: `scripts/lib/
gate-a-state-writer.mjs`, `provenance-heal.mjs` delegating to it). Skills loaded per the brief's own
standing line: `fsi-app:environmental-policy-and-innovation`, then `remediation-discipline`.

What landed, in the amendment's own order:

- **Items 2+3**: `scripts/maintenance/gate-a-rescan.mjs` (selection over non-archived, non-empty-brief
  `intelligence_items` whose `item_gate_a_state` row is missing or stale against `GATE_A_VERSION`;
  `resolveLimit` bounding a chained `workflow_run` firing to 50 always, a dispatch to 500 default /
  2,000 cap, refused not clamped above it; fresh `derivedCoveredTokens` per item, per-step readback
  verification, the `provenance_status` touch only when a scan actually changed), its tests (17 pass),
  the emitter `scripts/turns/emit-gate-a-rescan-artifact.mjs` (models `emit-brief-export-artifact.mjs`,
  reuses `resolveHarnessRunContext`/`buildRunArtifactEnvelope`, no copy of either), its tests (6 pass),
  and the `gate-a-rescan` harness family descriptor (`family.json`, `FAMILY.md`, pending files for both
  `gate-a-rescan` and `meta-harness` per F28).
- **Item 4**: `attach-found-sources.mjs` gains `filterWorklistRows` (pure) and `--items-file`/`--limit`
  CLI flags, applied to the read worklist BEFORE `partitionWorklist` -- one touch of that file, per the
  brief; `heal-provenance.mjs`'s `main()` call signature is unchanged (the scoping happens upstream of
  the `ids:` selector it already receives, so the brief's own STOP condition on that file never fired).
  Tests extended (23 pass).
- **Items 5+7**: `.github/workflows/gate-a-rescan.yml` -- chained off "Brief apply" or "Population turn"
  completing (both confirmed as the real `name:` lines on this base), plus `workflow_dispatch`
  (mode/limit/loop_run_id). Runs `gate-a-rescan.mjs`, then, only when the rescan left orphaned items,
  `attach-found-sources.mjs` scoped to exactly those ids over each committed ready worklist under
  `scripts/_worklists/attach-found-sources-*.json` (`.seed.json`/`.fixture.json` excluded); `dry` mode
  only on a chained attach firing, the dispatch's own `mode` input otherwise. One artifact both steps,
  via the new emitter, `if: always()`. F52-clean (checked directly against the file: 0 violations). Its
  own wiring test, `scripts/maintenance/gate-a-rescan-workflow.test.mjs` (10 pass), plus the
  `loop-run-id.test.mjs` attack-chain extension for this hop (Brief apply resolves, Population turn
  resolves, an unknown upstream resolves null, explicit wins -- 4 new tests, 18 total pass).
- **Item 8**: F25 (module-liveness) and F27 (producer-seam-proof) run clean against the new files
  (checked directly; also confirmed via the full fitness runner below). F30 (entity-spine) does not
  apply -- no host is derived by any file this lane touched.
- **Item 6**: `fsi-app/.discipline/governance/loop-hops.d/` does not exist on this base (confirmed:
  `test -d` returns missing) -- lane R7m has not merged. Per the brief's own instruction, `
  loop-manifest.mjs` was NOT touched. Both `brief-apply-to-gate-a-rescan` and
  `population-turn-to-gate-a-rescan` hops still read `consumerPending: true`, `family: null` in the
  manifest, unchanged by this lane -- the hop files this item would edit do not exist yet for it to edit.

### Owed

- **R7m** (or its successor): once `loop-hops.d/` lands, the two hop files
  (`brief-apply-to-gate-a-rescan`, `population-turn-to-gate-a-rescan`) still need `family: "gate-a-rescan"`,
  `enforceEdge: true`, `consumerPending` removed/false.
- **Coordinator**: the proof run (build plan section 6.2) -- a real "Brief apply" or "Population turn"
  firing chaining into `gate-a-rescan.yml`, landing `gate-a-rescan-run-001.json` with
  `trigger: "workflow_run"` and a resolved `loop_run_id`.
- Plan 6.3's `distinct(gate_a_version) = 1` CI data-audit verifier, and any real dispatch of this
  workflow, are explicitly NOT in this lane (brief's own "NOT in this lane" list).

Gates: `node --test` on every new/changed test file, individually, all green (see file list above for
per-file pass counts). `node .discipline/fitness/runner.mjs`: 46 function(s) checked, 0 violation(s)
(F28 lists `gate-a-rescan` among the families with a pending file and no artifact at the live hash yet,
same posture every other freshly-registered family shows; F50 reports "hops not yet enforced: 11",
unchanged by this lane since item 6 could not run). Push gate result recorded in the coordinator report.

UX compliance: not a UI change (no `.tsx`/`.css` touched).
