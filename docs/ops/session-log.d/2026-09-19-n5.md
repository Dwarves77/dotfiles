## 2026-09-19, lane N5: invariant registry, migrations inventory and shared-writer allowlist become derived (plan 6.8, Rule A)

Sonnet lane, worktree `wt-n5`, branch `lane/n5-derived-registries-2026-09-19`, base sha `41424140`
(carries N0, T2, N2, N1). Rebased once onto `origin/master` after it also carried N3 (07ee7f0c); this
entry covers both the original brief and the coordinator's Amendment 1 (2026-09-19 18:29 UTC), which
followed this lane's own STOP report on the first rebase attempt.

**Result [CONFIRMED by node --test, node fsi-app/.discipline/fitness/runner.mjs, node fsi-app/.discipline/governance/invariant-coverage.mjs, node fsi-app/.discipline/consistency/runner.mjs, npx tsc --noEmit, all run this session]:**
All three of the brief's targets landed, including the migrations inventory (originally a STOP; the
coordinator corrected the premise in Amendment 1 rather than waiving it).

1. **Invariant registry.** `fsi-app/.discipline/governance/invariants.mjs`'s 1,342-line `INVARIANTS`
   array literal is now one file per invariant under `invariants.d/<ID>.mjs` (132 files), loaded by a
   directory-scan-plus-natural-sort function `invariants.mjs` itself exports (`loadInvariantsFromDir`,
   `compareInvariantIds`). `invariants.d/README.md` states the one-entry-one-file rule.
   `invariants.test.mjs` (new) proves live load, duplicate-id refusal, filename-mismatch refusal, and the
   natural sort, against throwaway fixture directories, never the real one. `grep -c "id: 'RD-\|id: 'EP-\|id: 'SC-" invariants.mjs` = 0.
   `invariant-coverage.mjs` tallies unchanged (132 invariants, 119 enforced / 13 exempt; 63 doctrines, 44
   enforced / 19 exempt) before and after.

   **Rebase resolution (Amendment 1 item 1).** `origin/master` picked up lane N3 between this lane's
   first and second rebase attempts; N3 rewrote the `RD-55-harness-run-integrity` entry's `text`/
   `residual` fields (id/skill/section/anchor/enforcedBy unchanged) inside the same array-literal
   `invariants.mjs` this lane's own commit had already replaced with the directory loader, a genuine
   content conflict on that one entry. Resolved per the coordinator's instruction: took master's side of
   the whole file (`git checkout --ours`), re-ran this lane's split script against it (regenerating all
   132 `invariants.d/*.mjs` files fresh, so `RD-55-harness-run-integrity.mjs` carries N3's text verbatim),
   then restored this lane's own loader content into `invariants.mjs`. Re-verified: all 132 derived
   invariants deep-equal `origin/master`'s post-N3 array (order by id; master's order was file-literal
   order), and `RD-55-harness-run-integrity.mjs`'s `text` field contains N3's own added phrase
   ("re-derived from the live tree and git range at check time") confirming it is N3's text, not the
   pre-rebase text. The five `PENDING-RUN.md` modify/delete conflicts (below) were `git rm`'d per the
   same instruction: N3 deleted the whole hash-pinned-marker mechanism (plan 6.8 Rule B), so this lane's
   own re-stamps of those five files were moot on the far side of the conflict.

   **Necessary out-of-write-set fix, disclosed (same class as lane N1's own Source 9 precedent).**
   F25-module-liveness.mjs's static import-graph walk cannot see `invariants.mjs`'s computed
   `import(new URL(file, dirUrl).href)` calls, so all 132 new files immediately red-flagged as UNWIRED
   MODULE. Added Source 10 to `findDispatchRoots` (mirrors Source 9's own fitness/functions/ handling
   exactly): every non-README file directly under `invariants.d/` is a root. Two new fixture tests in
   `F25-module-liveness.test.mjs`, proven RED (without the fix) then GREEN (with it). Fitness runner:
   132 F25 violations to 0.

2. **Shared-writer allowlist.** `fsi-app/docs/inventories/shared-dataset-ownership.md`'s fenced json
   block is replaced by prose; `.discipline/shared-writer-registry.test.mjs` now derives the allowlist
   from a `// SHARED-WRITER: <table>[, <table>...]` header line each of the 90 real writer files carries
   (added by a one-shot scratch script), and asserts BOTH directions (every detected write declared,
   every declaration a detected write) instead of the old one-directional check. Already matched by
   `run-test-suite.sh`'s pre-existing `fsi-app/.discipline/*.test.mjs` glob; nothing to add there (checked
   first, per the brief's own "facts newer than the brief" note).

   **Findings, corrected in place (rule 14).** Deriving the allowlist and adding the reverse check
   surfaced 15 real defects the old json-parsing, one-directional test structurally could not see.
   `scripts/_reground/restore-overclear.mjs` was DELETED by lane F25-WAVE52 (2026-09-07) but still named
   in the allowlist and in two "(KEEP)" table rows; both corrected. Nine table associations were stale or
   wrong (each file's own header text or code says it does not write the table the allowlist claimed):
   `apply-mint-batch.mjs`/`section_claim_provenance`, `canonical-key-dedup.mjs`/`census_worklist`,
   `ratify-flag-to-census.mjs`/`integrity_flags` (reads it, writes only `census_worklist`),
   `apply-staged-update.ts`/`item_forward_events` (delegates to `flywheel-steps.mjs`, already
   registered), and four files that write NO shared table at all and had their marker removed entirely:
   `run-mint-batch.mjs` ("it never writes to Supabase"), `run-extraction.mjs` ("never a database"),
   `theme-brief.mjs` ("READ-ONLY... never writes"), `null-tier-host-worklist.mjs` (a pure helper),
   `discover-for-items.mjs` and `apply-record-briefs.mjs` (callers of an already-registered writer).
   Four real, previously-undeclared writers were found and registered:
   `scripts/maintenance/backfill-format-type.mjs`, `scripts/mint/rederive-record-provenance.mjs`,
   `scripts/mint/screen-reconcile-records.mjs` (all `intelligence_items`), and
   `scripts/maintenance/close-run-logs.mjs` (`integrity_flags`) -- invisible to the OLD test too, because
   its `GUARDED_RE` matched `guardedUpdate(` but not the `guardedUpdateByIds(` suffix (widened; the doc's
   own `origin-class-backfill.mjs` row had already named this exact gap for itself). 123 of 134 original
   (table, file) pairs are unchanged.

3. **Pending files under N3's mechanism (Amendment 1 item 2).** Adding SHARED-WRITER headers touches a
   governing file (per each family's own `family.json`) of six harness families in this lane's diff
   (`git diff --name-only origin/master...HEAD`), computed from the descriptors, not assumed: fetch-drain
   (`capture-worker/index.ts`), source-sweep (`run-source-sweep.mjs`), change-detection (`reconcile.ts`,
   `run-intake-cycle.ts`), corpus-turn (`consume-turn-requests.mjs`), brief-apply (`canonical-pipeline.ts`,
   `flywheel-steps.mjs`, `io-preflight.mjs`), and **inaccessible-triage** (`seek-more.mjs`).

   **Correction to this lane's own earlier claim (rule 14).** This lane's first report called
   inaccessible-triage's F28 "NO ARTIFACTS" finding pre-existing and out of scope, citing `git status`
   showing the family's own directory untouched. That was true of the directory but incomplete: the
   family's *governing file* `src/lib/sources/seek-more.mjs` (elsewhere in the tree) IS in this lane's
   diff (it kept its SHARED-WRITER marker from step 2). The finding was correctly pre-existing (zero run
   artifacts since lane M8 registered the family 2026-09-18) but this lane's own change is a second,
   independent reason the family owes an acknowledgment, not merely an untouched bystander. Added
   `scripts/harness-runs/<family>/pending/2026-09-19-n5.md` for all SIX families (not five), each with a
   `## Change` line naming the file(s) and marker(s) added and a `## Planned run` line naming the next
   run id; no hash anywhere, per `CONVENTION.md`'s "Declaring a pending run" section as `origin/master`
   now has it. `F28-harness-run-integrity.test.mjs`'s "F28 passes GREEN against the live tree" case: RED
   (6 PENDING FILE REQUIRED / range-rule violations) before these files were committed, GREEN after.

4. **Migrations inventory (Amendment 1 item 3; built, not a STOP any more).** This lane's own premise
   refutation stands as measured (about 200 of 296 subjects are hand-authored, not a function of the
   header), and the coordinator corrected the plan rather than the finding: the subject moves INTO each
   migration file once, by a scratch script, as a new first line `-- subject: <the Subject cell for that
   file, byte for byte>` (after a leading `/* fitness-allow */` line where one exists, migration 001 is
   the only one). Comment lines only; the applied schema is UNCHANGED (Supabase CLI tracks migrations by
   filename/version, never content). New `fsi-app/scripts/inventories/generate-migrations-inventory.mjs`
   derives `docs/inventories/migrations.md`'s "## Migrations" table from those lines plus each file's own
   name (rows sorted by number then filename; a generated header stating the command and "never edited
   by a lane"); everything after the table ("## Maintenance trigger", "## Records-truth corrections",
   "## Source files", "## Related") is preserved byte-for-byte from whatever is currently committed, the
   generator's own job being the table alone. `generate-migrations-inventory.test.mjs` (16 fixture cases)
   and `C3-migrations-reality.test.mjs` (4 cases: live-tree GREEN, a malformed-subject-line fixture
   caught, a new-migration-with-no-row fixture caught, a hand-edited page caught) both green.

   **Extraction defects found and fixed before insertion (rule 13/14).** The doc's own table has two
   pre-existing markdown-escaping defects an escape-aware-but-naive parser would still get wrong: seven
   rows (e.g. migration 144's `('off'|'weekly'|'monthly')`) contain a LITERAL unescaped `|` inside the
   Subject cell (never escaped as `\|`), which a five-cell-assuming parser truncates; fixed by
   reconstructing the subject as every cell from index 3 to the line's own trailing empty cell, joined
   back with `|`, before re-escaping correctly for the regenerated table (the regenerated table properly
   escapes these seven cells, fixing a real rendering defect as a side effect). One filename
   (`321_claim_versions_record_briefs_supersede.sql`) has TWO rows in the doc, the second an explicit
   correction ("fix round 1, review finding C2, 2026-09-13") appended after the first without removing
   it; kept the later, corrected text, matching one row per file by construction.

   **C3 rewritten**, sharing its logic with the generator module rather than re-deriving it (`import`
   from `../../../scripts/inventories/generate-migrations-inventory.mjs`): (a) well-formedness, every
   migration file has a parseable `-- subject:` line, violation names the file; (b) parity, the committed
   page equals the generator's own derived output byte for byte, violation says to run `--write`.

   **Diff against the pre-N5 page, fully catalogued, nothing unexplained:** the new generated header
   (instructed); the stale duplicate-321 row removed; the seven pipe-escaping fixes above; a block of
   wave-alpha rows (190-252 range) resorted from historical append order into strict numeric order, per
   the "rows sorted by number" instruction (the file's own migration 001 comment already documents these
   as "intentional reserved-lane gaps from wave-alpha parallel-agent migration numbering"); and 152 rows
   whose Subject contains an em/en dash gained a trailing ` [glyph:verbatim]` disclosure tag in their
   table cell. That last class needed its own small mechanism: pre-commit rule 022 scans ADDED lines, and
   a full-file regeneration re-adds every row's bytes at whatever position sorting puts them, so a row
   whose content is byte-for-byte unchanged HISTORY still trips the rule on the line it lands on; editing
   the historical prose to remove the dash was not an option (it would break byte-for-byte parity with
   the doc, the entire point of this lane's change), so the generator's `renderSubjectCell` appends the
   tag to any cell whose subject matches the same glyph pattern rule 022 scans for, deterministically, and
   the migration files' own `-- subject:` lines carry the identical tag (stripped back off by
   `parseSubjectLine` before the text is used or compared anywhere) for the same reason on their own line.
   152 of 296 migration files needed the file-side tag; two more standalone rule-022 hits were fixed by
   rewording (this lane's own new prose, not verbatim history): `invariants.d/README.md`'s title/prose and
   `invariants.test.mjs`'s comments, and one collision in `src/lib/sources/source-type-taxonomy.test.mjs`
   (below).

   **Unrelated defect found and fixed (rule 13, root-cause-before-tests).** Inserting migration 288's
   subject line (which quotes that migration's own `` `source_type <@ ARRAY[...]` `` CHECK clause in
   prose) broke `source-type-taxonomy.test.mjs`'s drift guard: its regex
   `/source_type\s*<@\s*ARRAY\[([\s\S]*?)\]::TEXT\[\]/` is comment-blind, so it matched the FIRST
   `ARRAY[` in the file (now inside the new comment line) and ran non-greedy to the real `]::TEXT[]`
   anchor 59 lines later, capturing everything in between as bogus values. Root cause is the test's own
   fragile pattern, not the (correct, byte-for-byte) subject text; fixed by stripping `--`-comment lines
   from the SQL text before matching, since the real CHECK constraint is never inside a comment. RED
   (captured ~50 lines of prose as "values") before the fix, GREEN (16/16) after.

5. **Lane-common-contract bullet.** One bullet added: a lane never edits a file under `docs/audits/`; it
   records a finding's closure in its own session-log file, and the coordinator's close lane folds
   statuses into the audit (lane N6 enforces it).

6. **Amendment 2 (coordinator, 2026-09-19 19:10 UTC): the 152 tags removed, not kept.** The finding
   above (item 4's "possible follow-on") is [REFUTED as a rule-022 policy question, corrected in place
   per rule 14]: the coordinator ruled the tags are debt, not disclosure, because a migration's own
   `-- subject:` line is a DESCRIPTION this lane wrote into the file, not quoted third-party text, so
   rule 022's actual remedy (fix the prose) applies, not its verbatim-disclosure escape hatch. By a new
   scratch script (`n5-amendment2-degylph-subjects.mjs`), every one of the 152 tagged subject lines was
   rewritten: an em/en dash used as a numeric range (digit-dash-digit, no surrounding spaces, e.g.
   "136-157", 5 occurrences across 4 files) became a plain ASCII hyphen (meaning-preserving, and a plain
   hyphen is not a banned glyph); every other em/en dash (423 occurrences, always space-dash-space)
   became a comma; every section sign (64 occurrences) became the word "section"; the tag was dropped.
   Lines changed: 152. Tags removed: 152 (equal, as required). Page regenerated with `--write`; C3
   parity PASS. The page no longer equals the pre-N5 page byte for byte in these 152 rows (it did after
   Amendment 1, modulo the tags); the difference is exactly this glyph-to-plain-punctuation rewrite,
   nothing else. `docs/inventories/migrations.md` now carries zero `[glyph:verbatim]` tags.
   `generate-migrations-inventory.test.mjs`'s tag-adding test (`renderSubjectCell`) is unchanged and
   still green on its own fixtures; it simply never fires again on the live corpus because no live
   subject carries the glyph any more.

**Blockers / next steps.** None owed by this lane.

### UX compliance (N5)

Not a UI change; no customer surface touched by this branch.
