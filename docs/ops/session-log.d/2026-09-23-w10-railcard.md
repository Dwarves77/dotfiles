# Lane W10-RailCard session log (2026-09-23)

Brief: section 6 ("W10-RailCard (with StatBlock)") plus the COMMON BLOCK of
`docs/dispatches/lane-briefs/2026-09-22/brief-w10-remaining-parts.md`. Worktree:
`C:/Users/jason/dotfiles/.worktrees/wt-l25-slot-mirror`, branch `lane/w10-railcard`, cut from
`origin/master` at `c4b017be` (Lane W10-ListRow), rebased onto `b4feb6d6` (Lane W10-StateNote)
before the locked gate.

Skills loaded: `fsi-app:environmental-policy-and-innovation`, `frontend-design`,
`remediation-discipline`, `caros-ledge-platform-intent` (required by the governed-file write gate
before touching `src/app/admin/parts/rail-card/page.tsx`, `src/app/admin/parts/stat-block/page.tsx`).

Read: `docs/dispatches/lane-common-contract.md`, `docs/design/ux-laws.md`,
`docs/design/design-principles.md`, `docs/design/parts-brief-2026-09-18.md` sections 2.12 (RAIL
CARD) and 2.13 (STAT BLOCK). No new artboard image beyond the parts-brief's own text for these two
parts; the brief carries no separate artboard number for RailCard/StatBlock past the general
"artboard 21" bundle already read by the FactCard/ActionCard lanes.

## Finding: RailCard and StatBlock were both partially built, in the wrong place

`StatBlock` (`src/components/ui/StatBlock.tsx`) already existed as a real shared part (stack/row/
tile layouts, `tone="critical"`), just missing `data-part="stat-block"`. `RailCard` existed only as
a LOCAL component inside `src/components/list-surface/ListSurfaceRailCards.tsx`, scoped to the five
list surfaces, while the exact same shell (SectionCard + 14px/16px padding + 10.5px/800/.12em
uppercase muted header) was independently hand-retyped in three more places: `DetailShell.tsx`'s
`AtAGlanceCard`/`RailLegend`/`ImpactRailCard` (the detail rail "At a glance"/"Legend"/"Impact
assessment" cards) plus an "In this list" card in the same file, `home/DashboardRailCard.tsx` (the
dashboard's Watchlist rail), and `admin/redesign/AdminIssuesRail.tsx` (the admin "Issues queue"
rail). This lane's real delta:

1. Extracted `src/components/ui/RailCard.tsx`, `data-part="rail-card"`, as the one shell, covering
   four call shapes: plain title, `headLink` (an artboard 02 "Calendar →" style link), `headRight`
   (AdminIssuesRail's computed total badge), and `titleHref`+`titleCount` (the dashboard Watchlist
   shape, title itself is the link).
2. Rewired all seven prior hand-rolled call sites onto it: `ListSurfaceRailCards.tsx` (its own
   `RailCard` deleted, `FiltersRailCard` also converted from a hand-rolled header to `headRight`),
   `DetailShell.tsx` (four cards), `DashboardWatchlist.tsx` (via `DashboardRailCard` deleted,
   `RailEmptyFrame` kept, out of this lane's scope), `AdminIssuesRail.tsx`.
3. Added `data-part="stat-block"` to every StatBlock render branch (stack/row/tile).
4. Converted `WorkspacesUsageRow.tsx`'s rail-layout cells (Companies/Individuals/Newest join/Active
   this month) from a hand-rolled eyebrow/26px-figure/note triplet to `StatBlock` instances,
   `layout="stack"` (StatBlock's stack numeral is 26px by construction, an exact match). The
   non-rail ("row") layout, whose figure is 30px and explicitly "stays exactly as it is" per its own
   prior comment, is untouched.
5. Fixture pages `/admin/parts/rail-card` and `/admin/parts/stat-block`, each with a `part.json`,
   reusing lane W10-ListRow's frozen real `intelligence_items` rows (`list-row-fixture.ts`) rather
   than a new SELECT (retrieval-before-generation).

## Ruling 5 (legend rail card)

"legend rail card: one live ImpactMeter frozen at N = 8 next to the text, no diagram." Already true
before this lane: `LegendRailCard` (in `ListSurfaceRailCards.tsx`) mounts `<ImpactMeter total={8}/>`
beside the Impact/Timeline/Source-tier text; confirmed by reading the component's source (no
diagram element in it) and reconfirmed live by the new `/admin/parts/rail-card` fixture, which
mounts `LegendRailCard` unmodified as its "Plain title" variant.

## Ruling 7 (`/admin/factors` as a list surface)

"`/admin/factors` is a list surface with an absence convention; every admin route uses the same
frame, masthead, SectionCard, StatBlock, ListRow." Investigated: the route already shares the
`Masthead` part. Its content is a raw HTML `<table>` over `emission_factors` rows, seven technical
columns (tier/kind/value/unit/envelope/as-at/licence). `ListRow`'s fixed grid template
(`3px 56px 1fr 88px 84px 76px 40px 44px`: band spine / jurisdiction / title / impact meter / due
date / timeline / tier / overflow) has no analog for those seven columns; no artboard in this brief
or the parts-brief draws a mapping from an emission-factor row onto ListRow's geometry. Per the
lane's own "a case not drawn is ASKED, never invented" rule, this is left as-is: an explicit STOP,
not an omission, reported below and in the final report.

## Presence report

| Call site | Before | After |
|---|---|---|
| `ListSurfaceRailCards.tsx` (`RailCard`, `FiltersRailCard`, `ObligationsRailCard`, `CarbonCostRailCard`, `NextDataDropsRailCard`, `LegendRailCard`) | local `RailCard` typed in this file | imports `ui/RailCard.tsx` |
| `DetailShell.tsx` (`AtAGlanceCard`, `RailLegend`, `ImpactRailCard`, "In this list" card) | 4x hand-rolled `SectionCard` + header | 4x `RailCard` |
| `DashboardWatchlist.tsx` | `home/DashboardRailCard.tsx`'s `DashboardRailCard` | `ui/RailCard.tsx` (`titleHref`/`titleCount`) |
| `AdminIssuesRail.tsx` | hand-rolled `SectionCard` + header + count span | `RailCard` (`headRight`) |
| `WorkspacesUsageRow.tsx` (rail layout only) | hand-rolled eyebrow/figure/note | `StatBlock` (`layout="stack"`) |
| `AdminDashboard.tsx` | already `StatBlock` (prior lane) | unchanged, now carries `data-part="stat-block"` |
| `/admin/factors` | raw `<table>` | unchanged, explicit STOP (see above) |

Six of seven call sites in the earlier grep for `RailCard`/`rail-card`/`StatBlock`
(`OperationsCalculatorPageView.tsx`, `RegulationDetailSurface.tsx`, `ObligationRegisterFilterBar.tsx`,
`ObligationRegisterPageView.tsx`, `SearchResultsView.tsx`, `UserProfilePage.tsx`,
`RegulationsLedger.tsx`, `MarketSignalDetailSurface.tsx`, `Skeleton.tsx`, `AdminTableView.tsx`,
`CommunityRooms.tsx`) were already correct consumers of the shared `RailCard`/`StatBlock`/
`ImpactMeter`/`LegendRailCard` exports from prior lanes; this lane's `tsc`/fitness/test passes
confirm none regressed.

## Gates

- `npx tsc --noEmit -p tsconfig.json`: clean, three times (mid-lane, pre-commit, post-rebase).
- `sh fsi-app/.discipline/run-test-suite.sh` (no-npm glob): 1527 tests, 0 fail (background run,
  confirmed via task notification).
- `node --test $(git ls-files '*.npmtest.mjs')`: 1486 pass on the first full run (before adding this
  lane's own npmtest files), 1486 pass again after adding `RailCard.npmtest.mjs` + the
  `StatBlock.npmtest.mjs`/`SectionRule.coverage.npmtest.mjs`/`AdminIssuesRail.npmtest.mjs`/
  `ListSurfaceRailCards.npmtest.mjs`/`SettingsPage.npmtest.mjs` corrections this lane made (four
  stale coverage-style assertions updated to match the moved shell: `ListSurfaceRailCards.npmtest.mjs`
  and `AdminIssuesRail.npmtest.mjs` no longer assert a locally-typed `RailCard`;
  `SectionRule.coverage.npmtest.mjs`'s "DetailShell's eight cards" split into 4 `SectionCard` + 4
  `RailCard`; `SettingsPage.npmtest.mjs`'s import-path assertion updated to `ui/RailCard`).
- `node .discipline/fitness/runner.mjs`: 47 functions, 0 violations (F45 duplicated lines held at
  5995, the pre-lane base, no increase; F49 caught one real violation mid-lane, a hand-typed Anton
  numeral in `rail-card/page.tsx`'s fixture, fixed by using a plain tabular-nums span instead).
- `node .discipline/rendering/run-rendering-guard.mjs`: PASS twice (node.exe count checked <=3
  before each run per this lane's corrections to the COMMON BLOCK; both runs green, 846+294+348
  checks, layout guard 0 findings outside the dated baseline).
- Locked push-gate preflight (`lane-prepush-check.sh`): see the final report for outcome.

## UX compliance

**`/admin/parts/rail-card`.** Primary goal: platform-admin part sign-off, read the RailCard shell's
four head variants against real fixture data. Path: one page load, no interaction required, static
fixture data (no database read at render time). One primary action: none (read-only gallery,
consistent with every other `/admin/parts/*` page). Feedback state: n/a, no async behavior.

**`/admin/parts/stat-block`.** Same shape: one page load, read-only gallery of stack/row/tile
layouts plus the loading state, no interaction, no async behavior.

**Every existing surface consuming RailCard/StatBlock (Regulations, Market, Research, Operations,
Watchlist, the four detail pages, Dashboard, Admin).** The shell move is structural only: the same
title, same content, same click targets and same data as before at every call site; no layout,
interaction, or feedback-state change. Law 2 (touch targets): unaffected. Law 16 (pattern
consistency): improved, one rail-card shell instead of four independently drifting copies (the
`AdminIssuesRail` padding was previously a uniform 16px against RailCard's canonical 14px/16px; now
unified, a 2px top/bottom delta, logged here since it is the one visible geometry change this lane
makes anywhere).

## Open items

- `/admin/factors` (ruling 7's ListRow/StatBlock migration) is NOT done; see the STOP above. The
  page's Masthead/frame sharing was already correct before this lane.
- `WorkspacesUsageRow.tsx`'s non-rail ("row") layout is untouched by design (explicit prior-lane
  instruction "stays exactly as it is").
- The coordinator PR-body file and the locked-gate outcome: see the final report, not duplicated
  here.
