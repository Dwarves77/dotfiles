## 2026-09-21, lane w10-factcard-b: FactCard part 2 of 2 (Amendment 1 sections A, B.1, B.2, C)

Worktree `C:/Users/jason/dotfiles/.worktrees/wt-w10-factcard`, branch `lane/w10-factcard-b`, cut
from `origin/master` at `b1dd38e4` (part 1 is PR #763, squash `2b2ac415`, already in this base).

### What landed

1. **Operations matrix panel migrated to `density="matrix"`.** `MatrixFactCard`
   (`src/components/operations/RegionDimensionMatrix.tsx`) is deleted as a standalone function. Its
   full anatomy (the figure/no-figure-headline branch, `indexAgainstBase` delta, the five
   `data-audit` hooks, the three-part source line, `sixWordHeadline`/`claimAfterHeadline`) moved
   into `src/components/ui/FactCard.tsx` as a new `density="matrix"` branch of the ONE `FactCard`
   component (a discriminated-union prop: `{ model }` for the default density, `{ density:
   "matrix", fact, baseFact }` for the matrix density). Nothing behavioural changed in the move -
   every pixel value, data-audit string and branch condition carried over unchanged. Both call sites
   in `MatrixPanel` now read `<FactCard density="matrix" fact={...} baseFact={...} />`. No kind band
   is drawn in this density: region/dimension facts are raw envelope rows, not classified fact
   paragraphs, and the artboard (08-operations-list.png) draws no band above these cards - this is
   the honest absence of a field FactCardModel has no equivalent for, not an omission.
   `RegionDimensionMatrix.npmtest.mjs`'s literal-source assertions were re-pointed at
   `FactCard.tsx`'s new `FACTCARD_SOURCE` where they pin the moved anatomy, and left on
   `RegionDimensionMatrix.tsx`'s own `SOURCE`/`CODE` where they pin the call site. All 35 tests in
   that file pass. `.discipline/rendering/audit/mounts.mjs`'s no-figure-fixture comment updated to
   name the new location.
2. **Full-brief depth insertion-point investigation (Amendment 1 section A).**
   [CONFIRMED, by direct render test, `src/components/shared/GfmSection.npmtest.mjs`] `GfmSection`
   (react-markdown + remark-gfm) renders the amendment's exact measured pattern (`**Cause:** FACT:
   "..."` / `*Source: ... 02/04/2025.*`) correctly into real `<strong>`/`<em>` elements with no
   literal `*` surviving. [CONFIRMED, by source grep] all four detail surfaces
   (`RegulationDetailSurface.tsx:332`, `MarketSignalDetailSurface.tsx:475`,
   `ResearchFindingDetailSurface.tsx:302,323`, `OperationsDetailSurface.tsx:394`) already route their
   `depth === "full"` insertion point through `<GfmSection markdown={r.fullBrief} />`, never a raw
   `<p>{r.fullBrief}</p>`. `git log` confirms this wiring predates this lane and part 1 (last touched
   by Lane L34, `2f59c88b`, before `b1dd38e4`). **[REFUTED, this lane]** the "Full brief depth
   bypasses GfmSection" half of Amendment 1's hypothesis, for these four call sites, on this tree -
   corroborates the ORIGINAL w10-factcard lane's own finding in `2026-09-20-w10-factcard.md` ("It
   does NOT by itself explain literal asterisks surviving into the DOM, because GfmSection parses
   `**bold**`/`*italic*` correctly"). The measured live-DOM matches (56/37) are NOT explained by a
   render-path bypass at this insertion point on the current tree; they most plausibly reflect
   either stale pre-fix content or literal markdown characters already escaped inside the stored
   `full_brief` text itself - a data question outside this lane's write set (no DB access from this
   worktree; NOT re-investigated further here per the lane boundary). I did NOT find a second raw
   `<p>`/`{value}` bypass anywhere in the four detail surfaces' Full-brief path, `SourcesGrid.tsx`,
   or `primitives.tsx` after a full read of each. Render tests are written as the regression guard
   the brief asked for regardless (`GfmSection.npmtest.mjs`, 7 tests, all pass).
   **Bare URL outside an anchor** (amendment's second finding): searched all four detail surfaces
   and the shared source-rendering paths (`SourcesGrid.tsx`, `FactCard.tsx`'s `ProvenanceBlock`) for
   a `.url`/`.href` value printed outside an `<a>`; found none remaining - every href-bearing value
   in these paths is wrapped in an anchor. The Summary-depth provenance path (part 1's
   `fact-card-model.ts` `extractProvenance` + `FactCard.tsx`'s `ProvenanceBlock`) already routes a
   bare URL into `href` rather than running text. Not independently re-measured against the live DOM
   (that re-measure is the coordinator's, per the brief's "NOT yours" list).
3. **`/admin/parts` and `/admin/parts/fact-card` built.** Platform-admin gated
   (`requirePlatformAdmin`), same frame as `/admin/factors` (`PageMasthead` + content, no separate
   layout wrapper). `/admin/parts/page.tsx` lists parts (one today: FactCard) via a new
   `PartsList.tsx` component. `/admin/parts/fact-card/page.tsx` renders every one of the 9
   `FACT_CARD_KINDS`, the no-lead case, a long claim (>66ch), a card with no provenance, and both
   `density="matrix"` branches (figure and no-figure), all from static fixtures in the new
   `src/lib/detail/fact-card-fixtures.ts` - NO database read. `fact-card-fixtures.npmtest.mjs` (6
   tests) pins the coverage requirements (every kind present, no-lead carries `figureLead: null`,
   long claim exceeds 66ch, no-provenance fixture has `provenance: null`, both matrix branches
   present). Linked from `/admin`'s stat-tile grid (`AdminDashboard.tsx`, a new "Parts" tile beside
   the existing "Emission factors" one) so the route is reachable by navigation, not only by URL.
   F49 (parts-not-pages) stays green: no page.tsx retypes a part's literal shell styles: the index
   uses `PartsList.tsx` (a `SectionCard`-shelled row list) and the fact-card page uses
   `FactCardGallery.tsx` (a `SectionCard`-shelled grouping of real `<FactCard>` mounts).
   Screenshots: could not run the app locally without secrets (no `.env`, no Supabase credentials in
   this worktree, and `requirePlatformAdmin` calls `createSupabaseServerClient()`); the coordinator
   captures 1440/390 from production after merge, per the brief's own fallback.
4. **`data-part`/`data-kind` convention.** Already on `FactCard.tsx`'s default-density root from
   part 1 (`data-part="fact-card"`, `data-kind="<slug>"`); carried onto the new `density="matrix"`
   root too (`data-kind="matrix"`), so both anatomies are measurable the same way.
5. This file.

### Presence report: FactCard (coordinator amendment)

Enumerated by `grep -rn "<FactCard" src --include="*.tsx"` (excluding `.npmtest.mjs`), then traced
each call site's own callers up to a route:

- `route: /admin/parts/fact-card` · `src/components/admin/FactCardGallery.tsx:44` · `<FactCard
  model={f.model} />` (default density, every fixture)
- `route: /admin/parts/fact-card` · `src/components/admin/FactCardGallery.tsx:53` · `<FactCard
  density="matrix" fact={f.fact} baseFact={f.baseFact} />`
- `route: /operations` · `src/components/operations/RegionDimensionMatrix.tsx:884` · `<FactCard
  density="matrix" .../>` (compare-mode card; `RegionDimensionMatrix` mounted by
  `src/components/operations/OperationsLedger.tsx`, mounted by `src/app/operations/page.tsx`)
- `route: /operations` · `src/components/operations/RegionDimensionMatrix.tsx:903` · `<FactCard
  density="matrix" .../>` (single-cell panel card, same mount chain)
- `route: /regulations/[slug]` · `src/components/detail/FactBlocks.tsx:38` · `<FactCard
  model={model} />` (mounted by `src/components/regulations/RegulationDetailSurface.tsx:340`, itself
  mounted by `src/app/regulations/[slug]/page.tsx`)
- `route: /market/[slug]` · `src/components/detail/FactBlocks.tsx:38` · same component, mounted by
  `src/components/pages/MarketSignalDetailSurface.tsx` (six call sites: lines 467, 482, 483, 514,
  522, 546, 556), mounted by `src/app/market/[slug]/page.tsx`
- `route: /research/[slug]` · `src/components/detail/FactBlocks.tsx:38` · same component, mounted by
  `src/components/research/ResearchFindingDetailSurface.tsx:309`, mounted by
  `src/app/research/[slug]/page.tsx`
- `route: /operations/[slug]` · `src/components/detail/FactBlocks.tsx:38` · same component, mounted
  by `src/components/operations/OperationsDetailSurface.tsx:379`, mounted by
  `src/app/operations/[slug]/page.tsx`
- `route: /regulations/[slug], /market/[slug], /research/[slug]` (record-grade items) ·
  `src/components/detail/primitives.tsx:75` `RecordFactCard` · `<FactCard model={model} />`,
  reached via `RecordFactsBody` (`primitives.tsx:101,106`) from
  `MarketSignalDetailSurface.tsx:627` and `ResearchFindingDetailSurface.tsx:367`, and directly via
  `RegulationDetailSurface.tsx:449,461`'s own `RecordFactCard` calls

**Equivalent fact UI still rendering WITHOUT the part:** none found. Grepped
`OperationsDetailSurface.tsx`, `SourcesGrid.tsx`, and every `<p>`/`<span>` printing a claim-shaped
value across the four detail surfaces (the same read done for section A above) for a hand-rolled
card carrying a kind word, a figure lead and a provenance line outside `FactCard`; found none. The
`AtAGlanceCard`/`StatBlock` rail tiles and `PenaltyFacts`'s label/value pairs are a different,
narrower anatomy (metadata rows, no kind band, no claim prose) and are not FactCard consumers per
the bundle README's own consumer list ("the four details, the operations matrix panel, and research
findings").

### Owed (from part 1, carried forward)

Every later part lane carries `data-part` on its own root element, the same convention FactCard
uses, so conformance can be measured instead of guessed (Amendment 1 section C).

### Gates

- `npx tsc --noEmit` - clean.
- `node --test src/components/operations/RegionDimensionMatrix.npmtest.mjs` - 35/35 pass.
- `node --test src/components/shared/GfmSection.npmtest.mjs` - 7/7 pass.
- `node --test src/lib/detail/fact-card-fixtures.npmtest.mjs` - 6/6 pass.
- `node .discipline/fitness/runner.mjs` - `46 function(s) checked, 0 violation(s)`.
- Full remaining gate results (`run-test-suite.sh`, the push-gate preflight) are in the final report.
- Playwright is not installed on this PC (operator ruling, brief's own text); the rendering guard
  runs on GitHub after push, per the brief.

### UX compliance

- **`/admin/parts`** (operator/platform-admin screen). Primary goal: find the sign-off page for a
  given UI part. Path: land on `/admin/parts` (from the `/admin` "Parts" tile) -> read the one row
  -> click it. One primary action per row (open the part's page); no async action on this screen (a
  static list), so no pending/success/failure state is needed. Every row is a `<Link>` at
  `minHeight: 44` (law 2 floor).
- **`/admin/parts/fact-card`** (operator/platform-admin screen). Primary goal: verify every
  FactCard kind/form/edge-case renders correctly before sign-off. Path: land on the page -> scroll
  the two grouped galleries (default density, `density="matrix"`) -> visually compare each labelled
  fixture against the artboard. One primary action: none required (read-only review surface, no
  form, no submit); every fixture is server-rendered from static data with no loading state to
  design (no async action on this page).

Both screens are read-only platform-admin diagnostics with no async action, so the Doherty/pending-
state half of DP-2 does not apply to either; the applicable laws (Hick, Fitts, Jakob, Proximity,
Prägnanz) are satisfied by reusing `SectionCard`/`PageMasthead`/`StatBlock` rather than inventing new
shells, and by the 44px row targets on `PartsList`.

=== Value Delivery Check ===

This dispatch's work does NOT directly advance customer-facing value delivery.

This is a platform-admin diagnostic surface (`/admin/parts`) plus a rendering-path fix and
investigation on the four EXISTING customer-facing detail surfaces (Regulations, Market Intel,
Research, Operations). The operations-matrix migration (item 1) and the Full-brief
insertion-point investigation (item 2) touch customer-facing surfaces, but as an INTERNAL rendering
consistency fix (one shared part instead of two, same visual result) and a verification pass (which
refuted a hypothesized defect rather than closing a new customer-facing gap), not as new
customer-facing value. `/admin/parts` itself (item 3) is explicitly a platform-layer surface
(`requirePlatformAdmin`), never customer-visible.

No surface's customer-facing value gap (per `caros-ledge-platform-intent`'s "Customer-Facing Value
Gap" section) is closed by this dispatch.

Dual-posture: not applicable - this work is platform-admin tooling and a shared-part refactor, not a
workspace-scoped feature.
