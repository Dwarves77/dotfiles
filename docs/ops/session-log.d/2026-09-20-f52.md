## 2026-09-20, lane F52: a workflow file is validated before it reaches GitHub

Why [CONFIRMED, GitHub run 35533637184, per brief-f52.md]: lane M9d put `${{ runner.temp }}` in a
job-level `env:` of `.github/workflows/producers.yml`. The `runner` context does not exist outside a
step, so GitHub refused the file: the workflow "failed" with zero jobs on the branch push. PR #756's
locked push gate PASSED and every required check was green or going green, because nothing in pre-push
or CI validated a workflow file as a workflow file. This lane closes that hole.

What changed:

- `fsi-app/.discipline/fitness/lib/yml-read.mjs` (new): `extractWorkflowRunNames`, `hasWorkflowRunEdge`
  and a new `extractWorkflowName` helper, extracted from `F50-loop-wiring.mjs` (brief item 1: "reuse the
  yml reader F50 already uses ... extract what you need into ONE shared helper both import rather than a
  copy"). `F50-loop-wiring.mjs` now imports and re-exports the two functions it already had, so its own
  test file (which imports them from `./F50-loop-wiring.mjs`) is unaffected, and there is no second copy
  of the `workflow_run:` regex for F45 to catch.
- `fsi-app/.discipline/fitness/functions/F52-workflow-file-validity.mjs` (new): a holistic fitness
  function (one sentinel enumerate() entry, same shape as F23/F25/F27/F50) that runs five checks over
  every `.github/workflows/*.yml` and `.github/actions/*/action.yml` file: (a) parses, has `on:` and a
  runnable job (an action has `runs:`/`using:`); (b) a workflow- or job-level `env:` value references
  only `github`/`needs`/`strategy`/`matrix`/`vars`/`secrets`/`inputs`, never `runner`/`env`/`steps`/`job`
  (the M9d class itself); (c) a job-level `if:` never references `steps.` or `runner.`; (d) every
  `needs:` names a real job and every `steps.<id>.outputs` reference names a real step id in the same
  job; (e) a `workflow_run` trigger names a workflow whose own `name:` exists in the tree. Also runs
  `actionlint` locally when it is on PATH ("actionlint not on PATH, skipped locally; CI runs it" when it
  is not, never a false red, never a silent skip).
- `fsi-app/.discipline/fitness/functions/F52-workflow-file-validity.test.mjs` (new): 35 tests. Each of
  the five checks has a RED (attack) fixture and a GREEN fixture, using a temp-repo harness
  (`DISCIPLINE_REPO_ROOT` + `_clearRepoRootCache`/`_clearCache`) the same way F50's own test builds a
  temp harness-run family. RED (b) is the M9d line itself, verbatim (`PRODUCER_SUMMARY_DIR: ${{
  runner.temp }}/producer-summaries` inside a job-level `env:` block), per the brief's own instruction:
  this line is NOT on master (M9d's PR is held), so it exists only as this fixture. A LIVE test asserts
  the real committed tree returns zero F52 violations.
- `fsi-app/.discipline/governance/invariants.d/RD-77.mjs` (new): registers the invariant, enforced by
  `fitness:F52` and `selftest:` its own test file. No new remediation-discipline SKILL.md category is
  added (out of this lane's write set); the entry cites the closest existing category, 46 ("a loop's own
  hops are checked as data, not remembered as wired"), whose theme this extends one layer down: before a
  hop's edge can be trusted, the file carrying it has to be something GitHub would accept. The residual
  names honestly that the CI actionlint step has no `enforcedBy` token in this registry's vocabulary
  (rule/fitness/consistency/audit/selftest/migration) and is documented in `text` rather than tokenized.
- `.github/workflows/discipline.yml`: one added step, "actionlint (pinned, checksum-verified) over
  .github/workflows", in the `fitness-check` job, after "Run fitness functions". Downloads actionlint
  v1.7.12's linux_amd64 archive, verifies it against the sha256 read from actionlint's own release
  checksums file (`gh release view --repo rhysd/actionlint --json tagName,assets` gave tag `v1.7.12`;
  `gh release download --repo rhysd/actionlint v1.7.12 --pattern '*checksums*'` into the gitignored
  `fsi-app/scripts/tmp/` gave sha256 `8aca8db96f1b94770f1b0d72b6dddcb1ebb8123cb3712530b08cc387b349a3d8`
  for `actionlint_1.7.12_linux_amd64.tar.gz`, matching the asset's own reported digest field), then runs
  it over `.github/workflows/*.yml` with no `-ignore` and no `|| true`.
- `docs/runbooks/CORPUS-TURN-RUNBOOK.md`: new section "Workflow-file validity gate (F52, lane F52,
  2026-09-20)" appended at the end, found via `grep -rl "F50" docs/runbooks/` per the brief (this is the
  only runbook file that mentions F50).
- This session-log file.

Item 4 (this lane's own change is a workflow edit): after editing `discipline.yml`, ran F52 on it
(`node .discipline/fitness/runner.mjs`, see gates below), zero violations. Re-read the added lines by
eye against the context rules: the new step has no `env:` block, no job-level `if:`, no `needs:`, and no
`workflow_run` trigger; it is a single `run:` shell block with a literal pinned version and sha256, so
none of the five checks apply to it beyond "the file still parses and has a runnable job", which it does.

Findings on master's own workflow files: [CONFIRMED] F52 itself found zero violations on origin/master's
`.github/workflows/*.yml` and `.github/actions/*/action.yml` (verified before and after this lane's own
edit to `discipline.yml`). The fitness runner's 3 pre-existing violations (F51 `no-shared-append`
hotspot warnings on `scripts/lib/loop-run-id.mjs`, `scripts/lib/loop-run-id.test.mjs`,
`scripts/turns/emit-downstream-chain-artifact.mjs`) are [CONFIRMED] present on plain `origin/master`
before this lane's commits (reproduced with `git stash` + a clean fitness run: 45 functions, same 3
violations), unrelated to workflow files and not F52's own findings, so per the brief's "if F52 finds
real violations... list them and STOP" instruction (which is about F52's OWN findings) this is not a
stop condition; they are named here for completeness and left untouched, out of this lane's write set.

Gates, verbatim:

```
$ node --test .discipline/fitness/functions/F52-workflow-file-validity.test.mjs .discipline/fitness/functions/F50-loop-wiring.test.mjs   (from fsi-app/)
ℹ tests 35
ℹ suites 0
ℹ pass 35
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0

$ node .discipline/fitness/runner.mjs   (from fsi-app/)
Checking [F52] workflow-file-validity (1 files)
  PASS  [F52] workflow-file-validity
Fitness summary: 46 function(s) checked, 3 violation(s).   [3 are pre-existing F51 hotspot findings, see above; not F52's]

$ node .discipline/governance/invariant-coverage.mjs   (from fsi-app/)
ALL 135 invariants + 63 doctrines are wired ... === meta-gate PASS ===

$ node .discipline/consistency/override-check.mjs --range=origin/master..HEAD   (from fsi-app/)
[consistency backstop] consistency runner clean (no drift).
```

Lane-prepush-check.sh result pasted once available (run as one background task, last, per instruction).

UX compliance (f52): not applicable. This lane touched no `.tsx` or `.css` under `fsi-app/src` and
mounted no row, ledger, or card component; it is a discipline fitness function, an invariant, a CI
workflow step, and docs.

What I could not do / open items: none. The brief's out-of-scope list (fixing any workflow's logic,
adding schedules or triggers, any derived registry list, `loop-manifest.mjs`, installing software on
this PC) was not touched. actionlint was never installed on this machine; F52's local run always takes
the "not on PATH, skipped locally" branch here, which is expected and documented behavior, not a gap.
CI installs and runs the pinned binary.
