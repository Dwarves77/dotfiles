## 2026-09-18, W9 lane m1: fetch-drain gets a runner and a workflow, the head of the loop's second hop

First person, lane m1 (build plan `docs/plans/complete-system-build-plan-2026-09-04.md` section 6.1 row
M1). The stage audit (README hops table; `s1-collect.md` rows 7 and 8) found the fetch-drain family had
no workflow at all, every drain of `pending_first_fetch` was a coordinator issuing `pg_net.http_post`
batches by hand through MCP. This lane closes that gap.

**What landed.** `fsi-app/scripts/turns/run-fetch-drain.mjs`, a harness runner in the shape
`run-source-sweep.mjs`/`run-ledger-consume.mjs` already have: `--mode dry` (default) selects the queued
rows (oldest first, `--limit`, default 8) and the stuck rows (`status='fetching'` older than one hour,
the reset condition `fetch-drain-run-003.json`'s own `inputs_ref` names), read-only, zero HTTP calls;
`--mode apply` resets stuck rows via a scoped id-list UPDATE, re-selects the queue, invokes
`capture-worker` directly over HTTPS in batches of 8, and reads back every batch afterward so an HTTP
failure still gets an honest per-row outcome. `--limit` is capped at 64 in code. Governing files
(`scripts/harness-runs/governing-files.mjs`) extended: `fetch-drain` now hashes the runner too, alongside
`capture-worker/index.ts`, the same shape every other scripted family has. `CONVENTION.md`'s matching
table row and `scripts/harness-runs/meta-harness/PENDING-RUN.md` (Re-pin 4) were updated in the same
commit, since that governing-file edit is self-referentially one of meta-harness's own governing files
too. `scripts/harness-runs/fetch-drain/LAST-PROPOSER-PASS.md` now names `fetch-drain-run-004` (F28 rule
d).

`.github/workflows/fetch-drain.yml` (new): `workflow_dispatch` (`mode` dry|apply default dry, `limit`
default 8) plus `workflow_run: workflows: ["Source sweep"], types: [completed]` running `apply` at
`limit=8` only when the upstream conclusion is `success`, copying `ledger-consume.yml`'s own conditional
verbatim per the brief. `GITHUB_EVENT_WORKFLOW_RUN_ID` exported into the runner's env on that trigger for
`config.upstream_run_id` (M9a adds the top-level loop-manifest field later). No schedule armed.
`.github/workflows/source-sweep.yml` gains one input, `loop_run_id` (default empty, resolved to
`github.run_id` when blank), passed as `LOOP_RUN_ID` env to `run-source-sweep.mjs`, which now records
`config.loop_run_id`; nothing else in that file changed.

Ran `--mode dry` once locally (env file at `wt-adr016-ft`, per the brief) against the live project and
committed the result: `fetch-drain-run-004.json`, `config.mode:"dry"`, `queued_selected=8`,
`stuck_selected=0`, `harness_version` matching the current `GOVERNING_FILES['fetch-drain']` hash exactly
(`sha256:8798fd9745d2c458`), so F28's staleness coupling this lane's own governing-files edit opened is
discharged by the same artifact. I do not run apply mode; that is the coordinator's first dispatch.

`docs/runbooks/CORPUS-TURN-RUNBOOK.md` gained a "Fetch drain" section (inserted before "Change detection")
documenting both modes, the two triggers, the `loop_run_id` addition, how to read the artifact, and the
first-dispatch evidence; it is the only place the hand `pg_net` procedure was ever written down (a grep for
`pg_net` under `docs/` found no other file naming the procedure to mark superseded).

`fsi-app/scripts/verify/population-report.mjs` gained one STORES entry (it did not already print
`pending_first_fetch` counts): `countFetchDrainQueued`/`describeFetchDrainQueueState`, same shape as the
existing "briefs pending"/"open flags" queue-depth entries. Live read at write time: 12 rows queued (moved
from the audit's 12 dating from 2026-09-01; four more accrued since the dry run's own 8-row snapshot,
consistent with normal accrual, not a defect).

`fsi-app/scripts/turns/run-fetch-drain.test.mjs` (new): 24 tests over the pure parts (`parseArgs`,
`stuckCutoffIso`, `batchIds`, `tallyByStatus`, `shapeDryPlan`, `shapeBatchPerItem`, `functionUrlFor`), no
network, no DB, including the attack test (rule 15) proving `--limit` above 64 is refused, never silently
clamped.

**Evidence per section 0 criterion.** Reachable: `fetch-drain.yml` carries a `run:` line
(`run-fetch-drain.mjs`) and the `workflow_run` edge (`workflows: ["Source sweep"], types: [completed]`).
Run: `fetch-drain-run-004.json` committed, `trigger` was a local dispatch (not yet a `workflow_run` firing,
the coordinator's apply dispatch is what proves that edge fires for real). Populated: to be read back by
the coordinator after the first apply (the queue moved from 8 selected to 12 live between this run and the
population-report check minutes later, so the mechanism is live). Visible: `population-report.mjs` now
prints the `pending_first_fetch` queued count. Gated: F50 (lane M9a) will enforce the `workflow_run` edge;
this lane's own unit tests are green (24/24), and F28 (33/33, incl. the CONVENTION-TABLE-PARITY and
staleness-coupling checks this lane's governing-files edit exercised) is green. Documented: the runbook
section above.

**Tests run.** `node --test scripts/turns/run-fetch-drain.test.mjs`: 24/24 pass. `node --test
scripts/turns/run-source-sweep.test.mjs`: 65/65 pass (no regression from the `loop_run_id` addition).
`node --test scripts/verify/population-report.test.mjs`: 55/55 pass (no regression from the new STORES
entry). `node --test scripts/harness-runs/governing-files.test.mjs`: 20/20 pass. `node --test
.discipline/fitness/functions/F28-harness-run-integrity.test.mjs`: 33/33 pass (green against the live
tree, including this lane's own governing-files/CONVENTION.md/PENDING-RUN.md edits). No `.ts`/`.tsx` file
touched, so no `tsc --noEmit` run.

**What I could not do, and why.** Apply mode was not run (the brief's own instruction: the coordinator
runs the first `--mode apply` dispatch). The `workflow_run` edge has therefore not fired for real yet;
`fetch-drain-run-005` (the coordinator's dispatch, or the first automatic chain firing after a source
sweep) is what will prove it. No problem met the stop conditions named in the brief (the worker's request
contract was determinable from `index.ts` directly; the reset step was determinable from migration 065's
own status vocabulary and `fetch-drain-run-003.json`'s `inputs_ref` line, not ambiguous; `ledger-consume.yml`'s
conditional fit `fetch-drain.yml`'s own shape unchanged; the one dry run consumed no Browserless or paid
unit, confirmed by its own `metrics.invocations: 0`).

**Addendum (same day, post-rebase): pre-push refused the branch on four gates, all four fixed.** Coordinator
rebased this branch onto master (HEAD now on top of a source-sweep PENDING-RUN re-pin, d3b85f22) and ran
pre-push, which found F45 duplicate-code (+18 over the 6227 ceiling: two clone pairs, `research-sweep.mjs`
sharing 8-line windows with both `run-fetch-drain.mjs` and `run-source-sweep.mjs`, the `--mode` validation
tail and the five-field artifact header), F39 unbounded-in-filter (`run-fetch-drain.mjs`'s batch read-back,
a bare `.in("id", batch)` on a runtime array), F47 db-object-reference (the `pending_first_fetch` allowlist
entry now stale, since this lane's own runner reads that table), and F28 harness-run-integrity staleness
coupling (fetch-drain's own governing files re-hashed past artifact 004's recorded value once the F45 fix
landed). Fixed all four: extracted `validateModeArg`/`baseArtifactFields` to `scripts/lib/run-artifact.mjs`
(the existing shared home every runner already imports from) and wired `research-sweep.mjs`,
`run-fetch-drain.mjs`, and `run-source-sweep.mjs` to use them, F45 back to exactly 6227 (at the ceiling,
not below, no re-seed); replaced the bare `.in()` with `db.mjs`'s `readAllByIds`, F39 clean, no allowlist
marker added; removed the stale `pending_first_fetch` entry from F47's `ALLOWLIST`; deleted the
coordinator's placeholder run-005 files and re-ran `--mode dry` once more (env file, live project) so
`fetch-drain-run-005.json` carries the final hash, re-pinning `meta-harness/PENDING-RUN.md` (Re-pin 5) and
`source-sweep/PENDING-RUN.md` (Re-pin 3), both moved again by the F45 fix's own edits (self-referential:
`run-artifact.mjs` governs meta-harness, `run-source-sweep.mjs` governs source-sweep), and updating
`fetch-drain/LAST-PROPOSER-PASS.md` to name run-005. All four gates green together: F45 0 violations, F39
16/16, F47 6/6, F28 33/33. Commits: `68dce99e` (F45/F39/F47) and the artifact-refresh commit that follows
this entry (F28 + PENDING-RUN re-pins + this addendum).
