# Lane W10-StateNote session log (2026-09-23)

Brief: `docs/dispatches/lane-briefs/2026-09-22/brief-w10-remaining-parts.md`, section "5. W10-StateNote"
plus its COMMON BLOCK. Worktree: `C:/Users/jason/dotfiles/.worktrees/wt-landdocs-0911`, branch
`lane/w10-statenote`, cut from `origin/master` at `9661a67d`.

Skills loaded via the Skill tool: `fsi-app:environmental-policy-and-innovation`, `frontend-design`,
`remediation-discipline`, `caros-ledge-platform-intent` (loaded before the governed-file write it
gated, `src/app/admin/parts/state-note/page.tsx`).

## Reconnaissance findings (read-only, before any edit)

- `src/components/ui/StateNote.tsx` (the shared part) already exists and already matches the artboard
  shape exactly (border-left 3px solid <band>; band tint background; radius 0 6px 6px 0; padding 9px
  12px; neutral variant `--brand` on `--tag`), built by an earlier lane, not this one. It already has
  full structural test coverage (`StateNote.npmtest.mjs`, 4 tests, all passing before this lane touched
  anything).
- StateNote is already broadly adopted: 22 files under `src/components` compose it (dashboard, detail
  shell, list-surface shell, map, market/operations/regulations/research ledgers and detail surfaces,
  watchlist, command-bar, profile, sources admin, search results). The Timeline part's next-obligation
  callout (`src/components/ui/Timeline.tsx`) already renders through StateNote, so "the Timeline callout
  built by ActionCard-a" is already reconciled to the one part; no separate implementation exists to
  merge.
- Swept for page-local duplicates of StateNote's exact shape (`grep -n "0 6px 6px 0"` and
  `borderLeft.*3px solid` across `src`). One real duplicate found: `src/components/ui/Masthead.tsx`'s
  `notice` block hand-typed the identical border-left/tint/radius/padding shape instead of composing
  StateNote. Fixed, see below. Every other `borderLeft: "3px solid ..."` hit is a different shell
  (FactCard's own edge, Community post-composer accent, the Conversion-trigger callout on
  `MarketSignalDetailSurface.tsx`, `SectorSynopsis.tsx`/`IntelligenceBrief.tsx`, the shelved
  sector-synopsis feature CLAUDE.md's own doctrine says not to touch, unreachable from any route today)
  and is out of this part's scope.

## Built

1. **`src/app/admin/parts/state-note/page.tsx`** (new) + **`src/lib/detail/state-note-fixtures.tsx`**
   (new): the sign-off page, 13 fixtures across empty / loading / error / not-scored / action-strip
   categories plus all five band tints (Immediate/Action/Monitor/Awareness/neutral), rendered through
   the real `StateNote` part. Every fixture string is copied verbatim from a real call site (cited by
   file:line in the fixture data, no invented copy), following the "hand-built, not a query" precedent
   `src/lib/detail/action-card-fixtures.ts` already set for this class of composite-part fixture (its
   own header: "Hand-built, not a query... no database read inside the part"). No database read.
2. **`src/app/admin/parts/state-note/part.json`** (new, per the coordinator's mid-task correction): the
   directory-registry descriptor lane ListRow's `parts-registry.ts::loadPartEntries` reads. Copied the
   shape from `wt-l25-slot-mirror`'s `masthead/part.json`. `src/app/admin/parts/page.tsx` itself was
   NOT touched, per the coordinator's explicit instruction.
3. **`src/components/ui/Masthead.tsx`**: the `notice` block (consumed by `SettingsPage.tsx` only) no
   longer hand-types StateNote's shape; it renders `<StateNote>` directly. Behavior preserved exactly:
   the action link still renders only when both `linkLabel` and `linkHref` are present (the opsclip
   "no dead '#' href fallback" defect stays fixed).
4. **`src/components/ui/Masthead.npmtest.mjs`**: updated the one test that asserted the old hand-typed
   markup to assert the new StateNote-delegated shape instead, plus one new test asserting the old
   literal border-left/radius strings are gone from this file (one-home, F42/F49 spirit).

## Consumers checked

`git grep -rn "notice={" src`, one call site (`SettingsPage.tsx:190`), unaffected by the prop
interface (unchanged). `git grep -rn "StateNote" src`, 22 consumer files, none required changes; all
already pass the correct `band`/`action` shape.

## The two HYPOTHESIS items (brief section 5, sourced from `docs/ops/session-log.md:24117`, the
2026-09-20 live-site audit)

**"Not scored" in the research legend, [REFUTED].** Method: read the three files that render this
text (`CredibilityChipEvidence.tsx`, `CredibilityChipAuthority.tsx`, `CredibilityChipShared.tsx`) and
their mount site (`src/app/research/page.tsx:63-94`, the "Credibility model" footer strip below the
research ledger), cross-referenced against `docs/design/handoff-2026-09-06/README.md`'s own fix note
("NOT SCORED second rows replaced by the absence convention"). That fixed defect was a literal SECOND
ROW under a list title, in ALL CAPS, sourced from raw source text, a different location and shape than
this. The text found is a chip BUTTON (not a list row/cell, so the Absence convention's "never in a
column, never a second row" scope does not govern it), reads "Not scored" in sentence case, carries an
honest `title` tooltip explaining exactly why (no evidence-synthesis pipeline yet computes the two
scores, spec-03 section 4), and the surrounding legend text states plainly "Two scores, never merged
(spec-03 section 4). Click a chip for the GRADE modifier ledger." This is intentional, sourced, honest
epistemic disclosure of an unbuilt pipeline, not a defect. No live render was available to confirm
visually (no `.env` in this worktree per the CommandBar-parts lane's own note; a dev server needs
Supabase creds this worktree does not have), so this refutation rests on code plus design-doc
cross-reference, not a live screenshot. Flagged honestly rather than asserted as a live-verified
[CONFIRMED].

**Watchlist "no re-check column", [CONFIRMED], fix deferred to the ListRow lane.** Method: read
`WatchlistSurface.tsx`'s own column-header docstring and its live `ListRowColumnHeader` call
(`titleLabel="Title · type · modes" dueLabel="Next date"`, no re-check column), cross-referenced
against `docs/design/handoff-2026-09-06/README.md`'s artboard-11 row ("Same row, one block, plus a
re-check column") and `WatchlistItem`'s own comment citing the same UILISTS-lane intent. The column is
genuinely absent. Not fixed here: adding it means extending `ListRow.tsx`'s shared `GRID` constant
(`"3px 56px 1fr 88px 84px 76px 40px 44px"`), which is the ONE row part on every list surface
(regulations/market/research/operations/watchlist) and is the exact file the coordinator's mid-task
message named as under active concurrent development by lane ListRow (which just had its own index
change refused for colliding with lane CommandBar-parts on a shared file). Touching that shared grid
from a second concurrent lane risks the identical class of conflict. Also: no live data field exists
today for a per-row re-check timestamp on item-backed watchlist rows (`WatchlistItem` carries no
`last_checked`/cadence field, confirmed by reading its full interface in `supabase-server.ts`), so any
column would either need new data plumbing (a source_id join, out of a UI-part lane's scope) or would
render the same neutral Absence state on every row reflecting the real, doctrinal, currently-true
build-mode fact that `system_state.scrape_cadence='off'` (CLAUDE.md standing rule 16), a legitimate but
larger decision than this lane should make unilaterally mid-dispatch. Decision-ready for whoever picks
it up: extend the shared `GRID`/`ListRowColumnHeader` with a trailing re-check cell, wire
`sources.last_checked` for `type: "source"` rows, and render the Absence convention ("checks paused")
for item-backed rows while cadence stays off.

## UX compliance

- **`/admin/parts/state-note`**, primary goal: platform-admin sign-off review of the StateNote part's
  every variant before it ships across surfaces. Path: nav to `/admin/parts/state-note` (one step, once
  ListRow's derived index lands it will also be linked from `/admin/parts`). One primary action: none
  (a read-only gallery, no submission flow, law 1/9, Hick's Law: nothing to decide on this screen but
  what to look at). Feedback state: N/A, no async action on this page (static fixture render, no
  fetch).
- **`src/components/ui/Masthead.tsx` notice block**, no change in user-facing behavior; the visual
  shape is byte-identical (same border-left/tint/radius/padding, now sourced from the shared part
  instead of a duplicate). Primary action unchanged: the optional trailing link, when present, still
  renders at its real target size (StateNote's own 28px+8px-clearance `ACTION_TARGET`, law 2). Verified
  with the real Rendering guard (two full PASS runs, below), which mounts `SettingsPage` (the notice's
  one live consumer) inside its UX smoke slot.

## Gate outputs

- `bash .discipline/run-test-suite.sh`: `tests 1518, pass 1514, fail 0, skipped 4` (skips pre-existing,
  unrelated to this lane). Exit 0. The `audit-finding-status` advisory line at the end (607 unlabeled
  findings across old audit docs) is pre-existing debt unrelated to this branch's diff; it does not fail
  the script (confirmed exit code separately).
- `node .discipline/fitness/runner.mjs`: `47 function(s) checked, 0 violation(s)`.
- `node .discipline/rendering/run-rendering-guard.mjs`, run twice: both printed
  `=== rendering guard PASS ===` (fixtures 14, viewports 12, checks 846; SM smoke specs 14/294 checks;
  layout guard 36 route by width, 0 findings; UX smoke specs 18/348 checks).
- `npx tsc --noEmit`: clean, both before and after adding `part.json`.
- Node contention: `tasklist | grep -ic node.exe` was above 3 twice during this session (37, 40) from
  concurrent lanes; waited (10s poll) both times until it dropped to 3 before running a gate, per this
  lane's correction 2.

## Open items

- Watchlist re-check column: CONFIRMED real, deferred to the ListRow lane per the reasoning above,
  needs an operator/coordinator decision on data source before any lane builds it.
- The "Not scored" research-legend refutation was not visually confirmed in a live render (no DB
  credentials in this worktree); the refutation rests on source plus design-doc cross-reference.
  Flagged per CLAUDE.md rule 14 rather than overstated as [CONFIRMED].
- Push gate / merge: not run from this session transcript segment; see final report for the locked-gate
  outcome.
