## 2026-09-20, lane M3b: the loop id reaches every hop downstream of the mint; two stale gate claims corrected

Landed on branch `lane/m3b-loop-id-downstream-2026-09-20`, cut from `origin/master` at `cc038a47`, in
worktree `wt-l34-detail-admin-primitives`. Timestamp from `date -u +%Y-%m-%dT%H:%M:%SZ`: 2026-09-20T18:48:36Z.

### What landed

1. `fsi-app/scripts/lib/loop-run-id.mjs`: added `FAMILY_BY_WORKFLOW_NAME` (the one home for
   workflow-name-to-harness-family mapping, keyed exactly as each yml's own `name:` line spells it) and
   `resolveLoopRunIdFromUpstream({ explicit, upstreamName, upstreamRunId, fsiRoot })`, which looks the
   name up in the map and calls the existing `resolveLoopRunId` with
   `harnessRunsDir = <fsiRoot>/scripts/harness-runs/<family>`, matching every existing caller's own
   convention (`run-fetch-drain.mjs`, `run-ledger-consume.mjs`).
2. `fsi-app/scripts/turns/emit-downstream-chain-artifact.mjs`: `buildArtifact` gains `loopRunId = null`
   written to `config.loop_run_id`; `main()` resolves it through `resolveLoopRunIdFromUpstream` from
   `DC_UPSTREAM_NAME`/`DC_UPSTREAM_RUN_ID`.
3. `fsi-app/scripts/turns/emit-corpus-turn-artifact.mjs`: same shape; `main()` resolves with
   `upstreamName: "Ledger consume"`, `upstreamRunId: process.env.GITHUB_EVENT_WORKFLOW_RUN_ID || null`.
4. `fsi-app/scripts/turns/run-propagation-drain.mjs`: `config.loop_run_id` resolved from
   `triggerContext?.name` / `triggerContext?.run_id` (stringified) through the same helper; null when
   there is no trigger context.
5. Tests extended in each file's own `*.test.mjs`: default-null, verbatim-recorded-when-passed, and a
   fixture test through `resolveLoopRunIdFromUpstream` with a temp harness-runs dir, for the
   downstream-chain, corpus-turn and propagation-drain emitters. `loop-run-id.test.mjs`'s four-hop attack
   chain extended to six hops (adding downstream-chain and propagation), keeping the
   break-one-hop-null-cascades assertion. Added a `FAMILY_BY_WORKFLOW_NAME` coverage test (every
   `LOOP_HOPS` producer name is a mapped key) plus its attack form (removing one key makes the same
   assertion fail).
6. Pending file: `fsi-app/scripts/harness-runs/propagation/pending/2026-09-20-m3b.md` (the only family
   whose `family.json` `governing_files` names a file this lane changed, `run-propagation-drain.mjs`; a
   grep of every `fsi-app/scripts/harness-runs/*/family.json` for `emit-downstream-chain-artifact.mjs` /
   `emit-corpus-turn-artifact.mjs` / `loop-run-id.mjs` found no other family listing them as governing).
7. Two stale claims corrected in place (rule 13 corollary):
   - `fsi-app/.discipline/governance/worktree-isolation.mjs` lines 15-16: was a present-tense claim that
     PreToolUse "is session-scoped and does NOT fire inside subagents/workflows (verified 2026-06-07)".
     [REFUTED 2026-09-19]: the main checkout's own `.gate-audit.log` recorded 11 denials of a sub-agent's
     own Edit/Write calls, and lane G1 (#754) made the skill gate judge the acting agent's own transcript.
     Rewrote as past-tense history plus the corrected present.
   - `fsi-app/.claude/skills/remediation-discipline/SKILL.md`, category 14: [CONFIRMED, not refuted] the
     same claim IS stated there (`git grep -n "PreToolUse" .claude/skills/remediation-discipline/SKILL.md`
     matched exactly one line, 117; the earlier framing "matched only two long lines, 117 and 142" does
     not hold, 142 does not contain "PreToolUse"). Corrected in place with the same wording pattern.
     Added `fsi-app/.discipline/governance/skill-acks/2026-09-20-m3b.md` naming the skill and the citing
     files reviewed (`skill-contract-map.mjs --check` passes, no drift).
   - Final grep `git grep -n "does NOT fire inside\|not fire inside sub" -- fsi-app docs/runbooks` from
     the repo root after the edits: zero remaining live statements. `docs/doctrine/worktree-isolation.md`
     still carries a related claim at two lines (different exact wording), but that path is outside this
     lane's grep/fix scope (`fsi-app` and `docs/runbooks` only); reported, not fixed.

### Gates (verbatim summaries)

- `node --test scripts/lib/loop-run-id.test.mjs scripts/turns/emit-downstream-chain-artifact.test.mjs scripts/turns/emit-corpus-turn-artifact.test.mjs scripts/turns/run-propagation-drain.test.mjs`: `tests 59, pass 59, fail 0`.
- `node --test .discipline/governance/loop-manifest.test.mjs`: `tests 9, pass 9, fail 0`.
- `node --test .discipline/fitness/functions/F50-loop-wiring.test.mjs`: `tests 12, pass 12, fail 0`.
- `node --test scripts/lib/run-artifact.test.mjs scripts/harness-runs/family-registry.test.mjs`: `tests 125, pass 125, fail 0` (combined run of both files plus the four already listed above).
- `npx tsc --noEmit`: clean, no output.
- `node .discipline/fitness/runner.mjs`: `Fitness summary: 45 function(s) checked, 0 violation(s).` F45 duplicated lines 6121, same as base 6121 (not exceeded). F50 hops not yet enforced: 11 (unchanged, as the brief predicted).
- `node .discipline/consistency/override-check.mjs --range=origin/master..HEAD`: `consistency runner clean (no drift)`.
- `node .discipline/governance/skill-contract-map.mjs --check`: `OK, 6 registered skills, no drift.`
- Push gate: run once, last, via the wrapper; result recorded in the report returned to the coordinator.

### UX compliance (m3b)

Not applicable. This lane touched no `.tsx` or `.css` file; all changes are `.mjs` runner/lib/test files,
one doctrine comment, one skill markdown correction, and two governance markdown files (pending/ack).

### Problems that stopped me

None. No STOP was needed; every step the brief named resolved cleanly against the committed tree.
