## 2026-09-19, lane N6: F51 no-shared-append, the gate that holds plan 6.8's line

Lane N6, model Sonnet, worktree `wt-n6`, branch `lane/n6-f51-no-shared-append-2026-09-19`, base
`origin/master` at `ccb6aa0c` (carries N0, T2, N2, N1, N3, N5, N4).

**Result [CONFIRMED by the commands pasted in the coordinator report].** Built fitness function F51
(`no-shared-append`), five checks each proven by attack: (1) no hand-written entry reappears in
`fitness/manifest.mjs`, `governing-files.mjs`, `run-artifact.mjs` or `invariants.mjs`; (2) no fitness
function stores a nonzero `_CEILING` or a hash-pin literal, a zero ceiling needs a dated allowlist entry;
(3) ids unique within each entry-file category; (4) a `lane/` branch never touches a coordinator-only
file; (5) the standing hotspot count (files changed by 3+ of the last 30 first-parent commits of
`origin/master`) is printed every run. Registered invariant RD-75 (F51 itself) and RD-76 (lane N4's own
skill-contract range-acknowledgment mechanism, which had no invariant naming it: a grep of
`invariants.d/*.mjs` for its own identifiers found only RD-68, whose text and enforcedBy are entirely
about the unrelated Windows main-guard defect, category 44). Both cite the same new skill section,
category 48. Wrote `fsi-app/.discipline/governance/skill-acks/2026-09-19-n6.md`, the first file this
mechanism has ever needed. `node --test F51-no-shared-append.test.mjs`: 32/32 pass, including a full
120-order merge replay of the 2026-09-18 lane set (M8, M9b, M9a, M1, W10-A) against a fixture seeded from
the REAL derived directories: zero conflicts in every order, elapsed under 10s.

**Registration needed no hand edit to any shared list** (the finding this lane exists to catch, per the
brief): `fitness/manifest.mjs` picked up F51 by directory scan; `invariants.mjs` picked up RD-75 and
RD-76 by directory scan; `skill-contract-map.mjs`'s `PINNED_MANIFEST` needed no re-pin, only the new ack
file. Confirmed by `git diff --name-only origin/master..HEAD` after commit: no manifest, no
`invariants.mjs`, no `governing-files.mjs`, no `run-artifact.mjs`, no `CONVENTION.md` in the list.

**Findings [CONFIRMED by running F51 against this live tree]: check 5 (the hotspot standing number)
and check 3 (duplicate ids) both surface real, pre-existing facts outside this lane's write set, per the
brief's own instruction to STOP and report rather than allowlist.** Full detail, every file named, is in
the coordinator report; summary: two ancient duplicate migration-number prefixes (006, 007, both predate
this build by a wide margin, harmless to Supabase since each has a distinct filename) and roughly 15
hotspot files, four more than the coordinator's own five explicitly-anticipated-but-not-allowlisted
files (`run-artifact.mjs` is still hot; `CONVENTION.md`, `governing-files.mjs` and F45 have fallen back
under the threshold on this exact measurement, `meta-harness/PENDING-RUN.md` no longer exists), plus a
larger set the coordinator's 2026-09-19 snapshot never named at all (`invariants.mjs`,
`skill-contract-map.mjs`, `run-test-suite.sh`, and several unrelated feature scripts under
`scripts/turns/`, `scripts/verify/`, `scripts/mint/`, `scripts/entities/`), because roughly a dozen
non-6.8 lanes (T2, W10-A, M8, M9a, M9b, M1, P7, D28b, L35h, L36, L37, L38, D2, M9c, P10) landed on master
in between, shifting the trailing 30-commit window. `node .discipline/fitness/runner.mjs` therefore
prints F51 as 17 violations on this exact tree, not 0. This lane did NOT allowlist any of them (the
brief explicitly forbids widening `HOTSPOT_ALLOWLIST` beyond the six coordinator-owned-by-contract
files) and did NOT run the push-gate wrapper, since it would fail at this same step for a reason outside
this lane's write set to fix, and a known-doomed run would waste one of the two allowed gate attempts.

**Next.** Coordinator ruling needed on: (a) whether `run-artifact.mjs` and the newly-surfaced files are
genuine debt to fix at the source, or whether check 5's 3-of-30 threshold is tighter than this repo's
current multi-lane cadence can bear and needs widening or a different window; (b) the two duplicate
migration-number prefixes (006, 007), whether they are accepted historical debt or worth a dated
allowlist entry of their own kind; (c) re-dispatch of this lane (or a follow-up) to push once the
coordinator has ruled, since the mechanism itself is built, tested and correct.

### UX compliance (N6)
Not a UI change; no customer surface touched by this branch.
