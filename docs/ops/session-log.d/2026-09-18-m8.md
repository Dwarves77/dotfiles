## 2026-09-18, W9 lane m8: collect completeness, the code half (plan section 6.1, row M8)

Worktree wt-hashsep-0911, branch lane/m8-collect-completeness-2026-09-18, on master 3da30b22. Closes S1's
findings on the sitemap backfill, the feed walker, the research walker, and the triage ladder's missing
artifact (`docs/audits/stage-audit-2026-09-18/s1-collect.md`).

**Sitemap backfill slices, landed.** `run-source-sweep.mjs` gains `--slice`/`--after` for `--walker
sitemap --all-hosts` (default slice 100, hard ceiling 500): `selectAllHostsTargets` extended with an
`afterHost` cursor (a stale/missing cursor restarts from the front, never throws, never skips the whole
bucket); two new pure functions, `lastFullyWalkedHost` (advances the cursor only past a host this run
FINISHED every row of, falling back to the prior cursor when the wall-clock budget stopped before the
first host completed) and `latestSitemapAllHostsCursor` (reads the cursor back from the newest matching
artifact, so a bare re-dispatch resumes with no state hand-carried). Every `--all-hosts` artifact now
records `config.cursor` and `metrics.hosts_remaining_after_slice`. Read-only SQL, this session, confirmed
the audit's own bucket counts byte-identical: `sitemap_walk_outcome` over all 2,572 `sources` rows --
walked 140, feed_only 120, no_sitemap 90, bot_wall 21, NULL (never walked) 2,201 (85.6%) -- zero net
movement since 2026-09-05.

**Feed walker -- STOPPED, per the execution rule.** `source-sweep-run-008`'s only-ever dispatch
(`theloadstar.com/feed`) failed `"fetch failed: HTTP 403"` -- a transport-level rejection, not a parser
defect (the fetch never reached a body for `feed-walk.mjs` to parse). None of the brief's three named
hypotheses (parser rejects a valid shape, wrong content-type check, missing redirect follow) fit a plain
403; distinguishing a genuine bot-wall from a bad URL or a CI-runner IP block needs one live fetch, which
this lane's brief forbids. Documented in the runbook with the exact reasoning and the audit's own
recommendation (one bounded dry re-dispatch, or a known-good feed as a control). No code change.

**Research walker -- confirmed complete, no code change.** `research-sweep.mjs` reads `sources.category =
'research'`, fetches each source's listing/feed, discovers candidates, builds and validates research-grade
payloads, and writes its own `source-sweep`-family harness artifact -- already wired as `walker=research`
in `source-sweep.yml` and dispatchable today. All 31 existing unit tests pass; it has simply never been
dispatched (0 of 18 recorded runs used it). Nothing to build.

**Triage ladder, committed harness artifact.** `inaccessible-triage.mjs` writes a structured, committed
run artifact every run (dry or apply) -- `per_item` for every triaged/skipped/errored source, `metrics`
mirroring the existing `_summary.json` plus the DB-write outcome, `full_trace_refs` pointing at the
dossier directory (dossiers themselves stay an ephemeral GitHub Actions upload, per CLAUDE.md rule 5 -- a
dossier can carry a full fetched page body). Per CONVENTION.md's own registration-order rule ("never a
family folded into an existing one just because it seemed similar"), this landed as a NEW family,
`inaccessible-triage`, not folded into `source-sweep`: it discovers no candidate URLs and writes only
`sources.fetch_status` on the rows it read, the opposite direction of every source-sweep walker.
Registered in `ALLOWED_FAMILIES`, `governing-files.mjs`, and CONVENTION.md (directory layout, prose,
standing metric, harness_version table row); `PENDING-RUN.md` written and hash-pinned (no live dispatch
possible from this lane -- SELECT-only, no network). Editing `governing-files.mjs`/`CONVENTION.md` also
moved `meta-harness`'s own hash (self-referential by construction) and editing `run-source-sweep.mjs`
moved `source-sweep`'s own hash; both families' `PENDING-RUN.md` markers were re-pinned in the same
commit, the same discipline every prior family registration in this repo has followed.

**Six-criteria acceptance (row M8).** Reachable: `--slice`/`--after` are on `run-source-sweep.mjs`, the
runner `source-sweep.yml` calls (quote: `parseArgs`'s new `slice`/`after` options, `usage()` updated to
match); `inaccessible-triage.mjs` is registered and its own artifact-write path is exercised end to end by
a real (non-faked) `claimRunId`/`writeRunArtifact` test against a temp directory. Run: the coordinator's
slice dispatches, after M1 lands (this lane's own dependency). Populated: the never-walked bucket to 0 --
not yet, current count 2,201/2,572, read back by this session and above. Gated: 89 new/updated unit tests
across `run-source-sweep.test.mjs` (77, up from 65) and `inaccessible-triage.test.mjs` (37, up from 27);
F28 and the full 41-function fitness suite both green (0 violations) on this tree. Documented: the
"Backfill in slices" and feed-walker-finding sections in `docs/runbooks/CORPUS-TURN-RUNBOOK.md`; the new
family's own CONVENTION.md entry.

**Test/tsc results.** `node --test scripts/turns/run-source-sweep.test.mjs`: 77 pass, 0 fail.
`node --test scripts/sources/inaccessible-triage.test.mjs`: 36 pass, 1 skip (the LIVE-gated smoke test,
unchanged, skipped by design without `LIVE_PROBE=1`), 0 fail. `node --test scripts/turns/research-sweep.test.mjs`:
31 pass, 0 fail (pre-existing, unmodified). `node --test .discipline/fitness/functions/F28-harness-run-integrity.test.mjs`:
33 pass, 0 fail. `node --test scripts/harness-runs/governing-files.test.mjs`: 20 pass, 0 fail.
`node --test scripts/lib/run-artifact.test.mjs`: 59 pass, 0 fail. `node .discipline/fitness/runner.mjs`:
41 functions checked, 0 violations. No `.ts`/`.tsx` file touched this lane, so `tsc --noEmit` was not run
per the common brief's own conditional.

**What this lane did not do (out of scope, named in the brief).** No fetch of `theloadstar.com/feed` or
any other network call (the feed-walker STOP above). No dispatch of the sitemap slices, the research
walker, or the triage ladder itself -- those are coordinator dispatches after M1 lands, per this row's own
"Depends on" column. `.github/workflows/source-sweep.yml` untouched (M1's file).
