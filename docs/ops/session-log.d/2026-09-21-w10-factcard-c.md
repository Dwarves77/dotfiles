## 2026-09-21, lane w10-factcard-c: the two live leftovers on the market detail

Worktree `C:/Users/jason/dotfiles/.worktrees/wt-w10-factcard`, branch `lane/w10-factcard-c`, cut
from `origin/master` at `c3199c05`.

### What landed

1. **Defect 1: inline list + bold inside a claim ("sentence. \n- **Name**, title...").**
   [CONFIRMED, by reading `toClaimNodes`, `src/lib/detail/fact-card-model.ts`] the bold-span
   regex (`\*\*(.+?)\*\*`) correctly isolates a `**bold**` run even after a newline, but the text
   BEFORE the bold run (everything up to and including the newline and the markdown list marker
   `- `) reached the returned node's `text` untouched - `stripStrayAsterisks` strips only `*`
   characters, never a newline or a leading dash. `ClaimNode[]` carries no list-item node type
   (the module's own header: pure inline nodes only), so the fix is inline per the brief's own
   fallback clause: `toClaimNodes` now folds an embedded list marker (at the very start of the
   text, or after a newline) into a single space, and collapses any remaining newline the same
   way, before the bold-span loop runs. Render test: `fact-card-model.test.mjs`, two new cases -
   the measured shape with a placeholder name (never a real person, integrity rule), and a
   marker at position 0 - asserting no `*`, no leading `- `, and the name survives as its own
   bold node. `FactCard.npmtest.mjs` pins that the bold node renders as a real `<strong>`, not
   `<b>` (the render-test target the brief names).
2. **Defect 2: bare URLs outside any `<a>` (five measured, market detail).**
   [CONFIRMED, by reading `extractProvenance`, same file] `extractProvenance`'s URL fallback
   (`URL_ANYWHERE_RE`, no `g` flag) claims only the FIRST bare url anywhere in a claim as this
   card's provenance. A claim carrying a SECOND, independent url - the exact shape amendment 1
   measured, "a citation line, publisher and date, then a bare URL" - left that second url sitting
   in the claim body, reaching `FactCard.tsx`'s `renderClaim`, whose non-bold branch wraps plain
   text in a `<span>` with no anchor. Both insertion points are the ONE shared path
   (`FactBlocks.tsx` -> `fact-card-model.ts` -> `FactCard.tsx`) every section on all four detail
   surfaces (Regulation, Market, Research, Operations) and the record-grade `RecordFactCard`
   claim path (`primitives.tsx`) already route through, so the fix in this one place covers all
   four surfaces in this commit - no second insertion point needed fixing. `ClaimNode` gains an
   optional `href`; `toClaimNodes`'s new `splitEmbeddedLinks` splits every REMAINING bare url in a
   node's text into its own link node, `text` set to `hostFromUrl(href)` - the same "host as
   visible text" anchor the provenance column already renders (F30), never a second url formatter
   (F45). `FactCard.tsx`'s `renderClaim` renders an `href` node as a real `<a target="_blank"
   rel="noopener noreferrer">`. Render test: `fact-card-model.test.mjs`, a claim with two embedded
   urls - the first lands in `provenance.href` (unchanged behaviour), the second survives as a
   link node, never bare text; `FactCard.npmtest.mjs` pins the `<a href={n.href}>` render branch
   exists and is checked before the bold/plain branch.

   **Two further insertion points considered, not modified this lane** (enumerated per the
   brief's "enumerate the insertion points" instruction, kept under the "more than four" STOP
   bound - two found, not four): `src/components/detail/primitives.tsx`'s `RecordFactCard`
   fallback branch (`{fact.text}`, line 71, fires only for a GAP row or a spanless FACT row) and
   `src/components/pages/MarketSignalDetailSurface.tsx:489` (`{r.conversionTrigger}`, a direct
   print of the `conversion_trigger` editorial-callout field). [HYPOTHESIS, unverified against the
   live item - no DB access from this worktree] neither is the measured shape: `fact.text` for a
   GAP row is always the fixed `GAP_MARKER` sentence (never a url) and a spanless FACT row's raw
   claim sentence is a lower-probability carrier; `conversion_trigger`'s field contract
   (`environmental-policy-and-innovation` skill) is short prose ("CORSIA Phase 2 review · Q4
   2026"), never a url, by format. Left unchanged; named here rather than silently dropped, per
   rule 13/14.
3. **`/admin/parts/fact-card` gains both fixtures.** `src/lib/detail/fact-card-fixtures.ts`:
   `INLINE_LIST_FIXTURE` and `EMBEDDED_LINK_FIXTURE`, both built through the real `toClaimNodes`
   (not hand-typed nodes), so the sign-off page shows the actual fix, never a re-typed
   reproduction of it. `fact-card-fixtures.npmtest.mjs` updated: the union-count assertion now
   expects six groups, plus two new tests pinning each fixture's claim shape.
4. This file.

### Gates

- `npx tsc --noEmit` - clean.
- `node --test src/lib/detail/fact-card-model.test.mjs` - 22/22 pass (2 new).
- `node --test src/components/ui/FactCard.npmtest.mjs` - 15/15 pass (1 new).
- `node --test src/lib/detail/fact-card-fixtures.npmtest.mjs` - 8/8 pass (2 new).
- `node .discipline/fitness/runner.mjs` - `46 function(s) checked, 0 violation(s)`.
- `bash .discipline/run-test-suite.sh` (the full no-npm suite) - `1480 tests, 1476 pass, 0 fail,
  4 skipped`; the trailing `audit-finding-status` step is report-only (`|| true`) and its 607
  unlabeled pre-existing findings are untouched by this lane.
- Full `*.npmtest.mjs` suite (coordinator amendment, the same `git ls-files 'fsi-app/**/*.npmtest.mjs'`
  glob CI's "App unit tests requiring npm deps" step uses) - `1435 tests, 1431 pass, 3 fail, 1
  skipped`. The 3 failures (`ProvisionalReviewTable.npmtest.mjs`; `SectionRule.coverage.npmtest.mjs`
  x2) are [CONFIRMED pre-existing] - reproduced identically with this lane's own diff `git stash`ed
  (i.e. at `origin/master`/`c3199c05`), unrelated to any file this lane touched
  (`ProvisionalReviewTable.tsx`, `SectionRule.tsx`, `SectionCard.tsx`, `ListSurfaceShell.tsx` - none
  of which this lane edited).
- Playwright is not installed on this PC (operator ruling, brief's own text); the rendering guard
  runs on GitHub. `git checkout --` was not needed for `coverage-report.json` - the fitness runner
  left it untouched (`git status --porcelain` after the run shows only this lane's own six edited
  files).
- Per the launching instruction for this lane (not the brief), this worktree commits only - no
  push, no PR, and the brief's own "locked push gate" step is therefore not run here.

### UX compliance

Both defects are fixed in the ONE shared render path (`fact-card-model.ts` / `FactCard.tsx`) every
FactCard consumer on all four detail surfaces already shares - no new component, no new page, no
new interactive element, no new state. The claim column's typography, spacing and the 66ch measure
are unchanged; a link node adds `text-decoration: underline` in the existing `--link-line` token
and opens in a new tab with `rel="noopener noreferrer"`, matching every other anchor this part
already renders (the provenance column's own link, `SourcesGrid`'s row link). No accordion, no
disclosure, no modal - DP-2/ux-laws' async-state and disclosure-default rules do not apply to a
text-node-shape fix.

=== Value Delivery Check ===

This dispatch closes a customer-facing rendering defect (raw markdown and bare urls visible inside
FactCard's claim text) on all four customer-facing detail surfaces (Regulations, Market Intel,
Research, Operations) - a direct customer-facing fidelity fix, not platform-admin tooling. The
`/admin/parts/fact-card` fixture additions (item 3) are the platform-admin sign-off picture for
that same fix, not separate new value.
