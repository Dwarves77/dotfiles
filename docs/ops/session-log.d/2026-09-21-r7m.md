## 2026-09-21, lane R7m: the loop manifest is a directory

What: `fsi-app/.discipline/governance/loop-manifest.mjs` held `LOOP_HOPS` as a hand-edited 11-entry array
literal, touched by 2 of the last 30 first-parent commits of `origin/master` (window positions 11 and 17),
category 48's shape, "a registry is a directory." Converted:

1. `fsi-app/.discipline/governance/loop-hops.d/<order>-<hop-id>.json`, one file per hop, all 11 of today's
   entries carried over byte-for-byte in meaning (`id`, `producer`, `consumer`, `trigger`, `family`,
   `enforceEdge`, `enforceFired`, `consumerPending` where present, `note`). Verified lossless by directly
   diff-comparing `JSON.stringify` of the pre-conversion hard-coded array (read from `origin/master`) against
   `JSON.stringify` of the new directory-derived array: byte-identical.
2. `loop-manifest.mjs` now exports `loadLoopHops(dir)` (pure) and `LOOP_HOPS = loadLoopHops(LOOP_HOPS_DIR)`
   (frozen, derived at import by reading `loop-hops.d/` sorted by filename). No consumer's import changes
   (checked: `F50-loop-wiring.mjs`, `loop-manifest.test.mjs` both still `import { LOOP_HOPS } from
   './loop-manifest.mjs'`; `loop-run-id.mjs`/`run-artifact.mjs`/`RD-74-loop-hop-wiring.mjs` reference the
   module only in prose, no import). The per-hop header commentary that used to live at the top of
   `loop-manifest.mjs` now lives in each hop's own `note` field.
3. `loop-manifest.test.mjs`: added three attack tests against `mkdtempSync` fixture directories (never the
   live one), a duplicate hop id across two files throws; a duplicate order prefix across two files throws;
   a file missing a required field throws naming the file. Added one shape assertion (`LOOP_HOPS` is a
   frozen, non-empty array of frozen hop objects) rather than a pinned content snapshot, the R6t/
   remediation-discipline lesson ("no pinned list of live entries"): the lossless-conversion proof (item 1
   above) was verified once, by hand, at conversion time, and is not re-asserted as a permanent test.

STOP (brief item 4, F51): the brief's step 4 required adding `loop-manifest.mjs` to F51 check 1's
converted-registry set and `loop-hops.d/` to the entry-directory set, with the instruction "If F51's own
file would become a hotspot by this range, STOP with the measurement." Measured:
`git log --first-parent -n 30 --pretty=format:'%H %s' ccb6aa0c091aba55f6e85d93ecc704c218acc20c..origin/master
-- fsi-app/.discipline/fitness/functions/F51-no-shared-append.mjs` returns 3 commits (2413b74f "Lane F51b",
cc038a47 "Lane G1", 0dd33c09 "Lane N6"). `F51-no-shared-append.mjs` is already at the hotspot threshold (3)
in the anchor window. This lane's own range editing that same file would add a 4th touch (masterCount 3 + 1
= 4, at or above threshold 3), which is exactly the condition the brief named to stop on. I wrote the F51
edit (check 1 pattern for `loop-manifest.mjs`, `loop-hops.d/` added to `ENTRY_DIRS`, a dated
`HOTSPOT_ALLOWLIST` entry for `loop-manifest.mjs`), confirmed it worked in isolation, then reverted it
(`git checkout -- fsi-app/.discipline/fitness/functions/F51-no-shared-append.mjs`) rather than commit it,
per "No workaround of any kind: no exclude, no flag, no allowlist beyond the one ruled." Landing it would
itself create the exact hotspot condition the coordinator's own gate is built to catch. Item 4 is therefore
NOT done; it needs a coordinator ruling (a dated `HOTSPOT_ALLOWLIST` entry for `F51-no-shared-append.mjs`
itself, or a decision to land the check-1/entry-dir change in a range that does not also touch anything
else hot) before another lane can land it.

Item 5 (skill-ack): not needed. `remediation-discipline`'s own SKILL.md was not edited, and no gate asked
for an ack file during this run.

Gates: `node --test .discipline/governance/loop-manifest.test.mjs` (13/13 pass); full discipline suite
`bash .discipline/run-test-suite.sh` (6309+1435 tests across two suite runs, 0 fail, 1 skipped); fitness
runner and push-gate results recorded in the lane's final report.

UX compliance: not applicable, no `.tsx`/`.css` touched.
