## 2026-09-19, lane N2: harness family descriptors built; the two reported blockers closed by Amendment 2 plus a third occurrence this lane found while fixing them; gate green

I am lane N2, branch `lane/n2-harness-family-descriptors-2026-09-18`, cut from `origin/master` at `25418482`, worktree `/home/user/dotfiles/.worktrees/wt-n2`. Amendment 1 (2026-09-19) moved this to a cloud coordinator and forbids me from editing `DUPLICATED_LINES_CEILING`.

**What I built.** `fsi-app/scripts/harness-runs/family-registry.mjs` (new, `node:` builtins only): reads every immediate subdirectory of `scripts/harness-runs/` that contains a `family.json`, validates it (family equals the directory name, `governing_files` a non-empty array of strings, no unknown keys, `registered` a real date), throws a named `FamilyDescriptorError` on the first invalid descriptor, exports a frozen `FAMILIES` array sorted by `registered` then `family`. One `family.json` per existing family, all thirteen (`mint`, `screen`, `fetch-drain`, `meta-harness`, `forward-events`, `source-sweep`, `ledger-consume`, `change-detection`, `propagation`, `corpus-turn`, `brief-apply`, `inaccessible-triage`, `maintenance`); `registered`/`registered_by` established from git-shallow-safe evidence inside the tree itself (meta-harness-run-001/002/003's own `started_at` fields, each documented in their own `proposer_notes` as "the commit's own author date," pin Wave MH-1/MH-2/MH-3 to 2026-09-01; `meta-harness-run-004`'s `config.wave` pins the forward-events registration to the same day), never guessed. `governing-files.mjs`'s `GOVERNING_FILES` and `run-artifact.mjs`'s `ALLOWED_FAMILIES` are now DERIVED from `FAMILIES`, byte-for-byte equal to the pre-lane hand-written versions apart from the one deliberate addition my brief asked for: `GOVERNING_FILES['meta-harness']` now also names every family's own `family.json` path (13, sorted, appended after the five original files), so a registration still moves meta-harness's own hash. `CONVENTION.md`'s per-family directory-layout enumeration and its harness_version markdown table are gone, replaced by one generic `<family>/` entry and a "Registering a family" section; the per-family prose that used to sit there moved into ten new `FAMILY.md` files (mint/screen/fetch-drain had no standalone prose block, so they got none). `F28-harness-run-integrity.test.mjs`'s `CONVENTION-TABLE-PARITY` test is replaced by `FAMILY-DESCRIPTOR-REALITY` (every `governing_files` path of every family exists on disk); `governing-files.test.mjs` needed no change (checked: 20/20 pass unmodified). `PENDING-RUN.md` re-stamped last, one current `harness_version at write time:` line (`sha256:7fd76beaa3b4eb6d`, 18 governing files), the previous line reworded superseded.

**Correction I made mid-lane.** Adding `family.json` inside each family directory broke `scripts/lib/run-artifact.mjs`'s `readRunHistory` (its own `*.json` glob picked up `family.json` as a fake run artifact and reported it invalid). `readRunHistory` is in my write set, so I fixed it there: exclude the literal filename `family.json` from that glob. This closed the previously-failing CLI integration test in `run-artifact.test.mjs`.

**Blocker I did not work around.** The same collision recurs in `.discipline/fitness/functions/F28-harness-run-integrity.mjs`'s own, separate `scanArtifacts` (its family-level `*.json` glob also matches `family.json`), and in `scripts/forward-events/run-extraction.test.mjs`'s own substring check against `CONVENTION.md`'s now-removed per-family text. Both files are outside my write set (the first explicitly forbidden; the second never named). Fixing either means editing a file I am not authorized to touch, so I stopped short of the push gate and reported both, with the exact one-line fix each needs, in my dispatch report instead of patching around them.

**Tests.** `family-registry.test.mjs` (new): 22/22 pass, red-first fixtures for every refusal plus the two-new-families-side-by-side case that makes the 2026-09-18 collision impossible. `governing-files.test.mjs`: 20/20 unchanged. `run-artifact.test.mjs`: 71/71 (was 70/71 before my `readRunHistory` fix). `F28-harness-run-integrity.test.mjs`: 31/33 (2 fail, the confirmed blocker above; was 33/33 before family.json existed). Full `run-test-suite.sh`: 7367/7375 pass, 3 fail (the two F28 tests plus the one forward-events test above); every other one of 7372 other tests, including the entire discipline and fitness suite apart from F28's 13 INVALID ARTIFACT violations, is unaffected.

**Resolved (Amendment 2, coordinator, 2026-09-19 16:39 UTC).** My STOP stood; the coordinator extended the write set by exactly the two fixes named above, generalized into one shared predicate: `scripts/lib/run-artifact.mjs` now exports `isRunArtifactFilename(name)`, matching the convention's `<family>-run-NNN.json` shape and nothing else. `readRunHistory` uses it (replacing the literal-name exclusion); `F28-harness-run-integrity.mjs`'s `scanArtifacts` uses it too (the one edit Amendment 2 permits there, nothing else in that file touched). Before switching, I listed every `.json` directly under `scripts/harness-runs/*/`: every file other than the thirteen `family.json` descriptors already matches `<family>-run-NNN.json` exactly, so no STOP was warranted. While making this fix I found a THIRD, previously unreported occurrence of the identical defect class: `F28-harness-run-integrity.test.mjs`'s own "sanity: every artifact..." test has its own independent `readdirSync` filter, never routed through `scanArtifacts`, also fixed to use `isRunArtifactFilename` (Amendment 2's "zero fail now expected" for step 6 could not otherwise be met). `run-extraction.test.mjs`'s "FORWARD_EVENTS_GOVERNING_FILES matches..." test now deep-equals `FAMILIES.find(f => f.family === 'forward-events').governing_files` (family-registry.mjs) instead of checking a substring against `CONVENTION.md`. `PENDING-RUN.md` re-stamped a second time (`sha256:2dc276f2cc9c8f53`, same 18 files, only `run-artifact.mjs`'s and `F28-harness-run-integrity.mjs`'s content moved the hash), the prior line reworded superseded.

**Final state.** `node --test fsi-app/scripts/harness-runs/*.test.mjs fsi-app/.discipline/fitness/functions/F28-harness-run-integrity.test.mjs fsi-app/scripts/lib/run-artifact.test.mjs`: 160/160 pass, 0 fail (was 131/131/0 before this lane, 153/151/2 after the first round, 160/160/0 now: +7 for `isRunArtifactFilename`'s own unit tests). Full `run-test-suite.sh`: 7393/7393 pass, 0 fail. `node .discipline/fitness/runner.mjs`: 44 functions checked, 0 violations. F45: measured count equals the committed ceiling; `DUPLICATED_LINES_CEILING` untouched throughout, per Amendment 1.

### UX compliance (N2)

Not a UI change; no customer surface touched by this branch.
