
## 2026-09-18, W9 lane m2: ledger-consume apply wired behind a max-promote cap, LEDGER_CONSUME_APPLY_ENABLED retired

I read `.github/workflows/ledger-consume.yml`, `scripts/turns/run-ledger-consume.mjs`,
`src/lib/intake/portal-harvest.ts`, all seven prior `ledger-consume-run-*.json` artifacts,
`scripts/harness-runs/CONVENTION.md`'s ledger-consume section, and the ledger-consume section of
`docs/runbooks/CORPUS-TURN-RUNBOOK.md`, per the brief. Apply mode's real write path, traced with file
and line before I changed anything: `portal-harvest.ts`'s `stamp()` (around line 363) updates
`portal_link_candidates.status` to 'promoted' or 'rejected' plus `disposition_reason`/`dispositioned_at`/
`item_id`; a genuinely new mint goes through `runIntakeCycle` (mode apply) to the single mint chokepoint
and writes `intelligence_items` directly. Apply does NOT write `census_worklist` at any point; that table
is only READ (plan mode's exclusion query) by this family and is written only by the separate batch path
(`apply-mint-batch.mjs`, fed by `census_worklist` itself, wired to `population-turn.yml`). The plan's own
loop diagram and my brief's "Populated" acceptance line both assume consume writes census_worklist; the
code does not. Flagged below rather than invented, since item 1 says write down what I find, not fix a
mismatch.

What landed: (1) `--max-promote` CLI flag (`parseArgs`, `run-ledger-consume.mjs`), default 50, hard
ceiling 200 refused, not clamped; the pure slicer `applyPromoteCap` (new file
`src/lib/intake/promote-cap.mjs`, zero imports) takes the oldest-first eligible list and returns
`{toPromote, deferred}`, wired into `portal-harvest.ts`'s apply branch; a deferred row gets
`disposition:"skipped"`, reason `capped: max_promote=N reached...`, left `status='candidate'` untouched;
`config.max_promote` recorded every run, `metrics.capped` counts deferrals, every `per_item` row now
carries `before`/`after` ledger status (`ledgerStatusAfter`). (2) `LEDGER_CONSUME_APPLY_ENABLED` deleted,
the constant, its declaration, and every functional read (`isApplyArmed` now takes only
`{verdictsGiven}`); no new flag replaces it. (3) `.github/workflows/ledger-consume.yml`: the
`workflow_run` job now runs plan then a new chained apply step (`--mode apply --max-promote 50`), gated
on `run_apply_chain` (armed by the same `run_consume`/D26-part-g decision the plan pass already makes);
`workflow_dispatch` gains `max_promote` (default '50'); a new step exports
`GITHUB_EVENT_WORKFLOW_RUN_ID` from `github.event.workflow_run.id`, read back as
`config.upstream_run_id`. (4) Tests, attack form: unit tests on `applyPromoteCap` (60 items/cap 50
promotes exactly the 50 oldest; cap 0; smaller-than-cap), `parseArgs` `--max-promote` bounds, and a yml
golden test that fails if the chained-apply step or its gate is removed or renamed. (5) Local plan-mode
run (`--mode plan --limit 5`, $0, no DB write per plan mode's own contract) committed as
`ledger-consume-run-008.json`; the now-superseded `PENDING-RUN.md` deleted; `LAST-PROPOSER-PASS.md`
gained a proportionate entry naming run-008 (a bounded local verification probe, "none warranted" for a
code proposal, per F28 rule (d)'s own "none warranted, with basis" escape valve). (6) Runbook
(`CORPUS-TURN-RUNBOOK.md`, "Ledger consume" section) and `scripts/harness-runs/CONVENTION.md` updated to
describe the cap instead of the retired constant.

Evidence per section 0: Reachable, the yml's `workflow_run` job has the chained apply step with
`--mode apply`, quoted in the golden test. Run, this lane's own local plan-mode probe
(`ledger-consume-run-008.json`, `config.mode:"plan"`); the coordinator's first real apply run (armed with
an explicit `--verdicts` file) is the evidence apply actually fired, not produced by this lane. Populated,
not produced here (my run was `--limit 5`, no verdicts, classified 2 for $0, minted nothing;
`portal_link_candidates.status='promoted'` unchanged at 3); the coordinator's apply dispatch is the
evidence, and (see Problems below) it lands in `intelligence_items`, not `census_worklist`. Visible,
population-report's existing candidate-count line is unchanged; I did not add a new one since none was
named missing. Gated, the yml golden test plus the `applyPromoteCap`/`parseArgs` unit tests. Documented,
the runbook section and CONVENTION.md above.

Test and tsc, verbatim: `node --test scripts/turns/run-ledger-consume.test.mjs`, tests 128, pass 128,
fail 0. `npx tsc --noEmit`, 0 errors. `node .discipline/fitness/runner.mjs`, 41 functions checked, 0
violations (F28 initially flagged the governing-files hash drift from my own edits; resolved by the
run-008 artifact plus the LAST-PROPOSER-PASS.md entry above; the re-run after every edit confirms green).
A second, smaller F28 consequence: fixing a stale `LEDGER_CONSUME_APPLY_ENABLED` reference in
`scripts/harness-runs/CONVENTION.md` (one of `meta-harness`'s own governing files) moved that family's own
hash too; resolved with a "Re-pin 4" entry in `scripts/harness-runs/meta-harness/PENDING-RUN.md`, following
the file's own established re-pin convention (three prior re-pins already there) rather than inventing a
new mechanism. Final combined run confirms all three green together: tests 128/128, tsc 0 errors, fitness
41 functions checked, 0 violations.

Problems, not stops, flagged per the execution rule: (a) brief/code disagreement: the brief's "Populated"
criterion names `census_worklist`; the real apply write path never touches that table, see the trace
above; the coordinator should correct the acceptance criterion or clarify which mint path M2 was meant to
cap. (b) the chained apply pass and the plan pass both scan up to `limit=2000` ledger rows on every
source-sweep completion even though promotion itself is capped at 50; not a spend issue (workflow_run
never sets `--allow-api`, so $0 either way) but redundant read/fetch work; item 4 named `--max-promote 50`
for the apply pass only, not a matching `--limit`, so I left `--limit` at the plan pass's own value rather
than inventing a change the brief did not ask for. (c) the chained apply pass on the automatic
`workflow_run` trigger never names `--verdicts`, so under D26's (pre-existing, out of scope) arming rule
it always runs disarmed; intentional per the brief's own acceptance line ("Run: the coordinator's first
apply after M1's sweep"), restated here so it is not mistaken for a defect. Out of scope, not touched:
`source-sweep.yml`, `population-turn.yml`, `corpus-turn.yml`, `downstream-chain.yml`, the four
review-apply ratification scripts, any schema change, and the census_worklist mismatch above.

## 2026-09-18, W9 lane m2 (correction): chained apply auto-arms on committed verdicts, verdicts_owed telemetry, populated criterion restated

Coordinator correction on the lane m2 report above. Verbatim: the chained apply step named no
`--verdicts`, so it stayed DISARMED until an operator dispatched with a `verdicts_file`, which keeps a
person inside the hop, the exact defect this lane exists to close. The machine design: session lanes
write verdict batches (the human judgment, committed under `scripts/turns/ledger-verdicts/`), and the
chained apply promotes every candidate that already has a committed verdict by itself, capped, skipping
the rest. Three corrections landed on top of the commit above, same branch, same worktree:

1. **Arming redesigned.** `isApplyArmed` (`run-ledger-consume.mjs`) no longer reads `verdictsGiven`
   (whether THIS dispatch named an explicit `--verdicts <path>`); it reads `verdictsFilesCount`, the
   number of committed batches this run actually read, auto-discovered or named, either way. Armed
   whenever that count is greater than zero; the ONLY disarmed case is zero committed batches at all.
   `resolveApplyGate`'s disarm message, `buildRunArtifact`'s proposer notes, and every prose reference in
   the driver's own header and `.github/workflows/ledger-consume.yml` (the MODES paragraph, the
   `verdicts_file`/`mode` input descriptions, the "This run's gates" NOTE, the chained-apply step's own
   comment, the `run_apply_chain` decision comment, the PR body text) were rewritten to match. The chained
   `workflow_run` apply step's own args are UNCHANGED (`--mode apply --max-promote 50`, no `--verdicts`
   flag needed): it now arms itself off whatever a session lane has already committed, the moment a first
   batch lands, no operator flip required for that half of the decision. `config.verdicts_files` (the
   array) and `config.apply_disarmed:false` were already present fields once armed; `config.verdicts_given`
   is now purely informational (whether this ONE dispatch named an explicit path), never the arming input.
   Five yml golden tests plus two new `isApplyArmed`/two `resolveApplyGate` composition tests updated to
   match (`run-ledger-consume.test.mjs`).
2. **The human half is no longer silent.** `metrics.verdicts_owed` (same value as the existing
   `without_verdict_skipped`, renamed for a reader who should not need the internal shaping-function name)
   lands on every plan AND apply artifact, this run's own window. Corpus-wide, `population-report.mjs`
   gained `computeVerdictsOwed` (pure: total minus matched, floored at 0), `loadCommittedVerdictedUrls`
   (reuses `discoverVerdictsFiles`, imported from `run-ledger-consume.mjs`, never a second directory walk),
   and `countCandidatesAwaitingVerdict` (one aggregate `count()` for the total, then a CHUNKED `.in()`
   count for the matched set, chunk size 150, marked `// fitness-allow: F39` as an already-bounded slice,
   the sanctioned exemption shape, never the unbounded runtime list F39 exists to catch). The CLI prints
   one line, `ledger-consume: N candidates await a verdict`, never a `--strict` gate (an empty
   `ledger-verdicts/` directory is a legitimate mid-build state). New tests: 3 on `computeVerdictsOwed`
   (plain case, floored at 0, null-safe), 3 on `loadCommittedVerdictedUrls` (union with duplicates, missing
   directory, malformed file skipped), 3 on `countCandidatesAwaitingVerdict` (arithmetic via an injected
   client stub, zero-verdicted-urls short-circuit, error surfaced not thrown), plus one assertion that
   `metrics.verdicts_owed` on the driver's own artifact equals `without_verdict_skipped`.
3. **"Populated" restated, my trace stands.** Per the coordinator's own correction: `census_worklist` is
   NOT written by this family, confirmed a second, independent way this pass (`writeCensusRows`,
   `src/lib/intake/census-writer.mjs`, the only function that writes that table from a consume-outcome
   shape, is called only from `apply-mint-batch.mjs`/`run-source-sweep.mjs`, never from
   `run-ledger-consume.mjs`/`portal-harvest.ts`). "Populated" for this family means, and only means:
   `portal_link_candidates.status` moving from `'candidate'` to `'promoted'`/`'rejected'` (`stamp()`,
   `portal-harvest.ts`, unchanged by either correction), and `intelligence_items` gaining minted rows
   through the SAME single mint chokepoint (`mint-item.ts`) `/api/agent/run` uses, via `runIntakeCycle`.
   This is a restatement, not a new finding: my earlier report already traced this path file-and-line; the
   correction is in how the brief's own acceptance criterion should be read against that trace, not in the
   trace itself.

Ratchet gates (common brief, run after the three corrections above, in the order named): F45
duplicate-code, F39 unbounded-in-filter, F47's own test, F28's own test with the harness artifact/re-pin
written LAST (this family's own governing files moved again from the arming rewrite), plus `node --test`
on both touched test files and `npx tsc --noEmit`. Verbatim counts in the commit this entry lands with.

## 2026-09-18, W9 lane m2 (second correction): countCandidatesAwaitingVerdict routed through readAllByIds, not a second chunking loop

Coordinator caught, before push, that `countCandidatesAwaitingVerdict` (`population-report.mjs`) open-coded
a chunked `.in()` loop and silenced F39 with `// fitness-allow: F39` rather than routing through
`readAllByIds` (`scripts/lib/db.mjs`), the shared chunked-id-read home (IN-CHUNK, 2026-09-06). Fixed: the
loop and `VERDICT_OWED_CHUNK` deleted; the matched-count now reads
`readAllByIds("portal_link_candidates", "url", urls, {idColumn:"url", client:sb, match:(q)=>
q.eq("status","candidate")})`. This also fixed a latent double-count the old loop had (a url repeated
across two committed verdict batches, landing in two different chunks, would have matched the same
UNIQUE-constrained row twice; `readAllByIds` dedupes its id list before chunking). Six
`countCandidatesAwaitingVerdict` tests rewritten to model what `readAll`/`fetchAllRows` actually issue
(`{data, error}` row reads through `.select().order().range().in().eq()`, not `{count, error}`), not the
old hand-rolled loop; `computeVerdictsOwed`/`loadCommittedVerdictedUrls` untouched.

**`[REFUTED]` in part:** the coordinator's message stated "master has ZERO uses of [`fitness-allow: F39`]".
`[CONFIRMED]` false as a whole-codebase claim: `git grep -c "fitness-allow: F39" -- fsi-app/src
fsi-app/scripts` returns matches in 52 pre-existing, already-committed files (e.g. `src/lib/trust.ts:777`,
committed 2026-09-06, `git blame` confirmed, no uncommitted changes to that file). The marker is used
elsewhere in the codebase already; it is not a first bypass. What stands, and is the part that matters: MY
OWN one instance (the loop I wrote) is now fully removed, `git grep -c "fitness-allow: F39" --
fsi-app/scripts/verify/population-report.mjs` reads 0, and no new second chunking implementation was
added, the substantive instruction. The literal codebase-wide-zero gate the coordinator named is not
achievable by this lane (those 52 markers predate and are unrelated to this work) and is not reported as
achieved.

**Leaked-state repair, found while running the gates in order:** F28's own check failed for `ledger-consume`
(not `population-report.mjs`, which is not a governing file of any family, confirmed via `governing-files.mjs`)
because commit `e8e86f76` fixed a pre-commit glyph violation in `run-ledger-consume.mjs` (one of that
family's own governing files) AFTER `ledger-consume-run-009.json` had already been generated, and the full
fitness suite was not re-run after that specific edit before the commit landed. Repaired: a fresh local
plan-mode probe, `ledger-consume-run-010.json` (`harness_version:sha256:b1abea99a2de2d4e`, matching the
current tree), and a matching `LAST-PROPOSER-PASS.md` entry naming exactly what moved the hash and why.

Gates, verbatim: `node .discipline/fitness/runner.mjs --function=F39`, PASS (0 violations).
`--function=F45`, PASS at the committed ceiling (6227, unmoved, the removed loop was not itself a
duplicate of anything else in the tree so nothing to re-seed down). `--function=F28`, FAILED on the
leaked-state issue above until run-010 landed, then PASS. F47's dedicated test file, 6/6 pass. Both
touched test files, `node --test scripts/turns/run-ledger-consume.test.mjs
scripts/verify/population-report.test.mjs`, 194/194 pass. `npx tsc --noEmit`, 0 errors. Full
`node .discipline/fitness/runner.mjs`, 41 functions, 0 violations, run last, after run-010 landed.
`population-turn.yml`/`POPULATION-TURN-RUNBOOK.md` untouched per the coordinator's instruction (lane M3's
own brief now carries those stale mentions).
