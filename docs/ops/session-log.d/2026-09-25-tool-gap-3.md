# 2026-09-25, Lane TOOL-GAP-3 (UI-side orphan checker + harness-family schedule walker)

Brief: `docs/plans/data-machine-tool-gaps-2026-09-25.md` build order step 3, "UI-side orphan checker +
harness-family schedule walker, read-only, extends F14/F50 patterns; closes audit coverage gaps 5 and 6."
Closes audit findings from `docs/audits/supabase-integrity-and-wiring-audit-2026-09-25.md` coverage checks
5 (unwired UI parts) and 6 (unrun producers). Worktree `.claude/worktrees/tool-gap-3`, branch
`lane/tool-gap-3`, off `origin/master` (48525148, TOOL-GAP-2 merged). R14: read-only, SELECT-only against
live data throughout; every write below is to a new tool file, allowlist JSON, or this log, zero rows in
any application table were mutated.

## Built

- `fsi-app/scripts/verify/lib/ui-orphan-scan.mjs` + `.test.mjs` (18 tests), pure core, the FIELD-grain
  sibling of F14 (`.discipline/governance/producer-consumer-orphan.mjs`, which is table-grain). Parses
  PostgREST `.select("...")` strings into (table, column) pairs (bare columns, `alias:column`,
  `embed_table(nested...)` recursion); extracts writer columns from `.insert/.update/.upsert({...})` object
  literals, the guarded-write helpers (`guardedInsert`/`guardedInsertMany`/`guardedUpdate`/`insertFn`), SQL
  `INSERT INTO (...)`/`UPDATE ... SET`, and RPC calls joined to their own migration function body's writes
  (coarse, whole-body join, same granularity note F14 already carries for RPCs).
  - **False-positive correction, made same-session before reporting** (see Corrections): the first live run
    against 472 UI-facing files found 185 "orphan" fields, almost all fields written through a helper
    function's row-builder (e.g. `scripts/gen/emission-factors-common.mjs`'s `buildRow()`, called as
    `insertFn("emission_factors", toWrite, {...})` where `toWrite` is a variable, not an inline object
    literal this parser can trace). Added `extractOpaqueWriteTables()`: any write call whose payload is NOT
    a literal object (or `[{...}` array-of-objects) marks that WHOLE table's columns as
    write-uncertain and suppresses them from column-level findings, conservative by construction (trades a
    possible missed finding for eliminating the high-false-positive failure mode F14's own header already
    names as the reason field-level analysis stays informational there). Live count dropped 185 → 16 after
    this fix.
- `fsi-app/scripts/verify/ui-orphan-audit.mjs`, runner. `// data-audit: label=ui-orphan hard=false`. Scope:
  API routes (`src/app/**/route.ts`), pages (`src/app/**/page.tsx`), `src/components/**/*.tsx`, and
  `src/lib/supabase-server.ts` (CLAUDE.md's own Key Files entry for "the server-data layer feeding pages").
  Writer corpus is broader (all of `src/`, `scripts/`, migrations, `supabase/functions`), matching F14's own
  writer-scan scope. Reuses `information-schema-scan.mjs` (shared, TOOL-GAP-2) for the live schema and
  `dead-column-scan.mjs`'s `scopedColumns` (excl. PK/FK/generated/timestamp, same scoping reasoning: those
  columns are "used" by a join/default/clock, not a literal write-site identifier).
- `fsi-app/scripts/verify/ui-orphan-allowlist.json`, empty by design (see Live run, the one live finding
  candidate for allowlisting, `state_cost_facts`, is a RULED build item, not an allowlist candidate).
- `fsi-app/scripts/verify/lib/harness-family-walk-scan.mjs` + `.test.mjs` (8 tests), pure core, extending
  F50's (`fitness/functions/F50-loop-wiring.mjs`) "every hop fires" pattern past the 11 named loop hops to
  EVERY registered harness family. Reuses `family-registry.mjs`'s `loadFamilies()` (no second family-listing
  scan) and F50's own `<family>-run-NNN.json` naming convention for run artifacts. Summarizes each family's
  dispatch history (run count, latest run id/`started_at`/`trigger`) and flags zero-dispatch families.
- `fsi-app/scripts/verify/harness-family-schedule-walker-audit.mjs`, runner. `// data-audit:
  label=harness-family-schedule-walker hard=false`. Per rule 16 (CLAUDE.md), this script's output is
  strictly factual (which families have ever been dispatched, which have not, each family's last run) and
  NEVER recommends flipping the build-mode cadence gate, verified by direct read of the runner's own text,
  no "enable a schedule" language anywhere in it.
- `fsi-app/scripts/verify/harness-family-walk-allowlist.json`, empty by design (no family is currently
  known-and-ruled as an acceptable permanent zero-dispatch state; every zero-dispatch family below is
  tracked under the build order's own later steps, not silenced here).

## Consumers checked

- `run-data-audit-lane.mjs`'s `deriveAudits()` scans `scripts/verify/*.mjs` for the `// data-audit:`
  marker (confirmed by direct read, same mechanism TOOL-GAP-2 used), both new audits are auto-registered,
  no manual edit to that file needed.
- `family-registry.mjs`'s `FAMILIES`/`loadFamilies()`: read in full before reuse; the walker calls
  `loadFamilies()` directly rather than re-deriving the family list, and reuses `HARNESS_RUNS_DIR` for path
  resolution so a future family-directory move needs no update here.
- `F50-loop-wiring.mjs`'s `familyFiredStatus()`: read in full before writing the walker, to keep the
  `<family>-run-NNN.json` naming convention and the `trigger` field read consistent between F50 (11 named
  loop hops, `workflow_run`-only) and this walker (every family, any dispatch kind), the two checkers now
  agree on what a run artifact looks like, deliberately disagree on what "fired" requires.
- `dead-column-scan.mjs`'s `scopedColumns`: reused directly (import), not re-implemented, per F45 ratchet.
- `information-schema-scan.mjs`: reused directly (import), per the brief's explicit reuse instruction.

## ADRs checked

`grep -ril -e "ui.orphan" -e "producer.consumer" -e "harness.family" -e "schedule.walk" docs/decisions/`
returned nothing on point. No ADR constrains either checker's thresholds or scope.

## Live run (project `kwrsbpiseruzbfwjpvsp`, read-only, direct Postgres via `scripts/lib/pg-conn.mjs`'s
pooler resolver, same transport TOOL-GAP-2 used; `.env.local` present in this worktree, read from the main
checkout, gitignored/untracked, never committed)

**ui-orphan-audit.mjs**: 472 UI-facing files scanned, 1,176 select-list entries parsed, 2,000 writer-corpus
files scanned, 633 distinct writer-column keys, 58 tables suppressed for having at least one opaque
(non-literal-payload) write call, 0 allowlisted. **16 UI-orphan fields found** `[CONFIRMED, live run,
method above]`:

- `state_cost_facts.{state_code,state_label,dimension,fact_label,value,unit,trend,statute_citation,
  origin_class}` (9 columns), `[CONFIRMED]` this IS audit finding RW-2/UI-3, re-detected here
  mechanically: read by `src/app/api/ask/route.ts:245` and `src/lib/supabase-server.ts:3507`, written by
  nothing. Already a RULED build item (operator ruling 2026-09-25, data-machine-tool-gaps register build
  order step 4: "build the producer"). Left as a live finding, not allowlisted, per this lane's own
  allowlist header, an allowlist entry would misrepresent a ruled-but-not-yet-built gap as "reviewed and
  accepted," which it is not.
- `org_invitations.token`, `community_group_members.muted`, `source_bias_tags.{dimension,tag,confidence}`,
  `sector_contexts.display_name`, `region_dimension_coverage.notes` (7 columns across 5 tables), `[HYPOTHESIS]`, newly surfaced by this pass, not independently re-verified beyond this parser's own
  method (no second read of each table's full writer surface was done this session, the parser's opaque-
  write suppression already removes the highest-confidence false-positive class, but a residual false
  positive from a write path this parser's regex set does not recognize at all, e.g. a raw `pg` client
  query string built by concatenation, cannot be ruled out without per-table manual confirmation). Flagged
  for coordinator triage, not asserted as confirmed defects.

**harness-family-schedule-walker-audit.mjs**: 17 registered harness families walked, 11 with dispatch
history, **6 zero-dispatch producers** `[CONFIRMED, live filesystem read, method above]`:
`inaccessible-triage`, `maintenance`, `downstream-chain`, `brief-export`, `producers`, `gate-a-rescan`. Per
rule 16 this is EXPECTED under build-mode/ADR-023 (most loop hops are "built-not-fired" per the
data-machine-tool-gaps register itself), not a defect, the script's own output states this and recommends
no cadence change. Cross-checked against the register: `downstream-chain`, `brief-export`, `gate-a-rescan`
match F50 hops already known `enforceFired:false`; `inaccessible-triage`, `maintenance`, `producers` are
NEWLY confirmed zero-dispatch by this walker (not previously named in the register's own table, which only
sampled `connection_theme_runs` per audit finding PROD-2's own admission of partial coverage).

## Gates

- `node --test fsi-app/scripts/verify/lib/ui-orphan-scan.test.mjs`: 26/26 pass (includes the opaque-write
  suppression tests added mid-session, see Corrections).
- `node --test fsi-app/scripts/verify/lib/harness-family-walk-scan.test.mjs`: 8/8 pass.
- `cd fsi-app && bash .discipline/run-test-suite.sh`: 1632 tests, 1628 pass, 4 skip, 0 fail.
- `node fsi-app/.discipline/fitness/runner.mjs`: 47 functions checked, 0 violations.
- `node fsi-app/.discipline/runner.mjs --mode=ci --range=origin/master..HEAD`: 4 pass, 0 fail, 6 skip.
- `node fsi-app/.discipline/consistency/override-check.mjs --range=origin/master..HEAD`: clean, no drift.
- No `.ts`/`.tsx`/`.css` touched, so `tsc --noEmit` and the rendering guard are not applicable to this
  diff.

**Known, pre-existing flake found while running the full wiring preflight (`DISCIPLINE_HOOK_TRAMPOLINE=1
sh fsi-app/.discipline/hooks/pre-push`), not caused by this diff** `[CONFIRMED, isolated repro below]`:
the preflight's step 3 runs `sh fsi-app/.discipline/run-test-suite.sh` and checks its SHELL exit code.
That script's core line is `node test-discovery.mjs --print0 | xargs -0 node --test`; run directly (both
via the full discovery list and independently), `node --test`'s own printed summary is clean (0 fail, 0
cancelled) but the process itself exits with a non-zero code, which `xargs` reports as 123. This
reproduces with a bare `node --discovery --print0 | xargs -0 node --test` outside any of this lane's own
files, so it predates this diff. It is also NOT part of what the lane-common-contract's own numbered gate
list checks: gate 1 there is `bash run-test-suite.sh 2>&1 | grep -E "^# (tests|pass|fail)"`, deliberately
piped through `grep` rather than checked by shell exit code, and TOOL-GAP-2's own session log
(`2026-09-25-tool-gap-2.md`, Gate outputs section) reports only that grep-based check, the fitness runner,
`tsc`, and F45, with no mention of running the full `pre-push` hook end to end. Root cause is outside this
lane's write set (a Node test-runner / xargs exit-code interaction, not this lane's audit scripts); flagged
for the coordinator, not fixed here.

## Corrections (recorded honestly, per lane-common-contract report requirement 5)

- First live run of `ui-orphan-audit.mjs` produced 185 findings, almost entirely false positives from
  helper-function-built row objects the column-level parser could not trace (see Built, above). Refuted in
  the same session by adding `extractOpaqueWriteTables()` before this was ever reported as a finding count, the 185 number never left this lane's own local testing.

## Open items

- The 7 `[HYPOTHESIS]` fields above need a coordinator/operator triage pass (confirm each table's real
  writer path, or its genuine absence) before any of them become allowlist entries or build tickets.
- `harness-family-schedule-walker-audit.mjs` walks EVERY registered family uniformly; it does not yet
  distinguish "never registered a producer at all" (e.g. `producers`, which is a directory of per-source
  producer scripts, not a single runtime) from "a real single-runtime family that has simply not fired", worth a coordinator look at whether `producers` even belongs in the walked set, or needs its own
  per-producer-script sub-walk (out of this lane's scope; flagging, not fixing, since the family-registry
  descriptor for `producers` is what the brief said to walk as-is).
- No ADR governs either checker's allowlist format; both allowlists mirror TOOL-GAP-2's dead-column/
  duplicate-table allowlist shape for consistency, not because a spec required it.
