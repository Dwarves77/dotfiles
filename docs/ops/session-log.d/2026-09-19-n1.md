## 2026-09-19, lane N1: every hand-edited registry list becomes a directory read (plan 6.8, Rule A)

Lane N1, model Sonnet, worktree wt-n1, branch lane/n1-derived-lists-2026-09-19, base origin/master
e179af83 (carrying lanes N0 and T2).

**1. Fitness manifest.** `fsi-app/.discipline/fitness/manifest.mjs` now reads `functions/F*.mjs`
(excluding `*.test.mjs`) with `readdirSync`, imports each with `await import()`, validates that every
module exports `fitnessFunction` with an `id` matching its filename's `F<N>-` prefix, refuses a
duplicate id by name, and sorts by numeric id (top-level await; both importers, runner.mjs and
invariant-coverage.mjs, need no change). Of the 32 functions whose import line in the old manifest
carried an explanatory comment, 30 already carried that same history in their own file header (checked
by reading each one; the dated "why this exists" prose duplicates almost verbatim). Only F41 and F43
carried a FOLD renumbering note nowhere else (F41 was numbered F40 before colliding with lane
TAGS-401's own F40; F43 was numbered F42 before colliding with the card-shell chokepoint's own F42); I
moved those two notes into F41's and F43's own headers, content unchanged, 0 dash-glyph replacements
needed (the moved text used plain hyphens already).

**2. Nine files renamed, run-test-suite.sh converted to directory globs.** `git mv` on
`scripts/lib/{batch-primitives,pg-conn}.test.mjs`,
`scripts/lib/{decision-anchors,drift-check,exclusion-audit,inconclusive-probe,surface-registry}.selftest.mjs`,
`src/lib/sources/reconcile.selftest.mjs`, `.discipline/rendering/layout-guard/layout-guard.test.mjs`,
each to the same name with `.npmtest.mjs`. `run-test-suite.sh`'s node --test block: every named file
became a directory glob EXCEPT `src/lib/sources/{classify-source-role,instrument-identity}.selftest.mjs`,
which stay named (see Refutations). `discipline.yml`'s npm-deps step: the 14-file named list deleted,
glob widened from `fsi-app/src/**/*.npmtest.mjs` to `fsi-app/**/*.npmtest.mjs` (also picks up the five
pre-existing `scripts/**/*.npmtest.mjs` files that list separately named). `bug-class-guard.yml` (a
workflow outside this lane's stated write set, fixed anyway, see Refutations) and two comment-only
citations (`scripts/lib/inconclusive-probe.mjs`, `scripts/source-role-cleanup.mjs`,
`execution-wiring.mjs`'s own Source 7 docstring) updated to the new name.

**3. Audits declare themselves.** All 34 `scripts/verify/*.mjs` / `scripts/*.mjs` audit scripts gained a
`// data-audit: label=<label> hard=<true|false>` first-comment-line marker (byte-for-byte the label/hard
values from the deleted array). `run-data-audit-lane.mjs`'s `AUDITS` array is deleted; `deriveAudits()`
scans `scripts/verify/` and `scripts/` (top level) for the marker, sorted by label, and the actual spawn-
every-audit run is now gated behind `isMainModule(import.meta.url)` (scripts/lib/is-main.mjs) so
importing `deriveAudits()` for a test never spawns 34 child processes.

**4. Equality proof, asserted once (this session, live, not a permanent test).**
```
FITNESS IDS: before 44 after 44 set-equal: true
TEST FILES (corrected for a testGlobFromSuite blind spot, see Refutations): before(minus 9 renamed, plus 2 new) 497 after 497, missing: [], extra: []
NPM SET: before(mapped through the 9 renames) 159 after 159, missing: [], extra: []
AUDITS: before 34 after 34 deep-equal (order-independent): true
```

**5. Tests.** New `.discipline/fitness/manifest.test.mjs` (live load off the real directory; duplicate-id
refusal and id/filename-mismatch refusal against `mkdtemp` fixtures; a `*.test.mjs` sibling is excluded).
New `scripts/verify/run-data-audit-lane.test.mjs` (live `deriveAudits()`; a fixture directory's markers
read back exactly; malformed marker refused by name; duplicate label refused by name; all against
isolated temp directories, never the real tree). `F25-module-liveness.test.mjs`'s one AUDITS-array
fixture test rewritten to the new marker-scan shape (see Refutations, this file was already touched).

**Result [CONFIRMED by the commands pasted in the coordinator report].** `run-test-suite.sh`: 7387
tests, 0 fail. `fitness/runner.mjs`: 44 functions, 0 violations (F45's ceiling unmoved, per its own PASS).
`grep -c "fitnessFunction as F" manifest.mjs`: 0. `grep -n "named=" discipline.yml`: no match.

**Refutations and findings, each labeled, are in the coordinator report** (F25-module-liveness.mjs's
static import-graph could not see the new dynamic-import manifest or the new marker-scan AUDITS
derivation, so it needed two small out-of-write-set fixes; `execution-wiring.mjs`'s `npmtestMatcher()`
had a hardcoded `src/**` pattern that the widened npmtest glob broke; `bug-class-guard.yml` pinned
`inconclusive-probe.selftest.mjs` by name outside this lane's stated write set; the brief's own
`src/lib/sources/*.selftest.mjs` glob instruction would have pulled two jiti-dependent, out-of-scope
files into the no-npm suite; `testGlobFromSuite`'s own regex, shared with `glob-portability.test.mjs`,
never saw bare `.selftest.mjs` literals, undercounting the pristine file by 15 [CONFIRMED]).

**Next.** None owed by this lane. F25-module-liveness.mjs's `LEGACY_ALLOWLIST` reason strings for
`decision-anchors.mjs`/`exclusion-audit.mjs`/`inconclusive-probe.mjs` still say "hard-named in
discipline.yml's npm-deps test step", which stopped being true this lane; that file's allowlist text is
outside this lane's write set and is flagged, not fixed, in the coordinator report.

### UX compliance (N1)
Not a UI change; no customer surface touched by this branch.
