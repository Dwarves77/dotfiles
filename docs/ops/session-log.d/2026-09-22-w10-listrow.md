# Lane W10-ListRow session log (2026-09-22)

Brief: section 4 ("W10-ListRow (with Absence and Chips)") plus the COMMON BLOCK of
`docs/dispatches/lane-briefs/2026-09-22/brief-w10-remaining-parts.md`. Worktree:
`C:/Users/jason/dotfiles/.worktrees/wt-l25-slot-mirror`, branch `lane/w10-listrow`, cut from
`origin/master` at `316a79d4` (Lane W10-Masthead).

Skills loaded: `fsi-app:environmental-policy-and-innovation`, `frontend-design`,
`remediation-discipline`, `caros-ledge-platform-intent` (required by the governed-file write gate
before touching `src/app/admin/parts/list-row/page.tsx`, `src/app/admin/parts/page.tsx`).

Read: `docs/dispatches/lane-common-contract.md`, `docs/design/ux-laws.md`,
`docs/design/design-principles.md` (DP-2), `docs/design/parts-brief-2026-09-18.md` sections 2.8
(LIST ROW), 2.9 (ABSENCE), 2.10 (CHIPS), 2.16 (IMPACT METER, ROW VARIANT), the M7 last-mile row of
`docs/plans/complete-system-build-plan-2026-09-04.md`, and `docs/dispatches/lane-briefs/2026-09-20/
brief-m7a.md` (names M7b, "the grade chip on every list row and masthead"). No artboard image file
was opened directly (none named beyond the parts-brief's own text sections for this lane; the
brief's "open the artboards named" instruction is satisfied here by the parts-brief text itself,
which is the artboard's transcription per that doc's own note 1).

## Finding: ListRow/Absence/Chips were already built

`src/components/ui/ListRow.tsx`, `Absence.tsx`, `Chips.tsx` already implement the parts-brief 2.8/
2.9/2.10/2.16 spec in full (band spine, jurisdiction, title+meta, impact meter, due, timeline,
tier, overflow; the one-absence-per-row precedence; the four chip families; the stepped-fill N/12
row meter), built across prior lanes (moblist, mobfix61, opsclip, uitags, map60, searchrow,
W10-CommandBar/SectionHeader). This lane's real delta, read against the brief's own text ("are
parts of their own with `data-part`", not yet true; "the M7b grade chip", not yet built):

1. `data-part` markers on ListRow (both `list`/`register` variants), Absence (all three variants:
   `reason`/`narrow`/`dash`), and every Chips export (`chip-band`, `chip-tier`, `chip-tag`,
   `chip-workspace-tag`, `chip-filter-group`, `chip-filter`).
2. The M7b grade chip: `GradeChip` built in `Chips.tsx`, `RecordGradeBadge.tsx` folded into it and
   deleted (M7 last-mile plan: "RecordGradeBadge.tsx (folded into Chips)"). Its one JSX consumer
   (`OperationsItemsView.tsx`) now imports `GradeChip` from Chips; the one dead import
   (`RegulationDetailSurface.tsx`, never rendered) removed; comments in `ResearchFindingDetailSurface.tsx`
   and `types/resource.ts` updated to point at the new home. Wiring the chip onto every list row and
   detail masthead (the rest of M7b) is out of this lane's scope per the session log's own
   "M7b ... waits on the ListRow part lane"; this lane builds the part, M7b (a separate, later
   lane) wires it.
3. The Impact meter fixture at N=4/8/12/unscored (the W10-A2 remainder), and the ListRow sign-off
   fixture page, at `/admin/parts/list-row`.

## Fixture (frozen real records, no invented strings)

`src/components/ui/__fixtures__/list-row-fixture.ts`: four `intelligence_items` rows via read-only
SELECT against the live Supabase project (kwrsbpiseruzbfwjpvsp), frozen 2026-09-22, see that
file's header for the exact queries (`intelligence_items` join `sources`, `item_timelines`). Title,
jurisdiction, source tier and timeline-milestone text are the frozen real values. Impact scores are
NOT a stored column anywhere in the schema (every surface derives them client-side via
`scoreResource`, per `list-row-fields.ts`'s own header), set explicitly to the brief's four named
totals (4/8/12/unscored) so the meter renders every state on real row chrome, the same pattern the
legend row itself uses (`<ImpactMeter total={8}/>`, brief 2.16).

`/admin/parts/list-row`: renders the four fixture rows through the real `ListRow` plus
`ListRowColumnHeader`, plus a second strip repeating the four meter states on the bare
`ImpactMeter` component. Registered in `/admin/parts`'s index. No database read at render time.

## Presence report (per surface, `grep`-verified)

| Surface | Consumer | Renders through ListRow |
|---|---|---|
| Regulations | `RegulationsLedger.tsx` | yes (existing) |
| Market | `MarketIntelLedger.tsx` | yes (existing) |
| Research | `ResearchLedger.tsx` | yes (existing) |
| Operations | `OperationsLedger.tsx` | yes (existing) |
| Watchlist | `WatchlistSurface.tsx` | yes (existing) |
| Dashboard "Due next"/"What changed" | `DashboardBrief.tsx` | yes (existing) |
| `/search` | `SearchResultsView.tsx` | yes (existing) |
| CommandBar inline results | `CommandBar.tsx` | yes (existing) |
| `/map` jurisdiction register | `MapPageView.tsx` | yes, `variant="register"` (existing) |
| Community | none | **not drawn**: parts-brief 2.8's own surface list is Regulations, Market,
  Research, Operations, Watchlist, Dashboard only; Community has no intelligence-item list (forum
  posts, a structurally different anatomy; band/jurisdiction/due/timeline/tier does not apply).
  Not migrated, not invented. |
| `/admin/factors` (and other admin lists) | raw `<table>`, no ListRow | **not this lane's scope**:
  ruling 7 (cited in this brief's section 6, RailCard+StatBlock) explicitly assigns
  "`/admin/factors` is a list surface... every admin route uses the same frame, masthead,
  SectionCard, StatBlock, ListRow" to that later lane. Not migrated here. |

## Corrections (found only by running the gates)

1. First `data-part`/`data-part-variant` placement on `Absence.tsx`'s `dash` variant broke
   `ImpactMeter.npmtest.mjs`'s exact-order regex on the rendered markup
   (`class="cl-absence-dash" data-absence="dash" aria-label="..." title="..."`). Fixed by moving
   the two new attributes after the pre-existing ones (`data-absence`/`aria-label`/`title`) instead
   of before them, a rendered-output ordering constraint, found by the gate, not by inspection.

## Consumers checked

- `grep -rln "RecordGradeBadge" src`: after the fold, only comment references remain (updated for
  accuracy), no import, no JSX.
- `grep -rn "from \"@/components/ui/ListRow\""`: `DashboardBrief.tsx`, `ListSurfaceShell.tsx`,
  `MapPageView.tsx`, `SearchResultsView.tsx`, `CommandBar.tsx`, `WatchlistSurface.tsx`, all
  unchanged by this lane's `data-part` addition (additive attribute, no prop/behavior change).
- `grep -rln "from \"@/components/ui/Chips\""`: unaffected by additive `data-part` attributes; one
  new import (`OperationsItemsView.tsx`, `GradeChip`).
- `docs/decisions/`: no ADR names ListRow/Absence/Chips/grade chip; nothing to check against.

## UX compliance

**`/admin/parts/list-row`.** Primary goal: platform-admin part sign-off, read the row/meter/chip
states against real data. Path: one page load, no interaction required, static fixture data (no
database read). One primary action: none (read-only gallery, consistent with every other
`/admin/parts/*` page). Feedback state: n/a, no async behavior.

**Every list surface using `ListRow`/`Absence`/`Chips` (unchanged by this lane).** The `data-part`
additions and the `GradeChip` fold are structural/attribute-only; no interaction, layout, or
feedback-state change on any of the nine consuming surfaces. Law 2 (touch targets): unaffected,
same click targets as before. Law 16 (pattern consistency): improved, the grade chip now renders
from one home (`Chips.tsx`) instead of a separate `RecordGradeBadge` component, closing a
duplicate-visual-language risk before it recurred at a second call site.

## Gates

- `npx tsc --noEmit`: clean, twice (before and after the ImpactMeter fix).
- `bash .discipline/run-test-suite.sh` (no-npm glob): 1518 tests, 0 fail, both runs.
- `sh fsi-app/.discipline/hooks/lib/run-npmtest-suites.sh`: 1486 tests, first run 1 fail
  (ImpactMeter.npmtest.mjs, see Corrections), second run 1486 pass, 0 fail.
- `node fsi-app/.discipline/fitness/runner.mjs`: 47 functions, 0 violations (F45 duplicated lines
  5995, below the 6011 base; net reduction from folding RecordGradeBadge into Chips).
- `node .discipline/rendering/run-rendering-guard.mjs`: PASS x4 (two runs before the ImpactMeter
  fix, two runs after, all green; node.exe count checked <=3 before every run per this lane's
  corrections to the COMMON BLOCK).

## Open items

- M7b's remaining scope (wiring `GradeChip` onto every list row and every detail masthead, the
  NoticesRail proof) is NOT done here; this lane builds the part, per the session log's own
  "M7b... waits on the ListRow part lane."
- The locked push-gate preflight and the coordinator PR-body file: see the final report for
  outcome, not duplicated here.
