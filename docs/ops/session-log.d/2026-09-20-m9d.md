## 2026-09-20, lane M9d: the `producers` harness family (build plan section 6.1 row M9)

First person, lane M9d. Follow-on from lane M5, whose report named the gap: the market and regional
producers (`scripts/producers/market/*.mjs`, the regional producers behind `run-envelope-producer.mjs`)
are the loop's own data-producers box and were the only loop box with no harness-run family, so a
producer run left no artifact, F28 could not check its staleness, and lane M5's own `assertEdgesAuthored`
fail-closed assertion had no artifact to record `status: "failed"` in.

### What landed

1. `fsi-app/scripts/producers/lib/producer-summary.mjs`: `writeProducerSummary({ producer, status,
   rows_changed, edges_authored, reason, counts })`. Writes `<PRODUCER_SUMMARY_DIR>/<producer>.json` when
   that env var is set; a no-op returning `null` otherwise, so a local dev run of any producer is
   unchanged. Pure path/shape logic, unit-tested (7 tests).
2. Wired all eleven `--apply`-invoked producer scripts `producers.yml` runs: the three `market_series`
   producers (`ecb-fx`, `eia-v2-petroleum-spot`, `eu-weekly-oil-bulletin`) through a new shared wrapper
   `assertEdgesAuthoredAndRecordSummary` (`author-market-series-delta.mjs`, one home, not three copies of
   the try/catch shape, per F45); the three regional producers via their `runEnvelopeProducer` return
   value; `refresh-published-price-statistics.mjs`; the two `emission_factors` seeders through a new
   shared wrapper `recordSeedFactorsSummary` (`emission-factors-common.mjs`, same F45 reasoning);
   `fetch-desnz-factors.mjs`; `ratify-series-items.mjs`.
3. Gate against recurrence, attack-tested: `scripts/producers/lib/producer-summary-wiring.test.mjs` parses
   `producers.yml`, collects every `node scripts/....mjs` invocation run with `--apply` under
   `scripts/producers/` or `scripts/gen/`, and asserts each one is wired into the summary mechanism
   (directly or through one of the two shared wrappers above). Proven against a fixture yml naming a
   script that is not wired.
4. `fsi-app/scripts/producers/emit-producers-artifact.mjs`: modelled on
   `scripts/turns/emit-downstream-chain-artifact.mjs` (read, not edited: protected by another lane's
   worktree instruction). Reads back a firing's own `PRODUCER_SUMMARY_DIR`, folds every summary into one
   `producers-run-NNN.json` via the shared `writeRunArtifact`/`claimRunId`/`hashHarnessVersion`
   (`scripts/lib/run-artifact.mjs`, including its `baseArtifactFields` helper to avoid a fresh clone of the
   five-field artifact header). `config.loop_run_id` is `null` by design: `producers.yml` runs on
   `workflow_dispatch` only (build mode, ADR-023) and is this hop's own loop head, never a `workflow_run`
   consumer.
5. `.github/workflows/producers.yml`: `PRODUCER_SUMMARY_DIR` set at job level; two new tail steps
   (`if: always()`), run the emitter, then commit-and-open-a-PR for the artifact, copied from
   `downstream-chain.yml`'s identical steps (with `working-directory: .` added, since this job carries a
   `defaults.run.working-directory: fsi-app` that `downstream-chain.yml` does not). No other change to the
   workflow's trigger, gates, or step list.
6. `scripts/harness-runs/producers/family.json` plus `FAMILY.md` plus `pending/2026-09-20-m9d.md`
   (registered by descriptor, per build plan section 6.8 Rule A: no edit to `ALLOWED_FAMILIES`,
   `governing-files.mjs`, or any shared list). Governing files: the workflow itself,
   `author-market-series-delta.mjs`, `run-envelope-producer.mjs`, the emitter, and `producer-summary.mjs`.
   The eleven individual producer scripts are deliberately NOT governing files (each writes through the
   one shared writer only).
7. `docs/runbooks/PROPAGATION-DRAIN-RUNBOOK.md`: corrected the "Tracing edge authorship" section's
   now-stale claim that no market/regional producer family was registered, `[REFUTED, lane M9d]` in
   place, per CLAUDE.md rule 13's corollary, rather than a silent edit.
8. `scripts/harness-runs/family-registry.test.mjs`'s own `LIVE_FAMILIES` list gained `"producers"` (a
   hand-maintained snapshot local to that test, not one of the derived/forbidden shared lists).

### Acceptance (section 0, from the row)

- **Reachable**: the `writeProducerSummary`/wrapper call in each of the eleven producer scripts, and the
  emitter plus commit step in `producers.yml`, quoted above, code committed.
- **Run**: not by this lane (rule: "you run NO producer, in any mode"). The coordinator's bounded
  `ecb-fx` dispatch is the first real run, per Amendment 1 section D.
- **Populated**: `producers-run-001.json` on master, the coordinator's dispatch, not this lane.
- **Gated**: F28 now covers the `producers` family (confirmed: F28's live-tree test and its
  `FAMILY-DESCRIPTOR-REALITY` test both pass with the new descriptor in place). F50's
  `data-producers-to-propagation-drain` hop is NOT changed by this lane, see "Problems / findings" below.
- **Documented**: `FAMILY.md` above; this entry; the runbook correction.

### Test results (verbatim counts)

- `node --test scripts/producers/lib/*.test.mjs scripts/producers/market/*.test.mjs
  scripts/producers/regional/*.test.mjs scripts/gen/*.test.mjs src/__tests__/regional-*.test.mjs
  src/__tests__/market-producer-composition.test.mjs`: tests 230, pass 230, fail 0.
- `node --test .discipline/fitness/functions/F28-harness-run-integrity.test.mjs
  .discipline/fitness/functions/F50-loop-wiring.test.mjs scripts/harness-runs/family-registry.test.mjs
  scripts/lib/run-artifact.test.mjs`: tests 148, pass 148, fail 0.
- `node --test .discipline/fitness/functions/F27-producer-seam-proof.test.mjs`: tests 26, pass 26, fail 0.
- `node .discipline/fitness/runner.mjs --function=F45`: PASS, duplicated lines 6121 (base 6121, no
  regression). Getting here took two rounds of extraction: the try/catch-plus-summary shape shared by the
  three `market_series` producers (`assertEdgesAuthoredAndRecordSummary`), the summary-record-plus-fail
  shape shared by the two `emission_factors` seeders (`recordSeedFactorsSummary`), and the five-field
  artifact header shared with `emit-downstream-chain-artifact.mjs` (`baseArtifactFields`, already existed
  in `run-artifact.mjs`, just not used by the new emitter until this lane reused it).
- `node .discipline/fitness/runner.mjs --function=F51`: PASS, 0 violations.
- No `.ts`/`.tsx` files touched, so `npx tsc --noEmit` was not run (brief's own instruction: only when a
  `.ts`/`.tsx` is touched).

### Problems / findings, labeled

- **[CONFIRMED]** Two discipline gates fired on the first push-gate run that this session-log's own
  original text did not anticipate, both fixed in the same commit (amended, not a second commit):
  F28's "PENDING FILE REQUIRED (range)" for the `meta-harness` family (registering ANY new family moves
  `meta-harness`'s own `harness_version`, by the deliberate self-application mechanism
  `governing-files.mjs`'s own header documents; fixed by adding
  `scripts/harness-runs/meta-harness/pending/2026-09-20-m9d.md`, the same pattern lane M3's own
  `downstream-chain` registration recorded on 2026-09-19), and F27's "NO COMPOSITION PROOF" for four
  producer entry points that gained a new direct seam (`producer-summary.mjs`) without a composition
  proof importing it alongside their existing seams (fixed by extending the four existing composition
  proofs, `propose-series-items.test.mjs` and the three `regional-*-composition.test.mjs` files, each
  with an import plus a genuine test exercising `writeProducerSummary` against that producer's own real
  fixture-derived row count, rather than a bare import to satisfy the mechanical check). Neither gate is
  named in brief-m9d or its amendments; both are real, pre-existing repo discipline this lane's own
  change triggered.
- **[REFUTED]** Amendment 1 item 6 read: "the hop `data-producers-to-propagation-drain` gets `family`
  handling only if its entry names a family for the PRODUCER side; read the entry ... and the F50 rule
  before touching it." Read both (`.discipline/governance/loop-manifest.mjs` line 146, and
  `F50-loop-wiring.mjs` in full). The entry's `family: 'propagation'` field is for the CONSUMER side
  (`propagation-drain.yml`'s own family, used only by `enforceFired`'s "does propagation have a
  workflow_run-triggered artifact" check). F50 has no concept of, and needs no field for, the PRODUCER
  side's own family at all. So the premise that this hop needed a producer-side family field the manifest
  lacks is false; nothing in the manifest needed changing, and nothing was changed. `enforceFired` on this
  hop stays `false`, untouched, per the brief's own "enforceFired is never yours."
- **[HYPOTHESIS]** Amendment 1's own header comment (item 4) cites "ADR-031" for the "producers are their
  own loop head" rationale. `docs/decisions/` has no `ADR-031-*.md` as of this session (`ADR-030` is the
  highest number present). The citation is carried verbatim into `emit-producers-artifact.mjs`'s own
  header comment as instructed (not this lane's id to allocate or verify), but the ADR itself may not
  exist yet; the coordinator should confirm or file it.
- **[CONFIRMED]** `producers.yml` has a job-level `defaults.run.working-directory: fsi-app` that
  `downstream-chain.yml` does not (that file sets `working-directory: fsi-app` per step instead). Copying
  its commit-and-PR step verbatim would have run `git add fsi-app/scripts/harness-runs/producers` from
  inside `fsi-app/`, which does not exist as a path from there. Fixed by adding `working-directory: .` to
  just that one new step.

### UX compliance (m9d)

No `.tsx`/`.css` under `fsi-app/src` touched by this lane; the UX-contract rule in
`docs/dispatches/lane-common-contract.md` does not apply.

### What I could not do and why

Did not run any producer, in any mode, per Amendment 1 section D ("You run NO producer, in any mode: the
first real artifact is the coordinator's bounded ecb-fx dispatch"). Did not touch the loop manifest, any
`fsi-app/scripts/turns/emit-*.mjs` file, `loop-run-id.mjs`, or `run-propagation-drain.mjs` (lane M3b's
concurrent worktree). Did not push; the locked push gate result is in the report delivered to the
coordinator, not in this file.
