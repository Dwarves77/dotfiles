## 2026-09-13, W9 lane L18: D27 apply-driver refusal, D28 per-lane session-log files

`defect-fix-plan-2026-09-12.md`'s D27 (the brief-apply driver processed a missing/unresolvable
`--briefs` path or a zero-entry file silently, planning zero items while both dry and apply runs
completed green) and D28 (every lane appending to the one `docs/ops/session-log.md` file made every
rebase onto a master that had merged another lane's own entry conflict on it), worktree
`wt-part1-0911`, branch `lane/w9-l18-apply-refusal-sessionlog-2026-09-13`.

**D27.** `fsi-app/scripts/turns/apply-record-briefs.mjs` gains `resolveBriefsInput`, a pure function
(fs access injected, matching this module's existing `deps` pattern) that resolves `--briefs` to an
absolute path and refuses, before any DB client is built and in both dry and apply mode, when the path
does not exist or the file parses to zero entries. Every refusal names both the path as given on the
command line and the resolved absolute path. `main()` calls it immediately after `claimRunId`, before
`rawItemIds`/`sb` are computed. `.github/workflows/brief-apply.yml` gains a "Validate briefs_file" step
before the driver step, printing the resolved path and entry count and failing the workflow when the
file is absent or empty; the dispatch recipe in the file's own header now states plainly that
`briefs_file` must be the fsi-app-relative path, since the job runs with `working-directory: fsi-app`,
and that an `fsi-app/`-prefixed value is wrong.

**D28.** `fsi-app/.discipline/governance/memory-gate.mjs`'s vault requirement now also accepts any file
matching `docs/ops/session-log.d/YYYY-MM-DD-<slug>.md`, alongside the existing `docs/ops/session-log.md`
and `docs/PROGRAM-BOARD.md`. `docs/ops/session-log.d/README.md` states the entry format and the rule:
one file per lane per day, never edit another lane's file. This file is this lane's own entry, proving
the mechanism end to end: this range adds only code plus this directory's two new files (no edit to the
shared `session-log.md`), and the memory gate is expected to pass on that basis alone.

**Tests.** `apply-record-briefs.test.mjs` gains seven `resolveBriefsInput` cases (missing path, zero
entries, missing `entries` key, a real fixture with one entry proceeding, malformed JSON, and two CLI
end-to-end cases in dry and apply mode) plus one pre-existing CLI test's fixture updated (a missing
`entries` key is now caught earlier by `resolveBriefsInput`, so that test's fixture was changed to a
non-empty `entries` array with one malformed entry, to keep exercising the later
`validateRecordBriefsFile` throw site it was written to cover). `memory-gate.test.mjs` gains five cases:
a `session-log.d` file classifies as MEMORY, the README does not (no date/slug in its name), a malformed
`session-log.d` filename does not, a range adding only a `session-log.d` file passes, and a range with
code and none of the three vault forms fails and names the `session-log.d` option in its message.

**Next steps.** None owed by this lane; D27 and D28 are both closed by this range.
