## 2026-09-22, lane W10-FactCard-e: the operator's sign-off corrections (provenance column, frozen real fixtures, sub-label)

What: three corrections from the operator's 2026-09-22 sign-off on panel 21c (`docs/dispatches/lane-briefs/2026-09-22/brief-w10-factcard-e.md`), quoted in full there. Sign-off itself stands ("Panel 21c fixture matches the artboard ... Sign-off stands once these land").

Fixed:

1. **Provenance column clip removed.** `PROVENANCE_COL` (`src/components/ui/FactCard.tsx`) no longer
   carries `maxHeight`/`overflow: hidden`, the clamp that used to cut the fourth row ("example.org"
   as the last visible line, the accessed date clipped beneath it) is gone; the column renders its
   natural height and the card grows to fit it. Added `sourceNameWrapsToTwoLines` (a character-count
   heuristic, `[HYPOTHESIS]`, calibrated so the real record's own `Decision 1999/652/EC`, 20 chars,
   does NOT trip it, since the artboard shows that name on one line with its organisation row still
   present): when the source name is long enough to wrap to two lines, the organisation row is
   dropped, never the accessed date. Order is unconditional: (1) tier square + source (2) organisation
   (3) link (4) accessed date.
2. **Frozen real fixtures.** New file `src/components/ui/__fixtures__/panel-21c-frozen-records.json`,
   built from two read-only SELECTs against the live Caro's Ledge Supabase project
   (`kwrsbpiseruzbfwjpvsp`), frozen 2026-09-22:
   - `intelligence_items.id = 018dcbf1-42e3-49f5-8029-01804818b3f8` (Decision 1999/652/EC,
     `instrument_identifier = 31999D0652`), joined to its `sources` row (EUR-Lex, tier 1,
     `https://eur-lex.europa.eu/legal-content/EN/TXT/?uri=CELEX:31999D0652`) and its
     `agent_run_searches` capture (`searched_at = 2026-09-04 13:33:50 UTC`, matching the artboard's
     "accessed 4 Sep 2026"), plus six verbatim `section_claim_provenance` claim spans (baseline
     target, national targets, jurisdictional scope, etc.).
   - `regional_data_facts.id = f6f28fc9-1039-4235-874f-f4f3d3969052` ("Hong Kong - Warehouse Worker
     Monthly Wage", `HKD 14,747/month average`, `source_note` "Indeed Hong Kong ·
     https://hk.indeed.com/career/warehouse-worker/salaries · 2025-09"). NOTE: the brief's prose calls
     this "the Belgian figures in 21c"; the actual matching record (HKD 14,747, Indeed HK, 2025-09) is
     the Hong Kong wage row under the "Asia, Singapore + Hong Kong" region, and the figures cited in
     the sign-off match this record exactly. No Belgium-labelled row exists with these numbers, so the
     record whose data matches the cited figures was used.
   `src/lib/detail/fact-card-fixtures.ts` and `fact-card-panel21c-fixture.ts` (which reuses
   `KIND_FIXTURES`) now build their `PROVENANCE`/`MATRIX_FIXTURES` objects from this JSON; every
   `"example.org"`/`"Example Regulation"`/`"Example Regulatory Body"` string is gone from
   `/admin/parts/fact-card`'s fixtures. New test
   `fact-card-fixtures.npmtest.mjs`: "no fixture ... carries generic placeholder text" runs
   `/example\.(org|com)|Example (Regulation|Regulatory)/i` against every default-density fixture,
   matrix fixture, and panel-21c group (title/qualifier/actionStrip/cards) and asserts zero matches.
   Two new fixtures added per build item 4 (four-line provenance; two-line source name, organisation
   dropped, the source string is a real verbatim 41-character substring of the frozen record's own
   title, not invented), wired into `DEFAULT_DENSITY_FIXTURES` and therefore into
   `/admin/parts/fact-card`, and into `panel21c-accept.mjs`'s acceptance model
   (`provenanceLineCount`, optional, independent of `claimLineCount`) plus its test file and the
   Playwright smoke spec (`panel-21c-smoke.mjs` now measures the real provenance column's rendered
   line count).
3. **Figure sub-label letter-spacing.** `FIGURE_SUB_LABEL` (`FactCard.tsx`) reduced from `0.06em` to
   `0.04em`, tried before the nowrap/ellipsis rule engages, per the sign-off ("'RECOVERY / RECYCLI...'
   truncates ... reduce letter-spacing to .04em before ellipsising").
4. **"10 more facts below" fixture chrome removed.** `FactCardGallery.tsx`'s reference "Default
   density" / `density="matrix"` galleries used to stack all 16/2 fixtures inside one `ItemGroup`
   each, tripping `ItemGroup`'s real 4-card visible cap and showing its `MoreBelowDisclosure` ("10
   more facts below"), a genuine, correct control on a real item group, but only fixture-page chrome
   here. `chunkIntoGroupsOf4` now splits the reference gallery across as many `ItemGroup`s as needed
   to keep every one at or under 4 cards, so the disclosure never fires on this page. `ItemGroup.tsx`
   itself (a real, shared, production part used elsewhere) is untouched: "the ItemGroup disclosure is
   the only overflow affordance" for real product content, unchanged.

Consumers checked: `git grep -n "fact-card-fixtures\|PANEL_21C_GROUPS\|FactCardGallery"` across
`fsi-app/src`, the only production importer of the fixtures module remains
`src/app/admin/parts/fact-card/page.tsx` (via `FactCardGallery.tsx`), unchanged and not edited per
the brief's forbidden-file list. `PROVENANCE_COL`/`sourceNameWrapsToTwoLines` consumed only inside
`FactCard.tsx`'s own `ProvenanceBlock`.

Gates: full `node --test` no-npm suite 1533 pass / 0 fail / 4 skip (self-skip, no creds); `npx tsc
--noEmit` clean; fitness runner 47 functions, 0 violations; CI-mode discipline runner
(`--range=origin/master..HEAD`) silent (0 fail); consistency override-check clean, no drift;
rendering guard run twice, both `=== rendering guard PASS ===` (see final report for the exact
summary lines).

## UX compliance

**FactCardGallery.tsx (variant gallery restructure, `/admin/parts/fact-card`, platform-admin
surface, not customer-facing).**
- Primary goal: let a reviewer see every FactCard kind/form/density variant without any one card's
  provenance being clipped or a disclosure control hiding cards that should just be visible.
- Path: navigate to `/admin/parts/fact-card`, scroll past the panel-21c sign-off fixture; the
  variant gallery renders every fixture directly, chunked into small `ItemGroup`s that never exceed
  their own visible cap.
- One primary action: none (a read-only reference page); no async action, no loading/error state
  introduced by this change.
- No new interactive control added or removed (the disclosure that used to appear is gone because it
  no longer fires, not because a control was deleted).

**FactCard.tsx (provenance column, `ProvenanceBlock`, rendered on every detail surface's FactCard,
customer-facing).**
- Primary goal: the reader can see a fact's full provenance (source, organisation, link, accessed
  date) without truncation.
- Path: no interaction required, the column renders its natural height and the card grows to
  accommodate it. The one interactive element (the source link) keeps its existing 24px+ hit area
  (law 2), unchanged by this lane.
- One primary action: the provenance link (opens the source in a new tab); unchanged feedback
  (browser-native new-tab navigation).
- Feedback state: no async action on this part; nothing to acknowledge beyond the browser's own
  navigation.

DP-1: not applicable (customer-facing FactCard render; the /admin/parts/fact-card gallery is a
read-only reference page with no triage actions to consolidate).
DP-2: compliant. Laws 2 (24px link hit area, unchanged), 4 (provenance grouped visually via the
column's left rule), 12 (no added visual noise), 16 (pattern consistency: the drop-org rule is the
ONE conditional in an otherwise fixed four-row order) all hold; verified against `ux-laws.md` and
`design-principles.md` DP-1/DP-2 before writing.

## Gates, resumed run

Resumed session, 2026-09-22, found the build already committed at 528359ee (bfde1be8..HEAD) with a
clean working tree; nothing staged, nothing to add. Re-verified independently rather than trusting
the prior commit message's claims (rule 15, execution over assertion): grepped the fixture, model,
and admin-parts tree for the banned pattern
(`/example\.(org|com)|Example (Regulation|Regulatory)/i`); the only hits are a code comment
describing the fix and generic markdown-link-parsing test fixtures in
`fact-card-model.test.mjs` (unrelated to panel 21c, not a claim fixture); the frozen JSON's `_frozen`
header carries the item ids, the four SELECT queries and `frozen_at: 2026-09-22`, satisfying the
brief's header requirement. Ran every gate fresh:

- Rendering guard, run 1: `=== rendering guard PASS ===` (fixtures: 14, viewports: 12, checks: 747;
  UX smoke specs: 14, ux checks: 249; layout guard: 0 findings outside the dated baseline), output
  `fsi-app/scripts/tmp/rg-run1.log`.
- Rendering guard, run 2: `=== rendering guard PASS ===`, identical fixture/viewport/check counts,
  output `fsi-app/scripts/tmp/rg-run2.log`.
- `bash .discipline/run-test-suite.sh`: discovered 535 test files; `node --test` tests 1531, pass
  1527, fail 0, cancelled 0, skipped 4, todo 0; script's own exit code 0 (`EXIT:0` printed by the
  script itself). The `audit-finding-status.mjs` report-only tail (`|| true` in the script, per its
  own header) surfaced 607 unlabeled findings across 122 pre-existing `docs/audits/` files, all
  outside this lane's write set and predating this lane; not a gate failure, report-only by design.
  Output `fsi-app/scripts/tmp/npm-suite.log`.
- `npx tsc --noEmit`: clean, exit 0. Output `fsi-app/scripts/tmp/tsc.log`.
- `node fsi-app/.discipline/fitness/runner.mjs`: 47 function(s) checked, 0 violation(s), exit 0.
  Output `fsi-app/scripts/tmp/fitness.log`.
- `git status --short` after all gates: clean; no `coverage-report.json` drift to restore.
- Locked push gate (`lane-prepush-check.sh`) skipped for this resumed run on the coordinator's
  explicit instruction: the coordinator's push queue runs the identical pre-push hook on push, so a
  second run here would be redundant contention on the same hook lock.

No STOP. Skills loaded: `environmental-policy-and-innovation` (trigger: dispatch touches freight
sustainability intelligence item types and their fixture data), `frontend-design` (trigger: `.tsx`
edit under review). `remediation-discipline` does not exist as a registered skill in this
environment; noted and continued per the brief's fallback instruction.
