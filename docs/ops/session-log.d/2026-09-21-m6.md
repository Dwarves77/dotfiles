## 2026-09-21, lane M6: evaluate invokers -- apply-deferrals built, gate-a-rescan STOPPED on a false brief premise

Skills loaded: `fsi-app:environmental-policy-and-innovation` (first tool call, per Amendment 1 section B).

What landed (item 3 of brief-m6.md, per Amendment 1 section E): `fsi-app/scripts/maintenance/
apply-deferrals.mjs` and `apply-deferrals.test.mjs`. Dry-then-apply MAINT wrapper: `--arg` is a reviewed
JSON file path, an array of `{ item_id, reason, deferred_until, owner, resolution_event }` rows. Every row
is validated with `scripts/lib/deferral.mjs`'s existing `isValidDeferral` (no second validation
mechanism); dry mode reports a plan; apply mode writes one `integrity_flags` row per VALID row only
(`created_by: "disposition_deferred"`, `recommended_actions: [{deferral: {...}}]` -- the exact shape
`scripts/verify/quarantine-disposition-audit.mjs`'s read side already parses). Invalid rows are reported
by item_id and reason, never written. I did NOT run this script (Amendment E is explicit: build only).

Tests: `node --test scripts/maintenance/apply-deferrals.test.mjs` -- 15 pass, 0 fail. Pure validation only
(no DB call in any test, per the module's own header and Amendment E).

### STOP: gate-a-rescan.yml, its emitter, family registration, and the loop-manifest update (items 1, 2, 4, 5)

Brief-m6.md item 1 says to find "the maintenance.yml step that re-scans" and item 2 says the new
`.github/workflows/gate-a-rescan.yml` should "run the existing re-scan step's script". This premise is
false on this base, confirmed two ways, not one:

- `git grep -rn "gate-a-scan.mjs\|gate_a_version\|GATE_A_VERSION" fsi-app/scripts/maintenance/
  fsi-app/scripts/verify/` from the repo root returns NOTHING [CONFIRMED by grep]: no maintenance or
  verify script anywhere imports the Gate A scanner or reads/writes `gate_a_version`.
- `fsi-app/.discipline/governance/loop-manifest.mjs`'s own committed comment (lane M9a, 2026-09-18) states
  it directly: "M6 builds the gate-a-rescan consumer... this manifest names the undecided path as pending
  rather than presuming maintenance.yml, which exists today but carries no gate-a-rescan step or
  workflow_run edge" [CONFIRMED by reading that file, lines 32-37 and 180-184].

So there is no existing re-scan script to wire a new workflow onto -- this is not "the re-scan script
cannot take a version-mismatch scope without a schema change" (the brief's own named stop condition,
which presumes a script that merely lacks a scope parameter); it is one step more basic: no re-scan
script/business-logic exists at all. Writing that logic from scratch (querying
`intelligence_items`/`item_gate_a_state` for a version-mismatch or upstream-touched population, calling
`scanBrief` per item, and writing the result back through the guarded db path) is a schema/mechanism
decision the brief does not make, matching the brief's own STOP-condition class and the common contract's
"a place where the brief and the code disagree: STOP." I did not invent an answer, widen scope, or build
a second grounding/scan mechanism.

Not built, per the STOP: `.github/workflows/gate-a-rescan.yml`, `scripts/turns/emit-gate-a-rescan-artifact.mjs`,
the `gate-a-rescan` family registration (`family.json`/`FAMILY.md`/`pending/2026-09-21-m6.md`), the
`loop-manifest.mjs` hop updates (`brief-apply-to-gate-a-rescan`, `population-turn-to-gate-a-rescan`:
`family`, `enforceEdge`, `consumerPending`), and the `loop-run-id.test.mjs` attack-chain extension for this
hop. F50's "hops not yet enforced" line is UNCHANGED by this lane (both gate-a-rescan hops still carry
`consumerPending: true`, `family: null`) -- see the report for the before/after quote (identical).

Also not built, per Amendment 1 section E's own split: the quarantine-disposition verifier is not wired
into the CI data-audit lane (that is lane M6b, dispatched by the coordinator after the 53-item deferral
file is reviewed and applied through `apply-deferrals.mjs`).

### Owed

- **Coordinator**: rule on what `gate-a-rescan.yml`'s re-scan step should actually run (build new scan
  logic under `scripts/maintenance/`, or point the workflow at a different existing entry point I did not
  find); re-dispatch M6 (or a successor lane) with that decision made.
- **Coordinator**: review and apply the 53-item quarantine disposition file through
  `apply-deferrals.mjs --mode apply --arg <reviewed-file>` (this lane built it, did not run it).
- **M6b** (named in Amendment 1 section E): wire `quarantine-disposition-audit.mjs` into
  `scripts/verify/run-data-audit-lane.mjs` as a hard audit, fail-closed, straight after the deferrals above
  are applied -- proven red-then-green.

Gates: `node --test scripts/maintenance/apply-deferrals.test.mjs` (15 pass, 0 fail); `node
.discipline/fitness/runner.mjs` result and prepush wrapper result recorded in the report.

UX compliance: not a UI change (no `.tsx`/`.css` touched).
