## 2026-09-24, lane REG-REDIRECT: uuid→slug redirect no longer lands on a 404

- **Defect closed** [CONFIRMED]: item 3 of the live-findings addendum above. `/regulations/d2da85da-0912-497a-b645-31e4ca73cd18` (quarantined) 307-redirected to `/regulations/uae-national-net-zero-by-2050-transport-sector-roadmap`, which rendered "This page doesn't exist." All four `[slug]` routes (`regulations`, `market`, `operations`, `research`) redirected on the mere presence of a `legacy_id`, never asking whether the target would render.
- **Fix**: one shared resolver, `fsi-app/src/lib/detail/id-redirect.ts` (`resolveIdRedirect`/`decideIdRedirect`/`admittedSurfaceFor`), consumed by all four routes via `applyIdRedirect` (`fsi-app/src/lib/detail/load-detail.ts`). The admission predicate mirrors `loadDetail`'s own (verified + canonical-surface match) so redirect and render can never disagree. A uuid whose item is admissible on no surface now 404s AT the uuid URL instead of bouncing into a dead slug.
- **Tests**: `fsi-app/src/lib/detail/id-redirect.test.mjs`, 15/15 passing, execution-wired via `run-test-suite.sh`'s tracked-file discovery. Includes an attack proof of the emit-check's pure core (fed the pre-fix rule, catches the exact live finding; fed the shipped rule, zero violations) and a drift guard against `loadDetail`'s own admission code.
- **Live emit-check**: `fsi-app/scripts/verify/id-redirect-target-audit.mjs` (new `data-audit: label=id-redirect-targets hard=true`, read-only, run live against production Supabase this session): 2,766 `intelligence_items` x 4 surfaces = 11,064 uuid URLs checked, 80 correctly land on not-found (legacy_id present but item inadmissible anywhere, the class the defect was in), **0 violations**.
- **Quarantined item's disposition** (reported only, not acted on, SELECT-only scope): `d2da85da-0912-497a-b645-31e4ca73cd18` is still `provenance_status='quarantined'`, unchanged since 2026-08-12. It carries 6 OPEN `integrity_flags` (data_quality/data_integrity), most recently opened 2026-09-13, none resolved: an unclosed research-or-erase investigation per remediation-discipline section 2.1.
- **UX compliance**: no visual/rendering change. The touched `page.tsx` files change only server-side redirect logic before the render path; no JSX, styling, or layout was touched. F35 (row-ux-coverage) and the rendering guard both passed with 0 violations.
- **Gates**: npm-glob suite 1562/1562 pass (+4 skipped, pre-existing, unrelated); `tsc --noEmit` clean; fitness runner 47/47 functions, 0 violations; `coverage-report.json` unchanged; the locked `lane-prepush-check.sh` (all 4 CI-parity steps, including goldens) PASS.
- Branch `lane/reg-redirect`.
