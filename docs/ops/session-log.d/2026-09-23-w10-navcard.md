# Lane W10-NavCard session log (2026-09-23)

Brief: `docs/dispatches/lane-briefs/2026-09-22/brief-w10-remaining-parts.md`, section "7. W10-NavCard"
plus its COMMON BLOCK. Worktree: `C:/Users/jason/dotfiles/.worktrees/wt-landdocs-0911`, branch
`lane/w10-navcard`, cut from `origin/master` at `b4feb6d6`.

Skills loaded via the Skill tool: `fsi-app:environmental-policy-and-innovation`, `frontend-design`,
`remediation-discipline`, `caros-ledge-platform-intent` (loaded before the governed-file write it
gated, `src/components/Sidebar.tsx`).

## Coordinator note on section numbering

The dispatch says "Read parts-brief 2.10 (NAV CARD)". The live `docs/design/parts-brief-2026-09-18.md`
has NAV CARD at 2.14 (2.10 is now CHIPS); the brief note 4 at the top of that file already documents a
prior mid-document insertion (2.16 added later) that shifted numbers. Content unchanged, only the
number moved; this lane cites the live section 2.14, per COMMON BLOCK's "artboard wins over the
README, and you say so."

## Reconnaissance findings (read-only, before any edit)

- `src/components/Sidebar.tsx` is already the ONE nav-card implementation site-wide. `AppShell.tsx` is
  its only call site (`grep -rn "Sidebar\b" src`), and `NO_SIDEBAR_ROUTES` already excludes
  `/login /auth /signup /onboarding /workspace/new` (bundle ruling 3, no nav card on the auth frame,
  already correct). `CommunityShell.tsx` only hides the global card with scoped CSS
  (`.cl-hide-global-sidebar`-style rule), it does not retype one. No duplicate to delete; this lane's
  job on the part itself is the `data-part` marker plus the fixture page.
- Research themes: `ResearchThemeCards.tsx` genuinely rendered `display: "grid",
  gridTemplateColumns: "repeat(4, 1fr)"` despite its own header claiming "a second facet row, not a
  second tile system", the 2026-09-20 measurement's finding was real, not a refutation. Fixed.
- `/market/series`: no route existed (`find src/app/market` showed only `[slug]`, `loading.tsx`,
  `page.tsx`); the "Series board" link on `/market` pointed at an in-page anchor
  (`#market-series-board`) into a `<MarketSeriesBoard>` mounted directly on `/market`.
- "Global room room" (Community): `grep -rn "Global room room" src` returns nothing. Already fixed
  (per dispatch instruction, skipped).

## Built

1. **`src/components/Sidebar.tsx`**: added `data-part="nav-card"` to both `<aside>` roots (desktop
   252px card, mobile 288px drawer), same part, two size variants, per the file's own header.
2. **`src/components/ui/__fixtures__/nav-counts-fixture.ts`** (new): frozen corpus-wide nav counts,
   read-only SELECT against the live database (project `kwrsbpiseruzbfwjpvsp`), 2026-09-23, exact
   queries and date in the file's own header. regulations 1319, market 62, research 32, operations 23,
   community 7, watchlist 0 (genuinely empty at capture time), byPriority CRITICAL 22 / HIGH 41 /
   MODERATE 1018 / LOW 359.
3. **`src/app/admin/parts/nav-card/page.tsx`** + **`part.json`** + **`NavCardPartsDemo.tsx`** (new):
   the sign-off page. Renders the real `Sidebar` unmodified; `NavCardPartsDemo.tsx` patches the shared
   `/api/workspace/bootstrap` fetch (module-load-time, same mechanism as
   `command-bar/CommandBarPartsDemo.tsx`) to return the frozen counts, WITHOUT mocking the signed-in
   operator's own identity (this is a platform-admin-only fixture; the Account/Admin footer rows show
   the real viewer, matching every other `/admin/parts/*` page). `src/app/admin/parts/page.tsx` NOT
   touched (it derives from `part.json` files per the ListRow lane's directory-registry change).
4. **`src/app/market/series/page.tsx`** (new): the Series board route, bundle ruling 6. Masthead
   (title "Market / Series board", size="list") + the real `MarketSeriesBoard` part, fed by the
   identical `fetchMarketSeriesBoard()` call `/market` already made for this section.
5. **`src/app/market/page.tsx`**: removed the inline `<div id="market-series-board"><MarketSeriesBoard
   .../></div>` block and the now-unused `MarketSeriesBoard` import. `fetchMarketSeriesBoard()` itself
   stays called on this page (its `Promise.all`) because `MarketIntelLedger`'s `headlineSeries`/
   `carbonCorridors` props still need the result.
6. **`src/components/market/MarketComparativeRibbon.tsx`**: the "Series board →" link is now a
   `next/link` `<Link href="/market/series">` instead of `<a href="#market-series-board">`.
7. **`src/components/research/ResearchThemeCards.tsx`**: the theme facet converted from a fixed
   `repeat(4, 1fr)` CSS grid of large cards to a wrapping `display: flex; flexWrap: wrap` facet row of
   compact pills (`data-part="theme-facet-row"`). Selection, live counts and the "+N new" badge are
   unchanged; the longer per-theme description now renders as a native `title` tooltip on the pill
   instead of a permanent second row (a facet row's controls are compact, a card is not). The mobile-
   only grid-column media query is gone; a wrapping flex row needs no width-based column override, so
   nothing was left behind to go stale (avoids a new F41 dead-media-query class instance).

## Consumers checked

- `git grep -rn "Sidebar\b" src`: one call site (`AppShell.tsx`), unaffected (prop interface
  unchanged; `data-part` is additive).
- `git grep -rn "market-series-board" src`: four hits after the edit, all prose/comments explaining
  the history of the move (this lane's own headers); no live selector or id target remains.
- `git grep -rn "ResearchThemeCards" src`: one consumer (`ResearchLedger.tsx`, `aboveRows` slot),
  props unchanged (`themes`/`selected`/`onSelect`), no caller change needed.
- ADRs: `grep -ril "nav.card\|market.series\|theme.facet" docs/decisions/`: no matching ADR; none of
  this lane's changes touch a cap, threshold, schema shape or vocabulary token that would require one.

## Gate outputs

- `bash .discipline/run-test-suite.sh`: two suite blocks, `tests 6351 / pass 6350 / fail 0` and
  `tests 1531 / pass 1527 / fail 0` (the 4-count gaps are pre-existing skips, not failures; exit 0).
- `node .discipline/fitness/runner.mjs`: `47 function(s) checked, 0 violation(s)`.
- `npx tsc --noEmit`: clean.
- Node contention: `tasklist | grep -ic node.exe` was checked before each gate (3, 3, 8-during-run
  which is the suite's own children, back to 3 after); no retry needed.
- Updated three existing structural tests that asserted the old shape and would otherwise have gone
  red on this diff: `Sidebar.npmtest.mjs` (the drawer `<aside>` regex now tolerates the new `data-part`
  attribute between `<aside` and `className`), `ResearchThemeCards.npmtest.mjs` (grid-layout assertions
  replaced with facet-row assertions), `MarketComparativeRibbon.npmtest.mjs` (anchor-href assertions
  replaced with `next/link` route assertions, plus a new test on the `/market/series` route itself).

## UX compliance

- **`/admin/parts/nav-card`**, primary goal: platform-admin sign-off review of the NavCard part (both
  size variants) against frozen real counts before it ships. Path: nav to `/admin/parts/nav-card` (one
  step; linked from `/admin/parts` automatically via the directory-derived index). One primary action:
  none (read-only gallery, law 1/9). Feedback state: N/A, no async action on this page itself (the
  bootstrap fetch it depends on is intercepted client-side; no user-triggered request).
- **`/market/series`**, primary goal: review every registered market_series producer's latest
  observation in one place. Path: from `/market`, click "Series board →" in the Headline series card
  (one step) or nav directly. One primary action: none (a read surface); each row's `WatchButton` is a
  secondary, per-row action with its own existing pending/success/failure states (unchanged by this
  lane, MarketSeriesBoard itself untouched).
- **`ResearchThemeCards` facet row**, primary goal: filter Research findings by theme. Path: unchanged,
  one click on a pill toggles the filter. One primary action: the pill click itself (`aria-pressed`,
  law 8). Feedback state: immediate (the list and the Window row's count line update synchronously on
  click; no async gap to cover). Accessibility: pills are 36px tall with 8px row gap between them
  (law 2's 24px+8px-clearance alternative, not the 44px minimum, a compact filter row, same class as
  RailCard's own 24px facet rows, parts-brief 2.12).

## Open items

- Rendering guard (`node .discipline/rendering/run-rendering-guard.mjs`) was NOT run in this session
  segment (no dev server / Playwright browser session available in this tool-call sequence); the two
  required PASS runs are outstanding before this lane's PR should land. Flagged, not silently skipped.
- Bundle rulings 3 and 6 were read from this lane's own dispatch text (quoted verbatim in the brief),
  not re-derived from a separate numbered "ruling" list in `docs/design/parts-brief-2026-09-18.md`
  (that file's own rulings, where present, are inline prose per section, not a numbered ruling index),
  their content is unambiguous and independently corroborated by parts-brief section 2.15 PAGE SCOPE
  ("Series board → /market/series") and by `AppShell.tsx`'s own live `NO_SIDEBAR_ROUTES`.
- RailCard lane (touching `DetailShell.tsx`, `ListSurfaceRailCards.tsx`, `DashboardWatchlist`, the
  admin rail) had not merged to `origin/master` as of this lane's `git fetch` (master still at
  `b4feb6d6`); no rebase was needed. Re-check before push if RailCard lands first.
