# Lane W10-Masthead session log (2026-09-22)

Brief: `docs/dispatches/lane-briefs/2026-09-22/brief-w10-remaining-parts.md`, section "2. W10-Masthead"
plus its COMMON BLOCK. Worktree: `C:/Users/jason/dotfiles/.worktrees/wt-landdocs-0911`, branch
`lane/w10-masthead`, cut from `origin/master` at `ddc5ccda`.

Skills: `fsi-app:environmental-policy-and-innovation` and `remediation-discipline` do not exist as
invocable skills in this session's skill list (confirmed by `ToolSearch`/`Skill` lookup). `frontend-design`
loaded via the Skill tool. `caros-ledge-platform-intent` also does not exist as an invocable skill here;
its `SKILL.md` was read directly before the governed-file edit it gated
(`src/app/admin/factors/page.tsx`), same fallback this lane's predecessor (W10-SectionHeader) used.

## STOP: this lane is NOT complete. Real, gate-verified partial progress only.

The brief's full scope (one Masthead on all 17+ routes plus /watchlist /privacy /invitations plus the
auth frame; migrate every page-local masthead variant; fix two measured mobile pixel regressions; a
presence report; the real Rendering guard; the full npm/fitness/push-gate stack) exceeds what this
session could complete with genuinely run, non-fabricated verification. Rather than report a fabricated
six-line final summary with invented commit SHAs or guard output (forbidden by the coordinator's own
correction and by CLAUDE.md rule 14/15), this is an honest interim state: what was found, what was
fixed and verified, what is explicitly left open.

## Reconnaissance findings (read-only, before any edit)

- `src/components/ui/Masthead.tsx` (the shared part) already exists, already implements the artboard
 21a shape (eyebrow, Anton 34/28 ramp, dek, one CommandBar, DERIVED NAME/notice slots) - built by an
 earlier lane, not this one.
- Most routes already compose it: `RegulationDetailSurface.tsx`, `WatchlistSurface.tsx`,
 `ResearchFindingDetailSurface.tsx`, `MarketSignalDetailSurface.tsx`, `RegulationsLedger.tsx`,
 `ObligationRegisterPageView.tsx`, `OperationsLedger.tsx`, `MarketIntelLedger.tsx`,
 `DashboardMasthead.tsx` (dashboard's own composition, confirmed correct - not a duplicate), plus the
 regulations/operations/market list+detail routes and `/regulations/register`. Comments in
 `regulations/page.tsx` and `regulations/[slug]/page.tsx` confirm "the old EditorialMasthead is gone"
 for those routes specifically - an earlier lane (UILISTS, 2026-09-06) already did this migration
 there.
- Real remaining page-local masthead usage found: `/admin/factors/page.tsx` (imported the legacy
 `PageMasthead` directly) - fixed this lane, see below. `CommunityMasthead.tsx` still wraps the older
 `EditorialMasthead.tsx` (which itself wraps `PageMasthead.tsx`) rather than the shared `Masthead` - 
 **not fixed this lane**, see Open items.
- `AuthFrame.tsx` (the artboard 16/17 left identity panel) already exists and matches the artboards I
 opened (`docs/design/handoff-2026-09-07/screens/16-auth.png`, `17-onboarding.png`) closely: VOL line,
 Anton app-name title, tagline, band-scale legend, disclaimer, no CommandBar (matches "no command bar"
 on the auth frame). It does NOT compose the shared `Masthead` component - it hand-rolls the same
 shape with a different prop surface (title = brand name not page title, no size ramp, dek replaced by
 tagline + band legend + disclaimer). Judged this a genuine undrawn-case conflict per the COMMON
 BLOCK's own STOP rule rather than force a mismatched merge - see Open items. Also note: the
 dispatch's own text says the masthead sits "inside the right panel" of the auth frame; the artboards
 themselves show it in the LEFT (identity) panel, form on the right. Per the COMMON BLOCK, "the
 artboard wins over the README and you say so" - recorded here.
- Found the eight dated F49 Anton allowlist entries named in the brief: `src/app/community/profile
 /moderation/discover/directory/browse/benchmarks/page.tsx` (six, all the same hand-typed Anton h2 +
 description-paragraph shape inside `CommunityShell`) and `src/app/auth/reset-password/update-password
 /page.tsx` (two, same shape, a bare Anton h1). All eight markers cited "Review-by: SectionHeader lane
 / operator ruling" - SectionHeader (`src/components/ui/SectionHeader.tsx`) merged in the immediately
 prior lane (W10-SectionHeader, `ddc5ccda`), so the review-by condition is now resolved.
- Found the mobile top-bar root cause for the "59 against 56" measurement in the brief: `TopBar.tsx`'s
 `<header>` was `height: 56` with `<BandGradientRule height={3}/>` stacked above it in normal flow - 
 56 + 3 = 59, 3px over the artboard's stated total. Fixed, see below.
- Investigated the "758 against 778" content-column measurement (`PageFrame.tsx` + `Sidebar.tsx` +
 `AppShell.tsx`): by hand-arithmetic (nav box 252px width + 16px left margin = 268px consumed of the
 1440px frame; `PageFrame` padding 80px + gap 28px + rail 300px) the content column computes to 764px,
 not the measured 758px or the target 778px - a 6px gap between my arithmetic and the measured value
 that is most plausibly a scrollbar-width effect, and a 14-20px gap to the target that I could not
 attribute to a single line of code by reading alone. **[HYPOTHESIS, NOT FIXED]**: guessing at a
 arithmetic-only fix here without an actual rendered measurement (the layout guard / rendering guard,
 neither run this session) would risk shipping an unverified pixel change on a shared shell three other
 lanes' surfaces depend on. Left open rather than guessed at, per CLAUDE.md rule 14 (a finding is a
 hypothesis until verified) - see Open items for the exact next step.

## Fixed and gate-verified this lane

1. **TopBar.tsx mobile height, 59px → 56px.** `<header>` height changed from 56 to 53 so
 `BandGradientRule` (3px) + header (53px) = 56px total, matching the artboard's stated whole-bar
 height. Touch targets (44×44 buttons) are padding-driven, not header-height-driven, so unaffected.
 Doc comment updated to record the root cause and the fix.
2. **`/admin/factors/page.tsx`** migrated from the page-local `PageMasthead` to the shared `Masthead`
 (`size="list"`, `eyebrowSuffix="Platform admin · emission factors"`, same dek text as before, real
 `nowIso`/`dateLabel` wiring copied from `OperationsLedger.tsx`'s own pattern). No data/query change.
3. **All eight F49 Anton allowlist entries cleared** (community profile/moderation/discover/directory
 /browse/benchmarks, auth reset-password/update-password): each hand-typed Anton `h1`/`h2` replaced
 with `<SectionHeader title="…" />` (adding `meta` for `browse/page.tsx`'s right-aligned filter note,
 which previously lived in a second flex row - folded into `SectionHeader`'s own `meta` slot instead
 of a page-local re-layout). The sibling description `<p>` below each title is untouched - it was
 never an F49 literal (no card-shell/chip/state-note pattern on it), so it stays plain content, not
 pulled into `SectionHeader`'s prop surface (which has no description slot). Judgment call, recorded
 honestly: `SectionHeader`'s own padding/border-bottom (designed for an S-section head inside a card)
 is now visible on these two bare (non-card) auth pages and the six community sub-route headers,
 which is a visual change from the previous zero-padding/zero-border `h1`/`h2`. Chose reuse-before-
 construction per the repo's own standing rule over inventing a bare-title variant; the brief's own
 ruling is unconditional ("no extension") so a "best defensible reuse, disclosed" reading is what I
 applied. Flagged for operator/coordinator review, not hidden.
4. **`/admin/parts/masthead` fixture page built** (`src/app/admin/parts/masthead/page.tsx`), added to
 the parts index (`src/app/admin/parts/page.tsx`). Two states (`size="list"`, `size="detail"` with the
 frozen record's own unabridged title, proving the title wraps rather than truncates), both from the
 same frozen real record every other W10 parts fixture already uses
 (`record-grade-fixture.ts`, `intelligence_items.id = f8268063-0e07-4562-82da-a1373d6dd797`, EC
 391/2009, EUR-Lex) - no new SELECT, no invented strings, id and a real date-fact value shown in the
 header line above each state per the operator's fixture convention.

## Gates actually run (verbatim summary lines, nothing fabricated)

- `node .discipline/fitness/functions/F49-parts-not-pages.mjs` run directly against the live tree
 (not just its own unit test): **0 violations, 40 files scanned** (run twice, before and after the
 masthead fixture page was added; unchanged).
- `npx tsc --noEmit`: **clean, exit 0** (run three times across the edit rounds).
- `bash .discipline/run-test-suite.sh`: **tests 1518, pass 1514, fail 0, skipped 4, exit 0** (the
 `audit-finding-status` step's 607 unlabeled-finding warnings are pre-existing state in old
 `docs/audits/*.md` files this lane never touched, confirmed by `git status` showing no changes under
 `docs/audits/`; the script's own exit code is 0, so this is not a gate failure).
- `node fsi-app/.discipline/fitness/runner.mjs`: **47 functions checked, 0 violations** (run twice,
 same as F49 above).
- `tasklist | grep -ic node.exe` checked before each gate run per the coordinator's correction: 0 both
 times, no contention wait needed.

## Gates NOT run this session (named honestly, not silently skipped)

- `node .discipline/rendering/run-rendering-guard.mjs` (the real Playwright guard) - not run. This is
 the only instrument that could confirm the top-bar/content-column pixel claims at 375/1440 rather
 than by arithmetic; its absence is why the "758→778" fix is left as a hypothesis, not shipped.
- `sh fsi-app/.discipline/hooks/lib/run-npmtest-suites.sh` (the npm-glob suite) - not run.
- `DISCIPLINE_HOOK_TRAMPOLINE=1 sh fsi-app/.discipline/hooks/pre-push` (the locked push-gate preflight)
 - not run.
- No commit has been made yet as of this log entry; it is written before the commit per this
 worktree's own convention (session log lands in the same range as the code).

## Consumers checked

- `grep -rn "PageMasthead" fsi-app/src`: after this lane's edit, remaining importers are
 `src/components/admin/AdminDashboard.tsx` (a shared component, out of any single route's write set,
 unchanged) and five `/admin/parts/*` fixture pages (`page.tsx`, `action-card`, `section-index`,
 `item-group`, `fact-card`) - fixture-page consistency, lower priority than a real customer/admin
 route, not touched this lane.
- `grep -rn "fitness-allow: F49"` fsi-app/src: zero matches after this lane's edits (was eight).
- `grep -rn "<SectionHeader"` fsi-app/src: now used by `DetailShell.tsx` (pre-existing, W10-
 SectionHeader lane) plus the eight sites this lane added - no prop-shape conflict, `index`/`meta` both
 optional.

## UX compliance

**`/admin/factors`.** Primary goal: unchanged - platform admin reads the emission-factors table.
Path: unchanged, one page load. One primary action: none (read-only). Feedback state: n/a, no async
control added or removed by this change.

**`/community/profile`, `/moderation`, `/discover`, `/directory`, `/browse`, `/benchmarks`.** Primary
goal: unchanged per route (edit profile / triage reports / follow a topic / read the peer directory /
join a public group / read house benchmarks). Path: unchanged navigation and step count - only the
section title's rendering component changed, not the flow. One primary action: unchanged per route,
`SectionHeader` carries no interactive control. Feedback state: n/a, `SectionHeader` is presentational.

**`/auth/reset-password`, `/auth/update-password`.** Primary goal: unchanged (request a reset link /
set a new password). Path: unchanged. One primary action: unchanged (`Send reset link` / the password
form's submit), `SectionHeader` sits above the form, does not compete with it (law 7). Feedback state:
unchanged - the existing `AuthErrorBanner`/`sent` confirmation states are untouched.

**`/admin/parts/masthead`.** Primary goal: platform-admin fixture sign-off, read the part's real
states. Path: one page load, static fixture data, no interaction required. One primary action: none.
Feedback state: n/a, no database read, no async behavior.

Law 2 (touch targets): `TopBar.tsx`'s height fix does not change any button's own box (44×44 via
padding, not header height) - re-verified by reading the diff, not by a live measurement (guard not
run). No new interactive control was added by any other change in this lane.

## Open items (explicit, for the coordinator/operator)

1. **CommunityMasthead.tsx still wraps `EditorialMasthead`/`PageMasthead`, not the shared `Masthead`.**
 Real remaining work: `/community` and all of its sub-routes render through it. Not attempted this
 session - its search bar + scope-chip UI does not map cleanly onto `Masthead`'s current prop surface
 (title/dek/commandBar/notice), and the parts-brief's own COMMAND BAR rule (section 2.5: "remove the
 Search|Ask toggle and the second Search button... one control on every page") suggests the correct
 fix replaces this bespoke search UI with the standard `CommandBar` prop rather than preserving it - 
 real design surgery, not a mechanical swap, left for a dedicated pass.
2. **AuthFrame.tsx does not compose the shared `Masthead`.** See the reconnaissance note above - judged
 a genuine undrawn-case conflict (its shape does not match any other `Masthead` caller) rather than
 force a merge that risks the artboard-matched panel an earlier lane built. Needs an explicit ruling:
 does "the Masthead IS used there" mean literal component reuse (requiring a new prop shape/variant)
 or the current artboard-matched bespoke panel already satisfies the intent.
3. **Content column 758→778 fix not shipped**, hypothesis only (see above) - needs the real rendering
 guard or layout guard run against a built app to locate the actual 14-20px source before touching
 `PageFrame.tsx`/`Sidebar.tsx`/`AppShell.tsx`, which are shared by every route.
4. **No presence report produced** for the 17+ routes plus `/watchlist`, `/privacy`, `/invitations` - 
 most already render the shared `Masthead` per the reconnaissance findings above (file-level evidence
 given there), but a route-by-route (route, file, line) table as the brief and COMMON BLOCK both
 require was not built this session.
5. **Rendering guard, npm-glob suite, and the locked push-gate preflight were not run** (see "Gates NOT
 run" above) - required before this branch could honestly be called handoff-ready.
6. **`/admin/parts` fixture pages still import the legacy `PageMasthead`** (five of them, listed in
 "Consumers checked") - lower priority than real routes, not touched.

No commit has been made as of this entry (see below for the commit that follows it in the same range).
