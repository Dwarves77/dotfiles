## 2026-09-20, lane M4: the brief-export hop wired at mint, with its own harness family

Landed on branch `lane/m4-brief-chain-2026-09-20`, cut from `origin/master` at `cc038a47`, in worktree
`wt-l34-detail-admin-primitives`. Timestamp from `date -u +%Y-%m-%dT%H:%M:%SZ`: 2026-09-20T19:25:29Z.

### Why

`docs/audits/stage-audit-2026-09-18/s3-evaluate.md` (rows 1 and 14) and the README's own "Mint to brief
chain" line: mint leaves a stub `full_brief` and nothing upgrades it automatically; `brief-export.yml` and
`brief-apply.yml` were both `workflow_dispatch` only, with a session lane's own authoring pass between
them. This lane's own brief (`docs/dispatches/lane-briefs/2026-09-19/brief-m4.md`) was corrected by
Amendment 1 (coordinator, 2026-09-20, in the coordinator's scratchpad): the export workflow wrote NO
harness artifact at all [CONFIRMED by grep of `brief-export.yml` on master `cc038a47`], and the
loop-manifest's own `population-turn-to-brief-export` hop was filed under family `brief-apply` "for now".
This lane closes that gap per Amendment 1 section C.

### What landed

1. New harness family `brief-export`, registered by descriptor only (build plan section 6.8 Rule A, no
   edit to any derived list): `fsi-app/scripts/harness-runs/brief-export/family.json`, `FAMILY.md`,
   `pending/2026-09-20-m4.md` (modeled on the `downstream-chain` family lane M3 registered 2026-09-19).
2. `fsi-app/scripts/turns/emit-brief-export-artifact.mjs`, modeled on `emit-downstream-chain-artifact.mjs`:
   reuses `writeRunArtifact`/`claimRunId`/`hashHarnessVersion` from `scripts/lib/run-artifact.mjs`, no copy
   of the writer. Config carries mode, selection, limit, upstream_name, upstream_run_id, loop_run_id,
   batch_path, branch; `per_item` carries one row per exported id; writes on every firing, including a
   zero-id run. `loop_run_id` resolved through `resolveLoopRunIdFromUpstream` (lane M3b's
   `scripts/lib/loop-run-id.mjs`), upstream name "Population turn".
3. `.github/workflows/brief-export.yml`: gained a `workflow_run` trigger on "Population turn" completing
   (job-level `if:` gate matching `corpus-turn.yml`/`downstream-chain.yml`'s own posture), a "Resolve run
   parameters" step normalizing `RUN_*` env vars from `inputs.*` (workflow_dispatch) or this file's own
   defaults (chained workflow_run, where `inputs.*` does not exist), and a final "Record this run's own
   harness-run artifact" step calling the new emitter, folded into the existing "Commit this run's exported
   part(s)" step so one commit/PR carries both the export part file (when any) and this run's harness
   artifact, never two competing branches for one run.
4. `.discipline/governance/loop-manifest.mjs`: the `population-turn-to-brief-export` hop moves to
   `family: 'brief-export'`, `enforceEdge: true` (was `'brief-apply'`, `false`); note rewritten to the
   present. `enforceFired` stays `false` per the coordinator's own instruction (flipped after the proof
   run). F50's own printed line, "hops not yet enforced", is unchanged at 11 both before and after this
   edit, because only `enforceEdge` flipped, never `enforceFired` (the metric counts
   `!(enforceEdge && enforceFired)`, which stays true for this hop either way until the proof run).
5. `scripts/lib/loop-run-id.test.mjs`: the six-hop attack chain (lane M3b) extended to seven hops, adding
   `brief-export` resolving off the same mint artifact (hop 4, "Population turn") as `downstream-chain`
   (hop 5) does, proving the shared name-to-family map resolves both branches off one upstream artifact.
6. `scripts/harness-runs/family-registry.test.mjs`: `LIVE_FAMILIES` gains `"brief-export"` (a local test
   constant, not a shared-append registry under N2's Rule A; F51 does not gate this file).
7. `scripts/verify/population-report.mjs` (actual path; the brief and the build plan's own row both name
   `scripts/maintenance/population-report.mjs`, which does not exist on this tree -- correction recorded
   here, not silently followed): a new "briefs owed" line (item 4 of the original brief), printed at CLI
   level like the existing "ledger-consume: N candidates await a verdict" line, never a STORES row. Reads
   the SAME stored-column stub predicate `countBriefsPendingStale` already uses (`item_grade='record'`,
   `provenance_status='verified'`, `is_archived=false`) -- never a `full_brief` text scan, per Amendment 1
   section E's own constraint. Buckets by `item_type` and age since `created_at` (under 7 days, 7 to 30,
   over 30), one line per item_type with a nonzero total plus a grand total line. New pure functions
   `bucketBriefsOwedByTypeAndAge` and `renderBriefsOwedLines`, both unit-tested in
   `population-report.test.mjs`.
8. `scripts/turns/record-briefs/README.md`: new section "The chain, automatic and human halves" (item 6
   of the original brief), documenting which half of the chain now fires on its own and which stays
   session-lane-authored by design.
9. F45 duplicate-code fix round (the pre-push gate refused the first pass, `+56` duplicated lines against
   the merge-base): extracted the three-statement `hashHarnessVersion`/`claimRunId`/
   `resolveLoopRunIdFromUpstream` sequence every `emit-*-artifact.mjs` writer's own `main()` repeated
   verbatim into `resolveHarnessRunContext` (`scripts/lib/loop-run-id.mjs`), and the CONVENTION.md
   top-level 11-field artifact envelope every `buildArtifact` hand-wrote into `buildRunArtifactEnvelope`
   (`scripts/lib/run-artifact.mjs`). `emit-brief-export-artifact.mjs` now calls both; `emit-corpus-turn-
   artifact.mjs` and `emit-downstream-chain-artifact.mjs` (already on master) were updated to call
   `resolveHarnessRunContext` too (their own `buildArtifact` return shapes were left as-is: only
   `brief-export`'s shared an exact 8-line window with `downstream-chain`'s, so only that one needed the
   envelope helper to clear the regression). Second fitness-runner pass: 0 violations, 45/45 functions.

### Stop conditions invoked (not deviation; named in the brief itself)

- **The record-briefs batch-skeleton on a `brief-lane/<loop_run_id>` branch** (original brief item 2's
  fuller ask) was NOT built. `schema.mjs`'s `validateRecordBriefsEntry` requires a non-empty `body` (the
  full authored markdown), a `metadata` object that round-trips through the real `parseAgentOutput`, and a
  `claims[]` array with every FACT claim's `source_span` verbatim-checked against the item's pool text --
  none of which an auto-export can honestly populate before a session lane has authored anything, and the
  schema has no "owed"/stub entry state at all. This is exactly the brief's own named stop condition ("the
  batch skeleton shape is not representable with the existing validator, say which field"): extending
  `schema.mjs` to accept an unauthored stub entry is a contract decision this lane does not make. Recorded
  in `brief-export.yml`'s own header ("NOT BUILT BY THIS LANE") and in the record-briefs README's new
  section.
- **`brief-apply.yml`'s `push` trigger** (original brief item 3: dry mode automatic on a batch-file merge
  to master, path-filtered) was NOT built in this lane's own time. This is a separate, comparably-sized
  wiring task (a new trigger, a job-level gate distinguishing push from dispatch, a path filter proof, and
  its own golden attack test per item 6) that this lane did not reach. Not a stop on an unresolved
  question, an honest scope limit; reported here rather than left silently undone.
- **Golden (attack form) on the two ymls' triggers and the path filter** (original brief item 6) was NOT
  built, for the same reason: `brief-apply.yml`'s own trigger change did not land, so a golden covering
  "the two ymls' triggers" together is incomplete without it. `brief-export.yml`'s own trigger is proven
  indirectly by F50's own live-tree test (`F50-loop-wiring.test.mjs`), which reads the real committed yml
  and fails if the `workflow_run` edge or the producer name is wrong -- not a dedicated golden of this
  lane's own, but a real, already-wired mechanical check on the exact fact a golden would assert.

### Corrections recorded honestly

- `docs/plans/complete-system-build-plan-2026-09-04.md` section 6.1's own M4 row and the original
  `brief-m4.md` both name `scripts/maintenance/population-report.mjs`; the live file is
  `fsi-app/scripts/verify/population-report.mjs`. Confirmed by `find`, not guessed; used the real path.

### Evidence

- `node --test scripts/turns/emit-brief-export-artifact.test.mjs`: 8/8 pass.
- `node --test scripts/lib/loop-run-id.test.mjs`: 14/14 pass (seven-hop attack chain included).
- `node --test .discipline/fitness/functions/F50-loop-wiring.test.mjs`: 12/12 pass, including the LIVE
  test against the real committed tree.
- `node --test scripts/harness-runs/family-registry.test.mjs scripts/lib/run-artifact.test.mjs`: 104/104
  pass (family-registry portion + run-artifact portion together).
- `node --test scripts/harness-runs/governing-files.test.mjs`: 20/20 pass.
- `node --test .discipline/fitness/functions/F28-harness-run-integrity.test.mjs`: 32/32 pass, including
  the LIVE test and the FAMILY-DESCRIPTOR-REALITY check (every governing_files path of every registered
  family, including `brief-export`, exists on disk).
- `node --test .discipline/fitness/functions/F51-no-shared-append.test.mjs`: 42/42 pass.
- `node --test scripts/verify/population-report.test.mjs`: 72/72 pass, including the six new
  `bucketBriefsOwedByTypeAndAge`/`renderBriefsOwedLines` tests.

No `.ts`/`.tsx` file touched; `tsc --noEmit` not run per the lane contract's own conditional ("if you
touched .ts/.tsx"). No UX-facing `.tsx`/`.css` touched; no UX compliance block required.

### Skill gate

First tool call this session: the Skill tool, `fsi-app:environmental-policy-and-innovation`, per Amendment
1 section B (lane G1's own gate judging this session's transcript). No denial observed.
