## 2026-09-18, W9 lane m9a: loop manifest and gate F50 land, both flags reporting today's real state

Lane m9a (build plan section 6.1 row M9, the gate half). `.discipline/governance/loop-manifest.mjs` states
the 11 hops of the loop (section 1) as data, each with `enforceEdge`/`enforceFired` booleans instead of one
`enforce` flag (the ledger-consume edge already existed in the committed yml before anything had ever fired
through it, so one boolean could not say both things). [CONFIRMED, by reading every named `.github/workflows/*.yml`
file directly this session] more of the loop is already wired than the plan's own prose implied:
`ledger-consume.yml`, `population-turn.yml`, `downstream-chain.yml` and `propagation-drain.yml` all already
carry their `workflow_run` edges; only `corpus-turn.yml` and `brief-export.yml` genuinely lack theirs, and
`fetch-drain.yml` / a gate-a-rescan consumer do not exist on the tree yet (marked `consumerPending`, named
to the lane that creates them, M1 and M6). `scripts/lib/run-artifact.mjs` gained two optional fields, `trigger` and `upstream_run_id`,
stamped in the one shared writer from `GITHUB_EVENT_NAME`/`GITHUB_EVENT_WORKFLOW_RUN_ID`, defaulting to
`"manual"`; no `.github/workflows/*.yml` file was edited (out of scope for this lane), so every hop's
`enforceFired` stays false until M1 through M6 add the new `env:` line to their own workflows and a real
`workflow_run`-triggered run lands.

Fitness function F50 (`loop-wiring`) checks every `enforceEdge`/`enforceFired` claim against the real
tree (a line-based yml scan, no YAML parser dependency, matching secrets-reference-audit.mjs's existing
precedent in the same directory) and reports every not-yet-enforced hop rather than failing on it:
`node .discipline/fitness/runner.mjs --function=F50` prints `hops not yet enforced: 11` with `0
violation(s)`, matching the plan's own acceptance criterion. Registered in the manifest, invariant
RD-74 added (`.discipline/governance/invariants.mjs`), a new SKILL.md category 46 written as the anchor
(`remediation-discipline`), and `CONVENTION.md`'s marker baseline re-seeded 56 to 57. Editing
`run-artifact.mjs` and `CONVENTION.md` (both meta-harness governing files) moved that family's own
`harness_version`; `scripts/harness-runs/meta-harness/PENDING-RUN.md` re-pinned (Re-pin 4) to the new hash
following the file's own established convention, closing the F28 staleness this lane's own edit caused.

Evidence: `node .discipline/fitness/runner.mjs` (42 functions, 0 violations, live this session);
`node .discipline/governance/invariant-coverage.mjs` (130 invariants + 63 doctrines, PASS); `node --test`
on `scripts/lib/run-artifact.test.mjs` (71/71), `.discipline/governance/loop-manifest.test.mjs` (9/9),
`.discipline/fitness/functions/F50-loop-wiring.test.mjs` (12/12), all green.

Not done, named in the brief as M1 through M6's job: no `.github/workflows/*.yml` file touched; no hop's
`enforceEdge`/`enforceFired` flipped to true beyond what the real tree already proved; the dispatch
ledger, closure-gate.mjs, community code and `src/` were left untouched per the brief's explicit scope.
