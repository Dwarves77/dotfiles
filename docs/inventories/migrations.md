# Migrations Inventory

**Generated 2026-05-21** (Layer 4 cross-skill consistency dispatch).

## Migrations

| # | File | Subject (from header comment) |
|---|---|---|
| 001 | 001_schema.sql | FSI Phase 2: Database Schema |
| 002 | 002_rls.sql | FSI Phase 2: Row Level Security |
| 003 | 003_indexes.sql | FSI Phase 2: Performance Indexes |
| 004 | 004_source_trust_framework.sql | ══════════════════════════════════════════════════════════════ |
| 005 | 005_rls_trust_framework.sql | ══════════════════════════════════════════════════════════════ |
| 006 | 006_multi_tenant.sql | ══════════════════════════════════════════════════════════════ |
| 006 | 006_rls_multi_tenant.sql | ══════════════════════════════════════════════════════════════ |
| 007 | 007_community_layer.sql | ══════════════════════════════════════════════════════════════ |
| 007 | 007_full_brief.sql | Add full_brief column for skill-standard intelligence briefs |
| 007 | 007_rls_community.sql | ══════════════════════════════════════════════════════════════ |
| 009 | 009_capture_undeclared_tables.sql | ════════════════════════════════════════════════════════════════════ |
| 010 | 010_migrate_legacy_to_item.sql | ════════════════════════════════════════════════════════════════════ |
| 011 | 011_backfill_orphan_supersessions.sql | ════════════════════════════════════════════════════════════════════ |
| 013 | 013_drop_legacy_tables.sql | ════════════════════════════════════════════════════════════════════ |
| 015 | 015_provisional_recommended_classification.sql | ════════════════════════════════════════════════════════════════════ |
| 016 | 016_add_processing_pause.sql | ════════════════════════════════════════════════════════════════════ |
| 017 | 017_add_admin_only_to_sources.sql | ════════════════════════════════════════════════════════════════════ |
| 018 | 018_b2_brief_schema.sql | ════════════════════════════════════════════════════════════════════ |
| 019 | 019_reclassify_mistyped_tools.sql | ════════════════════════════════════════════════════════════════════ |
| 020 | 020_intersection_readiness.sql | ════════════════════════════════════════════════════════════════════ |
| 021 | 021_canonical_source_candidates.sql | ════════════════════════════════════════════════════════════════════ |
| 022 | 022_canonical_source_classification_cache.sql | ════════════════════════════════════════════════════════════════════ |
| 023 | 023_intersection_detection_function.sql | ════════════════════════════════════════════════════════════════════ |
| 024 | 024_admin_action_cooldowns.sql | Migration 024 — Admin action cooldowns |
| 025 | 025_sector_activation_interest.sql | Migration 025 — Sector activation interest tracking |
| 026 | 026_research_pipeline_stage.sql | 026_research_pipeline_stage.sql |
| 027 | 027_user_profiles.sql | Migration 027 — User profiles |
| 028 | 028_community_groups.sql | Migration 028 — Community groups |
| 029 | 029_community_group_members.sql | Migration 029 — Community group members and invitations |
| 030 | 030_community_posts.sql | Migration 030 — Community posts |
| 031 | 031_community_topics.sql | Migration 031 — Community topics |
| 032 | 032_community_notifications_moderation.sql | Migration 032 — Community notifications, preferences, and moderation reports |
| 033 | 033_jurisdiction_iso.sql | 033_jurisdiction_iso.sql |
| 034 | 034_staged_updates_materialization_error.sql | 034_staged_updates_materialization_error.sql |
| 035 | 035_agent_integrity_flags.sql | 035_agent_integrity_flags.sql |
| 036 | 036_admin_notifications_rpc.sql | 036_admin_notifications_rpc.sql |
| 037 | 037_source_verification.sql | 037_source_verification.sql |
| 038 | 038_bulk_import_audit.sql | 038_bulk_import_audit.sql |
| 039 | 039_coverage_matrix_rpc.sql | 039_coverage_matrix_rpc.sql |
| 040 | 040_discovery_provenance.sql | 038a_discovery_provenance.sql |
| 041 | 041_post_promotions.sql | Migration 041 — Community-post promotion audit |
| 042 | 042_community_region_counts_rpc.sql | Migration 042 — Community region count RPC |
| 043 | 043_security_advisor_fixes.sql | 043_security_advisor_fixes.sql |
| 044 | 044_integrity_flag_trigger_tune.sql | 044_integrity_flag_trigger_tune.sql |
| 045 | 045_orphan_slugs_and_acf_dedup.sql | ════════════════════════════════════════════════════════════════════ |
| 046 | 046_community_rls_recursion_fix.sql | 046_community_rls_recursion_fix.sql |
| 047 | 047_workspace_intelligence_slim_rpc.sql | Slim sibling of get_workspace_intelligence for list-view callers. |
| 048 | 048_integrity_flags_platform.sql | 048_integrity_flags_platform.sql |
| 049 | 049_perf_v2_indexes.sql | Migration 049 — perf v2 indexes (2026-05-08) |
| 050 | 050_integrity_flags_workflow_gap.sql | Migration 050: Widen integrity_flags.category CHECK constraint |
| 051 | 051_sources_last_scanned_recovery.sql | 051_sources_last_scanned_recovery.sql |
| 052 | 052_raw_fetches.sql | ════════════════════════════════════════════════════════════════════ |
| 053 | 053_intelligence_item_versions.sql | ════════════════════════════════════════════════════════════════════ |
| 054 | 054_sources_scoreboard_columns.sql | ════════════════════════════════════════════════════════════════════ |
| 055 | 055_sources_auto_run_enabled.sql | ════════════════════════════════════════════════════════════════════ |
| 056 | 056_sources_access_method_extension.sql | ════════════════════════════════════════════════════════════════════ |
| 057 | 057_agent_runs.sql | ════════════════════════════════════════════════════════════════════ |
| 058 | 058_ingestion_control_log.sql | ════════════════════════════════════════════════════════════════════ |
| 059 | 059_ingestion_state.sql | ════════════════════════════════════════════════════════════════════ |
| 060 | 060_user_watchlist.sql | Migration 060: user_watchlist |
| 061 | 061_coverage_gaps.sql | Migration 061: coverage_gaps |
| 062 | 062_intelligence_items_hidden_reason.sql | Migration 062: intelligence_items.hidden_reason |
| 063 | 063_sources_classification_axes.sql | Migration 063: sources classification axes (5-axis framework v1) |
| 064 | 064_workspace_intelligence_dashboard_rpc.sql | Dashboard projection sibling of get_workspace_intelligence. |
| 065 | 065_pending_first_fetch_queue.sql | ════════════════════════════════════════════════════════════════════ |
| 066 | 066_workspace_intelligence_listings_rpc.sql | Listings projection sibling of get_workspace_intelligence. |
| 067 | 067_sources_classification_metadata.sql | sources classification metadata |
| 068 | 068_workspace_intelligence_aggregates.sql | Aggregates RPC for the workspace intelligence dashboard. |
| 069 | 069_workspace_intelligence_aggregates_scoped.sql | Scoped sibling of get_workspace_intelligence_aggregates (068). |
| 070 | 070_phase1_routing_rpcs.sql | ───────────────────────────────────────────────────────────────────────────── |
| 071 | 071_deterministic_tiebreaker.sql | Migration 071: deterministic tiebreaker on LIMIT-bounded RPCs. |
| 072 | 072_jurisdiction_normalizer.sql | 072_jurisdiction_normalizer.sql |
| 073 | 073_shared_workspace_scope.sql | Migration 073: extract shared workspace-scope SQL function. |
| 074 | 074_ecovadis_vendor_reclass.sql | Migration 074: reclassify EcoVadis as vendor_corporate |
| 075 | 075_profiles_consolidation_phase1.sql | Migration 075 — Consolidate user_profiles into profiles (Phase 1) |
| 076 | 076_org_invitations.sql | Migration 076 — Org invitations + onboarding state machine |
| 077 | 077_rpc_membership_checks.sql | Migration 077 — Membership-scoped data access (Workstream C) |
| 079 | 079_canonical_entity_columns.sql | Migration 079 — Canonical-entity columns on intelligence_items |
| 080 | 080_jurisdiction_vocabulary_extension.sql | Migration 080 — Jurisdiction vocabulary extension + RC-7 rejection logic |
| 081 | 081_admin_signal_documentation.sql | Migration 081 — Admin signal documentation (Option C resolution) |
| 082 | 082_operator_queues_and_routing.sql | Migration 082 — Operator queue tables + rejected-token routing |
| 083 | 083_trigger_derive_jurisdiction_iso.sql | Migration 083 — Trigger derive jurisdiction_iso from canonical jurisdictions |
| 084 | 084_sources_canonical_category.sql | Migration 084: Canonical category column on sources; refine 3 category-routing R |
| 085 | 085_d16_document_063_column_shadowing.sql | Migration 085: D16 resolution. Document the migration 063 column shadowing decis |
| 086 | 086_analytical_press_routing.sql | Migration 086: Analytical press routing for the 8 named sources. |
| 087 | 087_canonicalize_source_urls.sql | ───────────────────────────────────────────────────────────────────────────── |
| 088 | 088_citation_stats_rpc.sql | Migration 088: get_source_citation_stats(source_ids UUID[]) RPC |
| 089 | 089_intelligence_item_citations.sql | Migration 089: brief-to-source edge table (Q1). |
| 090 | 090_tier_schema_split.sql | ───────────────────────────────────────────────────────────────────────────── |
| 091 | 091_source_tier_opinions.sql | 091_source_tier_opinions.sql |
| 092 | 092_source_bias_tags.sql | Migration 092: source_bias_tags table (Q4 bias tag vocabulary) |
| 093 | 093_sources_tier_override.sql | Migration 093: tier_override mechanism on sources (Q5 decision) |
| 094 | 094_tier_compat_shim.sql | Migration 094: Compatibility shim for Q2 (migration 090) tier -> base_tier renam |
| 097 | 097_q4_bias_retune_option_b.sql | Migration 097: D1 Option B retroactive retune. |
| 098 | 098_get_source_citation_stats_edge_table.sql | Migration 098: get_source_citation_stats body swap. |
| 099 | 099_tier_opinion_review_state.sql | Migration 099: tier-opinion review state (dismissed_at/by/reason) + RLS for Phase 7 disagreement review surface. |
| 100 | 100_research_source_coverage_rpc.sql | Migration 100: get_research_source_coverage RPC. |
| 101 | 101_intelligence_items_domain_backfill.sql | Migration 101 (PROPOSED, NOT APPLIED): intelligence_items.domain backfill per docs/plans/classification-backfill-plan-2026-05-22.md. |
| 102 | 102_severity_band_theme_columns.sql | Migration 102: broaden severity enum (Q1), add signal_band (Q2), add theme (Q3) on intelligence_items per design rebuild dispatch v3. Applied 2026-05-24. |
| 103 | 103_intelligence_item_sections.sql | Migration 103: intelligence_item_sections table (Q4) with source_ids UUID[] per section. Powers 14-section reader on /regulations/[slug]. Applied 2026-05-24. |
| 104 | 104_community_post_intelligence_refs.sql | Migration 104: community_posts.referenced_intelligence_item_ids UUID[] (Q5) + GIN index for reverse-lookup powering Peer Discussion panels. Applied 2026-05-24. |
| 105 | 105_profiles_projection.sql | Migration 105: profiles projection columns (Q6) - org_id FK, workspace_role, sector text[], region text[]. region converted in place from scalar text. Applied 2026-05-24. |
| 106 | 106_regions_and_facts.sql | Migration 106: regions table with operations_decisions JSONB + regional_data_facts table (Q7). 5 current regions seeded (EU/US/ASIA/UK/UAE). Applied 2026-05-24. |
| 107 | 107_intelligence_items_trajectory_points.sql | Migration 107: intelligence_items.trajectory_points JSONB column + band-gated CHECK constraint (trajectory_points IS NULL OR signal_band = 'price'). Sprint 3 A4 belt 1 of three. No backfill, NULLs stay NULL per H1 trajectory precedent. |
| 108 | 108_market_intel_rpc_trajectory_payload.sql | Migration 108: extend get_market_intel_items RPC return shape with signal_band + trajectory_points so the page payload carries the data needed for A4-3's component-layer guard. CREATE OR REPLACE FUNCTION; idempotent. |
| 109 | 109_region_dimension_coverage.sql | Migration 109: region_dimension_coverage table (5 regions × 6 dimensions = 30 seeded rows) with 4-state CHECK (populated / partial / pending / missing), trigger-maintained fact_count from regional_data_facts. Sprint 3 A6.1. |
| 110 | 110_callout_columns_and_rpc_extension.sql | Migration 110: 4 new TEXT columns on intelligence_items (what_it_changes / does_not_resolve / conversion_trigger / cross_references) + DROP+CREATE both get_research_items and get_market_intel_items RPCs with extended return shapes. Sprint 3 R-A + M-A. |
| 111 | 111_workspace_overrides_dismissed_at.sql | Migration 111: ADD COLUMN dismissed_at TIMESTAMPTZ on workspace_item_overrides + partial index. Powers the manual priority tagging + dismissed stash dispatch (PRIORITY-TAGGING side-agent commit). |
| 112 | 112_provenance_invariant_schema.sql | Migration 112: source-provenance invariant — schema landing (provenance_status enum + columns + 3 supporting tables). Sprint 4 Block 1, task 1.1. |
| 113 | 113_seed_item_type_required_slots.sql | Migration 113: seed item_type_required_slots for the 5 D1 item_types (the provenance-criteria slot table). Sprint 4 Block 1, task 1.2. |
| 114 | 114_validate_item_provenance.sql | Migration 114: validate_item_provenance(item_id) — six-criteria provenance validation function. Sprint 4 Block 1, task 1.3. |
| 115 | 115_set_provenance_status_trigger.sql | Migration 115: set_provenance_status trigger on intelligence_items + sections + claims (re-derives + stamps the terminal provenance_status on write). Sprint 4 Block 1, task 1.4. |
| 116 | 116_active_intelligence_items_view.sql | Migration 116: active_intelligence_items view (verified-only customer surface). Sprint 4 Block 1, task 1.10. |
| 117 | 117_provenance_gate_customer_rpcs.sql | Migration 117: provenance-gate the RPC-routed customer surfaces — adds AND ii.provenance_status = 'verified' to the two query points (full-gate half). Sprint 4 Block 1, task 1.10. |
| 118 | 118_provenance_flip_binding.sql | Migration 118: #43 provenance-flip credential binding — a pre-existing intelligence_items row may be flipped off provenance_status='unverified' ONLY by the scoped non-owner reconciler role + guard trigger. Sprint 4 Phase 2 precondition. |
| 119 | 119_validate_item_provenance_failclose.sql | Migration 119: validate_item_provenance FAIL-CLOSE — a 0-section item no longer vacuously passes criteria 2-5; records no_section_content → quarantined. Sprint 4 Block 1. |
| 120 | 120_provenance_gate_remaining_customer_rpcs.sql | Migration 120: provenance-gate the remaining customer RPCs (completes the surface gate begun in 117). Sprint 4 Block 1. |
| 121 | 121_uniform_promotion_no_human_tick.sql | Migration 121: uniform promotion — a valid item flips to verified for ALL tiers (human-in-the-loop removed); criteria 1-5 byte-identical to 119. Sprint 4 Block 1. |
| 122 | 122_source_institutions.sql | Migration 122: institutions table (WHO published, keyed by registrable_domain) + sources.institution_id FK. A grouping/identity dimension, never a merge key; orthogonal to source_role/category. Source-layer fix, defect (b). |
| 123 | 123_source_label_derivation.sql | Migration 123: source label is a LIVE derivation — derive_source_category (== migration 084 CASE) + derive_source_intelligence_types + BEFORE INSERT/UPDATE trigger. category + intelligence_types now derive from source_role+name on every write (kills the hardcoded ['GUIDE'] placeholder + drift). See src/lib/sources/classify-source-role.ts. |
| 124 | 124_monitoring_queue_reconciled_at.sql | Migration 124: monitoring_queue.reconciled_at — claim marker for the reconcile worker (/api/worker/reconcile) so it consumes change_detected=true rows idempotently. Reconcile-loop activation. |
| 125 | 125_routing_by_item_type.sql | Migration 125: route customer surfaces by item_type → format → surface (get_market_intel_items / get_research_items / get_operations_items by item_type; supersedes source-attribute routing in 084/117). Applied 2026-06-04. |
| 126 | 126_research_required_slots.sql | Migration 126: seed item_type_required_slots for research_finding (finding, methodology_limits, decision_relevance, does_not_resolve). |
| 128 | 128_research_finding_slot_ledger_fix.sql | Migration 128: research_finding transitive-slot fix — decision_relevance + does_not_resolve descriptions signal GAP-satisfiability so synthesis sections (S3/S5) cover their slots. |
| 129 | 129_market_required_slots.sql | Migration 129: seed item_type_required_slots for market_signal + initiative (signal_event, driving_parties, conversion_trigger, action_now). |
| 130 | 130_technology_required_slots.sql | Migration 130: seed item_type_required_slots for technology/innovation/tool (deployment_reality, supplier_access, operational_fit, procurement_window). |
| 131 | 131_operations_required_slots.sql | Migration 131: seed item_type_required_slots for regional_data (cost_baseline, feasibility_choice, pending_change, region_jurisdiction). |
| 132 | 132_operations_slot_gap_satisfiable.sql | Migration 132: cost_baseline + feasibility_choice slots honestly GAP-satisfiable when the fetched content has no verbatim figure/verdict (the migration-128 pattern for regional_data). |
| 133 | 133_get_technology_items_rpc.sql | Migration 133: get_technology_items RPC — clone of get_research_items, item_type IN ('technology','innovation','tool'). Renamed from 130b to conform to 3-digit (F6) naming. |
| 134 | 134_fix_research_technology_rpc_columns.sql | Migration 134: fix get_research_items + get_technology_items — join intelligence_items for what_it_changes/does_not_resolve (not exposed by _workspace_active_items); the RPC-error → empty → /research fail-open root. |
| 135 | 135_source_registration_guard.sql | Migration 135: source-registration invariant DB guard — BEFORE INSERT/UPDATE trigger on intelligence_items refuses archive-as-source (reclassified_to_source/source_not_item/institutional_source/non_regulatory_source/portal_artifact) without a registered active source for the item host. DB twin of rule 019 + db.mjs reclassifyToSource() + orphan-source-audit.mjs. APPLIED 2026-06-06 via apply-135.mjs (fire-tested live: blocks unregistered archive-as-source, allows registered). |
| 136 | 136_theme_candidate_capture.sql | Migration 136: theme_candidate column — minimal capture-not-null (Emergence-Capture INV-1) for out-of-vocabulary theme values. The agent/parser emit topic-tag-form themes; the live theme_check requires the /research grouping vocab (no 1:1 map), so the pipeline writes a DB-valid theme or null and BANKS the agent's proposed value in theme_candidate (with row provenance) instead of dropping it. Nullable, no CHECK, partial index. APPLIED 2026-06-07 (read-back verified column exists). Minimal capture only; residual store + recurrence/promotion is the governed follow-on. |
| 137 | 137_reg_family_slot_gap_satisfiable.sql | Migration 137: penalty_summary + primary_deadline made honestly GAP-satisfiable on the NON-BINDING reg-family types (standard/framework/guidance) via the migration-132 description-rewrite pattern — fixes the redo's verified→quarantined regression where correctly-grounded voluntary standards/strategies/guidance (no deadline, no penalty) false-quarantined on criterion-5 missing_required_slot. regulation/directive stay fully HARD (binding); effective_date + jurisdictional_scope stay HARD on all five types. Carry-condition B: the GAP is authorised only by the source's own voluntary/non-binding characterisation, never the item_type label (a real deadline/penalty in the source still forces a FACT). Jason's approved slot×item_type matrix, 2026-06-08. APPLIED 2026-06-08 (read-back verified via verify-137.mjs: 6 slots loosened, reg/directive + date/scope unchanged). |
| 138 | 138_reg_only_authority_floor.sql | Migration 138: validate_item_provenance criterion-3 authority floor (source_tier_at_grounding IN (1,2)) becomes ITEM_TYPE-SCOPED — bites ONLY on the regulatory family (regulation/directive/standard/guidance/framework); non-reg types are EXEMPT (named exemption, REVISIT — per-type non-reg floor deferred to the research/tech calibration spec). F1 fake-certification fix (Jason 2026-06-11, Option B): the constant source_tier_at_grounding=2 stamp was masking that the floor was regulatory-calibrated but applied uniformly. Criteria 1/2/4/5/6 byte-identical to migration 121. Ships WITH the A6 corpus revalidation (status-is-a-cache rule); 30 reg flagships flipped verified→quarantined (grounded in secondary sources, not primary legal text → Phase 2 re-ground). APPLIED 2026-06-11. |
| 139 | 139_close_quarantine_flags_on_verify.sql | Migration 139 (Stage B / F5): set_provenance_status trigger gains a CLOSE-ON-VERIFY branch — when validate() recommends 'verified', it resolves any OPEN trigger-created data_quality integrity_flags row for that item (status→resolved, resolved_by='set_provenance_status_trigger'). Fixes the flag surface accreting stale open quarantine flags on items that later recovered (every re-ground / the A6 30-flip revalidation flips dozens of statuses). One-time backfill closed the stale ones already open on currently-verified items (301 resolved; 0 stale remain). Function otherwise byte-identical to 115. APPLIED 2026-06-12. |
| 140 | 140_attention_counts_platform_flags.sql | Migration 140 (admin flag a): admin_attention_counts() (migration 036) gains column platform_integrity_flags_open = COUNT of platform integrity_flags (migration 048) rows with status IN ('open','in_review'), added into total, so the admin Issues Queue + sidebar red-dot stop reading blind to the platform quarantine backlog (reported 3 per-brief while 523 platform flags sat open+unsurfaced, 2026-06-15). DROP+CREATE in one txn (RETURNS TABLE column add changes return type; CREATE OR REPLACE cannot). Existing 7 columns byte-identical; route/hook/IssuesQueue updated in the same change (targetTab platform-integrity-flags). STABLE/read-only, no data mutation. APPLIED 2026-06-15. |
| 141 | 141_per_type_authority_floor.sql | Migration 141 (Stage D1): validate_item_provenance criterion-3 authority floor becomes PER-ITEM-TYPE (v_floor_max) — reg family ≤T2 (unchanged from 138), research_finding ≤T4, technology/innovation/tool ≤T5 (forward default, 0 live items, REVISIT); market_signal/initiative/regional_data EXEMPT (named exemptions registered as invariant SC-8 with REVISIT — corroboration-count gate + per-section regional floor are UNBUILT, codifying them now would be the migration-113 pattern). Closes the migration-138 blanket non-reg exemption with calibrated, data-grounded per-type floors (research core sits at T3 per source-credibility-model; ≤T4 preserves it + one margin tier). Criteria 1/2/4/5/6 byte-identical to 138. Ships WITH corpus revalidation (status-is-a-cache): 2 research_finding items flipped verified→quarantined. STABLE/read-only. APPLIED 2026-06-15. |
| 142 | 142_legal_line_guard.sql | Migration 142 (WS1 / tier→label floor-router): validate_item_provenance criterion-4 LEGAL-LINE GUARD — an ANALYSIS claim asserting a present-tense enacted-law REQUIREMENT (c_legal_req_re) that is NOT forward-framed (c_forward_re) fails new reason legal_claim_mislabeled_analysis, so the credible-non-binding relabel path cannot launder a binding legal requirement out of the FACT/Legal lane. Strict-on-ambiguity: ambiguous tense routes to the stricter FACT/Legal path, never the early-signal escape. Criteria 1/2/3/5/6 byte-identical to 141. Verified 5/5 guard cases + blast-radius 0/538 no-flip. APPLIED 2026-06-25. |
| 143 | 143_label_variant_tolerance.sql | Migration 143 (WS1 / tier→label floor-router): criterion-4 ANALYSIS label match becomes VARIANT-TOLERANT yet strict-on-missing — c_label_re matches the four relabel tokens (per the workspace's reading / analytical inference / industry interpretation / operational implication) with OPTIONAL markdown asterisks and an OPTIONAL parenthetical qualifier, fixing analysis_missing_label_syntax false-fails where the model emits "Operational implication:" without bold asterisks (4→0 on the enacted set). STRICT on a genuinely missing label (no base token → no match → legal-line guard 142 holds). Applied in BOTH the ANALYSIS check and the unlabeled_assertion exemption. Verified 7/7 regex cases + 15/15 no-flip. APPLIED 2026-06-25. |
| 144 | 144_scrape_cadence.sql | Migration 144 (global scrape-schedule control): system_state gains scrape_cadence ('off'|'weekly'|'monthly', default 'off' = the hold) + scrape_start_date (anchor = first run + recurrence phase) + CHECK constraint. SINGLE source of truth for WHEN the whole system scrapes — per-source update_frequency/next_scheduled_check cadence RETIRED (Option 1: whole system scrapes as a unit on the cadence day). global_processing_paused (016) KEPT as the independent emergency stop; isGloballyPaused()=cadence 'off' OR emergency; the worker window-gates via scrapeWindowOpen (scrape-schedule.ts). Additive (old code keeps reading the hold), live hold preserved. APPLIED 2026-06-28. |
| 145 | 145_provenance_floor_inline_derive.sql | Migration 145 (D1 floor render-derive): validate_item_provenance criterion-3 authority floor DERIVES the FACT tier INLINE from the claim's resolved source (section_claim_provenance.source_id → sources.COALESCE(tier_override, base_tier)) instead of reading the stored source_tier_at_grounding — base_tier-only + sanctioned override, never effective_tier (moat-pure), read live (drift-proof). Criteria 1/2/4/5 + source_span checks + legal-line guard + label-variant match BYTE-IDENTICAL to 143. Zero blast radius (proven read-only before apply: 0 drift on the source_id basis; 145 output == 143 output for all 658 items — the lone confirm-script "flip" was a stale-stored-status false positive, 143 quarantines it identically). claims-tier audit re-pointed to derivation-consistency (lane green). APPLIED 2026-06-29. |
| 146 | 146_item_xref_origin_and_related_derive.sql | Migration 146 (Option A — phase-intake-gate, Confirmation 1): item_cross_references is the SoT for item↔item links. Adds `origin` column (manual\|agent_semantic\|entity_extraction, CHECK); one-time backfill of agent-emitted intelligence_items.related_items INTO edges (origin=agent_semantic — writes item_cross_references ONLY; intelligence_items is never mutated, so the provenance-flip guard row-43/mig-118 is untouched); related_items becomes READ-DERIVED via related_items_derived(uuid) STABLE fn + item_related_items_derived view. Fork resolution: the FIRST 146 draft wrote related_items back via a trigger + reconcile loop and tripped the provenance guard on unverified items — Option A derives on READ, no write-back, no trigger. Reversible (DROP VIEW/FUNCTION + DROP COLUMN). NOT YET APPLIED (pending a DDL-capable Caro's Ledge connection: the Supabase MCP is Dietl-scoped, scripts/lib/db.mjs is PostgREST-only and cannot run DDL). |
| 147 | 147_sources_fetch_status.sql | Migration 147 (item 5b — unreadable-source flag): adds `sources.fetch_status` (TEXT, CHECK ok\|cdn_block\|soft_404\|blocked\|error) + `fetch_status_at` (TIMESTAMPTZ). SOURCE-level readability verdict written by the unified transport (fetchPrimaryWithFallback→detectRoadblock reason 'cdn_block'; fetch-quality 'blocked_cloudflare') so customer surfaces never display a source the pipeline could not read (the IEA/Cloudflare case). Dependent code (transport write + render gate) is merge/deploy-gated on this being live (the gate SELECTs the column). APPLIES TOGETHER WITH migration 146 on the operator DDL window (single supabase db push) — NOT independently. Reversible (DROP COLUMN x2). NOT YET APPLIED. |
| 148 | 148_surface_counts.sql | Migration 148 (count-integrity build 2026-07-02): single classification + counting SoT for the customer surface counts. `surface_of(item_type, domain)` maps to regulations/market/operations/research/uncategorized — its CASE is GENERATED from `src/lib/surface-of.mjs` SURFACE_RULES (vocab-drift guard enforces byte-equality, binding 3). `get_surface_counts(org, surface)` = verified-population count bundle for ONE surface (total_items header + by_priority/by_severity/by_band/by_status/by_jurisdiction label instances; superset of 069 shape, drop-in via getScopedWorkspaceAggregates fail-soft). `get_all_surface_counts(org)` = one-scan {verified,total} per surface + grand total for the dashboard rail (customer reads .verified, admin reads both; ruling 1). Override overlay = one LEFT JOIN; uncategorized = defect signal never rendered customer-side (binding 4). Closes 5 count leaks (rail-vs-aggregates verified filter, /research empty-scope 259 degrade, header-vs-cards dual derivation, MARKET vocab split, override JOIN-vs-second-query). Consumers fail soft pre-apply (fall back to prior RPCs). RIDES THE SAME supabase db push as 146+147. Reversible (DROP FUNCTION x3). NOT YET APPLIED. |
| 149 | 149_severity_backfill_ops_reg.sql | Migration 149 (count-integrity: severity backfill enabling the leak #3 card swap). Deterministic, idempotent, ZERO re-classification. Read-only diagnosis 2026-07-03: 254/259 verified items already carry severity; the 5 nulls are ALL regulations-surface + carry priority (HIGH/MODERATE), and the ops/reg severity vocab IS lower(priority) (mapPriorityToSeverity) → fills from data on the row. Touches ONLY ops/reg surfaces (surface_of guard); never invents a market/research (semantic-vocab) severity. After it lands, verified severity is 100% so by_severity can back the cards. signal_band left as an HONEST PARTIAL (band is market-only; 35 null-band market items have no deterministic source; cards render classified + honest "N unclassified" — operator ruling, no spend). DEPENDS ON surface_of() (148); orders after it; applies in the SAME db push (146+147+148+149). Idempotent (WHERE severity IS NULL). Forward-only. NOT YET APPLIED. |
| 150 | 150_criterion2_url_canonicalize.sql | Migration 150 (URL-normalization 1b, operator ruling 2026-07-04). `validate_item_provenance` criterion-2 (citation-URL grounding) did an EXACT-string compare (rtrim '.,;:' only), so a content_md URL differing from an ACTIVE registered source by a trailing markdown-emphasis `*`/backtick, a `www.` prefix, or a trailing slash read as `ungrounded_url` despite the source being registered — ~30 of 33 such failures were canonicalization mismatches (ruling table 2026-07-04). Adds IMMUTABLE `canonicalize_citation_url(text)` (lowercase, strip trailing `*`/backtick, leading `www.`, trailing slash, rtrim '.,;:') applied to BOTH sides of all three criterion-2 compares (item source_url, pool result_url, registry sources.url). Defense-in-depth with the write-site fix (stripUrlMarkers on content_md) + the 1c cleanup pass (strips the `*` from stored rows); this normalizes at the COMPARE so the `www`/slash residual also matches. Copied BYTE-IDENTICAL from 145; only criterion-2 changes (criteria 1/3/4/5, the moat-pure inline authority floor, the legal-line guard, the label-variant match unchanged). RIDES THE DDL PUSH (checklist now 146-150). Reversible (restore 145's function body). NOT YET APPLIED. |
| 151 | 151_published_price_statistics.sql | Migration 151 (redesign T05 — signal-detail price board backing store). The Signal Detail hero (HANDOFF §6.5) shows a PRICE BOARD of Anton figures; HANDOFF §7 names the live price feed as KNOWN NEW BACKEND WORK — until it ships the board must NOT fake ticks. Commits the backing store so "when the live feed lands these slots tick" (dispatch: committed migration file + honest caption only). Adds `public.published_price_statistics` (id, item_id FK→intelligence_items ON DELETE CASCADE, label, value_display, unit, context_line, severity_tone CHECK, source_tier 1-7 CHECK, released_at, next_release_at, next_release_label, sort_order) — PUBLISHED STATISTICS (release-cadence anchored), NOT live ticks. RLS: authenticated SELECT (customer-facing published data); writes are service-role only (feed writer bypasses RLS). NO SEED DATA — `/market/[slug]` reads it fail-soft and the surface renders the honest §4 published-statistics pending frame while empty (never faked figures). Index on (item_id, sort_order). Standalone (no DDL-push dependency). Reversible (DROP TABLE). NOT YET APPLIED. |
| 152 | 152_state_cost_facts.sql | Migration 152 (Redesign TEMPLATE 07 Operations — schema home for state-level cost facts; renumbered from 151 to avoid collision with 151_published_price_statistics at integration). Adds `state_cost_facts` table: sub-national (state / member-state) sourced cost facts (minimum wage, labor rates, fuel taxes) for the Operations "By state" sub-list. One row per (state_code, dimension, fact_label) with `source_id` FK to sources + `statute_citation` + `effective_date` + `unit` + `trend`; dimension CHECK shares the 6-dimension vocab with `regional_data_facts` (106); world-readable RLS (no PII). WHY: `regional_data_facts` is REGION-level only (US/EU/ASIA/UK/UAE); the By-state sub-list needs the sub-national grain so each state figure carries its own statute citation — a US state with no sourced row shows PENDING on the surface, never a national average presented as state law (HANDOFF §1/§4/§7). Ships the schema home ONLY; the UI renders the honest pending frame until sourced rows land. NOT applied by the T07 UI PR (scrape hold + code-vs-data separation — apply + backfill is a separate operator-ruled data dispatch). Reversible (DROP TABLE). NOT YET APPLIED. |
| 153 | 153_community_post_signoff_requests.sql | Migration 153 (Template 11 Community redesign, 2026-07-06). The ONE genuinely-new table the redesign needs (community-schema-mapping.md §3.1 / HANDOFF §7): `community_post_signoff_requests` (id, post_id→community_posts, requested_by→auth.users, status CHECK pending\|signed_off\|declined\|withdrawn, verifier_id, primary_doc_url, decision_note, created_at, decided_at) + partial unique index (one open request per post) + companion `community_posts.signed_off_at`/`signed_off_by` columns + RLS (requester/active-verifier/admin select, group-member insert, active-verifier decide). Backs the mock's "Request verifier sign-off" action + the peer-signal→citable conversion moment. Builds ONLY on the newer conversation layer (community_posts) — does NOT touch the mig-007 forum layer. Until applied, the T11 UI renders the sign-off action honest-pending. Reversible (DROP TABLE + DROP COLUMN x2). NOT YET APPLIED. |
| 154 | 154_community_signoff_withdraw_policy.sql | Migration 154 (Template 11 sign-off wiring follow-on, 2026-07-07). ADDITIVE to 153 — adds ONE RLS policy `signoff_withdraw_own` on `community_post_signoff_requests`; touches none of 153's table/index/policies. WHY: 153's only UPDATE policy (signoff_decide) is active-verifier/admin-only, so a plain requester had NO RLS path to withdraw their own still-open request (community-schema-mapping.md §3.1 names withdraw as part of the lifecycle). The new policy is deliberately narrow: requester may move ONLY their OWN request, ONLY while `status='pending'`, and ONLY to `status='withdrawn'` (using pins the pre-image to the requester's own pending row; with_check pins the post-image to the withdrawn shape). Never overlaps signoff_decide. Backs `POST /api/community/signoff/[id]/withdraw`. Until applied, that route matches 0 rows and returns 404 (safe pre-apply); create + verifier-decide work against 153 immediately. Reversible (DROP POLICY). APPLIED + verified 2026-07-07. |
| 155 | 155_community_group_vertical.sql | Migration 155 (Template 11 Community — vertical groups, §7 KNOWN NEW BACKEND, 2026-07-07). Adds ONE nullable column `community_groups.vertical TEXT` + partial index `idx_community_groups_vertical (vertical, last_active_at DESC) WHERE vertical IS NOT NULL`. WHY: the surface shows regional ROOMS (region-keyed, vertical NULL) AND member-created VERTICAL groups that cut across regions by cargo vertical (fine art, live events, automotive…) — the mock's "they form when members create them" pending frame had no dimension to group on. A room has `vertical IS NULL`; a vertical group has `vertical` = a canonical sector id (validated at the app layer against ALL_SECTORS — NO CHECK, matching the free-text posture of `vertical_tags` from mig 007 so the taxonomy grows without a migration). Vertical groups are created cross-regional (region='GLOBAL') + public by `POST /api/community/groups`; the group INSERT goes through the caller's RLS client (mig-028 owner policy), the owner's bootstrap admin member row via service-role (members INSERT is admin-only, mig-029). Additive — does not touch the region column, the 7 seeded rooms (vertical stays NULL), or any RLS policy. Reversible (DROP COLUMN + DROP INDEX). APPLIED + verified 2026-07-07 (column text nullable + index present; 0 vertical groups; rooms keep vertical NULL). |
| 156 | 156_org_member_bans.sql | Migration 156 (org-scoped member ban, §7 KNOWN NEW BACKEND, operator ruling 2026-07-07 — ban is ORG-SCOPED block-rejoin, NOT platform-wide). Adds `org_member_bans` (PK (org_id, user_id), banned_by, banned_at, reason; org_id→organizations, user_id→auth.users) + index on user_id + RLS (owner/admin of the org may SELECT; writes service-role). AND `CREATE OR REPLACE FUNCTION accept_invitation` — IDENTICAL to migration 076 except a ban guard added immediately before the membership INSERT (a banned account raises 42501 instead of rejoining via a fresh invite; the RPC is the DB-layer choke covering every caller). The members-route `POST` action (owner-only, not-self, not-last-owner) upserts the ban row THEN deletes the membership (ban-then-delete fails closed). Account → Members ban button wired to it; UI copy corrected from "platform-wide" to org-scoped. Verified: live accept_invitation matched 076 before replace (no drift). Reversible (DROP TABLE + restore 076 function body). APPLIED + verified 2026-07-07 (table + RLS + ban guard present; membership insert intact). |
| 157 | 157_security_hardening.sql | Migration 157 (Phase 0 security hardening, DEEP-AUDIT 2026-07-07 P0-3/P0-4a). AUTHOR-ONLY / OPERATOR-GATED — changes production RLS, authored against the LIVE policies (project kwrsbpiseruzbfwjpvsp); apply through the ledger with the header's anon read-back smoke tests. Closes three live-confirmed anon-read exposures: (1) `intelligence_items_read` SELECT tightened from `USING(true)` to `USING (provenance_status='verified' AND is_archived IS NOT TRUE)` (anon/customer see only verified, non-archived; admin reads via service-role bypass RLS); (2) DROP `staged_updates_read` (no anon SELECT — service-role only, behind the now-admin-gated route); (3) DROP `provisional_sources_read` (admin-only working set). Plus (4) `ALTER VIEW item_related_items_derived SET (security_invoker = on)` — the single ERROR-level `security_definer_view` advisor (the other 4 public views already opt in). DELIBERATE non-change: `sources_read` left public (low-sensitivity registry powering the customer provenance chips). NOT included (flagged separately): 165-function search_path batch (break-risky, needs a reviewed companion migration) + leaked-password protection (a Supabase Auth dashboard setting, not SQL). Reversible (restore the 3 permissive policies + security_invoker=off). APPLIED + verified 2026-07-07 at BOTH layers (pg_policies read-back: tightened policy live, 2 public SELECT policies dropped, view security_invoker=on; anon PostgREST smoke: staged/provisional/quarantined/unverified all 0, verified-not-archived 283 visible, sources chips intact; service-role unaffected: 24 staged + 125 quarantined). SAME WINDOW: ledger repaired — versions 136–157 recorded in supabase_migrations.schema_migrations after per-migration signature verification (all 21 confirmed live), so live schema ↔ ledger ↔ this inventory now converge; leaked-password protection (HIBP) enabled + read-back via Management API. |
| 158 | 158_floor_unconditional_label_per_claim.sql | Migration 158 (P3c grounding holes, chrome-audit S1-07, dispatch 2026-07-07). AUTHOR-ONLY per the Phase-3 dispatch — the apply decision rides WITH the blast-radius figures below. Recreates `validate_item_provenance` (byte-derived from 150) with two moat-gate fixes: (1) the criterion-3 authority floor fires UNCONDITIONALLY for the reg family (regulation/directive/standard/guidance/framework) — previously only when `priority IN ('CRITICAL','HIGH')`, and priority is the MODEL'S OWN output, so its severity choice disarmed the floor (probed live: 90/113 verified reg-family items carry LOW/MODERATE priority; 72 hold 947 sub-floor FACT claims — 385 with NO claim-level source_id across 39 items, 562 at tiers 3-6 — these flip to quarantined at next RE-VALIDATION, not at apply); non-reg per-type floors (141) keep the CRITICAL/HIGH condition, failure objects gain `floor_basis`. (2) The criterion-4 ANALYSIS label check is PER-CLAIM (label must sit in the claim's own blank-line-delimited paragraph; one labeled sentence no longer licenses a whole section) — probed live: 1 claim on 1 item (Japan MLIT 68e05861) of 517 ANALYSIS claims/78 items fails, the generator already complied per-sentence. The per-claim SQL expression was red-then-green'd READ-ONLY against prod before authoring. Reason strings unchanged (consumers: ProvenanceFailures.tsx, deterministic-lever.mjs). Companion code fix same PR: cited-host gate in canonical-pipeline (criterion-2 auto-stub restricted to pool/registry-known hosts; novel hosts flagged via integrity_flags, never silent). Reversible (re-apply 150's body). APPLIED + verified 2026-07-07 (operator "Proceed" ruling; ledger row 158 recorded same transaction; read-back: definition carries floor_basis + per-claim split; behavioral probes read-only via STABLE fn — Japan MLIT forces exactly 1 analysis_missing_label_syntax, a LOW-priority reg item fails floor with floor_basis=item_type_unconditional (22 claims), CRITICAL/HIGH control still valid=true. NO items flipped at apply — flips ride future re-validation writes, currently gated by zero-mints/loop-OFF). |
| 159 | 159_intelligence_items_fts.sql | Migration 159 (Ask FTS substrate, chrome-audit S1-09, browser wave). The Ask assistant's context was the FIRST 30 items by priority — zero relevance to the question; recon confirmed NO FTS surface existed anywhere (no tsvector, GIN only on array columns, zero .textSearch callers). Adds: `intelligence_items.search_tsv` GENERATED ALWAYS STORED (weighted — title A / summary B / full_brief C; generated not trigger-fed so there is no writer to forget), GIN index, and `search_intelligence_items(q, max_rows)` RPC (websearch_to_tsquery + ts_rank_cd ordering; customer read predicate verified+non-archived enforced INSIDE the fn; STABLE; search_path pinned — new fns don't join the 165-fn backlog). Consumer: /api/ask retrieval (same PR); priority-pull kept as fallback for low-signal questions. APPLIED + verified 2026-07-07 (operator "Proceed"; ledger row 159 same transaction; probes: column+index present, 12 ranked hits on two real questions, 0 on noise). Reversible (drop fn + index + column). |
| 160 | 160_search_path_pin_app_functions.sql | Migration 160 (search_path pin, S1-12 residual — THE reviewed companion migration per the operator's standing ruling; AUTHOR-ONLY, DO NOT APPLY outside the DDL window). Pins `search_path = public, extensions, pg_temp` (the DB default minus the role-controlled `"$user"` injection vector — minimal-change, break-safe; deliberately NOT the strict `''` form) on ALL 56 app-owned public functions lacking a pin (26 SECURITY DEFINER — the real lint — + 30 invoker-rights for hygiene). CENSUS RECONCILED: the 165 raw unpinned fns = 56 app-owned + 109 EXTENSION-OWNED (pg_depend deptype='e', pg_trgm etc.) which are deliberately EXCLUDED (extension upgrades overwrite them) — resolves the earlier advisor-56-vs-live-165 discrepancy. Mechanically generated from live pg_proc (overload-safe identity args); header carries the re-generation query + post-apply verification recipe (generator returns 0 rows; behavioral smoke on validate_item_provenance / listings RPC / FTS RPC; advisor count → 0). Reversible per function (RESET search_path). APPLIED + verified 2026-07-08 via the ADR-011 DELEGATED-WITH-PROOF amendment (dispatch reclassified THIS migration off operator-window; RLS/drops/customer-read-path classes stay operator-window). Applied via execute_sql (BEGIN/COMMIT, 56 ALTERs), verified BEFORE recording, rollback readied. PROOF before→after: app-owned unpinned 56→0; customer read path UNCHANGED via simulated authed member (get_workspace_intelligence_listings 251→251, get_market_intel_items 78→78, get_research_items 43→43, _workspace_active_items 251→251); non-gated search_intelligence_items 30/30→30/30, validate_item_provenance valid→valid, detect_intersections 50→50; advisor `function_search_path_mutable` 168→0. Ledger row 160 recorded with the identity apply-record. |
| 161 | 161_sources_content_hash.sql | Migration 161 (P2-6 change detection, chrome-audit S1-10). check-sources wrote `change_detected: false` HARDCODED — zero change rows ever; "monitoring" was accessibility-only. Adds two additive nullable columns: `sources.last_content_hash` (sha256 of the last successful render's normalized text — the worker fingerprints the SAME Browserless render the accessibility check already pays for, zero extra units vs the 20k/mo budget) + `sources.last_content_changed_at` (stamped on fingerprint difference — the queryable change signal). Pure half = content-change.mjs (200ch error-page floor so outages never read as change; first observation only seeds). Downstream auto-action deliberately NOT wired (rides the loop flip). APPLIED + verified 2026-07-08 (operator "Proceed"; ledger row 161 same transaction; read-back 2 columns present). Reversible (DROP COLUMN x2). |
| 162 | 162_portal_link_candidates.sql | Migration 162 (P2-5 portal deep-link discovery, chrome-audit S2-08). ~55% of registry sources are ROOT PORTALS; nothing enumerated the deep links where instruments live (discovery = 1 manual path). New table `portal_link_candidates` (source_id FK, url UNIQUE, anchor_text, status candidate/promoted/rejected, first/last_seen_at; RLS on, ZERO policies = service-role-only admin working set, same posture as provisional_sources post-157). Producer: check-sources worker extracts candidates from the SAME uncapped html the accessibility render already returns (portal-links.mjs pure extractor — same-host, instrument-signal-required, nav/asset-excluded, capped 40; zero extra Browserless units). DISCOVERY not intake: classify→stage consume step rides the loop flip. APPLIED + verified 2026-07-08 (operator "Proceed"; ledger row 162 same transaction; read-back: table + RLS-on/0-policies + ledger). Reversible (DROP TABLE). |
| 163 | 163_reconciler_integrity_flags_insert.sql | Migration 163 (reconciler INSERT policy on integrity_flags — the moat guard's missing half, found by the conservation-audit re-ground proof). A single additive `CREATE POLICY integrity_flags_reconciler_insert` (roles={reconciler}, cmd=INSERT, WITH CHECK true) so `set_provenance_status`'s quarantine-branch flag insert stops failing RLS when the flip runs as the bound reconciler. FOUND ALREADY APPLIED OUT-OF-BAND on 2026-07-10 (policy present in pg_policies, ledger topped at 162) — ledgered idempotently as version 163 via a guarded DO block (semantics identical to the authored file; delegated per the reconciliation-remediation dispatch's single-additive-policy clause). POST-APPLY PROOF (2026-07-11, via the reconciler DSN, all rolled back): INSERT integrity_flags OK; DELETE integrity_flags DENIED; INSERT sources DENIED; MCP postgres cannot SET ROLE reconciler. KNOWN RESIDUAL (operator DDL window owed, ADR-011 break-risky): this policy is necessary but NOT sufficient — post-mig-157 the reconciler cannot SELECT the validator's inputs (agent_run_searches / section_claim_provenance / item_type_required_slots read 0 rows) and its intelligence_items writes fail WITH CHECK even same-value; the reconciler runner is unsound until that window. Reversible (DROP POLICY). |
| 164 | 164_market_intel_org_gate.sql | Migration 164 (Wave-α Track B1). AUTHOR-ONLY / OPERATOR DDL WINDOW — restores the `_assert_org_membership(p_org_id)` gate that migration 108 SILENTLY DROPPED when it rewrote get_market_intel_items as `LANGUAGE sql` (P1 #1; live pg_get_functiondef confirmed the gate absent — any authenticated user + foreign org_id read that org's overlay). Re-DROP+CREATE as `LANGUAGE plpgsql` (RETURN QUERY, the 077 pattern the 9 sibling RPCs still use), PERFORM the assert, + deterministic `, ii.id ASC` tie-break. Return shape reproduced byte-for-byte from the LIVE 33-col def (verified gate + item_type filter + what_it_changes/conversion_trigger/cross_references preserved); PostgREST contract unchanged. Proof: non-member call → 42501; member → rows; service_role → 0 rows no exception; def carries the assert. Reversible (rollbacks/164 restores the live gate-less SQL). APPLIED 2026-07-11 (wave-alpha). |
| 165 | 165_profiles_self_write_and_anon_pii.sql | Migration 165 (Wave-α Track B2). AUTHOR-ONLY / OPERATOR DDL WINDOW. `profiles` had ONLY the mig-002 `"Public read"` SELECT policy → (P1 #3) browser self-edit + onboarding UPDATEs matched 0 rows silently, and (P1 #4) email/linkedin_sub/is_platform_admin were anon-readable. Adds self INSERT + UPDATE policies (authenticated, `auth.uid() = id`); REVOKEs the table-wide anon SELECT and re-GRANTs SELECT on the 34 non-PII columns only (the three sensitive columns stay off the anon read path; authenticated + service_role unaffected; the "Public read" row policy left intact so community joins keep working). Proof: owner UPDATE round-trips; cross-id UPDATE 0 rows; anon SELECT email → permission denied; anon SELECT display_name/avatar_url → OK. Reversible (rollbacks/165). APPLIED 2026-07-11 (wave-alpha). |
| 166 | 166_provisional_sources_admin_select.sql | Migration 166 (Wave-α Track B3). AUTHOR-ONLY / OPERATOR DDL WINDOW. Migration 157 dropped `provisional_sources_read` leaving NO SELECT policy → the /admin queue (anon-client read, error dropped) rendered EMPTY since 2026-07-07 (P1 #2). Adds an admin-scoped SELECT policy (service_role OR platform-admin — the 099 pattern) as defense-in-depth; the OPERATIVE fix is the code half in the SAME dispatch (`fetchProvisionalSources()` → SERVICE client + captures `error`, src/lib/supabase-server.ts). Proof: policy present; platform-admin count = service count (queue populated); anon count = 0. Reversible (rollbacks/166; code half independent). APPLIED 2026-07-11 (wave-alpha). |
| 167 | 167_staged_updates_reviewer_notes.sql | Migration 167 (Wave-α Track B4). AUTHOR-ONLY / OPERATOR DDL WINDOW. Adds the phantom `staged_updates.reviewer_notes` (text, nullable) that the approve/reject route already writes — its absence made approve-WITH-notes materialize the item then 500 (status left pending → retry re-mint; P1 #5). Paired code (route.ts): idempotency guard keys on materialized_at OR materialized_item_id + backfills the item pointer on a notes-write failure so a retry no-ops. Paired script: `scripts/_wave-alpha/b4-disposition-stuck-staged-update.mjs` reconciles the ONE stuck orphan (b631762e — California ACF, already live as ccee10a4/w4_ca_acf; backfill pointer, do NOT re-materialize → dup-safe). Proof: column present; approve-with-notes persists without 500. Reversible (DROP COLUMN, rollbacks/167). APPLIED 2026-07-11 (wave-alpha). |
| 168 | 168_aux_table_parent_gates.sql | Migration 168 (Wave-α Track B5). AUTHOR-ONLY / OPERATOR DDL WINDOW. The five aux tables (item_timelines / item_cross_references / item_disputes / item_supersessions / item_changelog) exposed `USING(true)` SELECT → 689 timeline rows (+ xrefs/disputes/supersessions/changelog) leaked quarantined/archived items that intelligence_items_read (157) hides (P2 provenance). Replaces each `_read` policy with a parent-gate mirroring intelligence_items_read (`provenance_status='verified' AND is_archived IS NOT TRUE`); two-FK tables (cross_references, supersessions) require BOTH endpoints. service_role bypasses (admin unchanged); writes untouched. Proof: anon-visible rows with a non-verified/archived parent = 0 per table. Reversible (rollbacks/168 restores USING(true)). APPLIED 2026-07-11 (wave-alpha). |
| 169 | 169_reconciler_rls_repair.sql | Migration 169 (Wave-α Track B6). AUTHOR-ONLY / OPERATOR DDL WINDOW. Root cause of the carried reconciler-runner break: set_provenance_status → validate_item_provenance run as the reconciler (SECURITY INVOKER); the reconciler has GRANT SELECT but NO RLS policy on the validator's 3 input tables → 0 rows → mis-quarantines valid items (RLS-credential parity class). Adds `*_reconciler_select` SELECT policies on agent_run_searches / section_claim_provenance / item_type_required_slots. The "WITH CHECK even same-value" symptom was the integrity_flags INSERT — ALREADY fixed by mig 163; 163 + 169 close both halves. Proof: 3 policies present; reconciler SELECT counts > 0; reconcile-revalidate.mjs recommends correctly on a known item. Reversible (DROP 3 policies, rollbacks/169). APPLIED 2026-07-11 (wave-alpha). |
| 170 | 170_ledger_repair_107_134.sql | Migration 170 (Wave-α Track B8). AUTHOR-ONLY / OPERATOR DDL WINDOW. Ledgers the 15 applied-but-unledgered migrations (107,108,109,110,111,112,115,118,128,129,130,131,132,133,134 — DB-3 F1, verified via read-only diff of schema_migrations vs disk) into supabase_migrations.schema_migrations, mirroring the 136–157 repair (INSERT version+name, statements NULL, guarded idempotent DO block). Records the ledger FACT; runs NO DDL (objects already live). Proof: all 15 present post-apply; re-run is a no-op. Reversible (rollbacks/170 deletes exactly the 15 statements-NULL rows). APPLIED 2026-07-11 (wave-alpha). |
| 171 | 171_validate_provenance_brief_presence.sql | Migration 171 (Wave-α Track B9 / plan A8). AUTHOR-ONLY / OPERATOR DDL WINDOW. Adds CRITERION 6 (`missing_full_brief`) to validate_item_provenance so a verified item with NULL/empty `full_brief` fails the gate (P1 #9: 5 verified-live items render an empty detail page). Authored from the LIVE function body (mig-158 def via pg_get_functiondef, not the last file — drift-safe); criteria 1–5 + search_path preserved exactly, only `full_brief` added to the initial SELECT + the criterion-6 block. Ships WITH the corpus revalidation (RD-5, "status is a cache"): `scripts/_wave-alpha/revalidate-corpus-brief-presence.mjs --apply` re-runs validation over all verified items and honest-quarantines the 5 with valid RD-6 deferrals (reason names missing-brief + C7 regeneration; deferred_until 2026-10-31; owner operator; resolution_event C7/batch-1 hold-lift). Proof: NULL-brief item → valid=false criterion 6; healthy item unchanged; script quarantines exactly the 5. Reversible (rollbacks/171 restores the criteria 1–5 body). APPLIED 2026-07-11 (wave-alpha). |
| 195 | 195_error_events.sql | Migration 195 (Wave-β R0.2 first-party error tracking — the "Sentry or equivalent" with NO external service). New table `error_events` (one row per error GROUP keyed by (stack_hash, release, side, route); columns occurred_at/last_seen_at/release/env/side CHECK server\|client/route/message/stack_hash/stack_excerpt/user_scope (org id nullable — NO raw PII)/count). Dedup: UNIQUE index on (stack_hash, release, side, route) — the ingest lib (`src/lib/telemetry/capture-error.ts`) INSERTs then increments `count` on 23505. Plus a named (stack_hash, release) index + a last_seen_at DESC listing index. `stack_hash` is computed SERVER-SIDE ONLY (`src/lib/telemetry/stack-hash.mjs`, unit-tested) so clients cannot forge group collisions. RLS: enabled, NO write policies (service-role-only write path) + ONE platform-admin SELECT policy (profiles.is_platform_admin, mig 075 pattern) backing the /admin Runtime → Errors panel. message/stack_excerpt/route length-bounded by CHECK (defense in depth). Reversible (`supabase/rollbacks/195_error_events_rollback.sql`, DROP TABLE). APPLIED 2026-07-11 (wave-alpha). |
| 190 | 190_community_counter_integrity.sql | Migration 190 (Wave-α Track D d1; DB-4 F9/F10, 2026-07-11). AUTHOR-ONLY — applies in the operator DDL window; companion recount script `scripts/_wave-alpha/recount-community-counters.mjs` runs after apply. Recreates the three LIVE-layer community counter-maintenance trigger functions (`update_community_group_member_count`, `update_community_post_reply_count`, `update_case_study_validation_count`) as **SECURITY DEFINER** (were INVOKER — RLS-path writes silently no-op'd the parent-table UPDATE, so counters drift with no error; F10 silent-no-op class). Bodies verbatim from live `pg_get_functiondef` (read 2026-07-11) with search_path pinned per mig-160. ALSO gives `community_groups.weekly_post_count` its first writer (F9 — displayed, never written): the post trigger recounts the group's trailing-7-day window deterministically on every post insert/delete (documented honesty bound: exact as of the last post write; a >7-day-idle group can overstate until the next write or the recount script). Deliberately NOT flipped: `update_section_thread_count`/`update_thread_reply_count` (forum layer, dropped by 192) + `update_vendor_endorsement_count` (Track E). Reversible (rollback restores INVOKER bodies). APPLIED 2026-07-11 (wave-alpha). |
| 191 | 191_org_membership_ban_guard.sql | Migration 191 (Wave-α Track D d2; master-gap P2, 2026-07-11). AUTHOR-ONLY — operator DDL window. The DB half of the ban re-join block: BEFORE INSERT (and UPDATE OF user_id/org_id) trigger `org_memberships_ban_guard_trigger` on `org_memberships` raises 42501 when the (org_id, user_id) has an `org_member_bans` row. SECURITY DEFINER so the ban lookup is not blinded by the owner/admin-scoped SELECT policy on org_member_bans (INVOKER guard would read 0 rows for a non-admin caller and silently pass — the F10 class). Backstops the two app-layer checks that already exist (accept_invitation guard from mig 156; members-route PUT add-by-email check) so no writer — service-role, user-JWT RLS-path, or a future forgetful caller — can bypass the block. Reversible (DROP TRIGGER + FUNCTION; app checks remain). APPLIED 2026-07-11 (wave-alpha). |
| 192 | 192_drop_forum_layer.sql | Migration 192 (Wave-α Track D d4 ruling: DROP; DB-4 F6, 2026-07-11). AUTHOR-ONLY — operator DDL window. RULING = **drop the dead parallel forum layer** (decision rule: zero code paths + zero usage → delete via gate). Fresh evidence this dispatch: grep over src/+scripts/ for forum_sections/forum_threads/forum_replies = ZERO hits (only refs are the mig-007 DDL, a 075 comment, and the seed INSERTs — removed same commit); live data = forum_sections 17 seeded rows (thread_count 0), forum_threads/forum_replies 0 rows; the shipped Community surface runs entirely on the mig-028+ community_groups conversation layer. Drops (child→parent) forum_replies, forum_threads, forum_sections (+ their policies/triggers/indexes), the `case_studies.linked_thread_id` inbound FK column (case_studies STAYS — d5 scope), and the two orphaned trigger functions (`update_thread_reply_count`, `update_section_thread_count`). Data loss (17 seed rows) named + snapshotted in `docs/ops/wave-alpha-closeout-2026-07-11/deletions-log.md`; reproducible from git history of seed-community.sql. Reversible (rollback recreates schema+RLS+triggers+column; seed DATA restores from git history). APPLIED 2026-07-11 (wave-alpha). |
| 180 | 180_drop_orphan_rpcs_and_dead_views.sql | Migration 180 (Wave-α Track E e5). Drops 2 orphan RPCs (`get_workspace_members`, `related_items_derived`) + 5 zero-consumer views (`open_conflicts`, `provisional_sources_review`, `source_health_summary`, `active_intelligence_items`, `item_related_items_derived`) — each proven zero-consumer fresh (grep + live catalog). Reversible (rollbacks/180 recreates all 7). APPLIED 2026-07-11 (wave-alpha). |
| 181 | 181_drop_vendor_family.sql | Migration 181 (Wave-α Track E e3). Drops the vendor table family (`vendors`, `vendor_endorsements`, `vendor_regulations`, `vendor_technologies`, all 0 rows) + `update_vendor_endorsement_count` trigger fn (removed-from-scope per 2026-05-24 ruling). Reversible (rollbacks/181 recreates from mig-007 DDL + live policies). APPLIED 2026-07-11 (wave-alpha). |
| 182 | 182_repoint_policies_off_user_profiles.sql | Migration 182 (Wave-α Track E e6, part 1 — MUST apply before 183). Repoints the 3 RLS policy arms referencing `user_profiles` (moderation_reports_select/update_admin, post_promotions_select) onto `profiles.is_platform_admin`, so 183 can drop the mirror table. No standalone rollback (superseding policy defs; 183's rollback recreates user_profiles). APPLIED 2026-07-11 (wave-alpha). |
| 183 | 183_drop_user_profiles_mirror.sql | Migration 183 (Wave-α Track E e6, part 2). Drops the `user_profiles` mirror table (1 row) + both mirror functions + the profiles-side mirror trigger, after 182 repointed its last 3 RLS readers. Reversible (rollbacks/183 recreates the table + re-seeds). APPLIED 2026-07-11 (wave-alpha). |
| 184 | 184_drop_ingestion_pair.sql | Migration 184 (Wave-α Track E e7). Drops the frozen contradictory `ingestion_state` (774) + `ingestion_control_log` (709) pair (zero consumers). Precondition honored: rows exported read-only and relocated to the PRIVATE repo `Dwarves77/caros-ledge-backups` under `archives/ingestion-pair-2026-07-11/` BEFORE apply. Reversible (rollbacks/184 recreates schema; data restores from the private archive). APPLIED 2026-07-11 (wave-alpha). |
| 185 | 185_drop_dead_columns.sql | Migration 185 (Wave-α Track E e9). Drops 7 proven-dead (all-NULL, zero code/fn/view refs) columns: intelligence_item_versions.created_by_run_id, regions.operations_decisions, region_dimension_coverage.last_reviewed_at, sources.{classification_observed_distribution, last_observed_at, spotchecked_at, spotchecked_by}. Conservative slice (the other ~23 candidates held with reasons in the deletions log). Reversible (rollbacks/185 re-adds the columns). APPLIED 2026-07-11 (wave-alpha). |
| 200 | 200_canonical_instrument_key.sql | Migration 200 (Wave-α Track C8 / RD-5). Adds `intelligence_items.canonical_instrument_key text` + `derive_canonical_instrument_key(instr, src_url)` (IMMUTABLE — bare CELEX from a CELEX token or ELI reg/dir/dec path in instrument_identifier or source_url; NULL when not confidently derivable, never guesses R/L/D from a bare YYYY/N) + BEFORE INSERT/UPDATE trigger (derivation wins, preserves a non-derivable manual value) + partial UNIQUE index `WHERE key IS NOT NULL AND provenance_status='verified' AND is_archived IS NOT TRUE` (twin-defect guard, correctly excludes archived tombstones). Ships with `backfill-canonical-keys.mjs` (RD-5, 20/21 set; 1 unverified skipped by guard_provenance_flip, key immaterial) + `canonical-key-uniqueness.mjs` (invariant EP-11). Proof: 4 derive cases pass; uniqueness audit PASS (175 verified-live, 0 collisions). Reversible (rollbacks/200 drops index+trigger+both fns+column). APPLIED 2026-07-11 (wave-alpha). |
| 201 | 201_pause_flag_session_guard.sql | Migration 201 (REWORKED — pause-flag-has-one-writer / RD-23, ruled 2026-07-12; SUPERSEDES the DEAD 2a operator-credential design). **APPLIED 2026-07-12.** Structural one-writer for the system_state STOP FLAGS (`global_processing_paused`, `scrape_cadence`) with **no role, no credential, no secret, no manual step**. The ONE sanctioned writer is the SECURITY DEFINER RPC `admin_set_pause_state`, which declares a transaction-local marker `app.pause_flag_writer`; the BEFORE UPDATE trigger `guard_pause_flag_writer` BOUNCES any flag change lacking the marker (a generic service-role `UPDATE system_state SET global_processing_paused=…` is rejected insufficient_privilege — the exact 2026-07-12 abuse). Retains `system_state_flag_audit` (origin log) and defensively drops the never-applied 2a `guard_operator_stop_flags`/role. Paired with the STATIC F20 fitness function (no direct pause-flag write in src outside the admin route) + the runtime guard. Dependent code: the pause-global route calls the RPC via `supabase.rpc` (no direct write). Red-then-green: `scripts/verify/pause-flag-guard-proof.mjs` proves the bounce on a SYNTHETIC temp table in a rolled-back transaction — the live flag is NEVER written, including by the test. Reversible (DROP TRIGGER + both functions; audit table drop optional). The DEAD 2a artifacts (`operator-control.ts`, `OPERATOR_CONTROL_DATABASE_URL`, the credential harness) are removed in the same PR. |
| 202 | 202_standard_own_body_floor.sql | Migration 202 (operator ruling 2026-07-13). Scopes the `standard` authority floor to the item's OWN authoring body: `validate_item_provenance` grounds a `standard` item's FACT at the standards-body tier (4) ONLY when the fact's source shares the item's own-source `institution_id` (migration-122 institution SSOT); a same-tier UNRELATED host stays at the reg-family floor 2. Monotonic + standard-only (can add verifications, never remove any); all other item types/criteria byte-identical to the prior function. Mirrored in JS by `authorityFloorForFact` (source-blocks.mjs; accept/reject golden) + invariant SC-14 + source-credibility-model floor-vocab anchor. **APPLIED 2026-07-13** via the direct postgres pooler; verified non-regressive (30/30 verified-live still valid; non-standard controls o5/o13 unchanged; c5/c8/green-building correctly stay quarantined); recovered c3 (GRI) + c4 (ISO 14083) to verified at $0. Reversible (re-apply the captured prior function body). |
| 204 | 204_agent_runs_model_column.sql | Migration 204 (operator ruling 2026-07-15, from the build-phase spend-regime ledger findings). Adds `agent_runs.model text` (nullable) so per-MODEL actuals are FIRST-CLASS — the build-phase MEASUREMENT requirement (spend-regime: actuals per item/class/**model**). The model was already known at the spend chokepoint (`recordSpendCall`) but stashed in `errors[].telemetry.model` (documented NO-DDL workaround); this lands the queued column and `spend-client.ts::recordSpendCall` now writes it. Cost-0 marker/aggregate rows may leave it null. Additive + idempotent (`ADD COLUMN IF NOT EXISTS`). **APPLIED 2026-07-15** via apply_migration. Reversible (DROP COLUMN model). |
| 205 | 205_funded_pass_runlock.sql | Migration 205 (Wave 2 concurrent-race incident, 2026-07-15). Creates `funded_pass_runlock` (one row per lock_key: holder pid/host/label, acquired_at, heartbeat_at) + the atomic SQL functions `acquire_funded_pass_lock` (insert-or-stale-takeover-or-fail, OUT cols cur_* prefixed to avoid the plpgsql variable/column shadow), `heartbeat_funded_pass_lock`, `release_funded_pass_lock`. DB-level mutual exclusion so a second funded-pass process can never drive a worklist concurrently (the race that zeroed Nashville + Fjords); a live holder blocks acquisition and the runner exits ZERO-spend (exit 6); a holder whose heartbeat is >300s stale is claimable (takeover logged). Wired in `scripts/funded-pass.mjs` (acquire at start, between-item heartbeat + emergencyPaused poll, release in finally) + `scripts/lib/funded-pass-lock.mjs`. Golden `scripts/verify/funded-pass-lock-golden.mjs` (second-instance reject, stale takeover, heartbeat ownership, release). Additive (new table + fns). **APPLIED 2026-07-15** via execute_sql. Reversible (DROP the 3 fns + table). |
| 206 | 206_mint_gate_hold_marker.sql | Migration 206 (hardening A1 flip, operator ruling 2026-07-16). Adds `section_claim_provenance.mint_hold_reason text` and patches `validate_item_provenance` criterion 3 to fail `fact_mint_hold` on any non-null value, so the mint-time S-CONFLATE HARD gate holds a conflated FACT's item until a re-ground clears it. NON-REGRESSIVE by construction: new nullable column, null on every pre-flip fact; the mint pipeline sets it only going forward. Proven: 196 verified items, 194 still valid, 2 pre-existing drift (source_not_active / unlabeled_assertion, NOT fact_mint_hold). S-NUMERIC is a SOFT hold (a `data_quality` integrity_flag, item stays verified-eligible) and does NOT set the column. The function is patched PROGRAMMATICALLY (fetch current def, inject 2 edits, re-create) to avoid transcribing the escaped label/legal regexes; idempotent (no-ops if already `fact_mint_hold`). Wired in `canonical-pipeline.ts` (S-CONFLATE marks mint_hold_reason, S-NUMERIC writes the flag). Golden `scripts/verify/mint-gates-live-hold.golden.mjs`; hard-hold proven live-and-reversibly. Invariant RD-41. **APPLIED 2026-07-16** via execute_sql. Reversible (DROP COLUMN + restore the prior function body). |
| 207 | 207_own_body_types_extension.sql | Migration 207 (DRIFT MIGRATION: documents DDL already applied 2026-08-01 04:44:00 UTC, schema_migrations entry 20260801004400). Extends the authority floor scope from 'standard' items alone to the full voluntary-instrument family: `c_own_body_types := ARRAY['standard','framework','initiative']`. A FACT on a voluntary instrument grounds at tier 4 (issuing-body authoritative text) ONLY when its source shares the item's own-source institution_id; a same-tier UNRELATED host stays at the item-type floor. Monotonic + voluntary-only (expands the 'standard' scoped floor to framework and initiative; all other item types byte-identical to the prior function). Mirrored in JS by `authorityFloorForFact` (extends to framework/initiative). Re-applying is a no-op when c_own_body_types is present; includes an "already applied" notice in that case. Reversible (restore the prior function body). |
| 203 | 203_holdings_quality.sql | Migration 203 (operator dispatch 2026-07-14, "SUPABASE HOLDINGS AUDIT"). Creates `holdings_quality` — per-capture holdings-quality audit store (one row per stored capture: `capture_kind` snapshot=raw_fetches | pool=agent_run_searches aggregate) keyed to an item. Records `completeness` (NO-KNOWN-DEFECT / TRUNCATED / FURNITURE / STUB — detects KNOWN DEFECT CLASSES, never proves wholeness), `sufficiency` (covers_grounding / corroborators_only / insufficient), `publisher_shape`, `bytes_collected` (bigint, matches raw_fetches.html_bytes), `clean_content_chars`, `declared_size`, `defect_evidence` (jsonb proof), `stale_verified`. Partial unique indexes: one snapshot row per capture_ref, one pool-aggregate per item. RLS mirrors integrity_flags (org_memberships owner/admin SELECT; service_role ALL). The integrity guarantee stays grounding-side (floor-first truncation moat); this table is a re-collection triage index, not a completeness certificate. **APPLIED 2026-07-14** via apply_migration. Reversible (DROP TABLE holdings_quality). |
| 208 | 208_claim_versions_nondestructive_grounding.sql | Migration 208 (operator RULING — non-destructive grounding, 2026-07-16). Creates `claim_versions` — the PRESERVED claim history for the non-destructive grounding doctrine. The current ledger stays in `section_claim_provenance` (consumers unchanged); this append-only sibling holds the prior state of a claim whenever a re-ground CHANGES its attribution (`supersede_reason='changed'`) and the final state of a claim erased on PROVEN inaccuracy (`supersede_reason='proven_inaccurate'` + `inaccuracy_proof`). `current_claim_id` / `intelligence_item_id` are SOFT references (NO cascading FK) so history survives current-row/item deletion — the whole point. Constraints: `supersede_reason` vocab + `proof_required` (a proven_inaccurate row MUST carry `inaccuracy_proof`). Consumed by `ledger-apply.mjs` (`applyLedgerDiff` archives on change; `eraseClaimWithProof` archives-before-deletes fail-closed). Invariants RD-44 / RD-45; doctrines grounding-is-non-destructive + erase-only-on-proven-inaccuracy. **APPLIED 2026-07-16** via apply_migration. Reversible (DROP TABLE claim_versions). |
| 210 | 210_claim_versions_orphaned_reason.sql | Migration 210 (operator ruling 2026-07-16, third live-ledger exit). Extends the `claim_versions.supersede_reason` vocab to add `orphaned_no_prose_referent` — a claim whose text annotates NO prose paragraph in any section certifies nothing customer-facing (dead ledger weight from prior synthesis, proven FUNCTIONLESS not proven wrong). Distinct reason from `proven_inaccurate` so the taxonomy stays honest. Also tightens `claim_versions_proof_required` so ANY non-`changed` exit (both `proven_inaccurate` and `orphaned_no_prose_referent`) MUST carry `inaccuracy_proof`. The exhaustive live-ledger exits are now: changed (re-attribution) / proven_inaccurate (cross-instrument, evidenced) / orphaned_no_prose_referent (functionless, slot-coverage-safe); relabel-to-ANALYSIS stays LIVE. Consumed by drain-clear.mjs (three-exit version-out, condition-b slot-safety). Doctrine addendum on erase-only-on-proven-inaccuracy; invariant RD-45. **APPLIED 2026-07-16** via apply_migration. Reversible (restore the 208 2-value vocab + proof constraint). |
| 211 | 211_drain_worklist_and_mutation_leases.sql | Migration 211 (operator ruling 2026-07-16, PARALLEL DRAIN). Adds two tables for the two-writer drain: `drain_worklist` (the DB-resident lane split — Lane A judgment / Lane B mechanical, one row per quarantined item, upserted by the classifier) and `mutation_leases` (per-item H5 lease: one LIVE lease per item, row exists only while leased). Lease functions mirror the proven funded_pass_runlock (mig 205) atomic acquire-or-stale-takeover-or-fail: `acquire_mutation_lease` (insert-or-stale-takeover-or-refuse, returns acquired/takeover + incumbent holder), `heartbeat_mutation_lease` (still_held; empty = taken over), `release_mutation_lease` (holder-only delete). A live lease held by the other session blocks acquisition so no item is ever worked twice; heartbeat past 300s is claimable. Consumed by `scripts/lib/mutation-lease.mjs` (acquireLease/heartbeat/release/withLease). Golden `scripts/verify/mutation-lease.golden.mjs` (10/10: refuse-on-held, incumbent-named, holder-only heartbeat/release, re-acquire-after-release, stale-takeover). Closes the H5 residual named in RD-38. Additive tables + fns. **APPLIED 2026-07-16** via apply_migration. Reversible (DROP the 3 fns + 2 tables). |
| 212 | 212_corpus_census.sql | Migration 212 (operator ruling 2026-07-16, PARALLEL DRAIN census). Adds `corpus_census` — the total-corpus classification table, one row per item. Live items carry provenance_status ($0); ARCHIVED items carry a Haiku verdict verifying the archive_reason (archive_correct vs review_valuable-wrongly-archived) — the archive-endgame input + true corpus number. Read-only classification (no item mutations). Runner `scripts/_reground/census-run.mjs` (--audit N accuracy gate, --run full; metered Haiku through the spend-guard, $15 ceiling). Additive. **APPLIED 2026-07-16** via apply_migration. Reversible (DROP TABLE corpus_census). |
| 213 | 213_disposition_ledger.sql | Migration 213 (operator amendment 2026-07-16, tombstone-then-delete). Adds `disposition_ledger` — permanent institutional memory of every archived item DELETED to reach zero-archived: item_key, canonical_instrument_key, archive_reason, snapshot_pointer, disposition (tombstone_delete_* / merged_into), disposition_date. Append-only, NO FK (survives the item delete), indexed on canonical_instrument_key so the holdings-gate / expansion-time dedup can forever answer "already evaluated + dispositioned". Written BEFORE the guarded delete (fail-closed). Consumed by `scripts/_reground/tombstone-delete.mjs` (lease -> tombstone -> guarded delete). Additive. **APPLIED 2026-07-16** via apply_migration. Reversible (DROP TABLE disposition_ledger). |
| 209 | 209_provenance_status_on_delete.sql | Migration 209 (operator ruling 2026-07-16, drain root-gap fix). The `set_provenance_status` trigger fired only on AFTER INSERT/UPDATE, never DELETE — so versioning a gate-FAILING claim OUT of the live ledger (the drain prior-junk clearance / hold-loop via `eraseClaimWithProof`) removed the blocker but did NOT recompute `provenance_status`, leaving the item quarantined with a valid ledger until an unrelated write nudged it. Fix: extend `set_provenance_status()` to handle `TG_OP='DELETE'` (derive item id from OLD, RETURN OLD) + add an `AFTER DELETE` trigger on `section_claim_provenance`. A CASCADE delete of the item itself is a safe no-op (the NOT-FOUND guard). DB-layer, so it covers ALL writers by construction (executor-universality ruling). Pure trigger/function DDL, no row rewrites, no backfill; idempotent. **APPLIED 2026-07-16** via apply_migration. Reversible (restore the 139-revision function body + DROP the DELETE trigger). |
| 214 | 214_coverage_gap_candidates.sql | Migration 214 (Session C, coverage-discovery lane, 2026-07-17, read-only investigation dispatch). Adds `coverage_gap_candidates` — the ONE permitted write of a read-only lane pricing the 60-to-400 corpus expansion. One row per candidate instrument: rank, jurisdiction, primary_vertical, transport_mode, freight_relevance (FSI lens), estimated_priority, `coverage_class` (MISSING / AMBIGUOUS_ARCHIVED / HAVE_QUARANTINED — the operator's label-is-not-proof evidence hierarchy; HAVE items are confidently excluded and not stored), corpus_match_ref, sizing_class (major/minor), entity_confirmed (web-search-verified this session), authoritative_url, notes. Seeded with 16 candidates cross-checked against the live corpus (canonical_instrument_key, instrument_identifier, titles) and disposition_ledger (tombstones = husk-evidence, never instrument-coverage evidence). Not a worklist; mints/acquires/grounds nothing — candidates enter the corpus only through a future priced intake wave. Additive. **APPLIED 2026-07-17** via apply_migration. Reversible (DROP TABLE coverage_gap_candidates). |
| 215 | 215_drop_source_conflicts_dormant.sql | Migration 215 (dormant-systems purge P-6, operator ruling R2 2026-07-18). Drops the dormant `source_conflicts` table (0 rows, no writer since openSourceConflict removed 2026-07-11; its reader `fetchOpenConflicts`, store slice, "Data Conflicts" admin tab, and the `computeConflictResolutionImpact` engine were all removed in the same PR; the `open_conflicts` view was already dropped by migration 180). CONTENT GATE: a `DO` block asserts the table is empty and RAISEs (aborts) if it holds any row (the stop-if-live-data guard), then `DROP TABLE ... CASCADE` (drops the migration-005 RLS policies with it). BREAK-RISKY (ADR-011, destructive DROP + dev=prod). **APPLIED 2026-07-18** via apply_migration (operator granted full access; content gate passed — 0 rows; `to_regclass('public.source_conflicts')` now null, `open_conflicts` view already gone via 180). Reversible by re-creating from migrations 004 (table) + 005 (RLS); no data lost (empty). |
| 216 | 216_item_source_evidence.sql | **REVERTED by migration 218 (2026-07-19)** — full Supabase audit found this a DEAD/DUPLICATE store (0 rows; byte-identical to the existing `agent_run_searches.result_content_excerpt`; all 210 verified items already held their evidence in that pool). Original description follows. Migration 216 (Phase R F3 addendum, operator ruling 2026-07-19 option a). Adds `item_source_evidence` — the durable, append-only, DB-queryable criterion-3 evidence store: `(id, intelligence_item_id, content_hash, cleaned_text, created_at)`, UNIQUE(intelligence_item_id, content_hash), RLS on with no policy (service-role only). Holds the CLEANED pool text (byte-identical to `agent_run_searches.result_content_excerpt`) that `validate_item_provenance` criterion 3 checks, so a re-generate's DELETE-then-INSERT of the working pool no longer erases the prior criterion-3 evidence. APPEND-ONLY at the DB layer: a BEFORE UPDATE/DELETE trigger (`item_source_evidence_append_only`) RAISES for any UPDATE or DELETE, by anyone incl. service role; idempotent writes use ON CONFLICT DO NOTHING (not an UPDATE). Additive (new empty table). **APPLIED 2026-07-19** via apply_migration. Append-only proven by `scripts/_diag/_f3-append-only-proof.sql` (rolled-back probe: upd_blocked=t del_blocked=t, 0 rows persisted). Reversible (DROP TABLE + DROP FUNCTION). |
| 217 | 217_criterion3_durable_evidence_superset.sql | **REVERTED by migration 218 (2026-07-19)** — criterion 3 restored to the working-excerpt-only check (the superset referenced the dead `item_source_evidence`). Original description follows. Migration 217 (Phase R F3 addendum). Makes `validate_item_provenance` criterion 3 a SUPERSET: a FACT span passes when present in the working excerpt (`agent_run_searches.result_content_excerpt`) OR in the durable store (`item_source_evidence`, mig 216). SURGICAL + anchor-verified: a `DO` block reads the function's OWN `pg_get_functiondef`, asserts the exact criterion-3 span anchor exists (RAISEs if the function drifted), replaces ONLY that block, and re-creates the function — no hand-transcribed body. Monotonic add (the working-pool check is never removed), so no currently-verified item can flip to quarantined. **Gated on the zero-flip prover** (`scripts/_diag/_f3-zero-flip-prover.sql`: 0 would-flip / 210 baseline verified, run + committed BEFORE apply). **APPLIED 2026-07-19** via apply_migration; post-apply verified: superset present, old null-check replaced (not duplicated), verified-live still 210, sample verified item still valid. Reversible by re-applying the pre-217 function body. |
| 218 | 218_revert_item_source_evidence_dead_duplicate.sql | Migration 218 (2026-07-19). REVERTS 216+217. Full Supabase audit established item_source_evidence was dead/duplicate: 0 rows, its writer stored cleanCtl(b.text) byte-identical to the existing agent_run_searches.result_content_excerpt (per-item, SQL-queryable, up to 600KB/row), all 210 verified items already held their criterion-3 evidence in that pool (0 missing), and raw_fetches (678 rows) is the existing permanent snapshot the F3 instruction named. Surgical anchor-verified reverse of 217 (restores criterion 3 to the working-excerpt-only span check, monotonic-safe: all 210 verified still pass), then DROPs the empty table + trigger + trigger function. **APPLIED 2026-07-19** via apply_migration; post-apply verified: function no longer references the table, restored to original, table null, verified-live still 210, sample item still valid. Zero data loss (table was empty). |
| 219 | 219_drop_dead_backup_and_superseded_tables.sql | Migration 219 (structure-audit cleanup, operator ruling 2026-07-19). Tombstone-then-delete DROP of 8 dead tables per the full-schema audit (docs/audits/supabase-structure-audit-2026-07-19.md): the 6 zero-code-ref backup/one-shot tables (intelligence_items_pre_phase5 655, pending_jurisdiction_review_pre_phase5 107, item_supersessions_pre_phase5 5, ingest_rejections_pre_phase5 0, institution_regroup_snapshot_20260712 66, intelligence_items_domain_backfill_audit 212), hold_resolution_queue (39; created by NO committed migration = out-of-repo DDL; superseded by drain_worklist, proven: 32/39 already in drain, 6 verified, 1 gone, 0 rows needed migration), and briefings (0; early predecessor of full_brief). Content-gated (a table that grew since audit ABORTS). **APPLIED 2026-07-19** via apply_migration; post-apply verified: all 8 to_regclass null, verified-live 210 intact, drain_worklist intact, validator valid on a live sample. Backups reversible only from their creating migrations (live data untouched). |
| 220 | 220_portal_link_candidates_disposition.sql | Migration 220 (B1 portal-harvest consumer, scrape-and-build plan 2026-07-19). Adds disposition columns to `portal_link_candidates` (mig 162): `disposition_reason` (why the row left 'candidate' — entity-gate verdict / chokepoint reject reason / mint trail, verbatim from the machine gate), `dispositioned_at`, `item_id` (FK → intelligence_items, ON DELETE SET NULL — the item a promoted row minted). A disposition without a recorded reason is the RD-6 silent-backlog shape; the B1 consumer (src/lib/intake/portal-harvest.ts) stamps all three on every status change. Two-track: **APPLIED 2026-07-19** via apply_migration before the dependent consumer code merged. No data change (existing rows keep NULLs). Reversible (DROP COLUMN). |
| 221 | 221_census_worklist.sql | Migration 221 (census-management lane, operator dispatch 2026-07-19, Task 1). Adds `census_worklist`, the full-corpus gap-census data layer, one row per (source, document) an enumeration pass has seen (not yet a corpus item). Reuse-before-construction ruled against extending `corpus_census` (mig 212, keyed on `intelligence_item_id`, structurally cannot represent an undiscovered document) or `coverage_gap_candidates` (mig 214, a hand-curated one-off ranking, not a mechanical ledger); shape reused from `portal_link_candidates` (source_id + url + guarded status + disposition-reason pattern) but built as a new table since B1's live intake ledger and the census's measurement pass are different lifecycles. Columns: `source_id`/`document_url` (UNIQUE pair) + `lane` (A\|C producer) + `shape_class` + `enumeration_status` (discovered→classified→dry_run_complete→reconciled, plus `flagged`, guarded forward-only by trigger) + `cap_hit` + `dryrun_disposition` (would_mint/dedup_hit/congruence_reject/invariant_reject/hold+hold_reason) + `surface_tags` (multi-tag text[], CHECK-limited to the four machine-addressable surfaces, Community excluded) + `instrument_identifier`/`resolved_into_id` (cross-source dedup, Task 2) + `flagged_reason`/`flagged_at` (RD-6 shape). Two triggers: `census_worklist_no_delete` (append-only, blocks DELETE unconditionally, service role included) and `census_worklist_guarded_update` (identity columns immutable after insert; enumeration_status transitions forward-only through the ladder, `flagged` reachable from any rank, one reset path flagged→discovered). Lease discipline reuses `mutation_leases` (mig 211) unchanged: its `intelligence_item_id` column carries no FK constraint, so it already works as a generic leasable-key keyed on `census_worklist.id`; no schema change needed there. **APPLIED 2026-07-19** via apply_migration; post-apply verified live (column/type/default read-back matches spec) and smoke-tested inside a rollback (forward transition passes; backward transition, identity mutation, hold-without-reason, invalid surface tag, and DELETE all correctly rejected; zero rows persisted after rollback). Reversible (DROP TABLE census_worklist; DROP FUNCTION census_worklist_no_delete, census_worklist_guarded_update). |
| 222 | 222_census_rollup_stitch.sql | Migration 222 (census-management lane, Task 1 follow-on, discipline correction). Two parts. PART 1: retroactive capture of `coverage_gap_census_findings` (Session C's discovery-lane table, authored and owned by Session C, applied live 2026-07-19 with no committed migration file, the same DDL-before-migration gap 221 was originally flagged for). Verified by fresh introspection (columns, constraints, indexes, comments) against the live table, not written from memory; `CREATE TABLE IF NOT EXISTS` is idempotent, so if Session C's own migration for this table lands separately this is a no-op, never a conflict. PART 2: `census_rollup_by_surface`, a view stitching `census_worklist` and `coverage_gap_census_findings` per Session C's schema-stitch review (commit `b5185b6d`, posted to session-log/board, read as the spec per operator instruction). The two tables share a STRUCTURAL GRAIN MISMATCH, not a naming one, `census_worklist.source_id` is `NOT NULL` (models documents inside an already-held source); every one of `coverage_gap_census_findings`' rows is a candidate source not yet held (verified: zero of 81 rows match a registered `sources` row) so the view never joins them by FK, it normalizes both to a common per-surface reporting projection (held/missing_from_held_sources from `census_worklist`; missing_from_world/pending_on_session_a from `coverage_gap_census_findings`, `pending_dependency` counts carried as their own visible column, never folded silently into "missing"; honest residual buckets for every other disposition state, no silent caps). Alignment applied only where semantics genuinely match (`lane` matches natively; `would_mint` is the one disposition value aligned across both vocabularies; the rest of each vocabulary stays distinct, forcing them together would lose real information on both sides). Feeds `docs/census/gap-census-2026-07.md`'s per-surface rollup table. **APPLIED 2026-07-19** via apply_migration; post-apply verified live against real data (Session C's 81 rows correctly tallied per surface: e.g. regulations enumerated_world=20/missing_from_world=18/pending_on_session_a=1/declined_or_parked_world=1, sums consistent; all `census_worklist`-side columns 0, matching the empty table). Reversible (`DROP VIEW census_rollup_by_surface`; `DROP TABLE coverage_gap_census_findings` reverses PART 1 only if Session C's own migration hasn't also landed it by then, check first). |
| 225 | 225_gate_a_criterion7.sql | Migration 225 (Gate A criterion 7 flip, 2026-07-26). Injects criterion 7 into `validate_item_provenance` via `pg_get_functiondef` inject-and-apply (anchor = the unique final `v_result.valid := (jsonb_array_length(v_failures)=0);` line; the `failures :=` line is NOT unique — item-not-found branch). Criterion: a brief-bearing item (`full_brief<>''`) is valid ONLY with a CURRENT-HASH CLEAN `item_gate_a_state` row (`scanned_hash = md5(full_brief)` AND `orphan_count=0`); missing/stale/orphaned => quarantined. Landed after 224 + 100% backfill so it evaluates truthfully from moment one. **APPLIED 2026-07-26** via apply_migration; GREEN 4/4 red/green criterion test (clean→verified, orphaned/stale-hash/missing-state→quarantined); read-only-verified exactly 329/345 brief items fail criterion 7. Reversible (re-apply the pre-225 function def). NOTE: realizing the status flip is gated by `guard_provenance_flip` (mig 43) for `unverified`-origin items — see session log 2026-07-26. |
| 227 | 227_derived_claim_kind.sql | Migration 227 (Gate B derived-date mechanism, 2026-07-27). Adds `claim_kind='DERIVED'` to the section_claim_provenance CHECK + a self-FK `basis_claim_id` (→ the FACT claim a derived instance is computed from, ON DELETE SET NULL) + a fail-closed CHECK `scp_derived_requires_basis` (every DERIVED row MUST carry a basis). Backs the Gate-A scanner's SECOND coverage arm (`gate-a-derived.derivedCoveredTokens`, a pure DB lookup): a derived date (e.g. "1 June 2027" from an annual June-1 rule) is credited ONLY while its basis FACT exists AND that FACT's span still verbatim-matches its stored capture — staleness/deletion reverts the token to orphan on re-scan (derived-basis rule; re-grounds never destroy). No prose-pattern exemption anywhere in the scanner (stays literal). GATE_A_VERSION .2→2026-07-27.1. **APPLIED 2026-07-27** via apply_migration before the consumer (scanBrief derivedCovered arm + canonical-pipeline scan-site) committed; unit red/green 6/6; proven on one Operations item (e5c17fac "2027", orphan_count 5→4). Reversible (drop the 2 constraints + the column). |
| 228 | 228_census_identity_verification.sql | Migration 228 (Coverage Index identity verification, B1, 2026-07-27). Adds six nullable `identity_*` columns to `census_worklist` (`identity_checked_at`, `identity_http_status`, `identity_resolves`, `identity_scheme` CHECK celex/eli/uk-legislation/generic/none, `identity_shape_valid`, `identity_host_registered`) + a partial index on (dryrun_disposition, identity_resolves) WHERE would_mint. The SECOND verification axis for the Coverage Index (the `CoverageIndexPanel` mounted INSIDE the existing five surfaces — primarily Regulations, entries by category — NOT a sixth top-level surface, PI-1): the census `would_mint` set rendered as a dual-verified catalogue (axis 1 = relevance via `notes` `[low-relevance]` tags — firm-core vs soft-tail; axis 2 = identity, here). Populated by `scripts/coverage/identity-resolve.mjs` (deterministic URL + instrument-identifier shape via `src/lib/coverage/identity.mjs`, then a free-fetch liveness probe — plain node fetch, $0, no metered service). HONESTY of the probe: `identity_resolves` is TRUE only on a confirmed 2xx/3xx, FALSE only on a confirmed 4xx/5xx, NULL on timeout/rate-limit/network error (could-not-confirm) — our own throttling never fabricates a dead-link verdict. Additive/nullable: no change to the append-only identity columns or the guarded `enumeration_status` ladder. **APPLIED 2026-07-27** via apply_migration before the consumer read path (`getCoverageIndex`) + the `CoverageIndexPanel` mounts committed. Reversible (drop the 6 columns + the index). |
| 229 | 229_census_worklist_title.sql | Migration 229 (Coverage Index human-readable title, B1/P1, 2026-07-28). Adds nullable `title` + `title_source` to `census_worklist` so the customer Coverage panel shows a real title, never a bare identifier. FINDING it addresses: the prior enrichment pass (`unit3-enrich-titles.mjs`) captured 19,782 real titles via the FREE path (EUR-Lex Cellar SPARQL `expression_title` + legislation.gov.uk `dc:title`) but persisted them ONLY to gitignored scratch (`scripts/tmp/census-titles.json`) — not durable. `scripts/coverage/persist-titles.mjs` loads that capture into the column ($0, no re-fetch): 2,896/3,661 would_mint titled (79.1%), 765 residual URL-only feeds fall back to the notes descriptor in the panel (still human-readable). Additive/nullable. **APPLIED 2026-07-28** via apply_migration before the consumer (`getCoverageIndex` displayTitle + `CoverageIndexPanel`) committed. Reversible (drop the 2 columns). |
| 230 | 230_rls_lockdown_operator_control_residue.sql | Migration 230 (RLS lockdown of the anon-writable operator-control residue, P2 structural gate, 2026-07-28). RLS residue audit finding: 8 public tables had RLS DISABLED **and** full DELETE/INSERT/UPDATE/TRUNCATE/SELECT grants to `anon` + `authenticated` → **anon-key writable via PostgREST** (the anon key ships in the client bundle). Operator-control tables exposed: `funded_pass_runlock` (spend runlock), `disposition_ledger` (pipeline audit trail), `mutation_leases` (lease coordination), plus `corpus_census`, `coverage_gap_candidates`, `coverage_gap_census_findings`, `drain_worklist`, `claim_versions`. Fix: `ENABLE ROW LEVEL SECURITY` with NO policies = deny-all to anon/authenticated; `service_role` (BYPASSRLS — every internal write, script, agent runtime via `SUPABASE_SERVICE_ROLE_KEY`) unaffected. Same posture as `census_worklist`. Verified no legitimate anon path first (7/8 zero src refs; `claim_versions` written only by the service-role agent runtime). **APPLIED 2026-07-28**; post-apply verified 0/8 still RLS-disabled. Reversible (`ALTER TABLE … DISABLE ROW LEVEL SECURITY`). |
| 231 | 231_promotion_policy.sql | Migration 231 (Promotion policy engine — the policy record, P2, 2026-07-28). Creates `promotion_policy`, RLS-ENABLED FROM BIRTH (deny-all; only the `isPlatformAdmin`-gated service-role admin API at `/api/admin/promotion-policy` reads/writes it). The policy IS the authorization for promotion spend — same shape as a metered-gate amendment: `authority` (operator ref, never blank), scoped (`priority_jurisdictions`/`priority_topics`/`priority_instrument_types`), quality-floored (`require_dual_verified`/`require_firm_core` default true), hard-capped (`budget_envelope_usd` + `spent_usd`), and EXPIRING (`expires_at`). FAIL-CLOSED DEFAULT: no active/unexpired policy → no auto-promotion, no spend. Partial unique index `uidx_promotion_policy_single_active` enforces ≤1 active policy. Automated per-batch safeguards it carries: `audit_sample_size`/`audit_min_accuracy` (30-sample ≥90% audit gate), plus the metered gate + batch markers, Gate A/B per brief, and ledger projection halting at envelope exhaustion (engine, next). **APPLIED 2026-07-28** via apply_migration before the consumer route committed. Reversible (`DROP TABLE promotion_policy`). |
| 232 | 232_recent_changes_rpc.sql | Migration 232 (window-scoped What-changed feed, 2026-08-01). Creates `get_workspace_recent_changes(p_org_id, p_days default 7)` (STABLE SECURITY DEFINER, `_assert_org_membership` gated) — all verified live items with `added_date` inside the trailing window, priority-ordered, defensive LIMIT 500. Exists because the dashboard RPC (064) is LIMIT 50 by priority: once the corpus outgrew that slice, in-window additions fell outside it and the home This-week section rendered "nothing" while 216 items had been added in 7 days (2026-08-01 defect). Consumed by fetchDashboardData → WhatChanged (PR #395); render stays capped at 5 via the #393 show-all toggle, so the feed is window-complete and the render is bounded. **APPLIED 2026-08-01** via apply_migration before the consumer code merged (two-track policy). Reversible (`DROP FUNCTION public.get_workspace_recent_changes(uuid, integer)`). |
| 233 | 233_watchlist_type_expansion.sql | Migration 233 (watchlist item_type expansion, Landing B of the item-management program, 2026-08-01). Expands `user_watchlist`'s item_type CHECK from ('source','reg','signal') to add 'research' and 'operations' — the Research and Operations detail surfaces had no Watch affordance (spec-audit gap M-5); WatchButton now mounts on both (PR carries the API allowlist + component union widening). `org_watchlist` (077) carries no type CHECK, unchanged. **APPLIED 2026-08-01** via apply_migration before the consumer code merged (two-track policy). Reversible (restore the prior three-value CHECK). |
| 234 | 234_item_ownership.sql | Migration 234 (item ownership, Phase 1 of the item-management program, 2026-08-02). Adds `owner_user_id` (FK profiles, ON DELETE SET NULL), `owner_assigned_by`, `owner_assigned_at` to `workspace_item_overrides` — ownership is ORG-scoped team state per the operator ruling, so it rides the existing (org_id, item_id) override triad instead of a new table (reuse-before-construction). Partial index `(org_id, owner_user_id) WHERE owner_user_id IS NOT NULL` for By-owner rollups. Assignee-must-be-org-member is enforced API-side by the /api/workspace/overrides writer (fail-closed membership check + assigned_by/at stamping); the read path resolves display names via profiles and treats a departed member as unassigned. Consumers: OwnerTeamCard picker, DashboardByOwner, ?owner= ledger filter. **APPLIED 2026-08-02** via apply_migration before the consumer code merged (two-track policy). Reversible (drop the 3 columns + index). |
| 235 | 235_dual_scope_archive.sql | Migration 235 (dual-scope archive, item-management program, 2026-08-02). Three parts: (1) `archived_by` on `workspace_item_overrides` — attribution layer of the operator-approved 5-layer team-archive protection; (2) NEW `user_item_state` table (user_id, org_id, item_id, is_archived, archive_note, archived_at; UNIQUE user_id+item_id; RLS deny-all, service-role writes via /api/workspace/personal-state) — the PERSONAL archive scope per the operator ruling "archiving should be an option for group or individual"; (3) notifications kind CHECK gains 'archive' so workspace archives fan out to watchers + the assigned owner via the existing dispatchNotification helper. API-layer enforcement (role gate: workspace archive is admin/owner-only; reason required; any member restores). **APPLIED 2026-08-02** via apply_migration before the consumer code merged (two-track policy). Reversible (drop archived_by, drop user_item_state, restore 5-value kind CHECK). |
| 236 | 236_dual_scope_watchlist.sql | Migration 236 (dual-scope watchlist, item-management program, 2026-08-02). Aligns `org_watchlist`'s item_type vocabulary with `user_watchlist`: 077 created the team watchlist with NO type CHECK while 060/233 constrain the personal table to ('source','reg','signal','research','operations'). The two tables are the team and personal scopes of ONE feature, so the asymmetry was debt, not a design choice; both were empty (0 rows verified pre-apply) so the constraint was free to add. Also documents `item_type` and `note` via COMMENT. Deliberately introduces NO role gate: unlike team archive (235, admin/owner-gated because archiving hides an item from everyone), 077's member-scoped RLS lets any org member add or remove a team watch, and team-watching is additive. That shipped ruling stands. Consumers: the scope-aware /api/watchlist writer + reader, WatchButton's scope menu, DashboardWatchlist's team badge. **APPLIED 2026-08-02** via apply_migration before the consumer code merged (two-track policy). Reversible (`ALTER TABLE public.org_watchlist DROP CONSTRAINT org_watchlist_item_type_check`). |
| 237 | 237_personal_list_order.sql | Migration 237 (personal list order, item-management program, 2026-08-02). Creates `user_list_order` (user_id FK auth.users ON DELETE CASCADE, `list_key` text, `item_id` text, `position` numeric; UNIQUE (user_id, list_key, item_id); lookup index on (user_id, list_key, position); RLS enabled with four policies mirroring `user_watchlist` plus an UPDATE policy, all `auth.uid() = user_id`). Backs drag ordering per the operator ruling "drag order is personal", so there is NO org_id, not even nullable: a column never filtered on is dead schema that invites a wrong query later. `position` is `numeric`, not `float8` or `int`, so a drop between two neighbours writes ONE row at the exact midpoint `(a+b)/2` instead of renumbering the list, and repeated midpoint splits do not drift the way binary floats do. Reuse-before-construction was checked and REJECTED on evidence: `user_item_state` is keyed (user_id, item_id) so a position column there could express only ONE global order per item (the watchlist rail and the regulations ledger could never be ordered independently), and its `item_id` is a uuid FK to `intelligence_items` while the watchlist carries TEXT ids including `item_type='source'`, which is not an intelligence_items row at all, so reuse would silently exclude source entries from ordering. Consumers (not yet built): the `/api/user/list-order` reader/writer with a `list_key` allowlist, and the @dnd-kit sortable rail in DashboardWatchlist. **APPLIED 2026-08-02** via apply_migration before the consumer code merged (two-track policy); verified live n_cols=7, n_policies=4, rls=true, n_indexes=3, n_rows=0. Reversible (`DROP TABLE public.user_list_order`). |
| 238 | 238_reorder_user_list_item.sql | Migration 238 (atomic single-row reorder, drag ordering, item-management program, 2026-08-02). Creates `reorder_user_list_item(p_user_id, p_list_key, p_item_id, p_prev_item_id, p_next_item_id, p_seed_item_ids)` RETURNS numeric, SECURITY DEFINER, `SET search_path = public`, EXECUTE granted to service_role ONLY (revoked from PUBLIC/anon/authenticated: SECURITY DEFINER bypasses 237's RLS, so an authenticated grant would let any signed-in user rewrite another user's order). Exists because the obvious read-then-write in the API route carries two real defects, not because an RPC is tidier: (1) PRECISION, averaging two `numeric` neighbours in JavaScript rounds every midpoint through an IEEE-754 double and discards the exactness the column type exists to provide; (2) RACE, read-neighbours-then-write is two statements and 237's unique constraint is (user_id, list_key, item_id), NOT position, so two drops into the same gap would both compute the same midpoint and both write it with nothing to reject the collision. The function takes `pg_advisory_xact_lock(hashtext(user_id || ':' || list_key))` so concurrent reorders of the same list serialise while different users and lists never contend. Also seeds a never-ordered list from `p_seed_item_ids` on a 1000-step ladder in the same transaction (so the first drag has anchors), renormalises the whole list to a clean ladder when a gap falls below 1e-6, and raises when item_id equals either neighbour rather than silently placing the item where it already was. **APPLIED 2026-08-02** via apply_migration before the consumer code merged (two-track policy); smoke-verified live against a throwaway list_key covering seed+midpoint (1500), head insert (0), tail insert (3000), gap-collapse renormalisation, and the self-neighbour raise, then the smoke rows were deleted (user_list_order back to 0 rows). Reversible (`DROP FUNCTION public.reorder_user_list_item(uuid, text, text, text, text, text[])`). |
| 239 | 239_user_item_state_policies.sql | Migration 239 (RLS policies for `user_item_state`, item-management program, 2026-08-02). Hardening, not an incident: 235 created the personal-archive table with RLS ENABLED and ZERO policies, which is deny-all, and the only writer (`/api/workspace/personal-state`) uses the service client, which bypasses RLS, so nothing was ever exposed and the feature worked. Fixed because every sibling in the family carries policies (`user_watchlist` 3, `org_watchlist` 4, `workspace_item_overrides` 4, `user_list_order` 4) and deny-all-by-omission fails QUIETLY: the day a read path reaches this table with the anon or authenticated client, zero rows come back rather than an error, so a personal archive would silently read as empty and an archived item would reappear. Adds four policies mirroring 237 exactly, all `auth.uid() = user_id` (SELECT/UPDATE/DELETE USING, INSERT WITH CHECK, UPDATE carrying both so a row cannot be updated into another user's ownership), plus a COMMENT ON TABLE. `org_id` is deliberately absent from every predicate: it is nullable contextual metadata on a table whose point is the PERSONAL scope, and including it would hide a row written before the org resolved from its own author. Additive and idempotent (DROP POLICY IF EXISTS first); service_role behaviour is unchanged, so no existing caller's results move. Operator-ruled into this PR rather than filed as debt. **APPLIED 2026-08-02** via apply_migration before the consumer code merged (two-track policy); verified live n_policies=4, rls=true, n_rows=0. Reversible (drop the four policies). |
| 253 | 253_connection_themes.sql | Migration 253 (connection-theme persistence, flywheel U2, 2026-08-10). Creates `connection_themes` (id uuid pk = the theme's natural cluster anchor — `cluster.mjs`'s lexicographically-smallest-member-id, stable across reruns on a stable corpus per the fixpoint guarantee, so `gaps.mjs` can key `coverage_gap` `subject_ref` off it; member_ids uuid[], dominant_signals jsonb, surfaces text[], density real, convergence real, pivots jsonb; CHECK array_length(member_ids,1)>=2) and `connection_theme_runs` (append-only ledger: started_at/finished_at/status/args + full read/clustered/written/flagged counts — one row per `analyze-corpus.mjs` invocation, rule 15's execution record). `connection_themes` is a CACHE of the last clustering pass, not history: each analyze-corpus run fully replaces its contents (guardedDelete-all + guardedInsertMany) — `connection_theme_runs` is the durable audit trail. Consumed by `scripts/connections/analyze-corpus.mjs` (U2, writer) and the themes surface (U3, reader). RLS enabled on both, public SELECT (same posture as `item_cross_references`, migration 005 — derived analytics, not PII), service-role-only ALL for writes (guarded path only). Additive (new tables only, no existing schema touched). **APPLIED LIVE 2026-08-10** via Supabase MCP ahead of this commit — audit record, not a pending apply. Verified live: `connection_themes` 8 columns, `connection_theme_runs` 13 columns, both `relrowsecurity=true`. Reversible (`DROP TABLE connection_theme_runs; DROP TABLE connection_themes;`). |
| 254 | 254_drop_shadow_gate_a_and_broken_hrq.sql | Migration 254 (database-layer census cleanup, 2026-08-11). Content-gated tombstone DROP of 16 functions + 1 table, all out-of-repo DDL found by docs/audits/db-layer-census-2026-08-11.md. (a) FOUR BROKEN hrq_* functions (hrq_enqueue/escalate/exit/record_attempt) — their table hold_resolution_queue was dropped by migration 219 on 2026-07-19 and the API was left callable, throwing on a missing relation ever since; the second half of a cleanup that only completed one half, invisible because the functions were never in the repo next to the DROP. (b) TWELVE SHADOW gate_a_* functions re-implementing src/lib/agent/gate-a-scan.mjs in SQL, sharing its version literal 2026-07-30.1 by hand-copy with nothing enforcing equality, called by NOTHING (the TypeScript copy is the live path; canonical-pipeline.ts writes item_gate_a_state directly). (c) gate_a_route_b_baseline (430 rows, referenced by nothing anywhere) — ROWS PRESERVED verbatim in docs/audits/gate-a-route-b-baseline-2026-08-11.csv and the migration content-gates on that exact count. KEPT deliberately: gate_a_health/_compute/_refresh + gate_a_health_cache (live surface, dormant BY RULING 2026-08-10), capture_worker_fetch (runbook-invoked), next_uncensused_portal_candidates (dormant capability, product call), item_gate_a_state (984 live rows). Four pre-drop gates ABORT on any drift; post-drop assertions fail the migration if anything live was removed. **APPLIED 2026-08-11** via apply_migration; post-apply verified: 3 gate_a survivors (the health trio), baseline table null, item_gate_a_state 984 rows intact, gate_a_health() still returns its designed staleness error. Catalog 181 objects -> 164; functions 91 -> 75; F24 no-migration-home allowlist 22 -> 5; broken internal references 1 class -> ZERO. |
| 255 | 255_canonical_key_oj_sequence_suffix.sql | Migration 255 (canonical-key root cause, 2026-08-11). derive_canonical_instrument_key() discarded the OJ sequence suffix '(NN)', collapsing distinct instruments published under one CELEX stem (22008A0221(01) vs (02) are DIFFERENT agreements) onto one key — which the partial unique index uq_intelligence_items_canonical_key_verified_live then CORRECTLY refused to let stand verified together. The constraint was right; the derivation was wrong. Fix: every CELEX branch (instrument_identifier AND source_url, literal or URL-encoded parens) now tries the suffixed form first and returns CELEXKEY(NN) zero-padded; bare-CELEX and ELI derivations byte-identical to before, proven by an in-migration DO-block self-check (suffix preserved, single-digit padded, (01)/(02) distinct, 32022L2464 and eli/reg/2023/1805 unchanged, %28/%29 URL form derived). Data operation done in the same remediation pass, not in this file: 81 items on collapsed keys, 78 re-keyed via the fixed derivation, 3 legitimate manual-key residuals verified individually; the 5 stale-quarantined items recovered to verified through the normal derivation path; one item briefly mis-archived as duplicate on the collision REVERSED with the error stated in its flag note (docs/audits/data-drift-remediation-2026-08-11.md). Closes the fleet's Aug-2 shard-8 collision bug flags with root cause cited. **APPLIED LIVE 2026-08-11** via apply_migration; committed body byte-checked against pg_get_functiondef. Reversible only by restoring the suffix-discarding regex, which would re-create the collision. |
| 256 | 256_migration_homes_and_vault_capture_key.sql | Migration 256 (migration homes complete + vault the capture JWT, 2026-08-11). Writes the LAST FIVE out-of-repo database objects into the migration tree verbatim from their live definitions captured via pg_get_functiondef (gate_a_health_cache table, gate_a_health_compute, gate_a_health_refresh — deliberately UNSCHEDULED per operator ruling 2026-08-10, the 30-minute staleness gate makes the dormancy visible by design —, gate_a_health, next_uncensused_portal_candidates), all CREATE OR REPLACE / IF NOT EXISTS no-ops against live. ONE deliberate change: capture_worker_fetch's hardcoded anon-role JWT literal moves to Supabase Vault (secret capture_worker_anon_key, created idempotently), read at call time with a fail-loud if absent — key rotation becomes one vault update instead of a silent break inside a SECURITY DEFINER body no repo-side scan can see. The anon key is PUBLIC by design; vaulting is rotation visibility, not secrecy. Post-check RAISEs if the function still embeds a JWT literal ('eyJ'). Completes the arc db-layer-census -> migration 254 (drop 17) -> this (backfill 5): F24's NO_MIGRATION_HOME allowlist 22 -> 5 -> EMPTY, now audited as the permanent target state; F24 NET_EGRESS_SANCTIONED updated (JWT vaulted). **APPLIED LIVE 2026-08-11** via apply_migration; self-checks green. Reversible per object (DROP), though reverting the vault change would re-embed the literal. |
| 257 | 257_reconciler_select_policies.sql | Migration 257 (reconciler SELECT policies — the three inert grants, 2026-08-11). Found by rls-credential-parity's FIRST correctly-scored run (data-audit lane run #66 plus the roles::text[] harness fix): the `reconciler` role — the bound credential migration 250 keeps as a sanctioned writer of provenance_status='verified' — held table-level SELECT grants on intelligence_items, intelligence_item_sections, and sources with NO covering SELECT policy on any of the three. On an RLS-enabled table a grant without a policy is INERT (RLS denies by default), so the credential could UPDATE intelligence_items (migration 169's policy) while being unable to READ the rows it reconciles or the sources/sections it validates against — the migration-169 class, three tables over. Adds three PERMISSIVE SELECT policies (USING (true), role-scoped), the same shape as the five existing reconciler policies; grants unchanged — this makes grants the operator already issued EFFECTIVE, it does not widen them. Pre-check ABORTs if the reconciler role is absent; post-check re-runs the audit's own predicate in-migration and ABORTs if any reconciler grant on an RLS table still lacks a covering policy. **APPLIED LIVE 2026-08-11** via apply_migration, post-check green. Reversible (DROP POLICY x3, restoring the inert-grant state). |
| 258 | 258_emission_factors_and_licence_gate.sql | Migration 258 (emission factors, the source licence register, and the licence gate as an enforced database object, 2026-08-12). Replaces the two hardcoded allowlists the F1-F5 foundation modules carried while their consumer tables did not exist (`src/lib/contracts/source-licence.mjs` licence verdicts, `src/lib/contracts/factor-tier.mjs` tier hierarchy; both F25 LEGACY_ALLOWLIST entries naming this migration as the landing point are deleted in the same commit). Design decision: emission factors do not share one key — UK DESNZ/US EPA publish by mode/vehicle class/fuel with no lane, EMSA THETIS-MRV aggregates to an operator, Clean Cargo aggregates to a carrier+trade lane, only carrier primary data is movement-specific — so the table carries `scope_kind` as a discriminator with a per-kind CHECK on which dimensions that kind requires vs forbids, rather than one corridor column that is NULL for most tiers or invented corridors on non-lane-specific factors. Ships structure + the licence register only; deliberately NO factor rows (a separate, independently-verified data unit) and NO `corridors` table/FK (corridor identity via content-addressed `cl_corridor_id()`, the entity model is a later spine unit). **COMMITTED, NOT YET APPLIED.** **AMENDMENT 2026-08-30 (Wave 4, WO-16):** the `data_source_seed` GENERATED block in this file was REGENERATED through `scripts/gen/migration-258.mjs` to carry a 27th register entry, `ec_weekly_oil_bulletin` (European Commission Weekly Oil Bulletin, DG ENER — CC BY 4.0 under Decision 2011/833/EU, verified against two primary sources 2026-08-30). This is the file's own documented change protocol ("committing the regenerated diff is how a register change ships"), not a hand-edit inside the markers. The single new seed row was applied live by INSERT ... ON CONFLICT DO UPDATE, NOT by re-running this migration: `data_sources` 26 → 27, `licence_clear_sources` 14 → 15, every other row untouched. The rest of migration 258 is unchanged and stays applied history. |
| 259 | 259_rls_initplan_and_policy_destack.sql | Migration 259 (RLS InitPlan rewrites + policy de-stack, 2026-08-12). Sourced to a 2026-08-12 Supabase performance advisor run: 143 `auth_rls_initplan` findings (policies calling `auth.uid()`/`auth.role()` unwrapped in USING/WITH CHECK, re-evaluated once per row instead of once per query) and 257 `multiple_permissive_policies` findings. Section 1 rewrites 27 policies across 8 tables (`intelligence_items`, `item_timelines`, `intelligence_summaries`, `sources`, `org_watchlist`, `workspace_item_overrides`, `user_item_state`, `user_list_order`) via `ALTER POLICY ... USING ((select auth.role()/auth.uid()) ...)`, wrapping the bare auth call in a subselect so Postgres plans it as an InitPlan evaluated once per query instead of once per row; semantics-preserving, byte-for-byte the same predicates otherwise. Section 2 de-stacks two of the `multiple_permissive_policies` pairs: drops the now-redundant `sources_reconciler_select` (superseded by `sources_read`'s unconditional-true policy) and narrows `intelligence_items_read` off `public` to `TO anon, authenticated` (reconciler and service_role keep full visibility via their own migration-250/257 policies and BYPASSRLS respectively; `dashboard_user` and any future unlisted role lose implicit access — a deliberate narrowing, RLS is deny-by-default). Post-check `DO $$` block asserts no unwrapped auth call remains on the 8 target tables, `sources_reconciler_select` is gone, and `intelligence_items_read` no longer targets `public`. Migration 262 completes the initplan sweep for the remaining 109 policies this migration didn't touch. **APPLIED 2026-08-12** — verified: the 8 tables' policies carry no unwrapped auth call, feeding the overall 143 -> 0 result completed by 262. |
| 260 | 260_fk_indexes_and_scanner_hygiene.sql | Migration 260 (FK indexes and scanner hygiene, 2026-08-12). Adds eight `CREATE INDEX CONCURRENTLY IF NOT EXISTS` indexes for foreign-key columns the 2026-08-12 Supabase performance advisor flagged as unindexed: `intelligence_items.agent_integrity_resolved_by`, `intelligence_items.replaced_by`, `org_watchlist.added_by_user_id`, `user_item_state.item_id`, `user_item_state.org_id`, `workspace_item_overrides.archived_by`, `workspace_item_overrides.owner_assigned_by`, `workspace_item_overrides.owner_user_id`. Uses CONCURRENTLY to avoid locking the referencing tables; per the file's header comment, CONCURRENTLY cannot run inside a transaction block, so this migration must be applied outside the normal `supabase db push` transactional wrapper (direct psql session or a non-transactional runner), not via the standard migration-push path. **COMMITTED, NOT YET APPLIED.** |
| 261 | 261_drop_dead_notification_v1.sql | Migration 261 (drop the dead notification-v1 trio, operator ruling 2026-08-12). Drops `notification_deliveries`, `notification_events`, and `notification_subscriptions` — the three tables migration 007_community_layer.sql created for a notification-v1 design that never shipped a working dispatch path. Evidence: zero rows ever in all three; no src/ code reads or writes any of them; their writer endpoint `/api/notifications/trigger` was already deleted as dead (2026-07-11, Wave-α Track E e5); `.discipline/governance/producer-consumer-orphan.mjs` (~lines 48-52) already names the trio "dead end-to-end (DB-4 F5)" and flags its own `notification_deliveries` allowlist entry as stale for the same reason, deferring the table drop to this migration. Explicitly NOT the same subsystem as the live in-app inbox — `notifications` (bell icon) plus its readers under `src/app/api/community/notifications/`, written by `src/lib/notifications/dispatch.ts` — despite `notification_subscriptions` sharing a name-prefix; `notification_subscriptions` came from 007 too and is equally orphaned (zero rows, zero readers), which is why it is included in this drop rather than kept. `notifications` has no FK to any of the three and is untouched. Pre-check `DO $$` block ABORTS if any of the three tables holds even one row (refuses to drop if reality drifted from the zero-rows audit). Drops in FK-safe order (`notification_deliveries` first, holding the FK to `notification_events`, then `notification_events`, then `notification_subscriptions`), each a bare `DROP TABLE IF EXISTS` with NO CASCADE so an unexpected dependent object fails the migration loudly instead of being silently destroyed. Post-check `DO $$` block asserts all three are gone from `information_schema.tables` and that `public.notifications` still exists. **COMMITTED, NOT YET APPLIED.** |
| 262 | 262_rls_initplan_sweep.sql | Migration 262 (complete the RLS InitPlan sweep, 2026-08-12). Finishes what 259 started. The 2026-08-12 Supabase performance advisor reported 143 `auth_rls_initplan` findings: RLS policies calling `auth.uid()` / `auth.role()` UNWRAPPED, which Postgres re-evaluates ONCE PER ROW rather than once per query, and which was the dominant driver of the 1.3-2.5s query times observed in production. Migration 259 fixed the 8 tables on the evidence trail of the slow pages, taking the count 143 -> 109; this migration rewrites the remaining 109 policies across 43 tables (mostly `community_*`, plus `org_invitations`, `moderation_reports`, `notification_preferences`, `profiles`). Implemented as a `DO $$` LOOP over `pg_policies` rather than 109 written-out `ALTER POLICY` statements, deliberately: hand-transcribing 109 security predicates is a typo waiting to happen, and a typo in an RLS predicate fails SILENTLY in the dangerous direction (a user sees nothing, or a user sees rows they should not). The loop reads each policy's LIVE definition and replaces only the two exact tokens, leaving every other character, including helper calls like `user_belongs_to_org()`, untouched. Semantics-preserving: wrapping changes WHEN the call is evaluated, not WHAT it returns. Emits `USING` only when `qual` is non-null and `WITH CHECK` only when `with_check` is non-null (an INSERT policy has no qual). Role lists are never touched. Re-running is a no-op because the WHERE clause excludes already-wrapped policies. Post-check `DO $$` block RAISEs if any unwrapped auth call survives, asserted against a fresh read so a cursor-visibility miss cannot pass as success. **APPLIED 2026-08-12** — verified: unwrapped policies 143 -> 0, and the `auth_rls_initplan` lint no longer appears in the performance advisor output at all. |
| 263 | 263_mode_vocabulary_ocean_canonical.sql | Migration 263 (canonical transport mode is `ocean`; duplicate mode CHECK removed, operator ruling 2026-08-12). Fixes TWO defects in 258. (1) WRONG CANONICAL TOKEN: 258 stored `sea`, chosen because ISO 14083, GLEC and Regulation (EU) 2026/1030 all say "maritime". Overruled: that confuses an OUTPUT concern with an INTERNAL one. This is a freight forwarding product and its users, rate boards, bookings and lane names say OCEAN FREIGHT. `sea` and `maritime` are retained as INPUT ALIASES in normaliseMode() and are never stored, because they arrive constantly from regulatory text and must resolve rather than be rejected. (2) TWO CHECK CONSTRAINTS ON ONE COLUMN: 258 carried both a hand-written inline `CHECK (mode IN (...))` (auto-named emission_factors_mode_check) and the codegen'd emission_factors_mode, because the inline copy was left behind when the vocabulary moved into the generated block. They agreed, so nothing failed, which is exactly why it would have survived: a duplicated definition is invisible until the copies disagree, and then the column silently accepts only their INTERSECTION. Had the token change shipped without this, `mode` would have accepted road/rail/air/inland_waterway and NOTHING ELSE, rejecting both `sea` and `ocean` while each constraint looked individually correct. Pre-check ABORTS if emission_factors holds any row (with rows this is a DATA migration needing an explicit UPDATE, not a constraint swap). Post-check asserts exactly one mode CHECK carrying `ocean` and not `sea`; it uses a WORD-BOUNDARY regex because the first draft used ILIKE '%mode%' and aborted on 'modelled' inside the derivation and origin_class constraints, rolling the whole migration back on a false alarm from its own post-check. **APPLIED 2026-08-12** — verified: one mode CHECK, canonical token ocean, and JS/SQL corridor-id parity re-confirmed (both mint cl:corridor:f5bf8ebf91e1298c for CNSHA->NLRTM ocean via Suez). |
| 264 | 264_rename_result_content_excerpt.sql | Migration 264 (rename `agent_run_searches.result_content_excerpt` -> `result_content`, 2026-08-17). The name asserted a truncation ADR-016 forbids: the column is not an excerpt, it is the FULL captured source content and the whole grounding pool (`validate_item_provenance` criterion 3 checks every FACT `source_span` verbatim against it; canonical-pipeline gates usability at >200 chars). The name was part of why a 2,000-char cap proposal looked reasonable enough to reach the operator. New name matches the table's vocabulary (`result_url`, `result_title`, `result_index`, `result_content`). Blast radius verified LIVE before authoring: exactly 1 function references the column (`validate_item_provenance`), 0 views, 0 matviews, 0 indexes, 0 constraints, 0 RLS policies; 2 functions call the gate (`set_provenance_status`, `gate_a_health_compute`) but neither names the column. THE HAZARD: Postgres stores function bodies as TEXT, so `ALTER TABLE ... RENAME COLUMN` neither rewrites them nor refuses — a bare rename leaves the gate referencing a missing column and fails only when next CALLED, inside the provenance gate. Rename + function rebuild therefore run in ONE transaction, and the gate is rebuilt from its OWN `pg_get_functiondef` via text substitution, never a hand-transcribed body (the migration-217 pattern). Anchor-verified (RAISEs if the gate no longer references the old name rather than guessing), postcondition-asserted (new name present, old name absent, old column gone), and ZERO-FLIP gated (verified-item count captured before and re-asserted after; a pure rename must not move a single item across the provenance line). Idempotent no-op if already applied. Reversible (`rollbacks/264_rename_result_content_excerpt_rollback.sql`, exact inverse with the same anchor discipline). Paired with a 51-reference sweep across 15 live-code files; the 17 historical migrations/rollbacks that mention the old name are the applied record and are deliberately NOT edited. |
| 265 | 265_drop_detect_intersections.sql | Migration 265 (drop the `detect_intersections` RPC — flywheel U3 supersession, 2026-08-17). Migration 023 created `detect_intersections(min_strength int, max_results int)`: a SQL function that RE-SCORED item pairs at read time from `operational_scenario_tags` / `compliance_object_tags` overlap plus `related_items` linkage (3 pts/scenario, 2 pts/compliance-object, 5 pts explicit-link, 2 pts both-high-priority). It predates the connection-discovery engine. Since migration 252 and the flywheel wave (U0-U4), `item_cross_references` carries the ONE scoring home's persisted output — `discover.mjs` scores with grounded basis, `write-edges.mjs` / `mint-item.ts` persist it — and the flywheel build plan (`docs/plans/flywheel-build-plan-2026-08-10.md`, U3) names this exact retirement. EVIDENCE: the function's only caller, `/api/admin/intersections`, is re-pointed in the same PR to read `item_cross_references` and assemble pairs via `src/lib/connections/pair-view.mjs`; grep over `src/` and `scripts/` finds no other `.rpc("detect_intersections")` call. ORDERING (two-track policy, CLAUDE.md standing rule 3): this is a DROP whose safety depends on the CONSUMER change, not schema the code depends on — the reverse of the DDL-first case — so it applies AFTER the re-pointed route merges and no deploy window ever runs the old route against a missing function. Migration 023's historical file and migration 160's `search_path` pin are untouched history (the ALTER dies with the function); `intelligence_items.intersection_summary` is NOT part of the RPC's scoring and stays. **APPLIED 2026-08-18** — ordering honoured: PR #467 merged (`93611d4c`) and BOTH production Vercel deploys of that commit reported success BEFORE the DROP ran, so no window existed in which the deployed old route could meet a missing function (the migration header says "after the re-pointed route merges"; merge alone is not sufficient — the deploy is what removes the caller from production, and that distinction was checked, not assumed). Verified BY EXECUTION, not presence, on both sides: (1) pre-drop baseline `SELECT p.proname, pg_get_function_identity_arguments(p.oid) FROM pg_proc p JOIN pg_namespace n ON n.oid=p.pronamespace WHERE n.nspname='public' AND p.proname='detect_intersections'` returned exactly ONE row with identity args `min_strength integer, max_results integer` — matching the DROP's signature, which is what makes the post-check non-vacuous; (2) the same query post-drop returns ZERO rows. `to_regclass` is deliberately NOT used — it resolves relations only and returns NULL for a function that still exists, so it cannot distinguish "dropped" from "never visible to it" and would pass vacuously here. (3) SURFACE PROOF (the real test that the reader no longer depends on the function): /admin -> Sources -> Intersections loaded against production after the DROP and rendered `Total pairs 200 · Strong (>=0.9) 200 · Medium 0 · Weak 0 · Explicitly linked 0`, with per-pair scores (`1.00`) and grounded basis chips (`Basis (4)` / `Basis (6)`: "grounded in the same source", "both touch ocean-bunkering", "both touch ocean-fuel-blend-mandate", …) plus the engine-weights line; no error state, and a console read filtered for `error\|failed\|500\|404\|PGRST\|42883` over a full post-drop page load returned nothing. RESIDUAL, recorded not silently dropped: `fsi-app/supabase/seed/verify-intersections.mjs` still calls `.rpc("detect_intersections")` and is now dead — the migration's own evidence line scoped its grep to `src/` and `scripts/`, and this file is under `supabase/seed/`, outside that scope. It is NOT execution-wired (no lane, no npm script, no CI reference — only its own usage comments and a historical audit register mention it), so the DROP turns no lane red; it is retired in the same PR as this status update rather than left as a script that fails on first use. SECOND RESIDUAL, found while checking the first and materially worse: `pair-view.test.mjs` — shipped by #467 as the ONLY proof of the replacement reader — was git-tracked and executed by NOTHING. The npm-dep lane in `.github/workflows/discipline.yml` globs `fsi-app/src/**/*.npmtest.mjs` and names a fixed list; a `.test.mjs` under `src/` matches neither, and the file is referenced nowhere in `.github/` or `.discipline/`. #467's CI was green because the test never ran, not because it passed — the exact "execution over existence" class of standing rule 15, arriving on the same PR that removed the old scoring home. Fixed by renaming to `pair-view.npmtest.mjs`, which is the house wiring mechanism (the workflow's own comment says naming is what wires a proof, and 19 sibling proofs already use that suffix) — no new machinery, no identity change. Now confirmed matched by `git ls-files 'fsi-app/src/**/*.npmtest.mjs'` and passing 8/8 under the new name. |
| 266 | 266_theme_briefs.sql | Migration 266 (`theme_briefs` — F5 theme-brief storage, flywheel U6, operator-ruled 2026-08-21). One row per connection theme: `theme_id` uuid PK (NO FK — `connection_themes` is a cache the U2 pass REPLACES WHOLESALE, and theme ids are the lexicographically smallest member id, stable under re-clustering; an FK would make every re-cluster destroy durable editorial content), `member_hash` (md5 of the theme's sorted member_ids at generation time), `member_count`, `title`, `brief_md`, `generated_at` default now(), `generated_by` default 'session-executor'. WHY A TABLE, NOT A COLUMN: a brief column on connection_themes would be destroyed by every guardedDelete-all re-cluster; briefs are durable content keyed to theme identity and must SURVIVE re-clustering. STALENESS DETECTED, NEVER SILENT: the read path (admin themes route) recomputes the live membership hash via `src/lib/connections/brief-staleness.mjs` (the ONE home for the hash rule, F25-wired); mismatch renders a STALE badge, never silently-wrong current content; a brief whose theme id vanishes is orphaned and hidden by the join, kept as history. RLS ENABLED WITH NO POLICIES = deny-all to anon/authenticated; service role bypasses by construction (writes are operator-directed session-executor passes only — no trigger, no cron, $0 standing directive). **APPLIED LIVE 2026-08-21** via delegated DDL (additive, no customer read path) — verified post-write: 9 rows, every row hash-fresh against live connection_themes, `brief_md` 970–5,156 chars, all payload-md5-checksummed writes. Reversible (`DROP TABLE public.theme_briefs`). |
| 267 | 267_origin_class_and_envelope.sql | Migration 267 (WO-19 `origin_class` + WO-12 number envelope, 2026-08-29). Extends the SAME two vocabularies migration 258 shipped on `emission_factors` outward, never a second enum: `origin_class` (7 values, owned by `src/lib/contracts/vocabularies.mjs` ORIGIN_CLASS) added to `intelligence_items` and `state_cost_facts`; the full envelope (value_numeric, unit, currency, derivation +CHECK, origin_class +CHECK, source_key FK->data_sources, source_ref, n_observations +positive-CHECK, method_version, as_at_date, reference_period) added to `regional_data_facts`. ALL NULLABLE, ALL ADDITIVE, ZERO backfill in-migration — nullable/backfill/NOT NULL are three separately-reviewed steps. DDL is GENERATED by `src/lib/contracts/provenance-envelope.mjs` via `scripts/gen/migration-267-origin-class-and-envelope.mjs` (the migration-258 codegen pattern); an anti-drift test asserts the emitted origin_class CHECK is BYTE-IDENTICAL to what 258 already contains, so the two can never diverge. CORRECTION to master-plan C1/C2, found by reading the contracts modules end to end: `factor-tier.mjs` does NOT own origin_class or derivation — it imports them from `vocabularies.mjs` and `envelope.mjs` respectively; provenance-envelope.mjs imports the same real homes and re-exports the same array references (asserted by strictEqual). **APPLIED LIVE 2026-08-29** — post-apply verified: intelligence_items.origin_class 1 col, regional_data_facts 11 cols, state_cost_facts.origin_class 1 col, 4 origin_class CHECKs live (258's + 3 new), 0 rows backfilled by the migration itself. WO-19 backfill ran as its own guarded pass immediately after (241/274 verified-live items stamped, 33 left NULL as documented pre-vocabulary; mapping in `docs/plans/wo19-origin-class-backfill-mapping.md`). Reversible (`rollbacks/267_origin_class_and_envelope_rollback.sql`; legacy value/unit/source_id columns untouched by both). |
| 268 | 268_market_series.sql | Migration 268 (`market_series` — WO-16 time-series spine, 2026-08-30). New table keyed `UNIQUE(series_key, reference_period)`: `id` uuid PK, `series_key` text NOT NULL (format-CHECKed slug, e.g. `eu-oil-bulletin:automotive-diesel`), `label` text NOT NULL, `created_at`/`updated_at`, plus the full envelope (`value_numeric, unit, currency, derivation +CHECK, origin_class +CHECK, source_key FK->data_sources, source_ref, n_observations +positive-CHECK, method_version, as_at_date, reference_period`) — 16 columns total. DDL is GENERATED by `src/lib/contracts/provenance-envelope.mjs` via `scripts/gen/migration-268-market-series.mjs` (the migration-258/267 codegen pattern); an anti-drift test (`src/__tests__/contracts-market-series-migration.test.mjs`) asserts the emitted origin_class/derivation CHECKs are BYTE-IDENTICAL to 258's. ADDITIVE ONLY — no other table touched (asserted by the same test). RLS enabled, read-only to authenticated, no write policy (service-role/guarded-path only). WO-16.2 ruling (option a, held): `published_price_statistics` stays the item-page display cache PriceBoard reads and is FED from this table by a separate refresher (`src/lib/market/refresh-published-price-statistics.mjs`), not replaced — verified unchanged by reading `fsi-app/src/app/market/[slug]/page.tsx:149-168` and `MarketSignalDetailSurface.tsx`'s `PriceBoard`. **APPLIED LIVE 2026-08-30** via the sanctioned lane (two-track policy, CLAUDE.md standing rule 3) BEFORE this PR merges, so the producer code never references a table that does not exist. Post-apply verified by execution, not presence: 16 columns, exactly 1 UNIQUE constraint, 4 CHECK constraints, 0 rows; then FOUR live controls run in one DO block and all passed — (a) `origin_class='not-a-real-value'` REJECTED with check_violation, (b) `n_observations=0` REJECTED with check_violation, (c) `source_key='no-such-source'` REJECTED with foreign_key_violation, (d) `source_key='ec_weekly_oil_bulletin'` ACCEPTED (positive control, row deleted immediately); table back to 0 rows after. The negative controls matter more than the column count: a CHECK that exists but does not fire is the defect class this repo has hit before, so presence was not treated as proof. SOURCE-REGISTRATION GAP CLOSED IN THE SAME PASS: the WO-16 lane correctly reported that `source_key='ec_weekly_oil_bulletin'` was unregistered and would fail the FK closed (23503), leaving a producer that could never run. Rather than land that, the coordinator verified the licence against TWO primary sources on 2026-08-30 — the Weekly Oil Bulletin page carries NO dataset-specific copyright notice (so the Commission legal notice's 'individual copyright notice' carve-out does not apply), and that legal notice licenses Commission-owned content CC BY 4.0 under Decision 2011/833/EU with reuse allowed 'provided appropriate credit is given and changes are indicated' — added the register entry to `src/lib/contracts/source-licence.mjs`, REGENERATED migration 258's `data_source_seed` block through `scripts/gen/migration-258.mjs` (the sanctioned flow that file's own header names), and applied the one seed row live: `data_sources` 26 → 27, `licence_clear_sources` 14 → 15. UNCONFIRMED and recorded rather than assumed away: whether any individual Member State submission inside the bulletin carries separate upstream rights; no such notice appears on the publication and the Commission publishes it as its own document, so the default applies, and if one ever surfaces the entry drops to `conditional` and the gate closes automatically. The producer itself (EU Weekly Oil Bulletin, `scripts/producers/market/eu-weekly-oil-bulletin.mjs`) remains kill-switched OFF (`MARKET_PRODUCER_EU_OIL_BULLETIN_ENABLED` unset) — a separate gate from the licence one, deliberately not opened here. Reversible (`DROP TABLE public.market_series`). |
| 269 | 269_routing_rpcs_use_surface_of.sql | Migration 269 (the three category-routing RPCs stop carrying their own copy of the surface predicate, 2026-08-30). `surface_of(p_item_type, p_domain)` (migration 148) is the DATABASE half of the ONE home for "which surface does this item belong to" — generated from `SURFACE_RULES` in `src/lib/surface-of.mjs` by `renderSurfaceOfSql()`, with the vocab-drift guard asserting the migration contains exactly that text. `get_surface_counts` uses it; the three ROUTING RPCs did not — each carried a hand-written `ii.item_type IN (...)` list, a third, fourth and fifth copy of a predicate that already had a single home. THE COPIES HAD DRIFTED, and the drift was customer-visible on all three surfaces. Measured live before the change, over verified non-archived items, hardcoded list vs `surface_of`: **research 31 → 38** (+7 under-routed), **market 56 → 48** (−8 net; 12 leave, 4 arrive), **operations 21 → 24** (+3 under-routed). The Research undercount is what surfaced it: `/research` rendered "38 findings" in its masthead (`get_surface_counts`, correct) above ~31 cards (`get_research_items`, wrong) — two disagreeing totals on one screen, an 18% gap. Fixing the page's own fetcher was NOT sufficient: `research/page.tsx` intersects its rows against this RPC's id set, so the RPC was the binding constraint. The WO-15 lane found this, proved it live, and correctly refused to fix it out of scope. NOTHING IS LOST — every re-routed item MOVES, none disappear; the 12 leaving Market were enumerated before the migration was written, not assumed: 4 `initiative` domain 7 → research, 3 `market_signal` domain 7 → research, 3 `initiative` domain 3 → operations, 1 `initiative` domain 1 → regulations, 1 `market_signal` domain 1 → regulations. The two moving to regulations are ADR-020's regulation precedence doing exactly what it was decided to do: a domain-1 item is a regulation first, whatever its `item_type` says. Market shrinking is a CORRECTION, not a regression — those items were being shown on a surface the platform's own decided predicate says they do not belong to. WHY THE FIX LIVES IN THE RPC AND NOT THE PAGE: dropping the page-level intersection would have been the smaller diff and would have BYPASSED `_workspace_active_items(p_org_id)` / `_assert_org_membership` org scoping. Every other line of all three functions is byte-identical to the live definition read from `pg_get_functiondef()` immediately before the file was written — same RETURNS TABLE lists, same SECURITY DEFINER, same `SET search_path`, same joins, same ORDER BY. ONLY the WHERE predicate changed. **APPLIED LIVE 2026-08-30** — post-apply verified per function by inspecting `pg_get_functiondef()`: `uses_one_home` true, `still_hardcoded` false, `org_scoping_intact` true, `secdef_intact` true, `searchpath_pinned` true, for all three. Reversible (re-run migration 125's bodies and the market RPC's original body with their hardcoded `item_type` lists). |
| 270 | 270_widen_org_watchlist_market_series.sql | Migration 270 (WO-23 — `org_watchlist.item_type` admits `market_series`, 2026-08-30). Widens `org_watchlist_item_type_check` from `ARRAY['source','reg','signal','research','operations']` to the same five plus `market_series`. **TEAM SCOPE ONLY: `user_watchlist_item_type_check` is deliberately NOT widened**, and that is now the strictly safer state rather than merely the specced one — the master plan's premise was that both watchlist tables were empty, and `user_watchlist` has since gained a live row (`org_watchlist` is still 0). THERE IS NO 'DDL WINDOW' HERE, and the phrase was retired from the plan on measurement: `org_watchlist` is 0 rows, so `ADD CONSTRAINT` runs no validation scan and holds ACCESS EXCLUSIVE for microseconds against zero tuples; a window implies a scan or a rewrite and neither occurs. **APPLIED LIVE 2026-08-30** by the coordinator under two-track policy (CLAUDE.md standing rule 3), BEFORE the dependent code merged. Post-apply verified by reading `pg_get_constraintdef()` on BOTH tables, not just the changed one — the negative half is the half that matters here: `org_watchlist_item_type_check` → six values including `market_series`; `user_watchlist_item_type_check` → five values, unchanged. THE CODE HALF IS FOUR FILES, NOT THE ONE 'gains a value' implies, and the master plan's '5 readers' checklist named none of them: (1) `api/watchlist/route.ts`'s `ITEM_TYPES` Set is SHARED across personal and team scope with no conditional — widening it flatly would let a personal `market_series` write reach the un-widened `user_watchlist` CHECK and surface as a raw Postgres 500 instead of the route's own clean 400, so a scope-conditional branch (`TEAM_ONLY_TYPES` / `isTeamOnlyScopeViolation`, since moved to `src/lib/watchlist-scope.ts` so the client button and the server route share ONE home) was required; (2) `supabase-server.ts`'s `WatchlistItemType` union plus the exhaustive `SOURCE_FALLBACK` record; (3) `fetchWatchlist`'s resolution branch, which fell through to a bare `type: "signal"` literal and would have SILENTLY MISLABELLED every watched series as a Signal — the exact defect that file's own doc comment records happening once before, which is why a real `market_series` branch resolving by `id` against the table was written rather than an addition to `ITEM_BACKED_TYPES`; (4) `watchlist-links.ts`'s exhaustive `WATCHLIST_TYPE_LABEL` record and `watchlistHref` switch, where `market_series` returns `null` like `source` because no per-series detail route exists — an honest null, not an invented route that 404s. AND THE MOUNT, which the first pass left out and the operator correctly refused to accept: a type watchable in the database and unwatchable in the product is broken, not deferred. `WatchButton` carried a hardcoded 5-value COPY of `WatchlistItemType` that had already drifted; it now `import type`s the real home (erased at compile time, so no server code enters the client bundle) and renders TEAM-ONLY for team-only types, because a control offering an action the API will reject is itself a defect. `MarketSeriesBoard` threads `market_series.id` through `buildSeriesBoard` and mounts a per-series control. Reversible EXACTLY: re-run this file with `'market_series'` removed from the ARRAY — while the table holds no such row, the narrow form is byte-for-byte the prior constraint. |
| 271 | 271_assumption_register.sql | Migration 271 (`assumption_register` — WO-20 assumption register, docs/plans/wo20-assumption-register-spec.md, 2026-08-30). New table, natural key `assumption_key text UNIQUE NOT NULL` (dot-namespaced `<subsystem>.<mechanism>.<parameter>`, e.g. `connections-scorer.weight.shared_source`) — the register for modelling constants THIS PRODUCT chose (a connection-scorer weight, an idf coefficient, a score floor, a bias-tag confidence cutoff, an urgency-score mapping, a pedigree floor), distinct from `emission_factors` (WO-12/18), which registers numbers the WORLD published. Hand-written columns (11): `id` uuid PK, `assumption_key`, `subsystem` (denormalized first key-segment, free text v1 — spec §7 Q2), `label`, `rationale`, `code_location` (the drift-detectable file:line pointer), `governing_decision` (ADR id or NULL — NULL is an honest "no ratified decision governs this value today", never a placeholder), `status` (+CHECK active/superseded/retired, default active), `superseded_by` (self-referential FK, append-only-supersession posture), `created_at`/`updated_at`. Plus the envelope, NARROWED (spec §3): `value_numeric, unit, derivation +CHECK, origin_class +CHECK, source_key FK->data_sources, source_ref, n_observations +positive-CHECK, method_version, as_at_date` — 9 of the full 11-column envelope, `currency` and `reference_period` DELIBERATELY EXCLUDED (none of the 10 catalogued rows are a monetary rate or a period aggregate) — 20 columns total. DDL is GENERATED by `src/lib/contracts/provenance-envelope.mjs` via `scripts/gen/migration-271-assumption-register.mjs` (the migration-258/267/268 codegen pattern, adapted to a narrowed `columns` list); an anti-drift test (`src/__tests__/contracts-assumption-register-migration.test.mjs`) asserts the emitted origin_class/derivation CHECKs are BYTE-IDENTICAL to 258's, that `currency`/`reference_period` are absent from the DDL, and that `assumption_key` is the SOLE UNIQUE constraint. ADDITIVE ONLY — no other table touched (asserted by the same test). RLS enabled, read-only to authenticated, no write policy (service-role/guarded-path only, same posture as 258/268). SCHEMA-ONLY, ZERO seed rows in this migration — the 10-row backfill (spec §5 step 4, one row per §2 catalogued constant, every `code_location` re-verified this session against the live file, no correction needed on any of the 10) is a separate, later, guarded-path pass (`scripts/gen/assumption-register-seed.mjs` + fixture `scripts/gen/fixtures/assumption-register/wo20-catalogued-assumptions-2026-08-30.json`), not run by this migration and NOT RUN THIS SESSION (dry-run only — no DB access, no service-role credentials; a `--apply` run against a pre-migration database fails loudly on the missing table by design). NUMBERING CORRECTION against the spec, named in the generator's own header: spec §5 names this migration "269"; 269 and 270 landed as unrelated migrations (`269_routing_rpcs_use_surface_of`, `270_widen_org_watchlist_market_series`) the same day between the spec being written and this file being generated, so 271 is the real next-free number — the spec's DDL content (§3) is unaffected, only the file number moved. **NOT YET APPLIED — written by an executor lane (Sonnet, wave18/la) and left for the coordinator, per two-track policy (CLAUDE.md standing rule 3) and this WO's own §5 step 5 ("applied by the coordinator only").** Reversible: `DROP TABLE public.assumption_register` (schema-only, no dependent object exists yet — spec §4's admin-panel reader and §4's drift-check script are both explicitly named as NOT built by this WO). |
| 272 | 272_customer_rpcs_project_jurisdiction_iso.sql | Migration 272 (the eight customer-facing RPCs start projecting `ii.jurisdiction_iso`, 2026-08-30). Lane `lb` (Addendum 63) found that `Resource.jurisdictionIso` could never populate on any list/ledger surface: none of the eight customer-facing RPCs (`get_workspace_intelligence`, `get_workspace_intelligence_slim`, `get_workspace_intelligence_dashboard`, `get_workspace_intelligence_listings`, `get_market_intel_items`, `get_research_items`, `get_operations_items`, `get_technology_items`) project `jurisdiction_iso` in their live `RETURNS TABLE`/`SELECT`, even though the column is a TEXT ARRAY on `intelligence_items` (migration 033) and is already carried by `_workspace_active_items` (migration 077/117), the shared scope function six of the eight source from — it was projected away by every customer-facing wrapper, not absent from the data. Lane `lb` shipped the TS half as a dormant passthrough (`normalizeJurisdictionIsoColumn`, `src/lib/jurisdictions/iso.ts`, wired into the three `Resource`-mapper sites in `supabase-server.ts`) and left this migration as a decision-ready spec; this is the RPC half. BODIES SOURCED FROM (highest-numbered migration defining each on disk, verified by `git grep -n "CREATE OR REPLACE FUNCTION public.<name>"` across every migration file — no `pg_get_functiondef`, no DB access this lane): `get_workspace_intelligence`/`get_workspace_intelligence_slim` ← 120; `get_workspace_intelligence_dashboard`/`get_workspace_intelligence_listings` ← 077; `get_research_items`/`get_operations_items`/`get_market_intel_items` ← 269; `get_technology_items` ← **134, not 269** — 269's own text contains exactly three `CREATE OR REPLACE FUNCTION` statements (research/operations/market) and `get_technology_items` is not one of them, a correction against the brief that opened this migration, which had grouped all four routing RPCs under "269." DISCIPLINE (269's own precedent): every other line of each function is byte-identical to its source migration — same RETURNS TABLE order otherwise, same LANGUAGE/SECURITY DEFINER/`SET search_path` (preserved exactly as each source had it, including 120's/077's omission of an explicit `search_path` clause — not normalized here), same joins, WHERE, ORDER BY, org-scoping. ONLY `jurisdiction_iso text[]` is appended to the END of each RETURNS TABLE and SELECT list — verified programmatically this session (extract each function body from both the new file and its cited source, diff after stripping the one added RETURNS-TABLE token and the one added SELECT expression: all eight reduce to a zero-diff match). SAFE TO APPEND: `src/lib/supabase-server.ts` calls all eight via `supabase.rpc(name, ...)` and every consumer reads the PostgREST JSON-object result by column NAME (`row.jurisdiction_iso`), never by position — confirmed by reading the three existing mapper call sites. Two RPCs (`get_workspace_intelligence`/`_slim`, `get_market_intel_items`) read `intelligence_items ii` directly, so `ii.jurisdiction_iso` is a plain column reference; the other six read `_workspace_active_items(p_org_id) ii` (or `get_research_items`/`get_operations_items`/`get_technology_items`'s own alias of the same), which already SELECTs `ii.jurisdiction_iso` into its own output (migration 117) — passthrough, no new join in any of the eight. `_workspace_active_items` itself is UNCHANGED. **NOT APPLIED — DDL is coordinator-only (CLAUDE.md standing rule 3); written by an executor lane (Sonnet, wave18/ld) and left for the coordinator.** No rollback file: no prior migration in this function's own lineage that only redefines an existing SECURITY DEFINER function via `CREATE OR REPLACE` (071, 073, 077, 117, 120, 125, 133, 134, 148, 269) has ever shipped a `_rollback.sql` — `CREATE OR REPLACE FUNCTION` is its own reversal once the prior body is known, and every prior body this migration touches is reproduced verbatim in the source migration it cites. Reversible: re-run each named source migration's body (120 for the base/slim pair, 077 for dashboard/listings, 269 for research/operations/market, 134 for technology). |
| 273 | 273_coverage_gap_candidates_live_ddl_catchup.sql | Migration 273 (`coverage_gap_candidates` — record the five live-only columns migration 214 never created, audit 2026-08-31). Migration 214 (2026-07-17) created `public.coverage_gap_candidates` with 16 columns; migration 223 (2026-07-25, itself a retroactive catch-up for the LIVE view `public.acquisition_backlog_v`, schema-drift audit RD-49) projects five MORE columns off this table in its `SELECT` list — `data_class`, `discovery_class`, `disposition`, `surface_test`, `access_model` — that no migration file anywhere creates: they were added to the live table out-of-band, after 214 and before 223's 2026-07-25 capture, so 223 is correct against the LIVE table but a clean `supabase db reset` replay breaks at 223's `CREATE VIEW` referencing columns the replayed table does not have. Found by the 2026-08-31 migrations-reality audit (C3 gate). Adds the five columns (`ADD COLUMN IF NOT EXISTS`, exact live types/defaults/`NOT NULL` per the coordinator's `information_schema.columns` + `pg_get_constraintdef` dump) and their five named `CHECK` constraints (`coverage_gap_candidates_data_class_check`, `_discovery_class_check`, `_disposition_check`, `_access_model_check`, `_surface_test_required_check`; each `ADD CONSTRAINT` guarded by a `DO` block testing `pg_constraint` by `conname`, definitions copied verbatim from the live dump, not re-derived). **NO-OP ON THE LIVE DATABASE BY CONSTRUCTION** — every guard is false there since all five columns/constraints already exist under these exact names, so this migration executes zero DDL live; on a clean replay every guard is true and it constructs exactly what 214 was missing, closing the RD-49-class gap 223 opened. Post-check `DO` block re-asserts both counts (5 columns, 5 constraints) and `RAISE EXCEPTION`s otherwise. Wrapped `BEGIN`/`COMMIT`. Does NOT touch `validate_item_provenance` (coordinator lands that function's canonical body separately) and does not touch `acquisition_backlog_v` itself (223 already carries the correct view definition; this migration only supplies the table structure 223 assumes). Reversible: `ALTER TABLE public.coverage_gap_candidates DROP COLUMN data_class, DROP COLUMN discovery_class, DROP COLUMN disposition, DROP COLUMN surface_test, DROP COLUMN access_model` (constraints drop with their columns) — safe only if 223's view is dropped first, since the view depends on all five. |
| 274 | 274_item_forward_events.sql | Migration 274 (`item_forward_events` — the forward-events harness's extraction target, 2026-09-01). **WHY:** the corpus was never missing forward-looking dated obligations, it was missing their EXTRACTION into anything queryable — 179 of 322 live verified briefs name a future year and 1,143 grounded FACT/GAP claims do, while only 19 items carried `compliance_deadline`, 58 `entry_into_force`, and 0 `next_review_date` (a column with no prompt, parser or pipeline writer anywhere in the codebase). EU Aviation ETS renders a section reading "**Deadline:** Before 1 January 2026" against a null deadline column. **SHAPE:** one row per dated, obligation-bound event (an item's phase-out schedule is several rows, which the single-date columns structurally cannot hold); 13 columns — `intelligence_item_id` FK CASCADE, `event_date`, `date_precision` (day/month/year, so a bare-year row's normalized 1 January is never read as a source-stated day), `event_kind` (entry_into_force/compliance_deadline/review_or_report/phase_step/consultation_close/other), `obligation_text`, `source_kind` (claim/section), `source_claim_id` FK SET NULL, `source_section_id` FK CASCADE, `source_span` (verbatim), `confidence` (high/medium), `extractor_version`, `created_at`. **THE GROUNDING RULES ARE CONSTRAINTS, NOT CONVENTIONS:** `CHECK ((source_kind='claim') = (source_claim_id IS NOT NULL))` and `CHECK (confidence <> 'high' OR source_kind='claim')` — `high` confidence is structurally unreachable from a section, so the writer cannot drift from the rule. Vocabulary CHECKs are exactly the literal set `scripts/forward-events/extract-forward-events.mjs` can emit (verified by grepping every `kind:` literal in the extractor), not a speculative superset. Two lookup indexes (`event_date` for the what-is-due-next read, `intelligence_item_id`). RLS enabled, SELECT policy mirroring migration 103 (`intelligence_item_sections`) — customer-visible content posture, not `item_gate_a_state`'s service-role-only posture, chosen because forward events are content rather than pipeline QA state. Self-verifying `DO` post-check asserts 13 columns / 7 CHECKs / 1 UNIQUE / 2 indexes / RLS on / 0 rows. **APPLIED LIVE 2026-09-01** by the coordinator (two-track policy, CLAUDE.md standing rule 3), schema-only; the 901-row load followed as a separate guarded pass. Its original UNIQUE dedupe key was superseded by 275 before any row was written — see that row. Reversible: `DROP TABLE public.item_forward_events` (no dependent object; no reader shipped in this wave). |
| 276 | 276_connection_theme_runs_theme_delta.sql | Migration 276 (`connection_theme_runs.theme_delta` — a real column for the theme-set diff, replacing the `args.theme_delta` side-pocket, 2026-09-01). `analyze-corpus.mjs`'s F6 unit (`theme-delta.mjs`, `diffThemes`) computes a structured persisted/renamed/split/merged/dissolved/appeared digest before every run's guardedDelete-all replaces `connection_themes` (migration 253: "not append-only"); the digest previously rode inside the already-JSONB `args` column (`args: { dry, signals, theme_delta }`), which migration 253's own `COMMENT ON COLUMN` documents as holding only "the CLI args the pass ran with" — a computed RESULT does not belong there. Adds `theme_delta JSONB`, nullable, no default, no CHECK, no backfill of historical rows (their digest stays inside their own `args.theme_delta`; only new rows write to the new column going forward). `connection_theme_runs` is service-role-write-only / public-read (migration 253); a nullable additive column changes neither RLS policy. Authored by lane FIX (integration), left unapplied at authoring time — the migration's own header states the coordinator applies it before the write-site change (riding the same commit) reaches a live pipeline invocation. **APPLIED to the live DB 2026-09-01** (`docs/ops/session-log.md`, Addendum 80: "Migration 276 ... applied to live DB"; also "Migration 276 applied" in the same addendum's operational-turn note). No rollback file shipped (unconditionally safe one noted in the migration's own header: `ALTER TABLE public.connection_theme_runs DROP COLUMN IF EXISTS theme_delta;`). |
| 275 | 275_item_forward_events_dedupe_key_fix.sql | Migration 275 (replace 274's dedupe key, which would have silently discarded 54% of the first real run, 2026-09-01). 274 shipped `UNIQUE (intelligence_item_id, event_date, event_kind, source_span)`, which looked correct against FE-1's 24-item fixture where `source_span` was usually a full date phrase ("1 January 2026"). The first full-corpus run (`forward-events-run-001`, 322 live verified items, 902 events) showed the real distribution: **382 of 902 spans are a BARE YEAR** ("2030") because that is all the source text contains, so every distinct obligation in one item sharing a year and a kind collapsed to a single row — **489 of 902 events (54%) would have vanished** through `ON CONFLICT DO NOTHING` with no error and no log line. Two of the colliding rows, verbatim, are "scope of this Regulation should have the aim of including, by 2030, all the sectors covered by Directive 2003/87/EC" and "By 2030 | Commission target to include all EU ETS sectors in scope" — same date, same kind, different obligations, different source objects. **THE KEY WAS CHOSEN BY MEASUREMENT, NOT INTUITION** — three candidates counted against the real 902-event run before this file was written: `(item,date,kind,source_span)` keeps 413; `(item,date,kind,md5(obligation_text))` keeps 799; `(item,date,kind,md5(obligation_text),coalesce(source_claim_id,source_section_id))` keeps 901. The third is adopted; its single remaining drop is a genuine exact duplicate, which is what a dedupe key should collapse. Implemented as a UNIQUE INDEX on an expression rather than a materialized hash column, because `md5(obligation_text)` is a pure function of a stored column and a second home for the same fact is a drift surface; `coalesce(source_claim_id, source_section_id)` is exactly one non-null value per row, already guaranteed by 274's own claim-id/source-kind CHECK. Idempotency is preserved: the extractor is pure and deterministic, so a re-run over an unchanged slice reproduces byte-identical `obligation_text` and the same source ids, and therefore the same key — re-running still cannot duplicate, it can now only fail to collapse rows that were never the same event. **APPLIED LIVE 2026-09-01** by the coordinator, against an empty table (274 shipped schema-only and asserts 0 rows; the load ran after this), so the drop-and-recreate could not fail on existing data and rewrote nothing. Reversible: drop `uq_item_forward_events_dedupe` and re-add 274's `item_forward_events_dedupe_key` constraint — but doing so re-opens the 54% collapse, so the reversal is only safe on an empty table. |
| 277 | 277_corpus_turn_requests.sql | Migration 277 (`corpus_turn_requests` — a mechanical "this item needs a flywheel turn" queue, lane EV, 2026-09-01). **WHY:** rule 16 wires connection discovery + forward-event extraction into the two in-app write chokepoints (`mint-item.ts` INSERT, `apply-staged-update.ts` UPDATE), but every OTHER way an `intelligence_items` row changes — coordinator SQL applies, an un-archive, a `set_provenance_status`-trigger provenance flip cascading from a claims/sections write, an operator tag correction — left no record that a turn was ever needed. **SHAPE:** 6 columns — `intelligence_item_id` FK CASCADE, `reason` (inserted/verified/unarchived/updated/tags_applied/manual, CHECK-enforced; `manual` is reserved for the admin "Request corpus turn" action, same commit), `requested_at`, `consumed_at`/`consumed_by` (a second CHECK enforces they are NULL/non-NULL as a pair). A UNIQUE PARTIAL INDEX on `(intelligence_item_id) WHERE consumed_at IS NULL` enforces one open request per item at the DB layer. **THE TRIGGER:** `enqueue_corpus_turn_request()`, `AFTER INSERT OR UPDATE OF (provenance_status, is_archived, operational_scenario_tags, compliance_object_tags, topic_tags) ON intelligence_items`; skips when the resulting row is archived or not `verified` (nothing for the flywheel to act on); reason derived by comparing OLD vs NEW on the changed column, in priority order INSERT > is_archived-flip > provenance_status-flip > tag-column-change > generic `updated`; inserts via `ON CONFLICT (intelligence_item_id) WHERE consumed_at IS NULL DO NOTHING` (the documented partial-unique conflict-target syntax). **DELIBERATELY CARRIES NO `pg_trigger_depth()` GUARD**, unlike migration 115/209's own `intelligence_items` trigger — read and considered, not skipped: 115's guard exists because ITS action recurses into the same table/trigger; this trigger's action (an INSERT into a different, freshly-created table) cannot recurse, and a depth-restricted version would silently miss the exact nested-cascade case (a claims/sections write flipping `provenance_status` through `set_provenance_status()`'s own internal UPDATE, which fires at depth ≥ 1) that is half of this migration's reason to exist; the migration's header states this reasoning in full. Repeated/nested firing for the same item is absorbed harmlessly by the ON CONFLICT DO NOTHING against the partial index. RLS enabled, mirrors migration 195 (`error_events`): no INSERT/UPDATE/DELETE policy (every current `intelligence_items` writer uses a service-role client, verified against all 15 writers in `docs/inventories/shared-dataset-ownership.md`'s allowlist), one `profiles.is_platform_admin`-gated SELECT policy. Self-verifying `DO` post-check asserts 6 columns / 2 CHECKs / 1 partial-unique index / 1 lookup index / RLS on / trigger attached / 0 rows. Consumed by `scripts/turns/consume-turn-requests.mjs` (same commit) and, going forward, another lane's GitHub Actions corpus-turn workflow. **NOT YET APPLIED** — coordinator applies (two-track policy, CLAUDE.md standing rule 3). Reversible: `DROP TRIGGER enqueue_corpus_turn_request_trg ON public.intelligence_items; DROP FUNCTION public.enqueue_corpus_turn_request(); DROP TABLE public.corpus_turn_requests;` (no dependent object; no reader shipped in this wave). |
| 278 | 278_item_grade.sql | Migration 278 (`intelligence_items.item_grade` — the record/brief item-tier column, Lane POP, 2026-09-01). Adds `item_grade text NOT NULL DEFAULT 'brief' CHECK (item_grade IN ('record','brief'))` + a partial index `WHERE item_grade = 'record'` + a COMMENT. **WHY:** the system review (`docs/audits/system-review-2026-09-01.md` §10) measured 2,561 sources / 21,609 census documents / 3,661 `would_mint` census_worklist rows against only 322 live verified items — every item today requires a synthesized, grounded brief, roughly 500 batches' worth of backlog at 5-8/batch. A RECORD-GRADE item (identity + dates + forward events + tags + a short description made ONLY of verbatim FACT/GAP spans, no synthesis, $0) is the cheaper tier that still clears `validate_item_provenance` honestly — this column is a LABEL for what already passed C1-C7, never a second weaker gate (that function is untouched by this migration and stays unconditional on grade). Operator ruling 2026-09-01: record-grade items MAY appear on customer surfaces, labeled (see `RecordGradeBadge`, same commit). **NO DATA MIGRATION** — `DEFAULT 'brief'` correctly stamps every existing row (all were minted through the pre-record-tier path) in the same DDL statement; no backfill UPDATE. Text+CHECK, not an ENUM, matching `item_type`/`priority`/`provenance_status`'s established convention. **LEFT UNAPPLIED** (two-track policy, CLAUDE.md standing rule 3) — authored by lane POP, number reserved for this lane per the dispatch, applied only by the coordinator before the dependent code (`mint-item.ts`'s grade param, `run-mint-batch.mjs --grade record`, `RecordGradeBadge`) reaches a live invocation. Reversible: `ALTER TABLE public.intelligence_items DROP COLUMN IF EXISTS item_grade;` — noted in the file header, no separate rollback file (additive, no backfill, no dependent object yet, same posture as 276). |
| 279 | 279_source_content_fingerprint.sql | Migration 279 (lane CD, change-detection chain repair, 2026-09-01). **FILENAME NOTE:** this number was reserved for a `sources` content-fingerprint column; reading the code first (per the dispatch's own instruction) found that work already shipped on this branch — migration 161 (`sources.last_content_hash`/`last_content_changed_at`) plus `check-sources/route.ts`'s `contentFingerprint`/`isContentChange` wiring (commit `cd9b63df`) — so `monitoring_queue.change_detected` is already real, not hardcoded false; no new fingerprint column is added here. **WHAT THIS MIGRATION ACTUALLY DOES:** the RLS verification the same dispatch asked for (Task 3, customer-visible change surface reading `intelligence_changes`) found the table WORSE than "no SELECT policy" — grepping every migration for `intelligence_changes` turns up only `009_capture_undeclared_tables.sql`'s retroactive `CREATE TABLE IF NOT EXISTS` (0 rows at capture, RLS state explicitly unverified) and a comment in `049`; no migration anywhere runs `ENABLE ROW LEVEL SECURITY` or defines a policy on it, so PostgREST's default schema grants apply with **no row filter at all** — the identical anon-writable-residue shape migration 230 fixed for 8 other tables, just never caught for this one. Fixes it on the same posture: `ENABLE ROW LEVEL SECURITY` + one public SELECT policy mirroring migration 103's exact predicate (`EXISTS ... intelligence_items i WHERE i.id = intelligence_changes.item_id AND i.is_archived = false`) — narrower than "RLS off" only in the `is_archived=true` case, the same posture 103/274 already enforce for their own tables. No write policy added (service_role bypasses RLS; every current writer — `reconcile.ts`'s `recordItemChange`/`recordSourceChangeTrigger`, worker-secret-gated — already writes service-role), which only closes the anon-write hole RLS-off left open. Self-verifying `DO` post-check asserts RLS on + exactly one named policy. **NOT YET APPLIED — DDL is coordinator-only (CLAUDE.md standing rule 3); written by lane CD and left for the coordinator**, ordered before the dependent read-site (`ChangedSinceStrip`, this lane's Dashboard component) reaches a live render. Reversible: `ALTER TABLE public.intelligence_changes DISABLE ROW LEVEL SECURITY` (no separate rollback file, matching 274/276's convention for a 0-row table with no dependent object). |
| 280 | 280_theme_briefs_public_read.sql | Migration 280 (coordinator, 2026-09-01): `theme_briefs_read` public SELECT policy mirroring 253's `connection_themes_read`, so customer surfaces (ThemeStrip on /research, the research theme-brief card) can read briefs; writes stay service-role-only. Found by lane SURF: RLS-on-with-no-policies made the table invisible to request-scoped clients. Applied 2026-09-01 by the coordinator. |
| 281 | 281_data_sources_ecb.sql | Migration 281 (Lane PROD, system-completion train, 2026-09-02): registers `data_sources.source_key='ecb'` (European Central Bank euro foreign exchange reference rates; `redistribution='permitted'`, `embeddable=true`, `attribution` "Source: European Central Bank", `verified_on` NULL, `blocker` carries the licence-text `[UNCONFIRMED until first runner dry run]` note because the sandbox cannot reach ecb.europa.eu). Closes the FK gate that kept `ecb-fx-producer.mjs` from ever writing a `market_series` row (the producer's `ENABLED` flipped to true in the same commit; the runtime env switch still defaults off). Hand-written `INSERT ... ON CONFLICT (source_key) DO NOTHING` rather than the `source-licence.mjs`-regenerated block (that file was outside the lane's write set; the resulting `SOURCE_LICENCES`/`data_sources` divergence is recorded in the migration header). Idempotent; post-check `DO` block asserts the row exists. **APPLIED 2026-09-02** by the coordinator through the Supabase MCP after PR #517 merged, in order 281→287, each self-check green. Reversible: `DELETE FROM public.data_sources WHERE source_key='ecb';` (no dependent rows until the producer's first apply). |
| 282 | 282_entities.sql | Migration 282 (Lane DP-SPINE, system-completion train, 2026-09-02 — [ADR-024](../decisions/ADR-024-decision-propagation.md); spec `docs/specs/08-flywheel-design.md` §1.1/§1.2). The entity spine: `entity_kind` enum (11 values: corridor, node, jurisdiction, organisation, asset, instrument, obligation, method, technology, signpost, person), `entities` (`entity_id` text PK shaped `cl:<kind>:<16-hex-sha256>`, `kind`, `canonical_name`, `status` +CHECK active/merged/retired, `merged_into` self-FK, `created_at`; CHECK `id_matches_kind` ties the id prefix to `kind`; CHECK `merged_has_target` ties `status='merged'` to a non-null `merged_into`), `entity_identifiers` (the crosswalk: `entity_id` FK, `scheme` text — no CHECK, deliberately byte-faithful to spec §1.1 — `value`, `scheme_version`, `asserted_by`, `asserted_at`; PK `(entity_id, scheme, value)`; UNIQUE INDEX `one_entity_per_identifier` on `(scheme, value, coalesce(scheme_version,''))` so the same external id can never resolve to two entities), `entity_scope` (spec §1.2: `subject_id`/`scope_id` FKs, `relation`, `confidence numeric(3,2)`, `attributed_to`; PK `(subject_id, scope_id, relation)`). RLS on all three: world-readable (`FOR SELECT USING (true)`, sources/regions posture), no write policy (service-role/guarded-path only, same posture as 258/271/277). Idempotent (`IF NOT EXISTS` / `DO $$` enum guard); self-check `DO` block asserts enum cardinality, column counts, the unique index, and probes the id-shape CHECK. Zero rows by construction — backfilled by `scripts/entities/backfill-entities.mjs` (separate, guarded, `--dry`-by-default pass, same commit). Empirically verified against a local Postgres 16 fixture schema (apply clean, idempotent re-run clean, `id_matches_kind` correctly rejects a mismatched-kind id) — no live DB access this lane. **APPLIED 2026-09-02** by the coordinator through the Supabase MCP after PR #517 merged, in order 281→287, each self-check green. Reversible: `DROP TABLE public.entity_scope, public.entity_identifiers, public.entities; DROP TYPE public.entity_kind;` (no dependent object outside this migration and 283, which must be reverted first). |
| 283 | 283_entity_refs.sql | Migration 283 (Lane DP-SPINE, system-completion train, 2026-09-02 — [ADR-024](../decisions/ADR-024-decision-propagation.md)'s progressive-re-keying decision). The progressive-re-keying half: adds nullable `intelligence_items.instrument_entity_id` and `sources.organisation_entity_id` (both `text REFERENCES entities(entity_id)`, each with a partial index) beside the existing text keys (`canonical_instrument_key`, `sources.url`) they do not touch or drop. Creates `entity_refs` (`ref_table`, `ref_id uuid`, `entity_id` FK, `role`, `asserted_by`, `asserted_at`; PK `(ref_table, ref_id, entity_id, role)`; CHECK `entity_refs_ref_table_allowed` restricts `ref_table` to `('intelligence_items','regions')`) for the MULTI-valued case — `jurisdiction_iso` is `TEXT[]` on both tables (migration 033/106, confirmed live), so one row per (item-or-region, jurisdiction code) rather than a lossy singular FK column. **Deliberate, documented deviation from the governing plan doc's literal `jurisdiction_entity_id` column naming** (system-completion-plan-2026-09-02.md §2): a singular FK cannot hold a `TEXT[]` fact without silently narrowing it, which the migration's own header records at length. RLS mirrors 282 (world-readable, service-role writes). Self-check `DO` block asserts both new columns exist, `entity_refs` has 6 columns, and — additive-only proof — zero rows in `entity_refs` and zero non-null values in either new FK column at migration time (the backfill that populates them is `scripts/entities/backfill-entities.mjs`, a separate guarded pass, same commit). Empirically verified against a local Postgres 16 fixture (applies clean on top of 282, idempotent, `entity_refs_ref_table_allowed` correctly rejects a bogus table name). **APPLIED 2026-09-02** by the coordinator through the Supabase MCP after PR #517 merged, in order 281→287, each self-check green. Reversible: `ALTER TABLE public.sources DROP COLUMN IF EXISTS organisation_entity_id; ALTER TABLE public.intelligence_items DROP COLUMN IF EXISTS instrument_entity_id; DROP TABLE public.entity_refs;` (0 rows/0 populated FKs by construction until the backfill runs, so no data loss on rollback before that pass). |
| 284 | 284_propagation_outbox.sql | Migration 284 (Lane DP-ENGINE, system-completion train, 2026-09-02 — [ADR-024](../decisions/ADR-024-decision-propagation.md); spec `docs/specs/08-flywheel-design.md` §2.2 Part 1). The transactional outbox: `propagation_events` (`event_id bigserial PK`, `occurred_at`, `table_name`, `row_pk text`, `entity_id text REFERENCES entities(entity_id)` nullable, `change_kind` +CHECK insert/update/delete/supersede, `old_row`/`new_row jsonb`, `txid_current()`, `drained_at`, `drain_run_id` — 11 columns), `emit_propagation_event()` (generic AFTER-trigger function, `TG_ARGV[0]`=pk column name, no-op-update detection via jsonb diff excluding `updated_at`, supersede detection via a `superseded_by` NULL→NOT NULL transition), attached to `emission_factors`/`market_series`/`regional_data_facts`, and `propagation_queue_depth` (view: pending count + oldest pending timestamp). **Deliberate, documented deviation from the governing plan's own `propagation_events.subject_id NOT NULL REFERENCES entities` design**: replaced with generic `(table_name, row_pk)` addressing plus a nullable `entity_id`, because three of the four real source tables carry no `entity_id` column today (migration header records the full reasoning). RLS enabled, no policies (service-role only). Self-check `DO` block asserts column count, trigger attachment (3 tables), index count, zero rows. Empirically verified against a local Postgres 16 scratch database seeded with real migrations 282/258/268/106 (applies clean, idempotent). **APPLIED 2026-09-02** by the coordinator through the Supabase MCP after PR #517 merged, in order 281→287, each self-check green. Reversible: `DROP TRIGGER propagation_outbox_trg ON public.emission_factors, public.market_series, public.regional_data_facts; DROP VIEW public.propagation_queue_depth; DROP FUNCTION public.emit_propagation_event(text); DROP TABLE public.propagation_events;` (0 rows by construction). |
| 285 | 285_derivation_dag_and_derived_values.sql | Migration 285 (Lane DP-ENGINE, system-completion train, 2026-09-02 — spec `docs/specs/08-flywheel-design.md` §2.2 Part 2, §3.1-§3.3). The core value store + invalidation DAG: `derived_values` (22 columns — `value_id uuid PK`, `entity_id` nullable FK, `method_id`/`method_version`, `value`/`value_low`/`value_high`/`unit`/`currency`, `derivation` +CHECK (9-value spec vocabulary), `origin_class` +CHECK (7-value), `lifecycle` +CHECK (8-value), `admissibility` +CHECK (5-value), `base_confidence`, `asserted_at`, `half_life_days`, `inputs jsonb`, `supersedes` self-FK, `computed_at`/`computed_by`, `invalidated_at`/`invalidated_by_event`), `derivation_edges` (`from_table`/`from_pk` generic address, `to_value_id` FK, `edge_kind`; CHECK restricts `from_table` to a 6-table allowlist), `assert_acyclic()` (BEFORE-trigger, recursive-CTE reachability walk, depth-capped at 32), `invalidate_dependents(table, pk, event, apply)` (dry/apply recursive-CTE closure walk), `effective_confidence(base, asserted_at, half_life_days, now)` (spec §3.2's decay formula, `NULL` half-life = no decay), `derived_values_admissible` view (hides stale/falsified/obsolete — spec §3.3's second enforcement point), and `register_derived_value(...)` (one atomic RPC writing a value row + its `derivation_edges` in one call — `register-derivation.ts`'s sole write path). **Deliberate, documented deviations**: `value_id` is `uuid`, not a minted `cl:value:<hex>` entity id (a derived value is DATA ABOUT an entity, routinely superseded, not itself an entity — migration header's extended reasoning); `derivation_edges` uses generic `(from_table, from_pk)` addressing, not spec's own `entities`-FK-typed pair (same root cause as 284's deviation). RLS: raw table denied, `derived_values_admissible` granted (Postgres's default non-`security_invoker` view semantics). Self-check `DO` block builds real probe rows, proves `assert_acyclic` rejects a 2-node cycle, proves `invalidate_dependents` dry/apply modes, proves `derived_values_admissible` hides stale rows, proves `register_derived_value` writes value+edges atomically and records `supersedes`, cleans up to zero rows. Empirically verified against the same local Postgres 16 scratch database as 284 (applies clean on top of 284, idempotent re-run confirmed). **APPLIED 2026-09-02** by the coordinator through the Supabase MCP after PR #517 merged, in order 281→287, each self-check green. Reversible: drop the view, functions, triggers, `derivation_edges`, then `derived_values` (0 rows by construction). |
| 286 | 286_statutory_and_estimates.sql | Migration 286 (Lane DP-ENGINE, system-completion train, 2026-09-02 — spec `docs/specs/08-flywheel-design.md` §4 Layers 1 & 3). Statutory/estimate isolation: `statutory_computations` (`entity_id text PK` FK, `obligation_id` FK, `formula_id`/`formula_version`, `statute_citation`, `unit_price`/`unit_price_unit`, `inputs jsonb`, `result`/`result_unit`, `computed_at`; CHECKs requiring a citation and a non-null result), `estimated_values` (`entity_id text PK` FK, `model_id`/`model_version`, `point`/`low`/`high`, `distribution jsonb`, `pedigree jsonb`; CHECKs requiring uncertainty, range-ordering, point-in-range), `assert_statutory_purity()` (BEFORE-trigger inspecting a row's own `inputs` array directly — refuses any element naming a live `estimated_values` row or a `derived_values` row whose `derivation` is modelled/estimated/interpolated). **Deliberate, documented deviation**: `assert_statutory_purity()` inspects `NEW.inputs` directly rather than walking `derivation_edges` transitively (spec's own illustrative body) — `statutory_computations` is never a `to_value_id` in `derivation_edges` (a terminal figure, never an input to anything else), so there is no DAG edge to walk from this table; named as a residual for future multi-hop statutory composition. RLS: world-readable, service-role writes (mirrors `emission_factors`). Self-check `DO` block creates real entities, inserts a real `estimated_values` row, proves a `statutory_computations` INSERT citing it is rejected, cleans up (**cleanup-order finding preserved as an in-migration comment**: deleting `estimated_values` fires the outbox trigger, which re-creates a `propagation_events` row after an earlier cleanup pass — reorder to clear trigger-firing deletes before `propagation_events`, found live by this self-check on first draft). Empirically verified against the same local Postgres 16 scratch database as 284/285. **APPLIED 2026-09-02** by the coordinator through the Supabase MCP after PR #517 merged, in order 281→287, each self-check green. Reversible: `DROP TRIGGER statutory_purity_trg ON public.statutory_computations; DROP FUNCTION public.assert_statutory_purity(); DROP TABLE public.statutory_computations, public.estimated_values;` (0 rows by construction). |
| 287 | 287_sensitive_aggregates.sql | Migration 287 (Lane DP-ENGINE, system-completion train, 2026-09-02 — spec `docs/specs/08-flywheel-design.md` §5, antitrust/anonymisation safeguards; **two commits, same day**, per operator ruling "nothing deferred"). First commit: `sensitive_field_policy` (per-field policy: `table_name`/`column_name` PK, `k_min` +CHECK ≥5, `max_share_pct`, `min_lag_days`, `forward_looking_allowed`, `bucket_scheme`, `policy`; seeded with 4 spec-named fields, `table_name='community_contributions'` placeholder — not yet live), `aggregate_query_log` (`query_id uuid PK`, `table_name`/`column_name`, `cohort_hash`, `cohort_members jsonb`, `cohort_size`, `requested_at`/`requested_by`, `refused`, `reason`), `publish_aggregate(table, column, cohort_filter)` (SECURITY DEFINER, refuses/logs the k-anonymity floor and §5.2(a) query-set-size/tracker-attack overlap). Second commit completes §5.2(b)-(d), the dominance cap, and the forward-looking refusal: `aggregate_query_log` gains `frozen boolean`, `frozen_from uuid`, `granted_payload jsonb` (the exact returned payload, stored so a frozen repeat replays it verbatim); two new pure helper functions `bucket_width_multiplier(n, k_min)` (`GREATEST(1, CEIL(2*k_min/n))`, widening rule tied to spec §5.2(c)'s own "2× the minimum" threshold) and `bucket_value(value, scheme, multiplier)` (grammar `pct:N` / `abs:N` / `log2`, defined in this migration's header — NULL for an unrecognised scheme, never the raw value); `publish_aggregate` gains a 7-step check order (freeze → k_min → forward-looking → dominance → §5.2(b) complement → §5.2(a) overlap → grant+bucket) and 4 new optional `p_cohort_filter` keys (`member_values`, `parent_member_ids`, `period_start`/`period_end`), each check activating only when its data is supplied (no live subject exists to derive them from otherwise — see header). Seed `bucket_scheme` values updated in place from first-commit placeholders (`log10_decile` etc.) to the real grammar (`abs:100`/`abs:5`/`log2`/`pct:5`). **Major, documented deviation, found by testing (first commit, still binding)**: the ORIGINAL implementation followed spec §5.1's literal `RAISE EXCEPTION` pattern for refusals; testing showed Postgres rolls back a function's own prior `INSERT` statements when that SAME call later raises an unhandled exception — regardless of whether the caller catches it — so every refusal's log row was silently lost. Fixed by redesigning `publish_aggregate()` to NEVER raise for a policy-driven refusal; it always logs, then `RETURN`s a jsonb `{refused, reason, ...}` payload instead. **Second-commit bug, found by testing, fixed before landing**: the plpgsql local variable and the new `granted_payload` table column shared a name, making `SELECT query_id, granted_payload INTO frozen_row FROM aggregate_query_log ...` ambiguous (Postgres refuses to guess between a column and a same-named plpgsql variable) — fixed by renaming the local variable to `v_granted_payload` (the column name is unchanged). RLS enabled, no policies; `EXECUTE` on `publish_aggregate` granted to `authenticated`, the two bucket helpers `REVOKE`d from `PUBLIC` (callable only via the function owner's own implicit privilege, i.e. through `publish_aggregate`). Self-check `DO` block: 10 direct `bucket_value`/`bucket_width_multiplier` fixture assertions, then 14 real `publish_aggregate()` calls across four fields (rate_per_feu: unregistered/below-k_min/grant/freeze-repeat/tracker-attack-refuse/grant; wage_per_hour: grant/§5.2(b)-complement-refuse/fresh-grant; capacity_teu: dominance-refuse/balanced-grant/freeze-repeat-under-DOUBLED-values-proving-the-frozen-value-is-unchanged; saf_premium_pct: forward-looking-refuse/past-period-grant) — asserts the log holds exactly 14 rows. A pure JS mirror, `src/lib/propagation/aggregate-safeguards.mjs` (+ 37-test `aggregate-safeguards.test.mjs`), shares the SAME literal fixtures for `bucket_value`/`bucket_width_multiplier`/dominance/complement/freeze-window/forward-looking, so the SQL gate and the CI-testable-without-a-database mirror are proven to agree. Empirically verified (both commits) against the same local Postgres 16 scratch database as 284-286, including a standalone re-application of 287 alone to prove idempotency. **APPLIED 2026-09-02** by the coordinator through the Supabase MCP after PR #517 merged, in order 281→287, each self-check green. Reversible: `REVOKE EXECUTE ...; DROP FUNCTION public.publish_aggregate(...), public.bucket_value(...), public.bucket_width_multiplier(...); DROP TABLE public.aggregate_query_log, public.sensitive_field_policy;` (leaves only the 4 seeded policy rows to also delete on rollback). |
| 288 | 288_source_type_taxonomy.sql | Migration 288 (Lane HYG-2, finish plan 2026-09-02 — [SOURCE-TYPE-TAXONOMY-PROPOSAL](../plans/SOURCE-TYPE-TAXONOMY-PROPOSAL.md)). Retires the STOPGAP in `src/lib/coverage-gaps.ts`: two regex pattern sets (env-body, legislature) matched against each source's `name+url` at READ time, on every cache miss. Adds `sources.source_type TEXT[]`, **NULLable, no default** (deliberate deviation from the proposal's own `DEFAULT '{}'::TEXT[]` sketch — an empty array would assert "classified as zero types" for ~718 rows nothing has classified yet; NULL is the honest "not yet classified" value; see migration header), CHECK `sources_source_type_valid` (the proposal's 11-value vocabulary, `source_type <@ ARRAY[...]`, NULL-safe), and a GIN index `idx_sources_source_type`. Idempotent (`ADD COLUMN IF NOT EXISTS`; CHECK guarded by a `pg_constraint` existence probe; `CREATE INDEX IF NOT EXISTS`). **Checked for a competing axis first** (ledger B1/B2): `source_role`/`category` (migrations 063/084/123, content-routing) and `institution_id` (migration 122, WHO published) both already exist and are explicitly NOT superseded — neither encodes "environmental regulator vs legislature vs generic executive regulator," which is what the coverage-gaps consumer needs; `grep -ril source_type docs/decisions/` found no governing ADR. Vocabulary + classifier: `src/lib/sources/source-type-taxonomy.mjs` (11 tokens registered; only 2 — `environmental_body`, `legislature` — are classifiable today, the STOPGAP's own regexes ported verbatim; stated as an honest scope limit, not a silent gap, in that module's header). Backfill: `scripts/sources/backfill-source-type.mjs` (dry by default, `--apply` writes through `guardedUpdateByIds`, never overwrites an already-classified row). `coverage-gaps.ts` refactored to a thin I/O wrapper over the new pure `src/lib/coverage-gaps-rollup.ts` (`rollupRegions()` reads `source_type` first, falls back to the classifier for a still-NULL row) — split out because `node --test` cannot resolve `next/cache` at all (no bundler; Next's package.json declares no `./cache` export), so the pure rollup math needed a dependency-free home to be unit-tested, matching the `drain.ts`/`watchlist-scope.ts` convention already in this repo. Self-check `DO` block asserts column type, CHECK constraint presence, index presence. **APPLIED 2026-09-02 20:10 UTC** via Supabase MCP by the coordinator (column type ARRAY, CHECK `sources_source_type_valid` present, GIN index present, 0 rows classified until the `maintenance` → `source-type-backfill` step runs) [CONFIRMED by information_schema / pg_constraint / pg_indexes reads]. The HYG-2 lane brief had said: "The coordinator applies the migration and the backfill; you do not." Reversible: `DROP INDEX idx_sources_source_type; ALTER TABLE public.sources DROP CONSTRAINT sources_source_type_valid, DROP COLUMN source_type;` (0 rows populated until the backfill runs). |
| 289 | 289_criterion2_url_balanced_parens.sql | Migration 289 (coordinator, 2026-09-02, from mint-run-014): criterion-2 citation-URL extraction in `validate_item_provenance` balances one level of parentheses (`https?://(?:[^\s()\]}"'<>]|\([^\s()]*\))+`) so EUR-Lex "(01)" identifiers (CELEX 32023D0628(01)) extract whole; the old `[^\s)\]}"'<>]+` stopped at the first `)` and population run #12 lost 2/42 payloads to `ungrounded_url`. SHAPE: the 14,164-char body lives only in the database (no repo dump), so the migration patches the live definition IN PLACE with three guards: pre-md5 must equal `7cb3d38f6ea00eec9bb887e6fde25bb9` (the body read that day), exactly one occurrence of the old literal, post-check new-present/old-absent; a re-run is a no-op when the new literal is already present. **APPLIED 2026-09-02 20:35 UTC** via Supabase MCP; post md5 `82f7032e21424d127d0864e7626b810d`, length 14,182 [CONFIRMED by `pg_get_functiondef`]. Parity: `scripts/mint/validate-mint-payload.mjs` `URL_RE` changed in the same commit with two tests. Reversible by the same block with the literals swapped (pin the post md5). |
| 290 | 290_obligations.sql | Migration 290 (Wave 2 lane OBLIG, 2026-09-02, spec-01's core build): table `obligations`, one row per `item_forward_events` row (FK to it and to `intelligence_items`; `forward_event_id` UNIQUE for idempotency), denormalised `jurisdictions[]`/`modes[]` (CHECKed against the canonical transport-mode vocabulary and its input aliases; the corridor-only `multimodal` token is refused on a register row), `binding_position` CHECKed to the four `BINDING_POSITION` codes or NULL ("not yet classified"), `due_date` + `date_precision` paired-NULL, obligation text and source basis; RLS mirrors migration 274/103. Header states it ships the spec-01 §2 MVP grain, not the full §3.2 field list. Derivation: `scripts/obligations/derive-obligations.mjs` (dry by default; `--apply` through `guardedInsertMany`; classifier `src/lib/obligations/classify-binding-position.mjs` is the deterministic table from spec-01 §1, unmapped → NULL). Read model `src/lib/obligations/read-register.mjs`; surface `ObligationRegister*.tsx` on /regulations and /regulations/[slug]. Self-check DO block. **APPLIED 2026-09-03 ~01:20 UTC** via Supabase MCP, DDL-first before the train that carries the reader deployed (two-track policy); post-check [CONFIRMED]: 14 columns, 6 CHECKs, RLS on, 0 rows, 903 `item_forward_events` available to derive. Derivation dry → apply runs after the train lands. Reversible: `DROP TABLE public.obligations` (0 rows until derived). |
| 293 | 293_community_identity_and_guard.sql | Migration 293 (Wave 3 lane COMMUNITY-A, 2026-09-03; spec 05 §2/§4/§5): `community_member_profiles` (org_type/role/sector/region/verified only, no name/email/company by construction; `organisation_key` server-derived and column-locked via REVOKE + column-list GRANT), `community_posts.origin_class` (NOT NULL DEFAULT 'community', backfilled, 7-value CHECK shared with `vocabularies.mjs`), `community_thread_entities` (thread↔`entities(entity_id)` join, ≥1 per thread enforced by `POST /api/community/posts`). RLS on all. Self-check DO block. **APPLIED 2026-09-03 via Supabase MCP by the coordinator, DDL-first before the Wave 3 train deployed** [CONFIRMED: 11/4 columns, RLS on]. Reversible: drop the two tables, drop the column + CHECK. |
| 294 | 294_community_aggregate_instruments.sql | Migration 294 (COMMUNITY-A): `community_benchmark_instruments` (house-seeded calendar instruments, `created_by='house'` CHECK) and `community_benchmark_responses` (no anon/authenticated SELECT; read only through `publish_aggregate()` via `GET /api/community/benchmarks/current`); registers `sensitive_field_policy` row (k=5, 25 %, 90-day lag) and proves `publish_aggregate()` refuses a 3-org cohort and accepts a balanced 5-org cohort live. Seeder `scripts/community/seed-benchmark-instruments.mjs` (maintenance step `seed-benchmark-instruments`, dispatch-only). **APPLIED 2026-09-03** [CONFIRMED: 15/6 columns, RLS on, 0 rows]. |
| 295 | 295_community_promotion_machine.sql | Migration 295 (COMMUNITY-A, spec 05 §4): `community_posts.promotion_state` (5-value CHECK, default 'community'), `community_promotion_transitions` (append-only log, service-role writes; `src/lib/community/promotion.mjs` validates before insert), `community_posts.stance` (reply stance feeding corroboration). **APPLIED 2026-09-03** [CONFIRMED: 9 columns on transitions, RLS on]. |
| 296 | 296_spec09_market_tables.sql | Migration 296 (Wave 3 lane SPEC-09, spec 09 §1.1/§1.2/§1.3/§1.7): `carrier_compliance_pools` (never-statutory CHECK), `surcharge_audits` (`variance_eur` GENERATED; `pool_adjusted_eur` never populated, spec 09 §5 decision 1 conservative default), `oem_tech_roadmaps` (density basis required, Admiralty shape CHECK), `indexation_clauses` (mechanics only, never clause text), `reroute_events` (distinct-corridor CHECK). All corridor/organisation keys are `entities(entity_id)` FKs (kind not DB-enforced, stated). RLS + authenticated SELECT. Self-check inserts and removes selftest entities. **APPLIED 2026-09-03** [CONFIRMED: 9/13/15/12/10 columns, RLS on, 0 rows]. Producers `scripts/spec09/*-producer.mjs` ship the tables empty with the $0-source gap named in `SOURCES.md`; `spec09-reroute` maintenance step monitors corridor-entity count. |
| 297 | 297_spec09_operations_tables.sql | Migration 297 (SPEC-09, spec 09 §1.4/§1.5/§1.6): `tce_data_quality` (five 1..5 axes per transport-chain element, `primary_data_share`), `auxiliary_energy_profiles` (stationary load), `grid_connection_queues` (p50/p90 ordered CHECK, SDMX `obs_status`). RLS + authenticated SELECT. **APPLIED 2026-09-03** [CONFIRMED: 10/10/9 columns, RLS on, 0 rows]. |
| 298 | 298_spec09_regulations_tables.sql | Migration 298 (SPEC-09, spec 09 §1.8): `eudr_plot_claims` (`hold_risk` border_hold = blocking alert), `custody_chains` (`double_count_check`, retirement-needs-registry CHECK). RLS + authenticated SELECT. **APPLIED 2026-09-03** [CONFIRMED: 8/9 columns, RLS on, 0 rows]. |
| 299 | 299_item_type_required_slots_wave3.sql | Migration 299 (Lane POP-PREP, population-pass readiness, 2026-09-03): INSERT-only into the EXISTING `item_type_required_slots` table (migration 112), same convention as 113/126/128/129 — `market_signal.corridor_identity`, `initiative.corridor_identity`, `research_finding.evidence_agreement_signal`, `research_finding.source_authority_signal`, the three slots `scripts/mint/item-type-required-slots.json` already requires and this live table deliberately did not (MINT-RUNBOOK.md §13: adding them live before the population re-mint would flip existing verified items of those types to `quarantined` on criterion 5). Header carries the exact self-check SQL (counts currently-verified items that would newly fail criterion 5) and the sequence: self-check → apply 299 → re-mint the N items in the same pass. **LEFT UNAPPLIED** (two-track policy) — no DB credentials in this lane; the coordinator applies it after reading the self-check count. See `docs/plans/population-pass-2026-09-03.md` §2. Reversible: `DELETE FROM item_type_required_slots WHERE item_type IN ('market_signal','initiative','research_finding') AND slot_key IN ('corridor_identity','evidence_agreement_signal','source_authority_signal')`. |
| 300 | 300_criterion2_url_typographic_delimiters.sql | Migration 300 (Lane URL-GUIL, 2026-09-03): in-place patch of `validate_item_provenance` criterion 2's URL regex to exclude the eight typographic delimiters `« » ‹ › “ ” ‘ ’` (the kit's guillemet span delimiter was being swallowed into URLs: `http://eur-lex»`, population runs #15/#16). Guarded: pre-patch md5 `82f7032e21424d127d0864e7626b810d`, exactly one occurrence, post-check. **APPLIED 2026-09-04 00:13 UTC via Supabase MCP** [CONFIRMED: post md5 `da6b00972e5a9bdb089bbe1f1d65d697`, new literal present, schema_migrations `20260904001306`]. JS mirror `scripts/mint/validate-mint-payload.mjs` URL_RE changed in the same train. Reversible by re-running with old/new swapped against the post md5. |
| 302 | 302_criterion3_rating_not_refusal.sql | Migration 302 (Lane HEAL-7, 2026-09-04 — operator ruling verbatim: "get the source. then rate the source. it's that simple. ... find the source and then publish the data on the site"). Criterion 3's authority-floor half (`fact_below_authority_floor`) stops being a blocking `v_failures` entry and becomes a non-blocking RATING recorded on a new `validation_result.warnings` composite attribute (`{below_floor_facts, claims:[...]}`, same payload shape the failure used to carry). `fact_missing_source_span` / `fact_span_not_in_source` / `fact_mint_hold` are UNCHANGED — the ruling overrules the refusal half of the floor, never the grounding requirement; an ungrounded claim still quarantines. SHAPE: same in-place-patch discipline as 225/300 (read live `pg_get_functiondef`, guard on exact live text, `EXECUTE`, verify) plus one additive `ALTER TYPE public.validation_result ADD ATTRIBUTE warnings jsonb` (idempotent, checked via `pg_attribute`). Pre-patch md5 guard `da6b00972e5a9bdb089bbe1f1d65d697` (= migration 300's own post-patch md5, read live 2026-09-04, read-only via Supabase MCP — this lane holds no write credential to the DB). **DRIFT FOUND (read-only, reported not fixed here)**: the live function already carries a `c_own_body_types`/`voluntary_own_body` own-body-floor extension (standard/framework/initiative) and reads `ars.result_content` (migration 264's rename), neither of which appears in any committed file under `supabase/migrations/` as of this lane's base (`e6eff093`) — the schema is ahead of the committed chain by at least one applied-but-uncommitted change; migration 302's guard aborts loudly rather than silently mis-patching if the live body has moved again by apply time. JS mirror `scripts/mint/validate-mint-payload.mjs` changed in the same commit: its own `fact_below_authority_floor` push moves from `failures` to a new `warnings` return field, same shape, so the kit and the function agree on what blocks. **APPLIED LIVE 2026-09-04 05:49 UTC** by the coordinator via the Supabase MCP after re-reading the body: pre md5 `da6b00972e5a9bdb089bbe1f1d65d697` matched, post md5 `7c4ae6dc4fa4ef1c91c63fddbc8d56cb`, `validation_result` attrs now `valid,failures,recommended_status,warnings`, `schema_migrations` 20260904054918. Reversible: `ALTER TYPE public.validation_result DROP ATTRIBUTE warnings;` + re-run PART 2's three replacements with old/new swapped. |
| 303 | 303_slim_listings_id_tiebreak.sql | Migration 303 (Lane SLIM-ORDER, 2026-09-04 — follow-up to FIRSTPAGE's fix for /regulations priority-band-ranking loss). `get_workspace_intelligence_slim` (/operations, /market first-paint pagination via fetchResourcesOnly) carries the SAME defect FIRSTPAGE fixed for `/regulations`: its own ORDER BY ends `..., ii.added_date DESC;` with NO `id` tiebreak, so PostgREST outer `.order("added_date", desc).order("id", asc)` REPLACES the RPC's internal priority-band CASE rank ordering during pagination (live-confirmed SQL, 2026-09-04: under outer order = 100% MODERATE first 60 rows; under RPC's own = 14 CRITICAL, 30 HIGH, 16 MODERATE — identical defect to /regulations). Fix: add `, ii.id ASC` to the function's own ORDER BY, matching what `get_workspace_intelligence_listings` (migration 272) already carries. Once live, `buildWorkspaceItemsQuery`'s allowlist LISTINGS_RPCS_WITH_OWN_TOTAL_ORDER (supabase-server.ts) adds `"get_workspace_intelligence_slim"`, dropping the outer order for this RPC and restoring its own priority-band rank on /operations and /market (strategy identical to /regulations fix). SHAPE: CREATE OR REPLACE entire function body (idempotent; small self-contained function). Pre-patch md5 guard `02936dfa040b36c54bfb06343e217bcc` (live, 2026-09-04, read-only Supabase MCP). **APPLIED LIVE 2026-09-04 08:49 UTC** by the coordinator through the Supabase MCP (pre md5 matched, post md5 `3ca10db08f84c019c9fa0e16bfe3b49b`, schema_migrations `20260904084952`); the allowlist entry landed in the same train (wave25). Reversible (remove `, ii.id ASC` from ORDER BY). |
| 248 | 248_security_grants_hardening_2026_08_09.sql | Migration 248 (security grants hardening, 2026-08-09; full-code reading audit P0 #1/#2/#5). Revokes EXECUTE on `admin_set_pause_state(text, boolean, text, date, boolean)` and `gate_a_health()` FROM PUBLIC, anon, authenticated, and re-pins `set_provenance_status()` to `SET search_path = public, extensions, pg_temp`. Both RPCs carried anon+authenticated+PUBLIC EXECUTE grants while being called ONLY from server routes through `getServiceSupabase()` (service_role) — verify-before-fix confirmed the caller set before revoking, so closing the anon-callable exposure has no legitimate-caller impact; mirrors migration 238's complete grant pattern. The `set_provenance_status` pin had been lost since the search_path hardening that migration 160 established, re-opening the mutable-search_path class on a SECURITY DEFINER-adjacent trigger function. Idempotent (REVOKE is a no-op when the grant is absent; ALTER FUNCTION ... SET is last-write-wins). **APPLIED LIVE 2026-08-09** via Supabase MCP ahead of this commit — this file is the audit record per the code-vs-data doctrine, NOT a pending apply; do not re-run as a state change. Verified in the live ledger as two entries, `20260809030555 security_hardening_2026_08_09_grants_and_searchpath` and `20260809030625 security_hardening_2026_08_09_revoke_public`. Filename uses underscore date separators (F6 enforces `NNN_snake_case.sql`; the prose date stays hyphenated). Reversible (re-GRANT EXECUTE; drop the search_path setting). |
| 252 | 252_connection_discovery_basis_and_origin.sql | Migration 252 (connection-discovery edges, Pillar A, 2026-08-09). Adds `basis` jsonb + `score` real to `item_cross_references` and widens the origin CHECK with `provenance_discovery`, so discovered connections store WHY they connect (same instrument / shared scenario / non-role compliance object / jurisdiction+topic) — no ungrounded links. relationship stays semantic (`related`); signals live in basis. Consumed by src/lib/connections/discover.mjs + scripts/connections/backfill-edges.mjs. Additive (nullable columns; CHECK widened, no row violates). **APPLIED LIVE 2026-08-09** via MCP. Reversible. |
| 251 | 251_workspace_profile_readtime_anchoring.sql | Migration 251 (workspace profile for read-time contextualization, 2026-08-09; Option B ruling). Adds a `profile` jsonb to `workspace_settings` (roles, transport_modes, trade_lanes, products, operational_baseline, office_footprint, regulation_scope) — the remaining per-org profile fields the read-time anchoring layer needs; cargo verticals already live in `sector_profile`, jurisdictions in `jurisdiction_weights`. Shared briefs stay role-generic; this profile is applied per-viewer at READ time, never at generation. Seeds the current workspace (freight forwarder/importer/exporter, air/ocean/road, worldwide, 80 offices, automate-with-manual baseline). Consumed by `src/lib/workspace/profile.ts`. Additive (`ADD COLUMN IF NOT EXISTS`, default `'{}'`), no existing column touched. **APPLIED LIVE 2026-08-09** via Supabase MCP ahead of this commit — audit record, not a pending apply. Reversible (`DROP COLUMN profile`). |
| 250 | 250_provenance_verified_depth_binding.sql | Migration 250 (#43 provenance binding REBUILT, 2026-08-09; full-code reading audit P0 #6, plus a wider hole found live during the fix). Retires migration 118's `stamp_prov_origin` origin-stamp (a session GUC any role could forge with `set_config('app.prov_flip_origin','INSERT',true)`, making the reconciler binding decorative; the depth>=1 test inside the guard was always true; ON CONFLICT DO UPDATE mis-stamped updates as INSERTs) and rebuilds `guard_provenance_flip` to gate every transition INTO `provenance_status='verified'`, allowed only when `current_user='reconciler'` OR `pg_trigger_depth()>=2` (the write came from inside another trigger — verified against the live catalog that `set_provenance_status` is the ONLY trigger function writing this column, so depth>=2 == the validation derivation; depth is engine truth, unforgeable). Closes the previously-UNGUARDED `quarantined->verified` escalation (mig 118 fired only on OLD='unverified', but the corpus parks items at 'quarantined', so 180 live rows were one direct UPDATE from verified). Restrictive transitions (->quarantined, ->pending_human_verify, downgrades) are deliberately open, which dissolves the "reconciler credential broken" tech-debt wedge (touch-and-derive now works for any role; validation is the sole path to verified). Scope change vs 118's letter surfaced in ADR-017. **APPLIED LIVE 2026-08-09** via Supabase MCP ahead of this commit — audit record, not a pending apply; do not re-run as a state change. Verified in the live ledger as `20260809183427 provenance_verified_depth_binding`, and by the permanent adversarial proof `scripts/verify/prov-guard-adversarial-audit.mjs` (forged-GUC escalation denied, direct unverified->verified denied, ON CONFLICT escalation denied, restrictive flip allowed, derivation re-verify at depth>=2 allowed) now wired hard into the data-audit lane. Filename uses underscore date separators (F6). Reversible (restore mig 118's `stamp_prov_origin` + the OLD='unverified' guard). |
| 249 | 249_platform_admin_rls_alignment_2026_08_09.sql | Migration 249 (platform-admin RLS alignment, 2026-08-09; full-code reading audit P0 #3, de-conflated and verified). Repoints `integrity_flags_admin_read`, `integrity_flags_admin_update`, and `holdings_quality_admin_read` from an `org_memberships` owner/admin-of-any-org predicate to `profiles.is_platform_admin` (the app's actual platform-admin gate, migration 075). The old predicate combined with `create_org_for_self` — which lets ANY authenticated user become an org owner — meant any signed-in user could READ platform integrity flags, UPDATE (tamper with) them, and read the capture-quality audit. Verified safe before applying: the admin UI reads and writes these tables via service-role API routes (`service_role_write` policy plus service-role RLS bypass), and no browser client queries them directly, so no legitimate caller loses access. Idempotent by construction (`DROP POLICY IF EXISTS` then `CREATE POLICY`). **APPLIED LIVE 2026-08-09** via Supabase MCP ahead of this commit — audit record, not a pending apply; do not re-run as a state change. Verified in the live ledger as `20260809031157 platform_admin_rls_alignment_2026_08_09`. Filename uses underscore date separators (F6). Reversible (restore the prior org_memberships-based policies). |
| 226 | 226_gate_a_health_rpc.sql | Migration 226 (Gate-A read-side honesty backstop, 2026-07-26). Creates `gate_a_health()` (STABLE, service_role EXECUTE), the RPC the "Uptime and honesty probes" workflow reads via `/api/health/surfaces` (the workflow holds no DB creds). Returns 4 ALARM counts that must all read 0 (probe fails, fail-closed on null): `invariant_violations` (verified + non-empty brief + literal Gate-A orphan), `briefless_verified` (verified + empty brief), `no_gatestate_verified` (verified brief never Gate-A-scanned — the true bypass signal), `verified_failing_revalidation` (COMPLETE detector — any verified item that would fail `validate_item_provenance` now: stale-under-old-rules or any trigger bypass) + 1 INFO count `verified_gen_ver_null_info` (legacy stamp gap, not alarmed — clears as items regenerate). Read-side backstop to the write-time enforcement (the `set_provenance_status` trigger routes every write through validate; a direct status write is overridden by its recompute); catches the legacy-drift class this session's dig-fallback incident surfaced. **APPLIED 2026-07-26** via apply_migration; live read = all ALARMs 0, INFO gen_ver_null=2. Reversible (`DROP FUNCTION public.gate_a_health()`). |
| 224 | 224_item_gate_a_state.sql | Migration 224 (Gate A state layer, 2026-07-26). Creates `item_gate_a_state` (per-item prose-fact scan state: `scanned_hash` = md5(full_brief at scan time), `orphan_count`, `orphans` jsonb, `gate_a_version`, `scanned_at`) for GATE A — the guarantee that every fact a customer could act on (price/%/threshold/quantity/compliance-deadline) is individually span-proven. Landed FIRST with NO `validate_item_provenance` criterion (interlock-free ordering, operator ruling 2026-07-26): state accumulates while nothing gates and grounding never breaks; a later migration flips criterion 7 (require state present + `scanned_hash = md5(full_brief)` [not stale] + `orphan_count = 0`). RLS service-role-only; partial index on orphan_count>0. **APPLIED 2026-07-26** via apply_migration; backfilled 345/345 briefs (100% coverage), 329 with orphans. Reversible (`DROP TABLE public.item_gate_a_state`). |
| 223 | 223_acquisition_backlog_v.sql | Migration 223 (scheduled-workflow spend diagnosis follow-on, 2026-07-25). Retroactive byte-matching migration for the live view `acquisition_backlog_v`, which existed in the DB with NO committed CREATE anywhere in `supabase/migrations/` — the schema-drift audit's (RD-49) first-run finding, allowlisted 2026-07-20 and routed to a retroactive-migration owner. Source captured via `pg_get_viewdef('public.acquisition_backlog_v', true)` on 2026-07-25 (introspected, not written from memory); the view ranks `coverage_gap_candidates` into acquisition-backlog sections (instrument/free=1, feed-or-tracker/free=2, licensed/mixed=3, parked-with-surface-test=3/4, declined=NULL) with `mode_priority_weight` (air/road/ocean) and `surface_order_weight` (Operations/Market/Research). `CREATE OR REPLACE VIEW` is idempotent and byte-identical to the live def, so applying is a no-op. **APPLIED 2026-07-25** via apply_migration (no-op, byte-match). The `acquisition_backlog_v` entry is removed from the schema-drift allowlist the same commit — its committed CREATE now exists, so the nightly data-audit `schema-drift` ERROR line clears on the next run. Reversible (`DROP VIEW public.acquisition_backlog_v`). |
| 304 | 304_source_sitemap_coverage.sql | Migration 304 (lane SITEMAP-3, 2026-09-04). Five nullable, additive columns on `sources` answering the operator's own backfill ask ("do you do mapping of the sites and store them in supabase … you will have to backfill sources with sitemap info"; "did you do mapping of rss feeds and save them in supabase?"): `sitemap_url text`, `sitemap_last_walked_at timestamptz`, `sitemap_url_count int`, `sitemap_walk_outcome text` (CHECK: walked / no_sitemap / bot_wall / feed_only / unfetchable), `feed_last_probed_at timestamptz`. `sitemap_last_walked_at` is stamped on EVERY sitemap-walk attempt (success or not) — the resumability clock the new `--walker sitemap --all-hosts` mode (`run-source-sweep.mjs`, same lane) orders its host selection by (never-walked hosts first, then oldest `sitemap_last_walked_at`), turning a 2,563-source backfill into ~17 dispatches at 40 hosts/run (see that function's own `DEFAULT_MAX_HOSTS` comment for the arithmetic). `sitemap_url`/`sitemap_url_count` are written only on a successful sitemap walk (never cleared by a non-sitemap outcome — a `feed_only` walk didn't determine anything new about a prior recorded sitemap). Written by `buildSitemapCoveragePatch` (`fsi-app/scripts/turns/run-source-sweep.mjs`) through `db.mjs`'s guarded path on every apply-mode sitemap walk; dry mode writes nothing. Two partial indexes (`status='active'`) for the ordering query and the outcome breakdown `--walker sitemap --check-coverage` reports. Additive, no default (2,563 existing rows have no coverage to assert) — same NULL-is-honest convention as migration 288's `sources.source_type`. Registered in `fsi-app/docs/inventories/shared-dataset-ownership.md` (one-writer rule: `run-source-sweep.mjs` is the sole writer of all five columns). **WRITTEN, NOT APPLIED** — Supabase MCP is read-only for this lane; self-check SQL confirming none of the five columns pre-existed is in the migration file's own header, run read-only 2026-09-04; the coordinator applies via Supabase MCP after landing. Reversible (`DROP COLUMN` x5, drops the CHECK + two indexes with their columns). |
| 305 | 305_workspace_intelligence_listings_domain_param.sql | Migration 305 (lane PERF-11, 2026-09-04). Adds one trailing, optional parameter `p_domain integer DEFAULT NULL::integer` to `get_workspace_intelligence_listings(p_org_id uuid)` (migration 066, last body-patched by 272), filtering `WHERE (p_domain IS NULL OR ii.domain = p_domain)` ahead of the RPC's existing `ORDER BY` (CASE effective_priority, added_date DESC, id ASC — unchanged). Fixes a live, measured leak: simulating the RPC's own ordering against the live corpus, only 39 of the first 60 rows (65%) it returns for a real org are domain=1 (Regulations) — the rest are Tech/Regional/Market/Research rows that `/regulations` was fetching and (pre-this-lane) rendering into `RegulationsLedger` unfiltered. `CREATE OR REPLACE FUNCTION` with a NEW parameter list creates a second overload rather than patching the existing one in place — PERF-MERGE's correction (2026-09-04, `d1d881f9`) DROPs the stale 1-arg overload in the same transaction (a bare `{p_org_id}` PostgREST call otherwise 300s "could not choose the best candidate function" between the two overloads) and re-states its explicit grants on the surviving 2-arg signature. Guarded `DO $$ … $$` block pins the live pre-patch body by md5 (`6e329b1f407a0e14ee19596b26eb3198`, confirmed live via read-only `pg_get_functiondef` 2026-09-04), aborts loudly on drift, and is idempotent by arity (checks for a 2-arg overload before patching, not a `LIKE` match over one `INTO` row). No manual activation gate: `fsi-app/src/lib/supabase-server.ts`'s `fetchWorkspaceResources` tries the domain-scoped call first only when a caller asks for one (`page.domain` set + `DOMAIN_SCOPED_RPCS.has(rpcName)`), and falls back to the unscoped call on ANY error from the scoped attempt (signature-mismatch before this migration applies, or any other failure) — so `/regulations` never renders empty in the window before the coordinator applies this. **APPLIED LIVE 2026-09-04 19:55 UTC** (one 2-arg overload, ACL intact) — see migration 306 below for the apply-order note against the org-independent siblings. Reversible (`CREATE OR REPLACE FUNCTION` back to the pinned pre-patch body, restoring the original 1-arg signature; the 2-arg overload's grants would need to be dropped separately). |
| 306 | 306_public_workspace_intelligence_listings.sql | Migration 306 (lane PERF-10, 2026-09-04 — operator ruling verbatim: "clicking into any item or any page takes WAY too long. multiple seconds. every click should show items on a page instantly."; ADR-026's own Follow-up section named this the deferred fix). **Renumbered from 305 by the PERF-MERGE lane** (2026-09-04): PERF-10 and PERF-11 both authored a migration numbered 305 in parallel worktrees off the same train-43 base; PERF-11's 305 (the domain-param patch, row above) applied live first (19:55 UTC), so this file — a disjoint, unrelated migration (CREATEs five new functions; 305 only patches an existing one) — is renumbered to the next free slot rather than colliding. Five new org-independent RPCs mirroring five existing org-parameterized ones byte-for-byte minus the org join/guard: `get_workspace_intelligence_slim_public()` / `get_workspace_intelligence_listings_public(p_domain integer DEFAULT NULL, p_after_priority text DEFAULT NULL, p_after_added_date date DEFAULT NULL, p_after_id uuid DEFAULT NULL)` (counterparts to `get_workspace_intelligence_slim`/`_listings`, migrations pre-existing/272/303/305) and `get_market_intel_items_public()` / `get_operations_items_public()` / `get_research_items_public()` (counterparts to the three category-routed RPCs). All five `PERFORM public._assert_org_membership(p_org_id)`-gated originals raise on a NULL org_id (verified live, this lane, via `pg_get_functiondef` for all six affected functions including `_workspace_active_items`) — since the org_id feeding them comes from `resolveOrgIdFromCookies()` (a Dynamic API), calling them forced `/regulations`, `/market`, `/operations`, `/research` to build `ƒ` (Dynamic) even after every other cookie read on those pages was removed. The five new functions take no `p_org_id`, call no membership check, and use no `workspace_item_overrides` join (`effective_priority`/`effective_archived` collapse to the item's own `priority`/`is_archived` — the "no override" default); the per-org override merge moves client-side to `src/stores/resourceStore.ts`'s existing `mergeWithOverrides`, fed by a separately-fetched overrides map. Measured, not assumed: verified live that zero rows exercise the one edge case this simplification drops (an org override un-archiving a globally-archived item) before accepting the simplification — self-check SQL is in the migration file's own header. **RECONCILE fold-in (PERF-MERGE + PERF-12 lanes, 2026-09-04)**: `get_workspace_intelligence_listings_public` carries (1) the SAME optional `p_domain` migration 305 gave its org-scoped sibling, and (2) PERF-12's own keyset-cursor triple (`p_after_priority`/`p_after_added_date`/`p_after_id`, one OR-chain WHERE over the mixed-direction sort — see the migration file's own header) so `/api/listings/cursor` (the scroll-pagination route) can page this SAME org-independent, cacheable RPC instead of the org-scoped one — PERF-12's own migration that added the cursor triple to the ORG-scoped `get_workspace_intelligence_listings` (originally filed as its own 306) is DELETED, not applied: after routing the cursor route through this public RPC, nothing calls the org-scoped listings RPC with a cursor (see the ADR-027 reconciliation report), and shipping it would have left the org-scoped function with two live overloads (2-arg from 305, 5-arg from the deleted file) — the exact PostgREST-ambiguity defect the corrected 305 exists to prevent. Consumed by `src/lib/data.ts`'s `getPublicResourcesOnly`/`getPublicListingsOnly`/`getPublicMarketIntelItems`/`getPublicOperationsItems`/`getPublicResearchItems` and `/api/listings/cursor/route.ts`, each wrapped in one shared `unstable_cache` entry (no org_id in the cache key) tagged `PUBLIC_ITEMS_TAG`, revalidated at the same population/maintenance apply completion point ADR-023 already names for `APP_DATA_TAG`. **APPLIED LIVE 2026-09-04 22:52 UTC** by the coordinator via Supabase MCP before train 44 merged (DDL before code): all five functions present with exactly one overload each, anon/authenticated/service_role EXECUTE, and the file's own post-check passed live (row-count parity, category partition, domain=1 strict subset, keyset step returns exactly one fewer row). Was: written, not applied — the lane's Supabase MCP was read-only (305 is already live and this migration does not depend on it — the two touch disjoint functions — but if 306 is ever rolled back and reapplied, apply it after 305 so the two `p_domain` signatures read as one consistent design), then the post-check block verifies the live result. Reversible (`DROP FUNCTION` x5, no dependents at creation time). |
| — | (number 307 first drafted for a custom-access-token org-claim hook migration, withdrawn before it existed on master, never applied — see below for the number's actual claim) | PERF-12 authored a migration numbered 307 (ADR-027 §5 "D5: JWT custom claim via Supabase Auth Hook" — a `custom_access_token_hook(event jsonb)` Postgres function for a Supabase Auth Hook that would inject `org_id` into the access token). Deleted during the PERF-12/PERF-MERGE reconciliation (2026-09-04): PERF-12 shipped the hook migration and its client-side counterpart (`X-Org-Id` request header, bootstrap `orgId` field, a route-side verification check) but **no reader** — nothing in the app ever reads `claims.org_id` off the JWT, and the ONLY consumer of the bootstrap `orgId` field (the `X-Org-Id` header) was deleted in the same reconciliation once `/api/listings/cursor` stopped being org-scoped (see migration 306's row above). Per CLAUDE.md's no-dead-code rule, a migration with no reader is not shipped speculatively. **ADR-027 D5 stays this train's OPEN item** — ADR-027 §5 itself (`docs/decisions/ADR-027-standard-fast-page-architecture.md`) is the record of the deferred "org id in a JWT claim, collapse the auth+org round trip into one" design; a future lane that wants it starts from that ADR section, not from an orphaned migration file with no code ever built against it. |
| 307 | 307_item_forward_events_text_identity_dedupe.sql | Migration 307 (lane FE-DEDUP, 2026-09-04 — coordinator dispatch, root cause `[CONFIRMED]` via Supabase MCP 2026-09-04 23:22 UTC: `public.obligations` had 1,149 rows for only 562 distinct `(intelligence_item_id, event_kind, due_date)`, 359 duplicate `item_forward_events` groups). Root cause: migration 275's dedupe key — `(intelligence_item_id, event_date, event_kind, md5(obligation_text), coalesce(source_claim_id, source_section_id))` — discriminates on the source OBJECT as well as the text, so a claim-backed row and a section-backed row from the SAME extraction run with byte-identical `obligation_text` (necessarily different source object ids — one references `section_claim_provenance`, the other `intelligence_item_sections`) hash to two different keys and both insert cleanly; example cited by the coordinator, item `02470d94-abe6-4645-8f5e-6ae421f29393`, events `a4ad1ce7-…` (section) / `ca126684-…` (claim), both `obligation_text` "…entered into force on 14 April 1967…" (37 chars). This is compounded by (not caused by) a separate extractor bug fixed the same lane in `src/lib/forward-events/extract-forward-events.mjs` (`sameObligationContent`'s `DEDUPE_MIN_COMPARE_LEN` 40-char floor was wrongly applied even to an exact full-string match — see that file's own "SHORT-TEXT EXACT-DUPLICATE FIX" header; `EXTRACTOR_VERSION` `fe1-2026-09-04.5` → `.6`), which is why the extractor's own within-run dedupe never caught these particular 70-of-359 short-text groups at extraction time either. THE FIX: drops the `coalesce(source_claim_id, source_section_id)` term entirely — new index `uq_item_forward_events_text_identity` on `(intelligence_item_id, event_kind, event_date, md5(obligation_text))`, replacing `uq_item_forward_events_dedupe`. A genuinely distinct obligation sharing item/date/kind still coexists freely (Euro 7's 40-event phase-out schedule, NZIA's four distinct 2030-01-01 targets — migration 274/275's own preserved design intent), since its text differs. TWO-TRACK ORDERING (CLAUDE.md rule 3): this is schema DDL, but it can only be applied AFTER the one-time data cleanup removes the live duplicate rows — `CREATE UNIQUE INDEX` cannot build over rows that violate the target uniqueness. The cleanup is `scripts/maintenance/forward-events-retext.mjs --apply` (same lane): its `duplicate_groups` finding, previously report-only, now auto-deletes each group's section-backed `would_drop_id` via a guarded, cited (`DUPLICATE_CITE`), snapshotted, reversible delete (`obligations.forward_event_id` `ON DELETE CASCADE`, migration 290, removes the corresponding `obligations` row automatically — no second writer). Measured over the live corpus by this lane (real, unmodified `dedupeEvents`/`sameObligationContent` run directly against a full snapshot, read-only): with the extractor fix applied, 296 of 1,152 `item_forward_events` rows drop (zero remaining duplicate groups under the new key), so `obligations` moves **1,149 → 853** — not the naive 562 a bare `(item, event_kind, due_date)` group-count floor would suggest, since that floor would also collapse the genuinely-distinct multi-event cases above. Migration file carries a pre-check `DO` block that counts remaining duplicate groups under the new key and `RAISE EXCEPTION`s (naming the exact remediation command) if any remain, so applying out of order fails loudly rather than corrupting anything; a post-check confirms the old index is gone and the new one exists. `apply-extraction-output.mjs`'s own client-side `dedupeKey` (its pre-insert idempotency check) is updated the same lane to mirror this new key. **WRITTEN, NOT APPLIED** — Supabase MCP is read-only SELECT for this lane; the coordinator applies it via Supabase MCP AFTER dispatching and confirming the `forward-events-retext.mjs --apply` cleanup. Reversible: `DROP INDEX uq_item_forward_events_text_identity; CREATE UNIQUE INDEX uq_item_forward_events_dedupe ON item_forward_events (intelligence_item_id, event_date, event_kind, md5(obligation_text), coalesce(source_claim_id, source_section_id));` (restores migration 275's index; the deleted duplicate rows themselves are NOT restored by this reversal — see `forward-events-retext.mjs`'s own `--arg restore:<id,...>` for that). |
| 308 | 308_spec09_org_scope_and_pool_drop.sql | Migration 308 (lane SPEC09-B, 2026-09-05 — workstream W5.1, "spec-09 panels, the customer-data half": `docs/plans/complete-system-build-plan-2026-09-04.md`; `docs/specs/09-domain-extensions.md`). Root cause `[CONFIRMED]`, this lane, reading migrations 296-298 directly: `surcharge_audits`, `tce_data_quality`, `auxiliary_energy_profiles`, `eudr_plot_claims`, `custody_chains`, `indexation_clauses`, and `carrier_compliance_pools` were all created `SELECT TO authenticated USING (true)` — every signed-in user of ANY org could read every other org's rows. That posture was harmless while the tables were schema-only and 0-row; this lane wires the first real writer (a workspace CSV upload route, `POST /api/workspace/spec09-upload`, plus five refactored CLI producers sharing one parser, `src/lib/spec09/csv-upload-contract.mjs`) for genuinely customer-supplied operational data (billed invoices, DQI evidence, auxiliary loads, EUDR plot claims, custody certificates, contract indexation terms) — none of it is shared platform intelligence, so it cannot go live still world-readable-to-authenticated. THE FIX, six tables: adds `org_id uuid REFERENCES organizations(id) NOT NULL` (added nullable then set NOT NULL as a separate statement, safe only because of the 0-row precondition below) plus an index; replaces each table's read policy with `CREATE POLICY <table>_org_read ... USING (public.user_belongs_to_org(org_id) OR auth.role() = 'service_role')` — the identical pattern migration 077 already uses for `org_watchlist`, reusing migration 006's own `SECURITY DEFINER` function rather than inventing a second org-membership check. No write policy is added (service_role bypasses RLS; both the upload route and the CLI producers write via the service-role client). `oem_tech_roadmaps` and `reroute_events` are explicitly UNCHANGED — they stay platform-shared per spec 09 §1.1/§1.7's own sourcing model, not a customer upload. `carrier_compliance_pools` is DROPPED outright (with `surcharge_audits.pool_id`/`.pool_adjusted_eur` and their CHECK constraint): spec 09 names Market as its only possible reader, migration 296's own header already states `pool_adjusted_eur` is deliberately never populated (`poolAdjustedGuard()`, spec 09 §5 open decision 1 "stays unmade"), and this lane's brief requires either building the reader or dropping the table with 0 rows confirmed — building a UI element for a value the calculator layer refuses to surface would just move the unmade operator decision into a new, useless panel; `variance_eur` (billed vs statutory, the always-renderable sentence `surcharge_audits` exists to carry) is untouched. GUARD: a precondition `DO` block re-confirms live 0 rows on all ten spec09 tables (`RAISE EXCEPTION` naming the exact table and count if any is nonzero) before touching anything, so an out-of-date apply fails loudly rather than silently dropping data or breaking a NOT NULL backfill. POST-CHECK, per rule 15 (attack, don't assert): the migration's own final block runs a live ADVERSARIAL RLS PROOF — creates two throwaway orgs and two throwaway users via `gen_random_uuid()`, inserts one `surcharge_audits` row for org_a, then twice does `SET LOCAL ROLE authenticated; PERFORM set_config('request.jwt.claims', json_build_object('sub', <user>::text)::text, true)` to impersonate org_a's member (must see 1 row) and then org_b's member (must see 0 rows, `RAISE EXCEPTION` on any leak) before `RESET ROLE`, then deletes the test rows/memberships/orgs and re-confirms 0 rows at rest — the same `auth.uid()` mechanism this lane confirmed live via read-only `pg_get_functiondef` before writing the test. Four spec09 panels (`SurchargeAuditPanel.tsx`, `AuxiliaryEnergyPanel.tsx`, `DqiPanel.tsx`, `EudrCustodyPanel.tsx`) were found reading these tables with an UNSCOPED service-role query (no `org_id` filter at all) — a real, in-scope cross-org leak the moment this migration's `org_id` columns get populated — and were fixed the same lane to filter `.eq("org_id", orgId)` from `resolveOrgIdFromCookies()`; a new `IndexationPanel.tsx`/`IndexationPanelView.tsx` (Market) is org-scoped from creation. Registered in `fsi-app/docs/inventories/shared-dataset-ownership.md`. **WRITTEN, NOT APPLIED** — this lane's Supabase MCP access is read-only SELECT (used only to confirm the 0-row precondition and to introspect `auth.uid()`/`user_belongs_to_org` live before writing the guard and the adversarial test); the coordinator applies via Supabase MCP after landing. One apply-order note for the coordinator: the adversarial-proof block's own `INSERT INTO surcharge_audits` uses placeholder `corridor_id`/`carrier_id` values (`cl:corridor:0000000000000308` / `cl:organisation:0000000000000308`) that migration 296's FK to `entities(entity_id)` may reject if no such rows exist live — if that INSERT fails on apply, substitute any live corridor/carrier `entity_id` pair from `entities` before re-running; this does not affect the org-scoping DDL itself, only the self-test's own fixture row. Reversible: re-add `carrier_compliance_pools` from migration 296 (DDL only, 0 rows lost since none existed), re-add `surcharge_audits.pool_id`/`.pool_adjusted_eur` + CHECK, `DROP COLUMN org_id` on all six tables, and restore each table's original `<table>_read ... USING (true)` policy (migrations 296-298 have the exact original policy text). |

## Maintenance trigger

Any commit that adds or removes a migration MUST update this inventory. C3 consistency check (rule 014) enforces drift on push.

## Records-truth corrections (Wave-α Track B8, 2026-07-11)

Applied-migration status is authoritative from `supabase_migrations.schema_migrations` + this inventory,
NOT from the FILE header comments (applied migrations are immutable — standing rule 1 — so their header
comments are frozen and can go stale). The following file headers are STALE-BUT-FROZEN; the correct status:

- **101** — file header says "PROPOSED, NOT APPLIED", but `intelligence_items_domain_backfill_audit` is live
  with 212 rows and version 101 is ledgered → **APPLIED**. (The row above @101 predates this correction.)
- **149** — file header says "NOT YET APPLIED", but verified severity is ~100% and version 149 is ledgered → **APPLIED**.
- **152** — file header says "schema home only … NOT applied", but `state_cost_facts` is live (13 rows) and
  version 152 is ledgered → **APPLIED**. (Header also mislabels itself "Migration 151" — cosmetic, frozen.)
- **153** — file header says "COMMITTED, NOT APPLIED", but `community_post_signoff_requests` is live and
  version 153 is ledgered → **APPLIED**. (Header first line mislabels "151_…" — cosmetic, frozen.)
- **157** — file header says "AUTHOR-ONLY in this commit", but 157 is APPLIED + verified 2026-07-07 (the
  157 row above already records this). Frozen header, not re-edited.

Out-of-band live objects (4 `_pre_phase5` tables + 7 mig-009 capture-table policies) are registered in
[out-of-band-objects](./out-of-band-objects.md).

## Source files

- Migration files: `fsi-app/supabase/migrations/*.sql`
- Naming: `NNN_descriptive_name.sql` (3-digit zero-padded; F6 fitness function enforces)

## Related

- [sprint4-dataops-ledger](../runbooks/sprint4-dataops-ledger.md) — The ledger records apply scripts for migrations 118/119/121 and flagged 117/118 as missing from this inventory — shared Sprint 4 provenance…
- [deletion-reclassification-log](../ops/deletion-reclassification-log.md) — RECLASSIFY→source entries run through the migration-135 source-registration guard and migration-019 mistyped-tool reclassification — the DB…
- [ADR-002-tier-model](../decisions/ADR-002-tier-model.md) — shared migration 090 (Q2 tier schema split / tier->base_tier rename) tracked in migrations inventory
- [multi-tenant-foundation-followups-2026-05-15](../ops/multi-tenant-foundation-followups-2026-05-15.md) — Migrations 075/076/077 (multi-tenant) plus the unapplied 048/050 flagged in the followups doc are the same migration set
- [INTEGRITY-TRIAGE-PROCEDURE](../runbooks/INTEGRITY-TRIAGE-PROCEDURE.md) — The whole procedure consumes the migration-035 (agent_integrity_flags) trigger output on intelligence_items — that migration defines the flag…
