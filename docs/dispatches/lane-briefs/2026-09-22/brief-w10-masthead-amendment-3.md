# Lane W10-Masthead, Amendment 3 (coordinator, 2026-09-23): the last two items. Read after Amendment 2; this file wins.

State: `lane/w10-masthead`, head `c7df1a55` (three commits), worktree `C:/Users/jason/dotfiles/.worktrees/wt-landdocs-0911`, not pushed. Accepted: `AppShell` frame is now a CSS grid (`252px minmax(0,1fr)`), `Sidebar`'s fixed width removed, mobile collapse query added; no overflow at 375 and 1024.

1. **Content column must measure exactly what the artboard states.** The lane reported "about 780" at 1440 against the artboard's 778. "About" is not a measurement. Mount at 1440 with the real fonts (the guard's harness), read `getBoundingClientRect().width` of the content column to one decimal, and report it with the rail and gap widths. If it is not 778.0, find the 2px (a border, a gap, a padding, a scrollbar gutter) at the cause in `AppShell` or `PageFrame` and fix it. If the artboard's own tracks do not sum to 778 at 1440, STOP with that arithmetic.
2. **The `/community@1024` L10 finding: FIX IT at the part.** The lane proved it also fails on clean master (`ddc5ccda`) and traced it to `CommunityRooms.tsx` (the "you are here" marker, `youHere`), a file this lane already edits. Amendment 2 said: a master defect inside a part this lane touched is fixed here. It is data-driven [the lane's finding], which also explains why CI passed: CI's guard has no database credentials, so the marker never renders there, and this PC's guard reads live data. Fix the overlap in the part so it cannot occur for any data (the marker's box must not intersect its siblings at any width in the guard's list). Then ADD a fixture to the guard that renders the room list WITH the marker (frozen real record) so CI measures this case too. Report the L10 line before and after.
3. Then, each only when `tasklist | grep -ic node.exe` prints 3 or fewer: the real guard twice (PASS both), the npm-glob suite with CI's shared script, `tsc`, the FULL fitness runner (0 violations), restore `coverage-report.json` if dirtied, the locked gate once (`bash "C:/Users/jason/AppData/Local/Temp/claude/C--Users-jason/fddbeade-7f79-480a-9254-e8fdb3278567/scratchpad/lane-prepush-check.sh" "C:/Users/jason/dotfiles/.worktrees/wt-landdocs-0911"`). A child crash (exit 3221225794 or `signal 9`) is contention: wait and re-run that step once. Update `pr-w10-masthead.md` in the coordinator scratchpad. Commit with your own session's attribution trailer. Do the work yourself; do not spawn agents; do not push.

ONE final report, six lines maximum, sent once, no interim messages: commit shas; content column width at 1440 to one decimal (and the fix, if any); the L10 line before and after and the new fixture's name; the guard's two results quoted; npm totals, fitness violations, gate exit code; any STOP.
