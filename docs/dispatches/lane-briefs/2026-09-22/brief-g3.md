# Lane G3: the Rendering guard measures the site's real fonts, on every platform (coordinator, 2026-09-22)

Model: Sonnet. Worktree: `C:/Users/jason/dotfiles/.worktrees/wt-landdocs-0911`. Branch: `lane/g3-guard-real-fonts`, cut from `origin/master` (at or past `af898bc2`). Read `docs/dispatches/lane-common-contract.md`, then ONLY `fsi-app/.discipline/rendering/smoke/smoke-fixtures.mjs` (`fullAppCss`), the browser setup under `fsi-app/.discipline/rendering/browser/`, `run-rendering-guard.mjs` where it opens pages and measures, `fsi-app/src/app/layout.tsx` where the fonts are imported, and the CI step in `.github/workflows/discipline.yml` around line 456. STOP on anything not covered. Invariant id assigned: RD-80. Fitness id, if a gate is needed: F55.

## The finding [CONFIRMED, read-only diagnosis 2026-09-22]

The guard never loads Anton or Plus Jakarta Sans. `fullAppCss()` reads `globals.css` and `theme.css` only; the fonts are `@fontsource/*` imports in `layout.tsx`; no `@font-face` reaches the fixtures, no network font is fetched, nothing awaits `document.fonts.ready`. A live probe on this PC: `document.fonts.size === 0`, both font stacks resolve to the same OS fallback (106.5px for a probe string). CI (Ubuntu) falls back to different fonts than this PC (Windows), so the same commit gives 9 failures on CI and 118 here, with only six in common. Every text-dependent measurement the guard has ever made (clipping, wrapping, intersecting elements, hit targets, the 21c card heights) was made against the wrong typography, on both platforms. Operator ruling 2026-09-22: Playwright is installed locally and every UI lane runs the guard locally before pushing; that is only worth anything if the guard measures the real fonts and gives the same answer everywhere.

## Build

1. Vendor the exact font files the app ships: read `layout.tsx` and `package.json` for the `@fontsource` packages and weights in use; copy the corresponding `woff2` files (they are already in `node_modules/@fontsource/...`) to `fsi-app/.discipline/rendering/fixtures/fonts/`, only the weights and subsets the app imports (latin), with each package's license file beside them. Tracked in git (they are small; state the total size in the report).
2. `fullAppCss()` emits `@font-face` rules for those files (absolute `file://` or the harness's own served path, whichever the harness already uses for its fixtures; `font-display: block`) BEFORE the app CSS, so the declared stacks resolve to the real faces. One home; no smoke spec adds its own.
3. Every measurement pass awaits `document.fonts.ready` and then asserts `document.fonts.check(...)` is true for both families at the weights used; a failed check is a guard ERROR naming the family (never a silent fallback). Put this in the harness, once, where pages are opened.
4. Determinism knobs, set explicitly in the browser context (item (e) of the diagnosis): `deviceScaleFactor: 1`, `locale: 'en-US'`, `timezoneId: 'UTC'`, `colorScheme: 'light'`, headless. Same values in CI and locally by construction (they live in the harness, not the workflow).
5. RD-80 as one file under `invariants.d/`: "the Rendering guard measures with the application's own font files loaded and ready; a run whose fonts did not resolve is an error, never a pass". Enforcer: a test that mounts one fixture through the harness and asserts the resolved font family and a known glyph width (a number you MEASURE after item 2, recorded with the method), plus the `fonts.check` error path proven red then green.
6. Measure the tree. Run the full guard locally on `origin/master` (your branch before any smoke spec changes) with real fonts and write the complete failure list to `docs/audits/rendering-guard-real-fonts-2026-09-22.md` grouped by fixture and detector class, every line `[CONFIRMED]` (measured by the guard, method named). Do NOT fix those failures and do NOT allowlist, skip or loosen anything: they are findings for the part lanes that own each surface. If the list is empty, say so. This audit file is coordinator-only territory for editing later, but you CREATE it (F51 check 4 forbids a lane editing `docs/audits/`; if the gate refuses the new file, write it to `docs/ops/session-log.d/2026-09-22-g3-audit.md` instead and say so).
7. The CI step stays as it is (same command); only the harness changed. Run `actionlint` only if it is on the PATH.

## Gates

- `npm ci` and `npm install --no-save playwright@1.61.1 --no-audit --no-fund` in the worktree's `fsi-app` (chromium is cached). Run the guard locally until it is deterministic (two consecutive runs, identical output). Every npm suite with CI's shared script, `tsc`, the FULL fitness runner (all functions) to 0 violations, restore `coverage-report.json` if dirtied, then the locked push gate once as one background task (silence is normal; `signal 9` is contention, wait and retry once).
- First tool calls: Skill tool, `fsi-app:environmental-policy-and-innovation`, then `remediation-discipline`. Never `git stash`, never `git add -A`, never `--no-verify`. Commit trailer exactly `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`. No em dash, en dash or section sign in new prose. F27 (a new first-party import owes a composition proof), F25, F45 bind. F51 check 5: count touches with the window method; a non-entry-directory file at two is a STOP with its name. No workaround. You do not push.

## Report

ONE final report, seven lines maximum, sent once, no interim messages: commit sha; the fonts vendored (families, weights, total KB); the measured glyph width used as the RD-80 pin and the method; the guard's result on master with real fonts (total failures, by class, and the audit file path); whether two consecutive runs matched; npm totals, fitness violations, gate exit code; any STOP. Write `pr-g3.md` into `C:/Users/jason/AppData/Local/Temp/claude/C--Users-jason/fddbeade-7f79-480a-9254-e8fdb3278567/scratchpad/` (the coordinator's scratchpad, `C--Users-jason`, not your own): first line `## Lane G3: the Rendering guard measures the site's real fonts on every platform (RD-80)`, then `## Summary`, `## Evidence`, `## UX compliance` ("Not a UI change; the guard's own harness."), last line `🤖 Generated with [Claude Code](https://claude.com/claude-code)`.
