# Lane W10-ActionCard-b: wire ActionCard, Timeline and SectionIndex into the regulation surface (coordinator, 2026-09-22)

Model: Sonnet. Worktree: `C:/Users/jason/dotfiles/.worktrees/wt-l34-detail-admin-primitives` (has node_modules and Playwright). Branch: `lane/w10-actioncard-b`, cut from `origin/master` AFTER PR #777 (FactCard-d) has merged; confirm with `git log origin/master --oneline | grep -i "FactCard-d"` before cutting; if it is not there, STOP. Read `docs/dispatches/lane-common-contract.md`, `docs/design/ux-laws.md`, `docs/design/design-principles.md`, parts-brief section 1 and 2.6, the operator's review quoted in `docs/dispatches/lane-briefs/2026-09-21/brief-w10-actioncard-a.md`, and part A's to-do list in `docs/ops/session-log.d/2026-09-21-w10-actioncard-a.md` under "Part B's exact to-do list". Open the artboard picture (`docs/design/handoff-2026-09-07/screens/21-detail-parts.png`, panels 21a and 21b) with the Read tool before editing the surface. STOP on anything not covered.

## Operator rulings that bind this lane, verbatim (2026-09-22)

"You don't limit the number of characters in an analysis of something just because it doesn't flow you adjust how the pages set up. We need all of the text and all of the information from the analysis." Binds: no analysis text is ever cut. The review's 3-line clamp on EXPOSURE cells means the cell shows the first three lines with the rest reachable in place (an expand on the cell, or the full sentence in S1 as the review routes the trajectory), never a truncation. Ellipsis only on chrome (index tabs, sub-labels). Summary mode is a shorter VIEW; Full brief shows every section in full.

## Build, in part A's order

1. `RegulationDetailSurface.tsx`: replace `DetailHeader` + `DetailExposure` + `DetailTimeline` with ONE `<ActionCard>` (panel 21b: pill row with meta right-aligned; action row; rule; EXPOSURE; rule; TIMELINE; callout). No "workspace tags" label when there are no tags; no overflow control after the pill. Delete the three replaced components if nothing else imports them (grep first; F45 must fall, never rise).
2. Section order on every regulation from `REGULATION_SECTION_INDEX` (`src/lib/detail/section-index-data.ts`): Summary, Obligations, Requirements, Registration, Operations, Compliance, Penalties, Sources. The section header text stays the full name; the index shows the short name.
3. Summary depth shows S1 plus the first ItemGroup of S2; every other section renders at Full brief. The switch lives in the index bar (part A built it); the standalone switch row is removed.
4. The "WHAT CHANGED" block: grep `RegulationDetailSurface.tsx` and what it renders for the batch-id literal (`record-briefs-`) reaching the DOM; show the change sentence the data carries, or omit the block when there is none. Never print an internal id.
5. The trajectory sentence moves to S1 Summary in full; `ActionCard`'s fourth EXPOSURE cell is NEXT MILESTONE computed from the timeline prop (part A). No cell shows caps body text for absence; the Absence convention applies.
6. Part A's two smoke specs come off the F25 allowlist (commit `ce683020` added them "pending Part B's registration"): register them properly; the allowlist entries are deleted in this lane.
7. Presence report (route, file, line) for ActionCard, Timeline and SectionIndex; the "UX compliance" block; both in `docs/ops/session-log.d/2026-09-22-w10-actioncard-b.md`. Add the two fixture pages to the `/admin/parts` index.
8. Acceptance, measured by the real guard locally at 1440 (and 375 for overflow): masthead plus one action card plus index visible above 1080px on the review's record; zero EXPOSURE cells taller than three lines without an expand affordance; every timeline dot has a visible label; no index label with `scrollWidth > clientWidth`; no standalone switch row; one card border box for the action card. Add these to the UX smoke slot for the regulation surface fixture.
9. Find the exact record the operator reviewed by a read-only SELECT (next milestone "Transition deadline" on 2026-09-29, 7 milestones, 3 passed; regulation) and put its slug in the report so it can be re-sent after deploy.

## Gates

`npm install --no-save playwright@1.61.1 --no-audit --no-fund` in the worktree's `fsi-app` if `npx playwright --version` fails. The real Rendering guard locally (`node .discipline/rendering/run-rendering-guard.mjs` from `fsi-app`, output to a file, read selectively) to PASS twice before the gate; every npm suite with CI's shared script; `tsc`; the FULL fitness runner (all functions) to 0 violations; restore `coverage-report.json` if dirtied; the locked push gate once as one background task (silence is normal; `signal 9` is contention, wait and retry once). First tool calls: Skill tool, `fsi-app:environmental-policy-and-innovation`, then `frontend-design`, then `remediation-discipline`. Never `git stash`, never `git add -A`, never `--no-verify`. Commit trailer exactly `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`. No em dash, en dash or section sign in new prose. F42, F43, F45, F49, F36, F40 bind. No workaround. You do not push. Past about 400k tokens, commit what is green and report which items are done.

## Report

ONE final report, seven lines maximum, sent once, no interim messages: commit sha(s); items done; the guard's two results quoted; the review record's slug; npm totals, fitness violations, gate exit code; any STOP. Write `pr-w10-actioncard-b.md` into `C:/Users/jason/AppData/Local/Temp/claude/C--Users-jason/fddbeade-7f79-480a-9254-e8fdb3278567/scratchpad/` (the coordinator's scratchpad): first line `## Lane W10-ActionCard-b: the regulation surface on ActionCard, Timeline and SectionIndex (part B of 2)`, `## Summary`, `## Evidence`, `## UX compliance`, last line `🤖 Generated with [Claude Code](https://claude.com/claude-code)`.
