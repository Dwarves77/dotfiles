# Lane W10-Masthead, Amendment 1 (coordinator, 2026-09-22): rulings on the lane's open items; finish the lane. Read after section 2 and the COMMON BLOCK of `brief-w10-remaining-parts.md`; this file wins.

State: `lane/w10-masthead` in `C:/Users/jason/dotfiles/.worktrees/wt-landdocs-0911`, commit `05fd0854` (from master `ddc5ccda`): `/admin/factors` moved onto the shared `Masthead`; all eight F49 Anton entries deleted with F49 at 0; mobile top bar 59 to 56. Findings of the lane [its CONFIRMED, from reading the code]: the shared `src/components/ui/Masthead.tsx` exists and most routes already compose it (lane UILISTS, 2026-09-06). Remaining: `CommunityMasthead.tsx` wraps the legacy `EditorialMasthead` / `PageMasthead`; `AuthFrame.tsx` hand-rolls its identity panel; the content column (758 measured against 778) is unresolved; the guard, the npm-glob suite, the locked gate and the presence report were not run. You are a replacement agent; do not redo the commit.

Coordinator rulings:
1. **Auth frame: no conflict, the operator already ruled.** Bundle README ruling 3 of 2026-09-20: "/login, /signup, /onboarding keep artboards 16 to 17: no nav card, no command bar. The Masthead part IS used there (eyebrow + Anton title + dek), inside the right panel." So: the LEFT identity panel stays exactly as `AuthFrame.tsx` and artboards 16 and 17 draw it (it is not a masthead and is not migrated); the RIGHT panel (the form side) renders the shared `Masthead` (eyebrow, Anton title, dek) above the form on each of those three routes, using the texts the pages show today (no invented copy). Measured by the guard at 375 and 1440.
2. **Community: migrate onto the shared Masthead.** `CommunityMasthead.tsx` becomes a thin caller of `Masthead` (or is deleted and its callers call `Masthead` directly, whichever leaves one home); `EditorialMasthead.tsx` and `PageMasthead.tsx` are deleted when their last importer is gone (grep; F45 falls). The search element Community shows today is the shared CommandBar part (lane #769) passed through the same prop every other surface uses; if Community needs a behaviour CommandBar does not have, STOP and name it, do not build a second bar. The "Global room room" duplicate word (measured 2026-09-20) is fixed where it is printed, if it is in these files.
3. **Content column: measure, then fix at the cause.** Run the real guard or a one-off Playwright measurement script (scratch file, not committed) of `PageFrame` at 1440: report the measured widths of the frame, the nav card, the gap, the rail and the content column, and which value differs from the artboard (the bundle states 778). Fix the one that is wrong in `PageFrame.tsx` or the part that owns it; never a page override. If the artboard itself does not sum to 778 at 1440, STOP with the arithmetic.
4. **Presence report:** every route (17 routes plus `/watchlist`, `/privacy`, `/invitations`, and `/login`, `/signup`, `/onboarding`) with the file and line that renders `Masthead`, or the reason it has none. Grep `PageMasthead|EditorialMasthead` returns nothing at the end.
5. Fixture `/admin/parts/masthead` from the frozen real record, including the auth-frame variant.
6. Gates, in order, each started only when `tasklist | grep -ic node.exe` prints 3 or fewer: the real guard twice (PASS both), the npm-glob suite with CI's shared script, `tsc`, the FULL fitness runner (0 violations), restore `coverage-report.json` if dirtied, the locked gate once (`bash "C:/Users/jason/AppData/Local/Temp/claude/C--Users-jason/fddbeade-7f79-480a-9254-e8fdb3278567/scratchpad/lane-prepush-check.sh" "C:/Users/jason/dotfiles/.worktrees/wt-landdocs-0911"`). A child crash with exit 3221225794 or `signal 9` is contention: wait and re-run that step once. A second FAIL with a named test is a STOP.
7. Commit trailer: your own session's attribution line. PR body `pr-w10-masthead.md` in the coordinator scratchpad (the fixed path above's folder), first line `## Lane W10-Masthead: one Masthead part on every route, auth frame included; the eight dated F49 Anton entries cleared`.

Do the work yourself in this worktree; do not spawn agents. ONE final report, six lines maximum, sent once, no interim messages: commit shas; routes on the shared Masthead (count) and legacy files deleted; the content column measurement and the fix; the guard's two results quoted; npm totals, fitness violations, gate exit code; any STOP.
