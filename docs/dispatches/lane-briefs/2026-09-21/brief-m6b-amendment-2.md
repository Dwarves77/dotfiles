# Lane M6b, Amendment 2 (coordinator, 2026-09-21): item 6, after R7m merged. Read after `brief-m6b.md` and Amendment 1; this file wins.

State: `lane/m6b-gate-a-rescan` in `C:/Users/jason/dotfiles/.worktrees/wt-l25-slot-mirror`, head `e5fe1601` (items 1 to 5, 7, 8 built; fitness 0 violations; locked push gate passed). Lane R7m has merged: `fsi-app/.discipline/governance/loop-hops.d/` now exists on `origin/master`. You are a replacement agent. Do NOT redo or re-verify anything built.

1. `git fetch origin`, then `git rebase origin/master`. A conflict is a STOP with the file names; resolve nothing by script.
2. Confirm the directory exists. Read `loop-manifest.mjs`'s loader contract (the required fields and how `consumerPending` is expressed) and the two hop files whose `id` is `brief-apply-to-gate-a-rescan` and `population-turn-to-gate-a-rescan`. Edit ONLY those two files: `family` is `"gate-a-rescan"`, `enforceEdge` is `true`, `consumerPending` cleared in the shape the loader requires, `enforceFired` stays `false`, `note` rewritten to one sentence naming `.github/workflows/gate-a-rescan.yml` and lane M6b.
3. Run `loop-manifest.test.mjs`, the F50 test, and F50 itself on the live tree. F50 must now find the `workflow_run` edge for both hops in `gate-a-rescan.yml` and the family directory; its "hops not yet enforced" number drops by the two edge claims. If F50 refuses an edge, the defect is in the workflow's `on.workflow_run.workflows` list or its `name:`; fix it there, never in the hop file.
4. Restore `coverage-report.json` if a scan dirtied it. FULL fitness runner (all functions) to 0 violations, then the locked push gate once, as one background task; its silence for long stretches is normal. A FAIL from your change is fixed at the cause and the gate runs once more; a second FAIL on the same step is a STOP with the quoted line that starts "FAIL".
5. Write the PR body `pr-m6b.md` into `C:/Users/jason/AppData/Local/Temp/claude/C--Users-jason/fddbeade-7f79-480a-9254-e8fdb3278567/scratchpad/` (that exact path; the coordinator's scratchpad, `C--Users-jason`, not your own): title `## Lane M6b: the Gate A re-scan hop (script, workflow, family, one state writer), the last hop before proof run 6.2`; Summary of at most five bullets from the branch's own commits (`git log origin/master..HEAD`), leading with the machine (what is wired, what is gated, that nothing has fired and nothing was run); Evidence with the real numbers from your runs; `## UX compliance` with "Not a UI change."; last line `🤖 Generated with [Claude Code](https://claude.com/claude-code)`.

You do not push. ONE final report, five lines maximum, no interim messages: head sha; fitness violations; push gate result; F50's "hops not yet enforced" number before and after; any STOP with its measurement.
