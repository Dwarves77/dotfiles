# Lane F51c: check 5 measures concurrent editing, not serial touches by one owner (coordinator, 2026-09-22)

Model: Sonnet (gate logic). Worktree: `C:/Users/jason/dotfiles/.worktrees/wt-p5a-nonreg`. Branch: `lane/f51c-hotspot-concurrency`, cut from `origin/master` (at or past `af898bc2`). Read `docs/dispatches/lane-common-contract.md`, then ONLY the header comment and check 5 of `fsi-app/.discipline/fitness/functions/F51-no-shared-append.mjs` (`countHotspots`, `evaluateHotspotViolations`, `runCheck5`, `HOTSPOT_ALLOWLIST`) and the check 5 tests in its test file. STOP on anything not covered.

## The three occurrences (all [CONFIRMED] in the session log, 2026-09-19 to 2026-09-21)

Check 5 refused: (1) the lane-briefs README, appended by three serial coordinator docs PRs; (2) the ADR-031 resolver chain (`loop-run-id.mjs`, its test, one emitter), extended by lanes M3, M3b, M4, M6b, each cut after the previous merged; (3) `FactCard.tsx`, its test, `fact-card-model.ts`, its test, edited by FactCard parts 1, 2, c, d, each cut after the previous merged. In every case the lane rebased clean, no concurrent editor existed, and the answer was a dated `HOTSPOT_ALLOWLIST` entry, eleven of them now. The check measures "touched often", but the harm it exists to prevent (plan 6.8) is two lanes editing one file while both are open, which conflicts on rebase. Serial single-owner work on a part's single home (parts, not pages) is the design working, not a hotspot.

## Build

1. Define concurrency mechanically, from git alone, and prove it on both sides. Candidate the coordinator will accept if it passes the attack tests: a prior window commit T that touched file F counts against the lane's range ONLY if the lane's branch was open while T merged, i.e. T is NOT an ancestor of the lane branch's merge-base with `origin/master` at check time. Measure the merge-base BEFORE any rebase in the lane's own gate; in CI after the queue's rebase, T is always an ancestor, so read the ORIGINAL fork point from the PR's first commit's parent when `GITHUB_BASE_REF` is set (`git merge-base --fork-point` is unreliable; use the first commit of the range: `git rev-list --reverse <base>..<head> | head -1`, its first parent is the fork point). If you find this candidate cannot distinguish the three serial cases from a genuine concurrent pair, propose one that does, or STOP with the measurement. Do not weaken the threshold, the window, or the anchor.
2. Attack tests, red then green, on fixture repositories built in a temp dir (never the live tree): (a) two branches cut from the same master, both touching F, first merges, second is checked: VIOLATION; (b) three serial branches each cut after the previous merged, all touching F: PASS with zero violations; (c) the same as (b) but the third branch was cut BEFORE the second merged: VIOLATION; (d) a file at three touches from three serial lanes plus a genuinely concurrent fourth: exactly one violation naming the concurrent commit.
3. When the new definition is green, DELETE the eleven dated `HOTSPOT_ALLOWLIST` entries that exist only for serial single-owner work (all entries with `decidedOn` 2026-09-20 or 2026-09-21, and the lane-briefs README entry); keep the six "coordinator-only by contract" entries. Update the allowlist pin test to the remaining set. Re-run F51 on the live tree: it must print 0 violations with those entries gone (if it does not, the definition is wrong; go back to item 1, never re-add an entry).
4. Standing number stays: check 5 still prints the files at 3+ touches every run as information.

## Gates

- `npm ci` in the worktree's `fsi-app` if `node_modules` is missing. F51 test file, every npm suite with CI's shared script, the FULL fitness runner (all functions) to 0 violations, restore `coverage-report.json` if dirtied, then the locked push gate once as one background task (silence is normal; `signal 9` is contention, wait and retry once). A second FAIL on the same step with a named test is a STOP.
- First tool calls: Skill tool, `fsi-app:environmental-policy-and-innovation`, then `remediation-discipline`. Never `git stash`, never `git add -A`, never `--no-verify`. Commit trailer exactly `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`. No em dash, en dash or section sign in new prose. F51's own file is an entry-directory file (never a check 5 violation). No workaround. You do not push. Skill-ack files copy the exact heading shape of an existing one.

## Report

ONE final report, six lines maximum, sent once, no interim messages: commit sha; the concurrency definition in one sentence; attack test results (a) to (d); entries deleted (count) and F51 live-tree result; npm totals, fitness violations, gate exit code; any STOP with its measurement. Write `pr-f51c.md` into `C:/Users/jason/AppData/Local/Temp/claude/C--Users-jason/fddbeade-7f79-480a-9254-e8fdb3278567/scratchpad/` (the coordinator's scratchpad, `C--Users-jason`, not your own): first line `## Lane F51c: check 5 measures concurrent editing, not serial touches by one owner`, then `## Summary`, `## Evidence`, `## UX compliance` ("Not a UI change."), last line `🤖 Generated with [Claude Code](https://claude.com/claude-code)`.
