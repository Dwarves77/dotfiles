# Lane W10-CommandBar, Amendment 2 (coordinator, 2026-09-21): the column-header literal is fixed at the cause, not per spec. Read after the brief and Amendment 1; this file wins.

State: `lane/w10-commandbar`, head `d550c2d9`, worktree `C:/Users/jason/dotfiles/.worktrees/wt-l34-detail-admin-primitives`, not pushed since `89fc5671`. The previous agent found [its HYPOTHESIS, accepted as the working cause]: (a) `search-results-smoke.mjs` mounted the view with no app CSS, so the shared `ListRowColumnHeader` cells clipped; fixed by injecting `fullAppCss()` as the sibling specs do. KEEP that fix: it is a fixture defect fixed in the fixture. (b) the real header label "Tier" matches the placeholder detector's `HEADER_LITERALS`; it added `knownSafePlaceholders: ['Tier']` to the spec, as `dashboard-brief-smoke.mjs` already does. [REFUTED, the coordinator's Amendment 1 hypothesis that `/search` hand-built its header: it uses the shared part.]

Ruling on (b): REMOVE it. A per-spec safe-word list is the second occurrence of the same stop (dashboard-brief, now search-results), and every future spec that mounts the shared header would need the same edit. The cause: a genuine column-header word drawn by the ONE shared header part is indistinguishable, to the detector, from a leaked table-header literal. The precedent is FactCard part 1 (PR #763, squash `2b2ac415`): the kind word became a marked slot (`data-part-slot="kind-word"`) and the detector exempts ONLY a vocabulary word in that slot inside that part, with five attack tests. Read that change first (`git show 2b2ac415 --stat`, then only the detector and guard files it touched) and do the same here:

1. `ListRowColumnHeader` marks each label element `data-part-slot="column-label"`, and its root carries `data-part="list-row-header"` if it has no `data-part` yet.
2. The guard's placeholder leg exempts a `HEADER_LITERALS` word ONLY when it is the whole text of an element with that slot inside that part. Reuse the mechanism part 1 added; do not write a second one. `HEADER_LITERALS` itself is not edited.
3. Attack tests, red then green: the word in the slot inside the part passes; the same word in a plain `<p>`, in the slot OUTSIDE the part, and as part of a longer leaked string ("Tier | Juris. | Timeline") inside the slot all still FAIL.
4. Delete `'Tier'` (and any other column-label word) from `knownSafePlaceholders` in `search-results-smoke.mjs` AND `dashboard-brief-smoke.mjs`, and from any other smoke spec that lists a column label for the same reason (grep `knownSafePlaceholders`; enumerate first; list what you changed). An entry that exists for a different reason stays, untouched.
5. F51 check 5: if any file you must edit is already at two master touches in the window and is not an entry-directory file, STOP with the measurement.
6. npm suites touched, `tsc`, FULL fitness runner to 0 violations, restore `coverage-report.json` if dirtied, the locked push gate once as one background task (long silence is normal). Update `pr-w10-commandbar.md` in the coordinator scratchpad (`C:/Users/jason/AppData/Local/Temp/claude/C--Users-jason/fddbeade-7f79-480a-9254-e8fdb3278567/scratchpad/`, `C--Users-jason`, not your own) with two added bullets: the fixture CSS fix, and the slot fix with the specs it cleaned. You do not push.

First tool calls: Skill tool, `fsi-app:environmental-policy-and-innovation`, then `frontend-design`, then `remediation-discipline`. All standing lines bind. ONE final report, five lines maximum, no interim messages: commit sha; the specs cleaned (count and names); fitness violations and push gate result; any STOP with its measurement.
