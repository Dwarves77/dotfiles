# Lane M6b: the Gate A re-scan hop, the last hop before proof run 6.2 (coordinator, 2026-09-21)

Model: Sonnet. Worktree: `C:/Users/jason/dotfiles/.worktrees/wt-l25-slot-mirror`. Branch: `lane/m6b-gate-a-rescan`, cut from `origin/master` (at or past `b1dd38e4`). Read `docs/dispatches/lane-common-contract.md`, then plan row M6 (`docs/plans/complete-system-build-plan-2026-09-04.md` section 6.1) and sections C and D of `docs/dispatches/lane-briefs/2026-09-20/brief-m6-amendment-1.md`. Where they disagree with this file, this file wins. STOP on anything not covered.

## Premises, each checked against master `b1dd38e4` by the coordinator

- [CONFIRMED] No bulk re-scan script exists. What exists: the scanner `src/lib/agent/gate-a-scan.mjs` (`GATE_A_VERSION = "2026-09-04.1"`, `scanBrief`), the pure row builder `buildGateARow` in `src/lib/intake/write-item.ts` (lines 77 to 99), the table of migration 224 (`item_gate_a_state`, PK `intelligence_item_id`, service-role RLS).
- [CONFIRMED] The scanner has TWO coverage arms. `buildGateARow` takes `derivedCovered`; the live value is `derivedCoveredTokens(sb, itemId)` in `src/lib/agent/gate-a-derived.mjs`. A caller that omits it STRIPS Gate B coverage: this exact defect is recorded in `scripts/mint/heal-provenance.mjs` lines 432 to 464 (16 orphan tokens on 5 items). The re-scan MUST compute it fresh per item, immediately before the row is built, as `canonical-pipeline.ts` does.
- [CONFIRMED] The only upsert of the state row outside the mint path is inline in `scripts/maintenance/provenance-heal.mjs` (`readGateAState` about line 174, `upsertGateA` about line 257: `guardedUpdate` when the row exists, `guardedInsert` when not).
- [CONFIRMED] `set_provenance_status` does NOT watch `item_gate_a_state` (`scripts/mint/rederive-record-provenance.mjs` header). Status is a cache (remediation-discipline 5.6): a re-scan that changes a row's result and ends there leaves `provenance_status` stale. CLAUDE.md rule 17: a runtime that ends without triggering its downstream is a defect.
- [CONFIRMED] `Brief apply` and `Population turn` are the exact `name:` lines and are both keys of `FAMILY_BY_WORKFLOW_NAME`; `scripts/lib/loop-run-id.mjs` needs NO edit. `loop-run-id.test.mjs` carries a dated F51 allowlist entry that names this lane's one extension.
- [REFUTED, amendment 1 section D] "the same limit for the chained attach-found-sources step": `attach-found-sources.mjs` has no limit and selects nothing without a worklist file (`--arg <path>`, rows `{item_id, token, url, quote}` filled by a browser lane). Ruled in item 4 below.
- [CONFIRMED] `loop-manifest.mjs` is one touch from an F51 check 5 refusal. Lane R7m converts it to `loop-hops.d/` first. You do NOT edit `loop-manifest.mjs`. See item 6.

## Build

1. **One writer.** New `fsi-app/scripts/lib/gate-a-state-writer.mjs`: `readGateAStateRow(rc, itemId)` and `upsertGateAState(row, exists, { cite })`, the exact update-or-insert now inline in `provenance-heal.mjs`, which is changed to delegate to it (behaviour identical; its tests stay green; F45 must not rise). The re-scan imports the same two functions. Row shape ONLY from `buildGateARow`. No other code path writes the table from this lane.
2. **`fsi-app/scripts/maintenance/gate-a-rescan.mjs`** (first line: `// SHARED-WRITER: item_gate_a_state, intelligence_items`; CLI through `scripts/maintenance/lib/cli.mjs` and `isMainModule`; modes `dry` and `apply`; $0, no LLM, no fetch).
   - Selection, pure and tested: non-archived items with a non-empty `full_brief` whose state row's `gate_a_version` differs from `GATE_A_VERSION`, or that have no state row. Order: item ids the upstream run touched first (from the upstream family's committed artifact for `upstreamRunId`, when it is on the tree; when it is not, say so in `config.scope_source`), then the rest by `scanned_at` ascending. Reads page with the shared helpers (`readAll`, `readAllByIds`; F38, F39); never a bare `.in()` over a runtime list, never a `.limit()` above 1,000.
   - Bound, pure and tested, enforced in the SCRIPT: `resolveLimit({ trigger, requested })` returns 50 for `workflow_run` whatever is requested; for dispatch the default is 500 and any value above 2,000 is REFUSED with a non-zero exit and a named reason (not clamped).
   - Per item: read FACT claims (`claim_text`, `source_span`), compute `derivedCoveredTokens` fresh, `buildGateARow`, compare with the stored row. Apply mode writes through `upsertGateAState`. If `scanned_hash` or `orphan_count` changed (or the row was missing), touch the item through `guardedUpdateByIds` exactly as `rederive-record-provenance.mjs` does, then read back `provenance_status`. Per-step verification: read the state row back and require `gate_a_version === GATE_A_VERSION`; a mismatch halts the run non-zero.
   - [CONFIRMED by grep] no shared kill-switch helper exists; the chained workflows (`brief-apply.yml`, `downstream-chain.yml`) carry none, and this hop has no schedule, so rule 11's kill-switch duty does not apply. The gates are the job `if:`, the script bound, and `dry` as the dispatch default. Do not invent a kill-switch mechanism.
   - `per_item`: id, version before and after, `orphan_count` before and after, hash changed (bool), touched (bool), `provenance_status` before and after. Totals include `distinct_versions_remaining` read back from the table after the run.
3. **Family `gate-a-rescan`** by descriptor only: `scripts/harness-runs/gate-a-rescan/family.json`, `FAMILY.md`, `pending/2026-09-21-m6b.md`, PLUS `scripts/harness-runs/meta-harness/pending/2026-09-21-m6b.md` (F28). Governing files: `../.github/workflows/gate-a-rescan.yml`, the re-scan script, the writer module. One emitter `scripts/turns/emit-gate-a-rescan-artifact.mjs`, modelled on `emit-brief-export-artifact.mjs`: `resolveHarnessRunContext` and `buildRunArtifactEnvelope`, never a copy of either (F45). `config.loop_run_id` from the upstream name the event gives. The artifact records `trigger` exactly as the event gives it (`workflow_run` or `workflow_dispatch`); F50 reads that field.
4. **attach-found-sources, chained, same job, same artifact.** Ruling: add a pure, tested `filterWorklistRows(rows, { itemIds, limit })` and a `--items-file <path>` plus `--limit <n>` pair to `attach-found-sources.mjs` (one touch of that file). The chained step passes the item ids THIS re-scan left with `orphan_count > 0`, limit 50, over each committed ready worklist under `scripts/_worklists/attach-found-sources-*.json` (never `.seed.json`, never `.fixture.json`). On `workflow_run` the attach step runs in `dry` mode ONLY (it can fetch; build mode holds fetching to explicit dispatch); on dispatch it follows the `mode` input. Zero matching rows is a recorded result (`attach: { rows_offered: 0 }`), not an error. Its result is a section of the SAME artifact. If `heal-provenance.mjs`'s `main()` cannot take a filtered worklist without an edit to `heal-provenance.mjs`, STOP with the call signature.
5. **`.github/workflows/gate-a-rescan.yml`**, `name: Gate A rescan` (the manifest already names it). `on.workflow_run.workflows: ["Brief apply", "Population turn"]`, `types: [completed]`, job `if:` the upstream concluded `success` or the event is a dispatch; `workflow_dispatch` inputs `mode` (dry default), `limit`, `loop_run_id`. `CHAINED_LIMIT: 50` as a named constant. F52: workflow and job `env` use only `github`, `needs`, `strategy`, `matrix`, `vars`, `secrets`, `inputs`; anything runner-derived is set in a step through `$GITHUB_ENV`; no `schedule`; no `|| true`; no inline script longer than a call (put logic in the `.mjs`). Commit the artifact the way `brief-export.yml` does. Wiring test beside the other workflow wiring tests (triggers, names, no schedule), and `actionlint -shellcheck= -pyflakes=` clean if actionlint is on the PATH (do not install it).
6. **The two hops.** After everything above is committed: `git fetch`, rebase onto `origin/master`. If `fsi-app/.discipline/governance/loop-hops.d/` exists (lane R7m merged), edit ONLY the two hop files `brief-apply-to-gate-a-rescan` and `population-turn-to-gate-a-rescan`: `family: "gate-a-rescan"`, `enforceEdge: true`, `consumerPending` removed or false as the loader's shape requires, `enforceFired` left false, note rewritten to one sentence. If the directory does not exist, do NOT touch `loop-manifest.mjs`: finish, and report "built, hops await R7m" as your STOP line.
7. **Attack chain.** Extend `scripts/lib/loop-run-id.test.mjs` ONCE with this hop: a `Gate A rescan` firing after `Brief apply` and after `Population turn` resolves the upstream's loop id; an unknown upstream name resolves null and never invents a family; an explicit id wins.
8. F25: the script is wired by the workflow and the emitter; if F25 still reads it as unwired, fix the wiring, never the allowlist. F27: a new direct first-party import in a producer or runner owes its composition proof. F30 does not apply (no host is derived); if it does, only through `src/lib/entities/host-from-url.mjs`.

NOT in this lane: wiring a `distinct(gate_a_version) = 1` verifier into the CI data-audit lane (plan 6.3, after the 53 deferrals), running the workflow, any data pass. Do not dispatch anything.

## Standing lines (each cost a lane cycle)

- First tool calls: Skill tool, `fsi-app:environmental-policy-and-innovation`, then `remediation-discipline` (the write gate judges your own transcript, ADR-032).
- Skill-ack files copy the exact heading shape of an existing one. Never `git stash` (clean master: `git worktree add --detach <scratch path> origin/master`). Never `git add -A`, never `--no-verify`. Never run `repin.mjs`, `reseed-f45.mjs`, `repin-skills.mjs`, `resolve-conflicts.mjs`. Commit trailer exactly: `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`. No em dash, en dash or section sign in new prose.
- F51 check 5 refuses the lane whose own range makes a file's third touch in the window: restructure or STOP, never allowlist. An amendment to this brief will be a NEW file.
- A module the Next app imports never imports from `fsi-app/scripts/`. This lane's script imports FROM `src/` (allowed); nothing under `src/` may import the new script or writer.
- `coverage-scan.mjs` rewrites the tracked `fsi-app/.discipline/governance/coverage-report.json`; restore it with `git checkout --` before the gate.
- Run the FULL fitness runner (`node .discipline/fitness/runner.mjs`, all functions, never a selection) to 0 violations, then the locked push gate once, last, as one background task. A FAIL from your own change is fixed at the cause and the gate runs once more; a second FAIL on the same step is a STOP. You do not push; the coordinator's runner does. Past about 400k tokens, stop and report; you will be replaced, not resumed.
- No workaround of any kind: no exclude, no flag, no allowlist to pass. If the cause fix is not enough, STOP with the measurement.

## Report

ONE final report, five lines maximum, no interim messages: commit sha(s); full fitness runner violations (number); push gate result; whether item 6 was done or awaits R7m; anything you STOPped on, with the measurement.
