# Lane W10-FactCard-d, Amendment 1 (coordinator, 2026-09-22): the Rendering guard runs LOCALLY now. Read after `brief-w10-factcard-d.md`; this file wins.

Operator ruling, 2026-09-22, superseding "We do not need to install extra software use GitHub": Playwright is installed on this PC (`playwright@1.61.1`, chromium, verified by a real render), by the operator's own instruction, after two CI rounds on PR #777 failed on the Rendering guard while local math models claimed success. From now on NO UI lane pushes without a green LOCAL run of the real guard.

State: `lane/w10-factcard-d`, head `b0e035da` (unpushed; PR #777 is on GitHub at `aced67fe`, two Rendering guard failures). The last local diagnosis [HYPOTHESIS, never rendered]: the provenance column's `maxHeight: calc(1.45em * 4)` resolved `em` against an inherited 16px, not the column's 10.5px, so the clamp was ~93px instead of ~72px (the byte-identical 144px both rounds); rewritten as a px clamp. And the tap-target fix: the link row got `lineHeight: 1.45` plus `padding: 5px 0; margin: -5px 0` for a ~25px clickable box with zero layout height. CI's last failures, verbatim numbers: `panel-21c@1440` group 0 card 0, group 1 card 0, group 1 card 1 at 144px (> 140px) with 1 to 2 line claims; six `record-grade-facts` fixtures at 375 and 1280 with `a[example.com]` and `a[eur-lex.europa.eu]` at 12px tall (law-2 floor: 44px, or 24px with 8px clearance).

Do exactly this, in the worktree `C:/Users/jason/dotfiles/.worktrees/wt-w10-factcard` on the existing branch (confirm `git log -1 --oneline` shows `b0e035da`; tree clean):
1. `npm install --no-save playwright@1.61.1 --no-audit --no-fund` in that worktree's `fsi-app` (the chromium binary is already in the shared cache; no download). Verify with `npx playwright --version`.
2. Run the REAL guard, CI's own command: `node .discipline/rendering/run-rendering-guard.mjs` from `fsi-app`, as one background task (several minutes). Read its full output. Record every line starting with the cross mark and the final PASS or FAIL line.
3. If it FAILS: the numbers are now real. Fix at the cause, in the part or the model only (never the fixture, never the detector, never an allowlist). Re-run the guard. Iterate until the guard prints PASS with zero failures. Each iteration is one commit or one amended working set, your choice, but the final state is committed. If after four iterations the guard still fails, STOP with the last output.
4. When the guard passes: every npm suite with CI's shared script, `tsc`, the FULL fitness runner (all functions) to 0 violations, restore `coverage-report.json` if dirtied, then the locked push gate once (`bash "C:/Users/jason/AppData/Local/Temp/claude/C--Users-jason/fddbeade-7f79-480a-9254-e8fdb3278567/scratchpad/lane-prepush-check.sh" "C:/Users/jason/dotfiles/.worktrees/wt-w10-factcard"`, one background task, silence is normal). `signal 9` is contention: check `tasklist | grep -ic node.exe`, wait, retry once.
5. Commit trailer exactly `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`. Never `git stash`, never `git add -A`, never `--no-verify`. No em dash, en dash or section sign in new prose. You do not push.

ONE final report, six lines maximum, sent once, no interim messages: the guard's first-run result (fail lines or PASS); what you changed and why, with the guard's numbers before and after; the guard's final result (must be PASS, quoted); npm totals, fitness violations, locked gate exit code; head sha; any STOP.
