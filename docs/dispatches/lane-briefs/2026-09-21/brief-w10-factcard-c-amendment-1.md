# Lane W10-FactCard-c, Amendment 1 (coordinator, 2026-09-21): the push gate's F51 check 5 refusal. Read after `brief-w10-factcard-c.md`; this file wins.

State: `lane/w10-factcard-c`, head `528a341c`, worktree `C:/Users/jason/dotfiles/.worktrees/wt-w10-factcard`, not pushed. The locked push gate FAILED at step 3 on ONE test [CONFIRMED from the hook's test log]: `F51-no-shared-append.test.mjs:656`, "check 5 wired to the live tree ... reports 0 violations", with two hotspot violations: `fsi-app/src/components/ui/FactCard.tsx` and `fsi-app/src/components/ui/FactCard.npmtest.mjs`, each "already touched by 2 of the last 25 first-parent commits of origin/master (reaching 3 with this range)"; prior commits `2b2ac415` (#763, part 1) and `c3199c05` (#771, part 2). The FactCard fix itself is not in question. The coordinator's brief should have counted this before dispatch and did not.

Coordinator ruling (F51's own message names this path; precedent: lane F51b, 2026-09-20, the ADR-031 resolver chain): these are SERIAL lanes by one owner on the part's single home, each cut from master after the previous merged, no concurrent editor, so no conflict cause exists. Restructuring is not the answer: `FactCard.tsx` IS the one home of the part (parts, not pages) and the model is already its own module.

You are a replacement agent. Do exactly this:
1. In `fsi-app/.discipline/fitness/functions/F51-no-shared-append.mjs`, add TWO entries to the exported object `HOTSPOT_ALLOWLIST`, keys `fsi-app/src/components/ui/FactCard.tsx` and `fsi-app/src/components/ui/FactCard.npmtest.mjs`, each `{ decidedOn: '2026-09-21', reason: 'coordinator, lane W10-FactCard-c: serial part lanes by one owner (part 1 #763, part 2 #771, part c) each edited the FactCard part\'s single home after the previous one merged; no concurrent editor. Delete once the file has left the 30-commit window.' }`. Nothing else in F51 changes. (F51's own file is under an entry directory and is never a check 5 violation: lines 64 to 75.) If `F51-no-shared-append.test.mjs` pins the allowlist's keys or size, update that pin in the same commit and say so.
2. Check whether any OTHER file in your range is at two master touches (the gate reported only these two; confirm by re-running that one test file). If another appears, STOP with its name; do not add a third entry.
3. Never `git stash` (the previous agent did, against the rules; nothing was left behind). Run the F51 test file, then EVERY npm suite with CI's command, the FULL fitness runner (all functions) to 0 violations, restore `coverage-report.json` if dirtied, then the locked push gate once as one background task (long silence is normal). A second FAIL on the same step is a STOP with the quoted failing line (the hook's full test log is the `t.log` inside the newest `/tmp/discipline-prepush.*` directory; grep it for lines starting with the cross mark).
4. Commit with the exact trailer. Add one bullet to `pr-w10-factcard-c.md` in the coordinator scratchpad naming the two dated entries and why. You do not push.

First tool calls: Skill tool, `fsi-app:environmental-policy-and-innovation`, then `remediation-discipline`. ONE final report, five lines maximum, sent once, no interim messages: commit sha; F51 test result; npm totals; fitness violations and push gate result; any STOP with its measurement.
