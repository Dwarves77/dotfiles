# Lane L38: the admitted mirrors, each wired to one import or kept with a written reason; F45 down

Model: Sonnet. Worktree /home/user/dotfiles/.worktrees/wt-l35h (reused; its previous lane's branch is pushed), branch lane/l38-admitted-mirrors-2026-09-19, based on origin/master at branch creation (run `git log -1 --format=%h` and record it). Dependencies installed. Read brief-common-cloud.md first (pasted above in your prompt).

## Why
System-health audit 2026-09-17 (docs/audits/system-health-audit-2026-09-17.md, section 2, "Admitted mirrors"): "77 comments in source say a constant or helper was copied rather than imported. Some are legitimate client-bundle boundaries (a browser component cannot import a Node-only module) and say so; each will be listed with keep-or-wire in the removal lanes. The JS mirrors of SQL functions (url-canon, source-blocks, effective-confidence, aggregate-safeguards, canonical-key) are deliberate and drift-tested; they stay." Build plan section 6.7 (PR #718) row L38: "the 77 admitted mirrors: each keep-with-reason (a real client-bundle boundary) or wired to one import; F45 down; depends on L34 (merged)". The audit's 77 is [audit-stated, measured on master ed2ee7c9 with a pattern the audit does not print]. The coordinator's own grep at master a93a2271 over fsi-app/src, fsi-app/scripts and fsi-app/supabase/functions (tests, snapshots and archive excluded) found 40 lines matching "mirror(s|ed)? (of|from)", 5 "keep in sync with", 2 "copied (from|rather than)", 1 "duplicated from" [CONFIRMED]; the difference from 77 is unexplained and is yours to settle by measurement, not to paper over.

## Read in full before editing
- docs/audits/system-health-audit-2026-09-17.md sections 1 and 2 (the standing numbers, the duplicate-code table, the mirrors paragraph).
- fsi-app/.discipline/fitness/functions/F45-duplicate-code.mjs header and the DUPLICATED_LINES_CEILING line (the both-ways ratchet: only re-seed DOWN, to the number the runner prints, in the same commit).
- docs/dispatches/lane-common-contract.md (already required by the common contract).

## Scope, exactly, in this order
1. The census, stated and reproducible. From the worktree root, build the list of admitted-mirror sites with ONE git grep whose pattern you print in the report, over fsi-app/src, fsi-app/scripts and fsi-app/supabase/functions, excluding test files (`*.test.*`, `*.npmtest.*`, `*.selftest.*`, `*.golden.*`), fixtures, `_snapshots`, `_archive`, `_diag`, `_plans`, `_worklists`, `_reground`, `tmp`. Start from the coordinator's four patterns above, read every hit, and add the phrasings you find that mean "this is a copy of something that lives elsewhere" (the audit counted 77; find out what it counted). Report the final pattern, the count, and how it relates to 77 [CONFIRMED]. Save the list to fsi-app/scripts/tmp/l38-mirrors.txt (gitignored scratch, not committed).
2. Exclusions you must not edit (they are on the merge train tonight; a mirror in one of them is deferred with a note, never touched): every path in /tmp/claude-0/-home-user-dotfiles/d3ee595d-427a-5f48-a385-a337a6990473/scratchpad/l38-excluded-files.txt. Also leave alone the deliberate JS mirrors of SQL functions the audit names (url-canon, source-blocks, effective-confidence, aggregate-safeguards, canonical-key): list them as KEEP with the audit's reason and do not edit them.
3. Disposition, one site at a time, in the census list's order. For each mirror site, find the module the comment says it mirrors and read it. Then exactly one of:
   - WIRE: the mirror site can import that module (same runtime: a Node script or server module importing a Node or pure module; a browser component importing a PURE module with no Node-only import such as fs, path, pg, child_process, node: builtins). Replace the copy with an import of the one home, delete the copied lines, delete or rewrite the admitting comment. Run that file's own test file if one exists (`node --test <file>.test.mjs` or the npmtest form the file uses), and paste the result. If the mirror has diverged from its source (the copy behaves differently), do NOT wire: KEEP with the reason "diverged: <one line>" and report it as a finding, because silently changing behaviour is out of scope.
   - KEEP: a real boundary (a browser component and a Node-only source; an edge function bundle that cannot reach src; a deliberate drift-tested SQL mirror). Rewrite the admitting comment so its first line states the reason in this shape: `// mirror of <path>: kept, <reason>` (no new marker syntax, no allowlist, no gate change).
   Never introduce a new shared module to host a copy unless the one home already exists; if two copies exist and no home does, STOP and report the pair (creating a home is a design call for the coordinator).
4. The table. Add to docs/audits/system-health-audit-2026-09-17.md, directly after the "Admitted mirrors" paragraph, a subsection `#### Admitted mirrors, dispositions (lane L38, 2026-09-19)` with one row per site: file:line, mirror of, WIRE or KEEP or DEFERRED (on the train), reason. Every row is a finding and carries [CONFIRMED]. Then append to the paragraph itself: ` DONE (lane L38, 2026-09-19): N wired, M kept with reason, K deferred to the train's files; F45 <before> -> <after>.`
5. The ratchet, measured. After all wiring, run `node fsi-app/.discipline/fitness/runner.mjs 2>&1 | grep -A3 F45` from the worktree root and paste it. IMPROVEMENT with a number: set DUPLICATED_LINES_CEILING to exactly that number with the trailing comment `// lane L38, 2026-09-19: <before> -> <after> (admitted mirrors wired)` appended to the line's existing history text, re-run the runner (0 violations), run `node --test fsi-app/.discipline/fitness/functions/F45-duplicate-code.test.mjs`. REGRESSION (a number above the ceiling): STOP and report; you added duplication somewhere. No change: leave the line alone and say so.
6. Whole suite once: `bash fsi-app/.discipline/run-test-suite.sh 2>&1 | tail -4` from the worktree root (paste the totals). Then `cd fsi-app && npx tsc --noEmit 2>&1 | tail -3`.
7. Session-log entry per the common contract, heading `## 2026-09-19, W9 lane L38: admitted mirrors wired or kept with reason; F45 <before> to <after>`. Findings include the census pattern and count against the audit's 77, every diverged copy, every deferred site.
8. Commit in at most three commits (wired sites; kept sites and the table; the ratchet and session log may ride the last). Subjects `Lane L38: ...`. Then `node fsi-app/.discipline/runner.mjs --mode=ci --range=origin/master..HEAD 2>&1 | tail -4` (0 fail), rebase, gate wrapper as one background task, push, report.

## Not in scope (do not touch)
- The files in the exclusion list; the five SQL-mirror modules; any gate, allowlist, rule or invariant; any behaviour change; any new shared module.
- If the gate's F45 step or any other step refuses for a reason not in this brief, STOP and report the exact message.
