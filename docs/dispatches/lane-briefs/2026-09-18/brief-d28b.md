# Lane D28b: the memory gate's UX-compliance check reads the per-lane session-log files too

Model: Sonnet. Worktree /home/user/dotfiles/.worktrees/wt-d2, branch lane/d28b-memory-gate-ux-sessionlogd-2026-09-19, based on origin/master 91fd73fb. Dependencies installed. Read brief-common-cloud.md first (pasted above in your prompt); note its session-log rule: your own entry goes to docs/ops/session-log.d/2026-09-19-d28b.md.

## Why [CONFIRMED by the coordinator, 2026-09-19 00:40 UTC, the pre-push hook's own refusal]
D28 (2026-09-13, lane L18) added docs/ops/session-log.d/YYYY-MM-DD-<slug>.md as a MEMORY path in `fsi-app/.discipline/governance/memory-gate.mjs` so lanes stop appending to the one shared file and conflicting on it. Only half the gate learned it: `memoryGateVerdict` accepts the per-lane file, but the UX-compliance check (`uxGateVerdict`) is fed, by the script's CLI main, the diff of `docs/ops/session-log.md` ONLY. So a lane that touches any `fsi-app/src/**/*.tsx` or `.css` and writes its entry, UX block included, in its own per-lane file is refused: "UX compliance gate: this range touches a customer surface ... but the session-log addendum added in the same range has no 'UX compliance' block". Reproduced tonight on lane L38's branch (two .tsx files gained a comment line; entry moved to docs/ops/session-log.d/2026-09-19-l38.md; push refused at step 2b). The mechanism was built and half-wired: the operator's exact complaint. The pre-push hook's hint text (step 2b) and the CI step in `.github/workflows/discipline.yml` both hand the range to this one script, so the fix is in the script.

## Read in full before editing
- fsi-app/.discipline/governance/memory-gate.mjs (all of it, especially the header contract lines 20 to 45, the regexes near line 65, `uxGateVerdict`, and the CLI main at the bottom where `git diff <range> -- docs/ops/session-log.md` is produced)
- fsi-app/.discipline/governance/memory-gate.test.mjs
- fsi-app/.discipline/hooks/pre-push step 2b (lines about 158 to 176) and .github/workflows/discipline.yml lines about 165 to 200
- docs/ops/session-log.d/README.md

## Scope, exactly, in this order
1. RED on the real mechanism first. In your worktree make a throwaway commit on a scratch branch off your branch (`git checkout -b d28b-red`): add one comment line to any one file under fsi-app/src/**/*.tsx, and add a file docs/ops/session-log.d/2026-09-19-d28b-red.md containing a heading and a line `### UX compliance (d28b-red)` plus `Not a UI change.` Commit (the pre-commit hook runs; if it refuses the throwaway, say why). Run from the worktree root: `node fsi-app/.discipline/governance/memory-gate.mjs --range=origin/master..HEAD; echo exit=$?`. Paste the output: it must FAIL with the UX-compliance message although the UX block exists in the per-lane file. Then `git checkout lane/d28b-memory-gate-ux-sessionlogd-2026-09-19` (keep the scratch branch for the GREEN run in step 4; never push it).
2. The pure function and its tests. In memory-gate.mjs add `export function memoryDiffPaths(files)` returning, in order, `docs/ops/session-log.md` if present in files and every file matching SESSION_LOG_D_RE, and nothing else. Add tests in memory-gate.test.mjs: (a) a range with the shared file and two per-lane files returns all three; (b) a range with only a per-lane file returns just it; (c) the README and a malformed name are excluded; (d) `uxGateVerdict` PASSES when the surface file is present and the only '+' line containing 'UX compliance' comes from a per-lane file's diff (build the combined lines the CLI will now produce). Run `node --test fsi-app/.discipline/governance/memory-gate.test.mjs`: the new tests for (a) to (c) fail before you write the function (paste), then pass.
3. The fix, in the CLI main only: collect the range's changed files (it already does), compute `memoryDiffPaths`, run `git diff <range> -- <path>` for each and concatenate the added lines in that order, and hand the combined lines to `uxGateVerdict`. Update the script's header contract (the "UX gate" line about line 39) and the refusal message text to say "the session-log addendum in the same range (docs/ops/session-log.md or the lane's own docs/ops/session-log.d file)". No change to what counts as SURFACE, MEMORY or CODE; no new allowlist; nothing weakened. Keep node builtins and relative imports only (the file's own constraint).
4. GREEN on the real mechanism: `git checkout d28b-red && git rebase lane/d28b-memory-gate-ux-sessionlogd-2026-09-19` (or cherry-pick your fix commit onto the scratch branch), run the same `node fsi-app/.discipline/governance/memory-gate.mjs --range=origin/master..HEAD` and paste: it must PASS ("UX compliance gate OK"). Then back to your lane branch; delete nothing, push nothing from the scratch branch.
5. The hook hint and the README: in fsi-app/.discipline/hooks/pre-push step 2b, the hint line "append a docs/ops/session-log.md addendum (or update docs/PROGRAM-BOARD.md), and a UX compliance block if a .tsx/.css was touched, then re-push." becomes "append a session-log addendum (your own docs/ops/session-log.d/YYYY-MM-DD-<lane>.md file, or docs/ops/session-log.md, or update docs/PROGRAM-BOARD.md), with a UX compliance block in that same addendum if a .tsx/.css was touched, then re-push." Run `node --test fsi-app/.discipline/hooks/pre-push-tmpdir.test.mjs` if it exercises that text. In docs/ops/session-log.d/README.md add one sentence under "The rule": the UX compliance block, when a lane touches a .tsx or .css, goes in the lane's own file and the gate reads it there (lane D28b, 2026-09-19). If docs/inventories/discipline.md describes the memory gate's UX half as reading session-log.md only, correct that line in place; if it does not describe it, leave it.
6. Gates from the worktree root: `node --test fsi-app/.discipline/governance/memory-gate.test.mjs` (all pass), `node fsi-app/.discipline/fitness/runner.mjs 2>&1 | tail -3` (0 violations), `node fsi-app/.discipline/runner.mjs --mode=ci --range=origin/master..HEAD 2>&1 | tail -4` after committing (0 fail).
7. Your session-log entry as docs/ops/session-log.d/2026-09-19-d28b.md (no .tsx touched on your lane branch, so the UX gate is not applicable to you; the memory gate is satisfied by that file), heading `## 2026-09-19, W9 lane D28b: the UX-compliance check reads the per-lane session-log files`. Commit (explicit paths; subject `Lane D28b: the memory gate's UX-compliance check reads per-lane session-log files too`), rebase, gate wrapper as one background task, push, report with the RED and GREEN outputs of steps 1 and 4 pasted.

## Not in scope
- Any other gate, the SURFACE or CODE classification, the workflow file, any allowlist.
- If the pre-commit hook or any gate refuses for a reason outside this brief, STOP and report the exact message.
