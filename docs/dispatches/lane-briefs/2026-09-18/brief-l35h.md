# Lane L35h: eur-lex.europa.eu gets its one home; F46 ceiling 1 to 0

Model: Sonnet. Worktree /home/user/dotfiles/.worktrees/wt-l35h, branch lane/l35h-eurlex-one-home-2026-09-18, based on origin/master a93a2271 (#712 merged). Read brief-common-cloud.md first (pasted above in your prompt).

## Why
F46 external-host-home (fsi-app/.discipline/fitness/functions/F46-external-host-home.mjs, lane L31, RD-70) ratchets the count of external hosts with more than one URL-building home. Lane L35 (2026-09-17) homed six of seven hosts; eur-lex.europa.eu stayed at two homes because scripts/maintenance/capture-static-primaries.mjs was outside L35's write set (F46 header lines 55 to 61; identifier-variants.test.mjs lines 116 to 122). Build plan section 6.7 row L35h: "the eur-lex second home onto identifier-variants (capture-static-primaries); F46 1 to 0; depends on: none". Coordinator measurement [CONFIRMED, git grep over fsi-app/src and fsi-app/scripts source files at origin/master, tests, goldens, fixtures and F46's REFERENCE_FILES excluded]: exactly two non-comment files build an eur-lex.europa.eu URL: fsi-app/src/lib/sources/identifier-variants.mjs (the home, lines 71 to 122) and fsi-app/scripts/maintenance/capture-static-primaries.mjs line 145, inside deriveCelexTxtHtmlUrl. The home already exports celexTxtHtmlUrl(celex) (line 71) with the identical template. The maintenance file already imports from ../../src/lib/sources/ (transport-runtime.mjs, fetch-hold.mjs), so the import path is established.

## Read in full before editing
- fsi-app/.discipline/fitness/functions/F46-external-host-home.mjs and its .test.mjs
- fsi-app/src/lib/sources/identifier-variants.mjs (lines 60 to 130) and identifier-variants.test.mjs (lines 100 to 152)
- fsi-app/scripts/maintenance/capture-static-primaries.mjs (header, lines 80 to 160, and the call at about line 371) and its .test.mjs (lines 40 to 70)
- docs/audits/system-health-audit-2026-09-17.md section 2 (the F46 rows: lines about 36, 73, 81)

## Scope, exactly, in this order
1. RED first. In fsi-app/src/lib/sources/identifier-variants.test.mjs, the eur-lex sweep test (about line 124): remove "fsi-app/scripts/maintenance/capture-static-primaries.mjs" from EXCEPTIONS, rename the test to `ONE home for eur-lex.europa.eu: no other in-scope source file builds this host's URL`, and rewrite the comment block above it (lines about 116 to 122) to say the exception closed in lane L35h on 2026-09-18. Run `node --test fsi-app/src/lib/sources/identifier-variants.test.mjs` from the worktree root and paste the failure: it must name capture-static-primaries.mjs. If it passes at this point, STOP and report (the premise would be wrong).
2. The fix. In fsi-app/scripts/maintenance/capture-static-primaries.mjs: add `import { celexTxtHtmlUrl } from "../../src/lib/sources/identifier-variants.mjs";` beside the existing ../../src/lib/sources imports. In deriveCelexTxtHtmlUrl keep the name, the signature, the "already the clean-text form" null, the deriveKey call and the null-on-no-key; replace only the template literal on line 145 with `return celexTxtHtmlUrl(key);`. Amend the header comment (lines about 48 to 51) to say the clean-text URL comes from identifier-variants' celexTxtHtmlUrl, the host's one home (F46). No other change to this file.
3. GREEN. Re-run the identifier-variants test (passes) and `node --test fsi-app/scripts/maintenance/capture-static-primaries.test.mjs` (the four deriveCelexTxtHtmlUrl tests pass unchanged; do not edit that test file unless a test fails, and if one fails, STOP and report why).
4. The ratchet, measured not computed. In F46-external-host-home.mjs add to HOST_HOMES, first in the object: `'eur-lex.europa.eu': 'fsi-app/src/lib/sources/identifier-variants.mjs', // lane L35h, 2026-09-18`. Then, from the worktree root, run `node fsi-app/.discipline/fitness/runner.mjs 2>&1 | grep -A3 F46` BEFORE touching MULTI_HOME_CEILING and paste it: F46 must report IMPROVEMENT (multi-home count below the ceiling of 1). Then set `export const MULTI_HOME_CEILING = 0;` with the trailing comment `// lane L35h, 2026-09-18: 1 -> 0 (eur-lex.europa.eu homed in identifier-variants.mjs)`, keep the L35 history comment beneath it and add one sentence saying the eur-lex exception closed in L35h. Re-run the runner: `node fsi-app/.discipline/fitness/runner.mjs 2>&1 | tail -3` must print 0 violations, and `node --test fsi-app/.discipline/fitness/functions/F46-external-host-home.test.mjs` must pass. If the runner prints a multi-home count other than 0 after the fix, STOP and report the host and files it names; do not add anything to HOST_HOMES or REFERENCE_FILES to make it pass.
5. Whole-tree measure, paste the output: `git grep -n -E 'https?://eur-lex\.europa\.eu' -- 'fsi-app/src/**/*.mjs' 'fsi-app/src/**/*.ts' 'fsi-app/src/**/*.tsx' 'fsi-app/scripts/**/*.mjs' | grep -vE '\.(test|npmtest|selftest|golden)\.' | grep -vE '/_(archive|diag|plans|worklists|snapshots|reground|tmp)/' | grep -vE ':[0-9]+:\s*(//|\*|/\*)'` must list only identifier-variants.mjs and F46's four REFERENCE_FILES (source-licence.mjs, intake-url-corpus.mjs, url-canon.mjs, series-item-map.mjs).
6. docs/audits/system-health-audit-2026-09-17.md: on each F46 line that records six of seven done or the eur-lex remainder (about lines 36, 73, 81), append in place ` DONE (lane L35h, 2026-09-18): eur-lex.europa.eu homed in identifier-variants.mjs; F46 MULTI_HOME_CEILING 1 -> 0.` Do not delete history.
7. Session-log entry per the common contract, heading `## 2026-09-18, W9 lane L35h: eur-lex.europa.eu has one home; F46 ceiling 1 to 0`.
8. Discipline run on the range after committing: `node fsi-app/.discipline/runner.mjs --mode=ci --range=origin/master..HEAD 2>&1 | tail -4` (0 fail). Then tsc from fsi-app: `cd fsi-app && npx tsc --noEmit 2>&1 | tail -3` (the gate runs it too; paste it).
9. Commit (explicit paths). Subject: `Lane L35h: eur-lex.europa.eu has one home; F46 ceiling 1 to 0`. Then rebase, gate, push per the common contract. Report.

## Not in scope (do not touch)
- fsi-app/scripts/lib/eurlex-cellar.mjs (publications.europa.eu is its own home) and every other host.
- F45. This change removes a one-line literal, not an eight-line duplicate window, so F45 should not move. If the runner reports an F45 IMPROVEMENT anyway, re-seed DUPLICATED_LINES_CEILING DOWN to exactly the number it prints, in the same commit, and say so in the report; never up.
- Any gate allowlist, REFERENCE_FILES, or NOT_EXTERNAL_RE.
- If a gate refuses the scripts-to-src import for a reason of its own (a glob-portability or module-boundary rule), STOP and report the exact message; do not restructure.

## Amendment 1 (coordinator, 2026-09-18 about 23:50 UTC, after the lane's step-1 report)

[REFUTED, corrected in place] Step 1's RED premise as written. The lane removed the EXCEPTIONS entry and the sweep test still passed. Cause [CONFIRMED by the lane's repro and the coordinator's own, node on master's file]: the test strips comments with a mid-line `//` regex, which deletes everything after the `//` inside `https://`, so line 145 reads `return \`https:` by the time the host regex runs. The test can never see a URL literal. The substantive premise stands [CONFIRMED]: capture-static-primaries.mjs line 145 is the second home. The same stripper sits in six L35 sweep tests (coordinator grep at master: identifier-variants.test.mjs x2, transport-escalation.test.mjs, anthropic-stream.test.mjs, auth/linkedin/start/logic.test.mjs, eurostat-lc-lci-lev-producer.test.mjs), so none of the six has ever been proven RED. F46 itself is not affected: it drops whole comment LINES and never truncates mid-line.

Step 1 is replaced by:
1. In fsi-app/src/lib/sources/identifier-variants.test.mjs, in BOTH sweep tests (eur-lex, about line 134; legislation.gov.uk, about line 165), replace the mid-line strip with F46's own line filter: strip block comments, split on /\r?\n/, drop every line matching /^\s*(\/\/|\*)/, join with "\n", then test the host regex on that. Keep your EXCEPTIONS, rename and comment edits. Run the file: the eur-lex sweep MUST now fail naming fsi-app/scripts/maintenance/capture-static-primaries.mjs (the honest RED) and the legislation.gov.uk sweep must still pass. If the uk sweep fails naming a file, STOP and report that file: it is a real second home nobody has seen. Paste the output.
1b. Make the identical one-line stripper replacement in the four sibling sweep tests: fsi-app/src/lib/sources/transport-escalation.test.mjs (about line 269), fsi-app/src/lib/agent/anthropic-stream.test.mjs (about line 189), fsi-app/src/app/api/auth/linkedin/start/logic.test.mjs (about line 36), fsi-app/scripts/producers/regional/eurostat-lc-lci-lev-producer.test.mjs (about line 53). Run each file. Each must still pass; if one fails naming a file, STOP and report it, do not fix that host. Do NOT touch the other users of the pattern (F30-entity-spine.mjs, rules/021-cached-shape-key.mjs, capture-static-primaries.test.mjs lines 667 and 711): they are not host sweeps; the coordinator records them.
Then steps 2 to 9 as written, with these additions: the session-log Findings record [CONFIRMED] the stripper defect with the repro line, that the six L35 sweep tests were blind to URL literals since 2026-09-17 and had never been proven RED, and the three non-sweep users left untouched; the audit-doc append in step 6 gains "; the L35 sweep tests were blind to URL literals (mid-line // strip), fixed on F46's line filter". Base is now a93a2271 (#712 merged); the rebase step handles it.

## Amendment 2 (coordinator, 2026-09-18 about 23:57 UTC, after the lane's amended-step-1 report)

[CONFIRMED by the lane, verified by the coordinator against F46's source] The three extra files the honest stripper names (source-licence.mjs, intake-url-corpus.mjs, url-canon.mjs) are F46's own REFERENCE_FILES: data citations, not URL builders. The sweep tests carried their own scanner with no reference set, so they were both blind (the stripper) and incomplete (no REFERENCE_FILES). [REFUTED, corrected in place] Amendment 1's expectation that the uk sweep would still pass. Ruling: the sweep tests stop re-implementing F46 and reuse it by import; F46's `evaluate` already holds the strict rule (a host in HOST_HOMES may be built only in its home, reference files excluded), so the real gate produces the honest RED. Prior art for a src test importing from .discipline: fsi-app/src/lib/agent/source-blocks.test.mjs.

Steps 1 and 1b are replaced by this order:
1. Register first (the honest RED from the real gate). In fsi-app/.discipline/fitness/functions/F46-external-host-home.mjs add to HOST_HOMES, first in the object: `'eur-lex.europa.eu': 'fsi-app/src/lib/sources/identifier-variants.mjs', // lane L35h, 2026-09-18`. Do NOT touch MULTI_HOME_CEILING yet. Run `node fsi-app/.discipline/fitness/runner.mjs 2>&1 | grep -B2 -A6 F46` from the worktree root and paste it: F46 must now report a STRICT violation for eur-lex.europa.eu naming fsi-app/scripts/maintenance/capture-static-primaries.mjs and nothing else. If it names another non-reference file, STOP and report it.
2. Rewrite the two sweep tests in fsi-app/src/lib/sources/identifier-variants.test.mjs on F46 itself. Replace each test's body (the git ls-files walk, the strip, the regex loop, the EXCEPTIONS set) with: `import { scanTree, HOST_HOMES } from "../../../.discipline/fitness/functions/F46-external-host-home.mjs";` (one import, top of file, beside the others; drop the two execFileSync imports if nothing else uses them) and, per host, `const { strict } = scanTree(); const hit = strict.find((s) => s.host === "<host>"); assert.equal(HOST_HOMES["<host>"], "fsi-app/src/lib/sources/identifier-variants.mjs"); assert.equal(hit, undefined, hit && \`import identifier-variants.mjs instead of building a <host> URL in: ${hit.extra.join(", ")}\`);`. Keep the test names from step 1 of the original brief and rewrite the comment block above the eur-lex test to say: the sweep is F46's own strict check for this host, reused by import so the parser and the reference set can never drift (the earlier copy stripped comments mid-line and was blind to every URL literal, lane L35h 2026-09-18). Run the file: the eur-lex test FAILS naming capture-static-primaries.mjs (paste it), the uk test passes.
3. Then the original step 2 (the fix in capture-static-primaries.mjs) and step 3 (GREEN: identifier-variants test all pass; capture-static-primaries test passes; runner F46 shows no strict violation).
4. Then the original step 4, but only its second half: the runner must now print F46 IMPROVEMENT (multi-home count 0 below ceiling 1); paste it; then re-seed MULTI_HOME_CEILING to 0 with the comment given there; runner prints 0 violations; the F46 test passes.
5. Step 1b becomes: rewrite the four sibling sweep tests the same way, each on scanTree().strict for its own host(s) with the HOST_HOMES assertion: fsi-app/src/lib/sources/transport-escalation.test.mjs (www.federalregister.gov and www.ecfr.gov, both homed in transport-escalation.mjs), fsi-app/src/lib/agent/anthropic-stream.test.mjs (api.anthropic.com), fsi-app/src/app/api/auth/linkedin/start/logic.test.mjs (www.linkedin.com), fsi-app/scripts/producers/regional/eurostat-lc-lci-lev-producer.test.mjs (ec.europa.eu). Read each existing sweep test first to keep its name and its home path; the import path differs per file depth. Run each file; each must pass. A failure naming a file is STOP and report.
6. Then the original steps 5 to 9. The session-log Findings additionally record: the six sweep tests were both blind (mid-line strip) and without a reference set, now reuse F46 by import; the three non-sweep users of the mid-line strip (F30-entity-spine.mjs, rules/021-cached-shape-key.mjs, capture-static-primaries.test.mjs lines 667 and 711) were left as they are for the coordinator. If any gate refuses the .discipline import from a src test (a boundary or glob rule), STOP and report the exact message.
