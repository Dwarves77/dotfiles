# Lane T3: the test suite discovers its tests; a new directory can never orphan a proof

Coordinator brief, 2026-09-20. Executor: Sonnet (gate logic). Lane id `t3`. Read `docs/dispatches/lane-briefs/2026-09-19/brief-common-local.md` first (Amendment 1 wins over its body, EXCEPT the one line this brief refutes below), then this. Worktree and branch are in your dispatch message; base `origin/master`.

## Why

[CONFIRMED 2026-09-20, lane M7a's push gate] A lane added `fsi-app/src/app/api/admin/statutory-rows/logic.test.mjs`. F23 (governed-surface-coverage, ORPHANED PROOF, ratcheted at 0) failed the gate, correctly: `fsi-app/.discipline/run-test-suite.sh` runs `node --test` over a HAND-KEPT list of about sixty directory globs (lines 54 on), and a new directory matches none. [REFUTED] `brief-common-local.md` Amendment 1 calls the "`run-test-suite.sh` test lists" derived: the per-file lists are gone, the per-directory list is not, and no generator writes it. The same stop waits for lane M9d (`fsi-app/scripts/producers/*.test.mjs` is one level above the covered `producers/*/` glob). This is the class plan 6.8 retired everywhere else: a registry kept as a list. Second occurrence, so the cause is removed and the removal is gated by attack (operator ruling, 2026-09-18: no workarounds).

## Phase 1: measure before changing anything (read-only). STOP conditions live here.

From the REPO ROOT:
1. RUN set: expand the script's current globs exactly as bash does (write a throwaway node or bash snippet under `fsi-app/scripts/tmp/`, gitignored; do not commit it) into a sorted file list.
2. DISCOVERED set: `git ls-files -- '*.test.mjs'` restricted to `fsi-app/` and `.claude/hooks/`, plus exactly the selftest entries the script names today (the header at lines 31 to 38 explains why `src/lib/sources/` selftests stay a NAMED pair: keep that as is).
3. Report both counts and the two differences. RUN minus DISCOVERED must be empty (if not, say what the script runs that is not a tracked `*.test.mjs`). DISCOVERED minus RUN is the interesting set: for EACH file in it, find which other runner executes it (the npmtest glob in `package.json`, `run-goldens.mjs`, `run-data-audit-lane.mjs`, the rendering job, a fitness sentinel) using the same sources F23 and `fsi-app/.discipline/governance/execution-wiring.mjs` read. A file run by another runner that would FAIL or need credentials or a browser under plain `node --test` is an exclusion candidate: list it with the reason. If that list is longer than ten files, or any file in it is run by NOTHING (F23 should make that impossible), STOP and report the list; the coordinator decides.

## Phase 2: the change (only if Phase 1 did not stop)

1. `run-test-suite.sh` builds its file list by construction: tracked `*.test.mjs` under `fsi-app/` and `.claude/hooks/` from `git ls-files` (NUL-safe, sorted, so a file with a space cannot split), plus the named selftest entries, minus an exclusion set that is a DIRECTORY of reasons or, if Phase 1 found none, no exclusion mechanism at all (do not build one speculatively). If exclusions are needed: one home, `fsi-app/.discipline/test-suite-exclusions.d/<slug>.json` with fields path, runner, reason, decidedOn; never an inline list (F51).
2. Untracked test files: a lane's brand-new test is untracked until committed, and the pre-push gate runs on committed trees, so `git ls-files` is right for the gate. Say in the header that a local run before `git add` will not see a new file, and print the discovered count on every run so a surprising number is visible.
3. CI parity: read how `.github/workflows/discipline.yml` invokes the suite. If it calls this script, nothing more. If it carries its own copy of the list, it calls the script instead (one home); quote the before and after lines in your report. Workflow edits beyond that one call: STOP.
4. F23 and `execution-wiring.mjs` parse the script to learn what is executed. Read how (grep for `run-test-suite` under `fsi-app/.discipline/`). They must learn the executed set from the SAME discovery function, not from a regex over a list that no longer exists: extract the discovery into `fsi-app/.discipline/lib/test-discovery.mjs` (pure: takes the `git ls-files` output, returns the list), have the shell script call it (`node fsi-app/.discipline/lib/test-discovery.mjs --print0 | xargs -0 node --test`, or the equivalent that keeps exit codes honest: verify a failing test still fails the script), and have F23 and execution-wiring import it. If either gate's parsing cannot be moved onto the shared function without changing what the gate means, STOP and name the function.
5. Attack tests (rule 15), in `fsi-app/.discipline/lib/test-discovery.test.mjs`: a test file in a never-before-seen directory IS in the discovered list; a `*.npmtest.mjs` is NOT; the named selftests ARE; a fixture where discovery is replaced by the old fixed list shows the new-directory file missing (proving the old mechanism had the defect). Plus one end-to-end check you run once and quote: a deliberately failing temp test in a new directory, committed on a throwaway local branch you delete afterwards (never pushed), makes `bash fsi-app/.discipline/run-test-suite.sh` exit non-zero.
6. Correct the record in place: `glob-portability` and F25 notes are NOT yours. The header comments of `run-test-suite.sh` that describe the list (lines 28 to 48) are rewritten to the present, keeping the dated history true. Timing: print the suite's wall time before and after; if discovery grows the run by more than 30 seconds, say which directories were never run before.

## Out of scope: STOP, do not solve

Any test that newly runs and FAILS: do not fix it, do not exclude it on your own; list it and STOP. The npmtest runner. Any fitness function's meaning. `brief-common-local.md` (the coordinator corrects its Amendment 1 in a new amendment file).

## Standing constraints

Your FIRST tool call is the Skill tool `fsi-app:environmental-policy-and-innovation` (one call; a mid-lane denial costs a cycle). Commit trailer exactly `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`. No em dash, en dash or section sign in new prose. Never `git stash`, `git add -A`, `--no-verify`. Session-log file `docs/ops/session-log.d/<date>-t3.md`. Gates: the full suite through the new script, `tsc`, the fitness runner, the invariant-coverage meta-gate (it runs execution-wiring), the override check, then the locked push gate once, last, as one background task. You commit; you do not push. Report and PR body returned as TEXT.
