# Lane W10-FactCard, Amendment 1 (coordinator, 2026-09-20): answers to the lane's STOPs, and the measured cause. Read after `brief-w10-factcard.md`; where they disagree this file wins.

You are a FRESH agent continuing the lane in its worktree `C:/Users/jason/dotfiles/.worktrees/wt-w10-factcard`, branch `lane/w10-factcard-2026-09-20`, on top of commit `f8fb9d86` (unpushed). Read that commit's session-log file first (`docs/ops/session-log.d/2026-09-20-w10-factcard.md`), then `src/lib/detail/fact-card-model.ts` and `src/components/ui/FactCard.tsx` as they stand. New commits only; never amend.

## A. The cause of literal markdown on production [measured in the live DOM, 2026-09-20, sha `6d9d139c`]

On `/regulations/eu-emissions-trading-system-ets-extension-to-maritime-transport` and on a market detail, at BOTH depths (Summary: 7 and 14 matches; Full brief: 56 and 37), well-formed markers such as `**Cause:** FACT: "..."` and `*Source: FAQ ... 02/04/2025.` sit as plain text in `<p>` elements that have NO `<strong>` or `<em>` children, inside `section[data-section-card][data-guard-card]`, while sibling fields in the same card ARE rendered. [CONFIRMED] no renderer ran for those paragraph values; the markdown itself is clean. [HYPOTHESIS] at Summary depth specific extracted values (Cause, Effect on workspace, Source lines) are inserted as raw strings; at Full brief depth the whole `full_brief` string is printed without the GFM component or with it bypassed. Find both insertion points by reading the two detail surfaces (`RegulationDetailSurface.tsx`, `MarketSignalDetailSurface.tsx`) and whatever renders `[data-section-card]` bodies: grep for where a section's body string is placed inside `<p>` or `{value}` without passing through the model or `GfmSection`. Fix at the cause: every such value goes through the ONE model (`fact-card-model.ts`) when it is a fact paragraph, or through the existing GFM component when it is running prose. Add a render test per insertion point whose fixture is the measured pattern above and whose assertion is: no `*` character in the rendered text. If a value is printed raw in more than four places, list them and STOP after fixing the two surfaces named: that is a wider defect for its own lane.
Also one raw URL outside an anchor was measured on the market detail (a citation line, publisher and date, then a bare URL in a `<span>`): find it, route it through the provenance shape.

## B. Rulings on the lane's STOPs

1. **Operations matrix panel: MIGRATE.** Its header cites a 2026-09-09 ruling that it is a different anatomy; operator ruling 4 of 2026-09-20 (bundle README, "Undrawn cases") names the operations matrix panel as a FactCard consumer, and the later ruling wins. Artboard 08's note draws "standard fact cards (headline figure Anton 18, quote 12.5px, source line)" below the matrix: that size is a declared `density="matrix"` variant of the ONE part (lead Anton 18, claim 12.5px), not a second component. Delete `MatrixFactCard`; correct its header comment to cite the newer ruling.
2. **Fixture picture: `/admin/parts`.** One home for every part lane: a platform-admin route (same guard and frame as the other `/admin` routes; operator ruling 7 puts admin routes inside the program) with an index page listing parts and one page per part. You build the index and `/admin/parts/fact-card`, rendering every kind and form, the no-lead case, the matrix density, a long claim, and a card with no provenance, from static fixture models (no database read). Register it wherever the rendering guard or F25 requires so it is reachable and measured at 375. Screenshots: if you can run the app locally without secrets, take 1440 and 390 and give the paths; if not, say so: the coordinator captures them from production after merge.
3. **`RecordFactCard` kinds: a deterministic field-to-kind table in the model module.** A record field maps to DEADLINE, PENALTY, DEFINITION, SCOPE, BASELINE TARGET or NATIONAL TARGET only where the field's own key makes the meaning unambiguous (name the keys in a table with a test); everything else is SCOPE, per brief 2.1's unknown-lead-in rule. The field label stays the qualifier.
4. **No-lead count:** not yours; the coordinator obtains it from a bounded read-only sample.

## C. One addition, from the audits

Two audits could not tell which component rendered a node: no class or attribute survives to the DOM. The part's root element carries `data-part="fact-card"` and `data-kind="<kind slug>"`. Record in your session-log file, under "Owed", that every later part lane does the same (`data-part`), so conformance can be measured instead of guessed.

## D. Gates

As the brief says. The F51 hotspot failure you met is fixed by lane F51b; `git fetch origin` and rebase onto `origin/master` ONLY if F51b's merge is there (`git log origin/master --oneline | grep -i f51b`); if it is not, skip the locked push gate, run everything else, and say so. Never `git stash` (to look at clean master use `git worktree add --detach <gitignored scratch path> origin/master`, then remove it). Report as TEXT in the brief's shape, with an updated PR body.
