export const meta = {
  name: 'lane-heal10',
  description: 'Sonnet lane HEAL-10: heal apply #31 processed 15 of 87 items in 25 min (~100 s/item) and verified 0; 13 of 15 fail only on gate-A orphans that exist in no capture (111 tokens). Build the flag-gated BRIEF-HONEST step (strip the sentence carrying an unprovable figure, reversible, dry by default) and cut the per-item cost, from the two run summaries on disk',
  phases: [{ title: 'Lane', detail: 'HEAL-10 in its own worktree' }],
}
phase('Lane')
const P = `You are lane HEAL-10 on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/heal10 (branch lane/heal10-2026-09-04, based on origin/master 51815a5b); node_modules symlinked at fsi-app/node_modules. Cannot push; coordinator lands. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. Write set: fsi-app/scripts/mint/heal-provenance.mjs (+ its test), fsi-app/scripts/maintenance/provenance-heal.mjs (+ test), .github/workflows/maintenance.yml ONLY the maintain job's timeout-minutes line and its comment block plus the HEAL_TIME_BUDGET_SECONDS comment (nothing else in that file), docs/runbooks/MAINTENANCE-RUNBOOK.md provenance-heal section text. heal-provenance.mjs is NOT an F28-governed file (checked). Deterministic, $0, no LLM; "there is NO small follow-up fix, fix it now"; no claims ahead of evidence, [CONFIRMED]/[HYPOTHESIS]; read every file you touch IN FULL before editing (heal-provenance.mjs is ~3,600 lines; read all of it, the header carries nine passes of findings; STEP C ORPHANS at ~line 2073 says "the brief is never edited by this step"); commit message ends with "Co-Authored-By: Claude Sonnet <noreply@anthropic.com>". Gates before commit: node --test fsi-app/scripts/mint/heal-provenance.test.mjs and the wrapper's test; from repo root node fsi-app/.discipline/fitness/runner.mjs (0 violations) and node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs (141 pass); python3 -c "import yaml; yaml.safe_load(open('.github/workflows/maintenance.yml'))". The Supabase MCP (server "Supabase", tool execute_sql, project kwrsbpiseruzbfwjpvsp; load via ToolSearch "select:mcp__Supabase__execute_sql") is available for READ-ONLY SELECT; never write through it.

THE EVIDENCE, on disk: fsi-app/scripts/_snapshots/heal31.json is the checkpoint summary of Maintenance #31 (run 33855060659, provenance-heal APPLY, selection quarantined-live, master f9f6824a, HEAL_VERSION per its note) and heal28.json is the full DRY plan of #28 (run 33851505474, 89 candidates). Read both in full with a script (they are 330-430 KB). Facts the coordinator already read [CONFIRMED]: #31's step started 08:51:57 and was CANCELLED at 09:16:49 by the JOB's 30-minute timeout-minutes (job started 08:46:36; Install took 3m06s, the whole pre-step setup 5m21s), i.e. BEFORE the step's own 1,500 s budget could stop it cleanly; the maintenance.yml comment assumed "~1-2 min" of setup. In those 25 minutes the step processed 15 of 87 candidates (per_item has 15 entries; ~100 s per item), healed 0 to verified; counts: source_token_not_in_page 345, source_unresolved 111, source_grounded 12, source_registered 1, source_rated_tier 12, source_unfetchable 5, orphans_unprovable 111, orphans_grounded 6, relabel_no_owning_section 159, retrofit_refused_no_owning_paragraph 11; final_failures_by_item: (7, gate_a_unproven_or_stale) 13 items, (4, analysis_missing_label_syntax) 8, (2, ungrounded_url) 1. Example [CONFIRMED from per_item]: item 0781a8c0 (tool, Blue Visby) carries "15%" and "April 2026" in full_brief; both cited pages (imo.org hot topic, bluevisby.com/how-does-it-work) were fetched and neither contains either token; outcome unresolved. #28's dry plan over all 89 said STEP SOURCE would ground 506 orphan tokens; #31's apply grounded 12 in 15 items. CLAUDE.md standing rule 18 (2026-09-04): a figure with a source is published with that source's rating; an ungrounded figure is still refused. Standing rule 17: a heal that ends with items still quarantined and no path to verified is a defect in the heal. Lane HEAL-6's brief (2026-09-04) asked, for orphans present in no capture, to "make full_brief honest by the same rule the kit uses for record-grade items (strip the unprovable sentence from full_brief and keep the grounded facts; if the item's grade must change, say so with the ADR that governs item_grade and put the change behind a flag, dry by default, so the coordinator can put it to the operator)"; that step was never built (grep the file: no strip, no brief rewrite; STEP C reports unprovable and stops).

DO, in this order, diagnosis first and at the top of your report:
(1) PER-ITEM COST: from heal31.json per_item[].steps, attribute the ~100 s per item. Count per item: capture_cited candidates/to_fetch/fetched, source[] entries (each URL lookup is a fetch at the 1 req/s politeness pace plus locateSpanInText over the capture), resource/orphans scans over the bucket pool (how many captures × how many tokens × the four locateSpanInText tiers). State which of these dominates with the arithmetic, [CONFIRMED] from counts or [INFERRED]. Then read STEP SOURCE's loop and the budget split (SOURCE_MAX_PER_ITEM 25, free lookups vs fetch bound) and cut the cost where the numbers say: e.g. one page fetch per distinct URL per item (not per token), token lookups against an already-captured page without re-fetching, skip a URL that STEP SOURCE already found token_not_in_page for a sibling token when the page text is on hand, and a per-item wall-clock cap so one item can never eat the run. Target: state the projected s/item after the change with arithmetic.
(2) BUDGET ARITHMETIC: maintenance.yml timeout-minutes 30 vs HEAL_TIME_BUDGET_SECONDS 1500 left 5 min for setup that measured 5m21s + Population AFTER + artifact upload. Fix it honestly: raise timeout-minutes so the step's own budget always stops the run first with measured headroom (state the arithmetic from #31's step timestamps: setup 5m21s, Population AFTER 3 s, upload ~3 s, plus the checkpoint write), and update the comment block that documents it. The budget itself stays 1500 unless (1) shows it should change; if the step is cancelled by the job again, the last checkpoint is all that survives, and that is exactly what happened.
(3) BRIEF-HONEST (criterion 7, the 13 items): build the step HEAL-6 named. After STEP C has tried every capture for an orphan token and it is unprovable, and after STEP SOURCE has tried the cited URLs (i.e. the token is in no capture and on no cited page), the sentence in full_brief that carries the token is the unprovable claim. Plan a rewrite of full_brief that removes exactly that sentence (sentence boundary by the same sentenceStart/terminator rule the extractors use; if the sentence carries a grounded token too, remove only the unprovable clause when it is a clause between separators, else refuse and report), keeping everything else byte-identical, and records before/after and a restore_sql per item. Gate A must pass on the rewritten brief: re-run the live scanner (scanBrief / buildGateARow, already imported) on the planned text and only accept the plan when orphan_count reaches 0 or the remaining orphans are ones this run grounded. The step is DRY BY DEFAULT: in apply mode it writes ONLY when the dispatch arg carries an explicit token (extend parseSelection's arg grammar: e.g. "quarantined-live+strip-unprovable"; the existing selection forms keep their exact meaning), otherwise it reports the plan under summary.brief_honest with per-item before/after excerpts. If item_grade doctrine binds (grep docs/decisions for item_grade, full_brief, Gate A, ADR-016; cite what you find), keep the grade unchanged and say why, or plan the change behind the same token. Never invent text, never paraphrase, only remove; never touch claims, sections, or source_span.
(4) Criterion 4 (8 items, relabel_no_owning_section 159): read HEAL-6's finding on where the paragraph lives (full_brief vs section content_md) and measure on heal31's per_item which case these 8 are; if RELABEL still cannot find the owning paragraph because the claim's text lives only in full_brief, extend RELABEL to label the sentence in full_brief the same way, behind the same token; if the live function's criterion 4 wants a form RELABEL does not write, quote the regex from validate_item_provenance (pg_get_functiondef, read-only) and write that form.
(5) Tests for each new branch with fixtures built from the real per_item shapes in heal31.json (the Blue Visby item's tokens and sentences verbatim), and a test that the default dispatch never writes a brief. Report: the cost attribution with numbers, the budget arithmetic, the doctrine citations, files, exported functions, tests, fitness line, the exact maintenance dispatch inputs for (a) the dry run and (b) the strip apply, commit hash, anything refused with the reason.`
const r = await agent(P, { label: 'HEAL-10', phase: 'Lane', model: 'sonnet' })
return { heal10: r }
