export const meta = {
  name: 'lane-perf10',
  description: 'Sonnet lane PERF-10: the instant-click law is not met after PERF-8/9 because every route is still dynamic; this lane makes the public content of the listing and detail routes cached/static (Cache Components or the RPC split, whichever the measurement justifies) with the per-viewer layer client-side',
  phases: [{ title: 'Lane' }],
}
phase('Lane')
const P = `You are lane PERF-10 on Caro's Ledge (repo Dwarves77/dotfiles), the continuation of PERF-8 and PERF-9. Worktree /root/work/lanes/perf10 (branch lane/perf10-2026-09-04, based on train/wave40-2026-09-04 which carries PERF-9's three commits d86801eb, 89892f03, eca38c37 and ADR-026: read docs/decisions/ADR-026-detail-cache-and-viewer-state-split.md IN FULL first, then those commits); node_modules symlinked at fsi-app/node_modules (do not run npm install). Cannot push; coordinator lands and measures live. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. Write set: fsi-app/src/app/** , fsi-app/src/components/**, fsi-app/src/lib/data.ts, fsi-app/src/lib/supabase-server.ts, fsi-app/src/lib/detail/**, fsi-app/src/lib/api/**, fsi-app/src/middleware.ts or proxy.ts, fsi-app/next.config.*, their tests, ONE new migration fsi-app/supabase/migrations/305_<slug>.sql (written, not applied, md5-guarded like 304, inventory row in docs/inventories/migrations.md) ONLY if the RPC split is the path you take, docs/decisions/ADR-026 (amend its Follow-up section with what you did; a new ADR-027 only if you reverse a decision), and the UX compliance block (grep -rn "UX compliance" fsi-app/.discipline docs/doctrine; write it in your report, the coordinator lands it in the memory). Rule: check whether an established pattern exists before proposing a new one; flag conflicts with prior decisions (ADR-023, ADR-025, ADR-026, the PERF postscripts) and explain the trade-off before changing them. No claims ahead of evidence, [CONFIRMED]/[HYPOTHESIS]; "there is NO small follow-up fix, fix it now"; commit message ends with "Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>". Gates: fsi-app/node_modules/.bin/tsc --noEmit -p fsi-app/tsconfig.json (0 errors), node --test over every test you touch and fsi-app/src/__tests__/*.mjs, repo-root node fsi-app/.discipline/fitness/runner.mjs (0 violations) and node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs, the rendering smoke (fsi-app/.discipline/rendering), and cd fsi-app && npx next build --webpack: the route table (○ static / ◐ ISR / ƒ dynamic per route) before and after IS the evidence. Supabase MCP read-only SELECT (server "Supabase", tool execute_sql, project kwrsbpiseruzbfwjpvsp; ToolSearch "select:mcp__Supabase__execute_sql").

THE LAW (operator, 2026-09-04, verbatim): "clicking into any item or any page takes WAY too long. multiple seconds. every click should show items on a page instantly." THE STATE [CONFIRMED by PERF-9]: 130/130 routes build as ƒ (dynamic) before and after PERF-9; two causes remain: (1) src/lib/detail/load-detail-core.ts runViewerScoped() → getViewerRelevanceForItem → resolveOrgIdFromCookies() on every detail render, and the org-parameterised listing RPCs (p_org_id) on the four index pages; (2) Next 16.1.6 has no experimental.ppr; cacheComponents is a whole-app flag PERF-9 tested and reverted as too broad to flip in the same lane. PERF-9 measured that Suspense alone does not restore static eligibility (/privacy, /login, /signup stayed ƒ with zero dynamic APIs), which means the layout or config still forces dynamic rendering somewhere: FIND IT FIRST (next.config, middleware/proxy matcher, a dynamic = "force-dynamic" export, a cookies()/headers() call in a shared layout or in a provider, fetch with no-store): a page with no dynamic API must build ○; until /privacy builds ○ you have not found the root cause, and everything else is patchwork. Then the design, one of the two ADR-026 names, chosen by measurement not preference: (A) Cache Components ("use cache" on the public data reads of the listing/detail routes, per-viewer reads left uncached and streamed inside Suspense; cacheComponents: true with an inventory proving no per-viewer read is cached; a fitness function or test that fails if a function reading cookies()/headers()/the viewer's org is called inside a "use cache" scope), or (B) the RPC split (org-independent public listing RPC + a client-merged per-viewer override layer; migration 305; the four index pages and the detail core rendered from the public read with revalidateTag on apply, the existing tag model from ADR-026 §4) so that the listing and detail routes build ○/◐ and the per-viewer layer hydrates after first paint from /api/workspace/bootstrap (PERF-9). Whichever you pick, the acceptance is the route table: /regulations, /market-intel (or /market), /operations, /research, /community and the four /[slug] detail routes must build ○ or ◐, not ƒ; /privacy and the other zero-dynamic pages must build ○. Second acceptance: the discipline UX laws (docs/design/ux-laws.md) — no surface may render empty or wrong for a logged-in viewer while the per-viewer layer loads (the honest loading state, never a lie). Third: revalidation — an apply run (population-turn, maintenance, corpus-turn) must invalidate the cached public content: find the single point ADR-023/ADR-026 name and wire revalidateTag (or the route handler that calls it) there if it is not already wired; test it. Report with numbers: the root cause of the forced-dynamic (file:line), the route table before/after (paste it), what you cached and what you deliberately did not, the revalidation point, files, tests, fitness line, migration status, ADR text, commit hashes, the UX compliance block, anything refused with the reason.`
const r = await agent(P, { label: 'PERF-10', phase: 'Lane', model: 'sonnet' })
return { perf10: r }
