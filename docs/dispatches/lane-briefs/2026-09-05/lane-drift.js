export const meta = {
  name: 'lane-drift',
  description: 'Haiku lane MIGRATION-DRIFT: the live validate_item_provenance carries an own-body-types extension and the result_content rename that no committed migration file holds; write the missing migration file from the live definition so the chain matches the database',
  phases: [{ title: 'Lane', detail: 'MIGRATION-DRIFT in its own worktree' }],
}
phase('Lane')
const P = `You are lane MIGRATION-DRIFT on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/drift (branch lane/drift-2026-09-04, based on train/wave19 ed217b05); node_modules symlinked at fsi-app/node_modules. Cannot push; coordinator lands. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. Write set: ONE new file fsi-app/supabase/migrations/203_own_body_types_extension.sql (or the next free number BELOW 302 that keeps chronological order; read the directory listing and fsi-app/docs/inventories/migrations.md to choose, and explain the choice) and one row INSIDE the table of fsi-app/docs/inventories/migrations.md (the C3 consistency check fails if the row sits outside the table; read the file's table format first). Nothing else. No claims ahead of evidence; commit message ends with "Co-Authored-By: Claude Haiku <noreply@anthropic.com>". Gates: from repo root node fsi-app/.discipline/fitness/runner.mjs (0 violations) and node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs, plus the C3 consistency check if it has a local runner (grep .github/workflows/discipline.yml for the consistency job's command and run it). Supabase MCP (server "Supabase", execute_sql, project kwrsbpiseruzbfwjpvsp, via ToolSearch "select:mcp__Supabase__execute_sql") is READ-ONLY for you: SELECT only; the migration is WRITTEN, NEVER APPLIED (it records DDL that is already live).

THE DRIFT [CONFIRMED by lane HEAL-7, 2026-09-04, read-only]: select pg_get_functiondef('public.validate_item_provenance(uuid)'::regprocedure) shows (a) a c_own_body_types := ARRAY['standard','framework','initiative'] constant with a floor_scope 'voluntary_own_body' branch extending migration 202's standard-only own-body floor, and (b) ars.result_content (migration 264's rename) inside the function, while no committed file under fsi-app/supabase/migrations/ contains c_own_body_types or voluntary_own_body (grep to confirm). Also read supabase_migrations.schema_migrations (select version, name from supabase_migrations.schema_migrations order by version) to find the applied entry between 202 and 225 that carries this change, its version timestamp and name; that tells you the file name and number it should have had. Migration 302 (applied live 2026-09-04 05:49 UTC, post md5 7c4ae6dc4fa4ef1c91c63fddbc8d56cb) was written against the live body. WRITE the missing migration as a faithful record: header stating it documents DDL already applied (the schema_migrations version and date), the exact in-place edits it made relative to migration 202's text (diff 202's committed CREATE OR REPLACE body against the live definition, minus 264's rename and minus 225's criterion 7 and 300's URL regex and 302's warnings, to isolate what THIS change added), written in the same idempotent guarded-replace style as 225/300/302 so re-applying is a no-op ("already applied" notice when c_own_body_types is present). Report: the schema_migrations entry found, the isolated diff, file name and number with the reason, the inventory row, fitness, commit hash.`
const r = await agent(P, { label: 'MIGRATION-DRIFT', phase: 'Lane', model: 'haiku' })
return { drift: r }
