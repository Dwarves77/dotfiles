export const meta = {
  name: 'lane-legacy',
  description: 'Haiku lane BACKLOG-LEGACY: mint-run-001 and mint-run-005 predate per_item.item_id, so the backlog flywheel cannot select them and the gate refuses every new apply; resolve their items by CELEX/canonical key so the existing flywheel path connects them',
  phases: [{ title: 'Lane', detail: 'BACKLOG-LEGACY in its own worktree' }],
}
phase('Lane')
const P = `You are lane BACKLOG-LEGACY on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/legacy (branch lane/legacy-2026-09-04, based on train/wave19 ed217b05 = origin/master + HEAL-7 + TANDEM-2, landing now); node_modules symlinked at fsi-app/node_modules. Cannot push; coordinator lands. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. Write set: fsi-app/scripts/turns/run-population-flywheel.mjs (+ .test.mjs) ONLY the artifact-id resolution (extractMintedItemIds / hasRecoverableMintedIds and one new resolver), docs/runbooks/POPULATION-TURN-RUNBOOK.md (one paragraph). Nothing else; do not touch population-turn.yml or MINT-RUNBOOK.md (a governing file; if F28 fires anyway, re-pin fsi-app/scripts/harness-runs/mint/PENDING-RUN.md by appending "What changed (9)" in its chained format and making the LAST "**harness_version at write time:** \`sha256:...\`" line carry the hash the runner prints, rewording the previous final line to "at train/wave19's write time (superseded below)"). Deterministic, $0, no LLM; read every file you touch in full; no claims ahead of evidence; commit message ends with "Co-Authored-By: Claude Haiku <noreply@anthropic.com>". Gates: node --test scripts/turns/run-population-flywheel.test.mjs; from repo root node fsi-app/.discipline/fitness/runner.mjs (0 violations) and node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs. Supabase MCP (server "Supabase", execute_sql, project kwrsbpiseruzbfwjpvsp, via ToolSearch) READ-ONLY.

THE PROBLEM [CONFIRMED by lane TANDEM-2, 2026-09-04]: THE GATE (checkAllSlicesConnected) refuses every new population apply until every mint artifact with minted > 0 carries the three §9 outcome keys. mint-run-001 (6 items) and mint-run-005 (5 items) predate per_item.item_id, so extractMintedItemIds finds no ids, hasRecoverableMintedIds marks them unrecoverable, --backlog skips them, and the gate keeps refusing. Read both artifacts (fsi-app/scripts/harness-runs/mint/mint-run-001.json and mint-run-005.json) in full: what identity do their per_item entries carry (CELEX id? canonical_instrument_key? source_url? title?). BUILD: a resolver in run-population-flywheel.mjs that, for an artifact without per_item.item_id, maps each per_item entry to intelligence_items.id deterministically through the strongest key present (canonical_instrument_key, then instrument_identifier, then source_url + title exact match), via a read-only SELECT the driver already knows how to issue (read how it reads items elsewhere in the file and reuse that db access; if the driver has no read path yet, add one through scripts/lib/db.mjs's existing readAll helper, never a new client); an entry that resolves to zero or more than one item is reported as unresolved and the artifact stays unrecoverable with the list in the report, never guessed. Wire it so --backlog and --mint-run use resolved ids exactly like item_id ones, with an artifact-level note in the §9 outcomes recording that ids were resolved by key (e.g. outcomes.ids_resolved_by_key: N). Measure read-only against the live table which of the 11 entries resolve today and put the counts at the top of your report. Tests with fixtures built from the two real artifacts. Report: the identity fields found, resolution counts, files, exported functions, tests, fitness, marker hash if re-pinned, commit hash.`
const r = await agent(P, { label: 'BACKLOG-LEGACY', phase: 'Lane', model: 'haiku' })
return { legacy: r }
