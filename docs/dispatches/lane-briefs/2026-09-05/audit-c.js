export const meta = {
  name: 'audit-c',
  description: 'Wiring audit 2026-09-04, part C: the flywheel loop map with every handoff (C1) and rulings/ADRs/specs vs implementation since 2026-08-21 (C2)',
  phases: [{ title: 'Audit' }],
}
phase('Audit')
const COMMON = `Repo Dwarves77/dotfiles, worktree /root/work/lanes/audit (branch lane/audit-2026-09-04 = origin/master e8cb748f); node_modules symlinked at fsi-app/node_modules. READ-ONLY audit: touch NOTHING except your own output file under docs/audits/wiring-audit-2026-09-04/ (named below). Do not commit. Window: every commit since 2026-08-21 (docs/audits/wiring-audit-2026-09-04/_prs.txt, 107 PRs). Evidence rules: cite file:line / commit / artifact / read-only SQL for every verdict; [CONFIRMED]/[HYPOTHESIS]. Supabase MCP (server "Supabase", tool execute_sql, project kwrsbpiseruzbfwjpvsp; ToolSearch "select:mcp__Supabase__execute_sql") READ-ONLY SELECT only. Operator's question: "are all of our site tools built and wired? I want an audit of everything built in the last two weeks to see if it is being used and wired and fits within the flywheel loop."`
const C1 = agent(`${COMMON}

You are AUDIT-C1: THE LOOP MAP. Output file: docs/audits/wiring-audit-2026-09-04/C1-loop-map.md. Build the flywheel loop as the system actually runs today, stage by stage, from docs/specs/08-flywheel-design.md, docs/decisions/ADR-023 (producer execution model), ADR-024 (decision propagation), ADR-025 (deterministic derivations auto-adopt), CLAUDE.md rules 16-18, docs/PROGRAM-BOARD.md (first 200 lines and the 2026-09-0x rows), and the code: for each stage name the component that fills it, its state (LIVE-AND-RUN today / DISPATCH-ONLY / DRY-ONLY / OPERATOR-GATED (name the gate) / DESIGNED-ONLY), the artifact family that records it, and every HANDOFF between stages: which are automatic inside one run (e.g. mint -> flywheel in population-turn), which need a coordinator dispatch, which need a coordinator to land a branch (deliver-artifact-branch.sh refused-PR fallback), which need a train to land before the next stage's gate opens (the stamps-on-master gate), and which are blocked on an operator ruling (ledger-consume flip, standards-body tier, grounding). Draw it as a mermaid flowchart (fenced mermaid) with stages as nodes and handoffs as labelled edges, colour-coded by state, plus a table of the handoffs with "who does it today" and "what it would take to remove the human". Say plainly where the loop is closed and where it is open, and what the minimum set of changes is that would let one dispatch of source-sweep flow all the way to a customer surface without a coordinator, WITHOUT violating rule 16 (no standing schedules during build) or ADR-025.`, { label: 'AUDIT-C1', phase: 'Audit', model: 'sonnet' })
const C2 = agent(`${COMMON}

You are AUDIT-C2: RULINGS, ADRs, SPECS vs IMPLEMENTATION. Output file: docs/audits/wiring-audit-2026-09-04/C2-rulings-vs-implementation.md. (1) Every ADR and spec created or changed since 2026-08-21 (git log --since=2026-08-21 -- docs/decisions docs/specs docs/doctrine): implemented / partially / not, with the implementing files and the gap. (2) Every OPERATOR RULING recorded in docs/ops/session-log.md since 2026-08-21 (grep for "operator" and "ruling" and "verbatim" in Addenda 79-85; quote each faithfully, short) with its status: implemented (where), pending operator, or contradicted by something that shipped (say what). Include today's: rule 17 "nothing runs alone", rule 18 "get the source, rate the source, publish", "no human gate in the flywheel" (ADR-025), "if items are flagged not credible attach a source", "build before populate", "population is $0/no LLM", "no standing schedules during build", "community: no room per topic, people start rooms, regions exist", "every click should show items instantly", "use the browser not the API when Haiku can do it free". (3) The open operator decisions list, deduplicated, each with what it blocks. (4) A count: of the 107 PRs, how many touched a customer surface, how many a runtime, how many the harness/discipline layer only, how many docs only; one line each. Be precise and short; a table per section.`, { label: 'AUDIT-C2', phase: 'Audit', model: 'sonnet' })
const [c1, c2] = await Promise.all([C1, C2])
return { c1, c2 }
