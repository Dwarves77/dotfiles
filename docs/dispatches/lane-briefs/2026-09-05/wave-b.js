export const meta = {
  name: 'wave-b',
  description: 'Plan T39/T40 lanes: TURNREQ (corpus_turn_requests consumed by corpus-turn, last-turn-date retired) and T4-OVERRIDE (standards bodies at T4 via institution-canonicalize and the host class table)',
  phases: [{ title: 'Lanes' }],
}
phase('Lanes')
const C = (name, wt) => `You are lane ${name} on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/${wt} (branch lane/${wt}-2026-09-04, based on origin/master e8cb748f); node_modules symlinked at fsi-app/node_modules. Cannot push; coordinator lands. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. READ FIRST: docs/plans/complete-system-build-plan-2026-09-04.md on branch train/wave37-2026-09-04 in worktree /root/work/lanes/train37 (§0 definition of done applies to YOUR lane: reachable, run-able by a recorded dispatch, populated, visible, gated, documented; you cannot dispatch, so deliver everything that makes the first dispatch succeed and name it), and docs/audits/wiring-audit-2026-09-04/ (same worktree) for the evidence. CLAUDE.md rules: one writer per dataset; no LLM/API spend in runtimes; no schedules; no copies of logic; "there is NO small follow-up fix, fix it now"; [CONFIRMED]/[HYPOTHESIS]; read every file you touch IN FULL; commit message ends with "Co-Authored-By: Claude Sonnet <noreply@anthropic.com>". Gates: node --test over every test you touch; repo-root node fsi-app/.discipline/fitness/runner.mjs (0 violations; chain/re-pin a family marker if you move a governing file) and node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs; yaml check for workflows; tsc if .ts touched. Supabase MCP read-only SELECT (server "Supabase", tool execute_sql, project kwrsbpiseruzbfwjpvsp; ToolSearch "select:mcp__Supabase__execute_sql"); never write. Report: files, exported functions, tests, fitness line, marker hashes, commit hash, the exact first dispatch, anything refused with the reason.`

const B1 = agent(`${C('TURNREQ','turnreq')}

Write set: fsi-app/scripts/turns/consume-turn-requests.mjs (+test), .github/workflows/corpus-turn.yml, fsi-app/scripts/turns/last-turn-date.mjs (+test: retire it, or reduce it to a thin reader of the ticket queue if a caller genuinely needs a date; one mechanism), fsi-app/scripts/turns/export-corpus-for-extraction.mjs and any corpus-turn driver only where they take the item selection (read corpus-turn.yml's run: lines to find the drivers), docs/runbooks/CORPUS-TURN-RUNBOOK.md, fsi-app/scripts/harness-runs/<corpus-turn family if one exists, else the families corpus-turn writes: forward-events/...>/PENDING-RUN.md if a governing file moves, fsi-app/docs/inventories/shared-dataset-ownership.md (corpus_turn_requests consumer registration). THE FACTS [CONFIRMED, audit B1 gap 2, B2]: corpus_turn_requests (migration 277) is filled by a trigger and has 1,709 open / 0 consumed rows; scripts/turns/consume-turn-requests.mjs is its only reader and has no caller; corpus-turn.yml computes its own --since via last-turn-date.mjs (a marker file), so two "what changed" mechanisms exist and only the wrong one is wired; the corpus-turn family has no run artifact. BUILD: corpus-turn's item selection comes from the ticket queue: consume-turn-requests selects open tickets (bounded by --limit, oldest first, grouped by item), the turn runs discovery + forward events over exactly those items through the existing drivers, and marks tickets consumed (consumed_at, run id) through the guarded path only after the turn's writes succeeded; --since stays only as an explicit override for backfills, documented as such, and last-turn-date.mjs's marker is retired unless something else reads it (grep; report). The workflow input: limit (default from measurement of 1,709 tickets and the job timeout: state the arithmetic), mode dry/apply; dry lists the tickets it would consume and writes nothing. Artifact: corpus-turn's run artifact per F28 (create the family properly if it does not exist: CONVENTION.md registration, ALLOWED_FAMILIES, governing-files.mjs entry, LAST-PROPOSER-PASS.md seed per PROPOSER-RUNBOOK; read how ledger-consume was registered in #5xx and copy that shape). Tests: selection order and bounds, consumed-only-after-success, dry writes nothing, idempotent re-run. First dispatch: corpus-turn mode=dry limit=<N>, then apply.`, { label: 'TURNREQ', phase: 'Lanes', model: 'sonnet' })

const B2 = agent(`${C('T4-OVERRIDE','t4')}

Write set: fsi-app/scripts/maintenance/institution-canonicalize.mjs (+test), the host class table module classTierForHost lives in (find it: grep -rn "classTierForHost" fsi-app/src fsi-app/scripts; +test), fsi-app/scripts/mint/heal-provenance.mjs ONLY if the class table's consumer there needs the new class name (do not change heal logic), docs/runbooks/MAINTENANCE-RUNBOOK.md (institution-canonicalize section: the audit found it undocumented; write it), fsi-app/scripts/harness-runs/mint/PENDING-RUN.md if a mint governing file moves (heal-provenance.mjs is not one; check governing-files.mjs), docs/decisions/ADR-002 or the source-tier doctrine file only to record the class (find the tier vocabulary's home: grep -rn "base_tier" docs/decisions docs/doctrine). THE FACTS [CONFIRMED, audit C1 §6 and C2]: institution-canonicalize Part C reports ruling_needed for ifrs.org, cdp.net, sciencebasedtargets.org: the host class table rates them T5 while the class table's own rule for a body's own text is T4; SC-13 forbids inventing a tier, so STEP SOURCE marks their figures worklist_ambiguous_host and rule 18 (get the source, rate the source, publish) cannot reach them. OPERATOR RULING (2026-09-04, verbatim): "you know how to classify, fix it … T4". BUILD: a standards_body host class in the class table (ISSB/IFRS Foundation, CDP, SBTi, and by the same rule GHG Protocol/WRI-WBCSD, ISO, GRI, TCFD/TNFD if their hosts are present in sources: read the live sources table for hosts that match and list them; classify by rule, not by an ad-hoc list: a body that publishes standards/frameworks companies report against, rated T4 for its own text, never T1-T3), institution-canonicalize Part C applies the override to the three named hosts in apply mode (guarded write of sources.base_tier or whatever column the tier lives in; read the schema) and to any other live host the new class matches, with dry listing them all; tests for the class rule and for the apply plan; read-only SQL before/after counts in your report. First dispatch: maintenance institution-canonicalize dry, then apply.`, { label: 'T4-OVERRIDE', phase: 'Lanes', model: 'sonnet' })

const [b1, b2] = await Promise.all([B1, B2])
return { b1, b2 }
