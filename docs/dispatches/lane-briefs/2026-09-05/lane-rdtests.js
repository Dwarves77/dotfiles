export const meta = {
  name: 'lane-rdtests',
  description: 'Sonnet lane RD-TESTS: three market tests assert the LIVE series-item-map data module is unratified (0 ratified / 6 pending); ruling R-D just ratified all six, so the tests now fail; make them fixture-driven so they test the mechanism, never the live map\'s current state',
  phases: [{ title: 'Lane', detail: 'RD-TESTS in its own worktree' }],
}
phase('Lane')
const P = `You are lane RD-TESTS on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/rdtests (branch lane/rdtests-2026-09-04, based on train/wave37-2026-09-04 at ef5602b6, whose HEAD commit ratified all six oil-bulletin series in fsi-app/src/lib/market/series-item-map.mjs via scripts/producers/market/ratify-series-items.mjs --apply against mint-run-024 and mint-run-029; read that commit); node_modules symlinked at fsi-app/node_modules. Cannot push; coordinator lands. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md, and do NOT touch src/lib/market/series-item-map.mjs (it is now the ratified truth). Write set: fsi-app/scripts/producers/market/ratify-series-items.test.mjs, fsi-app/scripts/producers/market/refresh-published-price-statistics.test.mjs, and ONLY IF a test needs a way to point the script at a fixture map that does not exist yet: fsi-app/scripts/producers/market/refresh-published-price-statistics.mjs / ratify-series-items.mjs (a --map <path> flag or an env var, read in full first; prefer whatever injection the scripts already expose, e.g. an exported main(deps) or a path argument; never a second copy of logic). Deterministic; no claims ahead of evidence; commit message ends with "Co-Authored-By: Claude Sonnet <noreply@anthropic.com>". Gates: node --test fsi-app/scripts/producers/market/*.test.mjs fsi-app/src/lib/market/*.test.mjs (81 pass, 0 fail); repo-root node fsi-app/.discipline/fitness/runner.mjs (report the line; two F28 STALE PROPOSER ATTESTATION violations are expected on this branch and are being fixed by another lane, name them and ignore them; anything else is yours) and node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs (141).

THE FAILURES [CONFIRMED by the coordinator, node --test on ef5602b6]: (1) ratify-series-items.test.mjs:161 "CLI --mint-run dry run against a fixture artifact: reports ratifications, writes nothing" expects RATIFY for eurosuper-95 but the CLI now reads the LIVE map (already_ratified for all six); (2) refresh-published-price-statistics.test.mjs:45 "dry run (no flags): exit 0, reports 0 ratified / 6 pending…" — live map now 6 ratified / 0 pending; (3) refresh-published-price-statistics.test.mjs:55 "--apply with zero ratified entries also exits 0 without requiring DB creds" — with 6 ratified entries --apply now correctly demands DB creds (exit 1). All three tests assert the live module's state at the time they were written; the state was always going to change the day the ruling landed. Fix the TESTS, not the state: each test must run against a fixture map it builds itself (a temp .mjs data module with 6 pending entries, or 6 ratified, as the test needs), through whatever injection the scripts expose; if none exists, add the smallest one (an optional --map path argument or an exported function taking the map object) and keep the default path = the live module untouched. Add one NEW test per script asserting the live module's CURRENT invariant that will hold from now on: every non-underscore key is status "ratified" with a uuid-shaped item_id (that is the ruling), so a future regression to pending is caught. Report: which injection you used, files, test counts before/after, fitness line, commit hash.`
const r = await agent(P, { label: 'RD-TESTS', phase: 'Lane', model: 'sonnet' })
return { rdtests: r }
