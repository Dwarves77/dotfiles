export const meta = {
  name: 'lane-perf12merge',
  description: 'Sonnet lane PERF-12-MERGE: land PERF-12 (cursor rows + TanStack Virtual) onto train 44 after PERF-MERGE, on the public-RPC architecture PERF-10 established — one org-independent, cacheable cursor path; migrations renumbered and written to REPLACE overloads; fail-soft ladders removed once the DDL is live',
  phases: [{ title: 'Lane' }],
}
phase('Lane')
const P = `You are lane PERF-12-MERGE on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/perf12merge (branch lane/perf12merge-2026-09-04, based on train/wave44-2026-09-04 = origin/master a58478cc + the 305 correction + PERF-MERGE's landed PERF-10 + #418/banner fixes + ledger artifacts). node_modules: this worktree has its OWN copied node_modules (from /root/work/lanes/perf12/fsi-app/node_modules, which carries @tanstack/react-query@5 and @tanstack/react-virtual@3). Node 24 is at /home/claude/.npm/_npx/387698761821791d/node_modules/node/bin/node (use it for node --test and for next build). Cannot push; coordinator lands. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. CLAUDE.md rules: no copies of logic; no dead code; "there is NO small follow-up fix, fix it now"; [CONFIRMED]/[HYPOTHESIS]; read every file you touch IN FULL; commit message ends with "Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>".

OPERATOR RULINGS THAT GOVERN THIS LANE (verbatim): "you dont load every item at once, basic 101 developing"; "loading is faster but each page is blank when you navigate to it, there should be zero wait time for pages to load"; "modern websites do not have blank pages and take time to load"; "these solutions have been developed already, you dont need to reinvent a fix, find out how others do it and do the same"; no spend-based fixes.

READ FIRST, IN FULL: docs/decisions/ADR-027-standard-fast-page-architecture.md, docs/decisions/ADR-026-detail-cache-and-viewer-state-split.md, docs/audits/perf-waterfall-2026-09-04.md, fsi-app/supabase/migrations/305_workspace_intelligence_listings_domain_param.sql (APPLIED LIVE; note how it REPLACES the overload: EXECUTE new def, DROP the old signature, GRANT, assert count = 1) and 306_public_workspace_intelligence_listings.sql (PERF-10, NOT YET APPLIED — you may edit it), fsi-app/src/lib/data.ts, fsi-app/src/lib/supabase-server.ts, fsi-app/src/app/regulations/page.tsx, fsi-app/src/components/regulations/RegulationsLedger.tsx, fsi-app/src/lib/list-pagination.ts, fsi-app/src/app/api/listings/rest/route.ts, and on branch lane/perf12-2026-09-04 (worktree /root/work/lanes/perf12) the five PERF-12 commits af1139c2 f96eccf2 36203004 da1b8857 5e289b6b in full (git show), plus its migrations 306_workspace_intelligence_listings_cursor.sql and 307_custom_access_token_org_claim_hook.sql.

THE JOB: cherry-pick the five PERF-12 commits onto this branch (git cherry-pick -x, one at a time; resolve conflicts BY DESIGN, not by keeping one side), then reconcile the two architectures into ONE:

(1) PUBLIC DATA IS ORG-INDEPENDENT AND CACHEABLE, PER-VIEWER STATE IS AN OVERLAY. PERF-10 (now on this base) serves the four index pages from org-independent *_public RPCs (unstable_cache, PUBLIC_ITEMS_TAG) and merges workspace overrides client-side via mergeWithOverrides. PERF-12's cursor route (/api/listings/cursor) currently calls the ORG-scoped get_workspace_intelligence_listings via resolveOrgIdFromCookies() (2 sequential DB hops per scroll page, private no-store, an X-Org-Id 409 check). Make the scroll path use the same public architecture: add the keyset params (p_after_priority text, p_after_added_date date, p_after_id uuid, all DEFAULT NULL, trailing) to get_workspace_intelligence_listings_public in PERF-10's 306 (not applied yet, so edit it in place: one CREATE, one GRANT for the final signature, and a post-check asserting exactly one overload per public function), route /api/listings/cursor through the public RPC with p_domain + cursor, no org resolution, and make the response cacheable (Cache-Control public, s-maxage with stale-while-revalidate, keyed by the cursor; document why a stale nextCursor cannot dangle: keyset re-anchors on (rank, added_date, id), a moved row is simply skipped or re-seen, never a hole). Delete the X-Org-Id header check and the header-sending client code (PERF-12 item 4) — they exist only because the route was org-scoped. Keep the ADR-027 D5 auth-hook migration ONLY if something reads the claim; PERF-12 shipped no reader, so decide: either delete the hook migration and leave D5 as ADR-027's open item with a one-line note in docs/inventories/migrations.md, or keep it if a reader is in this train. No dead migrations.

(2) NO SECOND OVERLOAD, EVER. PERF-12's 306_workspace_intelligence_listings_cursor.sql uses CREATE OR REPLACE with a NEW signature, which leaves the live 2-arg overload in place beside a 5-arg one and makes every PostgREST rpc() call ambiguous (PERF-11's 305 had this exact defect and was corrected before it was applied). Decide whether the ORG-scoped cursor variant is needed at all: if after (1) nothing calls get_workspace_intelligence_listings with a cursor, DELETE that migration and the CURSOR_SCOPED_RPCS/afterPriority plumbing in supabase-server.ts's org path (grep every consumer first; ADR-027 §2 wants ONE cursor mechanism, not two). If something does need it, renumber it to the next free number and rewrite it to REPLACE the overload the way the corrected 305 does: pin the pre-image to the LIVE post-305 md5 7944932d0d139fade949f81fa458801f [CONFIRMED by the coordinator via Supabase MCP 2026-09-04 20:15], EXECUTE the new def, DROP FUNCTION public.get_workspace_intelligence_listings(uuid, integer), GRANT EXECUTE on the new signature TO anon, authenticated, service_role, assert count = 1. Also fix 'CASE p_after_priority WHEN NULL THEN NULL' (never matches; write IS NULL).

(3) THE FAIL-SOFT LADDERS COME OUT. The coordinator applies 306 (and any successor) via Supabase MCP BEFORE this train merges (DDL-before-code), so the try-cursor→try-domain→unscoped retry ladders in supabase-server.ts (PERF-11's domain retry and PERF-12's cursor retry) are dead branches once live: remove them and call the final signature directly; a missing function must fail loudly in the route (500 with the Supabase error in the log), not silently degrade to the wrong page of the wrong corpus. Keep the tests honest: delete/rewrite supabase-server-domain-scope.test.mjs and supabase-server-cursor-scope.test.mjs to assert the single call shape.

(4) FIRST PAINT ON NAVIGATION. The operator's complaint is blank pages on click. Prove the /regulations → /regulations/[slug] and nav-link paths paint a real shell immediately: loading.tsx for every index route and the four [slug] routes renders the page's own masthead + the ledger's row skeleton (never blank, never a spinner-only), Link prefetch on rows and nav, and the first 30 rows are in the SSR HTML (static/ISR via PERF-10) so the list is visible before any JS. Cite file:line for each. If a page still renders blank on client navigation, find the cause (a Suspense boundary above the shell? a client component gating the whole page on bootstrap?) and fix it.

(4b) PERF-MERGE'S OWN LEFTOVERS, which the operator's rule ("there is NO small follow-up fix") makes yours: (i) PERF-MERGE turned ObligationRegister.tsx into a client component that fetches its first page ON MOUNT — that is a blank register on every load of /regulations, the exact thing the operator named. The register's first unfiltered page must be in the SSR HTML from the same public, cached data path as the listings (an org-independent obligations read, cached under PUBLIC_ITEMS_TAG, or a public RPC added to 306 if the current read needs cookies), with the filter bar hydrating from it; "Load more"/filters stay client-side against /api/obligations/register. (ii) The unpinned .toLocaleString()/toLocaleDateString() calls repo-wide (~30 sites in admin/community/profile components per PERF-MERGE's grep) are the same #418 class: introduce ONE shared formatter module (src/lib/format/number.ts or extend format-fixed-date.ts, whichever already exists — no second copy) pinned to en-US, and route every site through it; a test that greps src/ for unpinned calls and fails on any. (iii) RegulationsLedger.tsx's useState(() => Date.now()) hydration hazard: compute the reference time on the server once and pass it down, or render countdowns client-only after mount with a stable SSR placeholder. (iv) PERF-MERGE added PUBLIC_DOMAIN_SCOPED_RPCS with another fail-soft ladder — item (3) covers it: remove once 306 is live.

(5) MEASURE. next build --webpack: route table (expect /regulations ○ or ●, /api/listings/cursor ƒ, the four [slug] ●). Rendering smoke via node fsi-app/.discipline/rendering/run-rendering-guard.mjs (read how PERF-12 wired QueryProvider into it). Bytes per scroll page from toLedgerRowPayload over fixture rows. DOM rows under virtualization (PERF-12's own Playwright smoke). Update fsi-app/src/lib/perf/perf-budget.mjs to the measured numbers and set sequentialDbHops for regulations-list and the cursor route to 1.

(6) GATES, from the repo root with Node 24: node fsi-app/.discipline/fitness/runner.mjs (0 violations on files you touch; if F25/F23 pre-existing violations remain, list them); node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs; node fsi-app/.discipline/governance/closure-gate.mjs; tsc --noEmit; bash fsi-app/.discipline/run-test-suite.sh (NOTE: CI runs this suite WITHOUT node_modules — any test importing react/next/@tanstack must be named *.npmtest.mjs, and every *.npmtest.mjs must be listed where the runbook says; check how server-timing.npmtest.mjs is wired and match it); the full node --test over fsi-app; docs/inventories/migrations.md rows correct (numbers, applied/not-applied status); the UX compliance requirement: since fsi-app/src/**/*.tsx changed, write a "UX compliance" block (docs/design/ux-laws.md; the memory gate checks for it) into your REPORT text so the coordinator pastes it into the session log.

Commit in logical commits (the cherry-picks, then one reconciliation commit per numbered item above or fewer if they are inseparable). REPORT: final migration list with numbers and apply order and the exact post-apply verification SQL for each; the single cursor mechanism and the file:line of every caller; deleted code (files/functions) with the reason; the loading.tsx/prefetch evidence per route; the route table; the measured numbers vs before; every gate's output line; commit hashes; anything refused with the reason.`
return { merge: await agent(P, { label: 'PERF-12-MERGE', phase: 'Lane', model: 'sonnet' }) }
