export const meta = {
  name: 'wave-a',
  description: 'Plan T39 lanes: LEDGER-ZERO (ledger-consume at $0 with session verdicts, flip, workflow_run from source-sweep) and REVIEW-WIRE (four ratification apply steps wired into maintenance.yml, review-digests fixed, community-topics-seed retired)',
  phases: [{ title: 'Lanes' }],
}
phase('Lanes')
const C = (name, wt) => `You are lane ${name} on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/${wt} (branch lane/${wt}-2026-09-04, based on origin/master e8cb748f); node_modules symlinked at fsi-app/node_modules. Cannot push; coordinator lands. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. READ FIRST: docs/plans/complete-system-build-plan-2026-09-04.md is on branch train/wave37-2026-09-04 in worktree /root/work/lanes/train37 (read it there: §0 definition of done applies to YOUR lane: reachable, run-able by a recorded dispatch, populated, visible, gated, documented; you cannot dispatch, so you deliver everything that makes the first dispatch succeed and you name the exact dispatch), and docs/audits/wiring-audit-2026-09-04/ (same worktree) for the evidence on your components. CLAUDE.md rules: one writer per dataset; no LLM/API spend in runtimes; no schedules; no copies of logic (one body, imported); "there is NO small follow-up fix, fix it now"; no claims ahead of evidence, [CONFIRMED]/[HYPOTHESIS]; read every file you touch IN FULL; commit message ends with "Co-Authored-By: Claude Sonnet <noreply@anthropic.com>". Gates before commit: node --test over every test you touch; from repo root node fsi-app/.discipline/fitness/runner.mjs (0 violations: if a harness governing file you touch moves a family hash, chain its PENDING-RUN.md marker in its existing format and pin the printed hash) and node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs (141+); python3 -c "import yaml; yaml.safe_load(open(f))" for every workflow you edit; tsc (fsi-app/node_modules/.bin/tsc --noEmit -p fsi-app/tsconfig.json) if you touch .ts. Supabase MCP read-only SELECT is available (server "Supabase", tool execute_sql, project kwrsbpiseruzbfwjpvsp; ToolSearch "select:mcp__Supabase__execute_sql"); never write through it. Report: files, exported functions, tests, fitness line, marker hashes, commit hash, the exact first dispatch (workflow, inputs) that proves the component per §0, anything refused with the reason.`

const A1 = agent(`${C('LEDGER-ZERO','ledgerzero')}

Write set: fsi-app/scripts/turns/run-ledger-consume.mjs (+test), fsi-app/src/lib/intake/portal-harvest.ts (+test), fsi-app/src/lib/llm/first-fetch-classify.ts (+test), .github/workflows/ledger-consume.yml, fsi-app/scripts/harness-runs/ledger-consume/PENDING-RUN.md (run-ledger-consume.mjs and both src files are ledger-consume GOVERNING files: chain and re-pin), docs/runbooks/CORPUS-TURN-RUNBOOK.md (the ledger-consume section), docs/decisions/ADR-023-producer-execution-model.md only to record the flip per its own mechanism, fsi-app/docs/inventories/shared-dataset-ownership.md if a new write path appears, a NEW fsi-app/scripts/turns/ledger-verdicts/README.md + JSON schema file for the verdict file. THE FACTS [CONFIRMED, audit A1/B1/B2]: ledger-consume.yml has never run; LEDGER_CONSUME_APPLY_ENABLED = false is a source constant at run-ledger-consume.mjs:~95; plan mode itself fails without ANTHROPIC_API_KEY in WORKFLOW_SECRETS; first-fetch-classify.ts calls Haiku through the metered API (~$0.001/candidate, HAIKU_MODEL claude-haiku-4-5-20251001, the prompt at its line ~52 defines entity_verdict specific_document|portal|uncertain and the item_type routing rule); 1,837 portal_link_candidates status=candidate wait. OPERATOR RULINGS (2026-09-04, verbatim): "stop offering API when you have a free option with Haiku"; "why is this costing me anything when it can be done for free?"; the flip is taken. BUILD: (1) a session-verdict file contract: fsi-app/scripts/_snapshots/ledger-verdicts/<batch>.json (gitignored dir? check .gitignore; if _snapshots is gitignored, the workflow must accept the file via a repo path input OR a committed path under scripts/turns/ledger-verdicts/ — choose the committed path so a dispatch can read it, document why) with entries {candidate_id, url, entity_verdict, item_type, confidence, rationale, classified_by: "session-haiku", classified_at, prompt_version} where prompt_version is a hash of the exact prompt text exported from first-fetch-classify.ts (export the prompt builder so the session lanes use the identical prompt; one body); (2) run-ledger-consume.mjs gains --verdicts <path>: for a candidate with a verdict in the file the classify call is bypassed and the verdict is used (recorded in agent_runs/spend telemetry as $0 with source "session-verdict"), candidates without a verdict are SKIPPED with a named outcome (never sent to the API: the API path stays only for an explicit --allow-api flag that defaults false and is not exposed by the workflow); (3) flip LEDGER_CONSUME_APPLY_ENABLED to true in the same diff, citing the operator ruling and ADR-023; remove the ANTHROPIC_API_KEY precondition from the workflow's plan path (keep the secrets check for what the run actually needs: Supabase creds); (4) workflow: inputs mode (plan/apply), verdicts_file (repo path), limit; add a workflow_run trigger on "Source sweep" completed (event-driven, NOT a schedule; on that trigger run mode=plan with the newest verdicts file if present, and name in a comment that apply requires an explicit dispatch or the presence of a verdicts file; decide and document); (5) artifact family ledger-consume per F28 with per_item outcomes and metrics {candidates, with_verdict, without_verdict_skipped, promoted, rejected, uncertain, est_usd: 0}; (6) a --export-candidates <path> read-only mode that writes the candidate list (id, url, source_id, anchor_text, first_seen_at, and the fetched page text if the runtime already stores a first fetch; else say the session lanes must fetch through the browser) so the coordinator's Haiku lanes can classify offline. Tests for the bypass, the skip, the schema validation, the $0 telemetry. First dispatch to name: ledger-consume mode=plan verdicts_file=<the first batch file>.`, { label: 'LEDGER-ZERO', phase: 'Lanes', model: 'sonnet' })

const A2 = agent(`${C('REVIEW-WIRE','reviewwire')}

Write set: .github/workflows/maintenance.yml (add four steps; remove the community-topics-seed step), fsi-app/scripts/maintenance/review-apply-portal-links.mjs, review-apply-provisional-sources.mjs, review-apply-canonical-candidates.mjs, review-apply-coverage-gaps.mjs (NEW thin wrappers in the exact pattern of the existing maintenance wrappers, e.g. scripts/maintenance/reopen-validation-holds.mjs: import the review script's exported main, dry/apply, summary.json, read-back; never reimplement), fsi-app/scripts/maintenance/review-digests.mjs (fix: the runbook says the builder "does not exist"; scripts/review/build-review-digests.mjs exists, wire it properly), their tests, DELETE fsi-app/scripts/seed/community-topics-seed.mjs, fsi-app/scripts/maintenance/community-topics-seed.mjs and their tests (operator ruling 2026-09-04: "community should not be populated with rooms for every topic; people start rooms; regions exist"; audit A2: the seeder would create a fixed 7-topic taxonomy under one user's RLS scope; cite the ruling in the commit), docs/runbooks/MAINTENANCE-RUNBOOK.md (§1 community-topics-seed → retired with the ruling; §6 review-digests corrected; four new sections for the apply steps; plus the four steps the audit found undocumented: apply-classifications, seed-benchmark-instruments, spec09-reroute, institution-canonicalize: write their sections from their scripts' headers), fsi-app/docs/inventories/shared-dataset-ownership.md (registry lines for the four writers, one-writer rule), fsi-app/.discipline/run-test-suite.sh only to wire new test files. THE FACTS [CONFIRMED, audit B1 gap 1]: scripts/review/build-review-digests.mjs's QUEUES[].maintStep names review-apply-provisional-sources, review-apply-canonical-candidates, review-apply-portal-links, review-apply-coverage-gaps; none exists in maintenance.yml; each apply-*.mjs is tested and writes through the guarded path; live: portal_link_candidates 1,837 candidate / 3 promoted; sources.status provisional 911; coverage_gap_candidates disposition null 91 (verify counts read-only). READ the four apply scripts and the digest builder in full: what each apply does (portal links: promote/drop; provisional sources: confirm/retire; canonical candidates; coverage gaps) and what arg each needs (a digest's decisions? an id list? an "all" mode?): the wrappers must expose exactly that through the maintenance arg, refuse blank scope where the upstream tool forbids blanket runs, and never invent a disposition. Name the first dispatch for each (dry) and what the operator/coordinator reads back.`, { label: 'REVIEW-WIRE', phase: 'Lanes', model: 'sonnet' })

const [a1, a2] = await Promise.all([A1, A2])
return { a1, a2 }
