export const meta = {
  name: 'lane-heal7',
  description: 'Sonnet lane HEAL-7 (operator ruling 2026-09-04: "get the source, then rate the source, then publish the data"): every orphan figure gets its source registered, rated by tier, captured and grounded; criterion 3 becomes a rating, not a refusal',
  phases: [{ title: 'Lane', detail: 'HEAL-7 in its own worktree' }],
}
phase('Lane')
const P = `You are lane HEAL-7 on Caro's Ledge (repo Dwarves77/dotfiles). RESUME NOTE: a previous run of this lane was killed after 130 tool calls with UNCOMMITTED work in this worktree (git status shows eight modified/new files). Start by reading git status and git diff in full, decide what is complete and correct, keep what is, finish the rest; do not start from scratch and do not discard that work unread. Worktree /root/work/lanes/heal7 (branch lane/heal7-2026-09-04, based on origin/master e6eff093); node_modules symlinked at fsi-app/node_modules. Cannot push; coordinator lands. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. Write set: fsi-app/scripts/mint/heal-provenance.mjs (+ .test.mjs), fsi-app/scripts/maintenance/provenance-heal.mjs (+ test; the DB wrapper: add the deps the new step needs, e.g. registerSource, updateClaim, readSourceByUrl), one new migration fsi-app/supabase/migrations/302_criterion3_rating_not_refusal.sql (written NOT applied, in-place edit of validate_item_provenance following the exact idempotent pattern of 225_gate_a_criterion7.sql and 300_criterion2_url_typographic_delimiters.sql, with the self-check SQL in its header and a row INSIDE the table of fsi-app/docs/inventories/migrations.md), fsi-app/scripts/mint/validate-mint-payload.mjs (+ test) ONLY to mirror the kit's criterion-3 behaviour to the migration (the kit and the function must agree), fsi-app/docs/inventories/shared-dataset-ownership.md if you add a writer of sources/section_claim_provenance, docs/runbooks/MAINTENANCE-RUNBOOK.md provenance-heal section, and fsi-app/scripts/harness-runs/mint/PENDING-RUN.md re-pinned if F28 names the mint family (append "What changed (6)" in the chained format; the LAST line "**harness_version at write time:** \`sha256:...\`" must carry the hash the runner prints; reword the previous final line to "at train/wave17's write time (superseded below)"). Lane TANDEM-2 is editing scripts/turns/run-population-flywheel.mjs and population-turn.yml right now: do not touch them. Deterministic, $0, no LLM (fetching a cited URL through the repo's polite fetch is allowed and is the point); "there is NO small follow-up fix, fix it now"; no claims ahead of evidence, [CONFIRMED]/[HYPOTHESIS]; read every file you touch IN FULL (heal-provenance.mjs ~2,600 lines; its header is the history of six heal lanes, read all of it); commit message ends with "Co-Authored-By: Claude Sonnet <noreply@anthropic.com>". Gates: node --test on what you touch; from repo root node fsi-app/.discipline/fitness/runner.mjs (0 violations), node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs, then bash fsi-app/.discipline/run-test-suite.sh (0 fail). Supabase MCP (server "Supabase", execute_sql, project kwrsbpiseruzbfwjpvsp, via ToolSearch "select:mcp__Supabase__execute_sql") is READ-ONLY for you: SELECT to measure; never write.

THE RULING [CONFIRMED, operator, 2026-09-04, verbatim]: "get the source. then rate the source. it's that simple. this isn't hard, find the source and then publish the data on the site." Context: heal #21 (Actions 33829526120) left 94 items quarantined; HEAL-6 measured that 386 of the 824 Gate-A orphan figures in those briefs have no floor-qualifying source: 167 have NO sources row at all for the URL the figure came from, 179 have a source whose tier is ABOVE the per-item-type floor (migration 141: reg family max tier 2, research_finding max 4, technology/innovation/tool max 5; market_signal/initiative/regional_data exempt; migration 202 is the live function, read it with select pg_get_functiondef('public.validate_item_provenance(uuid)'::regprocedure)). The ruling overrules the refusal half of that floor: a figure's source is registered and RATED (its tier, from the institution class table the source registry already applies: read scripts/maintenance/institution-canonicalize.mjs, src/lib/sources/classify-source-role.ts, and wherever registerSource derives base_tier), the figure is grounded on it verbatim, and it is published with its rating visible (the credibility chips already exist on the surfaces: grep src/components for credibility/tier chip). It does NOT overrule grounding: a figure with no source anywhere stays ungrounded and the sentence is not published as fact.

BUILD, in heal-provenance.mjs as a new step SOURCE (runs after CAPTURE-CITED, before ORPHANS, dry/apply, resumable, budgeted like the others): (1) for every Gate-A orphan token of the item, find where the figure came from: the item's citations and cited URLs, its sections' links, the search results already captured (agent_run_searches.result_content), and the claim/section text around the token; HEAL-6's measurement code shows how to classify tokens, reuse it. (2) NO SOURCE ROW (167): register the cited URL's host as a source through the existing registry path (db.mjs / src/lib/sources register helpers; institutionKey dedup; base_tier from the class table, never hand-typed; the same path the source-sweep walkers use, read run-source-sweep.mjs's registerSource use), capture the page through the polite fetch (fetch-hold.mjs; Wayback fallback exists in HEAL-5, reuse), then ground the token exactly as ORPHANS does (source_span verbatim from the capture, search_result_id, source_id). (3) ABOVE FLOOR (179): ground the token on the existing source the same way; the tier is recorded on the claim (derived_tier already exists on section_claim_provenance or is derived by the trigger: read migration 145/202 to know which) and criterion 3 no longer refuses. (4) Migration 302: criterion 3 changes from failure to a rating: the function records the claim's derived tier vs the type floor in the result (e.g. a 'below_floor_facts' count and per-claim tier in v_result) but adds NO failure; the kit's validate-mint-payload.mjs mirrors this (its criterion-3 check becomes a warning field, tests updated); item pages already show tier chips, confirm which component renders the claim/source tier and, if a FACT below floor has no visible rating on the regulations detail page, say exactly which component needs the chip (do not edit .tsx, another lane owns the page; give the diff). (5) Measure before building, read-only: for the 386, how many URLs are fetchable (state the hosts; the container proxy may refuse some; say which), how many tokens appear verbatim in the cited page you can fetch; put the numbers at the top of the report. (6) Tests on real shapes pulled read-only (one no-source case, one above-floor case, one unfetchable case), dry writes nothing, apply writes sources + captures + claims + gate A, summary per-item reasons (source_registered, source_rated_tier, grounded_after_register, unfetchable, token_not_in_page). Report: measurements, files, exported functions, migration self-check SQL, tests, fitness, marker hash, the exact maintenance dispatch (dry then apply, selection quarantined-live), commit hash, anything refused with the reason.`
const r = await agent(P, { label: 'HEAL-7', phase: 'Lane', model: 'sonnet' })
return { heal7: r }
