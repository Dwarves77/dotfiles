export const meta = {
  name: 'lane-perf11',
  description: 'Sonnet lane PERF-11: /regulations ships 886 KB of HTML+RSC for a 60-row first page (14 KB per visible row, paid twice: rendered HTML plus the self.__next_f flight duplicate) because the page fetches every domain and filters in JS and serialises columns the ledger never renders; cut it to what the row renders, filtered in the query',
  phases: [{ title: 'Lane' }],
}
phase('Lane')
const P = `You are lane PERF-11 on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/perf11 (branch lane/perf11-2026-09-04, based on train/wave41-2026-09-04 = master 6c1ccd78 + the sfo1 region change); node_modules symlinked at fsi-app/node_modules (no npm install). Cannot push; coordinator lands and measures live in Chrome. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. Lane PERF-10 is working IN PARALLEL on the caching model (cacheComponents, src/app/layout.tsx, src/lib/detail/**, next.config): do NOT touch layout.tsx, next.config, src/lib/detail/**, or add "use cache" anywhere; your lane is the PAYLOAD of the listing pages only. Write set: fsi-app/src/app/regulations/page.tsx, fsi-app/src/app/operations/page.tsx, fsi-app/src/app/market/page.tsx (or market-intel), fsi-app/src/app/research/page.tsx, fsi-app/src/app/page.tsx (home, 315 KB), the ledger components they render (src/components/regulations/RegulationsLedger.tsx and siblings: read what each row actually renders), fsi-app/src/lib/data.ts and fsi-app/src/lib/supabase-server.ts (getListingsOnly, buildWorkspaceItemsQuery, toLedgerRowPayload from PERF-3/PERF-8, the slim listing RPC from FIRSTPAGE/SLIM-ORDER migration 303), ONE new migration fsi-app/supabase/migrations/305_<slug>.sql ONLY if the slim RPC needs a domain parameter or a narrower projection (written, not applied, md5-guarded like 304, row in docs/inventories/migrations.md; migration 064's dashboard projection is the pattern to repeat), their tests, and the UX compliance block (grep -rn "UX compliance" fsi-app/.discipline docs/doctrine; write it in your report). Rule: check whether an established pattern exists before proposing a new one (PERF-3 toLedgerRowPayload, FIRSTPAGE first-page pagination, migration 303's slim RPC); flag conflicts with prior decisions. No claims ahead of evidence, [CONFIRMED]/[HYPOTHESIS]; "there is NO small follow-up fix, fix it now"; commit message ends with "Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>". Gates: tsc (fsi-app/node_modules/.bin/tsc --noEmit -p fsi-app/tsconfig.json, 0 errors), node --test over every test you touch and fsi-app/src/__tests__/*.mjs, repo-root node fsi-app/.discipline/fitness/runner.mjs (0 violations) and node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs, the rendering smoke, and a MEASUREMENT: cd fsi-app && npx next build --webpack then next start (or the rendering harness's renderer) and fetch /regulations with a logged-out request: report the HTML byte size and the byte size of the self.__next_f flight chunks before and after; if the build cannot start locally without env, say so and measure the serialised payload from a unit test that renders the page's data props to JSON (bytes per row, rows).

THE OPERATOR'S RULE (2026-09-04, verbatim): "you are loading an entire page when clicking on it and that is wrong, you dont load every item at once, basic 101 developing." So the acceptance is ALSO structural: a listing page's first response carries ONE page of rows (the first viewport's worth, state the number and why: the FIRSTPAGE work set 60 and the ledger renders 301 row-like elements today, so the bands/strip are inlining far more than the first page), and every further row arrives on scroll or on "Load more" through the existing client fetch (FIRSTPAGE's remainder fetch / /api/workspace list endpoints: read them, reuse them, do not add a second mechanism); the bands and the upcoming-obligations strip must not inline whole item sets either (ids + counts + the few fields they render, the rest on demand). No surface may show an empty or false state while the remainder loads (the honest "Loading N…" band state from FIRSTPAGE stays). THE MEASUREMENT [CONFIRMED by the operator and by the coordinator in Chrome, 2026-09-04 18:05 UTC, after train 40 deployed]: /regulations document 885,867 bytes decoded (88 KB transferred), document.documentElement.outerHTML 911,116 chars, inline scripts 401,226 chars (the self.__next_f flight data), 301 row-like elements, 1,259 unique UUIDs embedded, for a 60-row first page: roughly 14 KB of serialised payload per visible row, paid twice (rendered HTML plus the flight duplicate); home ships 315 KB. PERF-8 applied toLedgerRowPayload to the 60 SSR rows and the document did not shrink measurably, so the weight is elsewhere: find it (candidates: RegulationsPage calls getListingsOnly({limit: 60}) across ALL domains then filters to REGULATIONS_DOMAIN in JS, serialising rows it discards; props passed to client components that carry whole items (briefs, sections, connections, forward events) for the strip/bands; the same data passed twice to two client components; the upcoming-obligations strip or band headers carrying full items; provider props). Measure per prop: for each client component on the page, JSON.stringify(props).length, and the count of items with body/brief text. THEN FIX: the domain filter in the query (the slim RPC or buildWorkspaceItemsQuery takes the domain; the page never fetches other domains' rows), the projection cut to the fields the row renders (read RegulationsLedger's RegRow and the band header: title, slug, urgency, dates, jurisdiction chips, tier chip, counts; nothing else; detail fields load on the detail route), the strip/bands fed by ids + the minimal fields, no whole items; the same treatment on /operations, /market, /research and home. Acceptance: /regulations document under 200 KB decoded with the same 60 rows visible (state the number you achieved), home under 100 KB, no visible change to any surface (the rendering guard proves the layout), all bands still correct. Report with numbers before/after per page, what carried the weight (file:line, prop, bytes), files, tests, fitness line, migration status, commit hashes, the UX compliance block, anything refused with the reason.`
const r = await agent(P, { label: 'PERF-11', phase: 'Lane', model: 'sonnet' })
return { perf11: r }
