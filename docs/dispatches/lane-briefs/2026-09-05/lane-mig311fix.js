export const meta = {
  name: 'lane-mig311fix',
  description: "MIG311-FIX: migration 311's in-transaction adversarial RLS proof cannot pass live (org_memberships.user_id FK to profiles(id); surcharge_audits corridor_id/carrier_id FK to entities); make the proof executable for real",
  phases: [{ title: 'Fix' }],
}
phase('Fix')
const r = await agent("You are lane MIG311-FIX on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/mig311fix (branch lane/mig311fix-2026-09-05, based on origin/master c3003233 = train 47, PR #594). node_modules symlinked at fsi-app/node_modules; Node 24 at /home/claude/.npm/_npx/387698761821791d/node_modules/node/bin/node (dirname FIRST on PATH). Cannot push; the coordinator lands. Main clone /root/work/dotfiles is read-only; git only inside your worktree. A sibling lane MIG310-FIX is editing fsi-app/supabase/migrations/310_listing_rpcs_item_grade.sql and the 310 row of docs/inventories/migrations.md at the same time: do not touch 310 or its row. CLAUDE.md rules bind: rule 15 (a guard is proven by attack, not presence), no copies of logic, [CONFIRMED]/[HYPOTHESIS], read every file you touch IN FULL, commit message ends with \"Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\". Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md.\n\nTHE DEFECT [CONFIRMED by the coordinator, live read-only SQL on project kwrsbpiseruzbfwjpvsp, 2026-09-05 20:25 UTC]: fsi-app/supabase/migrations/311_spec09_org_scope_and_pool_drop.sql (lane SPEC09-B) ends with an in-transaction adversarial RLS proof that INSERTs into public.org_memberships with `user_id := gen_random_uuid()` and into public.surcharge_audits with placeholder corridor_id/carrier_id 'cl:corridor:0000000000000311' / 'cl:organisation:0000000000000311'. Live constraints: org_memberships_user_id_fkey FOREIGN KEY (user_id) REFERENCES profiles(id) ON DELETE CASCADE; surcharge_audits_corridor_id_fkey and surcharge_audits_carrier_id_fkey REFERENCES entities(entity_id). Both INSERTs will raise, the whole migration rolls back, and the DDL (which is correct and needed: drop carrier_compliance_pools, org_id + RLS on six spec09 tables) never lands. Migrations 308 and 309 are applied live; 310 is being fixed by the sibling lane; 311 is not applied. Live facts you may rely on: carrier_compliance_pools and the six tables all have 0 rows; profiles(id) is the FK target for memberships (read fsi-app/supabase/migrations for how profiles relate to auth.users before assuming a migration may insert one; migrations never write the auth schema); entities has 2,022 rows (read migration 296 for the entity_id format and the FK).\n\nBUILD, choosing the design that keeps rule 15 satisfied AND lets the migration apply in one Supabase-MCP transaction with no auth-schema writes: read 311 IN FULL, migrations 006 (organizations, user_belongs_to_org), 077 (the org_watchlist_member_read precedent 311 cites), 296-298 (the spec09 tables), 250 and scripts/verify/prov-guard-adversarial-audit.mjs (CLAUDE.md rule 15's named template for an adversarial check wired into a lane), and fsi-app/scripts/verify/run-data-audit-lane.mjs (the AUDITS registry). Then: (1) Keep 311's DDL and its structural post-check (table gone, columns gone, org_id NOT NULL on six, policies present by name) inside the migration. (2) Move the adversarial cross-org proof OUT of the migration into a verify script on the mig-250 template, fsi-app/scripts/verify/spec09-org-rls-adversarial-audit.mjs, that runs against the live DB under the service-role client inside a transaction it always rolls back, picks its fixture rows from LIVE data (two existing organizations with at least one membership each, or, if fewer than two exist, creates nothing and exits 2 'self-skip, no creds/fixtures' per rule 15's diagnosable-skip convention; two existing entities of the kinds surcharge_audits FKs require, read via SQL, never hardcoded ids), impersonates each org's real member via set_config('request.jwt.claims') under SET LOCAL ROLE authenticated exactly as 311 attempted, asserts org A sees its row and org B sees none, and rolls back. Register it in run-data-audit-lane.mjs's AUDITS so it is EXECUTION-WIRED (rule 15; .discipline/governance/execution-wiring.mjs must see it), with a unit test that proves the assertion logic with a fake client (red then green), and remove any F25 exposure by making the audit lane its dispatch root (no allowlist entry). (3) In the migration, replace the removed proof with a comment naming the script and the lane that runs it, and keep the migration's own RAISE NOTICE summary. (4) Update the 311 row in docs/inventories/migrations.md (still WRITTEN, NOT APPLIED; the coordinator flips it) and the runbook section that describes 311's proof if one exists (grep docs/runbooks and fsi-app/scripts/spec09/SOURCES.md for 'adversarial' and '311'). One writer per file: the only shared file you touch is migrations.md and only its 311 row.\n\nGATES: node fsi-app/.discipline/fitness/runner.mjs (count must not exceed master c3003233's own count; run on master first and paste both); node --test on your new test file and fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs; node fsi-app/.discipline/governance/execution-wiring.mjs or the invariant-coverage meta-gate that runs it (find the command in discipline.yml) proving the new verifier is wired; node fsi-app/.discipline/governance/closure-gate.mjs (paste; if red on master already, say so, not yours to fix); node fsi-app/.discipline/consistency/override-check.mjs --range=origin/master..HEAD (C3 clean); bash fsi-app/.discipline/run-test-suite.sh (WITH node_modules is enough here, but the new test must not import @supabase/supabase-js at module top level so the no-npm job stays green: read how prov-guard-adversarial-audit.test.mjs does it).\n\nREPORT: the design chosen and why, the diff summary of 311, the new script and test paths, the AUDITS registration line, every gate line, commit hash.", { label: 'MIG311-FIX', phase: 'Fix', model: 'sonnet' })
return { MIG311_FIX: r }
