export const meta = {
  name: 'lane-perf5',
  description: 'Sonnet lane PERF-5: the listings data layer scales badly with the ledger (getListingsOnly 2.8 s for the rest chunk at 1,415 live items); diagnose with EXPLAIN and fix at the query',
  phases: [{ title: 'Lane', detail: 'PERF-5 in its own worktree' }],
}
phase('Lane')
const P = `You are lane PERF-5 on Caro's Ledge (repo Dwarves77/dotfiles). Worktree: /root/work/lanes/perf5 (branch lane/perf5-2026-09-04, based on origin/master); node_modules symlinked at fsi-app/node_modules. Cannot push; coordinator lands. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. Write set: fsi-app/src/lib/data.ts (getListingsOnly, getResourcesOnly, getAppData and their helpers), fsi-app/src/lib/supabase-server.ts (only the listing/list-order/personal-state readers those call), fsi-app/src/app/api/listings/rest/route.ts, fsi-app/src/app/api/user/list-order/**, fsi-app/src/app/api/workspace/personal-state/**, fsi-app/src/lib/listings/** (list-pagination.ts etc.), one new migration fsi-app/supabase/migrations/301_*.sql for indexes if EXPLAIN proves one is missing (written, NOT applied, in-place or additive only, with the exact self-check SQL in its header), and tests. Do NOT touch fsi-app/scripts/** or .github/** (other lanes own them). Deterministic; "there is NO small follow-up fix, fix it now"; no claims ahead of evidence, [CONFIRMED]/[HYPOTHESIS]; commit message ends with "Co-Authored-By: Claude Sonnet <noreply@anthropic.com>".

THE MEASUREMENT [CONFIRMED, Vercel runtime logs, production deploy dpl_6rVU4uhL7eCwVYBSq8dsTQDtdrky, 2026-09-04 01:36-01:41 UTC]: GET /regulations "[perf] getListingsOnly 907ms"; GET /api/listings/rest "[perf] getListingsOnly 2812ms" (the second chunk, called after the first paint, 313 rows rendered); GET / "[perf] getAppData 1754ms" cold, 542ms warm. Browser side (Chrome, background tab so paint timings unavailable): /regulations TTFB 25 ms, document stream ends 2.1 s; /api/workspace/personal-state 1.0-1.4 s; /api/user/list-order?list_key=regulations 0.7-1.4 s; home document stream 3.85 s cold / 1.53 s warm. The ledger grew from 621 to 1,415 live verified items in the last day (2,204 total) and is still growing ~150 per population run; the operator's standing complaint is that 2-4.5 s page loads are far too slow. Yesterday's PERF-3/PERF-4 (docs/audits/perf-load-times-2026-09-03.md, Addendum 85 ps 12-13 in docs/ops/session-log.md) fixed the root-layout await and streamed the shell; this lane is the DATA LAYER: the queries themselves.

READ IN FULL: docs/audits/perf-load-times-2026-09-03.md; fsi-app/src/lib/data.ts (every function on the path: getAppData, getListingsOnly, getResourcesOnly, getSurfaceCounts, the verified gate + org scoping); fsi-app/src/lib/supabase-server.ts readers they call; fsi-app/src/lib/listings/list-pagination.ts (toLedgerRowPayload, PERF-4); fsi-app/src/app/api/listings/rest/route.ts; the list-order and personal-state routes; fsi-app/src/app/regulations/page.tsx (what it awaits before streaming). Then MEASURE, do not guess: the Supabase MCP is available to you via ToolSearch (server "Supabase", tool execute_sql, project id kwrsbpiseruzbfwjpvsp, READ-ONLY use only: SELECT / EXPLAIN (ANALYZE, BUFFERS)). Reconstruct the exact SQL each reader issues (PostgREST select with its columns, filters, order, range) and EXPLAIN ANALYZE it against the live database; read pg_indexes for intelligence_items and the joined tables; measure row width (which columns are selected: full_brief? sections? claims?) and count. Identify precisely where the 2.8 s goes: sequential scan, missing index on (is_archived, provenance_status, item_type, ordering column), selecting wide text columns the ledger row never renders, a per-row second query (N+1), JSON aggregation in JS over thousands of rows, or the org-scoped watchlist/list-order joins.

FIX at the query: select only the columns the ledger row payload needs (PERF-4's toLedgerRowPayload defines them), push filtering/ordering/pagination into SQL with a supporting index (migration 301 if needed, with EXPLAIN before/after in its header), remove N+1s, and make /api/listings/rest paginate in bounded chunks rather than "the rest" if that is what the 2.8 s is. Keep the verified gate, org scoping and list-order semantics byte-identical (tests pin them). Also fix personal-state and list-order if EXPLAIN shows the same class of cost. Re-run EXPLAIN ANALYZE after the change and report before/after timings for each query with the plan lines that changed. Tests: npx tsc --noEmit -p fsi-app/tsconfig.json clean; cd fsi-app && node --test the existing tests for these modules (find them: grep -rl "getListingsOnly\\|list-pagination\\|listings/rest" fsi-app/src/__tests__ fsi-app/src/lib fsi-app/src/app | grep test); repo root node fsi-app/.discipline/fitness/runner.mjs (0 violations) and node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs. Your final text is the report: where the time went (with EXPLAIN evidence), what changed per file, before/after query timings, whether migration 301 exists and its exact file name for the coordinator to apply, tests, tsc, fitness, commit hash.`
const r = await agent(P, { label: 'PERF-5', phase: 'Lane', model: 'sonnet' })
return { perf5: r }