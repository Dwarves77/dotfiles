export const meta = {
  name: 'lane-perf8-resume',
  description: 'Sonnet lane PERF-8: measured live 2026-09-04 14:40 UTC: every route is private/no-store dynamic (0.5-2.0 s server render, 857 KB HTML on /regulations), a client-side item click waits 4.25 s for its RSC payload, /regulations throws React #418 (hydration mismatch) so the whole page re-renders client-side (DCL 5.3 s), and five post-render API calls take 0.6-3.4 s each; the operator\'s law: every click should show items instantly',
  phases: [{ title: 'Lane', detail: 'PERF-8 in its own worktree' }],
}
phase('Lane')
const P = `RESUME (train 38 assembly, 16:45 UTC): a previous agent on this exact lane was interrupted at ~15:32 UTC. Its worktree is as it left it: run git status and git diff (and git log origin/master..HEAD) in the worktree FIRST, read every dirty file in full, keep what is correct, finish what is missing, and fix what is wrong; do not start over and do not discard its work without reading it. Note: origin/master has since advanced to 835e3df0 (train 37) and train 38 (lanes LEDGER-ZERO, TURNREQ, T4-OVERRIDE, REVIEW-WIRE, CHAIN, CLOSURE-GATE, F25-WIDE, DEAD-EXEC, DAG-AUTHOR) is being landed by the coordinator right now; stay on your worktree's base, the coordinator cherry-picks your commit. Commit trailers: end the message with 'Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>' (that supersedes the 'Claude Sonnet' trailer text below). You are lane PERF-8 on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/perf6 (directory name only; branch lane/perf6-2026-09-04, based on origin/master e8cb748f); node_modules symlinked at fsi-app/node_modules. Cannot push; coordinator lands and measures live after deploy. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. Write set: fsi-app/src/app/** (routes, layouts, loading.tsx), fsi-app/src/components/**, fsi-app/src/lib/data.ts, fsi-app/src/lib/supabase-server.ts, fsi-app/src/lib/api/**, fsi-app/src/middleware.ts or proxy.ts, fsi-app/next.config.*, their tests, docs/decisions/ADR-026-<slug>.md if you change the caching model (an ADR is required for that; read ADR-016..025 for format), and the UX compliance block the discipline gate demands when customer surfaces change (grep -rn "UX compliance" fsi-app/.discipline docs/doctrine and follow it). READ FIRST, in full: docs/ops/session-log.md Addendum 85 postscripts 1, 3, 5, 26 and 27 (PERF, PERF-2, PERF-5, PERF-6, PERF-7: what was already done and measured; PERF-6/7 moved src/lib/api/{org,auth,server-bootstrap,community-auth}.ts from getUser() to getClaims(), landed in #562/#563, so the remaining per-request cost is NOT the Auth round trip; and the July 2026 diagnosis of React #418 (WhatChanged.tsx relative-time Date.now(), fixed client-only at line ~127) is already on master, so TODAY's #418 on /regulations has a DIFFERENT source: find it), any docs/decisions or docs/doctrine text on caching, unstable_cache, ISR, personal state, and the FIRSTPAGE/SLIM-ORDER work in src/lib/supabase-server.ts (buildWorkspaceItemsQuery, LISTINGS_RPCS_WITH_OWN_TOTAL_ORDER). Rule: check whether an established pattern exists before proposing a new one; flag conflicts with prior decisions and explain the trade-off before changing them. No claims ahead of evidence, [CONFIRMED]/[HYPOTHESIS]; "there is NO small follow-up fix, fix it now"; commit message ends with "Co-Authored-By: Claude Sonnet <noreply@anthropic.com>". Gates: fsi-app/node_modules/.bin/tsc --noEmit -p fsi-app/tsconfig.json (0 errors), node --test over every test you touch and fsi-app/src/__tests__/*.mjs if present, repo-root node fsi-app/.discipline/fitness/runner.mjs (0 violations) and node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs (141), the rendering smoke (fsi-app/.discipline/rendering, read how it runs), and a production build if the environment allows (cd fsi-app && npx next build; if it needs env vars the worktree lacks, say so and run the type/lint gates instead). Supabase MCP read-only SELECT is available (server "Supabase", tool execute_sql, project kwrsbpiseruzbfwjpvsp; ToolSearch "select:mcp__Supabase__execute_sql").

THE MEASUREMENTS [CONFIRMED by the coordinator, Chrome on carosledge.com, 2026-09-04 ~14:40 UTC, logged-in operator session]:
(a) GET /regulations: TTFB 137 ms, HTML 857 KB, DOMContentLoaded 5,297 ms, load 5,793 ms. Console: "Minified React error #418" (hydration mismatch, args HTML) thrown from the main chunk, followed by four "TypeError: Cannot read properties of null (reading 'parentNode') at $RS" (the streaming-SSR completion script) and "message channel closed" errors. Post-render fetches: /api/admin/attention 1,355 ms; /api/telemetry/error 3,364 ms (the page reported its own error); a 77 KB fetch whose URL the tool redacted (query-string data: likely a Supabase REST call with the key in the query, find it) 2,389 ms; /api/workspace/personal-state 1,620 ms; /api/user/list-order?list_key=regulations 2,629 ms.
(b) Client-side click list -> /regulations/eu-net-zero-industry-act-2024-1735: the RSC request for the item route took 4,254 ms (18 KB); URL changed at 576 ms; then /api/community/entities/cl:instrument:… 601 ms and /api/workspace/members 1,649 ms.
(c) Direct fetch of routes, all with cache-control "private, no-cache, no-store, max-age=0, must-revalidate" and x-vercel-cache MISS: item page 1,640 ms then 900 ms (188 KB); /market-intel 515 ms (23 KB); /operations 1,830 ms (392 KB); /research 1,631 ms (136 KB); /community 1,992 ms (54 KB); /regulations 1,129 ms (857 KB).
(d) The operator's law (2026-09-04, verbatim): "clicking into any item or any page takes WAY too long. multiple seconds. every click should show items on a page instantly."

DIAGNOSE, then FIX, in this order, each with its own commit:
1. The hydration mismatch on /regulations. Find the server/client difference (suspects: locale/timezone date formatting like "September 25, 2026" in the upcoming-obligations strip or band headers rendered with toLocaleDateString/Intl on the server vs the client, Date.now()-relative labels, personal-state-dependent markup rendered on the server, a component reading window/localStorage during render, nested <p>/<div> invalid nesting, or text differing by whitespace). Reproduce it deterministically: a test that renders the offending component to HTML with a fixed TZ/locale and diffs against a client render, or at minimum a unit test on the formatter pinning TZ/locale. Fix at the source. This alone should cut DCL by seconds; say what you expect.
2. The payload: 857 KB HTML for /regulations. Measure what is in it (items rendered, briefs inlined, duplicated JSON in the RSC flight data). FIRSTPAGE already paginates the RPC; find why the document is still 857 KB and cut it to the first page's data only, the rest on scroll/click; the same for /operations (392 KB).
3. The caching model. Everything is dynamic because something at the top reads cookies/headers (auth, workspace) which opts the whole route out of static/ISR. Design and implement the split: PUBLIC intelligence content (listings, item pages, bands, forward-events strip, series board) from a cached data layer (unstable_cache with tags + revalidateTag fired by the population/maintenance runtimes on apply, or route-segment ISR with a short revalidate; pick one and justify against ADR-023/ADR-025 and the PERF trains' decisions) and PER-USER state (personal-state, list-order, members, admin attention) fetched client-side after first paint, in ONE batched call instead of four, with the shell rendered without waiting for it. If the auth/workspace read must stay in the request path for access control, keep it in middleware (cheap, no DB) and make the page's data reads independent of it. Record the decision as ADR-026.
4. Navigation: loading.tsx skeletons for the listing and item segments so a click paints instantly while the RSC streams; <Link prefetch> on item links in the viewport; explain why the RSC took 4.25 s when a direct fetch took 0.9-1.6 s (layout re-fetch? missing loading boundary? a serial await chain in src/app/regulations/[slug]/page.tsx: count the awaits and their dependencies; parallelise with Promise.all where independent) and fix it.
5. The five post-render API routes: read what each does (auth + queries); collapse into one /api/workspace/bootstrap call or make them cacheable per user for the session; the telemetry/error post must not be on the critical path.
6. A fitness function (fsi-app/.discipline/fitness/functions/F34-<name>.mjs or the next free number, read the existing ones for the format, register it, tests) that fails CI when a page route renders a Date with toLocaleDateString/toLocaleString/Intl.DateTimeFormat without a fixed timeZone, or when a listing route inlines more than N items server-side; state N from the measurement. This is how the regression is prevented.
Report with numbers: for each of the six, what you found [CONFIRMED with file:line], what you changed, and the expected effect on the four measured numbers (DCL on /regulations, HTML size, item-click RSC time, post-render API time); files; tests; fitness line; ADR; commit hashes. The coordinator will re-measure live after deploy and compare against your expectations.`
const r = await agent(P, { label: 'PERF-8', phase: 'Lane', model: 'sonnet' })
return { perf8: r }
