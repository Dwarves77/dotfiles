export const meta = {
  name: 'lane-perf12',
  description: 'Sonnet lane PERF-12: ADR-027 D2 executed — the listing pages load one screen of rows and the rest by cursor (PostgREST range + TanStack Query infinite query), rendered through TanStack Virtual, replacing the 5,000-row one-shot remainder fetch; the org-id hop removed from the listing read (JWT claim / bootstrap header); prefetch on hover so a click paints instantly',
  phases: [{ title: 'Lane' }],
}
phase('Lane')
const P = `You are lane PERF-12 on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/perf12 (branch lane/perf12-2026-09-04, based on train/wave43-2026-09-04 = master + PERF-11 + PERF-ARCH, plus one commit adding @tanstack/react-query 5 and @tanstack/react-virtual 3 to package.json: this worktree has its OWN real node_modules with both installed; do not run npm install again except to add a package you can justify by ADR-027). Cannot push; coordinator lands and measures live. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. PERF-10 runs in parallel on src/app/layout.tsx, next.config, src/lib/detail/** and "use cache": do not touch those. READ FIRST, in full: docs/decisions/ADR-027-standard-fast-page-architecture.md (D2 cursor rows, D5 one round trip, §9 kept vs replaced) and docs/audits/perf-waterfall-2026-09-04.md (the chain per route with file:line), then PERF-11's commit 995d82e3 (the register's first page + Load more, the domain scoping, migration 305), src/lib/list-pagination.ts (LIST_REMAINDER_LIMIT=5000: the thing you replace), src/components/regulations/RegulationsLedger.tsx and its remainder fetch, src/app/api/listings/rest/route.ts, src/lib/data.ts:280-300 (resolveOrgIdFromCookies before cachedListingsOnly), src/lib/api/org.ts (PERF-9's two-stage resolver), src/lib/hooks/useWorkspaceBootstrap.ts, docs/design/ux-laws.md. Rules: the standard library, not a home-grown mechanism (the operator: "these solutions have been developed already, you don't need to reinvent a fix, find out how others do it and do the same"); [CONFIRMED]/[HYPOTHESIS]; "there is NO small follow-up fix, fix it now"; commit message ends with "Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>". Gates: tsc (fsi-app/node_modules/.bin/tsc --noEmit -p fsi-app/tsconfig.json), node --test over your tests and fsi-app/src/__tests__/*.mjs, repo-root node fsi-app/.discipline/fitness/runner.mjs (0 violations; 31 functions; F37's perf budget must pass or be ratcheted DOWN, never up), the discipline governance tests, the rendering smoke (fsi-app/.discipline/rendering), and cd fsi-app && npx next build --webpack (must build; report the route table and the bytes of the /regulations RSC/HTML if the harness can render it).

BUILD, each its own commit: (1) QueryClientProvider once, in the client shell (find where providers already live; one provider, hydrated with the server-fetched first page via HydrationBoundary/initialData so the first paint is the SSR page and TanStack takes over without a second fetch); (2) the listing rows as a useInfiniteQuery keyed by (surface, filters, sort) with a keyset cursor (the RPC's own total order from FIRSTPAGE/SLIM-ORDER: priority, added_date desc, id asc: the cursor is the last row's (priority, added_date, id), passed to the listing route which applies it server-side via .range() or a where-clause keyset, whichever the slim RPC supports without a new migration; if the RPC cannot take a cursor, write migration 306 (written, not applied, md5-guarded like 305) adding p_after_* params, and keep an offset fallback until it applies), page size = one screen (state the number from the row height and a 1440x840 viewport, ~25–40), fetchNextPage on scroll near the end via IntersectionObserver or the virtualizer's range; LIST_REMAINDER_LIMIT and the one-shot remainder fetch are DELETED, not left behind; (3) useVirtualizer on the ledger so the DOM holds only the visible rows plus overscan, on every listing ledger (regulations, market, operations, research) through ONE shared ledger-list component if the four ledgers share a row shape (read them; do not fork four copies), with the band headers (CRITICAL/HIGH/…) preserved as sticky group rows and the honest "Loading N…" band state kept; (4) the org-id hop: the listing read must not call resolveOrgIdFromCookies() on the server before the RPC when the bootstrap already carries orgId: for the SSR first page use the request-scoped cache() value PERF-9's resolver produces (one hop, shared), for client pages pass orgId from useWorkspaceBootstrap to the listing route as a header/param that the route verifies against the session (never trusted blindly); ADR-027 D5's JWT custom claim via a Supabase Auth Hook is the end state: write it as migration/config notes if it needs Supabase dashboard work, and say exactly what the operator must click, but do not block on it; (5) prefetch: <Link prefetch> on every row link and on the primary nav so a click paints from the router cache; the detail route's loading.tsx must render the detail SHELL (title bar, section skeleton), never null; (6) F37: tighten the budget to what you achieved (document bytes, DOM rows on first paint, requests on click) so it cannot regress. UX laws: no surface renders empty or false while loading; the "N shown / total" disclosure stays truthful with the true total from the RPC. Report: numbers before/after (document bytes, rows in DOM, requests per click, bytes per scroll page), the cursor design, files, tests, fitness/F37 line, build route table, migration status, commit hashes, the UX compliance block, anything refused with the reason.`
const r = await agent(P, { label: 'PERF-12', phase: 'Lane', model: 'sonnet' })
return { perf12: r }
