export const meta = {
  name: 'lane-heal6',
  description: 'Sonnet lane HEAL-6: heal #21 healed 0/94 quarantined-live items; 88 fail criterion 7 gate_a_unproven_or_stale and 38 fail criterion 4 analysis_missing_label_syntax; diagnose from the code and live rows, then build the heal step that closes them',
  phases: [{ title: 'Lane', detail: 'HEAL-6 in its own worktree' }],
}
phase('Lane')
const P = `You are lane HEAL-6 on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/heal6 (branch lane/heal6-2026-09-04, based on origin/master 1356b381); node_modules symlinked at fsi-app/node_modules. Cannot push; coordinator lands. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. Write set: fsi-app/scripts/mint/heal-provenance.mjs (+ heal-provenance.test.mjs), fsi-app/scripts/mint/lib/gate-a-scan.mjs and gate-a-match.mjs (+ tests) ONLY if the diagnosis proves the scanner itself is wrong (they are mint governing files: F28 will then name the mint family and you re-pin fsi-app/scripts/harness-runs/mint/PENDING-RUN.md, which already exists, to the new hash in its existing format), one new migration fsi-app/supabase/migrations/302_*.sql ONLY if validate_item_provenance itself must change (written, NOT applied, additive, with the exact self-check SQL in its header, and a row inside the table in fsi-app/docs/inventories/migrations.md), docs/runbooks/MAINTENANCE-RUNBOOK.md provenance-heal section text. Seven other lanes are running on record-facts.mjs, validate-mint-payload.mjs, export-census-rows.mjs, run-mint-batch.mjs, apply-mint-batch.mjs, population-turn.yml, maintenance.yml, scripts/maintenance/**, src/lib/data.ts, the regulations detail page, src/lib/sources/**: do not touch any of those. Deterministic, $0, no LLM; "there is NO small follow-up fix, fix it now"; no claims ahead of evidence, [CONFIRMED]/[HYPOTHESIS]; read every file you touch IN FULL before editing (heal-provenance.mjs is ~2,400 lines; read all of it, its header carries prior lanes' findings including finding 3 "GATE A vs LABELS"); commit message ends with "Co-Authored-By: Claude Sonnet <noreply@anthropic.com>". Gates before commit: node --test on what you touched; from repo root node fsi-app/.discipline/fitness/runner.mjs (0 violations) and node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs. The Supabase MCP (server "Supabase", tool execute_sql, project kwrsbpiseruzbfwjpvsp; load via ToolSearch "select:mcp__Supabase__execute_sql") is available for READ-ONLY SELECT; never write through it. CLAUDE.md standing rule 17: nothing in this build runs alone; a heal that ends with items still quarantined and no path to verified is a defect in the heal, not a note.

THE EVIDENCE [CONFIRMED, Actions run 33829526120, Maintenance #21, step provenance-heal --mode apply, selection quarantined-live, HEAL_VERSION hp5-2026-09-04.2, master 1356b381, 11m56s, exit 0]: candidates 94, healed_verified 0, still_failing 94, gate_a_written 94, resourced 24, orphans_grounded 4, orphans_unprovable 824, relabeled_paragraphs 0 (relabel_no_owning_section 709), refactored_to_analysis 0 (refused_no_owning_paragraph 3), retrofitted 0 (retrofit_refused_no_owning_paragraph 152), cited_captured 25, cited_held 42, ungrounded_after_capture 3. Final failures by item (criterion, reason → items): (7, gate_a_unproven_or_stale) 88; (4, analysis_missing_label_syntax) 38; (3, fact_below_authority_floor) 2; (2, ungrounded_url) 1; (6, missing_full_brief) 1; (5, missing_required_slot) 1. Failures per item: 54 items have exactly 1 failure, 14 have 2, 13 have 3. Sample: item 007f42b1-265a-4504-8bd1-ea1557d410ad (framework) fails only criterion 4 on the claim "The Texas Freight Network Technology and Operations Plan (FNTOP) was published December 2020."; item 0ea6a710-5dc0-4f31-b857-5c0bf6825770 (regulation) fails criterion 4 on two NYCRR claims plus criterion 7. Heal's own header (finding 3) already states why gate A cannot move: scanBrief takes only full_brief + factClaims, counts any figure/date token of full_brief not literally present in the FACT-claim corpus as an orphan, ignores labels, and RELABEL/REFACTOR edit section content_md, never full_brief. So 94 gate_a rows were written and 88 still carry orphan_count > 0.

DIAGNOSE FIRST, from primary sources, and put the diagnosis at the top of your report before any code: (a) read validate_item_provenance as applied live (select pg_get_functiondef('public.validate_item_provenance(uuid)'::regprocedure)) and quote criteria 4 and 7 exactly; (b) for the 88 criterion-7 items, measure with read-only SQL and the scanner itself what the orphan tokens ARE (join item_gate_a_state to intelligence_items; run scanBrief locally on a sample of full_brief + claims you SELECT; classify orphans: figures/dates that exist verbatim in a captured source (agent_run_searches.result_content for the item's runs) vs figures that exist in no capture vs tokens the scanner should not count, e.g. citation numbers, years inside act titles, section numbers already in FACT claims under a different surface form); (c) for the 38 criterion-4 items, quote the failing claims and the section text and state exactly which label form criterion 4 wants and why RELABEL reported no_owning_section for 709 paragraphs (is the paragraph in full_brief rather than a section? is the section lookup keyed wrongly? is the claim stored on an item with no sections at all?); (d) grep docs/decisions/ for item_grade, full_brief, Gate A, orphan, ADR-016 and cite what binds you.

THEN BUILD the smallest deterministic heal that actually moves items to verified, inside existing doctrine: for criterion 7, whichever the measurement supports, ground orphans into FACT claims when the token is verbatim in a capture (source_span exact, the existing ORPHANS path, but fix why it grounded only 4 of 828 if the captures do contain the tokens), and for orphans present in no capture, make full_brief honest by the same rule the kit uses for record-grade items (rebuild/strip the unprovable sentence from full_brief and keep the grounded facts; if that means the item's grade must change, say so with the ADR that governs item_grade and put the grade change behind a flag, dry by default, so the coordinator can put it to the operator); for criterion 4, make RELABEL find the owning paragraph where it actually lives (full_brief vs section content_md) and write the label form the live function accepts; NEVER weaken the validator to pass items, and never add an LLM. Every step dry/apply with per-item reasons in summary.json, resumable by ids like the existing steps, under the existing HEAL_TIME_BUDGET_SECONDS checkpointing. Tests for each new branch with fixtures built from the real shapes you measured. Report: diagnosis with counts, the doctrine citations, files, exported functions, tests, fitness, the exact maintenance dispatch inputs for the next dry run, commit hash, and anything you refused with the reason.`
const r = await agent(P, { label: 'HEAL-6', phase: 'Lane', model: 'sonnet' })
return { heal6: r }
