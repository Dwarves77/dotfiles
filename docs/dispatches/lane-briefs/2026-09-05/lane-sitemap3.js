export const meta = {
  name: 'lane-sitemap3',
  description: 'Sonnet lane SITEMAP-3: the sitemap walker (feed first, sitemap fallback) has walked 1 host of 2,563 sources and only per-host dispatches exist; the operator wants every source backfilled with sitemap/feed info queryable in Supabase: a bounded, resumable all-hosts mode plus per-source coverage columns',
  phases: [{ title: 'Lane', detail: 'SITEMAP-3 in its own worktree' }],
}
phase('Lane')
const P = `You are lane SITEMAP-3 on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/sitemap3 (branch lane/sitemap3-2026-09-04, based on origin/master e8cb748f); node_modules symlinked at fsi-app/node_modules. Cannot push; coordinator lands. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. Write set: fsi-app/scripts/turns/run-source-sweep.mjs (+ test), fsi-app/src/lib/sources/sitemap-walk.mjs (+ test) only if the pure walker needs a new injected effect, .github/workflows/source-sweep.yml (new inputs; validate with python3 -c "import yaml; yaml.safe_load(open(...))"), ONE new migration fsi-app/supabase/migrations/304_source_sitemap_coverage.sql (written, NOT applied, additive, md5-guarded like 302/303, with a row inside the table in docs/inventories/migrations.md), docs/inventories/shared-dataset-ownership.md (the sources columns this lane writes, one-writer rule), fsi-app/scripts/harness-runs/source-sweep/PENDING-RUN.md (run-source-sweep.mjs is a source-sweep GOVERNING file: chain a "What changed" entry and re-pin to the hash the fitness runner prints), docs/runbooks (the source-sweep runbook section for the sitemap walker; find it by grep "walker sitemap"). Deterministic, $0, no LLM; polite fetching (1 req/s, the existing budgets); "there is NO small follow-up fix, fix it now"; no claims ahead of evidence, [CONFIRMED]/[HYPOTHESIS]; read every file you touch IN FULL (lane SITEMAP's and SITEMAP-2's commits: git log --since=2026-09-03 -- fsi-app/src/lib/sources/sitemap-walk.mjs fsi-app/scripts/turns/run-source-sweep.mjs .github/workflows/source-sweep.yml); commit message ends with "Co-Authored-By: Claude Sonnet <noreply@anthropic.com>". Gates: node --test fsi-app/src/lib/sources/*.test.mjs fsi-app/scripts/turns/run-source-sweep.test.mjs; repo-root node fsi-app/.discipline/fitness/runner.mjs (0 violations after the marker) and node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs (141). Supabase MCP read-only SELECT (server "Supabase", tool execute_sql, project kwrsbpiseruzbfwjpvsp; ToolSearch "select:mcp__Supabase__execute_sql"); never write.

THE OPERATOR'S ASK (2026-09-04, verbatim): "do you do mapping of the sites and store them in supabase so we can use a site map to identify new pages? … you will have to backfill sources with sitemap info in supabase" and "did you do mapping of rss feeds and save them in supabase?". THE FACTS [CONFIRMED by the coordinator, live SQL]: sources 2,563; sources.rss_feed_url populated on 189; portal_link_candidates 1,840 (383 from the single sitemap apply on smartfreightcentre.org, 1,837 status candidate, 3 promoted); monitoring_queue 580 total, 0 in 2 days; the per-source URL-set snapshot lives in Supabase Storage at sitemap-snapshots/<source_id>/current.json.gz (deferred baseline on partial coverage), not in a table; the walker takes --source-id or --host, exactly one, so a backfill of 2,563 sources is 2,563 dispatches today.

BUILD: (1) an --all-hosts mode for --walker sitemap in run-source-sweep.mjs: selects active sources rows (define "active" from the table: status/archived columns, read the schema), groups by host, orders by (never walked first, then oldest last_walked_at), walks up to --max-hosts N per run (default from measurement: what fits in the workflow's timeout at the existing per-host fetch budgets; state the arithmetic), resumable because the next run's ordering starts where coverage is thinnest; per-run artifact (source-sweep-run-NNN) carries hosts_walked, hosts_skipped_bot_wall, feeds_discovered, new_locs, lastmod_changes, hosts_remaining_unwalked. (2) coverage columns on sources so it is queryable in Supabase: sitemap_url text, sitemap_last_walked_at timestamptz, sitemap_url_count int, sitemap_walk_outcome text (walked / no_sitemap / bot_wall / feed_only / unfetchable), feed_last_probed_at timestamptz; migration 304 adds them (additive, nullable); the driver writes them through db.mjs's guarded path on every walk (dry mode writes nothing); register the columns in shared-dataset-ownership.md under sources with this driver as the one writer. (3) workflow input: all_hosts boolean + max_hosts; keep the existing inputs. (4) a --check-coverage read-only mode (or reuse dry) printing: sources total, walked, unwalked, by outcome, feeds populated. Tests for the selection/order, the budget arithmetic, the column writes (injected deps), the artifact counts. Report: files, the arithmetic for max-hosts default, exported functions, tests, fitness line, marker hash, migration status (written, not applied: the coordinator applies via Supabase MCP after landing), commit hash.`
const r = await agent(P, { label: 'SITEMAP-3', phase: 'Lane', model: 'sonnet' })
return { sitemap3: r }
