export const meta = {
  name: 'lane-sitemap2',
  description: 'Haiku lane SITEMAP-2: the sitemap walker artifact must carry the feed-probe evidence per source and classify an HTTP 403 bot wall as such, not as "no sitemap discovered"',
  phases: [{ title: 'Lane', detail: 'SITEMAP-2 in its own worktree' }],
}
phase('Lane')
const P = `You are lane SITEMAP-2 on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/sitemap2 (branch lane/sitemap2-2026-09-04, based on origin/master e6eff093); node_modules symlinked at fsi-app/node_modules. Cannot push; coordinator lands. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. Write set: fsi-app/src/lib/sources/sitemap-walk.mjs (+ .test.mjs), fsi-app/src/lib/sources/feed-discovery.mjs (+ test), fsi-app/scripts/turns/run-source-sweep.mjs (+ test) only its sitemap branch and shapeRunOutput, fsi-app/scripts/harness-runs/source-sweep/PROTOCOL.md §7, and fsi-app/scripts/harness-runs/source-sweep/PENDING-RUN.md re-pinned to the hash the fitness runner prints if F28 names the source-sweep family (keep the file's existing format; the last line "**harness_version at write time:** \`sha256:...\`" is what parsePendingRunHash reads). Nothing else. Deterministic, $0, no LLM; read every file you touch IN FULL first; no claims ahead of evidence, [CONFIRMED]/[HYPOTHESIS]; commit message ends with "Co-Authored-By: Claude Haiku <noreply@anthropic.com>". Gates: node --test on the four test files, from repo root node fsi-app/.discipline/fitness/runner.mjs (0 violations) and node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs.

EVIDENCE [CONFIRMED, source-sweep dry runs 009/010/011 on 2026-09-04]: run-009 (host aircargonews.net) reported per_item error "no sitemap discovered: robots.txt yielded 0 Sitemap: lines (fetched, none declared) and none of the fallback candidates parsed as a sitemap" while the trace shows every sitemap candidate answered HTTP 403; curl from a Codespace confirms the homepage, /feed and /sitemap.xml all answer 403 with a 5 KB Cloudflare challenge page. run-010 (smartfreightcentre.org) reported feed_found 0 although https://smartfreightcentre.org/feed answers 200; the artifact carries no evidence of what feed discovery tried (homepage status, <link rel=alternate> count, candidate paths and their statuses), so nobody can tell whether the feed probe ran, what it fetched, or why it rejected the candidate (isFeedDocument on an HTML redirect page? content-type? the byte cap?). Two defects: (1) EVIDENCE: walkSource/discoverFeed must return, and shapeRunOutput must record per source, the feed-probe trail: homepage {status, bytes, content_type, alternate_links_found}, each candidate path {url, status, content_type, is_feed_document, reason}; sitemap probes already record status per candidate, keep that. (2) CLASSIFICATION: when the homepage AND every probe answer 401/403/429 (or a Cloudflare/Akamai challenge body: detect the challenge markers you can prove from the real 403 body, fetch it yourself with curl if the container proxy allows, otherwise use the markers documented in the artifact trace and label the detection [HYPOTHESIS]), the per_item outcome is "bot_wall" with the status, never "no sitemap discovered"; the metrics gain bot_wall_sources; a bot-walled source is reported, never retried in the same run. Then re-run discovery logic against smartfreightcentre.org's real responses to state WHY its /feed was not recognised (fetch it if the proxy allows; if not, reason from the code and label it) and fix the recognition if the cause is ours (e.g. the feed is served as text/html but is valid RSS, or the byte cap cut the XML declaration). Tests for both defects with fixtures from the real responses. Report: root causes with evidence, files, exported functions, tests, fitness, marker hash if re-pinned, commit hash.`
const r = await agent(P, { label: 'SITEMAP-2', phase: 'Lane', model: 'haiku' })
return { sitemap2: r }
