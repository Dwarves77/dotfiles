export const meta = {
  name: 'wave-d',
  description: 'Plan T40/T42/T45 lanes: CHAIN (workflow_run chaining ledger-consume→population-turn, producers→propagation-drain) and DEAD-EXEC (the 2026-08-31 register\'s four unexecuted deletes, the 19 dead exports, the Gate-A shims)',
  phases: [{ title: 'Lanes' }],
}
phase('Lanes')
const C = (name, wt) => `You are lane ${name} on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/${wt} (branch lane/${wt}-2026-09-04, based on origin/master e8cb748f); node_modules symlinked at fsi-app/node_modules. Cannot push; coordinator lands. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. READ FIRST: docs/plans/complete-system-build-plan-2026-09-04.md on branch train/wave37-2026-09-04 in worktree /root/work/lanes/train37 (§0 definition of done), and the audit under docs/audits/wiring-audit-2026-09-04/ there. CLAUDE.md rules: no schedules (rule 16: no cron, no schedule: block; workflow_run on a completed run is event-driven and allowed, say so in the workflow comment); no copies of logic; "there is NO small follow-up fix, fix it now"; [CONFIRMED]/[HYPOTHESIS]; read every file you touch IN FULL; commit message ends with "Co-Authored-By: Claude Sonnet <noreply@anthropic.com>". Gates: node --test over every test you touch; repo-root node fsi-app/.discipline/fitness/runner.mjs (0 violations; chain/re-pin a family marker if you move a governing file) and node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs; python3 -c "import yaml; yaml.safe_load(open(f))" for every workflow; tsc if .ts touched; the full suite sh fsi-app/.discipline/run-test-suite.sh if you delete anything. Report: files, tests, fitness line, marker hashes, commit hash, anything refused with the reason.`

const D1 = agent(`${C('CHAIN','chain')}

Write set: .github/workflows/population-turn.yml and .github/workflows/propagation-drain.yml (on: blocks and the input defaults a workflow_run event implies), docs/runbooks/POPULATION-TURN-RUNBOOK.md and the propagation runbook (the chaining section), fsi-app/scripts/turns/run-population-flywheel.mjs or the population driver ONLY if a workflow_run-triggered run needs a "trigger context" field in its artifact (record which upstream run fired it: name, run_id, conclusion). NOT in your write set: ledger-consume.yml (lane LEDGER-ZERO owns it and adds its own workflow_run from source-sweep) and source-sweep.yml (lane SITEMAP-3). THE FACTS [CONFIRMED, audit C1 §3/§5]: every cross-workflow boundary is a coordinator dispatch; C1's minimum change set: workflow_run chaining, event-driven off a completed run, not a schedule. BUILD: (1) population-turn.yml gains on.workflow_run: workflows ["Ledger consume"] types [completed]; when triggered that way it runs mode=apply limit=<default> ONLY IF the upstream run concluded success AND its artifact reports promoted > 0 (read the artifact via actions/download-artifact from the triggering run, or the branch it pushed; choose the mechanism the repo's deliver-artifact-branch.sh makes available and document it), else exits 0 with a named no-op; THE GATE (--check-gate) still applies; operator's population stop: the coordinator sets a repository variable POPULATION_PAUSED=true (read it with vars.POPULATION_PAUSED; the chained run exits 0 no-op while it is true; hand dispatch also honours it; document that the coordinator flips it at T46); (2) propagation-drain.yml gains on.workflow_run: workflows ["Data producers"] types [completed], drains on success; the backfill_entities/seed_derived_values checkboxes stay hand-only; (3) both keep workflow_dispatch; (4) the artifact records trigger context. Tests: the yaml validates; a unit test for any driver change. First proof: a hand dispatch of ledger-consume apply followed by an automatic population-turn no-op (paused) with the trigger context in its log.`, { label: 'CHAIN', phase: 'Lanes', model: 'sonnet' })

const D2 = agent(`${C('DEAD-EXEC','deadexec')}

Write set: DELETE fsi-app/scripts/lib/anthropic.mjs, fsi-app/src/lib/llm/metered-emit.mjs, fsi-app/.discipline/lib/adr-loader.mjs, fsi-app/src/lib/sources/api-fetch.ts and their tests (the 2026-08-31 register docs/plans/unwired-disposition-2026-08-31.md rows 18, 3, 10, 12, ruling R-C taken 2026-09-03; read each row's body and the file in full first; if a file gained a real importer since 08-31, say so and keep it, citing the importer); the 19 dead exports in docs/audits/wiring-audit-2026-09-04/B1-modules.md Appendix B EXCEPT files owned by other lanes right now (skip: fsi-app/src/lib/sources/sitemap-walk.mjs (SITEMAP-3), fsi-app/scripts/turns/run-source-sweep.mjs (SITEMAP-3), fsi-app/scripts/maintenance/forward-events-retext.mjs, fsi-app/src/lib/auth/route-policy.ts (PERF-8 may touch auth), fsi-app/scripts/gen/fetch-desnz-factors.mjs (DAG-AUTHOR touches producers)); for each remaining dead export: remove the export keyword if used only in-file, or delete the symbol if unused, with tests green; fsi-app/scripts/mint/lib/gate-a-scan.mjs and gate-a-match.mjs shims: delete them and point the one test importer (record-facts.npmtest.mjs) at src/lib/agent/, update fsi-app/scripts/harness-runs/governing-files.mjs GOVERNING_FILES.mint (remove the two shim entries) and re-pin the mint marker (governing-files.mjs is meta-harness governing: chain that marker too), scripts/mint/CONVENTION.md / MINT-RUNBOOK.md rows that name the shims; docs/plans/unwired-disposition-2026-08-31.md: mark rows 3, 10, 12, 18 executed with the commit; fsi-app/.discipline/run-test-suite.sh for removed tests. Also the four register WIRE rows still open: report their status only (23 run-intake admin route, 24 admin users page, 26 assumption-register-seed: are they wired now? cite), do not build them (other lanes). Full suite green after deletions.`, { label: 'DEAD-EXEC', phase: 'Lanes', model: 'sonnet' })

const [d1, d2] = await Promise.all([D1, D2])
return { d1, d2 }
