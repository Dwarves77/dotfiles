export const meta = {
  name: 'lane-perf7',
  description: 'Sonnet lane PERF-7: the last two getUser() Auth round trips PERF-6 found and could not touch: server-bootstrap.ts and community-auth.ts (double getUser)',
  phases: [{ title: 'Lane', detail: 'PERF-7 in its own worktree' }],
}
phase('Lane')
const P = `You are lane PERF-7 on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/perf7 (branch lane/perf7-2026-09-04, based on train/wave16 00cc7f44 which is landing on master now; it already contains PERF-6's org.ts/auth.ts getClaims() swap and its tests, read them first as the pattern); node_modules symlinked at fsi-app/node_modules. Cannot push; coordinator lands. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. Write set: fsi-app/src/lib/api/server-bootstrap.ts, fsi-app/src/lib/api/community-auth.ts, their tests (*.npmtest.mjs next to them, matching org.npmtest.mjs/auth.npmtest.mjs), and docs/audits/perf-load-times-2026-09-03.md (append §13). Nothing else. Deterministic; "there is NO small follow-up fix, fix it now"; no claims ahead of evidence, [CONFIRMED]/[HYPOTHESIS]; read every file you touch in full and every caller (grep across fsi-app/src); commit message ends with "Co-Authored-By: Claude Sonnet <noreply@anthropic.com>". Gates: npx tsc --noEmit -p fsi-app/tsconfig.json (0 errors; PERF-6 measured 0 on this tree), node --test on your tests plus org.npmtest.mjs and auth.npmtest.mjs, from repo root node fsi-app/.discipline/fitness/runner.mjs (0 violations) and node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs.

EVIDENCE [CONFIRMED by lane PERF-6, 2026-09-04]: (1) fsi-app/src/lib/api/server-bootstrap.ts calls auth.getUser(); PERF-6 says it is inert on RSC-navigation click-throughs because of PERF-4's rscNav gate and only matters on document loads (the cold 3.85 s home load path), so it is exactly the cold-load cost the operator measured. (2) fsi-app/src/lib/api/community-auth.ts calls getUser() twice (lines ~48 cookieClient.auth.getUser() and ~64 tokenClient.auth.getUser(token)), on every community route. The proven pattern is fsi-app/src/proxy.ts lines 12-30 and 66, and PERF-6's resolveOrgIdFromAuthenticatedClient / resolveUserIdFromToken cores. DO: read both files and every caller; list which fields of the User object each caller needs (community likely needs email or user_metadata for display or salt: check src/lib/community/** and the community salt derivation, WORKER_SECRET-based per #552, before assuming id suffices) and handle each explicitly, keeping a documented getUser() fallback ONLY for a field claims cannot carry and only on that path; swap the common path to getClaims()/claims.sub; if community-auth's two calls are the cookie-vs-bearer branches, make them one resolver with one call; memoize per request where the same request resolves twice (React cache(), the pattern PERF-6 used and verified safe outside render). Tests: authenticated cookie, authenticated bearer, unauthenticated, expired, malformed, fallback path, no network. Report: consumers and fields, round trips before/after per route as [INFERRED] arithmetic, files, exported functions, tests, tsc/fitness, commit hash, anything refused with the reason.`
const r = await agent(P, { label: 'PERF-7', phase: 'Lane', model: 'sonnet' })
return { perf7: r }
