export const meta = {
  name: 'lane-mig310fix',
  description: "MIG310-FIX: migration 310 cannot apply (CREATE OR REPLACE cannot change RETURNS TABLE, error 42P13 reproduced live); rewrite it on the migration-272 pattern (DROP, re-create, re-grant) so the coordinator can apply it",
  phases: [{ title: 'Fix' }],
}
phase('Fix')
const r = await agent("You are lane MIG310-FIX on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/mig310fix (branch lane/mig310fix-2026-09-05, based on origin/master c3003233 = train 47, PR #594). node_modules symlinked at fsi-app/node_modules; Node 24 at /home/claude/.npm/_npx/387698761821791d/node_modules/node/bin/node (dirname FIRST on PATH). Cannot push; the coordinator lands. Main clone /root/work/dotfiles is read-only; git only inside your worktree. CLAUDE.md rules bind: no copies of logic, no dead code, [CONFIRMED]/[HYPOTHESIS], read every file you touch IN FULL, commit message ends with \"Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\". Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md.\n\nTHE DEFECT [CONFIRMED by the coordinator, live, 2026-09-05 20:20 UTC]: fsi-app/supabase/migrations/310_listing_rpcs_item_grade.sql (lane CHIPS) adds `item_grade text` to the RETURNS TABLE of 11 functions using CREATE OR REPLACE FUNCTION only. Postgres refuses: `ERROR 42P13: cannot change return type of existing function. DETAIL: Row type defined by OUT parameters is different. HINT: Use DROP FUNCTION get_workspace_intelligence_slim_public() first.` (reproduced in a BEGIN/ROLLBACK on the live project; nothing applied; live md5s of all 11 bodies still equal the file's pre-check table). The repo already documents this exact error and its fix: read fsi-app/supabase/migrations/272_customer_rpcs_project_jurisdiction_iso.sql IN FULL (its header explains 42P13, it DROPs then re-creates and re-GRANTs), and 305/306 for the newer functions' shapes and grants. Migrations 308 and 309 are already applied live; 311 is not yet (it is independent of 310).\n\nBUILD: rewrite 310 in place (same filename, same number) so it applies in one transaction via Supabase MCP apply_migration: keep its header, its md5 pre-check table, and its post-check DO block unchanged in substance; add, before the CREATE statements, DROP FUNCTION IF EXISTS for all 11 with their exact identity arguments [CONFIRMED live]: _workspace_active_items(p_org_id uuid); get_workspace_intelligence_slim(p_org_id uuid); get_workspace_intelligence_slim_public(); get_workspace_intelligence_listings(p_org_id uuid, p_domain integer); get_workspace_intelligence_listings_public(p_domain integer, p_after_priority text, p_after_added_date date, p_after_id uuid); get_market_intel_items(p_org_id uuid); get_market_intel_items_public(); get_operations_items(p_org_id uuid); get_operations_items_public(); get_research_items(p_org_id uuid); get_research_items_public(). Drop order: the dependents before _workspace_active_items is not required by Postgres (plpgsql bodies are not dependency-tracked) but do it anyway for readability. After the CREATEs, re-GRANT EXECUTE exactly as live [CONFIRMED live proacl on all 11]: anon, authenticated, service_role (PUBLIC also has X in the live ACL: `{=X/postgres,postgres=X/postgres,anon=X/postgres,authenticated=X/postgres,service_role=X/postgres}`; CREATE FUNCTION grants PUBLIC by default, so grant the three named roles explicitly and say so in a comment). Replace the file's now-false line 'Grants unchanged ... CREATE OR REPLACE preserves grants' with the truth. Keep the file's BEGIN;/COMMIT; wrapper convention as 308/309 have it (the coordinator strips the outer BEGIN/COMMIT when applying through the MCP, which runs its own transaction). Check whether anything in the tree depends on these functions' identities in a way a DROP breaks (views, other SQL functions via pg_depend: grep the migrations for the 11 names; there should be none, 272 dropped the same family) and state the result. Also check whether any OTHER unapplied migration file on master has the same defect (311 uses ALTER/DROP TABLE only; confirm). Then check the code side that lane CHIPS said needs no change (src/lib/supabase-server.ts mapWorkspaceItemRows / rpcRowToResource read row.item_grade) is true on this tree by reading it. Update docs/inventories/migrations.md's 310 row to describe the DROP+re-create shape (row stays WRITTEN, NOT APPLIED; the coordinator flips it after applying) and, if a runbook or the CHIPS lane's session-log postscript states 'CREATE OR REPLACE preserves grants' as the mechanism, leave the log alone (frozen history) but make sure the migration file itself is the truth.\n\nGATES: node fsi-app/.discipline/fitness/runner.mjs (0 violations beyond what master c3003233 already has: run it once on a clean checkout of master first and diff, paste both counts); node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs; node fsi-app/.discipline/governance/closure-gate.mjs (paste the line; if it is red on master already because train 47 landed, say so, do not fix it, that is lane W7.1-CLOSE's job); node fsi-app/.discipline/consistency/override-check.mjs --range=origin/master..HEAD (C3 clean). If a local Postgres is available (check `which postgres pg_ctl psql` and docker), prove the rewritten file applies against a scratch database seeded with the 11 pre-migration function bodies from 272/305/306; if not available, say so and instead prove syntactically with `node -e` splitting statements is not needed; the coordinator applies live.\n\nREPORT: the diff summary of 310, the DROP list, the GRANT statements, the dependency check result, the other-migrations check, the code-side confirmation, every gate line, commit hash.", { label: 'MIG310-FIX', phase: 'Fix', model: 'sonnet' })
return { MIG310_FIX: r }
