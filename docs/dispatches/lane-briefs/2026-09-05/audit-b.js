export const meta = {
  name: 'audit-b',
  description: 'Wiring audit 2026-09-04, part B: libraries/modules built since 2026-08-21 (B1) and the data layer: migrations, tables, RPCs (B2)',
  phases: [{ title: 'Audit' }],
}
phase('Audit')
const COMMON = `Repo Dwarves77/dotfiles, worktree /root/work/lanes/audit (branch lane/audit-2026-09-04 = origin/master e8cb748f); node_modules symlinked at fsi-app/node_modules. READ-ONLY audit: touch NOTHING except your own output file under docs/audits/wiring-audit-2026-09-04/ (named below). Do not commit. The window is every commit since 2026-08-21 on master: docs/audits/wiring-audit-2026-09-04/_prs.txt lists the 107 PRs. Evidence rules: every verdict cites evidence (file:line, artifact path, commit, or read-only SQL). [CONFIRMED]/[HYPOTHESIS]. Supabase MCP (server "Supabase", tool execute_sql, project kwrsbpiseruzbfwjpvsp; ToolSearch "select:mcp__Supabase__execute_sql") READ-ONLY SELECT only. Operator's question: "are all of our site tools built and wired? I want an audit of everything built in the last two weeks to see if it is being used and wired and fits within the flywheel loop." Loop stages: source sweep -> portal_link_candidates -> ledger-consume -> census_worklist -> population-turn (mint under THE GATE -> connections + forward events + tags) -> surfaces -> change-detection -> propagation -> sources. Output: a markdown section with a table per component: Component | Built (PR, date) | Importers / callers (count, names) | Reachable from (runtime step / page / nothing) | Evidence of use | Loop stage | Verdict (WIRED+USED / WIRED-NOT-RUN / BUILT-NOT-WIRED / DESIGNED-ONLY / DEAD) | Gap; then ranked gaps and what you could not confirm.`
const B1 = agent(`${COMMON}

You are AUDIT-B1: MODULES. Output file: docs/audits/wiring-audit-2026-09-04/B1-modules.md. Enumerate every .mjs/.ts module ADDED since 2026-08-21 under fsi-app/src/lib, fsi-app/scripts (mint, turns, maintenance, producers, forward-events, lib, harness-runs), fsi-app/supabase/functions, and fsi-app/.discipline (git log --since=2026-08-21 --diff-filter=A --name-only), excluding tests and fixtures. For each: who imports it (grep across src/ and scripts/ and .github/workflows), whether it is execution-reachable using the repo's OWN resolver (node fsi-app/.discipline/governance/execution-wiring.mjs, and coverage-scan.mjs; read their usage first; the 2026-08-11 census docs/audits/wiring-census-2026-08-11.md used them, keep the same definition of "wired"), whether F25 (consumption) or F22 governs it, and its loop stage. Also list every module whose ONLY importer is a test, and every exported function with zero callers in a module that otherwise is wired (dead exports), bounded to the window. Include the eight harness families' runners and the harness layer itself (governing-files.mjs, run-artifact.mjs, F28) as components.`, { label: 'AUDIT-B1', phase: 'Audit', model: 'sonnet' })
const B2 = agent(`${COMMON}

You are AUDIT-B2: DATA LAYER. Output file: docs/audits/wiring-audit-2026-09-04/B2-data-layer.md. Enumerate every migration file added since 2026-08-21 under fsi-app/supabase/migrations (numbers roughly 27x through 304) and, for each: applied live or not (compare against docs/inventories/migrations.md rows and verify live: schema_migrations if the repo uses it, else information_schema / pg_get_functiondef for the object the migration creates; say which check you used), what object it creates/changes (table, column, RPC, index, function), who reads/writes that object in code (grep table/RPC names across src/ and scripts/), live row counts for new tables, and loop stage. Flag: migrations written but not applied (name the operator gate), objects with zero readers, RPCs not on the app allowlist (read src/lib/supabase-server.ts allowlists), tables written by more than one path (cross-check docs/inventories/shared-dataset-ownership.md, the one-writer rule), and any live object with no migration (drift; the drift-207 precedent). Also report the current live counts that describe the loop's state: sources (total, provisional, with rss_feed_url), portal_link_candidates by status, census_worklist by dryrun_disposition, intelligence_items by item_grade x provenance_status (non-archived), item_forward_events, connection edges table, tags/themes tables, monitoring_queue, published_price_statistics, community tables.`, { label: 'AUDIT-B2', phase: 'Audit', model: 'sonnet' })
const [b1, b2] = await Promise.all([B1, B2])
return { b1, b2 }
