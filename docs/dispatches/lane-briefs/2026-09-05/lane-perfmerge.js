export const meta = {
  name: 'lane-perfmerge',
  description: 'Sonnet lane PERF-MERGE: land PERF-10 (static listing + SSG detail routes, public RPCs, revalidateTag on apply) on top of master + PERF-11 + PERF-ARCH; resolve the overlapping edits; renumber its migration to 306; fix the two live regressions measured after train 43 (React #418 on /regulations again, and a false "No workspace yet" banner for a viewer who has a workspace)',
  phases: [{ title: 'Lane' }],
}
phase('Lane')
const P = `You are lane PERF-MERGE on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/perfmerge (branch lane/perfmerge-2026-09-04, based on train/wave44-2026-09-04 = master a58478cc (train 43: PERF-11 + PERF-ARCH) + a migration-305 correction); node_modules symlinked (no npm install). Cannot push; coordinator lands and measures live. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. PERF-12 runs in parallel on a train-43 base (TanStack cursor rows + virtualization on the ledgers, src/lib/list-pagination.ts, the listing route, RegulationsLedger and siblings): do not redesign the ledger row list; the coordinator merges PERF-12 after you. Rules: [CONFIRMED]/[HYPOTHESIS]; measure, do not assume; "there is NO small follow-up fix, fix it now"; the standard library over home-grown; commit message ends with "Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>". Gates: tsc; node --test over every touched test and fsi-app/src/__tests__/*.mjs; repo-root node fsi-app/.discipline/fitness/runner.mjs (31 functions, 0 violations; F37 must pass or ratchet down); discipline governance tests; the rendering smoke (fsi-app/.discipline/rendering/run-rendering-guard.mjs); bash fsi-app/.discipline/run-test-suite.sh (the full no-npm suite: CI runs it WITHOUT node_modules, so any test importing react/next must be an *.npmtest.mjs, see server-timing.npmtest.mjs); cd fsi-app && npx next build --webpack (paste the route table: the acceptance is /regulations, /market, /operations, /research ○ and the four [slug] routes ● as PERF-10 achieved, on THIS merged tree).

STEP 1, THE MERGE: git cherry-pick -x f6d436f9 (PERF-10's single commit on lane/perf10-2026-09-04: "root-cause fix for ƒ-everywhere builds — RPC split + generateStaticParams", 47 files; read its full message and diff first: git show f6d436f9 --stat, then the files). Expect conflicts with PERF-11 (995d82e3 on master: src/app/regulations/page.tsx, src/app/market/page.tsx, src/lib/supabase-server.ts, src/lib/data.ts, src/components/regulations/ObligationRegister.tsx and ObligationRegisterFilterBar.tsx, src/app/api/obligations/register/route.ts, migration numbering) and PERF-ARCH (cec6549c: src/lib/perf/**, discipline manifest F37, bootstrap route instrumentation). Resolve by DESIGN, not by picking sides: PERF-10's public org-independent RPCs + client-side override merge + static/SSG routes is the architecture (ADR-026 addendum, ADR-027 D1/D5); PERF-11's first-page + Load more register, the /api/obligations/register route with {rows,total}, the query-side domain filter and toLedgerRowPayload trim all stay (PERF-10 also made the register a client component hitting the same route family: converge on ONE ObligationRegister/route design that keeps PERF-11's paging and PERF-10's client-only per-viewer resolution). The domain filter: PERF-11 added p_domain to the org RPC (migration 305, APPLIED live 2026-09-04 19:55 as one 2-arg overload); PERF-10's public RPCs (get_workspace_intelligence_listings_public etc.) must take the same optional p_domain so the static /regulations page fetches only its domain: fold that into PERF-10's migration. RENUMBER PERF-10's migration file from 305_public_workspace_intelligence_listings.sql to 306_public_workspace_intelligence_listings.sql (305 is taken by PERF-11's domain-param file, already applied), keep it written-not-applied, md5-guarded, with the docs/inventories/migrations.md row at 306; the code must keep working before 306 applies (PERF-10's fail-soft, or the fallback to the org RPC) and say exactly what the coordinator dispatches/applies in what order.

STEP 2, THE TWO LIVE REGRESSIONS [CONFIRMED by the coordinator in Chrome on carosledge.com at 19:56 UTC, deployment of train 43 = master a58478cc, logged-in operator session whose org HAS a workspace ("workspace verticals: Live events · Fine art" in the masthead)]: (a) "Minified React error #418" (hydration mismatch, args HTML) is thrown again on /regulations from the main chunk (it had been closed by PERF-8's timeZone pin; it returned with PERF-9's layout restructure or PERF-11's register/filter-bar changes: find which by reasoning from the diff and by a deterministic test: render the suspect components to HTML with react-dom/server and compare with a client render, or at minimum identify every server/client-divergent value: Date.now, locale, window, useSearchParams, a client-only state seeded differently from the server prop); (b) the AppShell banner "No workspace yet. Accept an invitation or create your own to start collaborating." (src/components/AppShell.tsx:35-37, showNoWorkspaceBanner = !!user && !orgId) renders as a role=status element for a viewer whose org exists: since PERF-9 moved headers()/the bootstrap into a Suspense-gated BootstrapResolver, AuthContext's orgId is null until the resolver lands, so user-present + orgId-null is now a transient state that the banner treats as a fact (docs/design/ux-laws.md: a surface must never show a false state while data loads). Fix: a three-value orgId (unknown | null | id) in AuthContext, the banner only for a RESOLVED null, and nothing rendered for unknown; and make sure the same pattern holds for every other consumer of orgId/user during the unresolved window (grep useAuth() consumers). Also verify that the layout's Suspense/streaming order cannot produce a server HTML that differs from the client's first render (that is (a) again).

STEP 3, PROVE: next build route table on the merged tree; the rendering smoke; a test for the banner's three states; a test that pins the hydration fix. Report: the conflict list and how each was resolved (file, design decision), the route table, the #418 root cause [CONFIRMED with file:line] and fix, the banner fix, the migration order for the coordinator (306 apply, then what), files, tests, fitness/F37 line, commit hashes, the UX compliance block for every surface touched (grep -rn "UX compliance" fsi-app/.discipline docs/doctrine), anything refused with the reason.`
const r = await agent(P, { label: 'PERF-MERGE', phase: 'Lane', model: 'sonnet' })
return { perfmerge: r }
