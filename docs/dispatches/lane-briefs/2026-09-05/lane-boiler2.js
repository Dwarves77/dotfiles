export const meta = {
  name: 'lane-boiler2',
  description: 'Sonnet lane BOILER-2: three evidenced record-facts defects HOLLOW-GATE reported and could not fix: the bare-domain guard rejects genuine operative provisions, jurisdictional_scope truncates at a numbered-list period, Cellar garbled metadata on agreement-type CELEX captures',
  phases: [{ title: 'Lane', detail: 'BOILER-2 in its own worktree' }],
}
phase('Lane')
const P = `You are lane BOILER-2 on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/boiler2 (branch lane/boiler2-2026-09-04, based on train/wave16 00cc7f44 which is landing on master now); node_modules symlinked at fsi-app/node_modules. Cannot push; coordinator lands. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. Write set: fsi-app/src/lib/intake/record-facts.mjs (+ record-facts.npmtest.mjs), fsi-app/scripts/mint/validate-mint-payload.mjs (+ test) only if the same guard is duplicated there, fsi-app/scripts/turns/export-census-rows.mjs (+ test) only for the Cellar capture item, fsi-app/scripts/mint/MINT-RUNBOOK.md §13, and fsi-app/scripts/harness-runs/mint/PENDING-RUN.md re-pinned to the hash the fitness runner prints (record-facts.mjs and validate-mint-payload.mjs are mint governing files; append a "What changed (4)" entry in the marker's existing chained format and keep the single line "**harness_version at write time:** \`sha256:...\`" as the LAST hash line, that exact phrase is what parsePendingRunHash reads). Nothing else. Deterministic, $0, no LLM; "there is NO small follow-up fix, fix it now"; no claims ahead of evidence, [CONFIRMED]/[HYPOTHESIS]; read every file you touch IN FULL (record-facts.mjs header carries every prior lane's findings; read URL-GUIL's and URL-BOILER's and HOLLOW-GATE's entries); commit message ends with "Co-Authored-By: Claude Sonnet <noreply@anthropic.com>". Gates: node --test on what you touch (record-facts.npmtest.mjs, validate-mint-payload.test.mjs, export-census-rows.test.mjs must all stay green, HOLLOW-GATE's 208 included), then from the repo root node fsi-app/.discipline/fitness/runner.mjs (0 violations) and node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs. Supabase MCP (server "Supabase", execute_sql, project kwrsbpiseruzbfwjpvsp, via ToolSearch) for READ-ONLY SELECT of real captures (agent_run_searches.result_content joined to intelligence_items by run) to build fixtures; never write.

THREE DEFECTS, all evidenced by lane HOLLOW-GATE (2026-09-04) [CONFIRMED in its report]:
(1) hasOnlyBareDomainUrls (record-facts.mjs ~line 311, wired into isProseSpan ~330, from lane URL-BOILER) disqualifies ANY span containing a bare-domain URL, so a genuine operative_provision span for CELEX 32012D0706(01) that merely mentions a domain is rejected (1 of 12 in HOLLOW-GATE's measured sample). URL-BOILER's guard exists for rows 429c85d2 (UK SI 2013/816) and a980a0b9 (UK SI 2012/2567), whose selected span is explanatory-note boilerplate pointing at http://eur-lex.europa.eu as a "jurisdictional scope" fact; HOLLOW-GATE tried a word-count-after-URL-removal fix and correctly rejected it because those two spans also carry >4 words of prose. The distinction that actually separates them is semantic and slot-specific, not lexical: for a URL-bearing slot (jurisdictional_scope, sources) a bare domain is the fact's content and is a pointer; for a prose slot (operative_provision, addressee, confirmed_measure, binding_position) a URL is incidental. Read how slots declare their triggers and whether a slot knows it is URL-bearing (criterion 2 grounds citations, so which slots produce a cited URL?). Implement the guard so that it applies where the URL would BE the fact (the two UK rows still refuse, prove it by replaying their real captured_text from scripts/_snapshots/population-33823467586 or 33821410389 census-rows.json, both on disk in this tree) and does not apply where the URL is incidental to an operative provision (32012D0706(01) accepted, prove it from its real capture). If a bare domain inside a prose slot should be stripped from the span rather than accepted verbatim, say why not: spans must be verbatim (validate_item_provenance matches source_span byte-exact), so the answer is accept-or-refuse, never edit.
(2) jurisdictional_scope truncates at a numbered-list period for recommendation-shaped documents (HOLLOW-GATE: "numbered-list period truncation"). Find the trigger, reproduce on a real recommendation capture (CELEX 32013H0179 or any 'H' sector-3 recommendation in the corpus; SELECT one), and fix the continuation regex so a "1." / "(a)" list marker does not end the span, using the same URL-safe continuation pattern URL-GUIL introduced, with tests on the real text.
(3) Cellar-endpoint garbled metadata for some agreement-type CELEX documents (HOLLOW-GATE: "Cellar garbled metadata captures"). Measure first: SELECT the captures for item_type in agreement-like types or CELEX sector 2 'A' documents on eurlex hosts, find what "garbled" means concretely (encoding? the Cellar notice page instead of the act? a metadata table?), and state whether it is a capture defect (export-census-rows.mjs capture path / the Cellar resolver in src/lib/sources or scripts/mint) or an extractor defect. Fix it where it lives if it is inside your write set; if the fix belongs in a file outside your write set, do NOT edit it: write the exact diff and the evidence in your report and in MINT-RUNBOOK.md §13 so the coordinator lands it, and add a screen in export-census-rows.mjs that holds such captures with a named reason (capture_garbled_metadata) rather than letting them mint hollow.
Report: for each defect the evidence, the fix, the real-capture fixtures used, tests, fitness, the marker hash, commit hash, anything refused with the reason.`
const r = await agent(P, { label: 'BOILER-2', phase: 'Lane', model: 'sonnet' })
return { boiler2: r }
