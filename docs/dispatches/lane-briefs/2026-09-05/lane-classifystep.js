export const meta = {
  name: 'lane-classifystep',
  description: 'Lane CLASSIFY-STEP: maintenance wrapper apply-classifications',
  phases: [{ title: 'Lanes', detail: 'CLASSIFY-STEP in its own worktree' }],
}
phase('Lanes')
const RULE = `THE RULE (operator, 2026-09-04, verbatim): "there is no thing within this entire build that works on its own ever. Everything works in tandem. It is important that this is a rule. Everything works together, that's the purpose of the flywheel and the harness." A runtime that ends without triggering its downstream is a defect in the runtime, not a note for a coordinator.`
const COMMON = `Repo Dwarves77/dotfiles (Caro's Ledge). Rules: work ONLY inside your worktree (a linked git worktree on branch lane/<name>-2026-09-04 based on origin/master 1356b381); node_modules is symlinked at fsi-app/node_modules; you cannot push, the coordinator lands your commits; do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md, and do NOT touch any file outside your write set (six other lanes are running now on other files); deterministic, $0, no LLM anywhere; "there is NO small follow-up fix, fix it now"; no claims ahead of evidence, label [CONFIRMED]/[HYPOTHESIS]; read every file you touch in full before editing; commit message ends with "Co-Authored-By: Claude Sonnet <noreply@anthropic.com>" (or Claude Haiku if you are Haiku). Gates before you commit: the node --test files for what you touched, then from the repo root: node fsi-app/.discipline/fitness/runner.mjs (0 violations; if F28 names a harness family, write that family's PENDING-RUN.md in the format of fsi-app/scripts/harness-runs/change-detection/PENDING-RUN.md with the hash it prints) and node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs; if you touch .tsx/.ts: npx tsc --noEmit -p fsi-app/tsconfig.json clean, and read docs/design/ux-laws.md + docs/design/design-principles.md first (F35 measures rows at 375 px). The Supabase MCP (server "Supabase", execute_sql, project kwrsbpiseruzbfwjpvsp) is available via ToolSearch for READ-ONLY SELECT/EXPLAIN; never write through it. Your final text is your report: root cause with evidence, files, exported functions, tests, fitness/tsc, commit hash, and anything refused and why. ${RULE}`

const TANDEM = `You are lane TANDEM. Worktree /root/work/lanes/tandem. Write set: .github/workflows/population-turn.yml, a NEW fsi-app/scripts/turns/run-population-flywheel.mjs (+ test), fsi-app/scripts/mint/run-mint-batch.mjs ONLY its existing --outcomes path if it needs a flag, docs/runbooks/POPULATION-TURN-RUNBOOK.md, fsi-app/scripts/mint/MINT-RUNBOOK.md §8/§9 text. Do NOT touch export-census-rows.mjs, record-facts.mjs, validate-mint-payload.mjs, apply-mint-batch.mjs internals, maintenance.yml (other lanes). ${COMMON}

THE DEFECT [CONFIRMED]: MINT-RUNBOOK.md §8 ("MANDATORY, post-apply — the flywheel": discovery, forward-event extraction, recluster, IN ORDER before a batch is closed) and §9 (--outcomes enrichment: edges_discovered, forward_events_extracted, isolated_items written back into mint-run-NNN.json) are documented as a separate coordinator pass. The population workflow ends after apply-mint-batch + propose-tags --dry. Population runs #15-#20 (2026-09-03/04, ~650 items, mint-run-017..022) were applied with no flywheel pass and no outcomes; every one of those items has zero item_cross_references, zero item_forward_events, no obligations, no tags, no signals. The coordinator (me) skipped it. Under THE RULE this is a runtime defect: the population workflow must not end before the flywheel has run over what it minted and the harness has recorded the outcome.

READ IN FULL: .github/workflows/population-turn.yml; .github/workflows/corpus-turn.yml (the chain it runs: scripts/connections/discover-for-items.mjs --since, scripts/turns/export-corpus-for-extraction.mjs, scripts/forward-events/run-extraction.mjs --execute, scripts/turns/apply-extraction-output.mjs --execute, scripts/connections/analyze-corpus.mjs --signals, scripts/turns/last-turn-date.mjs --record; read each script's header for its contract, --since semantics and idempotency); .github/workflows/maintenance.yml's derive-obligations, tag-proposals (arg), tag-ratification (arg auto) steps and their scripts under fsi-app/scripts/maintenance/; fsi-app/scripts/mint/run-mint-batch.mjs --outcomes and fsi-app/scripts/harness-runs/PROPOSER-RUNBOOK.md "Corpus outcomes" (the exact SQL for the three metrics); fsi-app/scripts/harness-runs/CONVENTION.md; docs/runbooks/POPULATION-TURN-RUNBOOK.md and CORPUS-TURN-RUNBOOK.md; MINT-RUNBOOK.md §8-§9, §11.

BUILD: (1) run-population-flywheel.mjs: given a mint-run artifact path (or the run's minted item ids from apply-mint-batch's report) it runs, in §8 order, discovery over exactly those items, corpus export + forward-event extraction + apply for those items, analyze-corpus with --signals scoped the way corpus-turn scopes it, derive-obligations apply, tag-proposals apply and tag-ratification auto for those items (reuse the maintenance scripts' exported main() with the same deps, never re-implement), then computes the three §9 metrics from the live tables and writes them into the mint artifact with run-mint-batch.mjs --outcomes (or its exported function), and records LAST-TURN the way corpus-turn does so the two families never double-process. Every step is dry-able (--mode dry runs every step's own dry path and reports what would happen; apply writes). It must reuse the existing scripts' exported functions or invoke them as child processes with their documented flags; it must not duplicate any of their logic. (2) population-turn.yml runs it after apply-mint-batch in apply mode (and in dry mode runs its dry) and fails the job if the flywheel step fails, so a green run means minted AND connected AND recorded. (3) THE GATE: in apply mode, before export, the workflow reads the newest mint-run-NNN.json on the checkout and refuses (exit 1, clear message) if its metrics block lacks edges_discovered/forward_events_extracted/isolated_items, i.e. a prior slice was left unconnected; a repo-checked script does the check (pure, tested). (4) Keep the tracking-issue/branch delivery unchanged. (5) Update both runbooks' text so §8/§9 read "run by the workflow itself; the coordinator's job is to read the outcomes". Tests for the flywheel driver's step ordering, dry/apply, the metrics SQL shape (fake db), the gate's accept/refuse. Report the exact env/secrets the new step needs (must already be among the four the workflow has; if you need another, say so, do not add one silently).`

const HOLLOWGATE = `You are lane HOLLOW-GATE. Worktree /root/work/lanes/hollowgate. Write set: fsi-app/src/lib/intake/record-facts.mjs (+ record-facts.npmtest.mjs), fsi-app/scripts/mint/validate-mint-payload.mjs (+ test), fsi-app/scripts/turns/export-census-rows.mjs (+ test) for the in-force screen only, fsi-app/scripts/mint/payload-schema.json if a hold reason vocabulary lives there, fsi-app/scripts/mint/MINT-RUNBOOK.md §13 text. Nothing else. ${COMMON}

THE DEFECT [CONFIRMED, live SQL 2026-09-04]: 1,230 record-grade items are live and verified; 551 carry ONLY the [title] FACT, every other slot a GAP; 115 carry one substantive fact. Example: item 8670d8bf-9847-4da6-8724-0d52308b008e, "1999/823/EC: Commission Decision of 22 November 1999 confirming the measures notified by the Netherlands pursuant to Article 6(6) of Directive 94/62/EC ... packaging and packaging waste", item_type initiative, CELEX 31999D0823, source https://eur-lex.europa.eu/legal-content/EN/TXT/?uri=CELEX:31999D0823. Its agent_run_searches capture is 17,022 chars of real EUR-Lex text (head: "EUR-Lex - 31999D0823 - EN Avis juridique important | 31999D0823 1999/823/EC: Commission Decision of 22 November 1999 ..."); record-facts.mjs found nothing in it but the title, and validate-mint-payload.mjs passed the payload because a title FACT plus GAPs satisfies criterion 5. That item is on the customer site with an empty Summary. The operator: items are being uploaded with no details; this is unacceptable.

READ IN FULL: record-facts.mjs (all triggers per item_type; the URL-safe continuation and bare-domain guard added 2026-09-04; SPAN DELIMITERS header), validate-mint-payload.mjs, item-type-required-slots.json, MINT-RUNBOOK.md §5, §10, §11, §13; export-census-rows.mjs (the screen: screen verdict, held reasons, buildHeldKeyIndex); the live captured text of the example item (Supabase read-only: select result_content from agent_run_searches where intelligence_item_id='8670d8bf-9847-4da6-8724-0d52308b008e') and of at least 10 more title-only items across item_types (query: verified live record items whose only FACT claim_text starts with '[title]'; join agent_run_searches for their captures) so your extraction rules come from the real text, not from assumption.

BUILD: (1) THE GATE: validate-mint-payload.mjs refuses a record payload whose only FACT is the title with a new kit failure (criterion 5-family, reason record_hollow) so apply-mint-batch's existing hold-back records it as validation_failed:5:record_hollow and the existing reopen step can re-admit it; existing fixtures that legitimately carry facts must still pass; document the new reason in §5. (2) THE EXTRACTOR: for EU acts on EUR-Lex (the CELEX families the captures show: decisions D, regulations R, directives L, and the legacy-HTML shape), extract deterministically what the act states about itself from the captured text: the subject/object sentence (Article 1 or the "This Decision/Regulation ..." sentence), addressees ("This Decision is addressed to ..."), entry-into-force and application dates ("shall enter into force on ...", "shall apply from ..."), the notified/confirmed measure when the act confirms a national measure, and the EUR-Lex page's own in-force status text where the capture carries it ("No longer in force", "Date of end of validity", "In force"). Each becomes a verbatim FACT with source_span (never paraphrased, never invented, GAP when absent) under the existing slot vocabulary (map to the slots record-facts already emits for the item_type; add a slot only if the vocabulary in item-type-required-slots.json / vocabularies.mjs allows it, and say which). Measure the yield: run the new extractor over the same 10+ real captures and report facts-per-item before/after. (3) THE IN-FORCE SCREEN: in export-census-rows.mjs, a row whose capture states the act is no longer in force (EUR-Lex "No longer in force"/end-of-validity in the past) is held not_in_force with the evidence span, never minted; keep the existing held-key/screen semantics byte-identical otherwise. (4) Bump RECORD_FACTS_VERSION and whatever version the kit validator carries. Tests for each new extraction rule on real-shaped fixtures, the hollow refusal, the in-force hold, and the existing suites green. Do NOT touch validate_item_provenance (SQL) — the kit is allowed to be stricter than the database. Report the measured yield and how many of the 551 you expect to re-mint with facts, labelled.`

const HOLLOWSWEEP = `You are lane HOLLOW-SWEEP. Worktree /root/work/lanes/hollowsweep. Write set: NEW fsi-app/scripts/maintenance/record-hollow-sweep.mjs (+ test), .github/workflows/maintenance.yml (add the step option and job step; you are the ONLY lane editing this file), fsi-app/docs/inventories/shared-dataset-ownership.md (register your writer), docs/runbooks/MAINTENANCE-RUNBOOK.md (one section). Nothing else. ${COMMON}

THE DEFECT [CONFIRMED, live SQL]: 551 of 1,230 live verified record-grade intelligence_items carry only the [title] FACT (every other claim a GAP); they render on customer surfaces with an empty Summary. A parallel lane (HOLLOW-GATE) is making the kit refuse such payloads and extracting real facts from EU acts; this lane owns the EXISTING rows: get them off the customer surfaces now, and route them back through the mint so they return with facts.

READ IN FULL: fsi-app/scripts/maintenance/lib/cli.mjs and two existing steps (census-off-vertical.mjs + test: the archive/park precedent with a ruling arg; institution-canonicalize.mjs + test: the newest); fsi-app/scripts/lib/db.mjs (guarded writes, the archive helpers, rule 019 on archive reasons and migration 135); migration 062 (hidden_reason) and every reader of hidden_reason and is_archived in fsi-app/src/lib (grep: hidden_reason, is_archived, pipeline_stage) to determine, with file:line evidence, which mechanism actually removes an item from EVERY customer surface (list, detail, search, watchlist, community, market, dashboard counts) without deleting it: is_archived + archive_reason, or hidden_reason + pipeline_stage, or both; census_worklist's hold columns (migration 221) and the reconcile stamp apply-mint-batch.mjs writes (which census row minted which item: read apply-mint-batch.mjs's reconcile section and export-census-rows.mjs's held-key index so a re-minted row is not blocked by its own archived twin — the M4 pre-check and buildHeldKeyIndex treat an archived holder specially; find out exactly how and design so the re-mint is admitted); the population-pass docs (docs/plans/population-pass-2026-09-03.md §2 on off-vertical archiving) for the precedent of archiving record items so the census re-mints them.

BUILD the MAINT step record-hollow-sweep: SELECTION = live verified record-grade items whose FACT claims are title-only (the exact SQL, with counts by item_type and by source host in the report); dry lists them; apply (a) takes each off the customer surfaces by the mechanism the readers prove (archive_reason a new vocabulary value such as record_hollow if rule 019 permits a script-set reason, else the sanctioned path), recording before/after per item in the artifact, (b) returns the corresponding census_worklist rows to would_mint with a note naming this sweep so the next population apply re-mints them through the improved extractor, and (c) leaves anything else untouched (claims, sections, edges stay; nothing deleted). It must be idempotent and reversible (the artifact carries the ids; a --arg restore:<artifact ids> path or documented SQL restores). Read-back after apply. Register the writer. Tests with the fake-db harness the other steps use. Report: the exact reader evidence for which flag hides an item everywhere, the archive/hold mechanism chosen and why, counts, commit hash.`

const CLASSIFY = `You are lane CLASSIFY-STEP. Worktree /root/work/lanes/classifystep. Write set: NEW fsi-app/scripts/maintenance/apply-classifications.mjs (+ test) ONLY, plus fsi-app/docs/inventories/shared-dataset-ownership.md if the writer registry test requires an entry. Do NOT edit .github/workflows/maintenance.yml (another lane owns it): instead put the exact YAML snippet for its step option + job step in your report and the coordinator adds it. ${COMMON}

CONTEXT [CONFIRMED]: fsi-app/scripts/classification/apply-classifications.mjs writes ratified or auto-adopted source classifications (scope_modes, scope_verticals, expected_output; the --auto-adopt path, operator ruling 2026-09-03, no human gate) and propose-classifications.mjs writes the proposals as integrity_flags; docs/runbooks/CORPUS-TURN-RUNBOOK.md line ~49 records that neither runs as part of any turn today, so classifications never run unless a coordinator runs them by hand, which cannot happen (no credentials outside Actions). Build the MAINT wrapper exactly in the shape of fsi-app/scripts/maintenance/tag-proposals.mjs and tag-ratification.mjs (read both + their tests + lib/cli.mjs in full): step name apply-classifications, dry = propose (dry) + list what --auto-adopt would adopt, apply = propose then apply --auto-adopt, both through the scripts' own exported functions with the same deps, artifact summary in the standard shape, read-back counts. Tests with the same fake-db pattern. Report the YAML snippet and the commit hash.`

const SURFACE = `You are lane RECORD-SURFACE. Worktree /root/work/lanes/recordsurface. Write set: fsi-app/src/app/regulations/[slug]/page.tsx and the detail-page components it renders for a record-grade item (find them; likely under fsi-app/src/components/regulations/** or a shared item-detail component), the equivalent detail routes for the other surfaces that carry record-grade items (read fsi-app/src/lib/surface-of.mjs to learn which item_types land where: regulation/directive/guidance/framework/standard → regulations; initiative/market_signal → ?; research_finding → research), plus tests and rendering-guard specs. Do NOT touch fsi-app/src/lib/data.ts, supabase-server.ts, the list pages or /api routes (lane PERF-5 owns them). ${COMMON} Read docs/design/ux-laws.md and docs/design/design-principles.md before any .tsx edit; F35 and the rendering guard measure at 375 px; include a "UX compliance" paragraph in your report (law by law, what you did) because the discipline CI requires it in the session-log addendum the coordinator writes.

THE DEFECT [CONFIRMED, screenshot from the operator's phone + live SQL]: a record-grade item (item_grade='record', e.g. 8670d8bf-9847-4da6-8724-0d52308b008e, "1999/823/EC: Commission Decision ...") renders the banner "CATALOGUE RECORD: EXTRACTED FACTS ONLY, FULL BRIEF PENDING", the Ask box, and an otherwise empty Summary tab. Its full_brief is the kit's generated markdown (a "Verbatim facts" list and a "Not stated in the captured source" list), its intelligence_item_sections carry identity / record_facts / sources_and_citations, and summary/what_is_it/why_matters are empty. After the parallel lanes land, record items will carry more facts (subject, addressees, dates, in-force status) plus flywheel output: item_cross_references edges, item_forward_events, obligations (migration 290), tags, signal flags. The page must show what the system knows for a record item instead of an empty tab, honestly labelled as extracted facts, never as a brief.

READ IN FULL: the detail page(s) and every component on the record path; how brief-grade items render their Summary/Exposure/Penalty/Timeline tabs; the readers for edges (item_cross_references), forward events, obligations, tags for a single item (grep in fsi-app/src/lib); docs/specs/01-*.md for the regulation surface's intended detail content; docs/design/ux-laws.md. BUILD: for item_grade='record', a Summary that renders (1) the verbatim facts as a clean fact list with their slot labels humanised and the source span attribution, hiding the "Not stated" boilerplate behind a single honest line ("N of M record fields not stated by the source"), (2) key dates and in-force status when present, (3) "Connected items" (edges with basis), "Upcoming obligations / forward events", and tags, each section rendered only when it has rows, with an honest empty state otherwise, (4) the existing banner reworded to say what a catalogue record is and that a full brief is a separate upgrade; keep the Exposure/Penalty/Timeline tabs behaving as they do today for record items unless they are trivially fed by the same data. Server-render everything from the same data path the page already uses; do not add client fetches; keep the payload small (no full_brief re-parse on the client). Tests: component tests for the record Summary with fixtures (facts present, facts absent, edges present/absent), the rendering-guard smoke spec if one covers the detail page, tsc clean, existing tests green.`

const r = await agent(CLASSIFY, { label: 'CLASSIFY-STEP', phase: 'Lanes', model: 'haiku' })
return { classifystep: r }
