export const meta = {
  name: 'lane-sweepbudget',
  description: 'Sonnet lane SWEEP-BUDGET: the sitemap walker gets a wall-clock budget so a dispatch never dies at the job timeout with no artifact (run #16 at 70 hosts and #17 at 40 both exceeded 30 min); per-fetch timeouts confirmed; the artifact is written on budget exhaustion with the hosts remaining',
  phases: [{ title: 'Lane' }],
}
phase('Lane')
const P = `You are lane SWEEP-BUDGET on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/sweepbudget (branch lane/sweepbudget-2026-09-04, based on train/wave44-2026-09-04); node_modules symlinked at fsi-app/node_modules; Node 24 at /home/claude/.npm/_npx/387698761821791d/node_modules/node/bin/node. Cannot push; coordinator lands. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. CLAUDE.md rules: no schedules; no copies of logic; no dead code; "there is NO small follow-up fix, fix it now"; [CONFIRMED]/[HYPOTHESIS]; read every file you touch IN FULL; commit message ends with "Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>".

THE DEFECT [CONFIRMED by the coordinator, GitHub Actions 2026-09-04]: Source sweep #14 (all_hosts, max_hosts 40) took 13m24s and #15 14m57s; #16 (max_hosts 70) was killed by the job's timeout-minutes: 30 with NO artifact; #17 (max_hosts 40, the next 40 thinnest-covered hosts, started 20:11 UTC) was still running at 34 minutes and will die the same way. The per-host cost arithmetic in run-source-sweep.mjs's DEFAULT_MAX_HOSTS comment (~35 s/host) is an average; the tail (sitemap index fan-out, slow hosts, feed probing) is unbounded, and the walk loop (for (const src of targets) in fsi-app/scripts/turns/run-source-sweep.mjs, ~line 960) has no wall-clock check, so a run either finishes or loses its artifact and its harness-run record entirely (apply mode's per-source coverage stamps and candidate upserts DO persist per source — verify by reading — so a killed run leaves the DB correct but the repo blind).

READ FIRST, IN FULL: fsi-app/scripts/turns/run-source-sweep.mjs, fsi-app/src/lib/sources/sitemap-walk.mjs (its fetch helpers: is there a per-request timeout? an AbortSignal? a max bytes? cite line numbers), fsi-app/scripts/turns/run-source-sweep.test.mjs and sitemap-walk.test.mjs, .github/workflows/source-sweep.yml, docs/runbooks/CORPUS-TURN-RUNBOOK.md ("The sitemap walker" section), fsi-app/scripts/harness-runs/source-sweep/PROTOCOL.md and CONVENTION.md, fsi-app/scripts/harness-runs/governing-files.mjs (run-source-sweep.mjs and sitemap-walk.mjs are source-sweep GOVERNING files: chain and re-pin fsi-app/scripts/harness-runs/source-sweep/PENDING-RUN.md or LAST-PROPOSER-PASS.md per the F28 convention — read how LEDGER-WALLS just did it for ledger-consume in git log -1 -p -- fsi-app/scripts/harness-runs/ledger-consume/PENDING-RUN.md), and the last two source-sweep artifacts (source-sweep-run-013.json, -014.json) for the metrics shape.

BUILD: (1) a wall-clock budget for the all_hosts/host/source_id sitemap walk: --time-budget-seconds (default derived from the SAME arithmetic the DEFAULT_MAX_HOSTS comment already states: 1800 s job timeout minus the 300 s reserve = 1500 s; the default lives in ONE exported constant next to DEFAULT_MAX_HOSTS and the comment is updated to say the budget, not max_hosts, is now the hard stop); the loop checks elapsed BEFORE each source and stops when the budget is spent, recording on the result: budget_seconds, elapsed_seconds, sources_walked, sources_not_reached (count and ids), budget_exhausted: true, and hosts_remaining_unwalked recomputed from what was actually stamped. The check is a PURE exported predicate with an injected clock (testable without sleeping). (2) per-fetch timeouts: if sitemap-walk.mjs's fetchBytes/fetchText path has no AbortSignal timeout, add one (one constant, e.g. 20 s, in the fetch helper used by every sitemap/feed/homepage fetch — not three copies) so a single hung host cannot eat the budget; if it already has one, cite it and leave it. (3) the workflow input time_budget_seconds (optional; blank = default) threaded to the CLI flag, yaml validates; keep timeout-minutes: 30 as the outer guard. (4) the artifact: on budget exhaustion the run still exits 0 (a bounded, complete unit of work, not an error) and the artifact's metrics carry the fields above; the per_item verdicts for unreached sources are NOT fabricated (they simply are not in per_item; the count is in metrics). (5) runbook: the sitemap walker section states the budget, how many dispatches remain at the measured pace, and that max_hosts is now a ceiling, not the stop. (6) F28 marker re-pin for the source-sweep family (governing files changed).

Tests: node --test fsi-app/scripts/turns/run-source-sweep.test.mjs fsi-app/src/lib/sources/sitemap-walk.test.mjs (new tests for the predicate, the loop's early exit with a fake clock, and the fetch timeout); python3 -c "import yaml; yaml.safe_load(open('.github/workflows/source-sweep.yml'))"; from the repo root: node fsi-app/.discipline/fitness/runner.mjs (0 new violations; F28 green after the re-pin; list any pre-existing) and node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs; bash fsi-app/.discipline/run-test-suite.sh.

REPORT: the fetch-timeout finding with line numbers; the predicate's signature and where the loop calls it; the new result/metrics fields; the workflow input; the marker hash before/after; every gate's output line; the commit hash; the exact next dispatch inputs the coordinator should use (walker sitemap, all_hosts true, max_hosts 40, mode apply, time_budget_seconds blank).`
return { sweep: await agent(P, { label: 'SWEEP-BUDGET', phase: 'Lane', model: 'sonnet' }) }
