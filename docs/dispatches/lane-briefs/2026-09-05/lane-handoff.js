export const meta = {
  name: 'lane-handoff',
  description: 'Sonnet lane HANDOFF: write the session handoff document for the next coordinator session, verifying every claim in the coordinator state dump against the repo, the lane branches and the train 47 worktree; commit it with the lane briefs, the proposed rulings, an INDEX line, a board pointer and session-log Addendum 86',
  phases: [{ title: 'Handoff' }],
}
phase('Handoff')
const P = `You are lane HANDOFF on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/handoff, branch handoff/2026-09-05, based on origin/master 012b10a2 (train 46). node_modules symlinked at fsi-app/node_modules; Node 24 at /home/claude/.npm/_npx/387698761821791d/node_modules/node/bin/node (dirname on PATH). Cannot push; the coordinator lands. Main clone /root/work/dotfiles is READ-ONLY for you (git show/log/diff against its refs; never checkout/commit there). Sibling worktree /root/work/lanes/train47 (branch train/wave47-2026-09-05, tip 0fc9e5c5 or later) is READ-ONLY for you too: read its files and log, touch nothing. CLAUDE.md rules bind: [CONFIRMED]/[HYPOTHESIS] on every claim; read every file you cite; no em dashes where a comma is correct; commit message ends with "Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>".

PURPOSE. The operator is ending this coordinator session and starting a new one. A new session gets a NEW container: everything under /root/work and /home/claude is gone; only what is in git on origin survives. The next coordinator loads memory through the ledger skill (CLAUDE.md, docs/INDEX.md board section, docs/PROGRAM-BOARD.md first 150 lines, docs/ops/session-log.md last addendum, git log). Your document must let that session resume with zero chat archaeology: what we are building, how, where every piece of information lives, what is in flight, what is next, and what must never be repeated. The operator's words: "Absolutely everything in the most detailed way possible to describe what we are doing how we are doing it where to find the information going forward." And: "we're supposed to be using sonnet and haiku agents to do the actual work" (the coordinator coordinates; lanes build).

INPUTS, all already in this worktree (uncommitted):
- docs/ops/COORDINATOR-STATE-2026-09-05.md: the coordinator's raw state dump from its own context. Treat every line as a claim to verify, not as truth. Where a claim verifies against the repo, keep it with [CONFIRMED]; where it does not, keep the corrected fact and note the discrepancy in a "Corrections to the coordinator's dump" section; where it cannot be verified from the repo (e.g. what the operator said in chat), keep it labelled [operator ruling, chat] or [HYPOTHESIS]. After you have absorbed it, DELETE this file from the worktree (git rm is not needed, it was never committed): its content lives on in your document.
- docs/dispatches/lane-briefs/2026-09-05/*.js|*.mjs (91 files): the Workflow-tool lane briefs the coordinator wrote this build (wave-f-common.mjs is the shared preamble). Keep them committed as the record of how lanes are briefed; write a README.md in that folder that explains the format (export meta, phase(), agent(prompt, {label, phase, model}), max 2 agents per workflow, worktree per lane, node_modules symlink, gates, REPORT contract) and indexes the files by lane name and purpose (one line each, derived from each file's meta.description).
- docs/ratifications/2026-09/proposed/: the four review-queue *.ruling.json with Haiku-proposed decisions (decision still null pending the operator) and RULINGS-1/2 summaries. Keep them committed; add a README.md stating they are PROPOSED, not ruled, and how they get applied (scripts/review/apply-<queue>.mjs via maintenance steps, per fsi-app/scripts/review/ headers, which you read).

READ, in this order, before writing: CLAUDE.md; docs/INDEX.md; docs/PROGRAM-BOARD.md (first 200 lines and any train 46/47 rows); docs/ops/session-log.md Addendum 85 in full including postscripts 55-58 (postscript 58 is on the train47 branch: git -C /root/work/lanes/train47 show HEAD:docs/ops/session-log.md, tail); docs/plans/complete-system-build-plan-2026-09-04.md in full; docs/dispatches/lane-common-contract.md; docs/runbooks/TRAIN-ASSEMBLY-RUNBOOK.md (train47 branch); docs/runbooks/MAINTENANCE-RUNBOOK.md and PROPOSER-RUNBOOK.md; fsi-app/scripts/harness-runs/governing-files.mjs; fsi-app/.discipline/fitness/functions/F25-module-liveness.mjs (latestTrainWave and the W7.1 block, on train47); docs/inventories/migrations.md (train47); the eleven lane commit messages (git -C /root/work/dotfiles log --format=%B <ref> for each branch named in the dump) and ASSEMBLE-47's two commit messages (train47 HEAD~1, HEAD); .github/workflows/*.yml headers (which are workflow_dispatch, which chain via workflow_run, which steps each maintenance/producers workflow exposes); docs/ops/dispatch-ledger.jsonl if it exists on master or train47 (find it; the dump's path may be wrong, report the real path); the ledger skill and done skill text if present under fsi-app/.claude/skills or docs (grep -ril "find_vault" .).

WRITE docs/ops/handoff-2026-09-05.md, prose-first, sectioned, every path and hash verified. Required sections:
1. Read this first: the five-line resume (master hash and train, what is red and why, what is in flight, the single next step, the roles rule).
2. What Caro's Ledge is and what "the build" is: the loop from plan §1 in your own words, the six-part definition of done (§0), the T41-T46 sequence and where it stands, what "population paused" means and the exact condition for resuming (T46 validation, POPULATION_PAUSED variable).
3. How work is done: roles (operator rules; coordinator briefs, launches, lands, applies migrations, dispatches, records; Sonnet builds; Haiku does bulk text; the coordinator never edits code/docs itself), the lane lifecycle (brief from the plan → worktree from origin/master → lane commits → coordinator lands via the browser transport → proposer pass per artifact → dispatch ledger row → board/session-log in the same train), the Workflow-tool mechanics, the memory discipline (ledger/done skills, addendum + postscript numbering: next addendum is 86, next postscript 59), and the operator's binding rulings as a numbered list (from the dump's §2, verified against CLAUDE.md where they are codified; mark the rest [operator ruling, chat]).
4. The browser transport, step by step, exactly as the dump §6 describes, plus the Codespace name, the tab titles, the one-line landing command, the fast-forward variant, the CI-log reading recipe, the "Run workflow" popover recipe, and the known failure modes (lost first keystroke, stopped codespace, frozen renderer screenshot timeouts: wait 5 s and retry).
5. Where everything lives: a table of every information source (plan, audits, board, session-log, dispatch ledger, harness-run artifacts and F28 markers, inventories, runbooks, ADRs, skills, workflows, lane briefs folder, proposed rulings folder, migrations folder, discipline gates) with path and what question it answers.
6. State of the repo at handoff: master hash; train 47 branch (tip, PR if the coordinator has pushed it by the time you read /root/work/lanes/train47's log, else "landing in progress; verify with git log origin/master -1"); every unlanded lane branch with hash and what it carries (from the dump §4, verified in the main clone); migrations 308-311 written-not-applied with apply order and post-check SQL (from the files on train47); the master CI red state and its root cause; the ASSEMBLE-47 re-grant to wave52 as an explicitly disclosed non-fix.
7. The next session's ordered worklist, each item as a lane dispatch (which agent model, which brief file or a new brief, which worktree, what evidence closes it): (1) verify train 47 landed and 308-311 applied, else do those; (2) lane W7.1-CLOSE (docs/dispatches/lane-briefs/2026-09-05/lane-w71close.js, adjust the worktree/branch lines to a fresh worktree from the new master), with the three classes A/B/C; (3) prune dead artifact branches via assemble-train --prune from the Codespace; (4) the plan-named dispatches from the dump §9 in order with the exact inputs and the artifact each leaves, plus proposer passes and ledger rows; (5) the operator rulings still open (dump §10) and how each is applied once ruled; (6) the Haiku browser worklists (statutory FuelEU inputs, spec09 rows-files, attach-found-sources 443 orphans); (7) the single-dispatch end-to-end chained dry build (which workflow starts it, which workflow_run chain follows, what artifacts prove each hop) and then the T46 validation lanes; (8) population resume only after T46 and the operator's explicit go. No source sweeps.
8. Errors and lessons (dump §11, each as "what happened → the rule now").
9. Corrections to the coordinator's dump (everything that did not verify).
10. Open questions for the operator, verbatim from the dump §10 plus anything you found.

ALSO: add a docs/INDEX.md line for the handoff in the ops section (match the section's line format) and lines for the two new folders; add at the TOP of docs/PROGRAM-BOARD.md (below the title block, above §0) a three-line "Resume from" pointer naming the handoff document and its date; append "## Addendum 86 — 2026-09-05 (cloud session): handoff" to docs/ops/session-log.md, first person as the coordinator, stating that the session is ending, what landed (train 46 hash, train 47 status), what the handoff document is and where it is, and the single next step. Keep the addendum short; the handoff document carries the detail.

GATES from the repo root (docs-only change, but the memory gate and consistency layer still run): node fsi-app/.discipline/consistency/override-check.mjs --range=origin/master..HEAD (C3 clean); node fsi-app/.discipline/fitness/runner.mjs (report the count; on this base you will see the wave46 F25/F38 expiries, that is master's own red state, not yours, say so); node fsi-app/.discipline/governance/closure-gate.mjs. Do not run the full test suite. One commit.

REPORT: the sections written with line counts, every discrepancy found between the dump and the repo, the real path of the dispatch ledger, the commit hash.`
return { h: await agent(P, { label: 'HANDOFF', phase: 'Handoff', model: 'sonnet' }) }
