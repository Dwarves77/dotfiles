export const meta = {
  name: 'lane-retext3',
  description: 'Sonnet lane RETEXT-COLLIDE: forward-events-retext apply #35 died on uq_item_forward_events_dedupe at the first row; rows that become byte-identical under the index after the rewrite must be collapsed (delete the derived duplicate, snapshotted, reversible) before the update, never silently',
  phases: [{ title: 'Lane', detail: 'RETEXT-COLLIDE in its own worktree' }],
}
phase('Lane')
const P = `You are lane RETEXT-COLLIDE on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/retext3 (branch lane/retext3-2026-09-04, based on origin/master 3650f663); node_modules symlinked at fsi-app/node_modules. Cannot push; coordinator lands. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. Write set: fsi-app/scripts/maintenance/forward-events-retext.mjs (+ its test), fsi-app/docs/inventories/shared-dataset-ownership.md ONLY if a delete on item_forward_events needs its own registry line (read how the allowlist and the narrative describe write paths), docs/runbooks/MAINTENANCE-RUNBOOK.md §12 text. Nothing else; the extractor is not in scope. Deterministic, $0, no LLM; "there is NO small follow-up fix, fix it now"; no claims ahead of evidence, [CONFIRMED]/[HYPOTHESIS]; read every file you touch IN FULL (forward-events-retext.mjs, its test, scripts/lib/db.mjs's guardedDelete / DELETE_PROTECTED_TABLES / snapshot semantics, migration 274/275 for item_forward_events and the index); commit message ends with "Co-Authored-By: Claude Sonnet <noreply@anthropic.com>". Gates before commit: node --test fsi-app/scripts/maintenance/forward-events-retext.test.mjs; from repo root node fsi-app/.discipline/fitness/runner.mjs (0 violations) and node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs (141 pass, includes shared-writer-registry). The Supabase MCP (server "Supabase", tool execute_sql, project kwrsbpiseruzbfwjpvsp; load via ToolSearch "select:mcp__Supabase__execute_sql") is available for READ-ONLY SELECT; never write through it.

THE EVIDENCE [CONFIRMED]: Maintenance #35 (run 33864089323, forward-events-retext APPLY, master e1a0287 = FWD-TEXT-2's normaliser) failed 6 s into the step: "db.mjs update failed: duplicate key value violates unique constraint uq_item_forward_events_dedupe". The live index [read via pg_indexes]: CREATE UNIQUE INDEX uq_item_forward_events_dedupe ON public.item_forward_events USING btree (intelligence_item_id, event_date, event_kind, md5(obligation_text), COALESCE(source_claim_id, source_section_id)). The dry run #33 (run 33863637329) planned 916 rewrites; grouping its retext_targets by (item, date, kind, after) — without the source column, so an over-count — gives ~154 groups / 324 rows that become identical. Example: one section sentence "Targets will ensure that the greenhouse gas intensity of fuels used in the sector will gra…" produced two rows for (2025-01-01, other) from the same source because the year appears twice in the sentence; after the rewrite both carry the whole sentence. Some rows may already have been rewritten by #35 before it died (per-row guarded updates), so the step must be idempotent over a half-applied table. The coordinator's ruling: a derived row that the table's own unique index forbids once the text is honest is deleted (item_forward_events is derived from claims/sections by the extractor and regenerable; the deletion is snapshotted through db.mjs's guardedDelete so it is reversible), the survivor is rewritten; nothing else is deleted, and the previously reported duplicate_groups (same date+kind, different text) stay REPORT ONLY as before.

DO: (1) Read the live table shape and confirm item_forward_events is not in DELETE_PROTECTED_TABLES; count with read-only SQL how many rows already carry the new normalised form (idempotence baseline) — e.g. rows whose obligation_text starts with "…" or that FWD-TEXT-2's classifyAfterResidue would call clean vs the dry-run before texts; report the number. (2) In the plan phase, compute for EVERY row of the table (not only targets) its post-rewrite key (intelligence_item_id, event_date, event_kind, md5(after_text), coalesce(source_claim_id, source_section_id)) where after_text = normalised text for targets and the current text for non-targets; group; for each group with more than one row choose the survivor deterministically (the row with the earliest created_at, then lowest id; if one row is already in the normalised form prefer it), and list the others as collide_delete with their full row JSON in the summary (summary.collisions: groups, survivors, deletions, and a restore note pointing at the guarded snapshot file the delete writes). Compute md5 the way Postgres does (md5 of the UTF-8 bytes, lowercase hex; node:crypto). (3) Apply order: guardedDelete the collide rows first (chunked, cite, snapshot), then the rewrites; make the rewrite loop tolerant of a row that no longer exists (already deleted or already rewritten) and count it as no_op, never as a failure; read back: zero rows left whose key collides, every target's text equals the planned after text. (4) Dry mode reports the collisions and deletes nothing; the existing --arg restore:<ids> path must also be able to restore a deleted row from the snapshot (read what the snapshot entry for a delete carries; if guardedDelete's snapshot has the full prior row, restore = guardedInsert of that row with the same id; add it, tested). (5) Tests with fixtures shaped like the real rows (two rows same source/date/kind whose after texts coincide; one target colliding with an untouched non-target row; a half-applied table). Report: baseline counts, the collision count from the live table under the real key, files, exported functions, tests, fitness line, commit hash, anything refused with the reason.`
const r = await agent(P, { label: 'RETEXT-COLLIDE', phase: 'Lane', model: 'sonnet' })
return { retext3: r }
