export const meta = {
  name: 'lane-sitemap',
  description: 'Sonnet lane SITEMAP: feed-first discovery + sitemap snapshot-and-diff walker for source-sweep, ported from the MIT control-center reference where its logic is better, wired to the census ledger and change-sweep',
  phases: [{ title: 'Lane', detail: 'SITEMAP in its own worktree' }],
}
phase('Lane')
const P = `You are lane SITEMAP on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/sitemap (branch lane/sitemap-2026-09-04, now reset to origin/master 1356b381); node_modules symlinked at fsi-app/node_modules. Cannot push; coordinator lands. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. Write set ONLY: fsi-app/src/lib/sources/sitemap-walk.mjs (+ .test.mjs; both already exist UNTRACKED in your worktree as a previous agent's partial work, read them first and keep what survives the comparison below), NEW fsi-app/src/lib/sources/feed-discovery.mjs (+ test) if the feed-locating logic does not already live in feed-walk.mjs, fsi-app/src/lib/sources/feed-walk.mjs ONLY to call feed discovery (read it in full first), fsi-app/scripts/turns/run-source-sweep.mjs (add the walker; read it in full first), .github/workflows/source-sweep.yml (walker option + inputs), fsi-app/scripts/harness-runs/source-sweep/PROTOCOL.md (one section), fsi-app/docs/inventories/shared-dataset-ownership.md if you add a writer, fsi-app/.discipline/fitness/functions/F25-module-liveness.mjs allowlist ONLY if a new module needs an entry, and a NEW fsi-app/THIRD-PARTY-NOTICES.md (or append to it if it exists) for the MIT attribution. Six other lanes are running on population-turn.yml, record-facts.mjs, validate-mint-payload.mjs, export-census-rows.mjs, maintenance.yml, scripts/maintenance/**, the regulations detail page, and src/lib/data.ts: do not touch any of those. Deterministic, $0, no LLM, no Browserless: a sitemap or feed is a plain GET through the repo's own polite fetch (assertFetchAllowed / makePoliteFetch, read fsi-app/src/lib/sources/fetch-hold.mjs); build mode holds the scrape cadence OFF (CLAUDE.md standing rule 16), so this walker is dispatch-only like every other walker and you never add a schedule. CLAUDE.md standing rule 17 (operator, verbatim): "there is no thing within this entire build that works on its own ever. Everything works in tandem." A discovered URL is worthless unless it lands in the census ledger the population turn mints from, and a detected change is worthless unless it lands where change-sweep reads it. Commit message ends with "Co-Authored-By: Claude Sonnet <noreply@anthropic.com>"; no claims ahead of evidence, [CONFIRMED]/[HYPOTHESIS].

OPERATOR RULING [CONFIRMED, 2026-09-04]: "major sites with news that we need on new tech and advancements from places like BYD will have RSS feeds and if this person already did the coding and it's on github I want you to find it and use that code if it's good … if its other code is better than ours use it or adjust ours to match … it must be wired and working not just built and fit into the existing system seamlessly." The reference is vendored READ-ONLY at /root/work/vendor/control-center (mreflow/control-center, MIT, (c) 2026 Matt Wolfe): lib/sitemap.ts (parseSitemap, walkSitemapRoots, walkSitemap with DEFAULT_SITEMAP_DOCUMENT_LIMIT 500 / DEFAULT_SITEMAP_ENTRY_LIMIT 100000 / SITEMAP_MAX_RESPONSE_BYTES, readBoundedResponseText, sourceContentPath + isUrlWithinSourcePath + filterSitemapEntriesForSource for source-path scoping, newSitemapEntries + nextSitemapSnapshotUrls for the snapshot diff, observeUndatedFeedStories for undated feed items, sitemapCoverageMessage), lib/feed-discovery.ts (isFeedDocument, discoveredFeedLinks from <link rel=alternate>), and lib/server/rss.ts readSource (the discovery ORDER: source URL is itself a feed document → <link rel=alternate> + common feed paths → robots.txt Sitemap: lines → standard sitemap candidates → recursive index walk; baseline snapshot deferred until the walk reached full coverage; entries scoped to the source's path). Read all three files in full. For each behaviour, compare the vendored logic with the untracked sitemap-walk.mjs draft and with feed-walk.mjs, and keep the BETTER one: port control-center's where it is more complete (bounded response bytes, document/entry limits, path scoping, deferred baseline on partial coverage, undated-item observation, feed discovery before sitemap), keep ours where it is (polite fetch through fetch-hold.mjs, gzip .xml.gz decode, single/double-quote and namespace tolerance, census-writer.mjs ledger contract, snapshot-store.mjs rule-015 reversible snapshots, dry/apply artifact conventions). Port as ES modules (.mjs, dependency-injected fetch, no TypeScript, no Node-only fs in the pure module), with a header comment "Derived from mreflow/control-center lib/sitemap.ts and lib/feed-discovery.ts (MIT, (c) 2026 Matt Wolfe); see THIRD-PARTY-NOTICES.md" and the MIT text in THIRD-PARTY-NOTICES.md. Do not copy the Next.js/server/file-store parts; state in the report exactly which functions were ported, which were kept ours and why.

READ IN FULL before building: run-source-sweep.mjs (walker contract, dry/apply, artifact shape, how feed/register walkers are dispatched and how they call writeCensusRows), feed-walk.mjs and register-walk.mjs (pure, injected transport shape), census-writer.mjs (ledger contract: fields, dedup by canonical key, held reasons), snapshot-store.mjs (how captures/snapshots are stored and compared), change-sweep.mjs (what a "source change" input looks like so lastmod deltas feed it), source-sweep.yml, PROTOCOL.md and LAST-PROPOSER-PASS.md, the sources table columns (Supabase MCP read-only via ToolSearch, server "Supabase", project kwrsbpiseruzbfwjpvsp: select column_name from information_schema.columns where table_name='sources'; look for access_method, url, feed_url, status, last_checked, last_content_hash) and F25's allowlist rules.

BUILD: (1) discovery per source in control-center's order, feeds FIRST: if the source URL is a feed document, or a <link rel=alternate type=rss/atom> or common feed path (/feed, /rss, /rss.xml, /atom.xml, /feed.xml, /index.xml) resolves to a feed, hand the source to the existing feed walker path (record feed_url on the artifact; if the sources table has a column for it, say which and hand the write to the existing writer, never a new one); only when no feed exists, sitemap: robots.txt Sitemap: lines → /sitemap.xml, /sitemap_index.xml, /sitemap-index.xml → recursive index walk with document and entry limits and bounded response bytes. (2) Scope entries to the source's path (isUrlWithinSourcePath) so a regulator's whole domain is not swallowed by one source row. (3) Snapshot the scoped URL set (loc + lastmod) through snapshot-store.mjs's convention; if coverage was partial (limits hit or a child sitemap failed), report it and do NOT write a baseline that would make every later URL look new (control-center's deferred-baseline rule). (4) Diff: new locs → census candidates via writeCensusRows with the same fields the feed walker supplies (title unknown → the capture step fills it; screen verdict left to the screen); changed lastmod on a loc matching an existing item's canonical URL → a change record in the shape change-sweep.mjs consumes (if it wants a monitoring_queue row, hand off to the existing writer, never invent one); removed locs → reported only. (5) Scope flags: --source-id, --host, --limit; dry reports the plan with counts (feed found?, sitemaps found, urls, scoped, new, changed, removed, coverage) and writes only the artifact; apply writes census rows + snapshot (+ feed_url if a writer exists). (6) Wire as walker "sitemap" in run-source-sweep.mjs and source-sweep.yml with the family's artifact metrics plus the sitemap counts. (7) PROTOCOL.md section. Tests: discovery order (feed wins over sitemap), robots parsing, index fan-out and entry limits, bounded bytes, path scoping, urlset parsing (both quote styles, namespaces, lastmod formats, gz), snapshot diff (new/changed/removed), deferred baseline on partial coverage, census row shape, change record shape, dry writes nothing. Then repo root: node fsi-app/.discipline/fitness/runner.mjs (0 violations; if F28 names source-sweep's governing files, write fsi-app/scripts/harness-runs/source-sweep/PENDING-RUN.md in the format of fsi-app/scripts/harness-runs/change-detection/PENDING-RUN.md with the hash it prints) and node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs. Report: functions ported vs kept (with reasons), files, exported functions, the exact dispatch inputs for a first dry run against one source you verified has a feed and one you verified has only a sitemap (a read-only fetch is allowed if the container proxy permits; if it refuses, say so and name the hosts from the sources table you would try first), tests, fitness, commit hash.`
const r = await agent(P, { label: 'SITEMAP', phase: 'Lane', model: 'sonnet' })
return { sitemap: r }
