export const meta = {
  name: 'lane-rdm4b',
  description: 'Sonnet lane RD-M4b: export-census-rows.mjs partitionExcludeHeld excludes census rows by raw source_url set membership, so once one bulletin series is live the other five siblings drop out at export time before checkM4 ever sees them; one shared instrument-identity predicate, both layers import it',
  phases: [{ title: 'Lane', detail: 'RD-M4b in the rdm4 worktree' }],
}
phase('Lane')
const P = `You are lane RD-M4b on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/rdm4 (branch lane/rdm4-2026-09-04, HEAD c744e586 = lane RD-M4's commit: read git show c744e586 in full first — it made checkM4's same-URL holder check series-aware with normalizeInstrumentIdentifier + sameInstrumentIdentity exported from fsi-app/scripts/mint/apply-mint-batch.mjs, buildItemsIndex.bySourceUrl an array per URL, and flagged fsi-app/scripts/mint/export-census-rows.mjs's partitionExcludeHeld as the same defect class one layer up); node_modules symlinked at fsi-app/node_modules. Cannot push; coordinator lands. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. Write set: fsi-app/scripts/mint/export-census-rows.mjs (+ test), fsi-app/scripts/mint/apply-mint-batch.mjs (+ test) only to MOVE the predicate, a NEW fsi-app/scripts/mint/lib/instrument-identity.mjs (+ test) holding normalizeInstrumentIdentifier and sameInstrumentIdentity so both layers import ONE body (CLAUDE.md: never two copies; apply-mint-batch.mjs re-exports if callers rely on it), fsi-app/scripts/mint/MINT-RUNBOOK.md (the export-step paragraph that describes the held/holder exclusion, if one exists; grep partitionExcludeHeld / "already minted" / "source_url" in it), fsi-app/scripts/harness-runs/mint/PENDING-RUN.md (chain a "What changed" entry onto RD-M4's and re-pin to the hash the fitness runner prints if MINT-RUNBOOK moved; export-census-rows.mjs itself is not a governing file: confirm against F28's list). Deterministic, $0; "there is NO small follow-up fix, fix it now"; no claims ahead of evidence; read every file you touch IN FULL; commit message ends with "Co-Authored-By: Claude Sonnet <noreply@anthropic.com>". Gates: node --test fsi-app/scripts/mint/*.test.mjs fsi-app/scripts/mint/lib/*.test.mjs (777 + yours); repo-root node fsi-app/.discipline/fitness/runner.mjs (0 violations) and node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs (141). Supabase MCP read-only SELECT available (server "Supabase", tool execute_sql, project kwrsbpiseruzbfwjpvsp; ToolSearch "select:mcp__Supabase__execute_sql").

DO: (1) Read partitionExcludeHeld and every exclusion in export-census-rows.mjs that keys on source_url or canonical key: which live rows does it read (intelligence_items? which columns?) and what does it exclude. Confirm with read-only SQL what the export would do to the five remaining bulletin census rows today (census_worklist rows with instrument_identifier LIKE 'eu-oil-bulletin:%' and dryrun_disposition; the live eurosuper-95 item 4fae403a at the bulletin URL): report whether they would be excluded under the current code. (2) Make the export's same-URL exclusion use the shared predicate: a census row whose source_url has a live holder is excluded only when sameInstrumentIdentity(row, holder) is true for some holder (the row's instrument_identifier vs each holder's; the null-vs-null and null-asymmetry rules exactly as RD-M4 defined them, one body); select instrument_identifier in the holders read. Keep every other exclusion unchanged. (3) Tests: the five-sibling census rows survive the export partition when eurosuper-95 is live; a true duplicate (same URL, same identifier) is still excluded; null/null excluded; the predicate module's own tests; a source-reading contract test that apply-mint-batch.mjs and export-census-rows.mjs both import the predicate from the lib (no local definition). Report: the live answer from (1), files, exported functions, tests, fitness line, marker hash, commit hash.`
const r = await agent(P, { label: 'RD-M4b', phase: 'Lane', model: 'sonnet' })
return { rdm4b: r }
