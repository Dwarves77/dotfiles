export const meta = {
  name: 'lane-perf9',
  description: 'Sonnet lane PERF-9: PERF-8 left the caching model (every route dynamic), the 4.25 s item-click RSC, and the five post-render API calls undone; this lane does exactly those three with ADR-026',
  phases: [{ title: 'Lane' }],
}
phase('Lane')
const P = `You are lane PERF-9 on Caro's Ledge (repo Dwarves77/dotfiles), the continuation of PERF-8. Worktree /root/work/lanes/perf9 (branch lane/perf9-2026-09-04, based on train/wave39-2026-09-04 which carries PERF-8's three commits 1ca75272 (timeZone pin, React #418), 624bbb2e (SSR payload trim on /regulations and /operations), bf7aea62 (F36 fitness): read all three in full first: git show <hash>); node_modules symlinked at fsi-app/node_modules (do NOT run npm install; if a package is missing say so). Cannot push; coordinator lands and measures live after deploy. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. Write set: fsi-app/src/app/** (routes, layouts, loading.tsx), fsi-app/src/components/**, fsi-app/src/lib/data.ts, fsi-app/src/lib/supabase-server.ts, fsi-app/src/lib/api/**, fsi-app/src/middleware.ts or proxy.ts, fsi-app/next.config.*, their tests, docs/decisions/ADR-026-<slug>.md (required for the caching model; read ADR-016..025 for format), the runtimes ONLY at the single point where an apply finishes (population-turn / maintenance drivers: a revalidateTag hook or an HTTP call to a revalidate route, if that is the design you choose), and the UX compliance block the discipline gate demands when customer surfaces change (grep -rn "UX compliance" fsi-app/.discipline docs/doctrine and follow it). READ FIRST, in full: PERF-8's report items 3/4/5 below, docs/ops/session-log.md Addendum 85 postscripts 1, 3, 5, 26, 27 (PERF..PERF-7), any docs/decisions or docs/doctrine text on caching, unstable_cache, ISR, personal state, src/lib/supabase-server.ts (buildWorkspaceItemsQuery, LISTINGS_RPCS_WITH_OWN_TOTAL_ORDER), src/app/regulations/[slug]/page.tsx, src/app/regulations/page.tsx, the five API routes named below, and PERF-8's own finding: next build shows EVERY route as ƒ (Dynamic) — the whole app is opted out of static/ISR. Rule: check whether an established pattern exists before proposing a new one; flag conflicts with prior decisions and explain the trade-off before changing them. No claims ahead of evidence, [CONFIRMED]/[HYPOTHESIS]; "there is NO small follow-up fix, fix it now"; commit message ends with "Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>". Gates: fsi-app/node_modules/.bin/tsc --noEmit -p fsi-app/tsconfig.json (0 errors), node --test over every test you touch and fsi-app/src/__tests__/*.mjs, repo-root node fsi-app/.discipline/fitness/runner.mjs (0 violations, 30 functions after F36) and node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs, the rendering smoke (fsi-app/.discipline/rendering, read how it runs; the Playwright leg may not run here, say so), and cd fsi-app && npx next build (PERF-8 got it to build; report which routes are now static/ISR ○ or ◐ vs ƒ; that table IS the evidence for item 3). Supabase MCP read-only SELECT is available (server "Supabase", tool execute_sql, project kwrsbpiseruzbfwjpvsp; ToolSearch "select:mcp__Supabase__execute_sql").

THE MEASUREMENTS [CONFIRMED by the coordinator, Chrome on carosledge.com, 2026-09-04 ~14:40 UTC, logged-in operator session]:
(a) GET /regulations: TTFB 137 ms, HTML 857 KB, DOMContentLoaded 5,297 ms, load 5,793 ms. Console: "Minified React error #418" (hydration mismatch, args HTML) thrown from the main chunk, followed by four "TypeError: Cannot read properties of null (reading 'parentNode') at $RS" (the streaming-SSR completion script) and "message channel closed" errors. Post-render fetches: /api/admin/attention 1,355 ms; /api/telemetry/error 3,364 ms (the page reported its own error); a 77 KB fetch whose URL the tool redacted (query-string data: likely a Supabase REST call with the key in the query, find it) 2,389 ms; /api/workspace/personal-state 1,620 ms; /api/user/list-order?list_key=regulations 2,629 ms.
(b) Client-side click list -> /regulations/eu-net-zero-industry-act-2024-1735: the RSC request for the item route took 4,254 ms (18 KB); URL changed at 576 ms; then /api/community/entities/cl:instrument:… 601 ms and /api/workspace/members 1,649 ms.
(c) Direct fetch of routes, all with cache-control "private, no-cache, no-store, max-age=0, must-revalidate" and x-vercel-cache MISS: item page 1,640 ms then 900 ms (188 KB); /market-intel 515 ms (23 KB); /operations 1,830 ms (392 KB); /research 1,631 ms (136 KB); /community 1,992 ms (54 KB); /regulations 1,129 ms (857 KB).
(d) The operator's law (2026-09-04, verbatim): "clicking into any item or any page takes WAY too long. multiple seconds. every click should show items on a page instantly."

PERF-8 LEFT THESE THREE UNDONE (its own words): "Item 3 (caching model / ADR-026): confirmed every route is force-dynamic; did not design or implement the public/cached vs per-user/client-fetched split, and wrote no ADR. Item 4 (navigation): [regulations]/[slug]/page.tsx already parallelizes its awaits and has loading.tsx (PERF/PERF-2/PERF-4, pre-existing); did not re-investigate why the RSC measured 4.25 s live vs 0.9-1.6 s direct-fetch, and did not audit <Link prefetch> usage. Item 5 (5 post-render API routes): not consolidated into a single bootstrap call; /api/telemetry/error still fires on the same path." YOUR JOB IS EXACTLY THESE THREE, each its own commit, no scope beyond them:

3 (item 3). The caching model. Everything is dynamic because something at the top reads cookies/headers (auth, workspace) which opts the whole route out of static/ISR. Design and implement the split: PUBLIC intelligence content (listings, item pages, bands, forward-events strip, series board) from a cached data layer (unstable_cache with tags + revalidateTag fired by the population/maintenance runtimes on apply, or route-segment ISR with a short revalidate; pick one and justify against ADR-023/ADR-025 and the PERF trains' decisions) and PER-USER state (personal-state, list-order, members, admin attention) fetched client-side after first paint, in ONE batched call instead of four, with the shell rendered without waiting for it. If the auth/workspace read must stay in the request path for access control, keep it in middleware (cheap, no DB) and make the page's data reads independent of it. Record the decision as ADR-026.
4 (item 4). Navigation: loading.tsx skeletons for the listing and item segments so a click paints instantly while the RSC streams; <Link prefetch> on item links in the viewport; explain why the RSC took 4.25 s when a direct fetch took 0.9-1.6 s (layout re-fetch? missing loading boundary? a serial await chain in src/app/regulations/[slug]/page.tsx: count the awaits and their dependencies; parallelise with Promise.all where independent) and fix it.
5 (item 5). The five post-render API routes: read what each does (auth + queries); collapse into one /api/workspace/bootstrap call or make them cacheable per user for the session; the telemetry/error post must not be on the critical path.
Report with numbers: for each of the three, what you found [CONFIRMED with file:line], what you changed, the next build route table before/after (○/◐/ƒ per route), and the expected effect on the four measured numbers (DCL on /regulations, HTML size, item-click RSC time, post-render API time); files; tests; fitness line; ADR-026; commit hashes; anything you refused with the reason. The coordinator re-measures live in Chrome after deploy and compares against your expectations.`
const r = await agent(P, { label: 'PERF-9', phase: 'Lane', model: 'sonnet' })
return { perf9: r }
