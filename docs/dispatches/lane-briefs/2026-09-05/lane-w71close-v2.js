export const meta = {
  name: 'lane-w71close-v2',
  description: 'Sonnet lane W7.1-CLOSE (v2, re-briefed on master c3003233 after train 47 landed): disposition every F25/F38/F14 allowlist entry that carries an expiry (all re-granted to wave52 by ASSEMBLE-47, a disclosed non-fix), close the closure-gate STALE-NEXT rows, retire inspect-oil-bulletin.yml; zero re-grants',
  phases: [{ title: 'Close' }],
}
phase('Close')
const P = `You are lane W7.1-CLOSE on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/w71close, branch lane/w71close-2026-09-05, based on origin/master c3003233 (= train 47, PR #594, merged 2026-09-05 19:55 UTC; migrations 308-311 are APPLIED live as of 20:00 UTC). node_modules symlinked at fsi-app/node_modules; Node 24 at /home/claude/.npm/_npx/387698761821791d/node_modules/node/bin/node (dirname FIRST on PATH). Cannot push; the coordinator lands. Main clone /root/work/dotfiles is read-only for you except read-only git commands (git show / git log on any ref, including the six audit branches named below). Sibling lanes are running concurrently: DOCS-FOLD (touches docs/ops/handoff-2026-09-05.md, docs/dispatches/lane-briefs/**, docs/audits/plan-completion-audit-2026-09-05/**, docs/inventories/migrations.md rows 308-311, docs/INDEX.md, and appends a session-log postscript). Your write set is everything else named below; you may append ONE postscript to docs/ops/session-log.md and edit PROGRAM-BOARD rows (the assembly lane merges both sides). CLAUDE.md rules bind: no copies of logic; no dead code; no schedules; $0; "there is NO small follow-up fix, fix it now"; [CONFIRMED]/[HYPOTHESIS]; read every file you touch IN FULL before editing; commit message ends with "Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>".

READ FIRST: CLAUDE.md; docs/plans/complete-system-build-plan-2026-09-04.md §0 and §W7; fsi-app/.discipline/fitness/functions/F25-module-liveness.mjs IN FULL (findDispatchRoots, latestTrainWave, the W7.1 allowlist block and its w/d/o helpers), F38-unbounded-supabase-read.mjs, F14-producer-consumer-orphan.mjs (TERMINAL_SINK_ALLOWLIST), fsi-app/.discipline/governance/closure-gate.mjs IN FULL (NEVER_RUN_ALLOWLIST, STALE_NEXT allowlist, WRITER_READER_ALLOWLIST, hasRunEvidence, readDispatchLedger, runbookHasRecord), docs/audits/wiring-audit-2026-09-04/B1-modules.md, fsi-app/.discipline/run-test-suite.sh, and the independent plan-completion audits written today (read them with git show from the main clone, they are not on your branch): git show audit/auditw567-2026-09-05:docs/audits/plan-completion-audit-2026-09-05/W5-W6-W7-surfaces-community-discipline.md ; git show audit/audittools-2026-09-05:docs/audits/plan-completion-audit-2026-09-05/tools-inventory-unused-duplicates.md ; git show audit/auditloop-2026-09-05:docs/audits/plan-completion-audit-2026-09-05/loop-harness-flywheel-one-unit.md ; git show audit/auditw12-2026-09-05:docs/audits/plan-completion-audit-2026-09-05/W1-W2-intake-population.md. Treat their claims as leads to verify, not evidence.

THE STATE [CONFIRMED by the coordinator on master c3003233, 2026-09-05 20:30 UTC]: node fsi-app/.discipline/fitness/runner.mjs reports 0 violations ONLY because ASSEMBLE-47 re-granted every expired F25 entry (32 entries with expiry 52 or 50, plus the F38 mint-gate-calibration .limit(8000) entry, expiry 52, plus F14's entity_scope entry) with a disclosure comment calling it "a standing, undischarged backlog". closure-gate.mjs is RED on master: NEVER-RUN 10 FAILING and STALE-NEXT 7 FAILING (expiry train 46, current train 47), so every PR from now on fails the Discipline engine until this lane lands. Master's CI was already red on F25/F38 for the whole of train 46; this is the second time the same mechanism has been re-granted instead of closed (41 -> 43 -> 46 -> 52).

FORBIDDEN: granting any entry a later expiry, or any new allowlist entry with an expiry. Every entry gets exactly one of: reachable by construction, wired to a real caller, or deleted. Three classes:

(A) REACHED BY A REGISTRY F25 DOES NOT READ: .discipline/governance/pretooluse-skill-gate.mjs, worktree-isolation-hook.mjs, check-pretooluse-wired.mjs, wire-pretooluse-settings.mjs, .discipline/consistency/runner.mjs, .discipline/install-hooks.mjs, .discipline/dispatch/start.mjs, dispatch/audit.mjs. Establish [CONFIRMED] for each what invokes it (grep .claude/settings.json, .claude/settings.local.json if tracked, fsi-app/.claude/**, .githooks/**, .husky/**, package.json scripts, the hook sources install-hooks.mjs writes, CLAUDE.md, every SKILL.md). For every one that IS invoked by a tracked registry, extend findDispatchRoots with ONE new source per registry kind ("Source N: .claude/settings*.json hook commands", "Source N+1: hook scripts written by install-hooks.mjs", "Source N+2: package.json scripts" if missing) so the whole class is reachable by construction, with a unit test per source in F25's test file; then delete their allowlist entries. Any of the eight that nothing tracked invokes is class (C).

(B) SPEC09: scripts/spec09/run-fixture-import.mjs and any other spec09 module still listed. Lanes SPEC09-A/B built the rows-file/CSV-upload mechanism (read fsi-app/scripts/spec09/SOURCES.md, the maintenance.yml spec09-* steps and their runbook sections). If a module is invoked by a workflow step or the CSV upload route, prove the chain and delete the entry; if not, wire it the same way (a maintenance.yml step selected by the existing step input, fixture-backed dry run in its test) or delete it with its test and inventory row.

(C) DATED ONE-SHOTS AND EVERYTHING ELSE, every remaining w/d/o entry: for each, grep the whole tree for importers and invocations (src, scripts, .github/workflows, docs runbooks, package.json, skills). A module with a LIVE importer that is itself reachable is not dead: fix the reachability chain (usually the importer is the real gap) and delete the entry. A module with no live importer is DELETED in this lane together with its test file(s), its inventory rows, its runbook/INDEX references, and any docs line that presents it as an available tool (a one-line "retired in train 48, see git history" note only where a runbook step would otherwise dangle). Keep git history as the archive; no _archive folder. src/lib/contracts/provenance-envelope.mjs is a contract module: if src/ consumes the same contract via another path, dedupe to one module; if nothing consumes it, delete it. scripts/seed-community-regional-rooms.mjs: the W6 ruling (user-started rooms on a regional spine) is OPEN with the operator; if nothing imports it and no workflow runs it, it is dead code under the current tree and is deleted (a future community lane rebuilds against the ruling); say so in your report. F38: mint-gate-calibration.mjs goes with its class; if it survives (only with a live caller), page it through fetchAllRows/exactCount and delete the allowlist entry. F14 entity_scope (TERMINAL_SINK_ALLOWLIST) and closure-gate WRITER_READER_ALLOWLIST entity_scope: two allowlists guarding one fact; give entity_scope its first reader as plan §W4.2 names it (read docs/specs for corridor scope and where the corridor/regulation detail surfaces would read corridor-to-jurisdiction rows; the smallest real reader that a surface renders counts, with a test), then delete BOTH entries; if the spec names no reader, drop the table and writer with a written migration (312, DDL-first pattern, md5 pre-check + post-check, the coordinator applies) and delete both entries. No third state.

(D) CLOSURE GATE, docs side: the 7 STALE-NEXT rows in docs/PROGRAM-BOARD.md (run node fsi-app/.discipline/governance/closure-gate.mjs --report for the exact lines: WO-17 reader, "Execute the ready four", WO-21/13, WO-23 CHECK-widening, "Nothing blocked. Optional: SERIES_ITEM_MAP", Addendum-84 corpus-turn NEXT, WO-26 stamp). For each: read the row and the code it names; mark it DONE / CLOSED / SUPERSEDED (pointing at the train or file that did the work) or DEFERRED with the explicit blocking ruling (rule 16 build-mode end) so it no longer reads as open work, and delete its STALE_NEXT allowlist entry. NEVER_RUN_ALLOWLIST: the coordinator is dispatching the maintenance steps today and will record docs/ops/dispatch-ledger.jsonl rows; do NOT touch those 9 maintenance entries (a follow-on commit removes them once the rows exist). The tenth, workflow:inspect-oil-bulletin.yml: retire it (delete .github/workflows/inspect-oil-bulletin.yml, its script if nothing else uses it, its runbook/inventory lines, and its allowlist entry; fetch-oil-bulletin.mjs inside producers.yml superseded it 2026-08-30 per the entry's own text: verify that claim by reading producers.yml before deleting).

(E) DUPLICATES the audits found in your area, verify and resolve: scripts/maintenance/reopen-validation-holds.mjs vs scripts/mint/reopen-validation-holds.mjs (diff them; keep the one maintenance.yml runs, delete the other with its test, fix imports); scripts/entities/backfill-lineage-edges.mjs vs backfill-derivation-edges.mjs (B1 Gap #5: one lineage backfill; delete the other); any other pair the tools-inventory audit lists as DUPLICATE that falls inside .discipline/** or scripts/** and is not another running lane's write set.

After deletions: node fsi-app/.discipline/governance/closure-gate.mjs, the F23 orphan-proof ratchet and coverage-report.json (regenerate with node fsi-app/.discipline/governance/coverage-scan.mjs), run-test-suite.sh's named lists, fsi-app/.discipline/governance/skill-contract-map.mjs citingFiles (a deleted citing file breaks skill-drift-gate; fix by regeneration path or by editing the map with a changelog comment, never by hand-editing counts elsewhere).

MEMORY: append ONE postscript to docs/ops/session-log.md under the newest addendum on your branch (first person, same format as the existing postscripts): root cause (two coordinator re-grants, then ASSEMBLE-47's third), the disposition table (file -> A/B/C/D/E -> wired-by-registry / wired-to-step / deleted / closed), and the rule now in force: an allowlist entry is granted once, with a named lane, never re-granted. Update the docs/PROGRAM-BOARD.md W7.1 row to closed with the evidence. No .tsx should change; if one does (the entity_scope reader may), include a "UX compliance" block in the postscript (docs/design/ux-laws.md).

GATES from the worktree root, all clean before you report: node fsi-app/.discipline/fitness/runner.mjs (0 violations AND zero allowlist entries with an expiry field left in F25, F38 and F14: prove with grep); node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs (the 3 closure-gate LIVE tests may still fail ONLY on the 9 NEVER-RUN maintenance entries the coordinator owns; STALE-NEXT must be 0); node fsi-app/.discipline/governance/closure-gate.mjs --report (paste); bash fsi-app/.discipline/run-test-suite.sh with the node_modules symlink temporarily moved away and with it present; node fsi-app/.discipline/consistency/override-check.mjs --range=origin/master..HEAD (C3 clean; ignore C4 worktree rows); cd fsi-app && npx tsc --noEmit; python3 -c "import yaml,sys; [yaml.safe_load(open(f)) for f in sys.argv[1:]]" on every workflow touched; node fsi-app/.discipline/governance/invariant-coverage.mjs; cd fsi-app && npx next build --webpack with no Supabase env (exit 0).

REPORT: the disposition table, the F25 sources added, files deleted (count and list), migrations written (number, post-check SQL) if any, every gate's output line, build exit code, commit hash(es), anything you could not disposition and why (there should be none except the 9 NEVER-RUN entries the coordinator owns).`
return { w: await agent(P, { label: 'W7.1-CLOSE', phase: 'Close', model: 'sonnet' }) }
