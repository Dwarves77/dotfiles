export const meta = {
  name: 'lane-slimorder',
  description: 'Haiku lane SLIM-ORDER: give get_workspace_intelligence_slim its id tiebreak (migration 303, written not applied) and let /operations and /market keep the RPC order the way FIRSTPAGE did for /regulations; measure the live first page of both first',
  phases: [{ title: 'Lane', detail: 'SLIM-ORDER on top of FIRSTPAGE' }],
}
phase('Lane')
const P = `You are lane SLIM-ORDER on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/slimorder (branch lane/slimorder-2026-09-04 = origin/master 697be18e + lane FIRSTPAGE's commit 5dac5a94, read that commit with git show first: it is the pattern you extend); node_modules symlinked at fsi-app/node_modules. Cannot push; coordinator lands. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. Write set: ONE new migration fsi-app/supabase/migrations/303_slim_listings_id_tiebreak.sql (written, NOT applied; additive; idempotent; the exact self-check SQL in its header; a row INSIDE the table of fsi-app/docs/inventories/migrations.md in the same form as the 302 row), fsi-app/src/lib/supabase-server.ts (only the LISTINGS_RPCS_WITH_OWN_TOTAL_ORDER allowlist and its comment), fsi-app/src/lib/supabase-server-listings-order.npmtest.mjs (extend), docs/audits/perf-load-times-2026-09-03.md (append §15). Nothing else. Deterministic; "there is NO small follow-up fix, fix it now"; no claims ahead of evidence, [CONFIRMED]/[HYPOTHESIS]; read every file you touch IN FULL; commit message ends with "Co-Authored-By: Claude Haiku <noreply@anthropic.com>". Gates before commit: fsi-app/node_modules/.bin/tsc --noEmit -p fsi-app/tsconfig.json (0 errors in your files; 19 pre-existing TS2882 CSS-import errors under npx are documented, use the node_modules binary from the repo root); node --test fsi-app/src/lib/supabase-server-listings-order.npmtest.mjs; from repo root node fsi-app/.discipline/fitness/runner.mjs (0 violations) and node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs (141 pass); the C3 consistency layer requires the migrations inventory row to sit inside the table. The Supabase MCP (server "Supabase", tool execute_sql, project kwrsbpiseruzbfwjpvsp; load via ToolSearch "select:mcp__Supabase__execute_sql") is available for READ-ONLY SELECT; never write through it, never apply the migration.

THE EVIDENCE [CONFIRMED by lane FIRSTPAGE, docs/audits/perf-load-times-2026-09-03.md §14.4, read it in full]: fetchWorkspaceResources's pagination chained an outer PostgREST .order("added_date", desc).order("id", asc) onto the RPC call, which REPLACES the RPC's own ORDER BY (CASE effective_priority … END, added_date DESC, id ASC), so /regulations' first 60 rows were 100% MODERATE and none of the 13 CRITICAL / 30 HIGH. FIRSTPAGE fixed it for get_workspace_intelligence_listings only, by dropping the outer order for that RPC name (buildWorkspaceItemsQuery allowlist), because get_workspace_intelligence_slim, used by /operations and /market via fetchResourcesOnly, ends its ORDER BY with added_date DESC and NO id tiebreak: dropping the outer order there would reopen the page-boundary duplicate-row defect. FIRSTPAGE named the remedy and left it: migration adding ", ii.id ASC" (or whatever the alias is in the live body) to get_workspace_intelligence_slim's ORDER BY, then adding "get_workspace_intelligence_slim" to the allowlist.

DO: (1) MEASURE FIRST with read-only SQL: pg_get_functiondef of public.get_workspace_intelligence_slim (state its md5 and quote its ORDER BY verbatim); then reproduce the defect for /operations and /market the way §14.2 did for /regulations: run the RPC's body logic (or call the function with the parameters fetchResourcesOnly passes, read data.ts/supabase-server.ts for them) ordered the OLD outer way (added_date desc, id asc) and take the first page size the code uses, count by effective_priority; then the RPC's own order; state both distributions as [CONFIRMED] with the SQL. If the slim RPC's own order does not carry the CASE priority rank, say so and stop at the tiebreak only, do not invent an ordering. (2) Write migration 303 in the house style (read 302_criterion3_rating_not_refusal.sql for the idempotent in-place function-body patch technique with md5 guard and count-guarded anchor replace; if the slim function is small, CREATE OR REPLACE with the full body is acceptable, but the header must carry the pre md5, the self-check SELECT, and a rollback note). (3) Add "get_workspace_intelligence_slim" to LISTINGS_RPCS_WITH_OWN_TOTAL_ORDER with a comment that it is gated on migration 303 being applied live, and make the code safe BEFORE 303 is applied: read how the allowlist is consumed; if dropping the outer order before 303 lands would reopen duplicates, keep the allowlist entry behind the same check the repo uses for not-yet-applied migrations (grep src/lib for "migration" feature flags, e.g. how counts fall soft when the RPC is absent) or, if no such pattern exists, leave the allowlist change out, say so explicitly, and put the one-line code change verbatim in §15 for the coordinator to land together with the apply. (4) Extend the order test for the new name. Report: the measured distributions, the migration file with its self-check, the allowlist decision and why, tests, tsc/fitness lines, commit hash, anything refused with the reason.`
const r = await agent(P, { label: 'SLIM-ORDER', phase: 'Lane', model: 'haiku' })
return { slimorder: r }
