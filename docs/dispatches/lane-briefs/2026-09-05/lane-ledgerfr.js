export const meta = {
  name: 'lane-ledgerfr',
  description: 'Sonnet lane LEDGER-WALLS: the ledger fetcher marks bot-wall/CAPTCHA shells as fetch_ok (federalregister.gov: 230 of 338 exported candidates were a 1,180-char "Request Access" shell) and EUR-Lex legal-content pages come back as the interface shell; use the standard transports (the Federal Register JSON API, EUR-Lex HTML/CELLAR endpoints), detect walls with the one shared detector, never classify a wall',
  phases: [{ title: 'Lane' }],
}
phase('Lane')
const P = `You are lane LEDGER-WALLS on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/ledgerfr (branch lane/ledgerfr-2026-09-04, based on train/wave43-2026-09-04); node_modules symlinked (no npm install). Cannot push; coordinator lands. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. CLAUDE.md rules: one body (reuse, never a second detector or a second fetcher); no LLM/API spend in runtimes (the Federal Register API is a free public JSON API, not an LLM: allowed); no schedules; polite fetching; [CONFIRMED]/[HYPOTHESIS]; read every file you touch IN FULL; commit message ends with "Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>". Gates: node --test over every test you touch (run-ledger-consume.test.mjs is 78 today); tsc; repo-root node fsi-app/.discipline/fitness/runner.mjs (0 violations, 31 functions; run-ledger-consume.mjs is a ledger-consume GOVERNING file: chain a "What changed" entry onto fsi-app/scripts/harness-runs/ledger-consume/PENDING-RUN.md and re-pin to the hash the runner prints); discipline governance tests; the full suite bash fsi-app/.discipline/run-test-suite.sh. Supabase MCP read-only SELECT (server "Supabase", tool execute_sql, project kwrsbpiseruzbfwjpvsp).

THE FACTS [CONFIRMED by the coordinator from ledger-consume export #5, run 33908401816, 2026-09-04 19:20, the first export with extracted text after LEDGER-TEXT]: of 400 candidates, 338 cleared the 200-char floor and 62 did not; of the 338, ~230 are www.federalregister.gov document URLs whose "text" is the same 1,180-character shell: "Request Access … your request has been flagged as potentially automated … CAPTCHA" — a bot wall that the fetcher reported as fetch_ok:true because it is over 200 characters; the seven session-Haiku lanes correctly returned "uncertain" for every one of them, so 230 verdicts are wasted; and ~15 eur-lex.europa.eu legal-content URLs (e.g. https://eur-lex.europa.eu/legal-content/EN/TXT/?uri=OJ:L_202601727) came back as the EUR-Lex interface shell (menus, "Your browser is no longer fully supported", sign-in) with little or no document text, while 63 other EUR-Lex URLs returned the instrument text fine. The repo already has: a bot-wall detector in the sitemap walker (src/lib/sources/sitemap-walk.mjs, bot_wall outcome; read it), a transport ladder with host rules in src/lib/agent/canonical-pipeline.ts (direct fetch vs Browserless render for JS/bot-walled hosts, transport-escalation.selectTransportOrder), the Federal Register register walker (src/lib/sources/register-walk.mjs, feed/register discovery for federalregister.gov: read how it already talks to that site), and inaccessible-triage (scripts/sources/inaccessible-triage.mjs). The operator's standing rule: "these solutions have been developed already, you don't need to reinvent a fix, find out how others do it and do the same."

BUILD: (1) ONE bot-wall / access-wall detector, exported from wherever the sitemap walker's lives (or a new src/lib/sources/access-wall.mjs that the walker imports too: one body), recognising the Federal Register "Request Access"/CAPTCHA shell, Cloudflare/Akamai challenge pages, cookie-consent-only shells, login walls, "browser not supported" shells; unit tests with the real Federal Register shell text (paste it from the export: /root/work/ledger/export-002.json in this container has all 400 rows; read a federalregister.gov row's text) and the EUR-Lex interface shell; (2) run-ledger-consume.mjs buildFetchDoc: after extraction, a detected wall is fetch_ok:false with fetch_error "access_wall:<kind>" (never sent to classify, never counted as fetched; the export carries the same fields), and portal-harvest.ts's consume path treats it as inconclusive (row stays candidate) with the reason recorded; (3) the STANDARD transport for federalregister.gov documents: the Federal Register API (https://www.federalregister.gov/api/v1/documents/<document_number>.json returns title, abstract, type, agencies, publication_date, full_text_xml_url, body_html_url, raw_text_url; the document number is in the URL path /documents/YYYY/MM/DD/<document_number>/<slug>): fetchDoc routes federalregister.gov document URLs to the API's raw_text_url (or the JSON's abstract + body when the raw text is unavailable) through the same polite gap, and records transport "federalregister-api"; read register-walk.mjs first: if it already has an API client, reuse it; if the repo's transport ladder (canonical-pipeline.ts) is the right home for a per-host transport rule, put the rule there and have fetchDoc call it, one ladder; (4) EUR-Lex: find out why some legal-content URLs return the shell (the OJ: uri form vs CELEX; the HTML endpoint /legal-content/EN/TXT/HTML/?uri=…; the ELI/CELLAR REST endpoints; language/redirect); implement the standard endpoint the same way (per-host transport rule), tests with the failing URLs; (5) the export (--with-text) and the plan path both use these, so re-running export_candidates=true after this lands yields real text for both hosts; document in scripts/turns/ledger-verdicts/README.md (walls are never exported as text; the coordinator's Haiku lanes never see them) and CORPUS-TURN-RUNBOOK.md. Report: the wall kinds detected with counts over the 400-row export file (run your detector over /root/work/ledger/export-002.json and print the histogram), the Federal Register API design (URL → document_number → endpoint), the EUR-Lex finding, files, tests, fitness line, marker hash, commit hash, refusals with reasons.`
const r = await agent(P, { label: 'LEDGER-WALLS', phase: 'Lane', model: 'sonnet' })
return { ledgerwalls: r }
