export const meta = {
  name: 'lane-firstpage',
  description: 'Sonnet lane FIRSTPAGE: the /regulations first paint shows 60 MONITOR rows with no next date while the 13 IMMEDIATE and 12 ACTION rows arrive only with the background load; make the first page carry what the surface sorts first',
  phases: [{ title: 'Lane', detail: 'FIRSTPAGE in its own worktree' }],
}
phase('Lane')
const P = `You are lane FIRSTPAGE on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/firstpage (branch lane/firstpage-2026-09-04, based on origin/master 697be18e); node_modules symlinked at fsi-app/node_modules. Cannot push; coordinator lands. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. Write set: fsi-app/src/lib/supabase-server.ts, fsi-app/src/lib/data.ts, fsi-app/src/lib/api/** listings path, the listings RPC's SQL ONLY as a new additive migration fsi-app/supabase/migrations/303_*.sql (written, NOT applied, idempotent, self-check SQL in the header, a row INSIDE the table in fsi-app/docs/inventories/migrations.md) if the ordering lives in the RPC, the components under fsi-app/src/components/regulations/ ONLY for the honest-state disclosure text, their tests, and docs/audits/perf-load-times-2026-09-03.md (append a §11). Another lane owns src/lib/forward-events/** and scripts/**; do not touch them. Deterministic; "there is NO small follow-up fix, fix it now"; no claims ahead of evidence, [CONFIRMED]/[HYPOTHESIS]; read every file you touch IN FULL before editing; commit message ends with "Co-Authored-By: Claude Sonnet <noreply@anthropic.com>". Gates before commit: npx tsc --noEmit -p fsi-app/tsconfig.json clean; node --test on the tests you touch; from repo root node fsi-app/.discipline/fitness/runner.mjs (0 violations) and node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs (141 pass). The Supabase MCP (server "Supabase", tool execute_sql, project kwrsbpiseruzbfwjpvsp; load via ToolSearch "select:mcp__Supabase__execute_sql") is available for READ-ONLY SELECT; never write through it.

THE EVIDENCE [CONFIRMED by the coordinator on the live customer surface https://carosledge.com/regulations, 2026-09-04 ~08:15 UTC, page text captured while "Loading the full ledger…" was still showing]: the band headers read IMMEDIATE "0 shown / 13", ACTION "0 shown / 12", MONITOR "60 shown / 703", AWARENESS "0 shown / 168", each empty band saying "No matching regulations in this band." The 60 rows present were MONITOR rows with an em-dash next date (a 1996 Commission Recommendation, an EC–Hungary road transport agreement, three US Federal Register notices). The header says "FOUR BANDS · SORTED BY NEXT DEADLINE". So the first page (the SSR/initial 60 rows, see RegulationsLedger.tsx header comment and the restStatus background load) carries rows the surface sorts LAST, and the rows a reader opens the page for (13 within 90 days, 12 within 6 months) paint only after the rest loads. Reading RegulationsLedger.tsx lines 1065-1147 [CONFIRMED]: total = bandCount(key) from the counts RPC, rows = the loaded rows in that band, "X shown" appears whenever rows.length !== total, and the empty-band copy says "No matching regulations" which is FALSE during load (nothing was filtered; the rows are not here yet). Lane PERF-5 (docs/audits/perf-load-times-2026-09-03.md §9, read it) fixed the chunk loop in supabase-server.ts and measured that every listing query is 0.6-25 ms at the database; PERF-6/7 moved auth to getClaims. None of them looked at WHAT the first page contains.

DIAGNOSE FIRST: (a) trace the initial-rows path end to end: the /regulations page server component → getListingsOnly/getAppData in src/lib/api/org.ts / data.ts → supabase-server.ts → the RPC or table query (name it; read its SQL in the migration that defines it, pg_get_functiondef via read-only SQL for the live body, and confirm its ORDER BY and LIMIT); state exactly which column the first page is ordered by and which the surface sorts by (next_date? deadline_at? the "Newest / Priority / A→Z / My order" segment's default); (b) measure with read-only SQL what the live first 60 rows of that query are (ids, next date, band) and confirm the 13 IMMEDIATE ids are not among them; (c) read RegulationsLedger.tsx's restStatus flow and the empty-band copy path.

THEN BUILD: (1) the first page carries the rows the default sort puts first: order the initial query the same way the surface's default sort orders (deadline soonest first, nulls last, then whatever tiebreak the surface uses), keeping the page size and the background load unchanged; if the ordering lives in the RPC, migration 303 (additive, or a new ORDER BY parameter with the old default preserved) and the caller passes it; if it lives in the TS query, change the .order() there; (2) during the background load, an empty band must not claim "No matching regulations": render the honest state ("Loading N regulations…" with the authoritative band total) while restStatus is loading and the band has rows per the count, and keep "No matching regulations" only when the load is complete or a filter is active; (3) tests for the ordering (a unit test on the query builder or a SQL self-check in the migration header) and for the empty-band copy under both states; (4) MEASURE: state the first-page composition before/after with the SQL you ran, [CONFIRMED] for what you read and [INFERRED] for the projected paint. Report: diagnosis, files, exported functions, tests, tsc/fitness lines, whether a migration is needed (and its self-check), commit hash, anything refused with the reason.`
const r = await agent(P, { label: 'FIRSTPAGE', phase: 'Lane', model: 'sonnet' })
return { firstpage: r }
