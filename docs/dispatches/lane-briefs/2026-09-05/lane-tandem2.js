export const meta = {
  name: 'lane-tandem2',
  description: 'Sonnet lane TANDEM-2: THE GATE must see every unconnected apply slice, not only the newest artifact, and the runtime needs a dispatchable backlog mode that runs the flywheel over mint-run-017..022 (~650 items minted with no downstream)',
  phases: [{ title: 'Lane', detail: 'TANDEM-2 in its own worktree' }],
}
phase('Lane')
const P = `You are lane TANDEM-2 on Caro's Ledge (repo Dwarves77/dotfiles). RESUME NOTE: a previous run of this lane was killed after 81 tool calls with UNCOMMITTED work in this worktree (git status shows six modified files: population-turn.yml, POPULATION-TURN-RUNBOOK.md, mint/PENDING-RUN.md, MINT-RUNBOOK.md, run-population-flywheel.mjs and its test). Start by reading git diff in full, decide what is complete and correct, keep what is, finish the rest; do not start from scratch and do not discard that work unread. Worktree /root/work/lanes/tandem2 (branch lane/tandem2-2026-09-04, based on train/wave17 489734e3 = origin/master 5cace829 + BOILER-2, landing now); node_modules symlinked at fsi-app/node_modules. Cannot push; coordinator lands. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. Write set: fsi-app/scripts/turns/run-population-flywheel.mjs (+ .test.mjs), .github/workflows/population-turn.yml, docs/runbooks/POPULATION-TURN-RUNBOOK.md, fsi-app/scripts/mint/MINT-RUNBOOK.md §8/§9 text only, and fsi-app/scripts/harness-runs/mint/PENDING-RUN.md ONLY if the fitness runner's F28 names the mint family after your MINT-RUNBOOK edit (append a "What changed (6)" entry in the marker's existing chained format; the LAST line of the form "**harness_version at write time:** \`sha256:...\`" must carry the hash the runner prints; that exact phrase is what parsePendingRunHash reads, so reword the previous final line to "at <lane>'s write time (superseded below)"). Nothing else. Deterministic, $0, no LLM; "there is NO small follow-up fix, fix it now"; no claims ahead of evidence, [CONFIRMED]/[HYPOTHESIS]; read every file you touch IN FULL (run-population-flywheel.mjs is ~800 lines, lane TANDEM's; read its header and the workflow's comments, they are the design); commit message ends with "Co-Authored-By: Claude Sonnet <noreply@anthropic.com>". Gates: node --test scripts/turns/run-population-flywheel.test.mjs; python3 -c "import yaml; yaml.safe_load(open('.github/workflows/population-turn.yml'))"; from repo root node fsi-app/.discipline/fitness/runner.mjs (0 violations) and node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs; then bash fsi-app/.discipline/run-test-suite.sh (the full suite CI runs; it must be 0 fail).

THE TWO DEFECTS [CONFIRMED by the coordinator reading the landed code, 2026-09-04]:
(1) checkPriorSliceConnected(newestArtifact) looks at ONE artifact, the newest by run number. mint-run-023 is a DRY artifact (R-D rows_file, minted absent) and sits newest, so the gate reports "nothing was minted by this slice" and lets a new apply through while mint-run-017..022 (six apply artifacts, ~650 items, run ids 33817563729, 33821410389, 33823467586, 33825867992 among them; read the artifacts under fsi-app/scripts/harness-runs/mint/) still carry no edges_discovered/forward_events_extracted/isolated_items. Any dry run resets the gate. CLAUDE.md standing rule 17: a mint is closed only when the flywheel has connected it and the harness recorded the outcome. Fix: the gate reads EVERY mint-run artifact in the harness-runs dir (the existing loader that finds the newest can be widened), and refuses while ANY artifact with metrics.minted > 0 lacks the three outcome keys, naming all of them and the exact fix command; a dry artifact never counts; tests for: all connected, one stale in the middle masked by a newer dry, several stale, none.
(2) There is no dispatchable way to connect the backlog. run-population-flywheel.mjs takes --mint-run <artifact> and runs the chain over that artifact's minted ids (extractMintedItemIds); the workflow calls it only for the run's own NEW artifact. Build a backlog mode wired into population-turn.yml as a workflow_dispatch choice (an input like flywheel_backlog: true, or a mode value; read the workflow's existing inputs and choose the form that does not break the existing mode/rows_file contract) that, in apply mode, skips export/mint entirely and instead runs the flywheel over every mint-run artifact that fails the gate, oldest first, each artifact enriched in place with §9 outcomes by the existing --outcomes path (run-mint-batch.mjs --outcomes, read how the driver calls it), then commits and delivers the enriched artifacts through the workflow's existing artifact-branch delivery step (deliver-artifact-branch.sh) so the coordinator lands them; a dry backlog run lists the artifacts and item counts and writes nothing. Bound it: a --max-artifacts flag with a documented default, and per-artifact checkpointing so a job timeout leaves earlier artifacts enriched and the gate correctly narrowed; read the workflow's timeout-minutes and the flywheel's per-item cost from TANDEM's header or measure it from the code paths (discovery, extraction, recluster are child processes with their own batch sizes) and state the projection for 650 items as [INFERRED]; raise the job timeout if the arithmetic demands it and say so. Also make the normal apply path's gate message name the backlog dispatch as the fix. Runbook text for both. Report: files, exported functions, the exact dispatch inputs for (a) the backlog dry, (b) the backlog apply, (c) the R-D six apply that follows, tests, fitness, marker hash if re-pinned, commit hash, anything refused with the reason.`
const r = await agent(P, { label: 'TANDEM-2', phase: 'Lane', model: 'sonnet' })
return { tandem2: r }
