export const meta = {
  name: 'lane-cap1000fix2',
  description: 'Sonnet lane CAP-1000-FIX-2: the no-npm discipline suite in CI fails on data-public-surface-slugs.test.mjs because it now imports supabase-service.ts, which imports @supabase/supabase-js (absent in that job); the env predicate belongs in a dependency-free module',
  phases: [{ title: 'Lane' }],
}
phase('Lane')
const P = `You are lane CAP-1000-FIX-2 on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/train46 (branch train/wave46-2026-09-05 = PR #593, CI failing); node_modules symlinked at fsi-app/node_modules; Node 24 at /home/claude/.npm/_npx/387698761821791d/node_modules/node/bin/node (dirname on PATH). Commit ON THIS BRANCH; the coordinator pushes. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. CLAUDE.md rules: no copies of logic; no dead code; read every file you touch in full; commit message ends with "Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>".

THE FAILURE [CONFIRMED, PR #593 "Discipline engine unit tests", run 33942997710]: bash fsi-app/.discipline/run-test-suite.sh → 5448 pass, 1 fail: fsi-app/src/lib/data-public-surface-slugs.test.mjs "test failed" (the suite prints no detail). That CI job runs WITHOUT node_modules; commit d81c5391 (CAP-1000-FIX) made the test import isServiceSupabaseConfigured from src/lib/supabase-service.ts, which imports @supabase/supabase-js, so the import fails there (locally it passes because node_modules exist). Reproduce first: run the single test with node_modules hidden (e.g. NODE_PATH unset and a temporary rename of the symlink, or run it from a copy of the worktree without the symlink) and paste the real error.

FIX, one rule in one place: move the predicate into a dependency-free module (e.g. fsi-app/src/lib/supabase-env.ts, or an existing env module if one exists: grep for other pure env readers under src/lib and reuse the convention) exporting isServiceSupabaseConfigured; supabase-service.ts imports it from there (no second copy); data.ts and the test import the pure module. The test must pass with node_modules absent AND present. Keep the three behaviours the test proves (configured+paged → all slugs; configured+page failure → throws; unconfigured → [] with one logged line). Also read fsi-app/.discipline/run-test-suite.sh's portability self-check to confirm the test stays eligible for the no-npm suite. Gates from the repo root: bash fsi-app/.discipline/run-test-suite.sh with the node_modules symlink temporarily removed (restore it after), then with it present; node fsi-app/.discipline/fitness/runner.mjs; node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs; tsc --noEmit. REPORT: the reproduced error text, files, both suite results, commit hash.`
return { f: await agent(P, { label: 'CAP-1000-FIX-2', phase: 'Lane', model: 'sonnet' }) }
