export const meta = {
  name: 'lane-heal8',
  description: 'Sonnet lane HEAL-8: the first rule-18 heal apply grounded 147 figures but reported 1,477 token_not_in_page, 359 unresolved and 302 capped; diagnose those three outcomes on real rows and make the SOURCE step find the figure where it actually is',
  phases: [{ title: 'Lane', detail: 'HEAL-8 in its own worktree' }],
}
phase('Lane')
const P = `You are lane HEAL-8 on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/heal8 (branch lane/heal8-2026-09-04, based on origin/master 4c90c5bb); node_modules symlinked at fsi-app/node_modules. Cannot push; coordinator lands. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. Write set: fsi-app/scripts/mint/heal-provenance.mjs (+ .test.mjs), fsi-app/scripts/maintenance/provenance-heal.mjs (+ test) only for deps, docs/runbooks/MAINTENANCE-RUNBOOK.md provenance-heal section, and fsi-app/scripts/harness-runs/mint/PENDING-RUN.md ONLY if F28 names the mint family (append "What changed (10)" in the chained format; the LAST "**harness_version at write time:** \`sha256:...\`" line carries the hash the runner prints; reword the previous final line to "at train/wave20's write time (superseded below)"). Deterministic, $0, no LLM; read heal-provenance.mjs IN FULL (its header carries seven lanes' findings; STEP SOURCE is HEAL-7's, read it and its tests first); no claims ahead of evidence, [CONFIRMED]/[HYPOTHESIS]; commit message ends with "Co-Authored-By: Claude Sonnet <noreply@anthropic.com>". Gates: node --test on what you touch; from repo root node fsi-app/.discipline/fitness/runner.mjs (0 violations), node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs, then bash fsi-app/.discipline/run-test-suite.sh (0 fail). Supabase MCP (server "Supabase", execute_sql, project kwrsbpiseruzbfwjpvsp, via ToolSearch "select:mcp__Supabase__execute_sql") READ-ONLY: SELECT to measure; never write. The container proxy refuses public hosts; the captures are in agent_run_searches.result_content, which you CAN read.

THE EVIDENCE [CONFIRMED, Maintenance #27, Actions 33844146038, provenance-heal apply quarantined-live, HEAL_VERSION hp7-2026-09-04.1, first run under rule 18]: candidates 94, healed_verified 5, still_failing 89. STEP SOURCE per token: token_not_in_page 1,477; unresolved 359; bound_hit 302; grounded_on_existing_source 123; worklist_ambiguous_host 77; unfetchable 58; source_registered_and_grounded 24 (24 sources registered and rated, 147 tokens grounded in all). Other steps: retrofitted 103, relabeled 52, refused_no_owning_paragraph 3+49. Final failures: criterion 7 gate_a_unproven_or_stale 83 items, criterion 4 analysis_missing_label_syntax 49 claims, criterion 5 missing_required_slot 4, criterion 2 ungrounded_url 1, criterion 6 missing_full_brief 1. The dry run the hour before predicted would_capture_and_ground 1,543; the apply found the token verbatim in only 123 + 24 of those pages. CLAUDE.md rule 18 (operator, verbatim): "get the source. then rate the source. it's that simple. this isn't hard, find the source and then publish the data on the site." A token the heal cannot find in the page it fetched is not yet "found"; this lane's job is to find it where it actually is, or prove it is nowhere.

DIAGNOSE FIRST, read-only, on real rows, and put the numbers at the top of the report: take the run's per_item source entries (the coordinator can hand you the summary.json if you cannot download it: ask in your report which fields you need, but first check whether fsi-app/scripts/_snapshots or the Actions artifact is on disk; if not, reproduce by running STEP SOURCE's classification locally against captures you SELECT). For a sample of at least 60 token_not_in_page tokens across 20 items, classify each: (A) NUMERIC-FORM MISMATCH: the figure is in the capture but in another surface form (35.5% vs 35,5 %, 1,200 vs 1 200 vs 1.200, €1.2bn vs EUR 1.2 billion, 2030 inside "2030." or "2030)", en-dash ranges, non-breaking spaces, thin spaces, superscripts); (B) ELSEWHERE ON THE SITE: the cited page is a landing/press page and the figure is in a linked PDF or a sub-page the capture references (an href in the captured HTML) one hop away; (C) PAGE CHANGED / CAPTURE THIN: the capture is a cookie wall, a JS shell, a 404 body, or shorter than the earlier capture the brief was written from; (D) NOWHERE: the figure is not in the capture, not in any linked resource, and not in any of the item's other captures. Also classify the 359 unresolved (why no candidate URL: the token's sentence cites nothing, the item has no citations, the URL is a bare domain) and the 302 bound_hit (SOURCE_MAX_PER_ITEM; what cap would clear them in one more pass, with the fetch-count arithmetic against the 1,500 s budget).

THEN BUILD, by class: (A) a format-tolerant MATCHER for locating the figure in the capture (digits, decimal/thousand separators, currency symbols vs codes, unicode spaces and dashes, trailing punctuation), which then grounds the claim on the page's OWN verbatim span (ADR-016: the stored source_span is byte-exact from the capture; only the search is tolerant), with tests for every form you measured; (B) one-hop follow: when the capture links a PDF or sub-page on the same host (or the same institution's document host, e.g. a Cellar/EUR-Lex link from a Commission press page), fetch it through the existing polite fetch + capture path, bounded (max 3 links per token, same host or registered institution only, PDF text via the repo's existing PDF extraction if one exists, grep for it; if none exists say so and bound to HTML), and ground on that capture with its own source row (register + rate, same as HEAL-7); (C) re-capture through the existing CAPTURE step with the Wayback fallback when the live capture is thin, and report capture_thin; (D) the honest terminal state: the sentence that carries an unprovable figure is refactored to a labelled analysis claim through the existing RECLASSIFY/REFACTOR path if its paragraph exists, else reported as unprovable_figure with the token and sentence, so the coordinator can put the residue to the operator with counts; never invent a source. Raise SOURCE_MAX_PER_ITEM or make the step resume across dispatches (ids: on the next dispatch continue where the cap stopped, using the checkpoint the budget already writes). Dry/apply, resumable, budgeted, summary per-token reasons with the new classes. Report: the sample classification table, files, exported functions, tests, fitness, marker hash if re-pinned, the exact next dispatch, commit hash, anything refused with the reason.`
const r = await agent(P, { label: 'HEAL-8', phase: 'Lane', model: 'sonnet' })
return { heal8: r }
