export const meta = {
  name: 'lane-govsingle',
  description: 'Sonnet lane GOV-SINGLE: eight family runners each keep a hand-synced copy of F28\'s GOVERNING_FILES; the mint copy is already out of sync (10 vs 12 files) so mint-run-024..026 stamp sha256:4f09523532bb7aee, a hash the gate never computes and no marker can ever be discharged by; one table, every runner and F28 import it',
  phases: [{ title: 'Lane', detail: 'GOV-SINGLE in its own worktree' }],
}
phase('Lane')
const P = `You are lane GOV-SINGLE on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/govsingle (branch lane/govsingle-2026-09-04, based on origin/master f2800ea9); node_modules symlinked at fsi-app/node_modules. Cannot push; coordinator lands. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. Write set: a NEW fsi-app/scripts/harness-runs/governing-files.mjs (+ test) holding THE table of governing files per family (exactly F28's current GOVERNING_FILES, including SCREEN_GOVERNING_FILES however F28 sources it today; read F28 in full first), fsi-app/.discipline/fitness/functions/F28-harness-run-integrity.mjs (import the table from that module instead of declaring it; F28 is itself a meta-harness governing file, so the meta-harness marker must be created: fsi-app/scripts/harness-runs/meta-harness/PENDING-RUN.md, format per git log -p -2 -- that path, hash as the fitness runner prints it; and the new module must be ADDED to the meta-harness family's governing list since it now defines what governs, say so in the marker), every family runner that declares its own *_GOVERNING_FILES copy (grep -rn "_GOVERNING_FILES = Object.freeze" fsi-app/scripts: run-mint-batch.mjs, forward-events/run-extraction.mjs, turns/run-ledger-consume.mjs, run-propagation-drain.mjs, run-change-detection.mjs, run-source-sweep.mjs, research-sweep / screen, and any other; each imports its family's entry from the table and re-exports the old name so existing importers/tests keep working), their tests (the per-runner "matches F28's hardcoded entry (kept hand-synced)" parity tests become ONE contract test in governing-files.test.mjs: no runner file may contain a literal governing-file array, and F28 must import the module), fsi-app/scripts/harness-runs/CONVENTION.md (its governing-file table: state that the module is the source and the markdown table is documentation checked by test against the module, and make that test exist if CONVENTION-TABLE-PARITY does not already do it, read fsi-app/scripts/lib/run-artifact.test.mjs / .discipline tests first), fsi-app/scripts/mint/MINT-RUNBOOK.md only where it tells a reader to hand-sync the list. Nothing else. Deterministic, $0; "there is NO small follow-up fix, fix it now"; no claims ahead of evidence, [CONFIRMED]/[HYPOTHESIS]; read every file you touch IN FULL; commit message ends with "Co-Authored-By: Claude Sonnet <noreply@anthropic.com>". Gates before commit: node --test fsi-app/scripts/harness-runs/*.test.mjs fsi-app/scripts/lib/*.test.mjs fsi-app/scripts/mint/run-mint-batch.test.mjs fsi-app/scripts/forward-events/run-extraction.test.mjs fsi-app/scripts/turns/*.test.mjs; from repo root node fsi-app/.discipline/fitness/runner.mjs (0 violations after the meta-harness marker) and node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs (141 pass, or more if you add one under .discipline: say which).

THE EVIDENCE [CONFIRMED by the coordinator]: F28's GOVERNING_FILES.mint has 12 entries (since #580 it includes src/lib/agent/gate-a-scan.mjs and gate-a-match.mjs, the single-source Gate A). run-mint-batch.mjs's MINT_GOVERNING_FILES has 10 (line ~70; it never gained the two src/ files). run-mint-batch.test.mjs line ~563 "MINT_GOVERNING_FILES matches F28's hardcoded mint entry (kept hand-synced until a future wave points F28 at this export)" only checks that each of the runner's entries appears in F28's source, so the two missing entries pass silently. Consequence, live: population-turn runs #34, #35, #36 today stamped mint-run-024, 025, 026 with harness_version sha256:4f09523532bb7aee, while the fitness runner computes the mint hash from 12 files (the marker fsi-app/scripts/harness-runs/mint/PENDING-RUN.md on master is pinned at a 12-file hash); F28 rule (c) discharges a marker only when an artifact carrying its hash lands, so the mint marker can NEVER be discharged by a real run until the lists agree. This is the "wrong copy of the truth" pattern meta-harness-run-008 (landed today) names as the wave's recurring defect. The forward-events runner's copy currently agrees (2 files) but by luck, not by construction. Decide and state whether the table should live in scripts/harness-runs/ (the harness layer's own home, next to CONVENTION.md) or under .discipline; the coordinator's preference is scripts/harness-runs/governing-files.mjs with F28 importing it (a fitness function importing from scripts/ — check that the fitness runner's module resolution and any F-function that forbids cross-imports allows it; if a rule forbids it, put the module where the rule allows and say which rule). Report: the table module path, which runners now import it, the removed literals (file:line each), the contract test, the meta-harness marker hash, whether the mint hash the runner will now stamp equals the marker's pinned hash on master (compute both and print them: after this change, run node fsi-app/scripts/mint/run-mint-batch.mjs's hash path or hashHarnessVersion over the imported list and compare with the marker), fitness line, test counts, commit hash, anything refused with the reason.`
const r = await agent(P, { label: 'GOV-SINGLE', phase: 'Lane', model: 'sonnet' })
return { govsingle: r }
