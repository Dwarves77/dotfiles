export const meta = {
  name: 'lane-tierchip',
  description: 'Sonnet lane TIER-CHIP: rule 18 publishes a figure WITH its source rating; the record-grade fact renderer shows no tier, so the claim tier must reach the page and render as the existing TierBadge',
  phases: [{ title: 'Lane', detail: 'TIER-CHIP in its own worktree' }],
}
phase('Lane')
const P = `You are lane TIER-CHIP on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/tierchip (branch lane/tierchip-2026-09-04, based on origin/master db01f605); node_modules symlinked at fsi-app/node_modules. Cannot push; coordinator lands. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. Write set: fsi-app/src/lib/agent/parse-record-sections.ts (+ .test.mjs), fsi-app/src/components/regulations/RegulationDetailSurface.tsx (RecordFactLine / RecordGradeSummary only), the equivalent record-fact renderers in fsi-app/src/components/pages/MarketSignalDetailSurface.tsx and fsi-app/src/components/research/ResearchFindingDetailSurface.tsx, the server data path that feeds those pages the record sections (find it: the [slug] page and its load helper, likely fsi-app/src/lib/load-detail-core.ts or the regulations page.tsx; add the claim-tier read there, one query, no N+1), fsi-app/.discipline/rendering/smoke/detail-surfaces-smoke.mjs fixtures, and tests. Do NOT touch data.ts, supabase-server.ts, list pages, /api routes, scripts/**, .github/**. Read docs/design/ux-laws.md and docs/design/design-principles.md before any .tsx edit; F35 and the rendering guard measure at 375 px; include a "UX compliance" paragraph in your report. Deterministic; no claims ahead of evidence, [CONFIRMED]/[HYPOTHESIS]; read every file you touch in full; commit message ends with "Co-Authored-By: Claude Sonnet <noreply@anthropic.com>". Gates: node --test on your tests; fsi-app/node_modules/.bin/tsc --noEmit -p fsi-app/tsconfig.json (0 errors); node fsi-app/.discipline/rendering/run-rendering-guard.mjs (the lane RECORD-SURFACE bridged chromium by symlink under /opt/pw-browsers if the pinned revision is missing; do the same, never skip the guard); from repo root node fsi-app/.discipline/fitness/runner.mjs (0 violations) and node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs. Supabase MCP (server "Supabase", execute_sql, project kwrsbpiseruzbfwjpvsp) READ-ONLY via ToolSearch to confirm column names.

THE RULING [CONFIRMED, operator 2026-09-04, CLAUDE.md standing rule 18]: "get the source. then rate the source. it's that simple. this isn't hard, find the source and then publish the data on the site." Migration 302 is APPLIED LIVE (2026-09-04 05:49 UTC): criterion 3 of validate_item_provenance now records a below-floor FACT as a rating (v_result.warnings.below_floor_facts, per-claim source_tier_derived) and never refuses it. Lane HEAL-7 (landing now) grounds orphan figures on registered, rated sources. THE GAP [CONFIRMED by HEAL-7 reading the page]: RegulationDetailSurface.tsx's TierBadge renders only the item-level r.sourceTier in the Sources tab; the record-grade FACT/GAP renderer RecordFactLine (~line 1156) shows no tier at all, because parseRecordSections (src/lib/agent/parse-record-sections.ts) parses content_md text only and never reads section_claim_provenance.source_tier_at_grounding (confirm the column name and whether derived_tier / source_id / the sources row's base_tier+tier_override is the rating to show; read migrations 141/145/202/302 and src/lib/sources/host-authority.ts classTierForHost for what "rated" means). BUILD: (1) the page's server load reads, in one query, the item's FACT claims with their tier (source_tier_at_grounding or the joined source's effective tier, whichever the validator uses) and the source URL/name; (2) parseRecordSections gains an optional claim-tier map so each RecordFactRow carries {tier, sourceName, sourceUrl} matched by slot_key and/or verbatim span (state the match rule and its failure mode; an unmatched fact renders without a chip, never a wrong chip); (3) RecordFactLine renders the existing TierBadge next to the span (same component, same tier vocabulary T1..T5, honest "unrated" state when tier is null), with the source name linked; (4) Market and Research record-fact renderers get the same; (5) fixtures in the rendering-guard smoke for a rated fact, an unrated fact, and a below-floor fact at 375 px; (6) tests for the parser's tier matching. Report: the rating field you chose and why (cite the migration), files, exported functions, tests, tsc, rendering guard line, fitness, UX compliance paragraph, commit hash, anything refused with the reason.`
const r = await agent(P, { label: 'TIER-CHIP', phase: 'Lane', model: 'sonnet' })
return { tierchip: r }
