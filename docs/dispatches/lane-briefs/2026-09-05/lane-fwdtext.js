export const meta = {
  name: 'lane-fwdtext',
  description: 'Sonnet lane FWD-TEXT: obligation_text on the customer-facing Upcoming Obligations strip starts mid-word and carries URL tails, markdown label syntax and table pipes; duplicate events per (item, date); fix the extractor at the producer, re-derive existing rows through a maintenance step',
  phases: [{ title: 'Lane', detail: 'FWD-TEXT in its own worktree' }],
}
phase('Lane')
const P = `You are lane FWD-TEXT on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/fwdtext (branch lane/fwdtext-2026-09-04, based on origin/master 697be18e); node_modules symlinked at fsi-app/node_modules. Cannot push; coordinator lands. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. Write set: fsi-app/src/lib/forward-events/extract-forward-events.mjs (+ its test), fsi-app/scripts/obligations/** and fsi-app/scripts/maintenance/derive-obligations.mjs ONLY if the obligations register copies obligation_text and must be re-derived too, ONE new maintenance step fsi-app/scripts/maintenance/forward-events-retext.mjs (+ test) and its step entry in .github/workflows/maintenance.yml (copy the exact form of the canonical-key-dedup / record-hollow-sweep steps: mode dry|apply, --out, idempotent, reversible with per-row restore_sql in summary.json, registered in fsi-app/docs/inventories/shared-dataset-ownership.md's allowlist JSON as a writer of item_forward_events, run node --test fsi-app/.discipline/shared-writer-registry.test.mjs), docs/runbooks/MAINTENANCE-RUNBOOK.md (one new section in the same structure). NOT the React components: the fix belongs at the producer, never a display-side sanitizer (CLAUDE.md, HANDOFF honest-state rule); if you believe a render-side change is unavoidable, say so with the reason and do not make it. Another lane owns fsi-app/src/lib/supabase-server.ts, src/lib/data.ts and the listings first page; do not touch them. Deterministic, $0, no LLM; "there is NO small follow-up fix, fix it now"; no claims ahead of evidence, [CONFIRMED]/[HYPOTHESIS]; read every file you touch IN FULL before editing (extract-forward-events.mjs is 830 lines, read all of it, its header states the verbatim source_span law: source_span is a substring by construction and assertVerbatim enforces it, that law is untouchable); commit message ends with "Co-Authored-By: Claude Sonnet <noreply@anthropic.com>". Gates before commit: node --test on what you touched; from repo root node fsi-app/.discipline/fitness/runner.mjs (0 violations), node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs (141 pass), python3 -c "import yaml; yaml.safe_load(open('.github/workflows/maintenance.yml'))". The Supabase MCP (server "Supabase", tool execute_sql, project kwrsbpiseruzbfwjpvsp; load via ToolSearch "select:mcp__Supabase__execute_sql") is available for READ-ONLY SELECT; never write through it.

THE EVIDENCE [CONFIRMED by the coordinator on the live customer surface https://carosledge.com/regulations, "Upcoming obligations" strip, 2026-09-04 ~08:15 UTC, page text captured]: of 8 events shown, these obligation texts render:
  (1) EU Net-Zero Industry Act, 25 Sep 2026: "venues generated from fines. By 25 September 2026, and every five years thereafter, Member States shall make public" — starts mid-word ("re|venues").
  (2) Euro 7, 29 Nov 2026 PHASE STEP: "7/oj/eng **Primary headline compliance deadline — FACT:** \\"It shall apply from 29 November 2026 for new types of vehicles of categories M₁ and N₁ …" — a URL tail (…/2024/1257/oj/eng) plus markdown bold label syntax leaked from section content_md.
  (3) Euro 7, 29 Nov 2026 COMPLIANCE DEADLINE: "hicles (M₂, M₃, N₂, N₃) | MONITORING **FACT — deadline:** \\"By 29 November 2026, the Commission shall adopt…" — mid-word ("ve|hicles"), a markdown table pipe and cell, and a label.
  (4) Euro 7 carries the SAME date 29 Nov 2026 five times (PHASE STEP ×2, COMPLIANCE DEADLINE ×3), at least two pairs being the same sentence once from a FACT claim (clean) and once from section content_md (garbled).
ROOT CAUSE [CONFIRMED by the coordinator reading fsi-app/src/lib/forward-events/extract-forward-events.mjs lines 262-271]: clauseAround(text, start, end, maxBefore=60, maxAfter=160) sets from = max(0, start - 60), a fixed byte offset, never snapped to a sentence/clause boundary, while the trailing edge does stop at the next terminator; sentenceStart(text, idx, maxBack=200) exists at line 279 and is used by the deontic-window checks at lines 534-547 but NOT by clauseAround. The input text for section-derived events is content_md (markdown labels like **FACT — deadline:**, table rows with |, link tails), so the window inherits them.

DIAGNOSE FIRST (read-only SQL + the extractor): (a) read item_forward_events for the Euro 7 item(s) and the NZIA item (find them by intelligence_items.title ILIKE) — list every row's event_date, event_kind, obligation_text, source_span, basis (claim id vs section id, whatever the columns are called; read migration 274/275 in fsi-app/supabase/migrations for the schema and the docs/decisions ADR that binds obligation_text, grep docs/decisions for forward_events, obligation_text, item_forward_events); (b) measure corpus-wide with SQL how many item_forward_events rows have obligation_text starting with a lowercase letter or a non-letter, containing '**', containing ' | ', containing '/oj/' or 'http', and how many (item_id, event_date, event_kind) groups have more than one row; (c) check whether migration 290's obligations table copies obligation_text (derive-obligations, scripts/obligations/**) so the register needs the same re-derivation, and whether the strip reads item_forward_events or obligations (grep UpcomingObligationsStrip*.tsx, RegulationDetailSurface.tsx, src/lib/data.ts, the API route).

THEN BUILD: (1) clauseAround snaps its leading edge to sentenceStart (bounded, keep the maxBefore cap as the outer bound, never start mid-word: if the boundary search fails, back up to the previous whitespace), and obligation_text is normalised for display from the markdown input: strip **label:** prefixes and bold markers, drop a leading table cell/pipe and any trailing pipe fragment, drop a leading URL tail (a token containing '/' with no whitespace before the sentence start), collapse whitespace; source_span stays byte-verbatim and unchanged (assertVerbatim must keep passing on every existing test). (2) Dedupe: within one extraction, when two hits share (event_date, event_kind) and one is claim-backed, keep the claim-backed one; when both are section-backed, keep the first and record the dropped one in the extractor's return (a counts field), never silently. Read how the extractor is called from apply-staged-update.ts and the flywheel driver so the dedupe rule lives in the extractor, not in each caller. (3) The maintenance step forward-events-retext: dry lists every row whose obligation_text would change (before/after, counts by defect class) and every duplicate group it would archive or delete — read the schema to see whether item_forward_events has an is_archived/superseded column; if it has none, do NOT delete rows: report the duplicate groups and let the coordinator put a deletion to the operator, and say so; apply rewrites obligation_text through the guarded db.mjs path with cite + snapshot + read-back, restore_sql per row. Tests with fixtures built from the real rows you read (the three garbled texts above verbatim). Report: diagnosis with the counts, the ADR/spec citations, files, exported functions, tests, fitness line, the exact maintenance dispatch inputs for the dry run, commit hash, anything refused with the reason.`
const r = await agent(P, { label: 'FWD-TEXT', phase: 'Lane', model: 'sonnet' })
return { fwdtext: r }
