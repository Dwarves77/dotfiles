export const meta = {
  name: 'lane-feslot2b',
  description: 'Sonnet lane FE-SLOT-2b: scope the grounding-pool (agent_run_searches.result_content) reads FE-SLOT-2 added to the items that actually carry a calendar-dated [due_date] slot claim, in all three callers; full captures for every corpus item is a payload the rule never needs',
  phases: [{ title: 'Lane', detail: 'FE-SLOT-2b in the feslot2 worktree' }],
}
phase('Lane')
const P = `You are lane FE-SLOT-2b on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/feslot2 (branch lane/feslot2-2026-09-04, HEAD 3edcc3da = lane FE-SLOT-2's commit; read it in full first: git show 3edcc3da --stat and the diff of fsi-app/src/lib/forward-events/read-and-extract.mjs, fsi-app/scripts/turns/export-corpus-for-extraction.mjs, fsi-app/scripts/maintenance/forward-events-retext.mjs); node_modules symlinked at fsi-app/node_modules. Cannot push; coordinator lands. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. Write set: those three files and their tests, and fsi-app/scripts/harness-runs/forward-events/PENDING-RUN.md only if the fitness runner says the forward-events hash moved (read-and-extract.mjs is NOT a governing file; extract-forward-events.mjs is, and you must not touch it). Deterministic, $0; "there is NO small follow-up fix, fix it now"; no claims ahead of evidence; commit message ends with "Co-Authored-By: Claude Sonnet <noreply@anthropic.com>". Gates: node --test fsi-app/src/lib/forward-events/*.test.mjs fsi-app/scripts/turns/export-corpus-for-extraction.test.mjs fsi-app/scripts/maintenance/forward-events-retext.test.mjs fsi-app/scripts/forward-events/run-extraction.test.mjs; repo-root node fsi-app/.discipline/fitness/runner.mjs (0 violations) and node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs (141). Supabase MCP read-only SELECT is available (server "Supabase", tool execute_sql, project kwrsbpiseruzbfwjpvsp; ToolSearch "select:mcp__Supabase__execute_sql").

THE DEFECT [CONFIRMED by the coordinator reading the commit]: FE-SLOT-2's context is consumed ONLY by claims whose claim_text starts "[due_date]" AND whose span carries a calendar date (the relative-deadline population never uses it; 638 of 756 slot claims live). Yet (a) export-corpus-for-extraction.mjs reads agent_run_searches "id, intelligence_item_id, result_content, result_index" for EVERY item id in 200-id chunks (result_content is the full grounding pool per ADR-016, never truncated: tens of KB per capture, several captures per item; measure live with read-only SQL: SELECT count(*), sum(length(result_content)), avg(length(result_content)) FROM agent_run_searches, and the same restricted to items that have a '[due_date]%' claim whose source_span ~ '\\y(19|20)\\d{2}\\y' — report both, that ratio is the payload this lane removes); (b) forward-events-retext.mjs's readPoolForItem runs for every item the step touches; (c) readExtractionInput (the live per-item reader) fires the pool query unconditionally in its Promise.all. FIX: export ONE predicate from read-and-extract.mjs, e.g. claimNeedsDueDateContext(claim) (the same test the extractor's rescue path applies: [due_date] prefix and a calendar-date-shaped span; import the date shape from extract-forward-events.mjs if it exports one, never a second regex), and one helper itemIdsNeedingContext(claims); then (a) the exporter reads the pool only for those ids (chunked, the chunk size the file already uses), (b) the retext step calls readPoolForItem only when the item's claims need it, (c) the live reader reads claims and sections in parallel first and the pool only when needed (one extra round trip only on items that need it; say in the header why). Keep attachDueDateContext's contract unchanged (claims that do not need context get context undefined/null exactly as before). Tests: the predicate (four shapes: relative deadline, calendar date, non-slot claim, GAP), each caller skips the pool read when no claim needs it (assert the pool reader is NOT called) and reads it when one does. Also state the row limit behaviour of readAll (does it paginate past PostgREST's default max rows? read scripts/lib/db.mjs readAll; if it caps at 1000 rows silently, say so with the line number, it matters for a 200-item chunk with several captures each). Report: the live payload numbers, files, exported functions, tests, fitness line, commit hash.`
const r = await agent(P, { label: 'FE-SLOT-2b', phase: 'Lane', model: 'sonnet' })
return { feslot2b: r }
