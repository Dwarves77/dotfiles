export const meta = {
  name: 'lane-ledgerexport',
  description: 'Sonnet lane LEDGER-EXPORT: the ledger-consume candidate export must carry the fetched page text (one fetcher, the runtime\'s own polite fetchDoc), delivered by a workflow dispatch as an artifact branch, so the session-Haiku classification lanes classify offline with zero fetching of their own',
  phases: [{ title: 'Lane' }],
}
phase('Lane')
const P = `You are lane LEDGER-EXPORT on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/ledgerexport (branch lane/ledgerexport-2026-09-04, based on origin/master e67b3223 = train 38, which carries lane LEDGER-ZERO: read its commit c1a76fa9 in full first: git show c1a76fa9 --stat, then the files); node_modules symlinked at fsi-app/node_modules. Cannot push; coordinator lands. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. CLAUDE.md rules: one body (never a second fetcher); no LLM/API spend in runtimes; no schedules; "there is NO small follow-up fix, fix it now"; [CONFIRMED]/[HYPOTHESIS]; read every file you touch IN FULL; commit message ends with "Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>". Gates: node --test fsi-app/scripts/turns/run-ledger-consume.test.mjs (60+ today) and any test you touch; repo-root node fsi-app/.discipline/fitness/runner.mjs (0 violations: run-ledger-consume.mjs is a ledger-consume GOVERNING file, so chain a "What changed" entry onto fsi-app/scripts/harness-runs/ledger-consume/PENDING-RUN.md and re-pin to the hash the runner prints); node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs; python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ledger-consume.yml'))"; node fsi-app/.discipline/governance/secrets-reference-audit.mjs.

THE DEFECT [CONFIRMED by the coordinator, 2026-09-04 16:55]: LEDGER-ZERO's --export-candidates lists candidate rows WITHOUT page text (its own note_on_fetched_text says "a session lane must fetch each URL itself (e.g. via the browser)"). The coordinator tried that: 1,837 candidates, Haiku lanes fetching through WebFetch hit rate limits within minutes and one lane started classifying from URL patterns instead (guessing, refused). The runtime already owns the ONE polite fetcher (makeFetchDoc / fetchDoc at run-ledger-consume.mjs ~line 229, the ConsumeOpts.FetchDocFn contract, with the politeness gap and budgets), and plan mode already fetches every candidate it classifies. The export must therefore carry the text, fetched by that fetcher, in the Actions runner, delivered as a workflow artifact branch, so classification lanes never fetch.

BUILD: (1) run-ledger-consume.mjs: --export-candidates <path> gains --with-text: for each listed candidate call the SAME fetchDoc (no second fetcher; same politeness gap, same timeout), and write per row text (sliced to CONTENT_MAX_CHARS, import or re-export that constant from first-fetch-classify.ts rather than retyping 6000), fetched_chars, fetch_ok, fetch_error (null when ok), fetched_at, transport; rows under the 200-char floor keep their text but fetch_ok=false with fetch_error "below_floor_200" (the same floor portal-harvest.ts applies: cite the line). Without --with-text the mode is unchanged. Still no classify, no DB write, no harness artifact. Add --offset/--after keyset support only if the existing selectCandidateLedgerPage already offers it (it takes opts.after per its header: expose it as --after "<first_seen_at>,<id>" so batches of --limit N can page through all 1,837 in the same keyset order the consume path uses). (2) .github/workflows/ledger-consume.yml: a new boolean input export_candidates (default false) with export_limit (default 400) and export_after (default ''); when true the job runs ONLY the export (node scripts/turns/run-ledger-consume.mjs --export-candidates scripts/_snapshots/ledger-candidates/candidates-<run_id>.json --with-text --limit <export_limit> [--after <export_after>]), then delivers the file the same way the family's artifacts are delivered (read how ledger-consume.yml / the repo's deliver-artifact-branch.sh push an artifact branch ledger-consume/<run_id>; reuse that exact mechanism so the coordinator fetches the branch; also upload as a workflow artifact). Job timeout: state the arithmetic (400 fetches × (politeness gap + timeout worst case)). (3) tests: --with-text row shape with an injected fetchImpl (ok, failure, below-floor), --after parsing, no DB write, CONTENT_MAX_CHARS slice. (4) docs: scripts/turns/ledger-verdicts/README.md step 1 rewritten (the export now carries text; the browser is no longer the fetch path), docs/runbooks/CORPUS-TURN-RUNBOOK.md ledger-consume section (the export dispatch and the branch name). Report: files, exported functions, the timeout arithmetic, tests, fitness line, marker hash, commit hash, the exact first dispatch (ledger-consume export_candidates=true export_limit=400 export_after='' and the following pages).`
const r = await agent(P, { label: 'LEDGER-EXPORT', phase: 'Lane', model: 'sonnet' })
return { ledgerexport: r }
