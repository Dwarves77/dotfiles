export const meta = {
  name: 'wave-perf13-fededup',
  description: 'Two Sonnet lanes after the train-44 live measurement: PERF-13 (every item click an edge hit: prerender the detail slugs, prefetch that covers the visible rows, a loading boundary that actually shows, the duplicate upcoming call, the scroll sentinel, the honest band state) and FE-DEDUP (forward-events claim+section twins → one event; unique index; the 587 duplicate obligation rows removed through the guarded writer)',
  phases: [{ title: 'Lanes' }],
}
phase('Lanes')
const C = (name, wt) => `You are lane ${name} on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/${wt} (branch lane/${wt}-2026-09-04, based on origin/master 1ae31181 = train 44, live on carosledge.com); node_modules symlinked at fsi-app/node_modules (carries @tanstack/react-query@5, @tanstack/react-virtual@3). Node 24 at /home/claude/.npm/_npx/387698761821791d/node_modules/node/bin/node (put its dirname on PATH for node --test and next build). Cannot push; coordinator lands. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. CLAUDE.md rules: one writer per dataset; no LLM; no schedules; no copies of logic; no dead code; "there is NO small follow-up fix, fix it now"; [CONFIRMED]/[HYPOTHESIS]; read every file you touch IN FULL; commit message ends with "Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>". Gates from the repo root: node fsi-app/.discipline/fitness/runner.mjs (0 violations on this tree; F28: chain/re-pin the family marker if you change a governing file — read fsi-app/scripts/harness-runs/governing-files.mjs), node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs (186), node fsi-app/.discipline/governance/closure-gate.mjs, tsc --noEmit if .ts/.tsx touched, bash fsi-app/.discipline/run-test-suite.sh (CI runs it WITHOUT node_modules: any test importing react/next/@tanstack must be *.npmtest.mjs), node fsi-app/.discipline/consistency/override-check.mjs --range=origin/master..HEAD (C3: every migration file named in docs/inventories/migrations.md must exist; ignore C4 worktree rows, they are local). Supabase MCP is read-only SELECT for you (project kwrsbpiseruzbfwjpvsp; ToolSearch "select:mcp__Supabase__execute_sql"); migrations are WRITTEN, the coordinator applies. If fsi-app/src/**/*.tsx changes, include a "UX compliance" block (docs/design/ux-laws.md) in your REPORT. REPORT: what you measured, root causes with file:line, what you built, what you deleted, tests, every gate's output line, commit hash(es), anything refused with the reason.`

const A = agent(`${C('PERF-13','perf13')}

OPERATOR (verbatim, governs): "every click should show items on a page instantly"; "loading is faster but each page is blank when you navigate to it, there should be zero wait time for pages to load"; "modern websites do not have blank pages and take time to load"; "find out how others do it and do the same"; no spend-based fixes.

LIVE MEASUREMENT BY THE COORDINATOR (Chrome, carosledge.com at 1ae31181, 2026-09-04 23:10–23:20 UTC) [CONFIRMED]: /regulations TTFB 21 ms (x-vercel-cache HIT), 39 KB transferred / 277 KB decoded, DCL 657 ms, 12 virtualized rows, no #418, no false banner. Cursor pages 3.3–4.0 KB transferred, 320–440 ms. Clicking a row: (a) a slug already rendered answers in 150–165 ms (x-vercel-cache HIT, x-nextjs-prerender 1, x-nextjs-stale-time 300); (b) a never-rendered slug costs 760–950 ms (the ● routes have generateStaticParams() { return [] } so every first visit renders on demand in sfo1 and the operator sits in iad1); (c) during those ~900 ms NOTHING on screen changes — a MutationObserver on document.body saw zero mutations until the whole detail arrived at 893 ms — the segment loading.tsx never shows on client navigation to an un-prefetched static route; (d) only 6 of 12 visible row links had an _rsc prefetch entry; (e) after the detail renders, four requests fire: /api/obligations/upcoming?itemId= TWICE (a duplicate), /api/detail/relevance, /api/obligations/register?itemId=, 400–670 ms each; (f) scrolling to the bottom of /regulations fires NO /api/listings/cursor request (the IntersectionObserver sentinel does not trigger); only the "Load more" button does, and one click fetched four pages in a row; (g) the Awareness band renders "0 shown 169 — Loading 169 regulations…" permanently, because the bands are priority-ordered and the cursor pages priority-first, so that band receives nothing until roughly page 37 — a loading state with nothing loading (docs/design/ux-laws.md: no surface renders empty or false while loading).

READ FIRST, IN FULL: docs/decisions/ADR-027-standard-fast-page-architecture.md, ADR-026, docs/audits/perf-waterfall-2026-09-04.md, fsi-app/src/app/regulations/[slug]/page.tsx (and the market/operations/research siblings), their loading.tsx files, fsi-app/src/app/regulations/page.tsx, fsi-app/src/components/regulations/RegulationsLedger.tsx, fsi-app/src/components/ledger/VirtualizedRowList.tsx, fsi-app/src/lib/hooks/useInfiniteScrollSentinel.ts, useLedgerInfiniteQuery.ts, useNearestScrollParent.ts, fsi-app/src/lib/data.ts (getPublic* and the PUBLIC_ITEMS_TAG revalidation), fsi-app/src/lib/detail/** (load-detail-core.ts), the client components on the detail page that call /api/obligations/upcoming and /api/detail/relevance, fsi-app/next.config.*, fsi-app/src/lib/perf/perf-budget.mjs, and the Next.js 16 documentation for generateStaticParams/dynamicParams, Link prefetch (Next 16: prefetch modes, hover behaviour, the prefetch budget), loading.tsx behaviour on client navigation to static vs dynamic segments, and the client router cache staleTimes — cite the doc pages you relied on. Confirm with Supabase MCP how many verified, non-archived items exist per surface (the four public RPCs are live: get_workspace_intelligence_slim_public etc.).

BUILD, in this order, each proven:
(1) EVERY CLICK AN EDGE HIT. generateStaticParams for the four [slug] routes returns every verified non-archived slug from the public RPC (through the same data.ts path, one function, no copy), dynamicParams stays true for new items, revalidateTag(PUBLIC_ITEMS_TAG) on mint already covers freshness — measure the build (next build --webpack: page count and wall time; if the build exceeds ~10 minutes, say so with the numbers and pick the standard alternative: ISR with a warm step that requests every slug once after deploy from the deploy hook / a post-merge workflow job that walks the sitemap, no schedule). The 1-minute revalidate on the four index pages and the detail routes: justify or change it — with tag revalidation on mint, a long revalidate (hours) plus stale-while-revalidate is the standard; cite what Next serves while revalidating.
(2) NOTHING FROZEN ON CLICK. Make the navigation paint immediately: prefetch must cover every visible row (Next 16 Link prefetch mode / router.prefetch on pointerenter and on viewport entry via the virtualizer's rendered rows; measure that all 12–17 rendered rows have an _rsc entry), AND the segment's loading.tsx must appear on a navigation whose payload is not yet in the router cache (find out why it does not today — static segments' loading boundaries and prefetch: cite the Next doc/issue; if the fix is making the detail page's data segment stream behind a Suspense boundary inside a static shell, do that: the shell (masthead, title from the listing row the user just clicked, passed via the router or read from the cached listing) paints at once and the body streams). Prove with a MutationObserver script in Playwright: time from click to first DOM change < 100 ms and time to full content.
(3) The duplicate /api/obligations/upcoming call on the detail page: one caller, or lift the data into the RSC payload if it is public.
(4) The scroll sentinel: find why the IntersectionObserver never fires on /regulations (root element? the sentinel sits inside a collapsed band? zero-height?), fix it, and prove with Playwright that scrolling to the list foot fires exactly one cursor page per viewport reached, not four at once and not zero.
(5) The band state: a band whose rows have not arrived shows its true count and a control ("Load N…" / "Jump to Awareness") or is collapsed with the count, never "Loading N…" while no request is in flight; "Loading" appears only while isFetchingNextPage. Alternatively page per band (cursor + a band filter, which the public RPC can take as a priority parameter — if you add one, edit migration 306's successor as a NEW migration 307 that REPLACES the function signature (DROP the old signature after CREATE, GRANT, assert one overload), written not applied). Choose the design that gives the operator rows-as-you-scroll in every band and say why.
(6) fsi-app/src/lib/perf/perf-budget.mjs: add budgets for click-to-first-paint and click-to-content on the detail route, measured, and for cursor-page bytes; tighten documentBytes for regulations-list to the measured 277 KB decoded.

MEASURE with Playwright chromium (this container has it at /opt/pw-browsers) against next start of your build with placeholder env where possible, and by reading; the coordinator re-measures live after landing. Commit in logical commits.`, { label: 'PERF-13', phase: 'Lanes', model: 'sonnet' })

const B = agent(`${C('FE-DEDUP','fededup')}

THE DEFECT [CONFIRMED by the coordinator, Supabase MCP 2026-09-04 23:22 UTC]: public.obligations has 1,149 rows but only 562 distinct (intelligence_item_id, event_kind, due_date); 359 duplicate groups. Each duplicate pair is two item_forward_events rows for the same item, same event_kind, same event_date, same obligation_text and same source_span ("14 April 1967"), created in the same extraction run (identical created_at 2026-09-04 13:09:42.772303+00, extractor_version fe1-2026-09-04.3): one with source_kind 'claim' (confidence high, source_claim_id set) and one with source_kind 'section' (confidence medium, source_section_id set). Example item 02470d94-abe6-4645-8f5e-6ae421f29393: events a4ad1ce7 (section) and ca126684 (claim). The obligations register on /regulations therefore shows every row twice (e.g. "EUR-Lex — 32001D0119 · Entry into force · January 1, 2002" twice). obligations.obligations_forward_event_unique is on forward_event_id, so the duplication passes straight through derive-obligations.

READ FIRST, IN FULL: fsi-app/src/lib/forward-events/extract-forward-events.mjs (it already has a dedupe pass around line 1580 keyed on event_date|event_kind with sameObligationContent() and a claim-over-section preference — find out why it did not catch these twins: is sameObligationContent tripped by the ellipsis/leading "…", are claim and section events extracted in separate passes that never meet the dedupe, does the retext path (fsi-app/scripts/maintenance/forward-events-retext.mjs, lane FWD-TEXT-4) insert without running it? reproduce with the live pair's texts in a unit test), read-and-extract.mjs, fsi-app/scripts/forward-events/run-extraction.mjs, fsi-app/scripts/maintenance/forward-events-retext.mjs, fsi-app/scripts/obligations/derive-obligations.mjs AND fsi-app/scripts/maintenance/derive-obligations.mjs (two files with the same name — determine which is the one writer per fsi-app/docs/inventories/shared-dataset-ownership.md and whether the other is a copy; if it is a copy, delete it and repoint), fsi-app/supabase/migrations/2xx item_forward_events migration(s) and 290_obligations.sql, fsi-app/src/lib/obligations/read-register.mjs, docs/specs/01-*.md (the obligations spec), docs/runbooks/FORWARD-EVENTS-RUNBOOK.md or whichever runbook governs, fsi-app/scripts/harness-runs/forward-events/ (latest artifacts, PENDING-RUN/LAST-PROPOSER-PASS) and governing-files.mjs for the forward-events family.

MEASURE FIRST with Supabase MCP: total item_forward_events, distinct (intelligence_item_id, event_kind, event_date, obligation_text), how many twins are exactly claim+section with the same span, how many are other shapes (different text, different span), and how many obligations rows hang off each side.

BUILD: (1) the extractor coalesces a claim-backed and a section-backed event for the same (item, kind, date) into ONE event that keeps the claim provenance and records the section as corroborating evidence (a field, if the schema has one, else the dropped-record log the existing pass already writes) — one dedupe, at the one point every extraction path (run-extraction, retext) goes through; unit tests with the live twin texts. (2) a unique index that makes the twin impossible: item_forward_events (intelligence_item_id, event_kind, event_date, md5(obligation_text)) or the natural key the spec names — WRITTEN as a new migration (next free number; check docs/inventories/migrations.md and the migrations directory: 306 is applied; 307 is unassigned), md5-guarded, with a pre-check that counts the duplicates it expects to be gone first and ABORTS if any remain (the index cannot be created over duplicates). (3) the one-time dedupe: a script through the existing guarded writer (db.mjs rule-015 path, one writer per dataset: forward-events' own writer) that, for each twin group, deletes the section-backed event and its obligations row(s) (or re-derives obligations after: read how derive-obligations handles a deleted forward event — obligations.forward_event_id has an FK; use the derivation, never a second writer), dry by default, --apply, a harness-run artifact for the forward-events family, bounded and idempotent, and the exact dispatch the coordinator runs (which workflow/step; if none exists, add the step to the maintenance or forward-events workflow, hand-only, no schedule). Expected result stated from your measurement: obligations 1,149 → ~562 (say the exact number). (4) fitness/closure: the forward-events F28 marker re-pinned if a governing file changed; runbook section; shared-dataset-ownership row if a new writer (avoid one).`, { label: 'FE-DEDUP', phase: 'Lanes', model: 'sonnet' })

const [a, b] = await Promise.all([A, B])
return { a, b }
