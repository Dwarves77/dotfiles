export const meta = {
  name: 'lane-cap1000',
  description: 'Sonnet lane CAP-1000: PostgREST max-rows (1,000) silently truncates every unranged read — 312 of 1,312 regulation slugs were never prerendered and the register says "60 of 1000" — one paging helper for every full-set read, filters pushed into SQL, exact counts; and a cache flush after every apply workflow so static pages stop serving pre-cleanup data for six hours',
  phases: [{ title: 'Lane' }],
}
phase('Lane')
const P = `You are lane CAP-1000 on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/cap1000 (branch lane/cap1000-2026-09-05, based on origin/master 84745fa3 = train 45, live); node_modules symlinked at fsi-app/node_modules; Node 24 at /home/claude/.npm/_npx/387698761821791d/node_modules/node/bin/node (dirname on PATH). Cannot push; coordinator lands. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. CLAUDE.md rules: no copies of logic; no dead code; no schedules; "there is NO small follow-up fix, fix it now"; [CONFIRMED]/[HYPOTHESIS]; read every file you touch IN FULL; commit message ends with "Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>". Supabase MCP read-only SELECT (project kwrsbpiseruzbfwjpvsp; ToolSearch "select:mcp__Supabase__execute_sql"). Gates from the repo root: node fsi-app/.discipline/fitness/runner.mjs (0 violations), node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs, node fsi-app/.discipline/governance/closure-gate.mjs, tsc --noEmit, bash fsi-app/.discipline/run-test-suite.sh (CI has no node_modules: react/next-importing tests are *.npmtest.mjs), python3 yaml.safe_load on every workflow you touch, node fsi-app/.discipline/consistency/override-check.mjs --range=origin/master..HEAD (C3 clean; ignore C4 worktree rows). If .tsx changes, a "UX compliance" block in your REPORT.

TWO DEFECTS, ONE CAUSE [CONFIRMED by the coordinator, live, 2026-09-05 01:30 UTC]. Supabase PostgREST enforces db-max-rows = 1000 per request: any supabase-js query without .range() paging returns at most 1,000 rows no matter what .limit(N) asks for. (1) fsi-app/src/lib/data.ts getPublicSurfaceSlugs (PERF-13, train 45) calls get_workspace_intelligence_listings_public with .limit(BUILD_TIME_SLUG_ENUM_LIMIT = 20000); the live corpus has 1,312 verified regulations; measured on carosledge.com: slug index 900 in the cursor order → x-vercel-cache PRERENDER (293 ms); indices 1000, 1050, 1200, 1311 → MISS (581–709 ms, rendered on demand). Exactly the first 1,000 were prerendered. (2) fsi-app/src/lib/obligations/read-register.mjs fetches OVERFETCH_CAP = 2000 obligations and filters jurisdiction/mode/due-window in JS; the register masthead read "60 of 1000 obligations" while the table held 1,141 (now 814 after FE-DEDUP's cleanup); the "total" was the truncated fetch's length. Both are the same bug class; find every other instance: grep fsi-app/src and fsi-app/scripts for .limit( with a literal > 1000, for full-set reads with no .range()/.limit() at all that expect the whole table (sitemaps, slug lists, backfills, exports, census, propagation drains, producers), and for counts derived from a fetched array's .length instead of { count: "exact", head: true }. List every site with its verdict (bounded by design / must page / must count exactly).

BUILD: (1) ONE shared paging helper in the place both src and scripts can import (read how src/lib/supabase-server.ts and scripts/lib/db.mjs already share code; scripts import .ts via Node type-stripping — follow the existing precedent, no second copy): fetchAllRows(buildQuery, { pageSize: 1000, max }) that walks .range(from, from+pageSize-1) until a short page, plus exactCount(query). Route every must-page site through it; getPublicSurfaceSlugs enumerates all 1,312 + 55 + 25 + 39 slugs (prove with a unit test using a fake client that returns exactly 1,000 rows per page). (2) read-register: push jurisdiction/mode/binding_position/due-window filters into the query builder (PostgREST filters, .contains/.overlaps for the array columns, .gte/.lte for the window) with exact count on the same filtered query, .range() for the page — no JS overfetch, no OVERFETCH_CAP; keep the response shape /api/obligations/register and the SSR first page already consume; tests for each filter shape. (3) A fitness function or governance test that fails on any new .limit(<literal > 1000>) or any full-set read outside the helper (read how F-functions are registered in fsi-app/.discipline/fitness/runner.mjs and the manifest; allowlist nothing you did not fix, with expiry train 46). (4) CACHE FLUSH AFTER EVERY APPLY. Today only scripts/mint/apply-mint-batch.mjs calls revalidateTags (scripts/lib/revalidate.mjs → /api/revalidate with APP_URL/WORKER_SECRET, both already in maintenance.yml's env). Maintenance #47 (forward-events-retext apply) deleted 331 forward events and 331 obligations at 01:20 UTC and /regulations (static, PUBLIC_ITEMS_REVALIDATE_SECONDS 6 h) still served the pre-cleanup register at 01:35. Add ONE workflow-level step (a small CLI over the existing revalidateTags: scripts/lib/revalidate-cli.mjs, tested) that runs after any apply-mode job in maintenance.yml, corpus-turn.yml, propagation-drain.yml, producers.yml and population-turn.yml (read each: which of their steps write rows the public caches read — intelligence_items, obligations, item_forward_events, market_series, entities; source-sweep and ledger-consume export do not) and flushes APP_DATA_TAG + PUBLIC_ITEMS_TAG (+ the surface-detail tags if the step names items; if it cannot, the two list tags and say so). Idempotent, best-effort with the same "no APP_URL: say so" contract revalidate.mjs already has; the dispatch-ledger/closure-gate NEVER-RUN table must not gain a new never-run step (it is a step inside existing jobs, not a new workflow). Record in docs/runbooks/MAINTENANCE-RUNBOOK.md (one section) and docs/decisions/ADR-026 or ADR-027 (an amendment note: revalidation is a property of every apply, not of mint alone).

REPORT: the audit table (every site, verdict, fixed or bounded-by-design with the number), files, tests, the slug counts the enumeration now returns per surface (from the fake-client test and from Supabase MCP counts), gate lines, commit hashes.`
return { c: await agent(P, { label: 'CAP-1000', phase: 'Lane', model: 'sonnet' }) }
