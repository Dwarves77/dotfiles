export const meta = {
  name: 'lane-w71close',
  description: 'Sonnet lane W7.1-CLOSE: master is red because every F25/F38 allowlist entry with expiry 46 tripped when train 46 landed; close the W7.1 ratchet for real (wire by construction, or delete with tests and inventory rows), no re-grants',
  phases: [{ title: 'Close' }],
}
phase('Close')
const P = `You are lane W7.1-CLOSE on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/train47, branch train/wave47-2026-09-05 (the assembled train 47: origin/master 012b10a2 content + eleven wave-F lanes, already folded and gated by lane ASSEMBLE-47; read its last commits with git log --oneline -20). node_modules symlinked at fsi-app/node_modules; Node 24 at /home/claude/.npm/_npx/387698761821791d/node_modules/node/bin/node (dirname on PATH). Cannot push; the coordinator lands. Main clone /root/work/dotfiles is read-only for you. CLAUDE.md rules bind: no copies of logic; no dead code; no schedules; $0; "there is NO small follow-up fix, fix it now"; [CONFIRMED]/[HYPOTHESIS]; read every file you touch IN FULL before editing; commit message ends with "Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>". Read first: CLAUDE.md, docs/plans/complete-system-build-plan-2026-09-04.md §0 and §W7.1, fsi-app/.discipline/fitness/functions/F25-module-liveness.mjs IN FULL (findDispatchRoots, latestTrainWave, the W7.1 allowlist block and its w/d/o helpers), F38-unbounded-supabase-read.mjs, docs/audits/wiring-audit-2026-09-04/B1-modules.md, fsi-app/.discipline/run-test-suite.sh, docs/inventories/ (find which inventory lists scripts, e.g. scripts inventory / shared-dataset-ownership), and docs/ops/session-log.md's last addendum + postscripts 57 and 58.

THE DEFECT [CONFIRMED by the coordinator, reproduced on a worktree at origin/master 012b10a2]: the Discipline engine is RED on master (push run 33944304088, jobs "Fitness functions" and "Discipline engine unit tests"): 66 F25 "ALLOWLIST ENTRY EXPIRED ... (expiry wave46) has passed (latest landed: wave46)" plus 1 F38 (fsi-app/scripts/verify/mint-gate-calibration.mjs .limit(8000), expiry 46). latestTrainWave parses origin/master's log for the newest train/waveNN commit and expiry trips at latestWave >= expiry; train 46 (#593) landed as train/wave46, so every expiry-46 entry tripped at once. On THIS worktree the W7.1 lane (commit e0fc5c18) already closed 14; run node fsi-app/.discipline/fitness/runner.mjs first and paste the current list (coordinator's count on this tree: 52 F25 + 1 F38 + 1 F14; the F14 may be ASSEMBLE-47's own leftover, check it and fix it too if it is real).

FORBIDDEN: re-granting any entry to a later wave. The coordinator did that twice (41 → 43 → 46); the rule text itself calls it "a permanent exemption wearing a temporary label". Every remaining entry gets exactly one of: reachable by construction, wired to a real caller, or deleted. Three classes, one rule each:

(A) REACHED BY A REGISTRY F25 DOES NOT READ. fsi-app/.discipline/governance/pretooluse-skill-gate.mjs, worktree-isolation-hook.mjs, check-pretooluse-wired.mjs, wire-pretooluse-settings.mjs, fsi-app/.discipline/consistency/runner.mjs, fsi-app/.discipline/install-hooks.mjs, fsi-app/.discipline/dispatch/start.mjs, dispatch/audit.mjs. Establish [CONFIRMED] for each what invokes it (grep .claude/settings.json, .claude/settings.local.json if tracked, fsi-app/.claude/**, .githooks/**, .husky/**, package.json scripts, the pre-push/pre-commit hook sources install-hooks.mjs writes, CLAUDE.md, skills' SKILL.md). For every one that IS invoked by a tracked registry, extend findDispatchRoots with ONE new source per registry kind (e.g. "Source N: .claude/settings*.json hook commands" and "Source N+1: hook scripts written by install-hooks.mjs") so that whole class is reachable by construction, with a unit test per source in F25's test file; then delete their allowlist entries. Any of the eight that nothing tracked invokes is class (C).

(B) SPEC09 PRODUCERS: fsi-app/scripts/spec09/surcharge-audit-producer.mjs, indexation-producer.mjs, dqi-producer.mjs, auxiliary-energy-producer.mjs, run-fixture-import.mjs (and any other spec09 module still listed). Lanes SPEC09-A (d33d77bb, a3d6199c) and SPEC09-B (b4998428) on this branch built the rows-file/CSV-upload mechanism; read fsi-app/scripts/spec09/SOURCES.md and their runbook section. If a producer is now invoked by a workflow step or the CSV upload route, prove it (the run: line / import chain) and delete the entry; if it is not, wire it the same way those lanes wired the others (a maintenance.yml step selected by the existing step input, with a fixture-backed dry run in its test) or delete it with its test and inventory row. No third state.

(C) DATED ONE-SHOTS AND EVERYTHING ELSE: scripts/verify/* (wave-acceptance-audit, surface-visibility-audit, stale-verified-audit, remediate-reclassify-proposal, remediate-orphan-sources, mint-gate-calibration, defect-signature-scan, cleanup-dup-sources, audit-finding-status, admin-phrase-scan), sprint4-114-spancheck-test, source-state-min-wage, source-role-cleanup, seed-community-regional-rooms, run-4c-relabel, apply-4c-plan, remediation/refetch-capped-worklist, remediation/acquire-primaries-batch, regen-quarantined, recovery-measure, holdings-audit, gen/migration-258, funded-pass, connections/backfill-edges, canonical-pipeline-proof, audit-optionc-reachability, _wave-alpha/backfill-canonical-keys, _ruling/null-tier-host-ruling, _reground/* (7), _diag/_pdf-probe, _dataops/interlock, src/lib/contracts/provenance-envelope.mjs. For each: grep the whole tree for importers and invocations (src, scripts, .github/workflows, docs runbooks, package.json, skills). A module with a LIVE importer that is itself reachable is not dead: fix the reachability chain (usually the importer is the real gap) and delete the entry. A module with no live importer is DELETED in this lane together with its test file(s), its inventory rows, its runbook/INDEX references, and any docs line that presents it as an available tool (turn those into a one-line "retired in train 47, see git history" note only where a runbook step would otherwise dangle). Keep the git history as the archive; do not move files to an _archive folder. provenance-envelope.mjs is a contract module: if src/ consumes the same contract via another path, dedupe to one module; if nothing consumes it, delete it. F38: mint-gate-calibration goes with its class; if it survives (it should not unless a live caller exists), page it through fetchAllRows.

Also run node fsi-app/.discipline/governance/closure-gate.mjs and the F23 orphan-proof ratchet after deletions: a deleted script's test must leave run-test-suite.sh's named lists and any coverage-report.json entries (regenerate with the repo's generator). Update fsi-app/.discipline/governance/orphan-modules.mjs baselines only through their own regeneration path if one exists, never by hand-editing counts.

MEMORY: append postscript 59 to docs/ops/session-log.md under the last addendum (first person, same format as 57/58): the root cause as stated above, that the coordinator's two re-grants caused it, the disposition table (file → A/B/C → wired-by-registry / wired-to-step / deleted, one line each), and the rule now in force: F25/F38 entries are granted only with a named lane and never re-granted. Update the docs/PROGRAM-BOARD.md W7.1 row to closed with the evidence. No .tsx should change; if one does, add the UX compliance block.

GATES from the repo root, all clean before you report: node fsi-app/.discipline/fitness/runner.mjs (0 violations, that is the point); node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs; node fsi-app/.discipline/governance/closure-gate.mjs; bash fsi-app/.discipline/run-test-suite.sh with the node_modules symlink temporarily removed (restore it) and with it present; node fsi-app/.discipline/consistency/override-check.mjs --range=origin/master..HEAD (C3 clean); cd fsi-app && npx tsc --noEmit; python3 yaml.safe_load on every workflow touched; node fsi-app/.discipline/governance/invariant-coverage.mjs; cd fsi-app && npx next build --webpack with no Supabase env (exit 0).

REPORT: the disposition table, the F25 sources added, files deleted (count and list), every gate's output line, build exit code, commit hash(es), anything you could not disposition and why (there should be none).`
return { w: await agent(P, { label: 'W7.1-CLOSE', phase: 'Close', model: 'sonnet' }) }
