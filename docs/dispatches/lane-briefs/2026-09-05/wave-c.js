export const meta = {
  name: 'wave-c',
  description: 'Plan T40 lanes: F25-WIDE (module liveness over scripts/** + repo-wide orphan check, allowlist from the audit) and CLOSURE-GATE (CI fails on never-run steps, aged NEXT board rows, writer-without-reader tables, lane briefs without §0)',
  phases: [{ title: 'Lanes' }],
}
phase('Lanes')
const C = (name, wt) => `You are lane ${name} on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/${wt} (branch lane/${wt}-2026-09-04, based on origin/master e8cb748f); node_modules symlinked at fsi-app/node_modules. Cannot push; coordinator lands. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. READ FIRST: docs/plans/complete-system-build-plan-2026-09-04.md on branch train/wave37-2026-09-04 in worktree /root/work/lanes/train37 (§0 definition of done; W7), and docs/audits/wiring-audit-2026-09-04/B1-modules.md (same worktree; its method, Appendix A (66 test-only modules with the in-CI flag), Appendix B (19 dead exports)). CLAUDE.md rules: no copies of logic; "there is NO small follow-up fix, fix it now"; [CONFIRMED]/[HYPOTHESIS]; read every file you touch IN FULL; commit message ends with "Co-Authored-By: Claude Sonnet <noreply@anthropic.com>". Gates: node --test over every test you touch; repo-root node fsi-app/.discipline/fitness/runner.mjs (your new/changed function must be green on THIS tree, with an allowlist that names every current offender with its disposition and an expiry so the ratchet only tightens) and node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs; wire new tests into fsi-app/.discipline/run-test-suite.sh (F23). The meta-harness family governs F28 only; if you touch F28 or governing-files.mjs, chain the meta-harness marker. Report: files, exported functions, the allowlist with dispositions, tests, fitness line, commit hash, anything refused with the reason.`

const C1 = agent(`${C('F25-WIDE','f25wide')}

Write set: fsi-app/.discipline/fitness/functions/F25-module-liveness.mjs (+test), fsi-app/.discipline/governance/coverage-scan.mjs (+test) if the import-graph builder lives there, a NEW fsi-app/.discipline/governance/orphan-modules.mjs (+test): the audit's own method (real import graph via F25's buildImportGraph/resolveSpecifier, plus every .github/workflows/*.yml run: line and package.json scripts as dispatch roots, plus esbuild alias tables in .discipline/rendering as roots) that lists every non-test module under fsi-app/src, fsi-app/scripts, fsi-app/.discipline whose only importers are tests and that no workflow dispatches; fsi-app/.discipline/run-test-suite.sh; fsi-app/.discipline/fitness/runner.mjs only to register; docs/doctrine or the fitness README where functions are catalogued. BUILD: (1) F25's scope widened from src/** + scripts/lib/** to all of fsi-app/scripts/** and fsi-app/.discipline/**, with dispatch-root awareness (a scripts/turns/run-*.mjs driver invoked by a workflow run: line is wired even with zero importers; the audit's B1 §Method explains this and the two blind spots it corrected: esbuild alias tables and hand-run scripts with artifacts); (2) the allowlist: every module the widened check flags today, each with disposition {wire: <plan lane/train>, delete: <train>, one-shot: <reason>} and an expiry train number, taken from Appendix A's "no" rows and the plan's W7.1 list (propose-classifications, generate-theme-brief, ratify-flag-to-census, migration-267/268/271 generators, held-classes, verification-audit-report, build-oil-bulletin-rows/ratify-series-items, assumption-register-seed, six spec-09 producers, skill-contract-map, the four review/apply-* (being wired now by lane REVIEW-WIRE), consume-turn-requests (being wired by lane TURNREQ)); the check fails when an allowlisted module's expiry passes (expiry = a train number compared with the latest train/wave number in git log --oneline origin/master | grep -o "wave[0-9]*"); (3) the orphan-modules check runs in CI (discipline.yml via run-test-suite.sh or the fitness runner; read how F23 is wired and match it); (4) dead exports: a second mode of the same check listing exports with zero references outside their file (Appendix B's method), reported (not failing) this train, with the 19 named as its first output. Tests with a fixture tree.`, { label: 'F25-WIDE', phase: 'Lanes', model: 'sonnet' })

const C2 = agent(`${C('CLOSURE-GATE','closure')}

Write set: a NEW fsi-app/.discipline/governance/closure-gate.mjs (+test), fsi-app/.discipline/run-test-suite.sh, fsi-app/.discipline/fitness/runner.mjs or discipline.yml only to register (read how the memory gate and F23 are wired; the closure gate is a governance check that runs in CI on every train), docs/dispatches/lane-common-contract.md (append §0 of the plan as the definition of done every lane brief must carry), docs/doctrine/<the doctrine file that lists governance gates> (one entry). THE DEFECT [CONFIRMED, plan §"Why the previous plans stopped short"]: nothing fails when a maintenance step or workflow has never run, when a docs/PROGRAM-BOARD.md row stays NEXT across trains, when a migrated table has a writer and no reader, or when a lane brief lacks the done-conditions; 12 NEXT rows and 19 "dispatch next" lines accumulated; the 2026-08-31 register's dispositions were half executed. BUILD the closure gate with four checks, each with a fixture test and a live run on this tree whose current offenders are listed (allowlisted with disposition and expiry train, ratchet-only): (1) NEVER-RUN: every maintenance.yml step name (parse the workflow's step list / the case statement) and every dispatchable workflow must have evidence of a run: a harness-run artifact under fsi-app/scripts/harness-runs/<family>/ OR a docs/runbooks/MAINTENANCE-RUNBOOK.md "dispatched" record OR an entry in a NEW machine-readable dispatch ledger docs/ops/dispatch-ledger.jsonl {date, workflow, step, mode, run_id, outcome} that the coordinator appends per dispatch (define the shape; seed it from today's session-log postscripts 45-47 and the audit A1 evidence); a step with none after its introduction train + N (N=3) fails; (2) STALE-NEXT: docs/PROGRAM-BOARD.md rows whose status cell is NEXT/"next:" older than N trains without an owning train name fail (parse the board; the row's date and the train references it carries); (3) WRITER-READER: for every table created by a migration in fsi-app/supabase/migrations since 266, grep src/ and scripts/ for .from("<table>") / rpc names: a table with writers and no readers, or readers and no writers, fails unless allowlisted with the plan item that closes it (B2 §4's list is the seed: assumption_register, entity_scope, statutory_computations, estimated_values, aggregate_query_log, community_promotion_transitions, carrier_compliance_pools, indexation_clauses …); (4) LANE-CONTRACT: docs/dispatches/lane-common-contract.md contains §0 verbatim (a marker string) so briefs referencing it inherit it. Output: a summary line per check and a non-zero exit on any unallowlisted failure. Also a --report mode printing the full table for the coordinator's train memory.`, { label: 'CLOSURE-GATE', phase: 'Lanes', model: 'sonnet' })

const [c1, c2] = await Promise.all([C1, C2])
return { c1, c2 }
