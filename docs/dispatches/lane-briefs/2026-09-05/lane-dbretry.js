export const meta = {
  name: 'lane-dbretry',
  description: 'Haiku lane DB-RETRY: the first backlog flywheel apply died on a transient "TypeError: fetch failed" inside db.mjs\'s snapshot read after analyze-corpus had already written 932 edges; every guarded read/write in db.mjs gets a bounded retry on transient transport errors',
  phases: [{ title: 'Lane', detail: 'DB-RETRY in its own worktree' }],
}
phase('Lane')
const P = `You are lane DB-RETRY on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/dbretry (branch lane/dbretry-2026-09-04, based on origin/master 4c90c5bb); node_modules symlinked at fsi-app/node_modules. Cannot push; coordinator lands. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. Write set: fsi-app/scripts/lib/db.mjs and its test file (find it: scripts/lib/db.test.mjs or the nearest existing test of db.mjs; if none exists, create scripts/lib/db.test.mjs with fakes only, no network), and one paragraph in docs/runbooks/POPULATION-TURN-RUNBOOK.md's backlog section. Nothing else. Deterministic; read db.mjs IN FULL first (it is the ONE guarded write path every script uses: rule 015 snapshots, cites, chunked updates with statement-timeout halving; do not change any contract); no claims ahead of evidence; commit message ends with "Co-Authored-By: Claude Haiku <noreply@anthropic.com>". Gates: node --test on db's tests; from repo root node fsi-app/.discipline/fitness/runner.mjs (0 violations) and node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs; then bash fsi-app/.discipline/run-test-suite.sh (0 fail).

THE EVIDENCE [CONFIRMED, Population turn #24, Actions 33847066554, the first flywheel_backlog apply, 2026-09-04 07:05 UTC]: over mint-run-004, discovery ran, forward-event extraction wrote 6 item_forward_events rows, analyze-corpus --signals loaded 1,009 items / 6,545 edges, persisted 38 themes, opened 20 gaps, wrote 932 signal edges and auto-resolved 1 flag, then died with "analyze-corpus: FAILED — db.mjs snapshot read failed: TypeError: fetch failed" (db.mjs line ~175, guardedUpdate's prior-state read), so the flywheel step exited 1, the artifact got no §9 outcomes, and the second artifact was never processed. "TypeError: fetch failed" is Node's undici transport error (connection reset, DNS blip, TLS handshake), not a PostgREST error: it has no .error body, it rejects the promise (or supabase-js surfaces it as error.message "fetch failed"; check both forms). BUILD: a single bounded retry helper in db.mjs (export it: withTransientRetry(fn, {attempts:3, delaysMs:[500,2000,5000], label})) that retries ONLY transient transport failures (message matches /fetch failed|ECONNRESET|ETIMEDOUT|EAI_AGAIN|socket hang up|ECONNREFUSED|UND_ERR/i, or a 502/503/504 status when supabase-js exposes one) and never retries a PostgREST/Postgres error (statement timeout stays with the existing chunk-halving logic, constraint violations fail immediately). Wrap every network call in db.mjs: readAll pages, the prior-state snapshot reads in guardedUpdate/guardedUpdateByIds/guardedDelete/archiveRows, and the writes themselves ONLY where a retry is idempotent (an UPDATE with the same patch and an upsert are; a plain INSERT is not, so for guardedInsert/guardedInsertMany retry the read side only and say so). Log each retry to stderr with the label and attempt. Tests with a fake client: rejects once with TypeError('fetch failed') then succeeds → result returned, one retry logged; rejects with a PostgREST error → no retry; three transient failures → throws the last error with attempts count. Report: the call sites wrapped and the ones deliberately not, tests, fitness, full-suite line, commit hash.`
const r = await agent(P, { label: 'DB-RETRY', phase: 'Lane', model: 'haiku' })
return { dbretry: r }
