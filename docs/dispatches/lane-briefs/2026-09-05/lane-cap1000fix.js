export const meta = {
  name: 'lane-cap1000fix',
  description: 'Sonnet lane CAP-1000-FIX: build-proof CI (no Supabase credentials) fails because generateStaticParams now throws through the fail-closed paging helper; the enumerator must return [] with a logged reason when no service key exists, and still fail the build when credentials exist and a page fails',
  phases: [{ title: 'Lane' }],
}
phase('Lane')
const P = `You are lane CAP-1000-FIX on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/train46 (branch train/wave46-2026-09-05, = origin/master 84745fa3 + CAP-1000 + memory; it is PR #593, open, CI failing); node_modules symlinked at fsi-app/node_modules; Node 24 at /home/claude/.npm/_npx/387698761821791d/node_modules/node/bin/node (dirname on PATH). Commit ON THIS BRANCH (the coordinator pushes). Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. CLAUDE.md rules: no copies of logic; no dead code; "there is NO small follow-up fix, fix it now"; [CONFIRMED]/[HYPOTHESIS]; read every file you touch in full; commit message ends with "Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>".

THE FAILURE [CONFIRMED, PR #593 check "Build proof (deployed-bundle gate)/next build", run 33940507368]: the build-proof job runs \`npx next build\` with real node_modules but NO Supabase credentials; since CAP-1000 (commit 47537584 on this branch), getPublicSurfaceSlugs → fetchAllRows (src/lib/db/paginate.mjs, fail-closed) throws "paginated read failed at offset 0: Data temporarily unavailable. Refresh to retry." inside generateStaticParams for /regulations/[slug] and the build aborts ("Failed to collect page data for /regulations/[slug]"). Before CAP-1000 the enumerator logged and returned [] in that environment. The market/operations/research category RPCs already log and continue ("[category-routing] get_market_intel_items_public failed: SUPABASE_SERVICE_ROLE_KEY is not configured").

READ IN FULL: fsi-app/src/lib/data.ts (getPublicSurfaceSlugs, fetchAllPublicListingSlugs), fsi-app/src/lib/db/paginate.mjs (+test), fsi-app/src/lib/supabase-server.ts (getServiceSupabase's fail-closed error and runCategoryRpcPublic), the four [slug]/page.tsx generateStaticParams, .github/workflows/build-proof.yml (how the job runs, which env it lacks), fsi-app/src/lib/data-public-surface-slugs.test.mjs.

FIX, one rule in one place: generateStaticParams enumerates fully when a service role key is configured and FAILS THE BUILD if any page of the enumeration fails (that is the CAP-1000 guarantee: never silently prerender a truncated set); when NO service key is configured (build-proof CI, local placeholder builds) it logs one line naming the reason and returns [] so the routes stay dynamicParams-served. Detect "no credentials" from the same predicate getServiceSupabase uses (import it or its check; do not re-implement env parsing), never by catching every error. Extend data-public-surface-slugs.test.mjs: (a) with credentials and a 1,000-row-per-page fake client all slugs enumerate, (b) with credentials and a page failure the call throws, (c) without credentials it returns [] and logs. Then prove: \`npx next build --webpack\` from fsi-app with NO Supabase env (unset NEXT_PUBLIC_SUPABASE_URL/SUPABASE_SERVICE_ROLE_KEY; check .env.local is absent or neutralised for the run) exits 0 and the route table shows the four [slug] routes ●. Gates from the repo root: node fsi-app/.discipline/fitness/runner.mjs (0), node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs, tsc --noEmit, bash fsi-app/.discipline/run-test-suite.sh. REPORT: the predicate used, the test results, the build's exit code and route lines, gate lines, commit hash.`
return { f: await agent(P, { label: 'CAP-1000-FIX', phase: 'Lane', model: 'sonnet' }) }
