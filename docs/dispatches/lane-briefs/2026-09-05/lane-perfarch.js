export const meta = {
  name: 'lane-perfarch',
  description: 'Sonnet lane PERF-ARCH: instrument the whole request path (Server-Timing per phase, cold and warm, every route), then write ADR-027: the standard, already-solved architecture for fast pages on Next.js 16 + Vercel + Supabase (static shell, tag-invalidated cache, one screen of rows by cursor, virtualized lists, per-user layer after paint, co-located pooled DB, budget gate in CI), mapped line by line onto this codebase, using the established libraries, not custom mechanisms',
  phases: [{ title: 'Lane' }],
}
phase('Lane')
const P = `You are lane PERF-ARCH on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/perfarch (branch lane/perfarch-2026-09-04, based on train/wave41-2026-09-04 = master + sfo1 region); node_modules symlinked (no npm install; if a library is needed, name the exact package and version for the coordinator to add, do not install). Cannot push. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. Two lanes run in parallel and you must not overlap their write sets: PERF-10 (cacheComponents / static eligibility: src/app/layout.tsx, next.config, src/lib/detail/**) and PERF-11 (listing payload: the four listing pages, their ledgers, data.ts/supabase-server.ts listing projection, a possible migration 305). YOUR write set: fsi-app/src/lib/perf/** (NEW: the Server-Timing instrumentation, one small module), fsi-app/src/middleware.ts or proxy.ts ONLY to emit the Server-Timing header if that is the right place (read PERF-2's precedent), the API route handlers under fsi-app/src/app/api/** only to add the timing wrapper, fsi-app/.discipline/fitness/functions/F37-<perf-budget>.mjs (+test, registered in manifest.mjs: read F36 for the format), docs/decisions/ADR-027-<slug>.md, docs/audits/perf-waterfall-2026-09-04.md (the measurement), a UX compliance block if any surface changes (none expected). Rules: [CONFIRMED]/[HYPOTHESIS]; measure, do not assume; the OPERATOR'S INSTRUCTIONS (2026-09-04, verbatim): "clicking into any item or any page takes WAY too long … every click should show items on a page instantly"; "you dont load every item at once, basic 101 developing"; "instead of fixing how the pages load youre trying to live with the problem and increase my spending to fix poor developing" (no spend-based fixes: no CPU tier, no provisioned concurrency, no cron pings); "think like the top .01% developers, what would they do to diagnose the FULL problem … and then fix it"; "these solutions have been developed already, you dont need to reinvent a fix, find out how others do it and do the same." So: the STANDARD stack patterns, by name, with their documentation cited (use WebFetch on the official docs: nextjs.org/docs for Cache Components / "use cache" / revalidateTag / ISR / Suspense streaming / Link prefetch on Next 16; vercel.com/docs for Fluid Compute, regions, ISR/CDN cache, Server-Timing; supabase.com/docs for PostgREST .range() and keyset pagination, RPC projection, Supavisor pooling, read replicas; tanstack.com for TanStack Query and TanStack Virtual; the Next.js bundle analyzer / size-limit / Lighthouse CI for budgets), never a home-grown mechanism where a standard one exists; where the repo already has a home-grown version (the FIRSTPAGE remainder fetch, useWorkspaceBootstrap, unstable_cache wrappers in data.ts), say whether it IS the standard pattern or should be replaced by the library, with the trade-off.

PART 1, DIAGNOSE THE FULL PATH (this is the deliverable the operator asked for first): (a) read the whole request path for /regulations and /regulations/[slug] and /api/workspace/bootstrap: middleware/proxy → layout → page → data loaders → Supabase calls → serialisation → client hydration → post-render fetches; count the sequential round trips per request (getClaims → org_memberships → RPC → counts …) and name each with file:line; (b) instrument it: a tiny fsi-app/src/lib/perf/server-timing.ts (or .mjs) that accumulates named phases per request (AsyncLocalStorage or the request-scoped React cache() pattern, whichever the repo already uses for per-request state: read src/lib/api/server-bootstrap.ts) and emits a Server-Timing header on the document and API responses (Next 16: set it via the response in a route handler; for RSC pages, via middleware/proxy or the after() API if headers are already sent: read the docs and pick the sanctioned way), phases: coldstart (process uptime < 5 s at request start, from process.uptime()), auth, org, listing_rpc, counts, detail_core, serialize_bytes (JSON size of the props passed to client components), total; (c) the waterfall document docs/audits/perf-waterfall-2026-09-04.md: the FACTS the operator already established (getAppData 7,970 ms cold / 200–465 ms warm; /api/version 1,542 ms cold / ~300 ms warm; 118–130 routes all ƒ; every document private/no-store x-vercel-cache MISS; iad1 vs us-west-1 (fixed in train 41: sfo1); /regulations 886 KB decoded, 1,259 UUIDs, 401 KB of self.__next_f, 301 row elements for a 60-row first page; home 315 KB; DB idle: 17/60 connections, 0 lock waits) plus what your reading adds: the round-trip chain per route, the bytes per prop, where the 886 KB actually comes from (measure by rendering the page's props to JSON in a test if a local build cannot start), the cold-start composition (bundle size of the server function: read .next/server route sizes from a build if one runs; the number of modules the layout imports). Be exact and cite file:line for every claim.

PART 2, ADR-027 "The standard fast-page architecture": one page, decisions with the pattern name, the library or framework feature, the doc citation, and the exact mapping to this codebase (which file changes, which lane owns it: PERF-10, PERF-11, or a new lane you name), covering: static shell served from the CDN (Cache Components "use cache" or ISR + revalidateTag; state which and why for Next 16.1.6, consistent with ADR-026 and PERF-10's direction); one screen of rows by cursor (PostgREST .range()/keyset, TanStack Query for the client cache and infinite query, TanStack Virtual for the list when rows exceed ~100); query-side filter and projection (RPC/view with the row's fields only; migration 064's dashboard projection as the precedent); per-user layer after first paint from one call (the bootstrap route PERF-9 built: is it the standard pattern? yes/no); one round trip per screen (the sequential auth→org→RPC chain collapsed: how, e.g. the org id carried in the JWT claim or resolved once in middleware and forwarded as a header); DB co-location and pooling (Supavisor transaction mode: is the app using the pooler URL? read the env names in .env.example and the supabase client construction; say what to change); cold-start reduction without spend (smaller server bundles per route: what the layout imports; the sanctioned Next optimizePackageImports; no cron); and the CI budget (F37: document decoded size per listing route, self.__next_f bytes, sequential DB hops per request, all measured by the rendering harness or a build-output check, with the numbers from the waterfall as the initial ratchet and the targets: listing document < 200 KB, item click < 300 ms warm RSC, DCL < 1 s). Also list what PERF-1..9 did that should be KEPT and what should be REPLACED by the standard library (name each).

PART 3, BUILD what is yours: the instrumentation (tests with a fake request), F37 with its allowlist/ratchet (tests), and the audit + ADR. Gates: tsc, node --test over your tests and fsi-app/src/__tests__/*.mjs, fitness runner 0 violations (31 functions after F37), discipline governance tests, yaml if touched. Commit message ends with "Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>". Report: the round-trip chain per route (file:line), the waterfall numbers, the ADR's decision list with citations, the exact lane plan with owners, files, tests, fitness line, commit hash, anything refused with the reason.`
const r = await agent(P, { label: 'PERF-ARCH', phase: 'Lane', model: 'sonnet' })
return { perfarch: r }
