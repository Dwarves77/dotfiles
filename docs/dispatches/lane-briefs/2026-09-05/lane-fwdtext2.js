export const meta = {
  name: 'lane-fwdtext2',
  description: 'Sonnet lane FWD-TEXT-2: the retext dry run over 654 forward events still leaves 316 texts starting at a clause boundary (lowercase), 107 at a quote/paren, 65 with ** residue, 11 with a bare FACT: label, 8 with table pipes; finish the normaliser from the dry-run evidence before the apply',
  phases: [{ title: 'Lane', detail: 'FWD-TEXT-2 in its own worktree' }],
}
phase('Lane')
const P = `You are lane FWD-TEXT-2 on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/fwdtext2 (branch lane/fwdtext2-2026-09-04, based on origin/master 72c8dc44, which already carries lane FWD-TEXT's commit: clauseStart, normalizeObligationText, dedupeEvents in fsi-app/src/lib/forward-events/extract-forward-events.mjs and the maintenance step fsi-app/scripts/maintenance/forward-events-retext.mjs; read git log -3 -- those files and the FWD-TEXT commit in full first); node_modules symlinked at fsi-app/node_modules. Cannot push; coordinator lands. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. Write set: fsi-app/src/lib/forward-events/extract-forward-events.mjs (+ its test), fsi-app/scripts/maintenance/forward-events-retext.mjs (+ test), fsi-app/scripts/harness-runs/forward-events/PENDING-RUN.md (re-pin to the new hash in its existing format; F28 will name it), docs/runbooks/MAINTENANCE-RUNBOOK.md §12 text. Nothing else. Deterministic, $0, no LLM; "there is NO small follow-up fix, fix it now"; no claims ahead of evidence, [CONFIRMED]/[HYPOTHESIS]; read every file you touch IN FULL; the verbatim source_span law (assertVerbatim) is untouchable, obligation_text is the display field you are fixing; commit message ends with "Co-Authored-By: Claude Sonnet <noreply@anthropic.com>". Gates before commit: node --test on the two test files and the downstream consumers FWD-TEXT ran (run-extraction, apply-extraction-output, apply-staged-update, forward-event-format tests); from repo root node fsi-app/.discipline/fitness/runner.mjs (0 violations) and node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs (141 pass). The Supabase MCP (server "Supabase", tool execute_sql, project kwrsbpiseruzbfwjpvsp; load via ToolSearch "select:mcp__Supabase__execute_sql") is available for READ-ONLY SELECT; never write through it.

THE EVIDENCE, on disk: fsi-app/scripts/_snapshots/retext32.json is the dry-run summary of Maintenance #32 (run 33856356721, forward-events-retext dry, master 2f110fea = FWD-TEXT's code). It has retext_targets[] (654 entries: id, intelligence_item_id, event_date, event_kind, source_kind, before, after, defect_classes) and duplicate_groups (262, report only). The coordinator measured the AFTER texts [CONFIRMED, regex over the file]: 316 still start with a lowercase letter, 107 start with a non-letter (a quote or parenthesis), 65 contain a '*' (e.g. "Databases (Implementing Acts Deadline: 2 June 2030)** \\"No later than 2 June 2030, the Commission shall adopt…"), 11 start with a bare label without bold (e.g. "FACT: \\"To align with the Net Zero Emissions by 2050 (NZE) Scenario, emissions must fall by 15%…"), 8 still carry ' | ' table cells (e.g. "By 2030 | Potential methanol demand at Port of Singapore … | Indicativ"), 1 a URL tail ("Regulator announcement (Tier 2) | European Commission DG TAXUD | 14 January 2026 | https://taxation-customs."). A representative lowercase case: BEFORE "iving for 10%, of the energy used by international shipping by 2030." AFTER "penetration requirement well beyond IMO's 5–10% target by 2030, using WtW lifecycle framing;" — clauseStart snapped to the previous ';' inside a long list sentence, so the display text is still a fragment, and it ends with ';'. Good cases exist too ("This Decision shall apply from 1 July 2026 until 30 November 2026.", "From 1 April 2025, all bunker suppliers at the Port of Singapore are required to…"): the rule works when the sentence is a sentence.

MEASURE FIRST from retext32.json with a script: for every 'after' classify the residue (lowercase start, non-letter start, star residue, bare label, pipe, url, trailing ';' or ',' or no terminal punctuation, length distribution) and for a sample of 30 read the matching item's source text (intelligence_items.full_brief or the section content_md the row's source_kind names; read migration 274/275 for the columns and FWD-TEXT's readers) to see what a correct display text would have been. State the counts.

THEN FIX the producer so that obligation_text is a readable, self-contained unit: (1) the leading edge snaps to a SENTENCE start (after . ! ? followed by whitespace and an uppercase/quote/digit, or a paragraph/list-item start, or a markdown cell start), not to ';' or ':' — raise the look-back to what the measurement says (the 200-byte cap in sentenceStart exists for run-on paragraphs; keep a cap but measure what it needs to be); when the sentence start is beyond the cap, fall back to the nearest clause start and then CAPITALISE nothing and invent nothing: instead prefix an ellipsis character "…" so the reader sees it is a fragment (an honest fragment marker beats a fake sentence); (2) the trailing edge ends at a sentence terminator; if it ends at ';' or ',' or has no terminal punctuation because of the cap, append "…" the same way; (3) strip: bare labels FACT:/GAP:/MONITORING:/ANALYSIS:/ **label:** in any of the forms the corpus has (measure them), bold/italic markers anywhere in the text (never only at the edges), a leading citation-key token like "32026D1440*", table cells: when the text carries ' | ', keep the cell that contains the event's date span (source_span position tells you which) and drop the others, URL tails and bare URLs; collapse whitespace; (4) idempotence: normalizeObligationText(normalizeObligationText(x)) === normalizeObligationText(x), tested over every 'before' in retext32.json; (5) a property test over ALL 654 'before' texts: after the new normaliser, zero texts start with a non-letter other than a quote, digit, "(" or "…", zero contain '*' or ' | ' or 'http', zero end without terminal punctuation or "…"; print the remaining counts of lowercase starts (which should now be only the "…"-prefixed honest fragments) in the test output. (6) The retext step's dry report gains the residue classification of the AFTER text so the next dry run proves itself. Report: measurements before/after, the rules, files, exported functions, tests, fitness line, the marker hash, commit hash, anything refused with the reason.`
const r = await agent(P, { label: 'FWD-TEXT-2', phase: 'Lane', model: 'sonnet' })
return { fwdtext2: r }
