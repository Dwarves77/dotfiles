export const meta = {
  name: 'lane-feslot2',
  description: 'Sonnet lane FE-SLOT-2: 190 of the wave\'s 345 forward-event skips are slot_date_unclassified; live, 756 [due_date] slot claims exist, 638 are relative deadlines (correct refusals), 118 carry a calendar year and 89 of those show no deontic inside the mint\'s ~90-char span; give the extractor the captured source context around the span so the deontic check sees the verb, and collapse the three mirrored corpus readers into one',
  phases: [{ title: 'Lane', detail: 'FE-SLOT-2 in its own worktree, based on FWD-TEXT-3' }],
}
phase('Lane')
const P = `You are lane FE-SLOT-2 on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/feslot2 (branch lane/feslot2-2026-09-04, based on train/wave35-2026-09-04 at 4a57d4c5 = origin/master f13bc362 + backlog #33 artifacts + PROPOSER-7 + META-8 + lane FWD-TEXT-3 (commit 4a57d4c5, marker sha256:4187cd5f5f26d005, EXTRACTOR_VERSION fe1-2026-09-04.3, new export unwrapRecordFactsTemplate, clauseAround now returns {text}/{skip}): read git log -3 and the FWD-TEXT-3 commit in full first, it changed fsi-app/src/lib/forward-events/extract-forward-events.mjs and created fsi-app/scripts/harness-runs/forward-events/PENDING-RUN.md); node_modules symlinked at fsi-app/node_modules. Cannot push; coordinator lands. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. Write set: fsi-app/src/lib/forward-events/read-and-extract.mjs (+ test), fsi-app/scripts/turns/export-corpus-for-extraction.mjs (+ test), fsi-app/scripts/maintenance/forward-events-retext.mjs (+ test; only its reader mapping), fsi-app/src/lib/forward-events/extract-forward-events.mjs (+ test), fsi-app/scripts/forward-events/run-extraction.mjs only if the item shape it passes through needs the new field, fsi-app/scripts/harness-runs/forward-events/PROTOCOL.md (a §5d paragraph), fsi-app/scripts/harness-runs/forward-events/PENDING-RUN.md (chain a "What changed" entry onto FWD-TEXT-3's marker and re-pin to the hash the fitness runner prints; F28 rule c). Nothing else; record-facts.mjs (mint governing file) is NOT in scope: do not widen the mint's window, the mint locates a date and this lane gives the extractor the context to type it. Deterministic, $0, no LLM; "there is NO small follow-up fix, fix it now"; no claims ahead of evidence, [CONFIRMED]/[HYPOTHESIS]; read every file you touch IN FULL; the verbatim source_span law (assertVerbatim) is untouchable; commit message ends with "Co-Authored-By: Claude Sonnet <noreply@anthropic.com>". Gates before commit: node --test over every test file you touch plus fsi-app/scripts/forward-events/run-extraction.test.mjs fsi-app/scripts/turns/apply-extraction-output.test.mjs; from repo root node fsi-app/.discipline/fitness/runner.mjs (0 violations after the marker re-pin) and node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs (141 pass); fsi-app/node_modules/.bin/tsc --noEmit -p fsi-app/tsconfig.json if you touch anything a .ts file imports (19 pre-existing TS2882 errors are known; report any NEW one). The Supabase MCP (server "Supabase", tool execute_sql, project kwrsbpiseruzbfwjpvsp; load via ToolSearch "select:mcp__Supabase__execute_sql") is available for READ-ONLY SELECT; never write through it.

THE EVIDENCE [CONFIRMED by the coordinator]: (1) PROPOSER-7's pass over forward-events-run-023..028 (backlog apply #33, 934 items, 176 events, 345 skips): slot_date_unclassified = 190 across the six runs, the largest skip reason; lane FE-SLOT (Addendum 85 ps 11, 2026-09-03) had already named the cause on 48 spans: "the located span is a duration this grammar cannot parse as a calendar date, or the deontic verb sits outside the ~90-char window record-facts.mjs captures". (2) Live, read-only SQL 12:40 UTC over section_claim_provenance WHERE claim_text LIKE '[due_date]%': 756 slot claims; source_span avg 79 chars, max 112; 118 carry a four-digit year; 44 carry shall/must/is required; 89 carry a year and NO deontic; 65 start "within N days/months/…"; 239 carry by/no later than/before/until/from/on; date_precision markers: day 99, month 7, year 12. Random sample of the year-less spans: "no later than three months from the date of its receipt— (a) decide on the validity of the complaint", "within 28 days beginning with the day on which the notice was received", "no later than 31 January of the following year", "no later than the 30th April immediately after the date on which the ship's attained annual operational", "no later than 18:00 CET for the outright transaction price assessment". Coordinator's reading: the 638 year-less spans are RELATIVE or RECURRING deadlines, not calendar dates; the extractor is right to refuse them and this lane must NOT invent an anchor for them (say so in PROTOCOL §5d, and make the skip reason distinguish "relative_deadline_no_calendar_date" from "calendar_date_no_deontic_in_context" from "calendar_date_deontic_context_unavailable", so the histogram tells the reader which population each is; three named reasons, never one bucket). The fixable population is the 89: the mint's span is a ~90-char verbatim substring of the item's captured source (agent_run_searches.result_content, the grounding pool: ADR-016, never truncated; read how heal-provenance.mjs or validate_item_provenance locate a span inside it, and how src/lib/intake/record-facts.mjs chose the span, to locate the same span the same way), and the deontic verb the extractor's requireDeonticWithin/requireDeonticOrAimWithin windows need sits just outside it.

THE DUPLICATION YOU MUST REMOVE FIRST (CLAUDE.md: no mirrored copies): three readers build the extractor's per-item input {claims[{claim_id, kind, text, span}], sections[{id, key, md}]} with the same two queries, by hand, each saying it "mirrors" another: src/lib/forward-events/read-and-extract.mjs (the live per-item reader, lines ~28-40), scripts/turns/export-corpus-for-extraction.mjs (header: "COLUMN MAPPING mirrors read-and-extract.mjs's query shape exactly"), scripts/maintenance/forward-events-retext.mjs (line ~134: "mirrors read-and-extract.mjs's row mapping"). Make read-and-extract.mjs export ONE reader (the query + the row mapping, e.g. readExtractionInput(sb, itemId) / mapRows(...)) and have the other two import it; keep each caller's own behaviour otherwise; a source-reading contract test (like IN-CHUNK-2's) that fails if any of the three carries its own .from("section_claim_provenance") select again. THEN add the third input through that single reader: for each due_date slot claim (claim_text starts "[due_date]"), the captured-source context: locate claim.span verbatim inside the item's usable captured text (which searches rows count as the grounding pool for the item: read canonical-pipeline.ts ~line 1003 and the heal's capture index; the pool may hold several captures per item, take the first that contains the span verbatim, exact substring, and record which) and carry context = { before: up to 240 chars ending at the span start, after: up to 240 chars from the span end, search_id } on the claim object; null when no capture contains the span (then the skip reason is calendar_date_deontic_context_unavailable). In the extractor, for a slot claim whose span carries a calendar date but whose own window shows no deontic/aim, re-run the SAME rule's deontic/aim check over before+span+after (the same requireDeonticWithin/requireDeonticOrAimWithin bounds measured from the span, the same sentenceStart rule); on a hit, emit the event with obligation_text built by the FWD-TEXT-2/3 rules over the CONTEXT text (sentence snap, honest "…" markers) and source_span still the verbatim slot span; never a kind assumed from the slot. Measure with read-only SQL and the real code before writing: for the 89, how many have a deontic/aim within 240 chars of the span in the captured text (report the number and five verbatim before/span/after triples); for the 118-with-year population, how many events the new path would emit by kind. Tests: fixtures from those five real triples (cite the SQL in the test header), the three skip reasons, the relative-deadline refusal, the reader contract test, exporter and retext callers unchanged in output shape (their tests). Bump EXTRACTOR_VERSION per its convention (the next after FWD-TEXT-3's value). Report: the measurements, the reader collapse (which files now import what), rules, files, exported functions, tests, fitness line, marker hash, commit hash, anything refused with the reason.`
const r = await agent(P, { label: 'FE-SLOT-2', phase: 'Lane', model: 'sonnet' })
return { feslot2: r }
