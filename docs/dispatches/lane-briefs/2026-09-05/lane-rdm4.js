export const meta = {
  name: 'lane-rdm4',
  description: 'Sonnet lane RD-M4: population apply #34 (rows_file, the R-D six oil-bulletin series) minted 1 of 6 valid rows; the other five were blocked not_applied_url_holder because all six series share the bulletin page URL; make the M4 same-URL holder check series-aware (a holder with a different non-null instrument_identifier is a sibling series, not the same document)',
  phases: [{ title: 'Lane', detail: 'RD-M4 in its own worktree' }],
}
phase('Lane')
const P = `You are lane RD-M4 on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/rdm4 (branch lane/rdm4-2026-09-04, based on origin/master f2800ea9); node_modules symlinked at fsi-app/node_modules. Cannot push; coordinator lands. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. Write set: fsi-app/scripts/mint/apply-mint-batch.mjs (+ its test), fsi-app/scripts/mint/MINT-RUNBOOK.md (the M4 pre-check paragraph, ~line 397: say exactly what a same-URL holder means now), fsi-app/scripts/harness-runs/mint/PENDING-RUN.md (MINT-RUNBOOK.md is a mint GOVERNING_FILE under F28: read fsi-app/.discipline/fitness/functions/F28-harness-run-integrity.mjs and git log -p -3 -- fsi-app/scripts/harness-runs/mint/PENDING-RUN.md for the marker format; create or chain it and pin the hash the fitness runner prints), and src/lib/intake/write-item.ts or src/lib/agent/canonical-pipeline.ts ONLY if they carry their own copy of the same-URL rule (grep "source_url" holder logic there and in src/lib/intake/mint-item.ts; if a copy exists, the rule must live in ONE exported predicate both import, never two bodies — CLAUDE.md forbids mirrored copies; report which). Deterministic, $0, no LLM; "there is NO small follow-up fix, fix it now"; no claims ahead of evidence, [CONFIRMED]/[HYPOTHESIS]; read every file you touch IN FULL; commit message ends with "Co-Authored-By: Claude Sonnet <noreply@anthropic.com>". Gates before commit: node --test fsi-app/scripts/mint/*.test.mjs fsi-app/scripts/mint/lib/*.test.mjs; from repo root node fsi-app/.discipline/fitness/runner.mjs (0 violations after the marker) and node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs (141 pass); tsc (fsi-app/node_modules/.bin/tsc --noEmit -p fsi-app/tsconfig.json) if you touch a .ts file. The Supabase MCP (server "Supabase", tool execute_sql, project kwrsbpiseruzbfwjpvsp; load via ToolSearch "select:mcp__Supabase__execute_sql") is available for READ-ONLY SELECT; never write through it.

THE EVIDENCE [CONFIRMED by the coordinator]: population-turn apply #34 (run 33875330945, master f2800ea9, rows_file scripts/_snapshots/population-browser/oil-bulletin-2026-09-03/census-rows.json, the six EU Weekly Oil Bulletin market_signal series of ruling R-D 2026-09-03) produced mint-run-024: attempted 6, valid 6/6, minted 1 (eu-oil-bulletin:eurosuper-95, minted_verified, item 4fae403a-ced5-4c8f-82b7-af0fd6127061), not_applied_url_holder 5 (automotive-diesel, heating-gas-oil, lpg-motor-fuel, residual-fuel-oil-1pct, heavy-fuel-oil-3-5pct, each "valid, 0 failures — recommended_status=verified"). All six rows carry source_url https://energy.ec.europa.eu/data-and-analysis/weekly-oil-bulletin_en, canonical_instrument_key null, instrument_identifier = the series key (build-oil-bulletin-rows.mjs's own choice, which ratify-series-items.mjs relies on). apply-mint-batch.mjs's checkM4 (line ~140) blocks any payload whose source_url already has an intelligence_items holder ("not_applied_url_holder, a same-URL holder with no key collision"); line ~253 writes instrument_identifier onto the item row. So the first series minted and then blocked its five siblings: the rule's purpose (MINT-RUNBOOK ~line 397: the same document must not mint twice) is right; its identity test (URL alone) is wrong for series pages, which ruling R-D made a first-class case. Read docs/decisions for anything binding on M4 or instrument identity (grep -ril "M4\\|url_holder\\|instrument_identifier" docs/decisions docs/specs) and cite it; the coordinator found no ADR naming the URL rule, only the runbook.

DO: (1) Confirm live, read-only: SELECT id, instrument_identifier, canonical_instrument_key, source_url, item_type, provenance_status FROM intelligence_items WHERE source_url = '<the bulletin URL>' (expect exactly the eurosuper-95 row), and how many OTHER live source_urls carry 2+ items today and with what instrument_identifiers (is the series case already present elsewhere, e.g. the same act minted twice with different identifiers? report counts; that is the population the new rule must not break). (2) Change checkM4 so a same-URL holder blocks ONLY when the identity is the same: both instrument_identifier null, or equal (case-insensitive, trimmed); a holder with a different non-null instrument_identifier is a sibling series and does not block; a payload with a non-null identifier against a holder with a null identifier still blocks (the older unlabelled row may be the same document; name that asymmetry in the runbook). Keep the canonical-key branch untouched. The index (buildItemsIndex or similar) must hold ALL holders per URL, not the first only, and the pre-check must see items minted EARLIER IN THE SAME BATCH (read how #34 blocked five after minting one: is the index rebuilt per payload, or is the minted row pushed into the index? make the behaviour explicit and tested). (3) Tests: the six-series batch (one minted, five siblings pass the pre-check), the true duplicate (same URL, same identifier → blocked), the null-vs-null duplicate (blocked), the null-holder asymmetry, and a regression that the canonical-key holder conflict still blocks. (4) MINT-RUNBOOK M4 paragraph updated; marker pinned. Report: the live counts from (1), the rule, files, exported functions, tests, fitness line, marker hash, commit hash, anything refused with the reason.`
const r = await agent(P, { label: 'RD-M4', phase: 'Lane', model: 'sonnet' })
return { rdm4: r }
