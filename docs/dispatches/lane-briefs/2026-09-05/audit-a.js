export const meta = {
  name: 'audit-a',
  description: 'Wiring audit 2026-09-04, part A: runtimes/workflows (A1) and customer surfaces (A2), over everything landed since 2026-08-21 (PRs #474-#583)',
  phases: [{ title: 'Audit' }],
}
phase('Audit')
const COMMON = `Repo Dwarves77/dotfiles, worktree /root/work/lanes/audit (branch lane/audit-2026-09-04 = origin/master e8cb748f); node_modules symlinked at fsi-app/node_modules. READ-ONLY audit: touch NOTHING except your own output file under docs/audits/wiring-audit-2026-09-04/ (named below). Do not commit. The window is every commit since 2026-08-21 on master: docs/audits/wiring-audit-2026-09-04/_prs.txt lists the 107 PRs (date, sha, title); use git log/show freely. Evidence rules: every "wired"/"used"/"dead" verdict cites its evidence (a file:line, a harness-run artifact path, a git commit, or a read-only SQL result). [CONFIRMED] = you ran or read it; [HYPOTHESIS] = not verified. The Supabase MCP (server "Supabase", tool execute_sql, project kwrsbpiseruzbfwjpvsp; load via ToolSearch "select:mcp__Supabase__execute_sql") is available for READ-ONLY SELECT; never write. The operator's question, verbatim: "are all of our site tools built and wired? I want an audit of everything built in the last two weeks to see if it is being used and wired and fits within the flywheel loop." The flywheel loop, for placement: source sweep (register/feed/sitemap walkers) -> portal_link_candidates -> ledger-consume (classify -> intake) -> census_worklist -> population-turn (export -> record-grade mint under THE GATE -> flywheel: connection discovery + forward events + tags) -> customer surfaces (Regulations, Market Intel, Operations, Research, Community) -> change-detection (monitoring_queue -> reconcile) -> propagation (ADR-024 decision propagation) -> back to sources. Write your file as a markdown section with a table per component: Component | Built (PR, date) | Invoked by (runtime step / route / script) | Evidence of use (runs, rows, artifacts, live page) | Loop stage | Verdict (WIRED+USED / WIRED-NOT-RUN / BUILT-NOT-WIRED / DESIGNED-ONLY) | Gap. End with a ranked list of the gaps you found and one paragraph of what you could not confirm.`
const A1 = agent(`${COMMON}

You are AUDIT-A1: RUNTIMES. Output file: docs/audits/wiring-audit-2026-09-04/A1-runtimes.md. Cover every workflow in .github/workflows/ at this tree (17 files) and every dispatchable step/mode/walker inside them: maintenance.yml's every step (read docs/runbooks/MAINTENANCE-RUNBOOK.md §1-§12 and the workflow's step list), population-turn.yml (normal, rows_file, flywheel_backlog), corpus-turn.yml, change-detection.yml, ledger-consume.yml (and the LEDGER_CONSUME_APPLY_ENABLED source constant), propagation-drain.yml, source-sweep.yml (walkers register-eurlex, register-federal-register, feed, research, sitemap), producers.yml (which producers; refresh-published-price-statistics, ecb-fx, lc_lci_lev …), data-audit-lane, discipline, build-proof, bug-class-guard, inspect-oil-bulletin, source-monitoring, spot-check-monthly, trust-recompute, uptime-probes. For each: when built/last changed (git log), how it is invoked (dispatch-only vs schedule; note rule 16: no standing schedules during build), evidence it has actually run (harness-run artifacts under fsi-app/scripts/harness-runs/<family>/ with run counts and the latest run_id/date; _snapshots; population/* commits; for maintenance steps the runbook's own "dispatched" notes and docs/ops/session-log.md Addendum 85 mentions), what it writes (tables), what consumes what it writes, and its loop stage. Flag every step that has NEVER been dispatched, every one that exists only in dry mode, every one whose apply is operator-gated (name the gate), and every branch-landing manual handoff (deliver-artifact-branch.sh's refused-PR fallback: which families depend on a coordinator to land branches).`, { label: 'AUDIT-A1', phase: 'Audit', model: 'sonnet' })
const A2 = agent(`${COMMON}

You are AUDIT-A2: CUSTOMER SURFACES. Output file: docs/audits/wiring-audit-2026-09-04/A2-surfaces.md. Enumerate every route under fsi-app/src/app and every component under fsi-app/src/components changed since 2026-08-21 (git log --since=2026-08-21 --name-only -- fsi-app/src/app fsi-app/src/components, grouped by feature: e.g. regulations bands and IMMEDIATE-first ordering, band empty states, forward-events "Upcoming obligations" strip, connections/relevance lens, credibility/tier chip, series board / published price statistics, market intel, operations, research, community topics/rooms/regions, item detail, the intelligence assistant, PERF work). For each feature: which page renders it, which RPC/table it reads (grep src/lib/supabase-server.ts, data.ts, the allowlist LISTINGS_RPCS…), whether the data it reads EXISTS live (read-only SQL counts: intelligence_items by item_grade/provenance_status, item_forward_events, item_connections/edges, published_price_statistics, community tables, tags), whether it is reachable from navigation (read the nav/layout), whether it is gated behind a flag, and its loop stage (which flywheel output it renders). Community specifically: the operator ruled today "community should not be populated with rooms for every topic; people start rooms, rooms don't already exist; regions exist" — report exactly what the Community surface seeds today (which tables, how many rooms/topics/regions rows, where seeded from: migration, maintenance step community-topics-seed, or code), citing spec 05 and any ADR, so the coordinator can put the change to the operator. Do NOT open the browser. Verdicts per the table format.`, { label: 'AUDIT-A2', phase: 'Audit', model: 'sonnet' })
const [a1, a2] = await Promise.all([A1, A2])
return { a1, a2 }
