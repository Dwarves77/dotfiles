export const meta = {
  name: 'lane-assemble47',
  description: 'Sonnet lane ASSEMBLE-47: fold the eleven wave-F lane branches into train/wave47, renumber the four colliding 308 migrations to 308-311 with every reference, do the cross-lane registrations the lane reports named, resolve the shared-file merges, run every gate and next build, write memory postscript 58',
  phases: [{ title: 'Assemble' }],
}
phase('Assemble')
const P = `You are lane ASSEMBLE-47 on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/train47, branch train/wave47-2026-09-05, ALREADY CREATED by the coordinator from train/wave46-2026-09-05 (= origin/master 84745fa3 + train 46's CAP-1000 commits, PR #593). node_modules symlinked at fsi-app/node_modules; Node 24 at /home/claude/.npm/_npx/387698761821791d/node_modules/node/bin/node (dirname on PATH). Cannot push; the coordinator lands. Main clone /root/work/dotfiles is read-only for you (git show / git diff against its refs only; NEVER checkout, commit or branch there). Sibling worktree /root/work/lanes/train46 has another lane running: do not touch it. CLAUDE.md rules bind: no copies of logic; no dead code; no schedules or crons of any kind; $0, no LLM call in any runtime; one writer per shared dataset; "there is NO small follow-up fix, fix it now"; every claim [CONFIRMED]/[HYPOTHESIS]; read every file you touch IN FULL before editing; every commit message ends with "Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>". Read first: CLAUDE.md, docs/plans/complete-system-build-plan-2026-09-04.md §0 and §W1.6, docs/runbooks/TRAIN-ASSEMBLY-RUNBOOK.md (it exists on branch lane/assembly-2026-09-05: git show lane/assembly-2026-09-05:docs/runbooks/TRAIN-ASSEMBLY-RUNBOOK.md), docs/ops/session-log.md's LAST addendum and postscript 57 (tail), docs/PROGRAM-BOARD.md first 150 lines, docs/inventories/migrations.md, fsi-app/.discipline/fitness/functions/F25-module-liveness.mjs, fsi-app/.discipline/governance/closure-gate.mjs, fsi-app/.discipline/run-test-suite.sh.

THE ELEVEN LANE BRANCHES (all in the main clone's refs, all based on origin/master 84745fa3, each 1 commit except spec09a with 2): lane/ledgerchain2-2026-09-05 (20388c6d), lane/w71wire-2026-09-05 (309b9043), lane/attachsrc-2026-09-05 (047d4c21), lane/corridors-2026-09-05 (4b815ae8), lane/notices-2026-09-05 (4c56b616), lane/chips-2026-09-05 (4c445218), lane/spec09a-2026-09-05 (b7b8e0eb, bd0c8e9e), lane/spec09b-2026-09-05 (95663eda), lane/rulingsexec-2026-09-05 (f04dfcdd), lane/kitbackfill-2026-09-05 (a2dd005f), lane/assembly-2026-09-05 (a18cc64a). NOT to be folded: lane/cap1000-2026-09-05 and lane/reggrain-2026-09-05 (already landed in trains 45/46). Before folding, read each branch's commit message in full (git log -1 --format=%B <ref>) and its diffstat; each message carries the lane's REPORT summary including cross-lane needs.

FOLD, in this order, one git cherry-pick -x per commit: assembly, kitbackfill, rulingsexec, attachsrc, corridors, chips, spec09a, spec09b, w71wire, notices, ledgerchain2. Known shared files [CONFIRMED by the coordinator with git diff --name-only per branch]: docs/runbooks/MAINTENANCE-RUNBOOK.md (w71wire attachsrc spec09a rulingsexec kitbackfill), docs/inventories/migrations.md (w71wire attachsrc chips spec09b rulingsexec), fsi-app/docs/inventories/shared-dataset-ownership.md (w71wire attachsrc spec09b rulingsexec), fsi-app/.discipline/fitness/functions/F25-module-liveness.mjs (w71wire notices spec09a spec09b), .github/workflows/maintenance.yml (w71wire attachsrc spec09a kitbackfill), fsi-app/.discipline/run-test-suite.sh (corridors spec09a assembly), fsi-app/src/app/market/page.tsx (notices spec09b), fsi-app/scripts/spec09/SOURCES.md (spec09a spec09b), fsi-app/scripts/mint/heal-provenance.mjs + .test.mjs and fsi-app/scripts/maintenance/provenance-heal.mjs (attachsrc kitbackfill), fsi-app/.discipline/governance/coverage-report.json (notices spec09a). Resolve every conflict by KEEPING BOTH lanes' intent (both maintenance steps, both inventory rows, both F25 edits, both test-list entries); for coverage-report.json regenerate it with the repo's own generator (find it: grep -rl coverage-report.json fsi-app/.discipline) rather than hand-merging; for maintenance.yml check the step ids stay unique and the workflow_dispatch input enum (if steps are chosen by an input) lists every step each lane added; python3 -c "import yaml,sys; yaml.safe_load(open(sys.argv[1]))" on it. Never resolve a conflict by dropping a lane's change; if two lanes changed the same function body, read both and produce one function with both behaviours and no duplication.

MIGRATION RENUMBER [CONFIRMED: four lanes each numbered their migration 308]. Final numbers, fixed by the coordinator (apply order): 308 = rulingsexec's census_worklist_archive_columns; 309 = attachsrc's source_tier_opinions_host_class_table; 310 = chips's listing_rpcs_item_grade; 311 = spec09b's spec09_org_scope_and_pool_drop. Rename the files (git mv) and update EVERY reference: the migration's own header comment, docs/inventories/migrations.md rows, any pre-check/post-check script or test that names the file or number, runbook sections, the lane's shared-dataset-ownership rows, any "applied" inventory wording (they are NOT applied yet: the inventory row must say written, not applied, and must not claim a live apply time), and fsi-app/.discipline/consistency/override-check.mjs's C3 rule (an inventory row must not name a migration file that does not exist: run it, see below). grep -rn "308_" across the tree afterwards: the only remaining 308_ must be the census one.

CROSS-LANE REGISTRATIONS the lane reports asked the coordinator to do (do them here): (1) NOTICES: register fsi-app/.discipline/rendering/notices-rail-smoke.mjs (find its real path on the notices branch) in the ux-smoke-specs.mjs registry, delete its F25 LEGACY_ALLOWLIST entry, add RecalculationNotice.tsx to F25 ROW_COMPONENTS; run node fsi-app/.discipline/rendering/run-rendering-guard.mjs. (2) ASSEMBLY: add the docs/INDEX.md line for docs/runbooks/TRAIN-ASSEMBLY-RUNBOOK.md in the runbooks section, matching the section's existing line format. (3) W71WIRE removed F25 entries and ledgerchain2 re-pinned an F28 marker: run F28's runner and confirm no PENDING-RUN/LAST-PROPOSER-PASS marker is now stale; if one is, say which and stop on it (do not invent an attestation).

MEMORY (the coordinator's write set, delegated to you for this train only): append postscript 58 to docs/ops/session-log.md under the last addendum, in the same first-person voice and format as postscript 57 (read it): what train 47 folds (the eleven lanes by name and hash, one line each with its plan workstream), the four migrations with their final numbers and that they are written not applied (the coordinator applies via Supabase MCP, DDL before code, then updates the inventory rows), the cross-lane registrations done, conflicts resolved and how, and the exact next step ("apply 308-311 live, then the dispatches each lane report named"). Update docs/PROGRAM-BOARD.md: one row per workstream advanced (W1.6, W2.x, W3.x, W4.x, W5.x, W7.1, W7.4 as the lane commits say), status per §0 (which of Reachable/Run/Populated/Visible/Gated/Documented each now has), no claims beyond the evidence in the commits. Because .tsx files change, include a "UX compliance" block (docs/design/ux-laws.md) in the addendum. Commit memory in the same commit as the registrations.

GATES from the repo root, all must be clean before you report: node fsi-app/.discipline/fitness/runner.mjs (0 violations); node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs; node fsi-app/.discipline/governance/closure-gate.mjs; bash fsi-app/.discipline/run-test-suite.sh WITH the node_modules symlink temporarily removed (CI has none; restore it after) AND with it present; node fsi-app/.discipline/consistency/override-check.mjs --range=origin/master..HEAD (C3 clean; C4 worktree rows are local noise); cd fsi-app && npx tsc --noEmit; python3 yaml.safe_load on every workflow touched; node fsi-app/.discipline/rendering/run-rendering-guard.mjs; and cd fsi-app && npx next build --webpack with NO Supabase env (exit 0, the four [slug] routes present). A gate failure is yours to fix in this lane, not to report as pending.

REPORT: fold log (branch → landed commit, conflicts and resolutions), the renumber table (old file → new file, references updated), the registrations, every gate's output line, the build exit code, the final commit hashes (git log --oneline origin/master..HEAD), and anything you refused with the reason.`
return { a: await agent(P, { label: 'ASSEMBLE-47', phase: 'Assemble', model: 'sonnet' }) }
