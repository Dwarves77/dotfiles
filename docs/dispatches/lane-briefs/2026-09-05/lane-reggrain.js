export const meta = {
  name: 'lane-reggrain',
  description: 'Sonnet lane REG-GRAIN: the obligations register shows what each obligation IS (the forward event\'s obligation text), so two genuinely distinct obligations on the same item, kind and date are distinguishable and a reader knows what is due; one read, one row shape',
  phases: [{ title: 'Lane' }],
}
phase('Lane')
const P = `You are lane REG-GRAIN on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/reggrain (branch lane/reggrain-2026-09-05, based on train/wave45-2026-09-04 = master + FE-DEDUP + PERF-13); node_modules symlinked at fsi-app/node_modules; Node 24 at /home/claude/.npm/_npx/387698761821791d/node_modules/node/bin/node (dirname on PATH). Cannot push; coordinator lands. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. CLAUDE.md rules: no copies of logic; no dead code; "there is NO small follow-up fix, fix it now"; [CONFIRMED]/[HYPOTHESIS]; read every file you touch IN FULL; commit message ends with "Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>". Supabase MCP read-only SELECT (project kwrsbpiseruzbfwjpvsp; ToolSearch "select:mcp__Supabase__execute_sql").

THE DEFECT [CONFIRMED, lane FE-DEDUP's report + the coordinator's live screenshot of /regulations at 1ae31181]: the obligations register renders one row per obligations row as (date, kind, item title, binding position, jurisdiction, modes) — fsi-app/src/lib/obligations/read-register.mjs selects id, intelligence_item_id, forward_event_id, jurisdiction, modes, binding_position, due_date, date_precision, event_kind, status (lines ~186 and ~231) and never the obligation's own text (item_forward_events.obligation_text, source_span). After FE-DEDUP removes the 296 claim/section twins, ~853 rows remain, and genuinely distinct obligations sharing (item, kind, date) still render as identical rows (Euro 7's phase-out schedule has many events on one date; NZIA has 4 distinct 2030-01-01 targets), so a reader cannot tell what is due, only that something is. Measure first with Supabase MCP: after excluding the twins (group by intelligence_item_id, event_kind, due_date having count(*)>1 and the events' obligation_text differ), how many register rows would still look identical to a neighbour today.

READ FIRST, IN FULL: fsi-app/src/lib/obligations/read-register.mjs (+ .test.mjs), fsi-app/src/components/regulations/ObligationRegister.tsx and ObligationRegisterFilterBar.tsx, fsi-app/src/app/api/obligations/register/route.ts, fsi-app/src/lib/data.ts (getPublicObligationRegisterFirstPage), fsi-app/supabase/migrations/290_obligations.sql and the item_forward_events migration(s) (obligation_text, source_span, confidence columns), docs/specs/01-*.md (the register spec: what a row must show), docs/design/ux-laws.md, fsi-app/src/lib/perf/perf-budget.mjs (the obligations-register budget: bytes per row must not balloon).

BUILD: (1) read-register selects the event's obligation_text (and source_span if the spec wants the verbatim anchor) via the forward_event_id join in the SAME read (one query shape, both the first page and the paged/filtered path); trim to a bounded length at the read (one constant, e.g. 160 chars, ellipsis) so the register's first-page bytes stay within the F37 budget — measure the payload delta over the live first page with toLedgerRowPayload-style accounting and update the perf-budget registry honestly. (2) The row shows the obligation text as the row's primary line under/beside the title (the title is the instrument, the text is the obligation), so two distinct obligations on one date read as two things; the kind label stays. (3) The Suspense/skeleton and the "Load more" contract unchanged. (4) Tests: read-register test for the new fields and the trim; the register component's npmtest if one exists (*.npmtest.mjs for anything importing react); rendering smoke via node fsi-app/.discipline/rendering/run-rendering-guard.mjs. (5) Gates from the repo root: node fsi-app/.discipline/fitness/runner.mjs (0 violations), node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs, node fsi-app/.discipline/governance/closure-gate.mjs, tsc --noEmit, bash fsi-app/.discipline/run-test-suite.sh. Since .tsx changes, put a "UX compliance" block in your REPORT. REPORT: the measurement, files, the row shape before/after, bytes per row before/after, gate lines, commit hash.`
return { r: await agent(P, { label: 'REG-GRAIN', phase: 'Lane', model: 'sonnet' }) }
