export const meta = {
  name: 'lane-ledgertext',
  description: 'Sonnet lane LEDGER-TEXT: ledger-consume feeds the classifier raw HTML (6,000 chars of <head> boilerplate); one exported htmlToText replaces the three private copies, fetchDoc returns text, the export carries text',
  phases: [{ title: 'Lane' }],
}
phase('Lane')
const P = `You are lane LEDGER-TEXT on Caro's Ledge (repo Dwarves77/dotfiles). Worktree /root/work/lanes/ledgertext (branch lane/ledgertext-2026-09-04, based on train/wave40-2026-09-04 = master ef3b6a6d + PERF-9); node_modules symlinked at fsi-app/node_modules. Cannot push; coordinator lands. Do NOT touch docs/ops/session-log.md, docs/PROGRAM-BOARD.md, docs/INDEX.md. CLAUDE.md rules: one body (never a second copy of the same logic); "there is NO small follow-up fix, fix it now"; [CONFIRMED]/[HYPOTHESIS]; read every file you touch IN FULL; commit message ends with "Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>". Gates: node --test over every test you touch (run-ledger-consume.test.mjs is 74 today); tsc (fsi-app/node_modules/.bin/tsc --noEmit -p fsi-app/tsconfig.json); repo-root node fsi-app/.discipline/fitness/runner.mjs (0 violations: run-ledger-consume.mjs is a ledger-consume GOVERNING file, chain a "What changed" entry onto fsi-app/scripts/harness-runs/ledger-consume/PENDING-RUN.md and re-pin to the hash the runner prints; if you touch src/lib/agent/canonical-pipeline.ts check governing-files.mjs for the families it governs (mint? population?) and re-pin those markers too, or, if a family has no marker and a discharged history, create a fresh short marker in the same format); node --test fsi-app/.discipline/governance/*.test.mjs fsi-app/.discipline/fitness/*.test.mjs fsi-app/.discipline/*.test.mjs; the full suite bash fsi-app/.discipline/run-test-suite.sh.

THE DEFECT [CONFIRMED by the coordinator from the first export, run 33902755838, 2026-09-04 17:51]: scripts/turns/run-ledger-consume.mjs's buildFetchDoc returns res.text() RAW: every one of the 400 exported candidates carries 6,000 characters that begin "<!DOCTYPE html><html lang=..." (head, scripts, nav markup), and the live plan/apply path feeds that same raw HTML to firstFetchClassify (portal-harvest.ts:~300 passes r.text straight to classify), so the classifier's "content excerpt" has been markup since the runtime was built. Meanwhile the repo already has htmlToText THREE times as private functions: src/lib/llm/haiku-classify.ts:140 (with maxChars), src/lib/agent/canonical-pipeline.ts:142 (plus decodeHtmlBytes charset-aware decode, cleanCtl, and a PDF branch via pdf-extract.mjs in directFetchClean), src/lib/sources/officialness.mjs:53 (stripTags). BUILD: (1) ONE exported body: a new fsi-app/src/lib/text/html-to-text.mjs (plain ESM so both .ts and .mjs import it without a loader; name it what the repo's conventions suggest after you read the neighbours) exporting htmlToText(html, {maxChars?}) with the canonical-pipeline behaviour (script/style removal, tag strip, whitespace collapse, trim, optional slice) plus tests (script/style/nav removal, entity handling if the existing bodies handle entities, empty input, maxChars); (2) the three private copies import it (haiku-classify.ts, canonical-pipeline.ts, officialness.mjs's stripTags if its semantics are the same; if officialness needs different semantics say why and leave it); (3) buildFetchDoc in run-ledger-consume.mjs returns TEXT: decode with the same charset-aware path canonical-pipeline uses (export decodeHtmlBytes/cleanCtl from wherever they live, or move them next to htmlToText, one home), handle a PDF body the way directFetchClean does (classifyBody + pdfToText), then htmlToText; the export's text field and fetched_chars therefore describe text, not markup (the 200-char floor now measures text, which is what portal-harvest.ts:306 always intended: cite it); (4) tests: fetchDoc with an injected fetchImpl returning HTML yields stripped text; a PDF body yields extracted text (mock pdfToText if needed); the export payload carries text; (5) docs: scripts/turns/ledger-verdicts/README.md step 1 (the text is extracted text, CONTENT_MAX_CHARS applies to text), and a one-line note in docs/runbooks/CORPUS-TURN-RUNBOOK.md's ledger-consume section that exports before this fix carried markup and must be re-run. Report: files, exported functions, which of the three copies were replaced (file:line) and any left with the reason, tests, fitness line, marker hashes, commit hash.`
const r = await agent(P, { label: 'LEDGER-TEXT', phase: 'Lane', model: 'sonnet' })
return { ledgertext: r }
