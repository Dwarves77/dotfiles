# Sprint 4 — Data-Operations Ledger (already-executed; do NOT re-run)

**Why this file exists.** The `sprint4/fetch-canonicalization` branch holds two kinds of change
mixed together: **code** (apply-on-merge, normal PR+CI) and **already-executed data operations**
(durable corpus mutations that ran once against the **shared** Supabase project — dev and prod are
the same database). A re-run of any script below **double-applies against production**. This ledger
is the audit record; the `scripts/_dataops/interlock.mjs` guard is its enforcement arm — every script
listed here imports it and refuses to run unless `CONFIRM_RERUN=<name>` is set.

**Gating rule (precondition for integration):** the code half does NOT merge until every data-op
script here is guarded or quarantined from auto-execution (CI / tests / build). Status: **guarded**
(interlock applied) as of 2026-06-01.

---

## Bucket 2 — Already-executed DURABLE mutations (record; do not re-run)

| # | Script | Commit | Effect (already applied) | Idempotent? | Reversal |
|---|--------|--------|--------------------------|-------------|----------|
| 1 | `recovery-readmit.mjs` | `513262d` | INSERT 90 systematic recoveries into `provisional_sources` (pending_review) | Yes — dedup guard skips rows already in `sources`/`provisional_sources` | Delete the 90 provisional rows by their URL set |
| 2 | `phase2-build-binding.mjs` | `61f86cd` | Applied migration **118** (provenance-flip binding DDL) + created non-owner `reconciler` login role | Yes — `IF NOT EXISTS`/`CREATE OR REPLACE`; **re-ALTERs the reconciler password** on each run | `DROP ROLE reconciler` + revert 118 objects |
| 3 | `phase2-reconcile.mjs` | `0571c11` | Flipped ~600 active `unverified` `intelligence_items` to their terminal `provenance_status` (through the bound reconciler trigger) | Yes — already-terminal rows are no longer `unverified`, trigger re-derives to the same value | Reverse-flip script; values are trigger-derived (deterministic), so clear + re-derive |
| 4 | `recheck-fabrication-16.mjs` | `b973fcc` | Resolved 5 timeout-false `integrity_flags` (`b-audit-2026-05-29`) open→resolved; annotated 11 unadjudicable | Yes — already-resolved rows unchanged | Set the 5 flags back to `open` |
| 5 | `reclassify-portals-content-gate.mjs` | `e4f801d` | Archived 210 root-URL `intelligence_items` (`is_archived=true`, portal/error-page artifact); 21 kept; **source rows untouched** | Yes — already-archived rows unchanged | Un-archive (the **#5B** restore — operator-locked to regenerate-through-the-gate, NOT a blind `is_archived` flip) |
| 6 | `tier-reconcile.mjs` | (this commit) | Moved `base_tier` on **25 sources**: CAT1 = 11 genuine-news `trade_press` T5→T6 (Decision 2, A canonical); CAT2 = 14 clean Class-1 fixes (EcoVadis ×5 T5→T6, US EIA ×3 T1→T4, 3 trade-press T4→T6, J.P. Morgan T3→T6, China MEE + EC DG-Energy T3→T2). Per-row `UPDATE … WHERE base_tier=expectedOld` + read-back assert; all 25 verified. **Role-mislabels (Class 2) deliberately excluded.** | Yes — `WHERE base_tier=expectedOld` no-ops once moved; re-run dry-run shows 25 `[already]`, 0 drift | Per-row reverse move (new→old) by the same id set |
| 10 | `sourcefix-link-create.mjs --execute` | (this commit) | **Closed the no-source_id gap.** 28 active items had a `source_url` but no `source_id` FK (scan/seed minted them without enforcing source linkage). Linked all 28: **14 to existing registered sources** (exact/same-domain match: EUR-Lex, MPA Singapore, GOV.UK, Korea ME, Lloyd's Register, etc.) + **created 12 new source rows** (METI, MEE, MAS, MOCCAE, India PIB/Commerce, Australia Infra, Canada, UAE, SAFA, ScienceDirect, EcoEnclose — classified by domain: gov→T2 primary_legal_authority, SAFA→T4 industry_association, ScienceDirect→T5 academic, EcoEnclose→T6 vendor) and linked their 14 items. Per-row `UPDATE … WHERE source_id IS NULL` + read-back. After: 0 active items lack a source. New sources `auto_run_enabled=false` (cold-start; not auto-scanned until enabled). | Yes — `WHERE source_id IS NULL` no-ops once set | Null the 28 `source_id`s back + delete the 12 created sources by name/url. |
| 9 | `apply-121.mjs` | (this commit) | **Uniform promotion (human-in-the-loop removed).** Applied migration **121** (`validate_item_provenance`: collapse criterion-6 tier branch — a valid item → `verified` for ALL tiers; CRITICAL/HIGH no longer route to `pending_human_verify`; criteria 1-5 byte-identical to 119). Re-evaluated all 390 active items via the trigger touch: **26 `pending_human_verify` → `verified`**, 364 quarantined unchanged. Verified by read-back: 26/26 verified items re-validate (valid=true); 0 still `pending_human_verify`; all 5 known-stuck CRITICAL still quarantined (no leak). Removed the task-1.12 human-tick code (`verify-claim`/`pending-verification` routes, `VerificationQueue` UI + tab, `verify-token`, the `generate-brief` hook loop + 3 helpers). Enum value `pending_human_verify` LEFT in the type but unused (dropping it requires recreating the type + all deps — invasive/irreversible; inert unused). | Yes — re-touch re-derives the same value | Revert 121 (restore 119 criterion-6) + re-touch to re-derive; status is trigger-deterministic. The 26 verified are now customer-visible (gate=`verified`). |
| 13 | `offdomain-archive-pass.mjs --execute` | (this commit) | **Off-domain portal-shell archive pass (19 items).** Resolved the off-domain "19-shell" set via 13-agent research (`wf_594b04f3-0ae`; `docs/audits/portal-shell-source-triage-2026-06-03.md`). **REMOVE 6** (source NOT a freight regulator) → archived item + `status='suspended'` source: `error_page_artifact`=24cf9264 (Montreal nav-error); `non_regulatory_source`=cd238eda/445a06b2/ec086e7d/14ff3453/653f174b (councils, directories, residential services). **KEEP 13** (on-domain regulator/repository, ingested shell only) → archived item `archive_reason='portal_artifact'`, **source PRESERVED** (re-point at confirmed instruments + generate when Browserless up): WA DWER, TN TDEC, NL ECCM, MLIT JP, Mass Gen Laws, Ley Chile, Oregon ORS, Korea KLRI, Manitoba, Saskatchewan Env, Utah AQ, Wisconsin DNR, Alberta AEPA. Four orthogonal archive_reason values defined (off_domain reserved for off-vertical ITEMS, never written here). Per-row UPDATE + read-back; 19/19 archived, 6/6 sources suspended, 13/13 preserved sources still active. Corpus 390→371 active (quarantined 289→270; 101 verified untouched). | Yes — re-run no-ops on already-archived rows | Un-archive (is_archived=false, archive_reason=null) the 19; set the 6 suspended sources back to status='active'. |
| 15 | `source-merge-canonical-url.mjs --execute --confirm` + code fixes `promote/route.ts`, `decide/route.ts` | (this commit) | **SOURCE-LAYER FIX (defect a + safe merge).** Diagnosed the duplicate-source breakage (~115 by name / 799). **Defect (a):** `promote/route.ts` canonicalized then inserted BLIND (no existence check) → same-URL dupes (MIT ×2); `decide` was client-dependent; `bulk-approve` already correct. **Sealed both** with a robust canonical resolve (host-narrow + JS `canonicalizeUrl` compare — catches legacy non-canonical + www/slash drift); proven: MIT/mas.gov.sg/WWW.safa.aero all ROUTE to the existing source, a new URL inserts. **Defect (b):** `canonical_source_candidates` is per-URL (migration 021), publisher is descriptive text → institution identity is a genuine gap (NOT built; flagged for decision). **Merge:** only **7 clusters share a canonical URL** (the safe set); **6 merged** (loser→`status='suspended'`+note, items repointed to winner; active sources 728→**722**), **1 flagged BLOCKED** (`flk.npc.gov.cn`: repointing `unverified` item e68c91e5 trips the #43 provenance-flip guard — needs the reconciler credential). **58 entity-dup clusters** (same name, different URLs — IMO/eur-lex/etc.) NOT merged: portal-vs-document decision owed. Reversal map: `scripts/_diag/source-merge-reversal-6.json`. Supply still PAUSED. | Yes — re-run skips suspended (idempotent); blocked cluster flagged not crashed | Per reversal map: set the 6 losers `status='active'`, repoint their items back to the loser ids. |
| 16 | `source-label-backfill.mjs --execute --confirm` + migrations 122/123 + code (`classify-source-role.ts`, promote/decide) | (this commit) | **SOURCE LABEL WIRED AS A LIVE DERIVATION (defect b + categorization).** The label was incoherent because the role→category rule existed ONLY as a one-time backfill (migration 084) and onboarding (promote/decide) created sources with NO source_role/category + a hardcoded `intelligence_types:['GUIDE']`. **Fix:** (1) `classify-source-role.ts` — deterministic entity-type classifier (gov/academic/IGO/standards/news/vendor/data; node `--test` green on 17 examples incl. MIT→academic, IMO→IGO, SBTi→standards, SAFA→association). (2) **Migration 122** institutions table (WHO; registrable_domain) + FK; 432 institutions, 722/722 active resolved. (3) **Migration 123** — `derive_source_category` (== 084 CASE verbatim, name-exceptions preserved) + `derive_source_intelligence_types` + BEFORE INSERT/UPDATE trigger → category + intelligence_types DERIVE from source_role+name on every write. (4) Backfill: classified all NULL roles, re-derived all 799 sources. **Result: GUIDE 381→0; NULL source_role 26→1 (ATAG, provisional, honestly flagged); NULL category→1; intelligence_types now canonical (regulation/research/market_intel/operational_data); category 502/132/53/35 → 505/125/58/34 (drift fixed, e.g. SAFA → market_news).** (5) promote+decide now classify source_role at onboarding. role/category/intelligence_types are coherent by construction. SUPPLY STILL PAUSED. | Yes — trigger is deterministic; re-run idempotent | Drop migration-123 trigger+functions (category/intelligence_types stop deriving, keep last values); migration-122: `UPDATE sources SET institution_id=NULL; DROP TABLE institutions`; null the classified roles per `source-label-backfill-log.json`. |
| 14 | `identity-ssot-backfill.mjs --execute --confirm --skip-pending` | (this commit) | **P1 Identity SSOT backfill (EU ELI/CELEX slice).** Made the source URL the authority for regulation identity. Populated `instrument_identifier`+`instrument_type` from source ELI/CELEX for the parseable active items: **`instrument_identifier` 5→16/371** (13/101 verified, 3/270 quarantined). 11 canonical-key holders added (`eu_regulation`/`eu_directive` — both in the `instrument_type` CHECK vocab), each via per-row `UPDATE … WHERE instrument_identifier IS NULL` + read-back; **canonical unique index integrity intact (0 dups)**. Resolved 4 collision groups (AFIR 2023/1804, FuelEU 2023/1805, CSRD 2022/2464, HDV 2019/1242 — incl. a pre-populated collision on 2024/1610 caught in dry-run) by assigning the id to ONE deterministic holder (prefer verified); losers deferred to dedup. Fixed the 2 mistitles to source identity: `62ba40b0`→"Alternative Fuels Infrastructure Regulation (AFIR) — Regulation (EU) 2023/1804" (collision-dup of verified `ff95b385`; title corrected, identity deferred); `6f1e6615`→"ReFuelEU Aviation — Regulation (EU) 2023/2405" (holds the id). COM proposals + `eu_decision` NOT canonicalized (not enacted instruments / not in CHECK vocab). **REVERTED 2026-06-03** — the `instrument_identity_status` column (migrations 122/123) + its 4-value classification were a PARALLEL categorization duplicating systems that already exist: the advanced-intake **entity gate** (`src/lib/sources/entity-gate.mjs` + `first-fetch-classify` `entity_verdict` = source-vs-item) and the **source classification** (`sources.category` / `source_role` / `intelligence_types`). Per operator (erase duplicates, use the intake): column dropped, 122 unregistered, 123 + the backfill/measure/`source-vs-item` heuristic scripts deleted. **RETAINED** (verified NOT duplicate): the 11 `instrument_identifier`+`instrument_type` populations on the pre-existing migration-079 dedup columns + the 2 mistitle fixes (AFIR 2023/1804, ReFuelEU 2023/2405); parser `src/lib/sources/instrument-identity.ts` (no existing ELI/CELEX→number extractor — `url-canonicalize.ts` only normalizes URL formatting; the agent emits ELI URLs but never parses them). **NEW FINDINGS:** 3 verified↔verified duplicate pairs (1805/2464/1242) + AFIR's 2 quarantined dupes = P2 dedup prerequisite; `6b0939a5` is a 3rd ambiguous mistitle (AFIR url, CSRD-ish title) — flagged not fixed. | Yes — `WHERE instrument_identifier IS NULL` no-ops once set; ledger = `scripts/_diag/identity-backfill-log.json` | Null the 11 added `instrument_identifier`+`instrument_type` by id; restore the 2 titles (oldTitle in the log). |
| 12 | `nrel-to-nlr-rewrite.mjs --execute` | (this commit) | **NREL→NLR host rewrite (registry).** NREL renamed National Laboratory of the Rockies eff. 2025-12-01; `nrel.gov` retired (dead 2026-05-29), `nlr.gov` live successor (DOE + NLR dev-network notice). Host-only swap `nrel.gov→nlr.gov`, paths preserved, on **5 active source rows** (2 NREL T3/T4 + NSRDB T1 + PVWatts T1 + SAM T1; subdomains `nsrdb./pvwatts./sam.` preserved). Per-row `UPDATE … WHERE url=expectedOld` + read-back; 5/5 verified; **0 nrel.gov hosts remain**. NOT reachability-checked (Browserless down) — all 5 flagged `needs_reachability_revalidation` (lab also restructured paths, so deep URLs may 404 even with right host; re-validate when quota restored). Code/seed refs to nrel.gov (2 in `seed-resources.json`, `source-mapping.ts:92` pattern, 6 in `FacilityOptimization/TechnologyTracker`) are a SEPARATE code change, NOT in this data-op. | Yes — `WHERE url=expectedOld` no-ops once swapped | Swap `nlr.gov→nrel.gov` on the logged 5-id set. |
| 11 | `wave2-reachability-restore.mjs --execute` | (this commit) | **Reversed wave2-cleanup-execute's reachability false-negatives.** `wave2-cleanup-execute.mjs` (`e4f801d`-era) Step 1 re-classified 12 stale `provisional_sources` via a FORBIDDEN plain-fetch reachability+content path (bot-UA HEAD; 403/429/5xx/timeout misread as "unreachable" → tier L → `status='rejected'`). That killed **4 LIVE national-gov sources** (METI JP transport-efficiency, MPA Singapore sustainability, UAE MoEI, Korea ME K-Taxonomy). Restored all 4 `rejected`→`pending_review` (cleared `reviewed_at`/`recommended_tier`, annotated notes), per-row `UPDATE … WHERE status='rejected'` + read-back assert; 4/4 verified. The 6 `confirmed` + 2 `needs_more_data` from the same run got benign verdicts on the same suspect path — left in place, re-verify naturally on next canonical pass. Proper Browserless re-verification of the 4 is OWED when quota is restored. | Yes — `WHERE status='rejected'` no-ops once flipped | Set the 4 ids back to `status='rejected'` (log lists them). |
| 8 | `block4-retroground-runner.mjs --execute` | (this commit) | **Block 4 retro-grounding (CRITICAL batch).** Populated the previously-empty per-claim substrate: **647 `section_claim_provenance` + 365 `agent_run_searches` rows** (were 0/0). Grounded **26 of 31 CRITICAL sectioned items** — each gets FACT claims with verbatim spans (span-check gated, tier-2), covering required slots + modal sections; `validate_item_provenance` → valid=true → trigger flips **quarantined → `pending_human_verify`**; stale data_quality flag resolved; read-back asserted per item. 5 not grounded: 4 with a persistent unlabeled-assertion (interpretive prose needing an ANALYSIS label or a real source — content fix, not runner), 1 (`eu-ets-extension`) has no `source_id`. Idempotent: skips items that already have claims. | Yes — skips grounded items on re-run | Delete `section_claim_provenance` + `agent_run_searches` rows WHERE intelligence_item_id IN (the 26) + re-touch to re-derive `quarantined` (trigger-deterministic). NOTE: items are at `pending_human_verify`, NOT customer-visible (gate=`verified`); the task-1.12 human-tick loop flips them to `verified`. |
| 7 | `apply-119.mjs` + `sprint4-provenance-reconcile.mjs --execute` | (this commit) | **Surface-honesty gate fix.** Applied migration **119** (`validate_item_provenance` FAIL-CLOSE: a 0-section item no longer vacuously passes criteria 2-5 — records `no_section_content` → quarantined). Then re-validated the corpus via the `set_provenance_status` trigger touch: **390 active items → `quarantined`, 0 verified, 0 pending** (was 207 verified / 55 pending / 128 quarantined). The prior 207 "verified" were ALL 0-section shells passing on the vacuous skip; nothing is genuinely grounded (`section_claim_provenance` + `agent_run_searches` are 0 rows corpus-wide — the Block-4 grounding pipeline is unbuilt). Read-back verified: stored dist = `{quarantined:390}`, `provenance_verified_at` cleared on all. | Yes — trigger no-ops when status already `quarantined`; re-touch re-derives the same value | Revert 119 (restore 114 fn body) + re-touch to re-derive; statuses are trigger-deterministic. **Note:** the customer RPCs (`get_workspace_intelligence_{slim,dashboard,listings}`, 071/073/077) do NOT gate on `provenance_status` — so this flip is admin-honest but NOT yet customer-effective (separate finding; gate-wiring is owed). |
| 7 | `roleaudit-reclassify.mjs` (OP 1, role) | (this commit) | Role-audit reclass — `source_role` on **28 sources**: Block A ×18 → `intergovernmental_body`, Block B ×9 → `industry_association`, Block D ×1 (Smart Freight/GLEC) → `standards_body`. Per-row `UPDATE … WHERE source_role='academic_research'` + read-back; all 28 verified. Excludes NREL (URL-dead, held) + 6 taxonomy-held rows. | Yes — `WHERE source_role=expectedOld` no-ops once moved | Per-row reverse (new→`academic_research`) by the id set |
| 8 | `roleaudit-reclassify.mjs` (OP 2, tier) | (this commit) | Role-audit reclass — `base_tier` on **13 sources**: Block B ×9 → T5 (WBCSD 2→5, rest 3→5), Block C ×4 (MIT Climate Machine 1→4, CSRF/MIT-CTL/Tyndall 2→4). Per-row `UPDATE … WHERE base_tier=expectedOld` + read-back; all 13 verified. | Yes — no-ops once moved | Per-row reverse (new→old) by the id set |

## Bucket 3 — The DANGEROUS MIDDLE: verification harnesses that wrote to prod — RETIRED 2026-06-01

These were bespoke one-time instruments that connected to the shared prod DB (wrote + self-cleaned
sentinel rows) to assert the stored outcome of already-merged fixes. Their one-time job is complete
(the fixes are merged + four-part verified). They are now **DELETED** — replaced by standing fixture
tests that **cannot touch prod at all**:

| # | Retired harness | Fix it verified | Standing fixture replacement |
|---|-----------------|-----------------|------------------------------|
| 6 | `d1interp-stored-state-verify.mjs` | D1-interp reachability tier (`f84ee2d`) | `reachability.selftest` (decision) + `verification-decision.selftest.mjs` (4/4) — **composition DONE** |
| 7 | `checksrc-consumer-verify.mjs` | check-sources status (`55536c9`) | `reachability.selftest` (decision) + `check-sources-decision.selftest.mjs` (5/5) — **composition DONE** |
| 8 | `d1methodswap-verify.mjs` | D1 method swap (`8cecfd6`) | `reachability.selftest` (decision) + `verification-decision.selftest.mjs` (4/4) — **composition DONE** |
| 9 | `entitygate-stored-verify.mjs` | portal-as-item gate (`569e7f7`) | `scripts/lib/entity-gate.selftest.mjs` (11/11) — gate + isErrorBody decision |

**Composition fixtures DONE (commit `483ff6c`):** the pattern is `src/lib/sources/fetch-now-decision.mjs`
+ `scripts/lib/fetch-now-decision.selftest.mjs` (`d7fbe09`) — extract a route's decision into a pure fn
(no DB/HTTP/Date) and assert it in a fixture. Applied to `assessAndUpdateSource`
(`check-sources-decision.mjs`) and `verifyCandidate` (`verification-decision.mjs`); both routes now
delegate to the pure fn. **Every route is now composition-fixtured** and all three fixtures run in the
CI HARD gate (`.github/workflows/bug-class-guard.yml`). Sentinel harnesses fully replaced.

## Bucket 1 — PURE CODE (mergeable via PR; NOT in this ledger)

D3 layer, `reachability.mjs` SSOT, `entity-gate.mjs`, `first-fetch-classify.ts`, `canonical-fetch.mjs`,
the route fixes (`check-sources`, `bulk-import`), the CI YAML fix, and all `*.selftest.mjs` /
`scripts/lib/*` probes (read-only / pure). These apply on merge and carry no prod side-effect.

---

## Auto-execution audit (2026-06-01)

`package.json` exposes no test/seed runner that globs `scripts/**` (only `perf:bundles` →
`measure-bundles.mjs`, read-only). No CI workflow or test file references any script above. So **nothing
auto-runs them today** — the live hazard is a manual re-run or a *future* `scripts/**` glob. The interlock
closes both.

## Migration inventory follow-up

The discipline pre-push hook flagged migrations **117** (`117_provenance_gate_customer_rpcs.sql`, prior
session) and **118** (`118_provenance_flip_binding.sql`, this branch) as missing from
`docs/inventories/migrations.md`. Add both before the code half merges to master.

## Related

- [migrations](../inventories/migrations.md) — The ledger records apply scripts for migrations 118/119/121 and flagged 117/118 as missing from this inventory — shared Sprint 4 provenance…
- [deletion-reclassification-log](../ops/deletion-reclassification-log.md) — Both are execution-time audit ledgers of durable intelligence_items mutations with snapshot/reversal columns; the dataops ledger's archive passes…
- [ADR-002-tier-model](../decisions/ADR-002-tier-model.md) — The tier-reconcile and role-audit ops move base_tier across 25+ sources per the base_tier vs effective_tier distinction ADR-002 sets
