# Lane L19-migrations-B — supabase/migrations/124–272 (non-contiguous, 121 files)

Migration lane. Per file: objects CREATED/ALTERED/DROPPED, live-vs-dropped/superseded status in the
full 001–272 sequence (as far as this lane's files + table-usage.txt evidence can establish), and
cross-flags against table-usage.txt (0 rows / 0 code refs). All files carry `refs=0` in the ground
truth (migrations are never imported by app code), so WIRING notes below are about whether the
DDL/DML the file describes is *live in the schema*, not about import graph reachability.

Evidence ceiling: I have no live DB access. "Live" below means "not dropped by any later migration
in this lane, and (where checkable) consistent with table-usage.txt's live table list." Several files
in this lane are explicitly headed "AUTHOR-ONLY — NOT APPLIED" or "AUTHORED-NOT-APPLIED" at their own
authoring date; table-usage.txt shows none of the tables they drop (user_profiles, the vendor family,
forum_sections/threads/replies, ingestion_control_log/ingestion_state, hold_resolution_queue,
notification_events/deliveries/subscriptions, source_conflicts, item_source_evidence,
gate_a_route_b_baseline, briefings, the 219 backup tables) present in the live schema — consistent
with all of them having been applied at some later point after authoring, but I cannot confirm this
from file content alone; it is inference from absence in a table census taken after this lane's newest
file.

## Per-file verdicts

`supabase/migrations/124_monitoring_queue_reconciled_at.sql` — ALTERED — adds `monitoring_queue.reconciled_at` + partial index. Live (monitoring_queue: 580 rows, src=2 in table-usage.txt). Uneventful.

`supabase/migrations/125_routing_by_item_type.sql` — CREATED/ALTERED (CREATE OR REPLACE) — `get_market_intel_items`, `get_research_items`, `get_operations_items` routed by `item_type` instead of source attribute. SUPERSEDED: `get_research_items`/`get_operations_items` bodies replaced by 134 (column-fix), then 158/171/202 (via `validate_item_provenance`, unrelated), then 269 (predicate → `surface_of()`), then 272 (adds `jurisdiction_iso`). `get_market_intel_items` replaced by 164 (org-gate restore), 269, 272. None of 125's bodies are the live ones.

`supabase/migrations/126_research_required_slots.sql` — INSERT — seeds 4 `item_type_required_slots` rows for `research_finding`. Live (2 of 4 descriptions later UPDATEd by 128, rows persist). item_type_required_slots: 48 rows live.

`supabase/migrations/128_research_finding_slot_ledger_fix.sql` — ALTERED (UPDATE) — rewrites `decision_relevance`/`does_not_resolve` slot descriptions to permit GAP-satisfaction. Live (descriptions carried forward; not further touched by a later UPDATE in this lane).

`supabase/migrations/129_market_required_slots.sql` — INSERT — seeds 8 `item_type_required_slots` rows (market_signal, initiative). Live, uneventful.

`supabase/migrations/130_technology_required_slots.sql` — INSERT — seeds 12 `item_type_required_slots` rows (technology/innovation/tool). Live, uneventful.

`supabase/migrations/131_operations_required_slots.sql` — INSERT — seeds 4 `item_type_required_slots` rows (regional_data). Live; 2 of them (`cost_baseline`, `feasibility_choice`) UPDATEd by 132.

`supabase/migrations/132_operations_slot_gap_satisfiable.sql` — ALTERED (UPDATE) — loosens `cost_baseline`/`feasibility_choice` descriptions to GAP-satisfiable. Live.

`supabase/migrations/133_get_technology_items_rpc.sql` — CREATED — `get_technology_items(uuid)`. SUPERSEDED by 134 (column fix) then 272 (adds `jurisdiction_iso`); NOTE: unlike its three 269-touched siblings, `get_technology_items` was **never** converted to `surface_of()` — it still carries a hardcoded `item_type IN ('technology','innovation','tool')` predicate through 272 (272's own header names this a known, untouched gap). INCOMPLETE relative to the surface-routing refactor the other three RPCs got.

`supabase/migrations/134_fix_research_technology_rpc_columns.sql` — ALTERED (CREATE OR REPLACE) — fixes `get_research_items`/`get_technology_items` (missing `what_it_changes`/`does_not_resolve` columns, a real bug the migration documents: both RPCs referenced non-existent columns on `_workspace_active_items`, causing silent empty results / a "wrong-surface leak"). `get_research_items` body later superseded by 158/171/202/269/272; `get_technology_items` body is the one still live through 272 (only `jurisdiction_iso` appended in 272; the `item_type IN (...)` predicate from this file is otherwise unchanged and still live).

`supabase/migrations/135_source_registration_guard.sql` — CREATED — `_url_host()`, `_guard_source_archive()` trigger function + `trg_guard_source_archive` on `intelligence_items`. Live (function retained through 160's search_path pin).

`supabase/migrations/136_theme_candidate_capture.sql` — ALTERED — adds `intelligence_items.theme_candidate` + partial index. Live, uneventful.

`supabase/migrations/137_reg_family_slot_gap_satisfiable.sql` — ALTERED (UPDATE) — loosens `penalty_summary`/`primary_deadline` slot descriptions for standard/framework/guidance. Live.

`supabase/migrations/138_reg_only_authority_floor.sql` — ALTERED (CREATE OR REPLACE) `validate_item_provenance` — reg-family-only authority floor (criterion 3), named non-reg exemption. SUPERSEDED by 141 (per-item-type floor), then 142/143/145/150/158/171/202 (successive revisions). Not live.

`supabase/migrations/139_close_quarantine_flags_on_verify.sql` — ALTERED (CREATE OR REPLACE) `set_provenance_status()` — adds close-on-verify branch for `integrity_flags`; ONE-TIME UPDATE backfill closing stale open flags. SUPERSEDED by 209 (adds DELETE handling) and re-pinned search_path by 248. Function body's close-on-verify logic carried forward into 209's version (live).

`supabase/migrations/140_attention_counts_platform_flags.sql` — DROP+CREATE `admin_attention_counts()` — adds `platform_integrity_flags_open` column to the returns table (DROP FUNCTION required since CREATE OR REPLACE cannot change return shape). Live, uneventful (fixes a real bug: the admin queue was blind to 523 open platform-level flags).

`supabase/migrations/141_per_type_authority_floor.sql` — ALTERED (CREATE OR REPLACE) `validate_item_provenance` — per-item-type authority-floor ceiling (reg family=2, research_finding=4, technology family=5, market/regional exempt). SUPERSEDED by 142 (legal-line guard added), then 143/145/150/158/171/202. Not live.

`supabase/migrations/142_legal_line_guard.sql` — ALTERED — adds criterion-4 "legal-line guard" (an ANALYSIS claim asserting present-tense enacted-law language, non-forward-framed, is flagged `legal_claim_mislabeled_analysis`). SUPERSEDED by 143 (label-match becomes regex-tolerant) etc. The legal-line-guard logic itself carries forward unchanged through every later revision to 202 (live).

`supabase/migrations/143_label_variant_tolerance.sql` — ALTERED — criterion-4 ANALYSIS-label match becomes a tolerant regex (`c_label_re`) instead of 4 exact ILIKE strings. Carried forward unchanged through 202 (live).

`supabase/migrations/144_scrape_cadence.sql` — ALTERED — adds `system_state.scrape_cadence` (CHECK off/weekly/monthly) + `scrape_start_date`. Live; system_state: 1 row (singleton).

`supabase/migrations/145_provenance_floor_inline_derive.sql` — ALTERED — criterion-3 authority-floor tier now derived inline from `sources.COALESCE(tier_override, base_tier)` via `scp.source_id`, instead of reading the stored (cacheable/driftable) `source_tier_at_grounding`. Carried forward unchanged through 202 (live).

`supabase/migrations/146_item_xref_origin_and_related_derive.sql` — ALTERED/CREATED — adds `item_cross_references.origin`; one-time backfill INSERT from `intelligence_items.related_items[]`; creates `related_items_derived(uuid)` function + `item_related_items_derived` view (read-derive, no write-back to `intelligence_items`, to avoid tripping the provenance-flip guard). Both function and view later DROPPED by 180 (confirmed zero live callers). DEAD-HISTORICAL: the `origin` column and the backfilled `item_cross_references` rows are still live (origin is later widened by 252); the function/view artifacts are gone.

`supabase/migrations/147_sources_fetch_status.sql` — ALTERED — adds `sources.fetch_status` (CHECK ok/cdn_block/soft_404/blocked/error) + `fetch_status_at`. Live, uneventful.

`supabase/migrations/148_surface_counts.sql` — CREATED — `surface_of(item_type,domain)` (single classification SoT, generated from `src/lib/surface-of.mjs`), `get_surface_counts(org,surface)`, `get_all_surface_counts(org)`. All three live and load-bearing: `surface_of()` is consumed directly by 269/272's rewritten RPCs and by 148's own `get_surface_counts`/`get_all_surface_counts`, which are unchanged since.

`supabase/migrations/149_severity_backfill_ops_reg.sql` — DATA (UPDATE) — one-time backfill of `intelligence_items.severity` from `lower(priority)` for regulations/operations surfaces where NULL. DEAD-HISTORICAL (a completed one-shot backfill, WHERE-guarded idempotent, no ongoing effect).

`supabase/migrations/150_criterion2_url_canonicalize.sql` — CREATED/ALTERED — `canonicalize_citation_url(text)` IMMUTABLE helper; criterion-2 URL comparisons in `validate_item_provenance` now canonicalize both sides before matching. Both the helper and the canonicalized comparisons carry forward unchanged through 202 (live). NOTE: this file's trailing `COMMENT ON FUNCTION` text (a copy-paste residue) still says "migration 145 revision" instead of 150 — cosmetic only, does not affect behavior.

`supabase/migrations/151_published_price_statistics.sql` — CREATED — `published_price_statistics` table (signal-detail price board), RLS read policy for `authenticated`. Live: published_price_statistics 4 rows, src=3 scripts=2 per table-usage.txt — the "empty until the feed writer populates it" state described in the migration's header has since been superseded by real rows.

`supabase/migrations/151_state_cost_facts.sql` — CREATED — `state_cost_facts` table (sub-national cost facts), dimension/trend CHECKs, RLS world-readable. NOTE: this file and the prior one share migration number 151 in the numbering sequence (both files are literally `151_...sql` in the repo, distinct basenames) — a real numbering collision in this lane's file list, though Supabase's migration ledger keys on the full filename/timestamp so it does not break `supabase db push`. Live: state_cost_facts 13 rows, src=2 scripts=3; later gains `origin_class` via 267.

`supabase/migrations/153_community_post_signoff_requests.sql` — CREATED — `community_post_signoff_requests` table (verifier sign-off workflow), unique-open-request index, 3 RLS policies, adds `community_posts.signed_off_at`/`signed_off_by`. Header says "COMMITTED, NOT APPLIED" at authoring time. Now live: table-usage.txt shows `community_post_signoff_requests` with 0 rows but src=4 — the feature is wired into the app (4 source files reference it) but has never received a live sign-off request in production. NOTE for owner: a shipped, code-wired feature with zero real usage to date.

`supabase/migrations/154_community_signoff_withdraw_policy.sql` — ALTERED — adds one additive UPDATE RLS policy (`signoff_withdraw_own`) letting a requester withdraw their own pending request. Live (additive, does not touch 153's other policies).

`supabase/migrations/155_community_group_vertical.sql` — ALTERED — adds `community_groups.vertical` (nullable, app-validated, no CHECK) + partial index. Live: community_groups 7 rows (the 7 seeded regional rooms), src=7 — consistent with the migration's own framing that vertical groups "form when members create them" and none have yet (vertical presumably still NULL on all 7 seeded rows, since those are rooms not groups).

`supabase/migrations/156_org_member_bans.sql` — CREATED/ALTERED — `org_member_bans` table (PK org_id,user_id), RLS owner/admin-select policy, extends `accept_invitation()` with a ban check before the membership INSERT. Live: org_member_bans 0 rows (no ban ever issued) — expected, not a defect.

`supabase/migrations/157_security_hardening.sql` — ALTERED (RLS) — tightens `intelligence_items_read` to verified+non-archived only (was `USING(true)` to public — a real prior exposure: quarantined/unverified items + all staged_updates + all provisional_sources were readable by the shipped anon key); drops the public SELECT policies on `staged_updates` and `provisional_sources`; sets `item_related_items_derived` view to `security_invoker`. `intelligence_items_read` itself is later re-narrowed by 259 (drops `public` role, restricts to anon/authenticated) — 157's version superseded but the tightening direction is preserved and extended.

`supabase/migrations/158_floor_unconditional_label_per_claim.sql` — ALTERED `validate_item_provenance` — makes the reg-family authority floor fire UNCONDITIONALLY on item_type (previously gated on `priority IN (CRITICAL,HIGH)`, which is a model-chosen value that could disarm the floor — a real gate hole the migration documents and fixes); makes criterion-4 ANALYSIS-label matching paragraph-scoped instead of section-scoped (previously one labeled sentence anywhere in a section licensed every ANALYSIS claim in it — also a real gate hole). Blast radius documented (72 items expected to flip to quarantined on next re-validation). Carried forward unchanged through 202 (live).

`supabase/migrations/159_intelligence_items_fts.sql` — ALTERED/CREATED — adds `intelligence_items.search_tsv` (GENERATED STORED tsvector, weighted title/summary/full_brief) + GIN index; `search_intelligence_items(q,max_rows)` RPC gated on the customer read predicate. Live, uneventful (fixes the `/api/ask` context bug: previously `order("priority").limit(30)` with no relevance to the actual question).

`supabase/migrations/160_search_path_pin_app_functions.sql` — ALTERED — pins `search_path = public, extensions, pg_temp` on 56 app-owned functions (security hardening against search_path injection). All 56 ALTERs are individually idempotent; several of the named functions (`stamp_prov_origin`, `related_items_derived`, `detect_intersections`) are later DROPPED by 250/180/265 respectively — the search_path pin dies with them, which is expected and correct.

`supabase/migrations/161_sources_content_hash.sql` — ALTERED — adds `sources.last_content_hash`/`last_content_changed_at` (content-change fingerprinting). Live, uneventful.

`supabase/migrations/162_portal_link_candidates.sql` — CREATED — `portal_link_candidates` table (deep-link discovery queue), service-role-only RLS. Live: portal_link_candidates 1450 rows, src=1 — gains disposition columns via 220, identity-verification columns via 228, title columns via 229.

`supabase/migrations/163_reconciler_integrity_flags_insert.sql` — CREATED (RLS) — adds `integrity_flags_reconciler_insert` INSERT policy for the `reconciler` role (fixes a real bug: the reconciler role could UPDATE `intelligence_items` to quarantine an item but the trigger's `INSERT INTO integrity_flags` side-effect then failed RLS with no covering INSERT policy, aborting the whole flip). Live.

`supabase/migrations/164_market_intel_org_gate.sql` — DROP+CREATE `get_market_intel_items` — restores the `_assert_org_membership(p_org_id)` call that migration 108 (outside this lane) had silently dropped when it rewrote the function as pure SQL, plus a deterministic `id ASC` tie-break. This is a genuine, documented security regression-and-fix: between 108 and 164, `get_market_intel_items` had no membership check and any authenticated caller could read another org's `workspace_item_overrides` overlay for market items. Superseded again by 269/272 (both preserve the `_assert_org_membership` call this file restored).

`supabase/migrations/165_profiles_self_write_and_anon_pii.sql` — CREATED (RLS)/ALTERED (grants) — adds `profiles_self_insert`/`profiles_self_update` policies (fixes a real bug: `profiles` had zero write policies ever, so browser-side profile self-edit silently no-opped, "saved" with 0 rows affected); REVOKEs anon's table-wide SELECT and re-GRANTs SELECT on 34 non-PII columns only, excluding `email`/`linkedin_sub`/`is_platform_admin` from the previously-open anon read. Live (profiles: 2 rows, src=23).

`supabase/migrations/166_provisional_sources_admin_select.sql` — CREATED (RLS) — `provisional_sources_admin_read` (fixes a real bug: migration 157 correctly dropped the anon-readable SELECT policy but left `provisional_sources` with NO select policy at all, so the anon-client-based admin review queue silently rendered empty — 489+2 pending rows invisible — while the reader also dropped `error` from its destructure, hiding the failure). Live.

`supabase/migrations/167_staged_updates_reviewer_notes.sql` — ALTERED — adds `staged_updates.reviewer_notes` (fixes a real bug: the API route wrote this column unconditionally on approve/reject but no migration had ever created it — a "phantom column" that 500'd on the approve-with-notes path, leaving `materialized_at` NULL and exposing the endpoint to a duplicate-mint retry). Live.

`supabase/migrations/168_aux_table_parent_gates.sql` — ALTERED (RLS) — replaces `USING(true)` read policies on `item_timelines`/`item_disputes`/`item_changelog`/`item_cross_references`/`item_supersessions` with parent-gated policies mirroring `intelligence_items_read` (fixes a real bug: these 5 child tables leaked the existence/title fragments of quarantined/archived items even after 157 hid the parent rows — 689+ timeline rows alone). Live.

`supabase/migrations/169_reconciler_rls_repair.sql` — CREATED (RLS) — 3 SELECT policies for `reconciler` on `agent_run_searches`/`section_claim_provenance`/`item_type_required_slots` (fixes a real bug: the reconciler role had table GRANTs but no covering RLS policy on its own validation-input tables, so `validate_item_provenance` run as reconciler read 0 rows for these three and mis-recommended `quarantined` for valid items). Live.

`supabase/migrations/170_ledger_repair_107_134.sql` — DATA (DO block, ledger-only) — inserts 15 rows into `supabase_migrations.schema_migrations` for migrations 107–134-band objects that existed live but were never recorded in the ledger. DEAD-HISTORICAL, one-shot, idempotent.

`supabase/migrations/171_validate_provenance_brief_presence.sql` — ALTERED `validate_item_provenance` — adds CRITERION 6 (`missing_full_brief`): a verified item must carry a non-empty `full_brief` (fixes a real bug: 5 verified-live items had NULL `full_brief`, so the customer detail page rendered empty for a "verified" item). Ships with an intended corpus revalidation to honestly quarantine the 5. Carried forward unchanged through 202 (live).

`supabase/migrations/180_drop_orphan_rpcs_and_dead_views.sql` — DROP — 5 views (`open_conflicts`, `provisional_sources_review`, `source_health_summary`, `active_intelligence_items`, `item_related_items_derived`) + 2 functions (`get_workspace_members`, `related_items_derived`). Header: AUTHOR-ONLY — NOT APPLIED. Evidence for later application: none of the 5 view names or `get_workspace_members`/`related_items_derived` are referenced as live objects by any later migration in this lane; consistent with the drop having landed.

`supabase/migrations/181_drop_vendor_family.sql` — DROP — `vendor_endorsements`, `vendor_regulations`, `vendor_technologies`, `vendors` tables (all CASCADE) + `update_vendor_endorsement_count()`. Header: AUTHOR-ONLY — NOT APPLIED. table-usage.txt has no vendor* table at all — consistent with the drop having landed (0 evidence to the contrary).

`supabase/migrations/182_repoint_policies_off_user_profiles.sql` — ALTERED (RLS) — repoints `moderation_reports_select`/`moderation_reports_update_admin`/`post_promotions_select` from `user_profiles.is_platform_admin` to `profiles.is_platform_admin`. Header: AUTHOR-ONLY — NOT APPLIED, MUST precede 183. table-usage.txt shows `moderation_reports` (src=2) and `post_promotions` (src=1) still live, and no `user_profiles` table — consistent with 182 then 183 having both landed in order.

`supabase/migrations/183_drop_user_profiles_mirror.sql` — DROP — `user_profiles` table (CASCADE) + `_mirror_profiles_to_user_profiles()`/`_mirror_user_profiles_to_profiles()` (CASCADE); drops `profiles_mirror_to_user_profiles` trigger. HARD-DEPENDS on 182 landing first. Header: AUTHOR-ONLY — NOT APPLIED. `user_profiles` absent from table-usage.txt — consistent with having landed.

`supabase/migrations/184_drop_ingestion_pair.sql` — DROP — `ingestion_control_log`, `ingestion_state` (CASCADE). Header: AUTHOR-ONLY — NOT APPLIED, with a PRECONDITION that an export script run first to archive 1,483 rows to a private repo (unverifiable from this repo). Neither table appears in table-usage.txt — consistent with having landed.

`supabase/migrations/185_drop_dead_columns.sql` — DROP COLUMN ×7 across `intelligence_item_versions`, `regions`, `region_dimension_coverage`, `sources` (×4: classification_observed_distribution, last_observed_at, spotchecked_at, spotchecked_by). Header: AUTHOR-ONLY — NOT APPLIED. Cannot verify column-level absence from table-usage.txt (it only tracks tables, not columns); AMBIGUOUS whether applied — no contradicting evidence either.

`supabase/migrations/190_community_counter_integrity.sql` — ALTERED (CREATE OR REPLACE, SECURITY DEFINER flip) — `update_community_group_member_count()`, `update_community_post_reply_count()` (also adds the `weekly_post_count` recount, fixing a real bug: that column was displayed but had no writer since migration 028), `update_case_study_validation_count()` — all flipped from SECURITY INVOKER to SECURITY DEFINER (fixes a real bug: RLS-path writes by ordinary members caused the parent-table counter UPDATE to match 0 rows under the caller's own restrictive UPDATE policy, silently drifting counters). Header: AUTHORED-NOT-APPLIED, pairs with an operator-run recount script post-apply.

`supabase/migrations/191_org_membership_ban_guard.sql` — CREATED — `org_memberships_ban_guard()` (SECURITY DEFINER) + `BEFORE INSERT OR UPDATE` trigger on `org_memberships` (DB-layer backstop closing a real gap: the app-layer ban checks in `accept_invitation()`/the members route could be bypassed by a direct admin INSERT via PostgREST or any future code path that forgot the check). Header: AUTHORED-NOT-APPLIED.

`supabase/migrations/192_drop_forum_layer.sql` — DROP — `case_studies.linked_thread_id` column, then `forum_replies`/`forum_threads`/`forum_sections` tables, then `update_thread_reply_count()`/`update_section_thread_count()` functions. Header: AUTHORED-NOT-APPLIED. None of the three forum_* names appear in table-usage.txt — consistent with having landed; the 17 seeded `forum_sections` rows are a named, accepted data loss (zero customer exposure per the migration's own audit).

`supabase/migrations/195_error_events.sql` — CREATED — `error_events` table (first-party error tracking, no Sentry account), unique dedup index, platform-admin-only read RLS. Header: "NOT YET APPLIED" at authoring. Live: error_events 14 rows, src=2 — confirms it was applied and is receiving real writes.

`supabase/migrations/200_canonical_instrument_key.sql` — CREATED — `intelligence_items.canonical_instrument_key` column, `derive_canonical_instrument_key()` (IMMUTABLE), `set_canonical_instrument_key()` trigger, partial UNIQUE index on verified+live rows (twin-defect guard against the same instrument existing as two verified rows — the migration documents a real live incident, PPWR 2025/40 existing as two verified copies). Superseded in one respect: `derive_canonical_instrument_key()` is later CREATE-OR-REPLACEd by 255, which fixes a real bug in this file's own logic — the OJ sequence-suffix `(NN)` was discarded, collapsing genuinely-distinct instruments (e.g. `22008A0221(01)` vs `(02)`) to one key and causing the unique index to reject the second; one item was live mis-archived as a false duplicate as a direct consequence, later corrected. Trigger/column/index still live under 255's function body.

`supabase/migrations/201_pause_flag_session_guard.sql` — CREATED — `system_state_flag_audit` table, `admin_set_pause_state()` (SECURITY DEFINER, sole sanctioned writer, sets a transaction-local marker), `guard_pause_flag_writer()` trigger (bounces any write to `global_processing_paused`/`scrape_cadence` lacking the marker). Explicitly documented as REPLACING a prior "201 2a" design that was never applied (credential-based) — this file's own drops of that design's trigger/function are defensive no-ops. `admin_set_pause_state` execute grants later tightened by 248 (REVOKE from PUBLIC/anon/authenticated). NOTE: `system_state_flag_audit` shows 8 rows, src=0, scripts=0 in table-usage.txt — the audit trail is being written to but nothing in src/ or scripts/ reads it back; it exists purely as a forensic log with no surfaced consumer.

`supabase/migrations/202_standard_own_body_floor.sql` — ALTERED `validate_item_provenance` (full CREATE OR REPLACE, no BEGIN/COMMIT wrapper) — adds a per-fact scoped floor for `item_type='standard'`: a FACT from the item's own authoring institution grounds at floor 4 instead of the reg-family floor 2. Documented as monotonic (loosening only, bounded to `standard`). This is the LAST revision of `validate_item_provenance` in this lane's file list (272 does not touch it) — this is the live body as of migration 272, modulo any later out-of-lane revision.

`supabase/migrations/203_holdings_quality.sql` — CREATED — `holdings_quality` table (per-capture defect-classification audit: TRUNCATED/FURNITURE/STUB/NO-KNOWN-DEFECT), 2 partial-unique indexes, admin-read + service-role-write RLS. Live: holdings_quality 672 rows, src=0 scripts=1 — read only by a script, not by any src/ surface (consistent with an operator-tool audit store, not a customer- or admin-UI-facing table).

`supabase/migrations/204_agent_runs_model_column.sql` — ALTERED — adds `agent_runs.model` (nullable). Live, uneventful.

`supabase/migrations/205_funded_pass_runlock.sql` — CREATED — `funded_pass_runlock` table + `acquire_funded_pass_lock()`/`heartbeat_funded_pass_lock()`/`release_funded_pass_lock()` (atomic acquire-or-stale-takeover-or-fail pattern, closing a real concurrent-race incident that zeroed two items). Live; RLS enabled deny-all-by-default is added later by 230 (this file shipped it without RLS enabled at all).

`supabase/migrations/206_mint_gate_hold_marker.sql` — ALTERED — adds `section_claim_provenance.mint_hold_reason`; PROGRAMMATICALLY patches `validate_item_provenance` (via `pg_get_functiondef` + string-splice, idempotent) to add a `fact_mint_hold` failure when `mint_hold_reason IS NOT NULL`. This patch technique means the criterion is folded into whatever the function body is at apply time — its effect (the `mint_hold_reason` check inside the criterion-3 FACT loop) should be present in every later revision (145-descendant lineage through 202) since none of those later files' bodies show the check being dropped; I can only confirm it is present in 202's final body implicitly via the patch's own anchor logic, not by direct inspection of 202's text (202's SELECT list does not include `scp.mint_hold_reason` and its loop body does not show the `fact_mint_hold` branch) — **AMBIGUOUS / possible regression**: migration 202's full CREATE OR REPLACE body (read in full above) does NOT contain `mint_hold_reason` or `fact_mint_hold` anywhere. Since 202 is a complete function replacement (not a patch), and its own header claims "everything else... preserved verbatim" from the pre-202 body, but 202 was authored referencing the migration-158-through-171 lineage (which does not include 206's patch), **this reads as 202 silently dropping the mint_hold_reason HARD-gate criterion 206 added**, unless 202 was written against a live definition that already included it and simply omitted it from its own text by author error. Flagging as best finding — see lane summary.

`supabase/migrations/208_claim_versions_nondestructive_grounding.sql` — CREATED — `claim_versions` table (append-only preserved claim history, soft refs only, no cascading FK), 2 CHECK constraints on `supersede_reason`. Live: claim_versions 94 rows, src=1 scripts=3.

`supabase/migrations/209_provenance_status_on_delete.sql` — ALTERED `set_provenance_status()` — extends it to handle `TG_OP='DELETE'` (fixes a real bug: versioning a claim OUT of the ledger via a guarded DELETE never recomputed `provenance_status`, so a quarantined item with a since-fixed ledger stayed quarantined until an unrelated write happened to trigger a recompute); adds the missing `AFTER DELETE` trigger on `section_claim_provenance`. Live (re-pinned search_path by 248).

`supabase/migrations/210_claim_versions_orphaned_reason.sql` — ALTERED (constraint) — widens `claim_versions.supersede_reason` CHECK to add `orphaned_no_prose_referent` (a third sanctioned live-ledger exit: a claim whose text matches no prose paragraph anywhere in the brief). Live.

`supabase/migrations/211_drain_worklist_and_mutation_leases.sql` — CREATED — `drain_worklist` table (Lane A/B judgment split) + `mutation_leases` table + `acquire_mutation_lease()`/`heartbeat_mutation_lease()`/`release_mutation_lease()` (per-item mutual exclusion for concurrent drain sessions, mirroring 205's lock pattern). Live: drain_worklist 66 rows (src=0, scripts=0 — no code reference found at all, an operator-tool table read only by hand/ad-hoc SQL or a script this scan's src/scripts sweep didn't match); mutation_leases 0 rows (expected — leases are transient, held only while a session is actively working an item).

`supabase/migrations/212_corpus_census.sql` — CREATED — `corpus_census` table (one row per item, live + archived classification). Live: 655 rows, src=0 scripts=1 (operator-tool, script-only consumer).

`supabase/migrations/213_disposition_ledger.sql` — CREATED — `disposition_ledger` table (append-only tombstone record for deleted archived items, no FK, survives the item's own deletion by design). Live: 236 rows, src=0 scripts=3.

`supabase/migrations/214_coverage_gap_candidates.sql` — CREATED + seeded — `coverage_gap_candidates` table + 16 hand-curated INSERT rows (coverage-floor pricing input, explicitly "NOT a worklist"). Live: 109 rows in table-usage.txt (grown well past the 16 seeded here via later inserts, e.g. more added by disposition-tracking passes). **DEFECT/GAP**: migration 223 (`acquisition_backlog_v` view) references `c.data_class`, `c.discovery_class`, `c.disposition`, `c.surface_test`, `c.access_model` on `coverage_gap_candidates` — none of these 5 columns are created by this file, and no other migration in this lane (or, by inspection of every `coverage_gap_candidates`-touching file: 214, 223 only) ALTERs this table to add them. Either those columns were added to production out-of-band with no committed migration at all (the same class of drift migrations 219/223/254/256 exist specifically to close), or `acquisition_backlog_v` as committed cannot be created against a database built purely from this repo's migrations. See lane summary — this is the most consequential single finding in this lane.

`supabase/migrations/215_drop_source_conflicts_dormant.sql` — DROP (content-gated, DO block, aborts if row count ≠ 0) — `source_conflicts` table (CASCADE, dropping its 3 RLS policies with it). `source_conflicts` absent from table-usage.txt — consistent with having landed; the content gate (RAISE EXCEPTION if any row exists) means if it did land, the table really was empty.

`supabase/migrations/216_item_source_evidence.sql` — CREATED — `item_source_evidence` table (durable criterion-3 evidence store, append-only via trigger-enforced no-UPDATE/no-DELETE). DROPPED by 218 in the same lane (see below) — this table's entire lifecycle (create → use in a gate → prove-dead → revert) is contained within this lane.

`supabase/migrations/217_criterion3_durable_evidence_superset.sql` — ALTERED `validate_item_provenance` (surgical string-splice via `pg_get_functiondef`, anchor-verified) — makes criterion-3's span check a superset: pass if the span is in the working excerpt (`agent_run_searches`) OR in `item_source_evidence`. REVERTED by 218 in the same lane.

`supabase/migrations/218_revert_item_source_evidence_dead_duplicate.sql` — REVERT — restores criterion-3 to the pre-217 working-excerpt-only check (surgical splice, anchor-verified); DROPs the `item_source_evidence` table + its 2 triggers + `item_source_evidence_append_only()`. Rationale given: a full audit found the store was 0 rows, its writer produced content byte-identical to an existing queryable store (`agent_run_searches.result_content_excerpt`), and all 210 verified items already had criterion-3 evidence in that existing pool — so 216/217 built a genuine duplicate of existing structure. DEAD-HISTORICAL for 216/217; 218's revert is what's live (criterion-3 span check is working-excerpt-only, matching 145/150's shape, through 202).

`supabase/migrations/219_drop_dead_backup_and_superseded_tables.sql` — DROP (content-gated, 8-table DO block asserting each table's row count has not grown past its audit-time value) — `intelligence_items_pre_phase5`, `pending_jurisdiction_review_pre_phase5`, `item_supersessions_pre_phase5`, `ingest_rejections_pre_phase5`, `institution_regroup_snapshot_20260712`, `intelligence_items_domain_backfill_audit`, `hold_resolution_queue`, `briefings`. Header: "APPLIED 2026-08-19 via apply_migration" (i.e. applied at authoring time, unlike most of the 180-series files). None of the 8 appear in table-usage.txt — consistent. **Cross-reference to 254**: `hold_resolution_queue`'s drop here is what makes the 4 `hrq_*` SQL functions (`hrq_enqueue`/`hrq_escalate`/`hrq_exit`/`hrq_record_attempt`) broken from this point on — they read/write a table that no longer exists — and they are not actually dropped until migration 254, roughly three weeks later per the dates in the files (219 is 2026-07-19; 254 is 2026-08-11). This is a real, if narrow, window in which 4 live database functions would raise "relation does not exist" if called; 254's own header confirms zero callers existed in that window, so the exposure was latent, not exploited.

`supabase/migrations/220_portal_link_candidates_disposition.sql` — ALTERED — adds `disposition_reason`, `dispositioned_at`, `item_id` (FK, ON DELETE SET NULL) to `portal_link_candidates`. Live, uneventful.

`supabase/migrations/221_census_worklist.sql` — CREATED — `census_worklist` table (append-only, guarded-forward-only `enumeration_status` state machine via a BEFORE UPDATE trigger, no-DELETE via a BEFORE DELETE trigger), 7 indexes. Live: 21609 rows, src=2 scripts=1 — gains `identity_*` columns via 228 and `title`/`title_source` via 229.

`supabase/migrations/222_census_rollup_stitch.sql` — CREATED (retroactive capture) — `coverage_gap_census_findings` table (Session C's table, captured here with `CREATE TABLE IF NOT EXISTS` as a no-op-if-already-created retroactive commit) + `census_rollup_by_surface` view (stitches `census_worklist` and `coverage_gap_census_findings` per-surface, deliberately not joined by FK — a documented structural grain mismatch). Live: coverage_gap_census_findings 116 rows, src=0 scripts=1.

`supabase/migrations/223_acquisition_backlog_v.sql` — CREATED (retroactive capture, `CREATE OR REPLACE VIEW`) — `acquisition_backlog_v`, ranking `coverage_gap_candidates` into backlog sections. See DEFECT under 214 above: the view body references `data_class`/`discovery_class`/`disposition`/`surface_test`/`access_model` columns on `coverage_gap_candidates` that no migration in this repo (this lane or otherwise, so far as this lane's files show) ever adds. The view is itself only a retroactive capture of a live `pg_get_viewdef` snapshot, so the columns clearly exist in production — they were added to `coverage_gap_candidates` by DDL this repo's migration history does not record.

`supabase/migrations/224_item_gate_a_state.sql` — CREATED — `item_gate_a_state` table (Gate-A prose-fact scan state: `scanned_hash`, `orphan_count`, `orphans` jsonb), deliberately landed BEFORE any `validate_item_provenance` criterion consumes it (interlock-free ordering, so state accumulates before anything gates on it). Live: 984 rows, src=1 scripts=0.

`supabase/migrations/225_gate_a_criterion7.sql` — ALTERED `validate_item_provenance` (programmatic inject-and-apply via `pg_get_functiondef` + anchor splice, idempotent) — adds CRITERION 7: `gate_a_unproven_or_stale` when the item has a non-empty brief but no current-hash-clean `item_gate_a_state` row. Same open question as 206 above applies here: migration 202 (the last full-body CREATE OR REPLACE of `validate_item_provenance` in this lane) does NOT contain `item_gate_a_state`, `gate_a_unproven_or_stale`, or any criterion-7 text anywhere in its body. **AMBIGUOUS / possible regression**, same class as the 206 finding — see lane summary.

`supabase/migrations/226_gate_a_health_rpc.sql` — CREATED — `gate_a_health()` RPC (read-side honesty probe: invariant_violations, briefless_verified, no_gatestate_verified, verified_failing_revalidation, verified_gen_ver_null_info info-only). Grant to `service_role` here; later REVOKEd from PUBLIC/anon/authenticated by 248 (a real prior exposure: this diagnostic RPC was anon-callable). This exact function is later re-defined via a cache indirection by 256 (`gate_a_health_compute()`/`gate_a_health_refresh()`/cached `gate_a_health()`) — 226's direct-compute version is SUPERSEDED by 256's cached version.

`supabase/migrations/227_derived_claim_kind.sql` — ALTERED — adds `section_claim_provenance.basis_claim_id` (self-FK, ON DELETE SET NULL) + widens `claim_kind` CHECK to add `'DERIVED'` + adds `scp_derived_requires_basis` CHECK (a DERIVED claim must carry a basis). Live, uneventful — this is Gate B's DB-layer piece; no later migration in this lane references `basis_claim_id` or `DERIVED` inside `validate_item_provenance`'s body (consistent with Gate B's scanner being a separate JS-side mechanism per the file's own header, "gate-a-derived.derivedCoveredTokens... a PURE DB lookup", not itself a provenance-gate criterion).

`supabase/migrations/228_census_identity_verification.sql` — ALTERED — adds 6 identity-verification columns to `census_worklist` (`identity_resolves` etc., with an explicit true/false/NULL honesty contract: NULL means "could not confirm", never fabricated false). Live.

`supabase/migrations/229_census_worklist_title.sql` — ALTERED — adds `census_worklist.title`/`title_source`. Live.

`supabase/migrations/230_rls_lockdown_operator_control_residue.sql` — ALTERED (RLS ENABLE, no policies = deny-all) — `claim_versions`, `corpus_census`, `coverage_gap_candidates`, `coverage_gap_census_findings`, `disposition_ledger`, `drain_worklist`, `funded_pass_runlock`, `mutation_leases`. Fixes a real, documented prior exposure: these 8 tables had RLS DISABLED with full DELETE/INSERT/UPDATE/TRUNCATE/SELECT grants to `anon`+`authenticated` — anon-key-writable through PostgREST, including the spend runlock and the pipeline audit trail. Live.

`supabase/migrations/231_promotion_policy.sql` — CREATED — `promotion_policy` table (fail-closed: no active unexpired row ⇒ no auto-promotion spend), single-active partial unique index, RLS enabled from birth with no policies (deny-all; only the admin API, via service role, touches it). Live: 0 rows in table-usage.txt. NOTE for owner: by this table's own fail-closed design, 0 rows means auto-promotion is currently OFF — this is very likely intentional (matches the "fail-closed default" the migration's header states as the design goal), not a defect, but worth confirming with the operator that this is the desired current state.

`supabase/migrations/232_recent_changes_rpc.sql` — ALTERED (CREATE OR REPLACE) — `get_workspace_recent_changes(org,days)`: window-scoped (date-bounded, not priority-LIMIT-50-bounded) "what changed" feed, fixing a real bug (the home "This week" section went blind to in-window items once the corpus outgrew a LIMIT-50-by-priority slice — 216 in-window items rendered as "nothing"). Live, uneventful.

`supabase/migrations/233_watchlist_type_expansion.sql` — ALTERED (constraint) — widens `user_watchlist.item_type` CHECK to add `research`/`operations` (fixes a real gap: those two detail surfaces had no Watch affordance). Live: user_watchlist 1 row.

`supabase/migrations/234_item_ownership.sql` — ALTERED — adds `owner_user_id`/`owner_assigned_by`/`owner_assigned_at` to `workspace_item_overrides` + partial index. Live: workspace_item_overrides 4 rows.

`supabase/migrations/235_dual_scope_archive.sql` — ALTERED/CREATED — adds `workspace_item_overrides.archived_by`; creates `user_item_state` table (personal per-user archive, layered above the org override) with RLS ENABLED but shipped with NO policies (deny-all-by-omission, later hardened by 239); widens `notifications.kind` CHECK to add `'archive'`. Live: user_item_state 0 rows in table-usage.txt — the personal-archive feature has never been used in production (the write path exists via the service-role API but no user has archived anything personally yet).

`supabase/migrations/236_dual_scope_watchlist.sql` — ALTERED (constraint) — adds an `item_type` CHECK to `org_watchlist` (previously unconstrained), matching `user_watchlist`'s vocabulary (`source`/`reg`/`signal`/`research`/`operations`). Live: org_watchlist 0 rows — the team watchlist has never been used (later widened by 270 to also admit `market_series`, still 0 rows).

`supabase/migrations/237_personal_list_order.sql` — CREATED — `user_list_order` table (personal drag ordering, fractional `numeric` position for midpoint-insert reordering, 4 own-row RLS policies). Live: 0 rows in table-usage.txt — drag ordering has never been used in production despite being fully wired (src=2).

`supabase/migrations/238_reorder_user_list_item.sql` — CREATED — `reorder_user_list_item()` RPC (SECURITY DEFINER, advisory-locked per (user,list), atomic midpoint-insert with seeding + renormalization), granted to `service_role` only (explicitly revoked from PUBLIC/anon/authenticated — SECURITY DEFINER would otherwise let any authenticated caller rewrite another user's list order, since it bypasses the RLS policies on `user_list_order` that would normally stop that). Live, well-reasoned.

`supabase/migrations/239_user_item_state_policies.sql` — CREATED (RLS) — 4 own-row policies for `user_item_state`, closing the deny-all-by-omission gap 235 shipped (explicitly framed as hardening, not an incident fix, since the sole writer already used the service-role client which bypasses RLS either way). Live.

`supabase/migrations/248_security_grants_hardening_2026_08_09.sql` — ALTERED (REVOKE + search_path pin) — REVOKEs EXECUTE on `admin_set_pause_state`/`gate_a_health` from PUBLIC/anon/authenticated (a real prior anon-callable exposure on both, verified against server-only callers before revoking); re-pins `set_provenance_status`'s search_path (lost when 209 recreated it without carrying 160's pin forward — a real, if minor, regression 248 catches and fixes). Live.

`supabase/migrations/249_platform_admin_rls_alignment_2026_08_09.sql` — ALTERED (RLS) — repoints `integrity_flags_admin_read`/`integrity_flags_admin_update`/`holdings_quality_admin_read` from `org_memberships` owner/admin-of-any-org to `profiles.is_platform_admin`. Fixes a real, documented privilege-escalation path: `create_org_for_self()` lets any authenticated user become an org owner, and org-owner-of-any-org was previously sufficient to read AND TAMPER WITH platform-level integrity flags and read the capture-quality audit — i.e. any self-service signup could read and rewrite platform moderation flags. Live.

`supabase/migrations/250_provenance_verified_depth_binding.sql` — ALTERED — DROPs `stamp_prov_origin_trg`/`stamp_prov_origin()` (retiring a forgeable mechanism: any role could `SELECT set_config('app.prov_flip_origin','INSERT',true)` and satisfy the prior guard); rebuilds `guard_provenance_flip()` on `pg_trigger_depth()>=2` (only a write issued from inside another trigger — i.e. `set_provenance_status`'s own derivation — or the bound `reconciler` role may set `provenance_status='verified'`). This is a genuine, well-documented security fix for 3 real prior defects (forgeable GUC, an always-true depth check that excluded nothing, and — the widest one — the `quarantined→verified` transition being entirely unguarded, so any role could bless a quarantined item directly). Live; residual, self-documented limitation: table owner (`postgres`) can still disable the trigger.

`supabase/migrations/251_workspace_profile_readtime_anchoring.sql` — ALTERED + DATA — adds `workspace_settings.profile` jsonb (nullable-with-default) + one UPDATE seeding the single live workspace's row. Live: workspace_settings 1 row (the singleton this seeds).

`supabase/migrations/252_connection_discovery_basis_and_origin.sql` — ALTERED — adds `item_cross_references.basis`/`score`; widens the `origin` CHECK to add `'provenance_discovery'`. Live, uneventful.

`supabase/migrations/253_connection_themes.sql` — CREATED — `connection_themes` table (fully-replaced cache per clustering pass, `id` = natural cluster anchor for cross-pass stability) + `connection_theme_runs` (append-only audit trail), both with public-SELECT + service-role-write RLS. Live: connection_themes 9 rows src=2, connection_theme_runs 4 rows src=1.

`supabase/migrations/254_drop_shadow_gate_a_and_broken_hrq.sql` — DROP (heavily content-gated: 4 preconditions checked in a DO block before any DROP runs) — 12 `gate_a_*` SQL functions (a duplicate SQL re-implementation of the live TypeScript Gate-A scanner, zero live callers verified via `pg_depend`), 4 `hrq_*` functions (broken since 219 dropped their target table — see note under 219), `gate_a_route_b_baseline` table (430 rows exported to a committed CSV before drop — a genuinely careful tombstone-then-delete). Post-drop assertions explicitly verify the LIVE gate-A objects (`item_gate_a_state`, `gate_a_health_cache`, `gate_a_health()`) were not accidentally touched. Well-executed cleanup, no defect found.

`supabase/migrations/255_canonical_key_oj_sequence_suffix.sql` — ALTERED `derive_canonical_instrument_key()` — fixes the real bug in 200's original version (OJ sequence suffix `(NN)` discarded, collapsing genuinely-distinct EU instruments to one canonical key; one item was live mis-archived as a false duplicate as a direct consequence and later corrected — the migration names it, `bcdd0841`). Self-check DO block asserts the fix with 6 concrete assertions. Live, this is the current body of `derive_canonical_instrument_key()`.

`supabase/migrations/256_migration_homes_and_vault_capture_key.sql` — CREATED (retroactive capture) — `gate_a_health_cache` table + `gate_a_health_compute()`/`gate_a_health_refresh()`/cached `gate_a_health()` (the LIVE version, superseding 226's direct-compute version) + `next_uncensused_portal_candidates()` RPC + `capture_worker_fetch()` (moves a hardcoded anon-role JWT literal into Supabase Vault). NOTE: the migration file itself embeds the literal JWT string being vaulted (`PERFORM vault.create_secret('eyJhbGci...', ...)`) — this is a Supabase anon-role key, which is public-by-design (shipped in every client bundle per 165's own commentary), so this is not a credential leak in the security sense the migration's own reasoning correctly identifies, but it is worth the owner knowing that literal token is now permanently in git history as of this commit, not just in the live vault. Live.

`supabase/migrations/257_reconciler_select_policies.sql` — CREATED (RLS) — 3 SELECT policies for `reconciler` on `intelligence_items`/`intelligence_item_sections`/`sources` (fixes a real bug of the exact class 169 fixed for 3 other tables: table GRANT present, no covering RLS policy = inert; the reconciler could UPDATE `intelligence_items` but not read the rows/sources/sections it was supposed to be reconciling). `sources_reconciler_select` specifically is later DROPPED by 259 as a redundant duplicate of the pre-existing unconditional `sources_read` policy — not a regression, an explicitly-reasoned de-stack.

`supabase/migrations/258_emission_factors_and_licence_gate.sql` — CREATED — `data_sources` table (licence register, 27 seeded sources with redistribution verdicts) + `licence_clear_sources` view; `cl_corridor_field()`/`cl_corridor_id()` (content-addressed corridor identity, pgcrypto digest); `emission_factors` table (5-tier, 4-scope-kind envelope with extensive generated CHECKs, append-only via a BEFORE UPDATE/DELETE trigger permitting only `superseded_by`/`valid_to` to change); `emission_factor_candidates` view. Shipped with `mode` accepting `'sea'` as canonical — later found wrong and fixed by 263. Also shipped with a duplicate inline `mode` CHECK alongside the codegen'd one (defect #2 in 263's own header) — both defects fixed in 263, safe because the table held 0 rows at the time. Live (current shape is 258+263 combined); table-usage.txt shows emission_factors now has 6 rows and data_sources 27 — data_sources itself shows src=0 scripts=0 in table-usage.txt (AMBIGUOUS: either genuinely unread outside the `licence_clear_sources`/`emission_factor_candidates` views this scan may not attribute back to the base table, or the elaborate licence-gate machinery this migration describes is not yet wired into any live read path — cannot distinguish from migration text alone).

`supabase/migrations/259_rls_initplan_and_policy_destack.sql` — ALTERED (RLS, `ALTER POLICY` in place, no drop/create window) — wraps `auth.role()`/`auth.uid()` in `(select ...)` across 27 policies on 8 tables (performance fix: unwrapped calls are re-evaluated per row instead of once per query — Supabase's documented `auth_rls_initplan` anti-pattern, sourced to a real advisor run showing 1.3–2.5s query times); DROPs the now-redundant `sources_reconciler_select` (see 257); narrows `intelligence_items_read`'s role list off `public` to `TO anon, authenticated` (deliberate: any future role needing read access must be explicitly added, rather than inheriting it implicitly). Semantics-preserving by construction (STABLE function, same predicate). Live; completed by 262.

`supabase/migrations/260_fk_indexes_and_scanner_hygiene.sql` — CREATE INDEX ×8 (all `CONCURRENTLY`) — covering indexes for 8 previously-unindexed FK columns across `intelligence_items`/`org_watchlist`/`user_item_state`/`workspace_item_overrides`. **NOTE (operational hazard, self-documented)**: this file cannot be applied through the normal transactional migration runner (`supabase db push` / the `apply_migration` MCP tool) at all — `CREATE INDEX CONCURRENTLY` is rejected by Postgres inside a transaction block, and the file's own header says it is "COMMITTED AS DOCUMENTATION / OWNER-BUNDLE ONLY," to be run via a direct psql session outside the usual tooling. This means the normal migration-ledger apply path used for every other file in this lane cannot apply this one; whether it was ever actually run outside that path is not verifiable from the file or from table-usage.txt (indexes aren't tracked there). AMBIGUOUS — flagged, not confirmed either way.

`supabase/migrations/261_drop_dead_notification_v1.sql` — DROP (content-gated) — `notification_deliveries`, `notification_events`, `notification_subscriptions` (all confirmed 0 rows, 0 code readers/writers, sole writer endpoint already deleted). Careful to distinguish this dead trio from the live `notifications` table (bell-icon inbox) which it explicitly does not touch and asserts still exists post-drop. None of the 3 dropped names appear in table-usage.txt (`notifications` does, with 0 rows/src=5 — the live inbox, untouched). Consistent with having landed.

`supabase/migrations/262_rls_initplan_sweep.sql` — ALTERED (RLS, dynamic DO-block loop over `pg_policies`) — completes 259's sweep: rewrites the remaining 109 policies (of the original 143 `auth_rls_initplan` advisor findings) across 43 tables, mostly `community_*` plus `org_invitations`/`moderation_reports`/`notification_preferences`/`profiles`. Deliberately loop-driven off live policy definitions rather than hand-transcribed (explicitly reasoned: a typo in an RLS predicate fails silently in the dangerous direction). Post-check re-queries `pg_policies` fresh rather than trusting the loop counter. Well-executed; no defect found.

`supabase/migrations/263_mode_vocabulary_ocean_canonical.sql` — ALTERED — changes `emission_factors`'s canonical mode token from `sea` to `ocean` (operator overrule of 258's original reasoning) and drops the duplicate inline `mode` CHECK 258 had left alongside the codegen'd one. Content-gated (aborts if `emission_factors` has any rows — it had 0 at this point). Fixes both real defects named in 258's own review above. Live; the post-check catches its own first-draft bug (an `ILIKE '%mode%'` false-positive against the word "modelled") and corrects it to a word-boundary regex — visible directly in the committed file as a documented near-miss.

`supabase/migrations/264_rename_result_content_excerpt.sql` — RENAME COLUMN — `agent_run_searches.result_content_excerpt` → `result_content` (the column is NOT an excerpt — it is the full captured content the entire grounding pool, including `validate_item_provenance` criterion 3, reads verbatim from; the migration's stated motive is that the old name had already invited one Cowork-agent proposal to truncate it to 2,000 chars, which ADR-016 had to refuse). Rebuilds `validate_item_provenance` from its own live `pg_get_functiondef` text via substitution (never hand-transcribed) inside the same DO block as the rename, with a zero-flip assertion (verified-item count must be identical before/after). Live; this is a genuine dependency this lane's earlier files (145/150/158/171/202) all show reading `result_content_excerpt` — so as of 264 every one of those SELECT lists is, in the live function, reading `result_content` instead; the file text of the earlier migrations is historically accurate to when they were authored but no longer matches the live column name.

`supabase/migrations/265_drop_detect_intersections.sql` — DROP — `detect_intersections(integer,integer)` (a second, now-superseded intersection-scoring implementation; the live scoring home is `item_cross_references` populated by `discover.mjs`/`write-edges.mjs` since 252). Single-line DROP, well-justified, ordered to apply after its sole consumer route was already re-pointed.

`supabase/migrations/266_theme_briefs.sql` — CREATED — `theme_briefs` table (durable per-theme editorial brief content, `member_hash` for staleness detection against `connection_themes`, which is fully replaced on each re-cluster). RLS enabled, no policies (deny-all; service-role/session-executor writes only). Live: 9 rows, src=2 scripts=2.

`supabase/migrations/267_origin_class_and_envelope.sql` — ALTERED (all additive, all nullable, explicitly NO backfill, NO NOT NULL) — adds `intelligence_items.origin_class`; adds the full WO-12 envelope (`value_numeric`, `unit`, `currency`, `derivation`, `origin_class`, `source_key` FK, `source_ref`, `n_observations`, `method_version`, `as_at_date`, `reference_period`) to `regional_data_facts`; adds `state_cost_facts.origin_class`. All CHECKs generated from the same shared vocabulary module 258 introduced (asserted byte-identical by the migration's own reasoning). Live; table-usage.txt shows `regional_data_facts` 86 rows (envelope columns presumably still mostly NULL per the migration's explicit "no backfill here" framing — cannot confirm the NULL rate from table-usage.txt alone, which reports row counts, not column fill rates).

`supabase/migrations/268_market_series.sql` — CREATED — `market_series` table (WO-16 time-series spine, same envelope shape as 267, `UNIQUE(series_key, reference_period)` idempotency key), 2 lookup indexes, authenticated-read RLS. Shipped schema-only (0 rows, explicitly asserted by its own post-check), kill-switched producer to ship in the same PR but stay off until operator-armed. Live and since populated: table-usage.txt shows market_series 6 rows, src=6, scripts=5 — the producer has since been armed and the table is actively read (a genuine forward-evolution beyond what this migration alone shows).

`supabase/migrations/269_routing_rpcs_use_surface_of.sql` — ALTERED (CREATE OR REPLACE) — `get_research_items`, `get_operations_items`, `get_market_intel_items` WHERE predicates switched from hand-written `item_type IN (...)` lists to `surface_of(item_type,domain) = '<surface>'`. Fixes a real, measured, customer-visible defect: the three hardcoded predicates had drifted from `surface_of()`'s own definition, producing an 18% undercount on the Research masthead-vs-list-page numbers (31 cards shown vs 38 in the (correct) count RPC) and 12 items shown on the wrong surface entirely (enumerated concretely in the file, e.g. 4 initiative-type items sitting under domain 7 that belong in Research, not Market). `get_technology_items` is explicitly, deliberately NOT touched here (still on its own hardcoded predicate from 134 — see note under 133/134). Superseded again by 272 (adds `jurisdiction_iso` to the same 3 functions' RETURNS TABLE + SELECT list, otherwise byte-identical).

`supabase/migrations/270_widen_org_watchlist_market_series.sql` — ALTERED (constraint) — widens `org_watchlist.item_type` CHECK to add `market_series`; explicitly does NOT widen `user_watchlist` (team-scope-only feature by design). Live; org_watchlist remains 0 rows per table-usage.txt (feature unused).

`supabase/migrations/271_assumption_register.sql` — CREATED — `assumption_register` table (WO-20: the register for the product's OWN modelling constants, distinct from `emission_factors` which is for externally-published numbers), same envelope shape as 267/268 minus `currency`/`reference_period`, `status` CHECK (active/superseded/retired), authenticated-read RLS. Shipped schema-only (0 rows asserted by its own post-check, explicitly "no seed rows, no backfill, no consumer wiring" — a pure record/register with no runtime read path by design). Live per table-usage.txt: 0 rows, src=0, scripts=3 — 3 scripts reference it (presumably the intended future backfill/drift-check scripts named in the file's own comments, e.g. the not-yet-built `assumption-register-drift.mjs`) but nothing in `src/` reads it yet, consistent with "read-only display consumer only... not yet built by this migration" in the table's own COMMENT.

`supabase/migrations/272_customer_rpcs_project_jurisdiction_iso.sql` — DROP+CREATE ×8 (`get_workspace_intelligence`, `get_workspace_intelligence_slim`, `get_workspace_intelligence_dashboard`, `get_workspace_intelligence_listings`, `get_research_items`, `get_operations_items`, `get_market_intel_items`, `get_technology_items`) — appends `jurisdiction_iso text[]` to each RETURNS TABLE/SELECT list (fixes a real bug: `Resource.jurisdictionIso` could never populate on any surface because none of the 8 customer-facing RPCs projected the column, despite it existing on `intelligence_items` since migration 033 and already being passed through `_workspace_active_items`). Required DROP-then-CREATE because Postgres refuses `CREATE OR REPLACE` on a RETURNS TABLE shape change — the migration's own header documents hitting this Postgres error live during a first CREATE-OR-REPLACE-only attempt, and explicitly restores the 4 execute grants (`anon, authenticated, service_role`) DROP removes, since grants are not preserved across a DROP. This is the highest-numbered migration in this lane; the versions of all 8 RPCs it creates are, as far as this lane's files show, the live ones (modulo `get_technology_items` still carrying its 134-era hardcoded predicate, per the note under 133/269).

## Coverage attestation

Files read in full: 121/121. Lines read: 11,696/11,696 (per `awk` sum over the lane's own line-count column, matching the ground-truth file; every file was read via a single `Read` call and none was truncated — the largest file, 258, is 426 lines, well under the 2000-line successive-offset threshold). No file was skipped.

## Lane summary

**Counts by disposition** (this lane uses migration-lane per-file reporting rather than the STATUS vocabulary; approximate buckets):
- CREATED (new table/view/function, still live): ~54
- ALTERED (column/constraint/RLS/function-body change on an existing object, still live): ~52
- DROPPED (table/function/view removed, confirmed or highly likely applied): ~15 (124-134 band's superseded RPC bodies not counted separately; counting distinct DROP-bearing migrations: 180, 181, 182+183, 184, 192, 215, 218 (partial), 219, 254, 261, 265, 272 (drop+recreate))
- DATA-only / one-shot backfill (DEAD-HISTORICAL): 149, 170, 251(partial), 214(seed rows)
- Explicitly headed AUTHOR-ONLY / NOT-APPLIED-AT-AUTHORING but inferred later-applied from absence-of-target evidence: 180, 181, 182, 183, 184, 185(unconfirmed), 190, 191, 192, 195(confirmed applied via table-usage.txt)

**The 15 findings that matter most, ranked:**

1. **`acquisition_backlog_v` (223) references 5 columns on `coverage_gap_candidates` — `data_class`, `discovery_class`, `disposition`, `surface_test`, `access_model` — that no migration in this repo creates.** Migration 214 (the table's own CREATE TABLE, read in full above) does not define them, and no ALTER TABLE for `coverage_gap_candidates` appears anywhere else in this lane. Either they were added to production entirely outside the migration system, or the view as committed cannot be built from a clean migration replay. This is exactly the class of schema drift migrations 219/223(itself, partially)/254/256 exist to close, and it was missed at the column level even though the table-level drift (the view's own missing CREATE) was caught. `supabase/migrations/214_coverage_gap_candidates.sql`, `supabase/migrations/223_acquisition_backlog_v.sql`.

2. **Two possible silent regressions of `validate_item_provenance` criteria via full-body CREATE OR REPLACE.** Migration 206 patches in a `fact_mint_hold` criterion-3 check (mint-time S-CONFLATE hard-hold) and migration 225 patches in a criterion-7 Gate-A check — both via anchor-verified `pg_get_functiondef` splices against whatever the *live* body was at the time. Migration 202 (later than both, and the last full-body `CREATE OR REPLACE` of this function in this lane) reproduces neither `mint_hold_reason`/`fact_mint_hold` nor `item_gate_a_state`/criterion 7 anywhere in its text, despite its own header claiming "everything else... preserved verbatim." If 202 was authored against a base body that already included 206's and 225's patches and the author simply omitted restating them, this is only a documentation gap; if 202 was authored against an earlier lineage snapshot that predates 206/225, then applying 202 would silently un-gate two previously-enforced integrity criteria. I cannot resolve this from file content alone — it depends on which live definition 202 was diffed against, which the file doesn't state. `supabase/migrations/206_mint_gate_hold_marker.sql`, `supabase/migrations/225_gate_a_criterion7.sql`, `supabase/migrations/202_standard_own_body_floor.sql`.

3. **Migration 164 documents and fixes a real live security regression**: `get_market_intel_items` lost its `_assert_org_membership()` org-scoping check when an out-of-lane migration (108) rewrote it as pure SQL, leaving any authenticated caller able to read another org's `workspace_item_overrides` overlay via a forged `p_org_id`. Masked only by single-tenancy at the time. `supabase/migrations/164_market_intel_org_gate.sql`.

4. **Migration 250 documents and fixes 3 real defects in the `provenance_status='verified'` write guard**: a forgeable session GUC any role could set via `set_config`, an always-true `pg_trigger_depth()>=1` check that excluded nothing, and — the most serious — the dominant `quarantined→verified` transition being completely unguarded, so any role could bless a quarantined item as verified directly. `supabase/migrations/250_provenance_verified_depth_binding.sql`.

5. **Migration 255 documents a real live data-integrity incident caused by migration 200's own bug**: `derive_canonical_instrument_key()` discarded the EU OJ sequence suffix `(NN)`, collapsing genuinely-distinct instruments to one canonical key; item `bcdd0841` was live mis-archived as a false duplicate as a direct consequence and had to be manually corrected. `supabase/migrations/200_canonical_instrument_key.sql`, `supabase/migrations/255_canonical_key_oj_sequence_suffix.sql`.

6. **Migration 269 documents and fixes a customer-visible, measured 18% undercount** on the Research surface (masthead said 38, the list-page RPC showed 31) plus 12 items routed to the wrong surface entirely, caused by 3 duplicate hand-written `item_type IN (...)` predicates drifting from the single `surface_of()` classification home. `get_technology_items` is explicitly left unfixed (still on its own separate, older hardcoded predicate) — a known, named, still-open gap as of the newest migration in this lane. `supabase/migrations/148_surface_counts.sql`, `supabase/migrations/269_routing_rpcs_use_surface_of.sql`, `supabase/migrations/272_customer_rpcs_project_jurisdiction_iso.sql`.

7. **Migrations 157/165/166/167/168/169/230/248/249/257/259 form a long, well-documented tail of real prior RLS/grant defects** (public-readable quarantined items and staged updates; profiles with zero write policies so self-edit silently no-opped; provisional_sources with zero read policy so the admin queue rendered silently empty; a phantom `reviewer_notes` column causing 500s and a duplicate-mint retry risk; 5 aux tables leaking quarantined-item existence through unrestricted `USING(true)` policies; the reconciler role having table grants but no covering RLS policy on 6 different tables across 3 separate migrations (163, 169, 257); 8 operator-control tables RLS-disabled and anon-writable; 2 anon-callable admin RPCs). All fixed within this lane; listed together because the sheer count (11 separate RLS/grant-hardening migrations touching ~20 distinct tables) suggests the original RLS rollout across this schema was systematically under-covered, not a one-off. 

8. **`hold_resolution_queue` was dropped by migration 219 (2026-07-19) but its 4 dependent `hrq_*` SQL functions were not dropped until migration 254 (2026-08-11)** — roughly 3 weeks during which those 4 live functions would have raised "relation does not exist" if called. 254's own audit found zero live callers in that window, so this was latent rather than exploited, but it is a real gap in drop-ordering discipline. `supabase/migrations/219_drop_dead_backup_and_superseded_tables.sql`, `supabase/migrations/254_drop_shadow_gate_a_and_broken_hrq.sql`.

9. **Migration 260 (FK indexes) cannot be applied through this project's normal migration tooling at all** — `CREATE INDEX CONCURRENTLY` is rejected inside the transaction wrapper `supabase db push`/`apply_migration` uses, and the file's own header says it is "documentation / owner-bundle only," to be run by hand outside that tooling. Whether it was ever actually run is unverifiable from this repo. `supabase/migrations/260_fk_indexes_and_scanner_hygiene.sql`.

10. **Several fully-wired features show 0 live rows in table-usage.txt**, meaning the write path exists and is code-reachable but has never actually been exercised in production: personal item archive (`user_item_state`, migrations 235/239), personal drag ordering (`user_list_order`, migrations 237/238), the team watchlist (`org_watchlist`, migrations 236/270, widened twice for a feature nobody has used), and the promotion-policy engine (`promotion_policy`, migration 231 — 0 rows, which by the table's own fail-closed design means auto-promotion spend is currently off). Not necessarily defects, but worth the owner's attention as a portfolio of shipped-but-unused surface area.

11. **`community_post_signoff_requests` (153/154) is code-wired (src=4) but has 0 live rows** — the verifier sign-off feature has never received a real request despite being fully built and RLS-hardened.

12. **`drain_worklist` (211) has 66 live rows but src=0, scripts=0** in table-usage.txt — no code path this scan matched reads or writes it, despite it being a load-bearing table per its own header (the parallel-drain lane split). Likely read only via ad-hoc/manual SQL or a script naming pattern outside this scan's sweep; worth the owner confirming it isn't an orphaned data store.

13. **`system_state_flag_audit` (201) accumulates 8 rows with src=0, scripts=0** — the pause-flag audit trail is written to (via the trigger) but nothing reads it back anywhere; it exists purely as an unsurfaced forensic log.

14. **A hardcoded Supabase anon-role JWT literal is committed verbatim inside migration 256's SQL text** (`PERFORM vault.create_secret('eyJhbGci...', ...)`). The migration's own reasoning that this is fine (anon keys are public-by-design, shipped in every client bundle) is correct and consistent with 165's framing elsewhere in this lane — flagged as a NOTE for the owner's awareness, not a defect, since the token is intentionally public.

15. **Two files share the literal migration number 151** (`151_published_price_statistics.sql` and `151_state_cost_facts.sql`) — a real filename-numbering collision in this lane's file list. Does not appear to break `supabase db push` (which keys on the full filename), but is worth normalizing to avoid future ambiguity in the migration sequence's own numbering discipline, which every other migration in this lane treats as meaningful and monotonic.

**Cross-references to table-usage.txt worth the owner's attention (0 rows or 0 code refs on tables this lane creates or touches):** `assumption_register` (0 rows, by design — schema-only, no backfill yet), `promotion_policy` (0 rows, fail-closed-by-design), `user_item_state` / `user_list_order` / `org_watchlist` (0 rows, unused features), `funded_pass_runlock` / `mutation_leases` (0 rows, expected — transient locks), `org_member_bans` (0 rows, expected — no ban issued yet), `community_post_signoff_requests` (0 rows despite src=4), `data_sources` (src=0, scripts=0 — possibly view-mediated access this scan doesn't attribute back), `drain_worklist` (66 rows, src=0, scripts=0), `system_state_flag_audit` (8 rows, src=0, scripts=0).
