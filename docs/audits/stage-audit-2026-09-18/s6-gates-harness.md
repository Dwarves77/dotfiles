# s6-gates-harness: stage audit 2026-09-18

Method: read `.discipline/{governance,fitness,rules}/*`, `run-test-suite.sh`, `.github/workflows/{discipline,maintenance}.yml`, `.claude/settings.json`, `docs/PROGRAM-BOARD.md`, `docs/ops/dispatch-ledger.jsonl`, and the prior audit's `loop-harness-flywheel-one-unit.md` / `skills-rules-doctrine.md` in full. Ran live, this session: `execution-wiring.test.mjs` (3/3), `invariant-coverage.mjs` (PASS), `F45-duplicate-code.test.mjs` (7/7, live ratchet), `F44-broken-main-guard.test.mjs` (9/9, live), `audit-finding-status.mjs`; read-only SQL against the live Supabase project for `community_promotion_transitions`, `post_promotions`, `community_groups`, `supabase_migrations.schema_migrations`; `gh run list --workflow=discipline.yml` for CI ground truth. HEAD moved under me twice during this session (`806c0c48` then `7f28bbcd`) because the worktree is shared with other live sessions on branch `lane/w10-plan-revision-2026-09-18`; final reads are against `7f28bbcd` (2026-09-18 13:31:01 -0400), one commit ahead of master `3da30b22`, matching the common brief's description.

## The table

| Component | Where (file / workflow / table) | 1 Reachable | 2 Run | 3 Populated | 4 Visible | 5 Gated | 6 Documented | Verdict | Evidence |
|---|---|---|---|---|---|---|---|---|---|
| Execution-wiring resolver | `.discipline/governance/execution-wiring.mjs` | YES [CONFIRMED, file read, 7 surfaces derived from real runners] | YES [CONFIRMED, ran `execution-wiring.test.mjs` live this session: 3/3 pass, 0.28s] | N/A | N/A | YES [CONFIRMED, called from `invariant-coverage.mjs`, itself a `discipline.yml:232` step, no `continue-on-error`] | YES [CONFIRMED, header cites CLAUDE.md rule 15] | COMPLETE | `node --test .discipline/governance/execution-wiring.test.mjs` |
| Invariant-coverage meta-gate | `.discipline/governance/invariant-coverage.mjs` | YES | YES [CONFIRMED, ran live this session: `129 invariants + 63 doctrines wired, ENFORCED 116/13 exempt, doctrine 44/19, PASS`, 0.4s] | YES (129 invariants, 63 doctrine entries) [CONFIRMED, same run] | N/A (console gate, no UI) | YES [CONFIRMED, `discipline.yml:232`] | YES | COMPLETE | `node .discipline/governance/invariant-coverage.mjs` |
| Fitness manifest (F2-F47, 41 registered functions) | `.discipline/fitness/manifest.mjs`, `.discipline/fitness/functions/*.mjs` | YES [CONFIRMED, 41 imports read, each with a dated header and a non-empty scope] | PARTIAL [CONFIRMED for F44, F45 individually this session (both live, both green, see below); the full 41-function runner was NOT run this session, running it is close to "the full suite" the brief excludes; today's discipline CI (`gh run list`, run `35374142991`, master, 2026-09-18T17:24:25Z, completed success, 3m28s) includes the fitness step, corroborating but not independently re-derived by me] | N/A | N/A | YES [CONFIRMED, `discipline.yml:335` runs `fitness/runner.mjs`] | YES | COMPLETE | `gh run list --workflow=discipline.yml`; per-function test runs below |
| F44 broken-main-guard (Windows argv/import.meta.url class) | `.discipline/fitness/functions/F44-broken-main-guard.mjs` | YES | YES [CONFIRMED, ran live: 9/9 pass incl. "LIVE: the whole scoped tree passes F44 clean", 0.7s] | N/A | N/A | YES (`.discipline`+`scripts` glob, run-test-suite.sh) | YES | COMPLETE | `node --test .discipline/fitness/functions/F44-broken-main-guard.test.mjs` |
| F45 duplicate-code ratchet | `.discipline/fitness/functions/F45-duplicate-code.mjs`, `DUPLICATED_LINES_CEILING=6227` | YES | YES [CONFIRMED, ran live: 7/7 pass incl. "LIVE ratchet: the tree measures exactly the committed ceiling", 3.4s] | YES (ceiling re-seeded 4 times since 2026-09-17 landing: 8061→7569→6866→6830→6227, each a real measured value) [CONFIRMED, file comment + live test] | N/A | YES | YES (extensive header) | COMPLETE | `node --test .discipline/fitness/functions/F45-duplicate-code.test.mjs` |
| Closure gate, NEVER-RUN + STALE-NEXT + LANE-CONTRACT | `.discipline/governance/closure-gate.mjs` | YES | YES [CONFIRMED via CI: today's green discipline run includes `closure-gate.mjs` at `discipline.yml:244`, no `continue-on-error`; a session-local live rerun was attempted twice and both timed out at this audit's wall-clock budget (60s hard kill, then the test's own 30s per-test default), the script's git-log/blame/merge-base cost has grown with the tree, see "what the operator must rule on"] | `NEVER_RUN_ALLOWLIST={}`, `STALE_NEXT_ALLOWLIST={}` (both empty in source) [CONFIRMED, file read lines 481, 499]; live PROGRAM-BOARD.md today has 0 rows matching the gate's own NEXT-row regex [CONFIRMED, `grep -nE '^\|\s*\*{0,2}(NEXT\b\|next:)' docs/PROGRAM-BOARD.md` = 0 hits], so STALE-NEXT has nothing to flag | N/A (console gate) | YES [CONFIRMED, `discipline.yml:244`] | YES (extensive header, plus PROGRAM-BOARD train-log entries for trains 48-50 recording live `--report PASS` runs) | COMPLETE | file read + `gh run list`; grep on PROGRAM-BOARD.md |
| Closure gate, WRITER-READER check | `.discipline/governance/closure-gate.mjs` `checkWriterReader`, reuses `producer-consumer-orphan.mjs`, scope = migrations >= 266 | YES | YES [CONFIRMED via the same today-green CI run; session-local live rerun not completed, see above] | `WRITER_READER_ALLOWLIST={}` (empty) [CONFIRMED, file read line 523]; scope has grown sharply since the 2026-09-05 audit's own live count ("34 tables, 29 rpcs, 1 write-orphan gating:1, 0 read-orphans"), live migration count today is 324 applied / files on disk to 325 [CONFIRMED, live SQL `supabase_migrations.schema_migrations` count=324, top version `20260918015309`; `ls supabase/migrations/*.sql` highest number 325], so the true current table/orphan count is [HYPOTHESIS] not re-derived this session | N/A | YES | YES | COMPLETE, with a re-measurement gap flagged | live SQL; file read |
| Harness-run integrity (F28) + `ALLOWED_FAMILIES` registry | `.discipline/fitness/functions/F28-harness-run-integrity.mjs`, `scripts/lib/run-artifact.mjs` | YES | not independently re-run this session [HYPOTHESIS, carried from wiring pattern above] | YES, 11 families now registered (`mint, screen, fetch-drain, meta-harness, forward-events, source-sweep, ledger-consume, change-detection, propagation, corpus-turn, brief-apply`) [CONFIRMED, file read] vs 9 named in the plan's own section 1 and 10 found by the 2026-09-05 audit, `brief-apply` is new since then | N/A | YES (F28 in the manifest, CI-wired per fitness runner) | YES | COMPLETE, extends the plan's list by 2 families | file read; see harness-artifact row below for freshness |
| Harness-run artifacts, per family | `fsi-app/scripts/harness-runs/{family}/*-run-NNN.json` | YES (all 11 dirs exist) [CONFIRMED, `ls`] | PARTIAL, artifact recency varies sharply [CONFIRMED, `started_at` read from the highest-numbered file in each dir today, 2026-09-18]: `brief-apply` 2026-09-17 (1 day old), `propagation` 2026-09-11 (7d), `ledger-consume` 2026-09-05 (13d), `source-sweep` 2026-09-05 (13d), `corpus-turn`/`forward-events`/`mint`/`meta-harness` all 2026-09-04 (14d), `change-detection` 2026-09-04 (14d), `fetch-drain` 2026-09-01 (17d), `screen` 2026-08-31 (18d) | YES, counts per family: brief-apply 6, change-detection 5, corpus-turn 2, fetch-drain 3, forward-events 35, ledger-consume 7, meta-harness 8, mint 29, propagation 8, screen 3, source-sweep 18 | N/A | YES (F28 checks family-with-≥2-runs has attestation) | YES (`CONVENTION.md` present) | PARTIAL | `ls -t`, `grep started_at` on the 11 latest files, this session |
| `ledger-consume` apply half | `run-ledger-consume.mjs`, `portal_link_candidates` | YES | NOT [carried [HYPOTHESIS] from the 2026-09-05 audit's live SQL: 57,469 candidate / 3 promoted, `LEDGER_CONSUME_APPLY_ENABLED=true` but zero `mode:"apply"` runs ever recorded; NOT independently re-queried live this session, out of s6's cross-cutting scope, this is s2's mint-gate finding 1, restated here only because it is the harness family (`ledger-consume`) whose own last artifact (2026-09-05) is now 13 days stale] | as of 2026-09-05: 57,469/3 | N/A | N/A | YES (PENDING-RUN.md) | BUILT-DORMANT (carried, not re-verified) | prior audit + artifact-date staleness above |
| Dispatch ledger (NEVER-RUN's 3rd evidence source) | `docs/ops/dispatch-ledger.jsonl` | YES | PARTIAL [CONFIRMED, `wc -l` = 81 lines, last row dated 2026-09-07, run_id `34082329193`; today is 2026-09-18, 11 days / roughly trains 55-71 with zero new ledger rows, while `session-log.md` has entries through 2026-09-18 and 24 new maintenance steps (62 today vs the 38 the 2026-09-05 audit counted) have landed in that window] | 81 rows, stale 11 days | N/A | it is one of three NEVER-RUN evidence sources (`harnessArtifact \|\| runbookRecord \|\| ledgerEntry`); the other two (harness artifacts, MAINTENANCE-RUNBOOK.md) still receive evidence, so this gap does not by itself fail the gate, but it narrows the margin for any of the 24 new steps that lack a harness family or a runbook section -record | NOT self-documented as a known gap anywhere read this session | PARTIAL | `wc -l`, `tail -3` on the file |
| Maintenance.yml step registry | `.github/workflows/maintenance.yml` | YES, all 62 [CONFIRMED, `grep -oE "env.RUN_STEP == '[a-z0-9-]+'"` returns exactly 62 distinct ids, matching the 62-entry `options:` dropdown 1:1, no orphan option without a step block or vice versa] | not verified per-step this session (62 is too many for the audit budget); aggregate evidence: dispatch-ledger has 43 `"workflow":"maintenance"` rows total (through 2026-09-07) [CONFIRMED, grep count] | 62 registered steps, up from 38 at the 2026-09-05 audit [CONFIRMED, option-list diff] | N/A | per-step `if:` gates present; 3 steps require an `arg=` per the prior audit (not re-verified) | YES, `docs/runbooks/MAINTENANCE-RUNBOOK.md` has 66 `## ` section headers [CONFIRMED, `grep -c`] for 62 steps (a few steps share a section or the runbook covers non-step topics too; not reconciled 1:1 this session) | PARTIAL | grep counts above |
| Discipline test suite (`run-test-suite.sh`) | `.discipline/run-test-suite.sh`, `.github/workflows/discipline.yml` | YES | YES via CI [CONFIRMED, today's green discipline run includes this step; not re-run locally this session, the full suite is explicitly excluded by the brief] | ~150 glob/literal lines covering `.discipline/`, `scripts/`, `src/` test trees, single source-of-truth for both CI and pre-push (file's own header) | N/A | YES, is itself a discipline.yml step | YES (extensive header, named exclusions documented) | COMPLETE | file read; `gh run list` |
| Pre-push hook trampoline | `.git/hooks/pre-push` (installed, common git dir), `fsi-app/.discipline/hooks/pre-push`, `install-hooks.mjs` | YES | YES [CONFIRMED, read the actually-installed file at the common git dir: `#!/bin/sh … exec sh "$top/fsi-app/.discipline/hooks/pre-push" "$@"`, dated 2026-09-16 17:40, matches the D19 trampoline shape exactly, 8 stale `pre-push.backup-*` files sit alongside it from May through Sep 13, none live] | N/A | N/A | YES, tracked hook's own step 0 refuses to run unless `DISCIPLINE_HOOK_TRAMPOLINE` is set | YES (`install-hooks.mjs` header, D19 note) | COMPLETE | direct read of `$(git rev-parse --git-common-dir)/hooks/pre-push` |
| Merge-train / assemble mechanism | `scripts/lib/assemble-train.mjs`, `docs/PROGRAM-BOARD.md` train-log entries | YES | YES [CONFIRMED, PROGRAM-BOARD.md's own train-log rows for trains 48-50 cite live `assemble-train --fold --propose --ledger` output (folded/conflicts/derived-rows counts); current train is 71 [CONFIRMED, `git log --oneline HEAD \| grep train/wave` highest = wave71]] | N/A | N/A | it is the mechanism that produces closure-gate's train numbering, not itself gated | YES (test file `assemble-train.test.mjs`, wired via run-test-suite.sh) | COMPLETE | git log; PROGRAM-BOARD.md text |
| PreToolUse skill gate (out-of-repo) | `.discipline/governance/pretooluse-skill-gate.mjs`, `check-pretooluse-wired.mjs`, `~/.claude/settings.json` (outside this repo, per memory) | N/A, deliberately outside the repo boundary [CONFIRMED, file header: "settings.json is outside the repo, so this check runs in pre-push (on the operator's machine)"] | not checkable from this worktree; the repo's own `.claude/settings.json` (dotfiles root) carries no `PreToolUse` entry [CONFIRMED, `grep -rn PreToolUse .claude/settings*.json` = 0 hits], which is correct by design, not a gap | N/A | N/A | YES, verified only on the operator's own pre-push, per the file's own contract | YES | N/A (out-of-repo boundary component; correctly documented, not scoreable read-only from here) | file read |
| Memory hooks (SessionStart / PreCompact / SessionEnd) | `.claude/settings.json` (dotfiles root), `.claude/hooks/{vault-sync,session-start-vault,pre-compact-snapshot}.mjs` | YES [CONFIRMED, all 3 hook commands present in settings.json, all 3 target files exist] | not independently fired this session (would require a real session start/compact/end) [HYPOTHESIS, carried from the 2026-09-05 audit's own "COMPLETE" verdict on the same 3 hooks] | N/A | N/A | YES (settings.json) | YES (vault-sync.test.mjs exists, wired via run-test-suite.sh) | COMPLETE | `.claude/settings.json` read |
| Community promotion mechanism A: `community_promotion_transitions` (migration 295, 5-gate machine) | `src/lib/community/promotion.mjs` | YES (file exists, migration 295 applied) | NOT [CONFIRMED, `grep -rn "community/promotion" src scripts` outside the module's own file returns only its own `.test.mjs`, zero production importers] | **0 rows live** [CONFIRMED, live SQL this session: `select count(*) from community_promotion_transitions` = 0] | N/A (no production caller means nothing to show) | N/A | PARTIAL (module header, no ADR naming it canonical) | BUILT-DORMANT | live SQL + grep, this session |
| Community promotion mechanism B: `post_promotions` (migration 041) | `src/app/api/community/posts/[id]/promote/route.ts` | YES | YES, the route is live and reachable [CONFIRMED, file exists, wired] | **0 rows live** [CONFIRMED, live SQL this session: `select count(*) from post_promotions` = 0], this refines the 2026-09-05 finding's framing: B is code-wired where A is not, but NEITHER mechanism has ever actually fired; 0 promotions have happened through either path | N/A | N/A (route-level auth only, not independently checked) | PARTIAL | BUILT-DORMANT (revised from the prior audit's "the actually-live path") | live SQL this session, **[REFUTED as "live"]**, both mechanisms are equally unused at the data layer |
| Community room region-binding | `POST /api/community/groups` (`src/app/api/community/groups/route.ts`) vs live `community_groups` table | YES | user-creation path is live and reachable | live table has 7 rows: `Global/GLOBAL, EU, US, UK, APAC, LATAM, MEA` [CONFIRMED, live SQL, full row dump this session], **all 7 share one `owner_user_id` and were created within 1 second of each other on 2026-07-07**, i.e. all 7 are seed data, zero rows show any user-created group | YES (Community surface reads these) | N/A | PARTIAL | PARTIAL, refines finding 9 | live SQL, code grep `region: "GLOBAL"` at `route.ts:103` |
| `audit-finding-status.mjs` (rule 14 enforcer) | `scripts/verify/audit-finding-status.mjs`, invoked from `run-test-suite.sh`'s tail (`\|\| true`, report-only) | YES | YES [CONFIRMED, ran live this session, 0.2s: "631 finding-shaped lines across 114 audit file(s); 598 unlabeled"] | 598 unlabeled findings today, up from 593 at train 50 (2026-09-06) [CONFIRMED this session vs METADATA from the train-log] | N/A | explicitly NON-gating by the script's own designed default (`\|\| true`); `--strict` exists, not used | YES (own header explains the report-only default) | COMPLETE (as designed; the backlog itself is real debt, not a wiring gap) | live run this session |

## Prior claims re-checked

1. **Finding 8 (two community promotion mechanisms, only one wired).** Still true as a code-wiring claim: `community_promotion_transitions`'s `promotion.mjs` has zero production importers; `post_promotions`'s route is the only one wired into a live API. **[CONFIRMED]**. New this session: **neither mechanism has ever fired**, live SQL shows 0 rows in both tables, so "the actually-live path" language in the original finding overstates B's liveness; both are BUILT-DORMANT at the data layer, only one is BUILT-DORMANT-BUT-REACHABLE. **[REFUTED, partially]**, corrected in the table above, not dropped.

2. **Finding 9 (rooms not region-bound).** The code claim still holds: `route.ts:103` hardcodes `region: "GLOBAL"` on every group a user creates, unchanged since the prior audit. **[CONFIRMED]**. The prior finding's supporting evidence ("every live group is a vertical, cross-regional room") is **[REFUTED]** by live data: 6 of 7 live `community_groups` rows carry a real region (EU/US/UK/APAC/LATAM/MEA), not GLOBAL, but all 7 are seed rows from one batch insert on 2026-07-07, zero are user-created. So the underlying gap (no user-facing region-scoped creation path) is real and unexercised, but the specific "every live group is GLOBAL" claim is false as of today's data.

3. **Finding 10 (closure gate, seven STALE-NEXT allowlist entries one train-landing from red).** **[REFUTED, already resolved before this session]**, this was itself already fixed and documented inside the 2026-09-05 audit period (lane W71-D, same date); the current tree's `closure-gate.mjs` source shows all three allowlists (`NEVER_RUN_ALLOWLIST`, `STALE_NEXT_ALLOWLIST`, `WRITER_READER_ALLOWLIST`) at `{}`, and PROGRAM-BOARD.md's train-log for trains 48 and 50 records live `--report PASS` runs with the allowlists confirmed empty. Not re-verified by a session-local live re-run (see below), but corroborated by today's green CI.

4. **loop-harness-flywheel file's summary table row "closure gate (W7.5)".** Verdict COMPLETE there is **[CONFIRMED, still true]** by the same evidence above, now 65 trains later.

5. **loop-harness-flywheel file's open question**: whether `WRITER-READER`'s `"gating":1"` under an overall `PASS` (as recorded at train 46) was a bug or a threshold. **COULD NOT VERIFY** this session, the live gate script did not finish inside this audit's wall-clock budget (see below), and the WRITER_READER_ALLOWLIST is empty in the current source, meaning either the one gating write-orphan from 2026-09-05 was resolved (most likely, given `entity_scope` retired per its own comment) or a different one exists uncounted. Recommend a future session capture the CI job's own console log via `gh run view --log` rather than re-running the script locally.

6. **skills-rules-doctrine.md, hooks row ("SessionStart / PreCompact / SessionEnd all wired in settings.json").** **[CONFIRMED, still true]**, re-verified by direct file read this session, unchanged since 2026-09-05.

## What the operator must rule on

1. **`closure-gate.mjs` (and its own test) no longer finish inside a short wall-clock budget on this tree.** Two attempts this session (a 60s hard kill, then the test file's own 30s per-test default) both failed to complete, where the 2026-09-05 audit ran it live in the same session with no reported delay. The tree has grown (trains 46→71, migrations to 325, maintenance steps 38→62) since; the git `merge-base --is-ancestor` loop in `trainOf()` is called once per commit being classified and is the likely cost driver. Recommendation: either raise the node test timeout for this one test and confirm it still finishes in CI's own budget, or have the gate cache `trainOf()` results, or accept `gh run view --log` on the CI job as the standing read-only verification method instead of a local rerun. This is a maintainability finding, not a red gate, today's CI (`35374142991`) passed.
2. **Dispatch ledger is 11 days stale (last row 2026-09-07) while 24 new maintenance steps landed since.** Recommend either resuming ledger appends per dispatch, or explicitly documenting that harness-family artifacts + MAINTENANCE-RUNBOOK.md sections are now the operator's chosen evidence path and the ledger is deliberately retired, right now it reads as an abandoned mechanism, which is exactly the silent-drift shape rule 15 exists to catch.
3. **`community_promotion_transitions` vs `post_promotions`: pick one, per the standing "needs an ADR" note from finding 8.** New leverage for the decision: since neither table has ever been written to, retiring the unused one costs nothing in data migration. Recommend: delete `community_promotion_transitions` (migration 295 + `promotion.mjs` + its test) since `post_promotions` is the one with a live route, unless the 5-gate design is wanted and the plan is to wire it in and retire `post_promotions` instead, either way, a decision closes the WRITER-READER-class ambiguity a future migration-266+ scan will otherwise keep re-flagging.
4. **Region-bound community rooms: the creation path was never built, and 0 user rooms have tested it.** Recommend closing W6.2 for real (a `region` selector at group-creation time) or documenting the seed-only 7-room model as the deliberately-final shape, since as written the feature reads unfinished but has accrued no cost from being unfinished (zero users have hit the gap yet).

## Counts

Verdict totals across the 19 scored rows: **COMPLETE 12, PARTIAL 5, BUILT-DORMANT 3** (note: `post_promotions` is counted once, under its own row, as BUILT-DORMANT, revised from the prior audit's implicit COMPLETE/wired framing), **NOT BUILT 0, COULD NOT VERIFY 0** (open sub-questions are flagged inline as COULD NOT VERIFY without taking a whole row), **N/A 1** (PreToolUse, correctly out-of-repo).

Rows added beyond the plan/prior-audit's own start list: **8**, F44, F45 (named fitness functions, not enumerated by name in either source document), the dispatch-ledger staleness row, the maintenance.yml step-registry aggregate row, the discipline test-suite row, the pre-push trampoline row, the merge-train/assemble row, and the split of "community promotion" into two explicit rows (A and B) instead of one.
