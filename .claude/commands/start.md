Run `/ledger`. That is the boot sequence, it is maintained in one place (`.claude/skills/ledger/SKILL.md`), and this command exists only so an old habit still lands somewhere correct.

Do not load `fsi-app/STATUS.md`. It was retired from the loading priority on 2026-08-14 and is historical; `docs/PROGRAM-BOARD.md` is the live build state.

After loading, tell me where the project stands, what I was working on last, and the next step. Then ask what I want to work on.
