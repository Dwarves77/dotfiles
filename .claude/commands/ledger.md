Load the vault before doing anything else. Follow `.claude/skills/ledger/SKILL.md` in full: Section A0 (locate the repo, fetch, pin to `origin/master`), Section A (read CLAUDE.md, docs/INDEX.md, docs/PROGRAM-BOARD.md, the last addendum of docs/ops/session-log.md, git log), then Sections B and C.

Report back in <=5 lines: the vault path and the `origin/master` commit you read, the working branch and its divergence from master, what the last addendum says completed, and the open threads. If you could not reach the vault, say so plainly and stop, do not answer from the database or from memory.
