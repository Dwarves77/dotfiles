// RD-57-statutory-purity: split from invariants.mjs (plan 6.8, Rule A, lane N5). One entry, one file; see
// invariants.d/README.md. The comment block below (if any) is exactly what preceded this entry in
// the array before the split.

export const invariant = {
    id: 'RD-57-statutory-purity',
    skill: 'remediation-discipline',
    section: 'Section 4 — category 32: Propagation engine gates (a computed value\'s pollution barrier and a statutory formula\'s purity are class fix targets, not per-caller trust)',  // glyph:verbatim (unedited content carried over from invariants.mjs; see invariants.d/README.md)
    text: 'Spec 08 §4 Layer 3: a legally-prescribed statutory figure must never quietly absorb a model\'s guess or an unvalidated estimate as one of its own inputs. Migration 286\'s assert_statutory_purity() trigger inspects a statutory_computations row\'s OWN declared inputs jsonb array directly (adapted from spec\'s own transitive-DAG-walk body — statutory_computations is never a to_value_id in derivation_edges, so there is no DAG edge to walk from this table; the migration\'s own header names this deliberate deviation) and refuses any element naming a live estimated_values row, or a derived_values row whose derivation is modelled/estimated/interpolated — proven live by that migration\'s own self-check (a real estimated_values row, a real rejected INSERT). F32 (statutory-purity) is the filesystem-only backstop: it fails if the repository copy of migration 286 no longer defines assert_statutory_purity() or no longer attaches it as a BEFORE INSERT OR UPDATE trigger on statutory_computations, and ships a tested, pure JS mirror (assertStatutoryPurity) transcribed byte-for-byte from the SQL trigger\'s own two-EXISTS-query logic for a future JS-side pre-flight caller.',  // glyph:verbatim (unedited content carried over from invariants.mjs; see invariants.d/README.md)
    anchor: 'A statutory computation\'s declared inputs MUST NOT cite a live `estimated_values` row or a `derived_values` row whose derivation is modelled/estimated/interpolated — a legally-prescribed figure never quietly absorbs a model\'s guess',  // glyph:verbatim (unedited content carried over from invariants.mjs; see invariants.d/README.md)
    enforcedBy: ['fitness:F32', 'selftest:fsi-app/.discipline/fitness/functions/F32-statutory-purity.test.mjs'],
    residual: 'F32 CANNOT run the live DB proof itself (filesystem-only, no database, matching every other holistic function in this registry) — the actual write-time rejection is proven by migration 286\'s own embedded self-check DO block (run against a local scratch Postgres for this lane, not the live project — named as an [UNCONFIRMED]-against-production item in this lane\'s own REPORT). F32\'s structural-presence regex is a REGRESSION guard (the trigger disappearing or being downgraded to AFTER/moved to another table), not a re-proof of correctness on every commit. The JS mirror\'s NAMED residual (migration 286\'s own header, restated here): the check is SHALLOW (a row\'s own inputs column) rather than a transitive graph walk, which is safe today because Layer 2\'s type barrier (StatutoryInput, types.ts) is the only sanctioned path into this table and a bypass caller\'s inputs column is exactly what both the trigger and the mirror inspect — a future multi-hop statutory composition (a statutory result cited as another statutory computation\'s own input) would need the schema widening the migration\'s header already names, not a fix to this invariant\'s enforcement alone.',  // glyph:verbatim (unedited content carried over from invariants.mjs; see invariants.d/README.md)
  };
