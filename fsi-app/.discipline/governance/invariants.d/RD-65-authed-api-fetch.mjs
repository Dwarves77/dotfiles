// RD-65-authed-api-fetch: split from invariants.mjs (plan 6.8, Rule A, lane N5). One entry, one file; see
// invariants.d/README.md. The comment block below (if any) is exactly what preceded this entry in
// the array before the split.

export const invariant = {
    id: 'RD-65-authed-api-fetch',
    skill: 'remediation-discipline',
    section: 'Section 4 — category 40: a browser call to a requireAuth-guarded route attaches the session token in ONE place, never at the call site',  // glyph:verbatim (unedited content carried over from invariants.mjs; see invariants.d/README.md)
    text: 'A browser module calling a requireAuth-guarded `/api/` route is forbidden from attaching the session token itself: it goes through the one shared authenticated fetcher (`authedFetch`/`authHeaders`, src/lib/api/authed-fetch.ts), and no module under src/ outside that file and the two server modules that build a Supabase client FROM an already-verified token (src/lib/api/auth.ts, src/lib/api/community-auth.ts, plus the OAuth callback route) may write an `Authorization: Bearer` header. requireAuth reads that header and nothing else, so a caller that sends a cookie instead, or a bearer carrying no token, gets a 401 that fails soft and is invisible to every test that mounts components against canned fixtures. [CONFIRMED, production, click-through audit 2026-09-08]: src/lib/tags/client.ts and useWorkspaceTagsFacet.ts sent `credentials: "include"` and a Content-Type, so all seven workspace-tags requests 401d for every signed-in user from the day the feature landed and the + Tag popover, the detail tag row and the list rail facet were dead on every page — 23 GET /api/workspace/tags 401 in three hours on a signed-in session while /api/auth/identity, /api/workspace/bootstrap and /api/admin/attention returned 200 on the same page loads. A census then found 24 further files hand-rolling the header four ways, 21 of them interpolating the token unconditionally (`Bearer ${session?.access_token || ""}`, or literally `Bearer undefined`), which clears requireAuths startsWith check and then 401s on any read that fires before the session resolves.',  // glyph:verbatim (unedited content carried over from invariants.mjs; see invariants.d/README.md)
    anchor: '### Section 4 — category 40: a browser call to a requireAuth-guarded route attaches the session token in ONE place, never at the call site',  // glyph:verbatim (unedited content carried over from invariants.mjs; see invariants.d/README.md)
    enforcedBy: ['fitness:F40', 'selftest:fsi-app/.discipline/fitness/functions/F40-authed-api-fetch.test.mjs'],
    residual: 'F40 is a LEXICAL scanner over `fsi-app/src/**/*.{ts,tsx}` (test files excluded), and its two rules carry different strengths. Rule (a) — no hand-rolled `Bearer ${` outside the named allowlist — is fully decidable and is the one that closes the class. Rule (b) — a `fetch()` on a literal `/api/` path whose route calls requireAuth must be `authedFetch` or sit in a file importing the helper — CANNOT prove by dataflow that the `headers` value a bare `fetch()` receives actually came from `authHeaders()`; it verifies only that the file has the helper in scope. That gap is closed by rule (a) rather than by rule (b), because the only way to produce a bearer header in this codebase without tripping (a) is to call the shared builder. A dynamically-built request path (`fetch(url)` where `url` is a variable) is invisible to rule (b) entirely; rule (a) still binds it, and the route\'s own requireAuth still rejects an unauthenticated call, so such a failure is loud at the call site rather than silent. F40 also does not see a non-browser caller (a script or worker using a service key), which is a different auth surface with its own gates (F19, worker-auth).',  // glyph:verbatim (unedited content carried over from invariants.mjs; see invariants.d/README.md)
  };
