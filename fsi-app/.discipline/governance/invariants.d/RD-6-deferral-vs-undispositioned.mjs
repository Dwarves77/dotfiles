// RD-6-deferral-vs-undispositioned: split from invariants.mjs (plan 6.8, Rule A, lane N5). One entry, one file; see
// invariants.d/README.md. The comment block below (if any) is exactly what preceded this entry in
// the array before the split.

export const invariant = {
    id: 'RD-6-deferral-vs-undispositioned',
    skill: 'remediation-discipline',
    section: 'Section 2.2: Deferred vs Undispositioned (a deferral is dispositioning-as-blocked, never silencing)',
    text: 'A past-bound quarantined item (>14d) is either disposed (recovered/archived/registered) OR carries a VALID time-bounded deferral (reason names the blocker + the awaited disposition path, a named future resolution event, an owner); expired deferrals re-open as undispositioned. Deferral is dispositioning-as-blocked, never silencing — the undispositioned count is the hard tripwire.',  // glyph:verbatim (unedited content carried over from invariants.mjs; see invariants.d/README.md)
    anchor: 'Deferred vs Undispositioned (a deferral is dispositioning-as-blocked, never silencing)',
    enforcedBy: ['audit:fsi-app/scripts/verify/quarantine-disposition-audit.mjs', 'audit:fsi-app/scripts/verify/deferral-hygiene-audit.mjs'],
    residual: 'The audit (CI-with-secrets / ops lane, DB creds) is the live-data guard: it splits past-bound into undispositioned (HARD tripwire, fails the lane) vs deferred (standing, reported only), applying isValidDeferral on the read side AND re-checking deferred_until is in the future. The write-time guard scripts/lib/deferral.mjs prevents vague deferrals from ever being written (reason must name blocker + disposition path, named future resolution_event, real owner). The meta-gate proves wiring (audit file git-tracked + skill-cited) in the secret-less pre-push. Self-resurrection on expiry is the anti-silence property — a deferral cannot quietly outlive its own clock. The FLAG side is guarded too: deferral-hygiene-audit.mjs names the integrity_flags disposition_deferred rows that rotted — THREE classes, ALL of which FAIL the lane (exit 1): expired-open (deferred_until passed while status=open), deleted-subject (subject item hard-deleted), and RENEWAL-REPACKAGE (operator ruling 2026-07-13, item 3: an open deferral whose reason matches a sibling deferral\'s reason on the same subject — a clock re-set that recycled the prior blocker instead of naming a new one, the RD-6 silencing). "Never writes" means it does NOT blind-resolve/re-open (that is a later disposition dispatch) — it is NOT report-only in the sense of non-failing: expired-open is a failing condition. The write-time guard now also exports isValidRenewal/assertValidRenewal (deferral.mjs): a renewal MUST carry a NEW blocker finding — repackaging the superseded reason with a fresh date is rejected at write time. Standing backlog: 47 expired-open + 5 deleted-subject await a disposition dispatch (flag-rate is not defect-rate); 0 renewal-repackage live. NOT mechanized: whether the named blocker is genuinely the real blocker (vs a plausible-sounding one) is remediation judgment (RD-1), not a checkable property.',  // glyph:verbatim (unedited content carried over from invariants.mjs; see invariants.d/README.md)
  };
