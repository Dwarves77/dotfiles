// RD-12-size-cap-doctrine: split from invariants.mjs (plan 6.8, Rule A, lane N5). One entry, one file; see
// invariants.d/README.md. The comment block below (if any) is exactly what preceded this entry in
// the array before the split.

export const invariant = {
    id: 'RD-12-size-cap-doctrine',
    skill: 'remediation-discipline',
    section: 'Section 4 — category 11: The size-cap doctrine (no silent slice on the grounding path)',  // glyph:verbatim (unedited content carried over from invariants.mjs; see invariants.d/README.md)
    text: 'Every size cap on the capture→synthesis→grounding→judge path is either sized so it never binds in normal operation, or fails LOUD (surfaced wall — recordTruncation → coverage_gap flag + item held with a named reason) when it binds. Silent slicing is forbidden (the GROUND_SECTION_MAX_CHARS=12000 category-2 defect that hid the back of every long section from the grounder). Every path cap is enumerated + classified; a new unregistered/silent cap is RED. EVERY WRITER, not every module: a cap binds on the COLUMN, so it must hold at every process that writes that column — agent_run_searches.result_content_excerpt has two independent writers (the Next.js pipeline and the Deno capture-worker, which cannot import the pipeline\'s config) and the ceiling was live on one of them, which is the same silence one layer out.',  // glyph:verbatim (unedited content carried over from invariants.mjs; see invariants.d/README.md)
    anchor: 'The size-cap doctrine (no silent slice on the grounding path)',
    enforcedBy: ['fitness:F17', 'fitness:F26', 'selftest:fsi-app/src/lib/agent/section-grounding.test.mjs', 'selftest:fsi-app/.discipline/fitness/functions/F26-storage-ceiling-parity.test.mjs'],
    residual: 'F17 (grep-class, red-then-green: a new unregistered *_MAX_CHARS/*_BUDGET_CHARS/*_CEILING_CHARS on the path is RED; a registry entry marked silent-grounding is RED) gates the STRUCTURAL guarantee — no silent cap slips onto the path unclassified. section-grounding.test.mjs proves the fix red-then-green: a binding fact beyond the old 12KB boundary is invisible to the old slice(0,12000) and reaches the grounder complete post-fix; a pathological over-ceiling section is SURFACED (truncated=true + fullLength), never silent. NAMED RESIDUALS: (1) the classify-path excerpt caps (haiku-classify ~6000, first-fetch ~6000, recommend-tier 4000) are SILENT binders on the CLASSIFICATION path (tier/type/portal), not the fact-grounding path — LOW risk (source nature is evident early), registered in the cap-inventory as REVISIT, not converted now; (2) F17 registers cap CONSTANTS on generation-config + section-grounding — an inline `.slice(0, N)` re-introduced elsewhere on the path is caught by review + the section-grounding selftest, not by F17 grep (the constant-declaration form is the robust signal). Full inventory: docs/design/cap-inventory-2026-07-06.md. SECOND-WRITER HALF (2026-08-17): F17 registers cap constants inside the Next.js tree, so it structurally could not see the Deno capture-worker, and the ADR-016 storage ceiling was enforced on one of the column\'s two writers — three captures landed over it (17,787,345 / 12,579,090 / 10,351,091 chars, all capture-worker:first-fetch, all dated AFTER the 2026-07-21 ruling) with no signal, because the unguarded path had nothing to fire. F26 closes it as PARITY rather than presence: both writers must resolve STORAGE_MAX_CHARS from the same env var with the same fallback literal (a hand-copied constant IS the divergence — cf. the gate_a_* version literal in the 2026-08-11 db-layer census), and a worker bind must be LOUD (the [truncation-guard] warn AND the integrity_flags write, mirroring recordTruncation()). Negative-tested by F26-storage-ceiling-parity.test.mjs (19 assertions, fixture-driven: divergence in BOTH directions, ceiling removed, ceiling hard-coded, wrong env var name, and the silent-ceiling cases that satisfy parity while quietly slicing the pool). NAMED RESIDUAL: F26 is a STATIC source-text check by necessity — the two runtimes cannot share an import, so agreement can only be verified in the text before deploy; a divergence introduced by setting the STORAGE_MAX_CHARS env var to different values in the Vercel and Supabase environments is invisible to it, and is the out-of-repo-boundary class (docs: the env/hooks/DB config boundary manifest), not a code gate.',  // glyph:verbatim (unedited content carried over from invariants.mjs; see invariants.d/README.md)
  };
