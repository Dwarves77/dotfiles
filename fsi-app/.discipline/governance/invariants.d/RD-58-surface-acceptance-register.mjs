// RD-58-surface-acceptance-register: split from invariants.mjs (plan 6.8, Rule A, lane N5). One entry, one file; see
// invariants.d/README.md. The comment block below (if any) is exactly what preceded this entry in
// the array before the split.

export const invariant = {
    id: 'RD-58-surface-acceptance-register',
    skill: 'remediation-discipline',
    section: 'Section 4 — category 33: Surface-acceptance register (a customer surface\'s own existence is a class fix target, not just its rendering fidelity)',  // glyph:verbatim (unedited content carried over from invariants.mjs; see invariants.d/README.md)
    text: 'Every customer-facing surface named in docs/specs/01..11-*.md must be either PRESENT (a register entry naming a real route, a real data_path, and a real rendering_spec, each verified against the actual import graph — reachability, not a name that merely looks plausible) or EXEMPTED (a register entry naming reason, ruled_by, and date). A surface silently missing from both the register and the spec text is the gap this closes: SF-10 proved that a BUILT surface renders its data extremes cleanly; this proves a surface the specs OBLIGATE the product to have was not quietly dropped between planning and delivery, and turns "is this built yet" from a manual per-release audit into a self-reporting register a reviewer can diff. This is the same class as RD-9/RD-9b (a promised half with nothing proving the other half exists) applied one level up, to the surface itself rather than to a table\'s writer/reader pair.',  // glyph:verbatim (unedited content carried over from invariants.mjs; see invariants.d/README.md)
    anchor: '## Section 4: Remediation Strategy by Category',
    enforcedBy: ['fitness:F33', 'selftest:fsi-app/.discipline/fitness/functions/F33-surface-acceptance.test.mjs'],
    residual: 'Anchor is a stable skill-section marker (SF-8/SF-9\'s precedent: no single existing line in remediation-discipline/SKILL.md names surface acceptance, since category 33 did not exist before this lane) — the invariant itself is owned by F33 and this registry entry, not by prose already in the skill file. F33 (filesystem-pure: reads surface-acceptance-register.json, hardcodes the spec-derived surface list with basis citations in its own source rather than re-parsing the spec prose, and reuses buildImportGraph — already the F25-module-liveness reachability engine, not a second implementation — to verify a register entry\'s route/data_path actually resolve and connect in the real import graph) catches an entry that NAMES a path nothing imports, and a spec-named surface present in neither the register nor an exemption. NAMED RESIDUAL: F33 cannot verify a rendering_spec path is the CORRECT smoke/rendering proof for that surface, only that the path exists and is reachable — the semantic match between a surface and its cited proof stays authoring discipline enforced at review, the same posture F27 states for producer-seam import supersets. F33 also cannot detect a spec being edited to remove an obligation instead of building or honestly exempting it — the register is honest only as of the humans who wrote docs/specs/01..11-*.md; a silently narrowed spec is a doctrine-authoring failure outside a fitness function\'s reach.',  // glyph:verbatim (unedited content carried over from invariants.mjs; see invariants.d/README.md)
  };
