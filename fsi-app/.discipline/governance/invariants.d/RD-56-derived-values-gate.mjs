// RD-56-derived-values-gate: split from invariants.mjs (plan 6.8, Rule A, lane N5). One entry, one file; see
// invariants.d/README.md. The comment block below (if any) is exactly what preceded this entry in
// the array before the split.

export const invariant = {
    id: 'RD-56-derived-values-gate',
    skill: 'remediation-discipline',
    section: 'Section 4 — category 32: Propagation engine gates (a computed value\'s pollution barrier and a statutory formula\'s purity are class fix targets, not per-caller trust)',  // glyph:verbatim (unedited content carried over from invariants.mjs; see invariants.d/README.md)
    text: 'Spec 08 §3.3\'s pollution barrier, made structural: nothing outside src/lib/propagation/ reads the raw derived_values table directly. Migration 285 denies raw-table SELECT via RLS and grants only derived_values_admissible (the view, already excluding stale/falsified/obsolete rows); F31 (derived-values-gate) is the SECOND enforcement point the spec names — a service-role client, which bypasses RLS entirely, could otherwise read derived_values directly and see ungated rows with no admissibleFor() check applied. F31 fails CI on a literal .from("derived_values") call site (any quote style) anywhere outside src/lib/propagation/; derived_values_admissible never matches (a different string), so reading the view — already-admitted data — is unaffected anywhere in the repo.',  // glyph:verbatim (unedited content carried over from invariants.mjs; see invariants.d/README.md)
    anchor: 'Nothing outside `src/lib/propagation/` MUST read the raw `derived_values` table directly — every consumer reads through `derived_values_admissible` (RLS-granted) or calls `admissibleFor()` (the pollution barrier)',  // glyph:verbatim (unedited content carried over from invariants.mjs; see invariants.d/README.md)
    enforcedBy: ['fitness:F31', 'selftest:fsi-app/.discipline/fitness/functions/F31-derived-values-gate.test.mjs'],
    residual: 'F31 is filesystem-pure (globs fsi-app/src/**/*.{ts,tsx,mjs,js} and fsi-app/scripts/**/*.mjs, excluding test/story files) and per-file, matching F21\'s single-grounding-entry shape: a per-line regex over each file\'s content, sanctioned by directory prefix rather than a fixed file list (src/lib/propagation/ is expected to GROW as later lanes add method modules, unlike F21\'s closed SANCTIONED set). It catches the ONE bypass shape named in the task brief and this invariant (.from("derived_values") in application/script code); a raw SQL string issuing the same read from outside a migration file is a materially different, currently-unheld residual — named, not silently narrowed, in F31\'s own header. RLS (migration 285) remains the FIRST enforcement point (a role with no grant on the base table cannot read it regardless of what F31 catches); F31 backstops the service-role/bypass-RLS case specifically.',  // glyph:verbatim (unedited content carried over from invariants.mjs; see invariants.d/README.md)
  };
