// EP-1-integrity: split from invariants.mjs (plan 6.8, Rule A, lane N5). One entry, one file; see
// invariants.d/README.md. The comment block below (if any) is exactly what preceded this entry in
// the array before the split.
//
  // ───────────────────────── environmental-policy-and-innovation ─────────────────────────
  

export const invariant = {
    id: 'EP-1-integrity',
    skill: 'environmental-policy-and-innovation',
    section: 'The Integrity Rule',
    text: 'No invented facts/operators/costs/competitors/suppliers; when facts run out, stop; omit-with-note, never fill via invention.',
    anchor: 'The Integrity Rule (mandatory, never violated)',
    enforcedBy: ['migration:035', 'migration:044', 'migration:121'],
    residual: 'Mechanical layer is a PROXY: migration 035/044 auto-flag hedge phrases ("unable to verify"…) and the provenance gate (121) keeps unverified items off customer surfaces. Semantic invention that uses no hedge phrase is not mechanically detectable — that residue is judgment-load at authoring.',  // glyph:verbatim (unedited content carried over from invariants.mjs; see invariants.d/README.md)
  };
