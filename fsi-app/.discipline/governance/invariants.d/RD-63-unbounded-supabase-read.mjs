// RD-63-unbounded-supabase-read: split from invariants.mjs (plan 6.8, Rule A, lane N5). One entry, one file; see
// invariants.d/README.md. The comment block below (if any) is exactly what preceded this entry in
// the array before the split.

export const invariant = {
    id: 'RD-63-unbounded-supabase-read',
    skill: 'remediation-discipline',
    section: 'Section 4 — category 38: A capped read that claims to fetch "the whole set" pages, it does not lean on a bigger .limit() (PostgREST\'s db-max-rows silently truncates any single request past 1,000 rows, no matter what N a caller asks for)',  // glyph:verbatim (unedited content carried over from invariants.mjs; see invariants.d/README.md)
    text: 'A `.limit(N)` call with N > 1,000, or a full-set read with no `.range()`/`.limit()` at all where the code path assumes it received every row, is forbidden outside the shared fetchAllRows/exactCount helpers (src/lib/db/paginate.mjs) — Supabase PostgREST enforces a server-side db-max-rows=1000 cap that a larger requested limit does not override, so any such call silently returns at most the first 1,000 rows in whatever order the query happens to produce, and any "total" derived from that array\'s .length is not a count, it is a truncated sample wearing a count\'s clothes. [CONFIRMED, live, 2026-09-05, lane CAP-1000, "two defects one cause"] carosledge.com\'s [slug] prerender coverage: PERF-13\'s getPublicSurfaceSlugs called the public listings RPC with `.limit(BUILD_TIME_SLUG_ENUM_LIMIT = 20000)` against a live 1,312+-row regulations corpus — slug index 900 was a prerender HIT, 1000/1050/1200/1311 were all MISS, exactly the first 1,000 enumerated. The SAME bug class independently produced: the obligations register masthead reading "60 of 1000 obligations" against a live 1,141-row table (read-register.mjs\'s OVERFETCH_CAP=2000 JS-side overfetch-then-filter, whose displayed "total" was the capped fetch\'s array length, not a real count); community/directory/page.tsx\'s profiles aggregate silently dropping rows past 5,000 (found live by this invariant\'s own fitness function\'s whole-tree test, not named in the original dispatch); and run-change-detection.mjs\'s readPendingDrainRows deriving its backlog "overflow beyond --drain-limit" figure from a capped array\'s length, silently reading 0 overflow once the true staged_updates backlog exceeded 1,000 rows no matter how deep it actually ran.',  // glyph:verbatim (unedited content carried over from invariants.mjs; see invariants.d/README.md)
    anchor: '### Section 4 — category 38: A capped read that claims to fetch "the whole set" pages, it does not lean on a bigger .limit()',  // glyph:verbatim (unedited content carried over from invariants.mjs; see invariants.d/README.md)
    enforcedBy: ['fitness:F38', 'selftest:fsi-app/.discipline/fitness/functions/F38-unbounded-supabase-read.test.mjs'],
    residual: 'F38 is a LEXICAL scanner over `fsi-app/src/**/*.{ts,tsx,mjs}` and `fsi-app/scripts/**/*.mjs` (test files and `/_archive/` excluded): it flags a `.limit(<literal>)` call with a literal > 1,000, or `.limit(<SAME-FILE ALL_CAPS CONST>)` where the const\'s own declared value exceeds 1,000, and REDS a registered ALLOWLIST entry once its named `expiry` train has passed (per F25\'s `latestTrainWave`, reused rather than re-implemented). By deliberate scope decision (stated in F38\'s own file header) it does NOT attempt generic detection of an unranged `.rpc()`/`.select()` call with no `.limit()`/`.range()` at all — a codebase-wide scan for that shape would false-positive on the many legitimate single-row/small-result RPCs (a lookup by primary key, an aggregate RPC that returns one row, etc.), so a genuinely new full-table read with no limit clause of any kind is NOT mechanically caught by this invariant; it would need to be found the way this lane found the community/directory instance — by a human or a fitness function\'s own live end-to-end test discovering the symptom, not by the static scanner naming the site directly. The ALLOWLIST is not empty by construction: `scripts/verify/mint-gate-calibration.mjs`\'s `.limit(8000)` is a deliberate calibration-sample width, not a claimed full-set read, and is named with a reason and an expiry (train 46) rather than silently exempted.',  // glyph:verbatim (unedited content carried over from invariants.mjs; see invariants.d/README.md)
  };
