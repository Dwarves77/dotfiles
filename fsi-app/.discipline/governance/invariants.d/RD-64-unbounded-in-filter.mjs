// RD-64-unbounded-in-filter: split from invariants.mjs (plan 6.8, Rule A, lane N5). One entry, one file; see
// invariants.d/README.md. The comment block below (if any) is exactly what preceded this entry in
// the array before the split.

export const invariant = {
    id: 'RD-64-unbounded-in-filter',
    skill: 'remediation-discipline',
    section: 'Section 4 — category 39: a PostgREST `.in(col, list)` filter URL-encodes its whole list into the GET request; a runtime id list whose size follows the data, not a literal or a provably capped slice, eventually exceeds the gateway\'s URL-length limit and the request itself 400s',  // glyph:verbatim (unedited content carried over from invariants.mjs; see invariants.d/README.md)
    text: 'A `.in(column, list)` call whose `list` is a runtime value (a variable, a property access, a method-chain expression) is forbidden outside the shared readAllByIds/guardedUpdateByIds/fetchAllByIdChunks helpers (scripts/lib/db.mjs, src/lib/db/paginate.mjs) unless the call site carries a same-line or preceding-line `// fitness-allow: F39 (reason)` marker naming why the list is bounded — PostgREST URL-encodes an `.in()` filter\'s entire list into the request line, and past roughly 2,000 UUIDs (about 80KB) or any sufficiently long id list the gateway answers 400 Bad Request or an HTML error page instead of the JSON the caller expects, independent of and in addition to the row-count truncation RD-63/F38 already covers (RD-63 is about a RESPONSE silently truncated past 1,000 rows; this is about a REQUEST that never completes at all, because the URL itself is too long). [CONFIRMED twice, lane INCLAUSE-CLASS, 2026-09-06]: review-apply wrappers (run 34045479342, 911 ids) and census-off-vertical.mjs (Maintenance run 34046850770, 1,655 ids, raw `<!DOCTYPE html>` error page returned in place of JSON) — in the second case the error surfaced only on the POST-write read-back, after the chunked write itself had already succeeded, live-confirming 1,655 rows correctly archived despite the read-back\'s own failure.',  // glyph:verbatim (unedited content carried over from invariants.mjs; see invariants.d/README.md)
    anchor: '### Section 4 — category 39: a PostgREST `.in(col, list)` filter URL-encodes its whole list into the GET request',  // glyph:verbatim (unedited content carried over from invariants.mjs; see invariants.d/README.md)
    enforcedBy: ['fitness:F39', 'selftest:fsi-app/.discipline/fitness/functions/F39-unbounded-in-filter.test.mjs'],
    residual: 'F39 is a LEXICAL scanner over `fsi-app/src/**` and `fsi-app/scripts/**` (test files and `/_archive/` excluded): it flags a `.in(col, X)` call whose `X` is not an array literal, a string/template literal, or a SCREAMING_SNAKE_CASE constant reference — i.e. a shape that reads as bounded by construction versus one that reads as a runtime-sized value. Unlike F38, F39 carries NO allowlist-with-expiry escape hatch by deliberate design (this lane\'s explicit dispatch constraint): a site that cannot be proven bounded gets fixed (routed through the shared chunking helpers) or, when investigation shows it genuinely is bounded by a mechanism the lexical scanner cannot see (a `.limit()` clause earlier in the same function, a request-body length validated upstream, a singleton/per-item/per-user scope), gets an inline `// fitness-allow: F39 (reason)` marker stating that bound at the site itself, permanently, not in a separate file with a countdown. F39 cannot verify that a marker\'s stated reason is actually true — it only verifies a reason was written down; a false or stale marker is a governance-discipline failure the same way a false `[CONFIRMED]` label would be, not a gap F39 itself closes. Like F38, F39 does not attempt generic dataflow analysis to prove a variable\'s bound automatically — it recognizes bounded SHAPES at the call site, not bounded VALUES traced through arbitrary prior code.',  // glyph:verbatim (unedited content carried over from invariants.mjs; see invariants.d/README.md)
  };
