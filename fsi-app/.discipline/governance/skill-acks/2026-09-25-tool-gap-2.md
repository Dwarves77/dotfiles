## Skill

remediation-discipline

## Change

`fsi-app/.claude/skills/remediation-discipline/SKILL.md` itself is unchanged. This range ADDS three new
`GOVERNING SKILL:` citations of it:

- `fsi-app/scripts/verify/dead-column-audit.mjs` line 2 (sweep-before-claim; the column-grain sibling of
  F14's producer-consumer orphan check).
- `fsi-app/scripts/verify/lib/dead-column-scan.mjs` line 2 (same, its pure core).
- `fsi-app/scripts/verify/duplicate-table-audit.mjs` line 2 (the "F45-shaped structural comparison"
  audit finding DUP-1 itself calls for, in place of DUP-1's own manual comment-reading pass).

Both are straightforward instances of remediation-discipline's existing "sweep-before-claim" and
"structural comparison over information_schema" framing already used by the pre-existing sibling audits
(`schema-drift-audit.mjs`, `column-existence-parity.mjs`, F14, F45) this lane's brief named as the
model. Not a new interpretation of the skill.

## Citing files reviewed

Per skill-contract-map.mjs's convention (citing files = files matching a `GOVERNING SKILL(S):` marker
for this skill under `CITATION_SCAN_ROOTS`, `fsi-app/src` and `fsi-app/scripts`):
`git grep -rln "GOVERNING SKILL" -- fsi-app/src fsi-app/scripts | xargs grep -l "remediation-discipline"`
from the worktree root names 30 citers, 27 pre-existing (unchanged in this range) plus this range's
three new ones: `fsi-app/scripts/verify/dead-column-audit.mjs`,
`fsi-app/scripts/verify/lib/dead-column-scan.mjs`, `fsi-app/scripts/verify/duplicate-table-audit.mjs`.

The pre-existing citers are unchanged in this range (this lane's only edit to an existing file,
`fsi-app/scripts/verify/column-existence-parity.mjs`, touched its `walk()` implementation only, not its
own `GOVERNING SKILL` header, which was already present before this range and is not moved or removed
here). No conflict, no reinterpretation of the skill's framing.
