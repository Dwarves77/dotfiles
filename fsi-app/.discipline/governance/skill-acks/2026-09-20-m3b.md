## Skill

remediation-discipline

## Change

File changed: `fsi-app/.claude/skills/remediation-discipline/SKILL.md`.

Corrected the stale claim in category 14 (Worktree isolation), where the file stated "PreToolUse is
session-scoped and does NOT fire inside subagents/workflows (verified 2026-06-07)" as a present-tense
fact. [REFUTED 2026-09-19, lane M3]: it now does fire inside sub-agents (the main checkout's own
`.gate-audit.log` recorded 11 denials of a sub-agent's own Edit/Write calls), and lane G1 (issue 754)
made the skill gate judge the acting agent's own transcript. The sentence now states the 2026-06-07
finding as history and the corrected present, matching the same correction made in
`fsi-app/.discipline/governance/worktree-isolation.mjs`'s own header comment.

## Citing files reviewed

Per skill-contract-map.mjs's own convention (citing files = files matching a `GOVERNING SKILL(S):`
marker for this skill under CITATION_SCAN_ROOTS, `fsi-app/src` and `fsi-app/scripts`): none of them
changed in this lane's range, and the correction below does not change any operative clause a citing
file could depend on (it corrects a factual claim in an incident narrative, not a rule or a contract).
`grep -rln "GOVERNING SKILL" src scripts | xargs grep -l "remediation-discipline"` from `fsi-app/` names:

`src/lib/sources/canonical-fetch-caller-thread.test.mjs`, `scripts/audit-skill-conformance.mjs`,
`scripts/lib/db.mjs`, `scripts/lib/deferral.mjs`, `scripts/verify/candidate-dwell-audit.mjs`,
`scripts/verify/canonical-key-uniqueness.mjs`, `scripts/verify/check-vocabulary-drift.mjs`,
`scripts/verify/claims-tier-audit.mjs`, `scripts/verify/column-existence-parity.mjs`,
`scripts/verify/deferral-hygiene-audit.mjs`, `scripts/verify/flag-age-audit.mjs`,
`scripts/verify/layer-c-insert-gate-proof.mjs`, `scripts/verify/no-generic-source-audit.mjs`,
`scripts/verify/one-tier-per-host-audit.mjs`, `scripts/verify/orphan-source-audit.mjs`,
`scripts/verify/pause-flag-guard-proof.mjs`, `scripts/verify/prov-guard-adversarial-audit.mjs`,
`scripts/verify/quarantine-disposition-audit.mjs`, `scripts/verify/remediate-orphan-sources.mjs`,
`scripts/verify/rls-credential-parity.mjs`, `scripts/verify/schema-drift-audit.mjs`,
`scripts/verify/source-link-audit.mjs`, `scripts/verify/spec09-org-rls-adversarial-audit.mjs`,
`scripts/verify/staged-transit-audit.mjs`, `scripts/verify/substrate-agreement-audit.mjs`,
`scripts/verify/surface-visibility-audit.mjs`, `scripts/verify/unregistered-span-host-audit.mjs`.

Also reviewed directly (outside the citation-scan mechanism, but load-bearing for this correction):

- `fsi-app/.discipline/governance/worktree-isolation.mjs` (the sibling doctrine home carrying the same
  claim, corrected in the same lane)
- `docs/doctrine/worktree-isolation.md` (also carries the same claim, at two lines; OUT OF SCOPE for
  this lane's brief, which named only `fsi-app` and `docs/runbooks` for the final grep and only
  `fsi-app/.discipline/` and `fsi-app/.claude/` as fix locations, so it is reported here, not fixed)

The dated historical docs (`docs/ops/`, `docs/audits/`, `docs/archive/`, ADRs) that also mention
`remediation-discipline` were left alone, per the brief and CLAUDE.md's own convention for dated
historical material.
