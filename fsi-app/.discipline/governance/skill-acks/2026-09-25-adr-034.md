## Skill

caros-ledge-platform-intent

## Change

File changed: `fsi-app/.claude/skills/caros-ledge-platform-intent/SKILL.md`.

Per ADR-034 (`docs/decisions/ADR-034-domain-agnostic-core-and-industry-packs.md`, operator approval
landed 2026-09-25), amended the frontmatter `description` line and the "Architectural intent" paragraph
under Platform Value Proposition. Both previously framed expansion as industry-only ("expand into the
broader freight forwarding industry"); both now frame the core as domain-agnostic, profiled by industry,
role, and organisation size, with freight forwarding named explicitly as the first industry pack and a
future public-body "on behalf of many" aggregate mode named as a separately-ruled future direction. This
satisfies the skill's own Authority Grant requirement ,  "explicit operator authorization with
strong-emphasis correction" ,  for editing the platform model framing; ADR-034 records that
authorization. No other section of the skill (the five-surface model, Intelligence Assistant as research
helper, Map as a view of Regulations, Operations as structured content, Community as core) was touched.

## Citing files reviewed

Per skill-contract-map.mjs's own convention (citing files = files matching a `GOVERNING SKILL(S):`
marker for this skill under CITATION_SCAN_ROOTS, `fsi-app/src` and `fsi-app/scripts`):
`git grep -rln "GOVERNING SKILL" -- fsi-app/src fsi-app/scripts | xargs grep -l "caros-ledge-platform-intent"`
from the worktree root names:

`fsi-app/scripts/verify/rls-credential-parity.mjs`, `fsi-app/scripts/verify/routing.mjs`,
`fsi-app/scripts/verify/spec09-org-rls-adversarial-audit.mjs`,
`fsi-app/scripts/verify/surface-visibility-audit.mjs`.

None of these four files changed in this range. All four cite the skill for the five-surface routing
and RLS/visibility contracts, none of which this edit touches ,  the edit is confined to the expansion
framing (description line + Architectural intent paragraph); the five-surface model, its routing, and
its RLS contract are unchanged text.
