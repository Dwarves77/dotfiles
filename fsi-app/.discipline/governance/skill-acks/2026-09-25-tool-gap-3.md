## Skill

remediation-discipline

## Change

`fsi-app/.claude/skills/remediation-discipline/SKILL.md` itself is unchanged. This range ADDS one new
`GOVERNING SKILL:` citation of it:

- `fsi-app/scripts/verify/ui-orphan-audit.mjs` line 2 (sweep-before-claim; the field-grain, UI-facing
  sibling of F14's table-grain producer-consumer orphan check).

A straightforward instance of remediation-discipline's existing "sweep-before-claim" framing already
used by the sibling audits this lane's brief named as the model (`dead-column-audit.mjs`,
`duplicate-table-audit.mjs`, F14 itself). Not a new interpretation of the skill.

`fsi-app/scripts/verify/harness-family-schedule-walker-audit.mjs` and the two new `lib/*-scan.mjs`
pure-core modules cite F14/F50 and rule 16 directly in their header comments, not `GOVERNING SKILL:`
markers, so they are not additional citers under `skill-contract-map.mjs`'s own scan pattern.

## Citing files reviewed

Per skill-contract-map.mjs's convention (citing files = files matching a `GOVERNING SKILL(S):` marker
for this skill under `CITATION_SCAN_ROOTS`, `fsi-app/src` and `fsi-app/scripts`):
`git grep -rln "GOVERNING SKILL" -- fsi-app/src fsi-app/scripts | xargs grep -l "remediation-discipline"`
from the worktree root names 33 citers, 32 pre-existing (unchanged in this range, including the three
TOOL-GAP-2 added in `2026-09-25-tool-gap-2.md`) plus this range's one new one:
`fsi-app/scripts/verify/ui-orphan-audit.mjs`.

The pre-existing citers are unchanged in this range. No conflict, no reinterpretation of the skill's
framing.
