{
  "part": "Map, jurisdiction register (ListRow's register variant)",
  "mount": "listrow-1440",
  "viewport": 1440,
  "artboards": [
    "10-map (\"Jurisdiction register\" card)"
  ],
  "source": "docs/design/handoff-2026-09-06/'Caros Ledge UI System.dc.html' p10 (#p10, Map): the 'Jurisdiction register' card's column header row and its example rows (grid-template-columns:3px 1fr 1fr 110px 80px 40px; min-height:44px); src/components/ui/ListRow.tsx's own `endStat` prop (an additive extension the map lane built onto the shared ListRow, per its header comment: 'ListRow's new optional endStat prop (additive extension, see ListRow.tsx) replaces those four cells with a single band-coloured highest-band + item-count stat, matching the artboard's own HIGHEST BAND / ITEMS columns without forking a second row component'); src/components/map/MapPageView.tsx (real usage: `endStat={{ label: row.band.label, value: formatNumber(row.count), band: row.band }}`)",
  "notes": [
    "This spec extends the SHARED listrow-1440 mount (mounts.mjs) with one additional wrapped instance (`[data-audit=\"row-endstat\"]`) that reproduces MapPageView.tsx's own ListRow call exactly (title=jurisdiction label, meta=activeThemes, endStat={label,value,band}, minHeight=44, no impact/due/timeline/tier/overflow) — the values below are read from the SAME real ListRow.tsx component, not a page-local fork.",
    "FIXED (fix58-lists, 2026-09-07, B130): ListRow.tsx's `minHeight` is now an optional prop (default 56, the general row's own value, unchanged for every other caller) — MapPageView.tsx passes `minHeight={44}` for register rows per p10's own stated value, an additive extension, not a fork.",
    "FIXED (fix58-lists, 2026-09-07, B131-B134): endStat's label span now colours only the leading dot with the band colour; the label TEXT stays `var(--ink)` (#1A1A1A), at `var(--fs-10)` (10px) and letter-spacing 0.06em, matching p10's 'HIGHEST BAND' column exactly. The value numeral is font-size 16 (from 18); its band colour was already correct.",
    "FIXED (lane map60, 2026-09-08): the six-column register grid IS now what ListRow renders, `variant=\"register\"` reproduces dc.html p10's `3px 1fr 1fr 110px 80px 40px` / 44px row / `padding:0 16px 0 0` directly, and the merged `4 / span 4` approximation inside the eight-column list grid (which the removed note above described as a genuine layout-strategy difference from p10) is DELETED, not left beside it. The targets below therefore address the register cells by their own class names."
  ],
  "selector": "[data-audit=\"row-endstat\"] .cl-list-row-register",
  "expect": {
    "min-height": "44px",
    "grid-template-columns": "3px 229.5px 229.5px 110px 80px 40px"
  },
  "children": [
    {
      "name": "register grid, p10's own six columns, not the list row's eight",
      "selector": "[data-audit=\"row-endstat\"] .cl-list-row-register",
      "expect": {
        "grid-template-columns": "3px 229.5px 229.5px 110px 80px 40px",
        "padding": "0px 16px 0px 0px"
      },
      "note": "dc.html p10 states `3px 1fr 1fr 110px 80px 40px`; getComputedStyle reports the RESOLVED tracks, and at this mount's row width the two 1fr tracks are 229.5px each. The three fixed tracks (110/80/40) and the 3px spine are the artboard's literals."
    },
    {
      "name": "HIGHEST BAND cell, label text colour",
      "selector": "[data-audit=\"row-endstat\"] .cl-row-register-band",
      "note": "p10: label text colour #1A1A1A (ink); only the dot is band-coloured.",
      "expect": {
        "color": "#1A1A1A",
        "font-size": "10px",
        "font-weight": "800",
        "letter-spacing": "0.06em"
      }
    },
    {
      "name": "ITEMS cell, value numeral",
      "selector": "[data-audit=\"row-endstat\"] .cl-row-register-count",
      "note": "p10: font-family Anton, font-size 16px, colour = band colour (#DC2626 for Immediate, this fixture's band), right-aligned.",
      "expect": {
        "font-family": "contains:Anton",
        "font-size": "16px",
        "color": "#DC2626",
        "justify-content": "flex-end"
      }
    },
    {
      "name": "trailing arrow cell",
      "selector": "[data-audit=\"row-endstat\"] .cl-row-register-arrow",
      "expect": {
        "color": "#7A6E6C"
      },
      "textMatch": "→",
      "note": "p10: `color:#7A6E6C;text-align:center`, the --ink-3 muted arrow."
    }
  ],
  "forbid": []
}
