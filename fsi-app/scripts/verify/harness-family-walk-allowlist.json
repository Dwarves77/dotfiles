{
  "_comment": "Family allowlist for harness-family-schedule-walker-audit.mjs, keyed by family name. Per rule 16 this audit never recommends turning cadence on, so an entry here is for a family that is intentionally retired or pre-registration (not for 'silence a finding because R14/build-mode holds it', that is the DEFAULT, expected state and is already reported factually, not gated). Empty by design on this first run."
}
