## Change

Migrated from the family's old `PENDING-RUN.md` (lane N3, build plan section 6.8 Rule B; see
`fsi-app/scripts/harness-runs/CONVENTION.md`'s "Declaring a pending run" section). Latest state before
migration (lane L6, D10, 2026-09-13): the section-side extractor (`src/lib/forward-events/extract-forward-events.mjs`,
this family's one code-bearing governing file; `PROTOCOL.md`, the other, is unchanged) gained two new
refusals in `scanText`, a reference-date refusal and a status-only refusal, plus a new `enters-into-force-on`
RULES entry, fixing defect D10 (a fabricated `item_forward_events` row whenever a deontic clause sat
within the CANDIDATE_ONLY_RULES look-ahead of an "In force as of [run date]." sentence; cleaned up by
migration 318). `EXTRACTOR_VERSION` bumped to `fe1-2026-09-13.1`. This family has 35 valid artifacts on
record (`forward-events-run-001` through `-035`); none records the current hash. The marker's own prior
history (the FE-DEDUP fix, `sameObligationContent`'s exact-match short-circuit, and the coordinator's
one-time `forward-events-retext.mjs` maintenance cleanup it named) is not carried forward here, the
pending/ mechanism needs no hash, only this acknowledgment.

## Planned run

The next `forward-events-run-036.json` (or whichever number is next), produced by the coordinator's next
`run-extraction.mjs` dispatch under the current code. Delete this file the moment that artifact lands.
