## Change

Migrated from the family's old `PENDING-RUN.md` (lane N3, build plan section 6.8 Rule B; see
`fsi-app/scripts/harness-runs/CONVENTION.md`'s "Declaring a pending run" section). Latest state before
migration (lane T2, 2026-09-19, one guarded env-file loader): `scripts/turns/apply-record-briefs.mjs`,
one of this family's four governing files, moved its bare `process.loadEnvFile` call onto the one home,
`fsi-app/scripts/lib/env-file.mjs`'s `loadLocalEnvFile()`. No behaviour change for a real run. The other
three governing files (`scripts/turns/record-briefs/schema.mjs`, `src/lib/agent/canonical-pipeline.ts`,
`src/lib/intake/flywheel-steps.mjs`, `scripts/turns/io-preflight.mjs`) were untouched by that lane. No
run of this family has landed since; the marker's own history (six prior hand re-pins across
2026-09-17 through 2026-09-19) is not carried forward, the pending/ mechanism needs no hash, only this
acknowledgment.

## Planned run

The next `.github/workflows/brief-apply.yml` dispatch (dry or apply), landing the next
`brief-apply-run-NNN.json` (the last artifact on record is `brief-apply-run-006`). Delete this file the
moment that artifact lands.
