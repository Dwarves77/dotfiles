## Change

Lane N5 (2026-09-19, plan 6.8 Rule A: the invariant registry, the migrations inventory and the shared-writer allowlist become derived) added a `// SHARED-WRITER: <table>[, <table>...]` header line to every real shared-table writer named in `fsi-app/docs/inventories/shared-dataset-ownership.md`'s former json allowlist, so that the allowlist can be derived from those markers instead of hand-copied. `src/lib/agent/canonical-pipeline.ts` gained `// SHARED-WRITER: intelligence_items, item_cross_references, integrity_flags, section_claim_provenance, agent_run_searches, intelligence_item_sections, item_gate_a_state`; `src/lib/intake/flywheel-steps.mjs` gained `// SHARED-WRITER: item_forward_events`; `scripts/turns/io-preflight.mjs` gained `// SHARED-WRITER: brief_apply_runs`. No behaviour change (comment-only header lines). No run of this family has landed since.

## Planned run

The next `brief-apply-run-007.json` for this family. Delete this file the moment that artifact lands.
