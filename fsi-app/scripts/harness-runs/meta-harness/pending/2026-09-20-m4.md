## Change

Lane M4, 2026-09-20, build plan section 6.1 row M4, Amendment 2 (coordinator, after lane M9d's two gate
failures today): registered the new `brief-export` harness family by descriptor only
(`fsi-app/scripts/harness-runs/brief-export/family.json`, `FAMILY.md`), per build plan section 6.8 Rule A.
`governing-files.mjs` (a meta-harness governing file) derives `GOVERNING_FILES` from `FAMILIES`
(`family-registry.mjs`, which reads every `family.json` under `scripts/harness-runs/`), so adding a new
family directory changes what that derived module reads off disk even though the module's own source text
is untouched, the same self-application mechanism lane M3's own 2026-09-19 pending file (this file's own
predecessor for the `downstream-chain` registration) already recorded.

Also in this same range: `scripts/lib/run-artifact.mjs` (a meta-harness governing file, listed directly)
gains `buildRunArtifactEnvelope`, extracted from `emit-brief-export-artifact.mjs`'s own `buildArtifact`
after the F45 duplicate-code gate refused the first pass (a third `emit-*-artifact.mjs` writer repeating
the same 11-field CONVENTION.md top-level artifact shape verbatim). No existing family's own
`governing_files` list or written artifact shape changed; confirmed by the green `run-artifact.test.mjs`,
`family-registry.test.mjs` and `governing-files.test.mjs` runs in this same range, and a second, clean
fitness-runner pass (0 violations, 45/45 functions).

## Planned run

The next `meta-harness-run-NNN.json`, the coordinator's next self-application review pass over this wave
(the last artifact on record is `meta-harness-run-008`). Delete this file the moment that artifact lands.
