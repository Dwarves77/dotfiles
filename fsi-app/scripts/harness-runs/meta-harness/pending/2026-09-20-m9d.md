## Change

Lane M9d, 2026-09-20, build plan section 6.1 row M9: registered the new `producers` harness family by
descriptor only (`fsi-app/scripts/harness-runs/producers/family.json`, `FAMILY.md`), per build plan
section 6.8 Rule A. `governing-files.mjs` (one of meta-harness's own governing files) derives
`GOVERNING_FILES` from `FAMILIES` (`family-registry.mjs`, which reads every `family.json` under
`scripts/harness-runs/`), so adding a new family directory changes what that derived module reads off
disk even though the module's own source text is untouched, the same self-application mechanism
`meta-harness`'s own `family.json` describes ("the loop applies to itself") and the same pattern lane M3's
own `downstream-chain` registration recorded here on 2026-09-19 (`pending/2026-09-19-m3.md`). No
behaviour change to any existing family's own `governing_files`/`harness_version`; confirmed by the green
`family-registry.test.mjs` and `run-artifact.test.mjs` runs in this same range.

## Planned run

The next `meta-harness-run-NNN.json`, the coordinator's next self-application review pass over this wave
(the last artifact on record is `meta-harness-run-008`, per lane M3's own pending file). Delete this file
the moment that artifact lands.
