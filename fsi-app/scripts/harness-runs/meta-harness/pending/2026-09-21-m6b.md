## Change

Lane M6b, 2026-09-21, build plan section 6.1 row M6: registered the new `gate-a-rescan` harness family
by descriptor only (`fsi-app/scripts/harness-runs/gate-a-rescan/family.json`, `FAMILY.md`), per build
plan section 6.8 Rule A. `governing-files.mjs` (a meta-harness governing file) derives `GOVERNING_FILES`
from `FAMILIES` (`family-registry.mjs`, which reads every `family.json` under `scripts/harness-runs/`),
so adding a new family directory changes what that derived module reads off disk even though the
module's own source text is untouched -- the same self-application mechanism every prior family
registration's own pending file already recorded (lane M3's `downstream-chain`, lane M4's
`brief-export`). No existing family's own `governing_files` list or written artifact shape changed.

## Planned run

The next `meta-harness-run-NNN.json`, the coordinator's next self-application review pass over this wave
(the last artifact on record before this lane is `meta-harness-run-008`). Delete this file the moment
that artifact lands.
