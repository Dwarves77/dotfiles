## Change

Migrated from the family's old `PENDING-RUN.md` (lane N3, build plan section 6.8 Rule B; see
`fsi-app/scripts/harness-runs/CONVENTION.md`'s "Declaring a pending run" section). Latest state before
migration (lane N2, 2026-09-19, Amendment 2): `scripts/lib/run-artifact.mjs` (one of this family's own
governing files) gained the new export `isRunArtifactFilename`, and `F28-harness-run-integrity.mjs`
(a second governing file) had its `scanArtifacts` filter changed to use it. This lane (N3) itself edits
two more of this family's own governing files, `F28-harness-run-integrity.mjs` (Rule B rewrite of rules
(b)/(c)) and `CONVENTION.md` (the marker section, this file's own migration), so this pending file also
covers that further drift; nothing further needs re-pinning under this convention. The prior hash-pin
history (nine re-pins across 2026-09-01 through 2026-09-19, tracing every self-referential edit to
`governing-files.mjs`, `run-artifact.mjs`, `CONVENTION.md`, and this file) is not carried forward, the
pending/ mechanism needs no hash, only this acknowledgment.

## Planned run

The next `meta-harness-run-NNN.json`, the coordinator's next self-application review pass over this wave
(the last artifact on record is `meta-harness-run-008`). Delete this file the moment that artifact lands.
