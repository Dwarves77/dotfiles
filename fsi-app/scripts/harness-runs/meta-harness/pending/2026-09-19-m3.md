## Change

Lane M3, 2026-09-19, build plan section 6.1 row M3: registered the new `downstream-chain` harness family
by descriptor only (`fsi-app/scripts/harness-runs/downstream-chain/family.json`, `FAMILY.md`), per build
plan section 6.8 Rule A. `governing-files.mjs` (one of meta-harness's own governing files) derives
`GOVERNING_FILES` from `FAMILIES` (`family-registry.mjs`, which reads every `family.json` under
`scripts/harness-runs/`), so adding a new family directory changes what that derived module reads off
disk even though the module's own source text is untouched, the literal self-application mechanism
`meta-harness`'s own `family.json` describes ("the loop applies to itself"). No behaviour change to any
existing family's own `governing_files`/`harness_version`; confirmed by the green
`family-registry.test.mjs` and `governing-files.test.mjs` runs in this same range.

## Planned run

The next `meta-harness-run-NNN.json`, the coordinator's next self-application review pass over this wave
(the last artifact on record is `meta-harness-run-008`). Delete this file the moment that artifact lands.
