## Change

Migrated from the family's old `PENDING-RUN.md` (lane N3, build plan section 6.8 Rule B; see
`fsi-app/scripts/harness-runs/CONVENTION.md`'s "Declaring a pending run" section). Latest state before
migration (lane T2, 2026-09-19, one guarded env-file loader): `scripts/turns/run-change-detection.mjs`,
one of this family's three governing files, moved its bare `process.loadEnvFile` call onto the one home,
`fsi-app/scripts/lib/env-file.mjs`'s `loadLocalEnvFile()`. No behaviour change for a real run. The other
two governing files (`src/lib/sources/reconcile.ts`, `src/lib/intake/run-intake-cycle.ts`) were untouched
by that lane. No run of this family has landed since; the marker's own history (four prior hand re-pins
across 2026-09-04 through 2026-09-19) is not carried forward, the pending/ mechanism needs no hash,
only this acknowledgment.

## Planned run

The next live `change-detection` dispatch (the last artifact on record is `change-detection-run-005`),
landing the next `change-detection-run-NNN.json`. Delete this file the moment that artifact lands.
