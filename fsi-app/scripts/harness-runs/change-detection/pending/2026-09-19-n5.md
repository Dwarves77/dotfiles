## Change

Lane N5 (2026-09-19, plan 6.8 Rule A: the invariant registry, the migrations inventory and the shared-writer allowlist become derived) added a `// SHARED-WRITER: <table>[, <table>...]` header line to every real shared-table writer named in `fsi-app/docs/inventories/shared-dataset-ownership.md`'s former json allowlist, so that the allowlist can be derived from those markers instead of hand-copied. `src/lib/sources/reconcile.ts` gained `// SHARED-WRITER: monitoring_queue, intelligence_changes`; `src/lib/intake/run-intake-cycle.ts` gained `// SHARED-WRITER: agent_run_searches, staged_updates`. No behaviour change (comment-only header lines). No run of this family has landed since.

## Planned run

The next `change-detection-run-006.json` for this family. Delete this file the moment that artifact lands.
