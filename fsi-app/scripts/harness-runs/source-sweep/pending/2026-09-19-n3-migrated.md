## Change

Migrated from the family's old `PENDING-RUN.md` (lane N3, build plan section 6.8 Rule B; see
`fsi-app/scripts/harness-runs/CONVENTION.md`'s "Declaring a pending run" section). Latest state before
migration (lane T2, 2026-09-19, one guarded env-file loader): `scripts/turns/run-source-sweep.mjs`, one
of this family's three governing files, moved its bare `process.loadEnvFile` call onto the one home,
`fsi-app/scripts/lib/env-file.mjs`'s `loadLocalEnvFile()`. No behaviour change for a real run. The other
two governing files (`src/lib/sources/register-walk.mjs`, `src/lib/sources/feed-walk.mjs`) were untouched
by that lane. No run of this family has landed since (the last artifact on record is
`source-sweep-run-018`); the marker's own history (the IN-CHUNK id-list class fix, the F46
external-host-home consolidation for EUR-Lex/Federal Register/eCFR, and lane M8's `--slice`/`--after`
sizing-and-cursor mechanism for `--all-hosts`) is not carried forward, the pending/ mechanism needs no
hash, only this acknowledgment.

## Planned run

The next real `node scripts/turns/run-source-sweep.mjs` dispatch (dry or apply), landing
`source-sweep-run-019.json`. Delete this file the moment that artifact lands.
