## Change

`scripts/turns/run-ledger-consume.mjs` (a governing file of this family) now records `config.loop_run_id`
(lane M3, 2026-09-19, build plan section 6.1 row M3, Amendment 1 item B): resolved via the new
`scripts/lib/loop-run-id.mjs`'s `resolveLoopRunId`, matching this family's own artifacts against
`source-sweep`'s `config.loop_run_id` by the upstream run id `GITHUB_EVENT_WORKFLOW_RUN_ID` already
carries (`config.upstream_run_id`'s own existing field, lane M2). Amendment B's own premise named
"fetch-drain" as this family's upstream; `[REFUTED]`, confirmed by reading `ledger-consume.yml`'s own
`on.workflow_run.workflows: ["Source sweep"]` and `loop-manifest.mjs`'s `sweep-to-ledger-consume` hop
(producer: Source sweep; no `fetch-drain-to-ledger-consume` hop exists anywhere) -- `source-sweep` is the
correct upstream family, used here. No behaviour change to anything else this runner reads or writes;
every existing test (`run-ledger-consume.test.mjs`) is unchanged and green.

## Planned run

The coordinator's proof run (build plan section 6.2): one dispatch at the head (`source-sweep.yml`)
chaining into `ledger-consume.yml` via `workflow_run`, landing a `ledger-consume-run-NNN.json` with
`config.loop_run_id` matching the sweep's own. Delete this file once that artifact lands.
