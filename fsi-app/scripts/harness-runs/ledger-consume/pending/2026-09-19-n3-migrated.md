## Change

Migrated from the family's old `PENDING-RUN.md` (lane N3, build plan section 6.8 Rule B; see
`fsi-app/scripts/harness-runs/CONVENTION.md`'s "Declaring a pending run" section). Latest state before
migration (lane T2, 2026-09-19, one guarded env-file loader): `scripts/turns/run-ledger-consume.mjs`, one
of this family's three governing files, moved its bare `process.loadEnvFile` call onto the one home,
`fsi-app/scripts/lib/env-file.mjs`'s `loadLocalEnvFile()`. No behaviour change for a real run. The other
two governing files (`src/lib/intake/portal-harvest.ts`, `src/lib/llm/first-fetch-classify.ts`) were
untouched by that lane. No run of this family has landed since (the last artifact on record is
`ledger-consume-run-007`); the marker's own history (three prior hand re-pins across 2026-09-11 through
2026-09-19, including the census-exclusion RPC fix in `portal-harvest.ts`) is not carried forward, the
pending/ mechanism needs no hash, only this acknowledgment.

## Planned run

The next `ledger-consume-run-NNN.json`, the next ledger-consume dispatch after this lane merges. Delete
this file the moment that artifact lands.
