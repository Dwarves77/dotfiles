## Change

Lane M2 (build plan section 6.1, 2026-09-18; rebased onto master `ddcd9a63` by the coordinator on
2026-09-19, after plan section 6.8 closed). Two of this family's three governing files changed:
`scripts/turns/run-ledger-consume.mjs` (the chained apply arms itself on the committed verdict batches
under `scripts/turns/ledger-verdicts/`, bounded by `--max-promote`, default 50, ceiling 200 refused; the
`LEDGER_CONSUME_APPLY_ENABLED` source constant is deleted; `metrics.verdicts_owed` is reported) and
`src/lib/intake/portal-harvest.ts` (the promote cap, through `src/lib/intake/promote-cap.mjs`).
`src/lib/llm/first-fetch-classify.ts` is untouched. The lane's own artifacts
(`ledger-consume-run-008` to `ledger-consume-run-010`) were written on the lane's pre-rebase tree, before
lane T2 moved `run-ledger-consume.mjs` onto the one env-file loader, so none of them is at the live hash
of the combined tree.

## Planned run

The coordinator's bounded hop proof for this family: one ledger-consume apply, cap 50, dispatched from the
operator's machine after this lane merges (handoff 2026-09-18 section 3 step 4). Its
`ledger-consume-run-NNN.json` lands at the live hash. Delete this file, and
`2026-09-19-n3-migrated.md` beside it, the moment that artifact lands.
