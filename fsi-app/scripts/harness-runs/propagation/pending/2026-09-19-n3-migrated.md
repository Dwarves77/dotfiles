## Change

Migrated from the family's old `PENDING-RUN.md` (lane N3, build plan section 6.8 Rule B; see
`fsi-app/scripts/harness-runs/CONVENTION.md`'s "Declaring a pending run" section). What changed: lane T2
(2026-09-19, one guarded env-file loader) changed `scripts/turns/run-propagation-drain.mjs`, one of the
three `propagation` governing files: its own bare `process.loadEnvFile` call moved onto the one home,
`fsi-app/scripts/lib/env-file.mjs`'s `loadLocalEnvFile()`. No behaviour change for a real run. The other
two governing files (`src/lib/propagation/drain.ts`, `src/lib/propagation/admissible-for.ts`) are
untouched. No run of this family has landed since (the last artifact on record is `propagation-run-008`)
, the pending/ mechanism needs no hash, only this acknowledgment.

## Planned run

The next `node scripts/turns/run-propagation-drain.mjs` dispatch (dry or apply), landing
`propagation-run-009.json`. Delete this file the moment that artifact lands.
