## Change

Lane M3b (2026-09-20, build plan section 6.1 row M3b) changed `scripts/turns/run-propagation-drain.mjs`,
one of the three `propagation` governing files: `config.loop_run_id` is now resolved from
`triggerContext?.name` and `triggerContext?.run_id` through `resolveLoopRunIdFromUpstream`
(`scripts/lib/loop-run-id.mjs`), so an artifact this family writes carries the loop id it inherited from
whichever upstream ("Downstream chain" or "Data producers") triggered the drain. Null when there is no
trigger context (a plain hand dispatch), same as before. No other behaviour change: mode, batch and
trigger_context recording are untouched. The other two governing files (`src/lib/propagation/drain.ts`,
`src/lib/propagation/admissible-for.ts`) are untouched. Lane M3's own pending file for this family
(`2026-09-19-n3-migrated.md`) is left as is; this is a separate, later change.

## Planned run

The next `node scripts/turns/run-propagation-drain.mjs` dispatch (dry or apply), landing
`propagation-run-009.json` (or the next free number), which will carry the new `config.loop_run_id`
field. Delete this file the moment that artifact lands.
