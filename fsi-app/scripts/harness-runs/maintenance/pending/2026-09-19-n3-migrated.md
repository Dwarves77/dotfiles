## Change

Migrated from the family's old `PENDING-RUN.md` (lane N3, build plan section 6.8 Rule B; see
`fsi-app/scripts/harness-runs/CONVENTION.md`'s "Declaring a pending run" section). This family was
registered by lane M9b (2026-09-18, closing stage-audit-2026-09-18's finding that `maintenance.yml`
uploaded only an ephemeral artifact) with zero valid artifacts from the start, no live dispatch was
possible from the authoring environment. Latest state before migration (lane T2, 2026-09-19, one guarded
env-file loader): `scripts/maintenance/lib/cli.mjs`, one of this family's two governing files, moved its
bare `process.loadEnvFile` call onto the one home, `fsi-app/scripts/lib/env-file.mjs`'s
`loadLocalEnvFile()`. No behaviour change for a real run. The other governing file
(`../.github/workflows/maintenance.yml`) is untouched. This family still has zero valid run artifacts.

## Planned run

The coordinator's next `.github/workflows/maintenance.yml` dispatch (any step, dry or apply), landing
`maintenance-run-001.json`. Delete this file the moment that artifact lands.
