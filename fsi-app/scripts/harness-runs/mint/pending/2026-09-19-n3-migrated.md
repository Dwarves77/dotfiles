## Change

Migrated from the family's old `PENDING-RUN.md` (lane N3, build plan section 6.8 Rule B; see
`fsi-app/scripts/harness-runs/CONVENTION.md`'s "Declaring a pending run" section). Latest state before
migration (lane L38, 2026-09-19, at push after rebase): `scripts/mint/validate-mint-payload.mjs`, one of
this family's eight governing files, gained admitted-mirror disposition comments only (per
`docs/audits/system-health-audit-2026-09-17.md`'s "Admitted mirrors, dispositions" table); no validation
logic changed. The previous mint marker was discharged by `mint-run-029`; the prior hand re-pin history
since (the Gate-A shim removal, the mint-flywheel documentation/outcome-metric edits, the DATECHAIN
`STUB_BRIEF_MARKER` extraction, the hashsep CLI-guard fix, and task 3.5's brief-queuing documentation) is
not carried forward, the pending/ mechanism needs no hash, only this acknowledgment.

## Planned run

The next `population-turn` dispatch under this landed code, recording the next `mint-run-NNN.json`. No
mint-kit VALIDATION behavior changed by the edit above, so no behavioral defect is expected, only the
marker's discharge. Delete this file the moment that artifact lands.
