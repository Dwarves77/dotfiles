## Change

Lane N5 (2026-09-19, plan 6.8 Rule A: the invariant registry, the migrations inventory and the shared-writer allowlist become derived) added a `// SHARED-WRITER: <table>[, <table>...]` header line to every real shared-table writer named in `fsi-app/docs/inventories/shared-dataset-ownership.md`'s former json allowlist, so that the allowlist can be derived from those markers instead of hand-copied. `src/lib/sources/seek-more.mjs` gained `// SHARED-WRITER: integrity_flags`. No behaviour change (comment-only header lines). This is the family's first-ever pending file and it still has zero run artifacts (lane M8 registered the family 2026-09-18 with no run landed since); this lane's own edit is a second, independent reason a run is owed, not the original one, since the governing-file hash this lane leaves behind differs from the one at registration time either way.

## Planned run

The next `inaccessible-triage-run-001.json` for this family. Delete this file the moment that artifact lands.
