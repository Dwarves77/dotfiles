## Change

Migrated from the family's old `PENDING-RUN.md` (lane N3, build plan section 6.8 Rule B; see
`fsi-app/scripts/harness-runs/CONVENTION.md`'s "Declaring a pending run" section). This family was
registered by lane M8 (2026-09-18, closing the 2026-09-18 stage audit's S1 finding) with zero valid
artifacts from the start, no live dispatch was possible from the authoring environment (no Supabase
credentials, no Actions runner). Latest state before migration (lane T2, 2026-09-19, one guarded
env-file loader): `scripts/sources/inaccessible-triage.mjs`, the driver of this family's five governing
files, moved its bare `process.loadEnvFile` call onto the one home,
`fsi-app/scripts/lib/env-file.mjs`'s `loadLocalEnvFile()`. No behaviour change for a real run. The other
four governing files (`src/lib/sources/primary-fallback.mjs`, `seek-more.mjs`, `officialness.mjs`,
`host-authority.ts`) are untouched. This family still has zero valid run artifacts.

## Planned run

The next real `node scripts/sources/inaccessible-triage.mjs` dispatch (dry or apply), landing
`inaccessible-triage-run-001.json`. Delete this file the moment that artifact lands.
