## Change

Migrated from the family's old `PENDING-RUN.md` (lane N3, build plan section 6.8 Rule B; see
`fsi-app/scripts/harness-runs/CONVENTION.md`'s "Declaring a pending run" section). Latest state before
migration (lane T2, 2026-09-19, one guarded env-file loader): both of this family's governing files,
`scripts/turns/consume-turn-requests.mjs` and `scripts/turns/export-corpus-for-extraction.mjs`, moved
their bare `process.loadEnvFile` calls onto the one home, `fsi-app/scripts/lib/env-file.mjs`'s
`loadLocalEnvFile()`. No behaviour change for a real run. No run of this family has landed since; the
marker's own history (four prior hand re-pins across 2026-09-06 through 2026-09-19, including the
IN-CHUNK id-list class fix and the corpus-export `forward_events`/`timelines` and `--ids` provenance-scope
additions) is not carried forward, the pending/ mechanism needs no hash, only this acknowledgment.

## Planned run

The next `.github/workflows/corpus-turn.yml` dispatch, landing the next `corpus-turn-run-NNN.json` (the
last artifact on record is `corpus-turn-run-002`). Delete this file the moment that artifact lands.
