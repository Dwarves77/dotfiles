## Change

Registered the `brief-export` harness family by descriptor (`family.json`, `FAMILY.md`), closing the
loop-manifest's `population-turn-to-brief-export` hop (build plan section 6.1 row M4, Amendment 1 section
C), previously filed under the `brief-apply` family "for now". `.github/workflows/brief-export.yml` gains
a `workflow_run` trigger on "Population turn" completing and a final step that builds and commits this
family's own `brief-export-run-NNN.json` artifact (config: mode, selection, limit, upstream_name,
upstream_run_id, loop_run_id, batch_path, branch; per_item: one row per exported id) via the new
`scripts/turns/emit-brief-export-artifact.mjs`. Previously this workflow's only record of a run was a
GitHub Actions ephemeral `upload-artifact` and a PR, with nothing under `scripts/harness-runs/`.

## Planned run

The coordinator's proof run (build plan section 6.2): one dispatch at the head (`source-sweep.yml`)
chaining through to a `population-turn.yml` apply that mints something real, firing `brief-export.yml` via
`workflow_run` and landing `brief-export-run-001.json` with `trigger: "workflow_run"` and a non-null
`loop_run_id` resolved from the mint run's own artifact. Delete this file the moment that artifact lands.
