## Change

Registered the `downstream-chain` harness family by descriptor (`family.json`, `FAMILY.md`), closing the
loop-manifest's `population-turn-to-downstream-chain` and `corpus-turn-to-downstream-chain`
`familyPending: true` hops (build plan section 6.1 row M3). `.github/workflows/downstream-chain.yml`
itself is unchanged in its trigger/gate logic by this same-day range except for the addition of a final
step that builds and commits this family's own `downstream-chain-run-NNN.json` artifact (config: the
upstream workflow name and run id, the four steps run; outcome: per-step counts read from each step's own
`summary.json`) -- previously this workflow's only record of a run was a GitHub Actions ephemeral
`upload-artifact`, the `maintenance` family's own posture, per this workflow's own header.

## Planned run

The coordinator's proof run (build plan section 6.2): one dispatch at the head (`source-sweep.yml`)
chaining through to a `population-turn.yml` or `corpus-turn.yml` apply that mints or turns something real,
firing `downstream-chain.yml` via `workflow_run` and landing `downstream-chain-run-001.json` with
`trigger: "workflow_run"`. Delete this file the moment that artifact lands.
