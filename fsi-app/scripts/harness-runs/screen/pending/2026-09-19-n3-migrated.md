## Change

Migrated from the family's old `PENDING-RUN.md` (lane N3, build plan section 6.8 Rule B; see
`fsi-app/scripts/harness-runs/CONVENTION.md`'s "Declaring a pending run" section). Latest state before
migration (task 0.3b, 2026-09-11, lane hashsep): `scripts/mint/screen-worklist.mjs`, one of this family's
two governing files, had its CLI main guard swapped onto `isMainModule(import.meta.url)`, a mechanical,
behavior-preserving edit; `screen-rules.mjs` (the other governing file) is untouched, and no
classification RULE changed since the 2026-09-06 R-B ruling (`eu_weekly_oil_bulletin_price_series`).
`screen-run-003.json`'s own record (empty `defects_found`, 23 `mechanism_question_flags` open) still
holds exactly as recorded; a future proposer pass over this family should read it in full before round 4.
No round 4 batch is scheduled by this wave. The marker's prior hand re-pin history (Wave MH-2's own
artifact-emission wiring, the 2026-09-02 and 2026-09-06 rule re-stamps, the GOV-SINGLE governing-files.mjs
refactor) is not carried forward, the pending/ mechanism needs no hash, only this acknowledgment.

## Planned run

The next real screen batch (round 4, whenever the census worklist next needs a re-screen). Because
emission is wired into the harness itself, that run's own `screen-run-004.json` is written automatically
by `screen-worklist.mjs --input ... [--reviewed ...]`. Delete this file the moment that artifact lands.
