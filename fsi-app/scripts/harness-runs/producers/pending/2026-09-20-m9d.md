## Change

Registered the `producers` harness family by descriptor (`family.json`, `FAMILY.md`), closing the
build plan section 6.1 row M9 gap lane M5's own report named: the market and regional producers are the
loop's data-producers box and were the only loop box with no harness-run family. Added
`scripts/producers/lib/producer-summary.mjs` (the per-producer summary writer every `--apply`-invoked
producer script now calls on its own exit path, no-op unless `PRODUCER_SUMMARY_DIR` is set) and
`scripts/producers/emit-producers-artifact.mjs` (the one emitter that folds a firing's summaries into one
`producers-run-NNN.json`). `.github/workflows/producers.yml` gains `PRODUCER_SUMMARY_DIR` at job level and
a final `if: always()` step running the emitter, then the commit-and-deliver step copied from
`downstream-chain.yml` (same `deliver-artifact-branch.sh` pattern every other harness-run family in this
repo uses) -- no other change to the workflow's trigger, gate logic, or step list.

Wired: `ecb-fx-producer.mjs`, `eia-v2-petroleum-spot-producer.mjs`, `eu-weekly-oil-bulletin.mjs` (wrap the
existing `assertEdgesAuthored` call site, recording `status: "failed"` with the throw's own message before
the non-zero exit; `assertEdgesAuthored` itself is unchanged), the three regional producers via their
`runEnvelopeProducer` return value, `refresh-published-price-statistics.mjs`, `fetch-desnz-factors.mjs`,
`emission-factors-desnz.mjs`, `emission-factors-epa.mjs`, and `ratify-series-items.mjs`. A gate test
(`scripts/producers/lib/producer-summary-wiring.test.mjs`) parses `producers.yml`, collects every
`node scripts/....mjs` invocation run with `--apply` under `scripts/producers/` or `scripts/gen/`, and
proves each one imports `producer-summary.mjs`; attack-tested against a fixture yml naming a script that
does not.

## Planned run

The coordinator's own bounded `ecb-fx` dispatch (brief-m9d Amendment 1 section D: "you run NO producer, in
any mode: the first real artifact is the coordinator's bounded ecb-fx dispatch"), landing
`producers-run-001.json` with `trigger: "workflow_dispatch"`. Delete this file the moment that artifact
lands.
