# Supabase (optional — app falls back to static seed data if not set)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Anthropic (for weekly worker + skill generator + briefing generation)
ANTHROPIC_API_KEY=

# API key for cron/worker triggers (set to a secure random string)
FSI_API_KEY=

# Slack webhook URL for worker notifications (optional)
SLACK_WEBHOOK_URL=
