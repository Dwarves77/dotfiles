# Supabase (optional — app falls back to static seed data if not set)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Worker API key (shared secret for POST /api/worker/run)
WORKER_API_KEY=

# Anthropic (for Q&A and worker)
ANTHROPIC_API_KEY=your-anthropic-api-key
